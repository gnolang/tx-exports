// probe — a one-line realm, deployed only to test whether new packages are
// enabled automatically while pixelgnomes stays parked.
package probe

func Render(path string) string { return "probe\n" }
