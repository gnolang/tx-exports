{"tx":{"msg":[{"@type":"/bank.MsgSend","from_address":"g18qhq2fl54lszhmxeyqlvxnwjzc3xpu4nnakclp","to_address":"g1mm2j64gq3mztdqcg4f3y595npys0fq7fkl9vsy","amount":"300000000ugnot"}],"fee":{"gas_wanted":"5000000","gas_fee":"1000000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"AhUuKSwiL4XKy0BM9cK0UJFToerTzvgLTHFBvYk/xOtY"},"signature":"yYHMdiBrPmwzxW0TiCrxU37X2HJrKrS8AC4+SiJ1JodS8muQEhA++dyvvmG4GWr/JFrq9Zi/yf4SSnPIluLTyQ=="}],"memo":""},"metadata":{"timestamp":"1788741986"}}
{"tx":{"msg":[{"@type":"/bank.MsgSend","from_address":"g18qhq2fl54lszhmxeyqlvxnwjzc3xpu4nnakclp","to_address":"g1wa7nwsdex6l3ysvng84djv24395n8l4pqq5r64","amount":"300000000ugnot"}],"fee":{"gas_wanted":"5000000","gas_fee":"1000000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"AhUuKSwiL4XKy0BM9cK0UJFToerTzvgLTHFBvYk/xOtY"},"signature":"6usQnd0b8+MKZdyAvo4omj5RStcXkypEcIwFmlTv19lS469A2BLs/JgeqfrMCie9ce4rCYhSmXq2jkbX52zM5g=="}],"memo":""},"metadata":{"timestamp":"1788742116"}}
{"tx":{"msg":[{"@type":"/bank.MsgSend","from_address":"g18qhq2fl54lszhmxeyqlvxnwjzc3xpu4nnakclp","to_address":"g1q4ty86gf4vcvcfxz5drhqxxe5kqkc0wcu3pvps","amount":"300000000ugnot"}],"fee":{"gas_wanted":"5000000","gas_fee":"1000000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"AhUuKSwiL4XKy0BM9cK0UJFToerTzvgLTHFBvYk/xOtY"},"signature":"TKFg7/CJGJsWn3f9v8m3sZGLGZOecU7VmztGYgng1ogmCuKG+RW25J/Mm7AmHh18jvzHQoYYW9u+PMId5D+suA=="}],"memo":""},"metadata":{"timestamp":"1788742308"}}
{"tx":{"msg":[{"@type":"/bank.MsgSend","from_address":"g18qhq2fl54lszhmxeyqlvxnwjzc3xpu4nnakclp","to_address":"g1jygvzdqgkgjqzvm4389qdp3et4el3plpalreet","amount":"300000000ugnot"}],"fee":{"gas_wanted":"5000000","gas_fee":"1000000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"AhUuKSwiL4XKy0BM9cK0UJFToerTzvgLTHFBvYk/xOtY"},"signature":"zB0v56VILKOQWnrGn46lgwZCQM2rcNFwF0fr1Ndm+VlxgFM/r/EANn5VEKcfhhmw4+tFHYL0tLBMXZT5+Uxdjg=="}],"memo":""},"metadata":{"timestamp":"1788742344"}}
{"tx":{"msg":[{"@type":"/bank.MsgSend","from_address":"g18qhq2fl54lszhmxeyqlvxnwjzc3xpu4nnakclp","to_address":"g1s8n84l34pwgj24yhp2mlfdtz4ava793wnutm8l","amount":"300000000ugnot"}],"fee":{"gas_wanted":"5000000","gas_fee":"1000000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"AhUuKSwiL4XKy0BM9cK0UJFToerTzvgLTHFBvYk/xOtY"},"signature":"tjpf7RrlTJ7DJGoj6CJwfCu21SpRlepH1SWDg04oyoYrDN9FYdo7cvvFesyG1Dmj1mZnqvVGiUlzx6W5yiBwpw=="}],"memo":""},"metadata":{"timestamp":"1788742440"}}
{"tx":{"msg":[{"@type":"/bank.MsgSend","from_address":"g18qhq2fl54lszhmxeyqlvxnwjzc3xpu4nnakclp","to_address":"g1xd8lgtur2cj7s8639fa7tqxv0pm9mymjvty2ee","amount":"300000000ugnot"}],"fee":{"gas_wanted":"5000000","gas_fee":"1000000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"AhUuKSwiL4XKy0BM9cK0UJFToerTzvgLTHFBvYk/xOtY"},"signature":"tHkPQahIk6AgUmKhi6Ytl4HTFi4qQJsbVk5UMMSS2WZ4AQjjsvSCMg21ubMkvk626nPgsitSJrOy2VIch8MabA=="}],"memo":""},"metadata":{"timestamp":"1788742647"}}
{"tx":{"msg":[{"@type":"/bank.MsgSend","from_address":"g18qhq2fl54lszhmxeyqlvxnwjzc3xpu4nnakclp","to_address":"g1y3nkxe27svh9l7v69mxjk3rtlnr4fu0z4a4qfx","amount":"300000000ugnot"}],"fee":{"gas_wanted":"5000000","gas_fee":"1000000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"AhUuKSwiL4XKy0BM9cK0UJFToerTzvgLTHFBvYk/xOtY"},"signature":"7dlXYSsu10XMa227THvVxJ+URpa8evwvsFOX+XwHTU9W9DyI1/neix7JwNFhs6FdIwSb16TXtMNd64UcNi6wTw=="}],"memo":""},"metadata":{"timestamp":"1788742661"}}
{"tx":{"msg":[{"@type":"/bank.MsgSend","from_address":"g18qhq2fl54lszhmxeyqlvxnwjzc3xpu4nnakclp","to_address":"g1c66en7u9j8xp3wvltzzu4d9tf9u837hycf0edg","amount":"300000000ugnot"}],"fee":{"gas_wanted":"5000000","gas_fee":"1000000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"AhUuKSwiL4XKy0BM9cK0UJFToerTzvgLTHFBvYk/xOtY"},"signature":"bXWTPSUXjz4Q8oq2KhkBRBSssLMExRGwsEdFKEN9Mm4AjmUNzUoKFbj3Vrc1Hwm85Wu2+f4B7pN1Z1QmMNf5yg=="}],"memo":""},"metadata":{"timestamp":"1788742781"}}
{"tx":{"msg":[{"@type":"/bank.MsgSend","from_address":"g18qhq2fl54lszhmxeyqlvxnwjzc3xpu4nnakclp","to_address":"g10dffknj9348nxcadev73kcfeduupsuhpmvlvmv","amount":"300000000ugnot"}],"fee":{"gas_wanted":"5000000","gas_fee":"1000000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"AhUuKSwiL4XKy0BM9cK0UJFToerTzvgLTHFBvYk/xOtY"},"signature":"3MlXghkZ4lPfwI6zO/WRoLXqWKHZOYtjjT7Xv1tYwwYy71e5mIUIsfrpYDKgz5rhnfK4qHMv8piaGzlgj12G9Q=="}],"memo":""},"metadata":{"timestamp":"1788742795"}}
{"tx":{"msg":[{"@type":"/bank.MsgSend","from_address":"g18qhq2fl54lszhmxeyqlvxnwjzc3xpu4nnakclp","to_address":"g12a9wa2mm4jucgls9pg0erg8pdd8gznn6yj6ea2","amount":"300000000ugnot"}],"fee":{"gas_wanted":"5000000","gas_fee":"1000000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"AhUuKSwiL4XKy0BM9cK0UJFToerTzvgLTHFBvYk/xOtY"},"signature":"czbVeMWqSiSOj2EYx1kEpk5Hu3/M9H+2d/cvW8Lm2tcpY80ikvVhyRoDgEqiQobAcTJIZRkREtnQozvifGa9bw=="}],"memo":""},"metadata":{"timestamp":"1788742806"}}
{"tx":{"msg":[{"@type":"/bank.MsgSend","from_address":"g18qhq2fl54lszhmxeyqlvxnwjzc3xpu4nnakclp","to_address":"g1tlljkncyxtwmfdl9vpm38uq2mq55mdesek2ttr","amount":"300000000ugnot"}],"fee":{"gas_wanted":"5000000","gas_fee":"1000000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"AhUuKSwiL4XKy0BM9cK0UJFToerTzvgLTHFBvYk/xOtY"},"signature":"byjDpKAKUZotqaFIORur64IUHyHkJ32oUIEqeFxUzcA8Hklnvph93qHS85SesOUNE6+ikIBDc7GAC0Pe1hkuQQ=="}],"memo":""},"metadata":{"timestamp":"1788742812"}}
{"tx":{"msg":[{"@type":"/vm.m_call","caller":"g1mv0052e7r6s09f5t9xsqf00nj3tqsgt9dg52jr","send":"","max_deposit":"","pkg_path":"gno.land/r/g1mv0052e7r6s09f5t9xsqf00nj3tqsgt9dg52jr/zdex/v2","func":"HarvestPoints","args":["ugnot|ZDEX"]}],"fee":{"gas_wanted":"8938806","gas_fee":"8939ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"AtXGYyknX/d1/cizOprH2X1W89cp3THaNaJ1Z1UEc4Ec"},"signature":"HQQIJ6zozbZXQ3q/g9LBohAeN9AcIyf/2JCUhGZG2m9L6rznCAPQniv7+RM1ilb+FPjd+YW9CpUaezRlDk/+rQ=="}],"memo":"zdex"},"metadata":{"timestamp":"1788742830"}}
{"tx":{"msg":[{"@type":"/bank.MsgSend","from_address":"g18qhq2fl54lszhmxeyqlvxnwjzc3xpu4nnakclp","to_address":"g127n456mqzrwngcye5ed648gj4uzu9hcy29uxmj","amount":"300000000ugnot"}],"fee":{"gas_wanted":"5000000","gas_fee":"1000000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"AhUuKSwiL4XKy0BM9cK0UJFToerTzvgLTHFBvYk/xOtY"},"signature":"0l/UikSorK/XDX5yWVNNJ5TXuipFZ72bf9mcuJcH0z5KJ7ETkoo6elOzwGPQvuw04+7D/4a716R4au2gFIkJoQ=="}],"memo":""},"metadata":{"timestamp":"1788742957"}}
{"tx":{"msg":[{"@type":"/bank.MsgSend","from_address":"g18qhq2fl54lszhmxeyqlvxnwjzc3xpu4nnakclp","to_address":"g1v5mlmypgljnc8hg9q3frg7tf0k90cl2957h4rd","amount":"300000000ugnot"}],"fee":{"gas_wanted":"5000000","gas_fee":"1000000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"AhUuKSwiL4XKy0BM9cK0UJFToerTzvgLTHFBvYk/xOtY"},"signature":"fX+WPhRqsL3SY9F41JRDOspjNTFMRfrdFP13QimlEnIEen/s5fD3OawrjMTewxOLreF3dgSHlAQFsEc3B8pixQ=="}],"memo":""},"metadata":{"timestamp":"1788743103"}}
{"tx":{"msg":[{"@type":"/bank.MsgSend","from_address":"g18qhq2fl54lszhmxeyqlvxnwjzc3xpu4nnakclp","to_address":"g1v7fmkc4cjrxdf3epad46nwh9l0x7yyt9msjh9j","amount":"300000000ugnot"}],"fee":{"gas_wanted":"5000000","gas_fee":"1000000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"AhUuKSwiL4XKy0BM9cK0UJFToerTzvgLTHFBvYk/xOtY"},"signature":"fo/cBK2fbYZfnNJn+1k0FcB3fCLwre4y5bfyPEsET+AHg52p4BYfLPMeecjN7ZPd3qe0w94kjIbnIHa+HZMnFw=="}],"memo":""},"metadata":{"timestamp":"1788743459"}}
{"tx":{"msg":[{"@type":"/bank.MsgSend","from_address":"g18qhq2fl54lszhmxeyqlvxnwjzc3xpu4nnakclp","to_address":"g1d9nu7nvlmsa00u3tch8vfepj8rvfu3zpgfxw83","amount":"300000000ugnot"}],"fee":{"gas_wanted":"5000000","gas_fee":"1000000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"AhUuKSwiL4XKy0BM9cK0UJFToerTzvgLTHFBvYk/xOtY"},"signature":"065GjbJIVoqqF2iKTS3Q5al+G6bYz+lALknSI/auQElfBcvdqGL5tCXy9S4O48RHAp61BvCRbBEvRLGxwGQpnw=="}],"memo":""},"metadata":{"timestamp":"1788743476"}}
{"tx":{"msg":[{"@type":"/bank.MsgSend","from_address":"g18qhq2fl54lszhmxeyqlvxnwjzc3xpu4nnakclp","to_address":"g1yapq0n8nhd2t990rxjnld0np8mpercdj6j4g74","amount":"300000000ugnot"}],"fee":{"gas_wanted":"5000000","gas_fee":"1000000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"AhUuKSwiL4XKy0BM9cK0UJFToerTzvgLTHFBvYk/xOtY"},"signature":"L3dCgDMp/yCDTQTMjt03yvyItvNcJ/UTyHV3gQy/XCUD9JVj/wn8UALgBSr11zTatBQS+dIXIqpa/Ml/5MBmoA=="}],"memo":""},"metadata":{"timestamp":"1788743590"}}
{"tx":{"msg":[{"@type":"/vm.m_call","caller":"g1xr6tgxnpled50h74eafmvxway7z0ytr5rsmeme","send":"","max_deposit":"","pkg_path":"gno.land/r/g1xr6tgxnpled50h74eafmvxway7z0ytr5rsmeme/nslogic/v2","func":"GrantFreeName","args":["testclaim","meme","g1gn6t0q9wenwhdda47rkrpfd63kcxjvyp7eqwku"]}],"fee":{"gas_wanted":"40000000","gas_fee":"40000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"Auy1VwQmQltziWh2LuRJj7xKSpD9chk7NirrueqIJYrk"},"signature":"TFLrgf7z3+r1tLjUqXk4ynkddwAYLjMnduwC3GMSSBJcIePqye0ALTM2WYyiFx4adso/roKoo0wScoU2c2itFw=="}],"memo":""},"metadata":{"timestamp":"1788744189"}}
{"tx":{"msg":[{"@type":"/bank.MsgSend","from_address":"g18qhq2fl54lszhmxeyqlvxnwjzc3xpu4nnakclp","to_address":"g137df9wm5d4qklvvhudsaz8z88n68rklha758q3","amount":"300000000ugnot"}],"fee":{"gas_wanted":"5000000","gas_fee":"1000000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"AhUuKSwiL4XKy0BM9cK0UJFToerTzvgLTHFBvYk/xOtY"},"signature":"DkUP3hsC0cMIqCfIGkRLwEYgmlmmZNtNMS0r99B6PnFqx70fJFhDjbOqPpTEj54ZnDEWG1EGq6r5eUFzWy+5Ag=="}],"memo":""},"metadata":{"timestamp":"1788744211"}}
{"tx":{"msg":[{"@type":"/vm.m_run","caller":"g169d3un6y4sqxxmzwt7le3l5pawlfrhg5aa3xvp","send":"","max_deposit":"","package":{"name":"main","path":"","files":[{"name":"main.gno","body":"package main\n\nimport (\n\tgrc20reg \"gno.land/r/demo/defi/grc20reg\"\n)\n\nfunc main(cur realm) {\n\tgrc20reg.Transfer(0, cur, \"gno.land/r/g169d3un6y4sqxxmzwt7le3l5pawlfrhg5aa3xvp/reward1.RWD1\", address(\"g1jg8mtutu9khhfwc4nxmuhcpftf0pajdhfvsqf5\"), 1000000)\n}\n"}]}}],"fee":{"gas_wanted":"14554934","gas_fee":"14555ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"A+FwLm4upty6jrOUKvPWMtxesfw3fytyPVyMKb9/GLoE"},"signature":"NYjur8Rjy3o9IQlhU3yZW9PVuLfMXcau8ZJhlSHKDvtStRsAFAUuyVAxICu0CtbA2NhhNJFvM8G2VFOSWM/6SA=="}],"memo":""},"metadata":{"timestamp":"1788744332"}}
{"tx":{"msg":[{"@type":"/bank.MsgSend","from_address":"g18qhq2fl54lszhmxeyqlvxnwjzc3xpu4nnakclp","to_address":"g15gv3sqyk97hra7u64vuhrh7g4mq5457p9cxtdh","amount":"300000000ugnot"}],"fee":{"gas_wanted":"5000000","gas_fee":"1000000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"AhUuKSwiL4XKy0BM9cK0UJFToerTzvgLTHFBvYk/xOtY"},"signature":"JkB4zja4jG6wlKA5uLKwltBN5RnUzc6dQLrRdexW5zEqq8pMFwRbiGwFQiialDWc3jznQlfDHctbT/lCV2jxVw=="}],"memo":""},"metadata":{"timestamp":"1788744390"}}
{"tx":{"msg":[{"@type":"/bank.MsgSend","from_address":"g18qhq2fl54lszhmxeyqlvxnwjzc3xpu4nnakclp","to_address":"g1f9xunxsfllvrc7paerpf2yded9085k6x62jvv4","amount":"300000000ugnot"}],"fee":{"gas_wanted":"5000000","gas_fee":"1000000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"AhUuKSwiL4XKy0BM9cK0UJFToerTzvgLTHFBvYk/xOtY"},"signature":"y3p1pITs5DdvACgjIpCqX6WhvyZ6tbcRj3opY8uBzDVWLJRH/hHnYbzRu+aosVeLCJS+JJdQ1omYOBX7LHlagQ=="}],"memo":""},"metadata":{"timestamp":"1788744422"}}
{"tx":{"msg":[{"@type":"/bank.MsgSend","from_address":"g18qhq2fl54lszhmxeyqlvxnwjzc3xpu4nnakclp","to_address":"g1jcryxefsxd5h6htnrp7hp6xqcefk265xtjy58w","amount":"300000000ugnot"}],"fee":{"gas_wanted":"5000000","gas_fee":"1000000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"AhUuKSwiL4XKy0BM9cK0UJFToerTzvgLTHFBvYk/xOtY"},"signature":"FCRm5KacAuDviOJdVI9rhTJiTCKnCTgH5d/1qGR+TLFDR3On4ZVeXedVwb1x5x+9vvIANCNe81D6rNqpCTtkeA=="}],"memo":""},"metadata":{"timestamp":"1788744758"}}
{"tx":{"msg":[{"@type":"/bank.MsgSend","from_address":"g18qhq2fl54lszhmxeyqlvxnwjzc3xpu4nnakclp","to_address":"g12wan93a6wr5zvgzx4j23hjkss5u0d64e22y62z","amount":"300000000ugnot"}],"fee":{"gas_wanted":"5000000","gas_fee":"1000000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"AhUuKSwiL4XKy0BM9cK0UJFToerTzvgLTHFBvYk/xOtY"},"signature":"j3gqH086l5bOKTcV58s5V7SGYJx71nTqtNTYrVYILbRoIbWYZdh5Qo5Qc36VMz8TI79KPPMMa0o5TjH+9bPOdw=="}],"memo":""},"metadata":{"timestamp":"1788744819"}}
{"tx":{"msg":[{"@type":"/bank.MsgSend","from_address":"g18qhq2fl54lszhmxeyqlvxnwjzc3xpu4nnakclp","to_address":"g1nuy4pnldsxg43xua7r8ndc7e7enthsjq2e3tcz","amount":"300000000ugnot"}],"fee":{"gas_wanted":"5000000","gas_fee":"1000000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"AhUuKSwiL4XKy0BM9cK0UJFToerTzvgLTHFBvYk/xOtY"},"signature":"MVB85t9HZaZ63n/dWg7VqCO04PXJXqkU0mL/69FVnfdF2ISQXetKmnKyT/phh3gztqrJDxRN6A+4v4zqZPZFlA=="}],"memo":""},"metadata":{"timestamp":"1788744923"}}
{"tx":{"msg":[{"@type":"/bank.MsgSend","from_address":"g18qhq2fl54lszhmxeyqlvxnwjzc3xpu4nnakclp","to_address":"g1vqrh0vypvrt7ywppklt7frqmtmq2r4562kjccj","amount":"300000000ugnot"}],"fee":{"gas_wanted":"5000000","gas_fee":"1000000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"AhUuKSwiL4XKy0BM9cK0UJFToerTzvgLTHFBvYk/xOtY"},"signature":"KGp+u0EXXJ7ru7cfDSJOVB4CGkaiqeQ6U7WezOC9hFsN+JvJf/C+2lnb8IAtA4jzsyrsUHyGx0BzlOloj1Gftg=="}],"memo":""},"metadata":{"timestamp":"1788745114"}}
{"tx":{"msg":[{"@type":"/vm.m_addpkg","creator":"g1xr6tgxnpled50h74eafmvxway7z0ytr5rsmeme","package":{"name":"nslogic","path":"gno.land/r/g1xr6tgxnpled50h74eafmvxway7z0ytr5rsmeme/nslogic/v3","files":[{"name":"admin.gno","body":"package nslogic\n\nimport (\n\t\"strconv\"\n\t\"strings\"\n\n\t\"gno.land/r/g1xr6tgxnpled50h74eafmvxway7z0ytr5rsmeme/nsdata/v1\"\n)\n\n// assertIsAdmin reads nsdata.GetAdmin() fresh every call — correct even\n// immediately after a TransferAdmin, and correct for a freshly-deployed\n// logic realm with no local state of its own to go stale.\n//\n// Note: admin authority to swap WHICH realm is trustedLogic, or to\n// transfer admin itself, is deliberately NOT exposed here — those live\n// only in nsdata's own admin.gno, callable solely by a direct wallet\n// call from the current admin address, never proxied through logic.\n// That's the one part of the system a compromised or buggy logic realm\n// cannot touch.\nfunc assertIsAdmin(cur realm) {\n\tif cur.Previous().Address() != nsdata.GetAdmin() {\n\t\tpanic(\"nslogic: restricted to project admin\")\n\t}\n}\n\n// SetLabelPattern no longer changes anything, and refuses anything but\n// the rule this realm actually implements.\n//\n// It used to compile an arbitrary regex and store it, with validateLabel\n// reading it back. That flexibility cost 15 GNOT of permanent storage\n// per deploy — see the measurements in validate.gno — for a rule that\n// has never changed and should not change casually.\n//\n// It is kept rather than deleted so that an admin who reaches for it\n// gets told what happened instead of a \"function not found\". And it\n// refuses a different pattern rather than accepting one it will not\n// enforce: a stored string describing rules the code does not apply is\n// the exact silent divergence the caching version was written to avoid.\nfunc SetLabelPattern(cur realm, pattern string) {\n\tassertNoSend(cur)\n\tassertIsAdmin(cur)\n\tif pattern != labelRule {\n\t\tpanic(\"nslogic: the label rule is fixed in this realm (\" + labelRule +\n\t\t\t\"). Changing it means deploying a new logic realm — which is deliberate, \" +\n\t\t\t\"because a stored pattern the code does not enforce is worse than no setting at all.\")\n\t}\n\tnsdata.SetLabelPatternRecord(cross(cur), pattern)\n}\n\nfunc SetLabelLengthLimits(cur realm, minLen, maxLen int) {\n\tassertNoSend(cur)\n\tassertIsAdmin(cur)\n\tif minLen \u003c 1 || maxLen \u003c minLen {\n\t\tpanic(\"nslogic: invalid length bounds\")\n\t}\n\tnsdata.SetLabelLengthRecord(cross(cur), minLen, maxLen)\n}\n\n// SetDefaultNamePrice sets the fallback price in micro-USD, used by any\n// domain that has not set its own base.\nfunc SetDefaultNamePrice(cur realm, microUsd int64) {\n\tassertNoSend(cur)\n\tassertIsAdmin(cur)\n\tif microUsd \u003c 0 {\n\t\tpanic(\"nslogic: price cannot be negative\")\n\t}\n\tsetGlobal(cur, keyDefaultPrice, strconv.FormatInt(microUsd, 10))\n}\n\n// SetUsdRate records how many ugnot one USD buys — the bridge between\n// the USD price list and settlement on this chain.\n//\n// It is a MANUAL rate, not a market feed: it sits where it was last\n// written. A stale rate during a sharp move sells names at the wrong\n// price, and no amount of contract logic fixes that — keeping it current\n// is an operational duty. Isolated as one global so a future logic realm\n// can swap in a real oracle without touching the vault or restating a\n// single price.\nfunc SetUsdRate(cur realm, ugnotPerUsd int64) {\n\tassertNoSend(cur)\n\tassertIsAdmin(cur)\n\tif ugnotPerUsd \u003c= 0 {\n\t\tpanic(\"nslogic: rate must be positive\")\n\t}\n\tsetGlobal(cur, keyRate, strconv.FormatInt(ugnotPerUsd, 10))\n}\n\n// SetTermLength / SetGracePeriod retune the lifecycle itself. Both were\n// hardcoded constants in the vault until 2026-08-21 — meaning the 1-year\n// term and 90-day grace could never have been changed, on a realm that\n// is never redeployed. There was no reason to accept that.\n// maxLifecycleDays bounds both setters. time.Duration is int64\n// NANOSECONDS, so `days * 24h` overflows above ~106,751 days and wraps to\n// a NEGATIVE duration — which would install a negative term and brick\n// every registration and renewal in the registry. Guarding the input is\n// what keeps the multiplication in range.\nconst maxLifecycleDays = 36500 // 100 years\n\nfunc SetTermLength(cur realm, days int64) {\n\tassertNoSend(cur)\n\tassertIsAdmin(cur)\n\tif days \u003c= 0 || days \u003e maxLifecycleDays {\n\t\tpanic(\"nslogic: term out of range (1..36500 days)\")\n\t}\n\tterm := days * 24 * 60 * 60\n\t// Keep gracePeriod \u003c= termLength. Anchoring a renewal to the previous\n\t// due date is only coherent while a renewal cannot happen more than\n\t// one full term after it; the vault clamps forward defensively, but\n\t// the combination should not be reachable in the first place.\n\tif nsdata.GetGracePeriod() \u003e term {\n\t\tpanic(\"nslogic: term must be at least the current grace period\")\n\t}\n\tnsdata.SetTermLengthRecord(cross(cur), term)\n}\n\nfunc SetGracePeriod(cur realm, days int64) {\n\tassertNoSend(cur)\n\tassertIsAdmin(cur)\n\tif days \u003c 0 || days \u003e maxLifecycleDays {\n\t\tpanic(\"nslogic: grace period out of range (0..36500 days)\")\n\t}\n\tgrace := days * 24 * 60 * 60\n\tif grace \u003e nsdata.GetTermLength() {\n\t\tpanic(\"nslogic: grace period may not exceed the term length\")\n\t}\n\tnsdata.SetGracePeriodRecord(cross(cur), grace)\n}\n\n// SetTokenURIBase re-points TokenURI at a different renderer, so the NFT\n// artwork can be replaced without touching the vault.\nfunc SetTokenURIBase(cur realm, base string) {\n\tassertNoSend(cur)\n\tassertIsAdmin(cur)\n\tnsdata.SetTokenURIBaseRecord(cross(cur), base)\n}\n\nfunc SetExtraLimits(cur realm, keyLen, valueLen, entries int) {\n\tassertNoSend(cur)\n\tassertIsAdmin(cur)\n\tif keyLen \u003c 1 || valueLen \u003c 1 || entries \u003c 1 {\n\t\tpanic(\"nslogic: limits must be positive\")\n\t}\n\tnsdata.SetExtraLimitsRecord(cross(cur), keyLen, valueLen, entries)\n}\n\n/*\nSetMarketFee is nsmarket's fee control, living here because this is the\nrealm the vault trusts.\n\nnsmarket.SetFee could never work: it wrote through nsdata.SetGlobalRecord\nfrom a realm deliberately outside the trusted set, so it passed its own\nadmin check and then panicked inside the vault for every caller. Rather\nthan grant nsmarket vault privileges — which would surrender the property\nthat realm is built around — the setter moved to the realm that already\nhas the grant, and the validation moved with it.\n\nThe spec grammar is nsmarket's, restated here rather than imported so\nthis realm does not take a dependency on the marketplace to set a number:\nan amount in ugnot (\"1000000\"), or a percentage (\"%2.5\"), or \"\" for none.\nA malformed spec would otherwise be read on every sale and resolve\nsilently to zero.\n*/\nfunc SetMarketFee(cur realm, spec string) {\n\tassertNoSend(cur)\n\tassertIsAdmin(cur)\n\tassertValidFeeSpec(spec)\n\tsetGlobal(cur, \"marketfee\", strings.TrimSpace(spec))\n}\n\n// assertValidFeeSpec mirrors nsmarket.parseFee. Percentages are carried\n// as basis points so a fee is expressible without floating point\n// anywhere near money.\nfunc assertValidFeeSpec(spec string) {\n\tspec = strings.TrimSpace(spec)\n\tif spec == \"\" {\n\t\treturn\n\t}\n\tif !strings.HasPrefix(spec, \"%\") {\n\t\tn, err := strconv.ParseInt(spec, 10, 64)\n\t\tif err != nil || n \u003c 0 {\n\t\t\tpanic(\"nslogic: a fee is an amount in ugnot or a percentage like %2.5 — got \" + spec)\n\t\t}\n\t\treturn\n\t}\n\tbody := spec[1:]\n\twhole, frac := body, \"\"\n\tif dot := strings.Index(body, \".\"); dot \u003e= 0 {\n\t\twhole, frac = body[:dot], body[dot+1:]\n\t}\n\t// ONE decimal, matching nsmarket.parseFee exactly. I wrote two here\n\t// first, which made this validator MORE permissive than the parser\n\t// that has to read the value back — and a spec nsmarket refuses is\n\t// read as a zero fee on every sale, silently, forever. A validator\n\t// looser than its consumer is worse than no validator: it converts a\n\t// rejected input into a silent revenue leak.\n\tif len(frac) \u003e 1 {\n\t\tpanic(\"nslogic: a fee percentage carries at most one decimal — got \" + spec)\n\t}\n\tfor _, part := range []string{whole, frac} {\n\t\tfor i := 0; i \u003c len(part); i++ {\n\t\t\tif part[i] \u003c '0' || part[i] \u003e '9' {\n\t\t\t\tpanic(\"nslogic: a fee percentage must be digits — got \" + spec)\n\t\t\t}\n\t\t}\n\t}\n\tif whole == \"\" \u0026\u0026 frac == \"\" {\n\t\tpanic(\"nslogic: a fee percentage needs a number — got \" + spec)\n\t}\n\t// Basis points the same way parseFee computes them: whole*100 plus\n\t// one tenth-digit worth ten. Not two digits, and not a 100% ceiling\n\t// — nsmarket caps at maxFeeBps, which is 50%.\n\tw, _ := strconv.ParseInt(\"0\"+whole, 10, 64)\n\tbp := w * 100\n\tif frac != \"\" {\n\t\tf, _ := strconv.ParseInt(\"0\"+frac, 10, 64)\n\t\tbp += f * 10\n\t}\n\tif bp \u003c= 0 || bp \u003e 5000 {\n\t\tpanic(\"nslogic: a fee percentage must be above zero and at most 50 — got \" + spec)\n\t}\n}\n\n// SetGlobal writes an arbitrary project-wide key into the vault's\n// package-level extension slot — the escape hatch for global state that\n// does not exist yet.\nfunc SetGlobal(cur realm, key, value string) {\n\tassertNoSend(cur)\n\tassertIsAdmin(cur)\n\tsetGlobal(cur, key, value)\n}\n\nfunc setGlobal(cur realm, key, value string) {\n\t/* THE RESTORE SEAL IS NOT AN ORDINARY GLOBAL.\n\t   restore.gno says sealing is \"IRREVERSIBLE… There is no argument\n\t   that unseals it\", and RestoreGlobal carries a reserved-key guard\n\t   to make that true. This function did not, so one admin call\n\t   writing key \"restore\" reopened the seal and the claim in that file\n\t   was simply false. The admin key is trusted and already holds\n\t   worse powers, so this is documentation drift rather than an\n\t   escalation — but a permanent-sounding promise that a two-word call\n\t   undoes is the kind of thing an auditor and a court both notice.\n\t   The durable version of this guard belongs in the vault, where a\n\t   logic swap cannot drop it; this is the half that ships today. */\n\tif key == keyRestore || key == keyRestoredCount {\n\t\tpanic(\"nslogic: \" + key + \" is written by the restore machinery, not by SetGlobal\")\n\t}\n\tif err := nsdata.SetGlobalRecord(cross(cur), key, value); err != nil {\n\t\tpanic(err)\n\t}\n}\n\n// SetTreasury moves the revenue-receiving role — independent of\n// nsdata's TransferAdmin, same reasoning as the original build's\n// admin/treasury separation.\nfunc SetTreasury(cur realm, newTreasury address) {\n\tassertNoSend(cur)\n\tassertIsAdmin(cur)\n\tif !newTreasury.IsValid() {\n\t\tpanic(\"nslogic: invalid treasury address\")\n\t}\n\tnsdata.SetTreasuryRecord(cross(cur), newTreasury)\n}\n\n// SelfAddress reports this deployed realm's own address — needed once,\n// at wiring time, so admin knows what to pass to\n// nsdata.ProposeTrustedLogicGrant.\nfunc SelfAddress(cur realm) address {\n\treturn cur.Address()\n}\n"},{"name":"domain.gno","body":"package nslogic\n\nimport (\n\t\"strconv\"\n\t\"strings\"\n\t\"time\"\n\n\t\"gno.land/r/g1xr6tgxnpled50h74eafmvxway7z0ytr5rsmeme/nsdata/v1\"\n)\n\n// RegisterDomain mints a new `*domain` — ADMIN-ONLY (2026-08-15 policy:\n// domains are never sold or owned by third parties). No payment: the\n// project isn't paying itself. Domains are also permanently\n// non-transferable by users (see nft.gno's TransferFrom).\nfunc RegisterDomain(cur realm, label string) {\n\tassertNoSend(cur)\n\tassertIsAdmin(cur)\n\tvalidateLabel(label)\n\tcaller := cur.Previous().Address()\n\tif err := nsdata.RegisterDomainRecord(cross(cur), label, caller, time.Now().Unix()); err != nil {\n\t\tpanic(err)\n\t}\n}\n\n// RenewDomain extends a domain's expiry by exactly one year from its\n// PREVIOUS due date, not from the payment date. No payment — domains are\n// always project-owned, so this is self-service upkeep, not a sale.\n// Deliberately does not call validateLabel — a tightened pattern only\n// gates new registrations, never renewals of what's already registered.\nfunc RenewDomain(cur realm, label string) {\n\tassertNoSend(cur)\n\tcaller := cur.Previous().Address()\n\tif err := nsdata.RenewDomainRecord(cross(cur), label, caller, time.Now().Unix()); err != nil {\n\t\tpanic(err)\n\t}\n}\n\n// ReassignDomain moves a domain record to a new owner. ADMIN-ONLY, and\n// the deliberate exception to \"domains are non-transferable\".\n//\n// It exists because domain ownership is snapshotted into the record at\n// registration time, while `admin` is a rotatable role. After the\n// planned EOA-to-multisig rotation, the incoming admin could not renew\n// or reprice any domain the outgoing key had registered — and if the\n// rotation happened *because* the old key was compromised, the attacker\n// could still reprice those domains afterwards. This is the recovery\n// lever for exactly that situation.\n//\n// Not a loophole in the non-transferability policy: that policy exists\n// to stop domains reaching third parties as tradeable assets, and this\n// path is reachable only by the current admin, moving a project domain\n// between project-controlled addresses.\nfunc ReassignDomain(cur realm, label string, newOwner address) {\n\tassertNoSend(cur)\n\tassertIsAdmin(cur)\n\towner := nsdata.GetDomainOwner(label)\n\n\t/* THE TRANSFER WIPES THE DOMAIN'S CONFIGURATION, so it is read back\n\t   and rewritten around the call.\n\t   RawTransferNFT applies wipeOnHandover, whose long rationale is\n\t   entirely about NAMES — clearing a seller's payment addresses so a\n\t   buyer does not inherit them. A domain's extra slot is not user\n\t   data: it is the domain's settings. price, len, nm, open and\n\t   maxterm all live there, so a deliberately CLOSED domain silently\n\t   reopened, pricing fell back to the global default, and the\n\t   per-domain term cap released to the project default — the brake\n\t   that exists to stop a script hoarding every good name under a free\n\t   domain. Nothing errored and nothing was emitted, and this fires\n\t   during a compromised-key recovery, when the operator is least able\n\t   to notice.\n\t   The durable fix is a vault function that moves the owner and\n\t   leaves extra alone; domains are project-owned and never reach a\n\t   third party, so there is no previous-owner data to protect. This\n\t   is the stopgap that works against a vault already deployed. */\n\tkeep := []string{\"price\", \"len\", \"nm\", \"open\", \"maxterm\"}\n\tsaved := make([]string, len(keep))\n\tfor i, k := range keep {\n\t\tsaved[i] = nsdata.GetDomainExtra(label, k)\n\t}\n\n\tif err := nsdata.RawTransferNFT(cross(cur), owner, owner, newOwner, \"*\"+label); err != nil {\n\t\tpanic(err)\n\t}\n\n\tfor i, k := range keep {\n\t\tif saved[i] == \"\" {\n\t\t\tcontinue // absent before, absent after — writing \"\" would store a blank\n\t\t}\n\t\tif err := nsdata.SetDomainExtraRecord(cross(cur), label, k, saved[i]); err != nil {\n\t\t\tpanic(err)\n\t\t}\n\t}\n}\n\n// -- pricing controls (admin) --\n//\n// Every amount here is MICRO-USD (1 USD = 1_000_000). Nothing is priced\n// in the chain token; ugnot is derived at payment time from the stored\n// rate, so a second settlement token later is a second rate rather than\n// a second price list.\n\n// SetDomainOpen enables or disables registration of new names under a\n// domain. Existing names are untouched: closing a domain stops new\n// mints, it does not seize anything already held.\nfunc SetDomainOpen(cur realm, label string, open bool) {\n\tassertNoSend(cur)\n\tassertIsAdmin(cur)\n\tv := \"1\"\n\tif !open {\n\t\tv = \"0\"\n\t}\n\tsetDomainExtra(cur, label, keyOpen, v)\n}\n\n// SetDomainBasePrice sets the domain's base price in micro-USD. Every\n// \"%N\" rule under the domain is relative to this, so one call reprices\n// the whole domain.\nfunc SetDomainBasePrice(cur realm, label string, microUsd int64) {\n\tassertNoSend(cur)\n\tassertIsAdmin(cur)\n\tif microUsd \u003c 0 {\n\t\tpanic(\"nslogic: price cannot be negative\")\n\t}\n\tsetDomainExtra(cur, label, keyPrice, strconv.FormatInt(microUsd, 10))\n}\n\n// SetDomainLengthRules prices by label length: \"3:2000000,4:%150,5:0\"\n// means 3-char names cost $2.00, 4-char names cost 150% of the domain\n// base, and 5-char names are free.\nfunc SetDomainLengthRules(cur realm, label, rules string) {\n\tassertNoSend(cur)\n\tassertIsAdmin(cur)\n\tsetDomainExtra(cur, label, keyLens, normalizeRules(rules, true))\n}\n\n// SetDomainNameOverrides prices specific labels: \"420:5000000,gm:%500\".\n// Highest precedence — this beats length rules and the base.\nfunc SetDomainNameOverrides(cur realm, label, rules string) {\n\tassertNoSend(cur)\n\tassertIsAdmin(cur)\n\tsetDomainExtra(cur, label, keyNames, normalizeRules(rules, false))\n}\n\n// SetDomainField writes an arbitrary key on a domain, for whatever a\n// future feature needs without a vault change.\nfunc SetDomainField(cur realm, label, key, value string) {\n\tassertNoSend(cur)\n\tassertIsAdmin(cur)\n\tif strings.HasPrefix(key, \"_\") {\n\t\tpanic(\"nslogic: keys beginning with '_' are reserved\")\n\t}\n\tsetDomainExtra(cur, label, key, value)\n}\n\nfunc setDomainExtra(cur realm, label, key, value string) {\n\tif err := nsdata.SetDomainExtraRecord(cross(cur), label, key, value); err != nil {\n\t\tpanic(err)\n\t}\n}\n\n// FreezeDomain / UnfreezeDomain: project-only power to permanently block\n// re-registration of a domain even after its grace period lapses.\n// DeleteDomain destroys a domain and every name beneath it.\n//\n// THE VAULT PRIMITIVE EXISTED BEFORE THIS DID, AND THAT MADE IT\n// UNREACHABLE. nsdata.DeleteDomainRecord gates on assertIsTrustedLogic,\n// so the only way to reach it is a function in this realm — and there\n// was not one. The capability shipped in the vault and could not be\n// used, which is worse than not shipping it, because it reads as\n// present.\n//\n// Bounded by the vault at maxDeletePerCall names per call. It returns\n// how many it removed and whether it finished, so a large domain is\n// several calls rather than a transaction that cannot fit in a block.\n// Repeat until done is true; calling it on a domain that is already gone\n// reports done rather than panicking, so a loop terminates cleanly.\n//\n// ADMIN ONLY, and deliberately not owner-callable: domains are\n// project-owned (SPEC.md §17b) and this destroys other people's names.\nfunc DeleteDomain(cur realm, label string) (removed int64, done bool) {\n\tassertNoSend(cur)\n\tassertIsAdmin(cur)\n\treturn nsdata.DeleteDomainRecord(cross(cur), label)\n}\n\n// SetDomainTTL sets how long a resolver may cache answers about this\n// domain, in seconds, where 0 means never cache — the ENS rule. Admin\n// only, since domains are project-owned.\nfunc SetDomainTTL(cur realm, label string, seconds int64) {\n\tassertNoSend(cur)\n\tassertIsAdmin(cur)\n\tif seconds \u003c 0 {\n\t\tpanic(\"nslogic: ttl may not be negative\")\n\t}\n\tif err := nsdata.SetDomainTTLRecord(cross(cur), label, seconds); err != nil {\n\t\tpanic(err)\n\t}\n}\n\nfunc FreezeDomain(cur realm, label string) {\n\tassertNoSend(cur)\n\tassertIsAdmin(cur)\n\tnsdata.SetDomainFrozen(cross(cur), label, true)\n}\n\nfunc UnfreezeDomain(cur realm, label string) {\n\tassertNoSend(cur)\n\tassertIsAdmin(cur)\n\tnsdata.SetDomainFrozen(cross(cur), label, false)\n}\n"},{"name":"freetier.gno","body":"package nslogic\n\nimport (\n\t\"strconv\"\n\t\"time\"\n\n\t\"gno.land/r/g1xr6tgxnpled50h74eafmvxway7z0ytr5rsmeme/nsdata/v1\"\n)\n\n/*\nTHE FREE TIER: a name somebody can hold without ever owning a wallet.\n\nThe vault has supported this since it was written — RegisterNameRecord\ntakes mintNFT=false and stores a record with no GRC-721 token, and\nPromoteToNFTRecord is described there as \"the paid free-tier upgrade\".\nNothing in the logic realm ever reached either. This is that wiring.\n\nWHY A RECORD WITHOUT A TOKEN IS THE RIGHT SHAPE, and not a compromise:\n\n\tIT IS A REAL NAME. Owned, resolvable, renewable, on chain, visible\n\tto anyone. Not a promise, not a queue position, not a row in our\n\tdatabase. Everything the paid tier does except be sold.\n\n\tIT CANNOT BE SOLD, and that is what makes giving it away safe.\n\tnsmarket escrows a TOKEN, and there is no token — so a squatter who\n\tfarms a thousand of these has farmed a thousand things nobody can\n\tbuy from them. The anti-abuse property falls out of the data model\n\trather than out of a rule somebody has to enforce.\n\n\tIT BECOMES SELLABLE BY BECOMING PAID. UpgradeFreeName takes the full\n\tlist price and mints the token. So the same name costs nothing and\n\tcosts full price, and which one depends entirely on whether you want\n\tto sell it.\n\nWHY THE PROJECT PAYS. The whole point is somebody with no GNOT, and\npossibly no wallet, who is going to put this name in front of an\naudience. Measured on Pearl: a registration is about 0.04 GNOT of fee\nplus a small storage deposit, and a free-tier record is smaller than a\npaid one because there is no token and no ledger entry. That is a\nmarketing cost per name, bounded by a daily cap we apply off chain,\nwhere it can be changed without a deploy.\n\nWHAT IS DELIBERATELY NOT HERE. No wordlist: storage is 100 ugnot a byte,\nso a ten-thousand-word dictionary is about 57 GNOT paid forever to\nencode a marketing decision that will change. The contract carries the\nstructural rules in reserved.gno and nothing else; the lists live off\nchain with the issuer, which is us, because the free path is granted\nrather than bought. See tools/gen-reserved.py.\n*/\n\nconst (\n\tkeyFreeDomain = \"free\"      // per-domain: \"1\" means the free tier is open here\n\tkeyGrantEnd   = \"grantend\"  // global: unix seconds, or \"never\"\n\tkeyGranter    = \"granter\"   // global: the address allowed to issue grants\n)\n\n/*\nTHE GRANTER IS NOT THE ADMIN, and that separation is the point.\n\nFree names are issued by a machine — something has to sign the grant\nafter a person proves an X account, and that key has to sit somewhere\nwarm. If that key were the admin key, a compromise of the issuing\nmachine would be a compromise of the registry: DeleteDomain destroys\nevery name under a domain and burns their tokens, instantly, with no\ntimelock.\n\nSo there is a second role that can do exactly one thing. Worst case for\na stolen granter key is that somebody gives away free names until we\nnotice and clear the role — a bounded, reversible, embarrassing loss\nrather than an unbounded one.\n\nStored in the vault's globals rather than in this realm, so it survives\na logic redeploy. Unset means nobody but the admin may grant, which is\nthe right default for a role that exists to be delegated deliberately.\n*/\nfunc Granter() address {\n\treturn address(nsdata.GetGlobal(keyGranter))\n}\n\n// SetGranter delegates grant issuance, or withdraws it. Passing the\n// empty address is the revocation, and it is the first thing to reach\n// for if the issuing machine is ever in doubt.\nfunc SetGranter(cur realm, a address) {\n\tassertNoSend(cur)\n\tassertIsAdmin(cur)\n\tif a != \"\" \u0026\u0026 !a.IsValid() {\n\t\tpanic(\"nslogic: not an address\")\n\t}\n\tsetGlobal(cur, keyGranter, a.String())\n}\n\n/*\nSetFreeLengths tunes which lengths the free tier will issue.\n\nIn the panel rather than in a redeploy: nslogic is about 22 GNOT to\ndeploy, and \"should four-character names be free this month\" is a\nmarketing question that should cost a signature, not a deployment.\n\nIT DOES NOT REACH BACKWARDS. Raising the minimum stops NEW issuance of\nshort names; every one already granted stays granted and keeps renewing\nfree. FreeLabelAllowed is called by GrantFreeName and by nothing else,\nwhich is what makes that true — see the note on it in reserved.gno.\n*/\nfunc SetFreeLengths(cur realm, min, max int) {\n\tassertNoSend(cur)\n\tassertIsAdmin(cur)\n\tif min \u003c 1 || max \u003e 63 || min \u003e max {\n\t\tpanic(\"nslogic: a free-tier length range must sit inside 1..63 and be the right way round\")\n\t}\n\tsetGlobal(cur, keyFreeMin, strconv.Itoa(min))\n\tsetGlobal(cur, keyFreeMax, strconv.Itoa(max))\n}\n\nfunc assertMayGrant(cur realm) {\n\tcaller := cur.Previous().Address()\n\tif caller == nsdata.GetAdmin() {\n\t\treturn\n\t}\n\tg := Granter()\n\tif g == \"\" || caller != g {\n\t\tpanic(\"nslogic: only the admin or the granter may issue free names\")\n\t}\n}\n\n// DomainFree reports whether a domain accepts free-tier registrations.\n// Off by default, and per-domain on purpose: the free tier is a\n// promotion aimed at a few domains, not a property of the registry.\nfunc DomainFree(domainLabel string) bool {\n\treturn nsdata.GetDomainExtra(domainLabel, keyFreeDomain) == \"1\"\n}\n\n// SetDomainFree opens or closes the free tier for one domain.\nfunc SetDomainFree(cur realm, domainLabel string, on bool) {\n\tassertNoSend(cur)\n\tassertIsAdmin(cur)\n\tif !nsdata.DomainExists(domainLabel) {\n\t\tpanic(\"nslogic: no such domain\")\n\t}\n\tv := \"\"\n\tif on {\n\t\tv = \"1\"\n\t}\n\tif err := nsdata.SetDomainExtraRecord(cross(cur), domainLabel, keyFreeDomain, v); err != nil {\n\t\tpanic(err)\n\t}\n}\n\n/*\nGrantWindowOpen reports whether free grants are being issued at all.\n\nA single global switch that closes everything at once, because the\nfailure this guards against is not a clever attack — it is noticing on a\nSunday that something is wrong and wanting one call that stops it. An\nunset value means open; \"never\" means open with no end date, which is\nwhat the admin panel already writes.\n*/\nfunc GrantWindowOpen() bool {\n\treturn readGrantWindow(nsdata.GetGlobal(keyGrantEnd), time.Now().Unix())\n}\n\n// readGrantWindow is GrantWindowOpen with the clock and the stored value\n// handed in, so the reading can be tested without a chain.\nfunc readGrantWindow(v string, now int64) bool {\n\tif v == \"\" || v == \"never\" {\n\t\treturn true\n\t}\n\tend, err := strconv.ParseInt(v, 10, 64)\n\tif err != nil {\n\t\t// An unparseable window is a misconfiguration, and the safe\n\t\t// reading of a misconfigured gate is \"closed\". Giving names away\n\t\t// because a setting was mistyped is the wrong way to fail.\n\t\treturn false\n\t}\n\treturn now \u003c end\n}\n\n/*\nGrantFreeName registers a free-tier record to somebody else.\n\nADMIN OR GRANTER, and that is the whole trust model: the eligibility rules —\nwhich Twitter account, how old, how many followers, whether the label is\na dictionary word — live off chain with the issuer. Putting them here\nwould freeze a marketing policy into a realm and cost storage to do it.\nWhat the chain enforces is the part that must not depend on our\ncorrectness: the label is structurally allowed, the domain opted in, and\nthe window is open.\n\n`owner` may be an address the recipient generated in their browser\nseconds ago and has never funded. That is the point.\n*/\nfunc GrantFreeName(cur realm, label, domainLabel string, owner address) {\n\tassertNoSend(cur)\n\tassertMayGrant(cur)\n\n\tif !GrantWindowOpen() {\n\t\tpanic(\"nslogic: free grants are closed\")\n\t}\n\tif !DomainFree(domainLabel) {\n\t\tpanic(\"nslogic: *\" + domainLabel + \" does not offer free names\")\n\t}\n\tif !DomainOpen(domainLabel) {\n\t\tpanic(\"nslogic: this domain is not accepting new names\")\n\t}\n\tvalidateLabel(label)\n\t// The structural backstop. The issuer has already applied the full\n\t// policy; this is what stops a bug there giving away a four-letter\n\t// name or a pure-numeric one.\n\tif !FreeLabelAllowed(label) {\n\t\tpanic(\"nslogic: \" + label + \" is not available on the free tier\")\n\t}\n\tif !owner.IsValid() {\n\t\tpanic(\"nslogic: a grant needs a recipient\")\n\t}\n\n\t// mintNFT=false — a record, not a token. See the note at the top.\n\tif err := nsdata.RegisterNameRecord(cross(cur), label, domainLabel, owner,\n\t\ttime.Now().Unix(), false); err != nil {\n\t\tpanic(err)\n\t}\n}\n\n/*\nUpgradeFreeName mints the token for a free-tier record, at full price.\n\nThis is the only way a free name becomes sellable, and the price is the\nordinary list price with no referral discount — a discount is for\nbringing somebody new, and this person is already here.\n\n`newOwner` exists because the common case is somebody who has decided to\nsell and, in the same moment, wants the name in a wallet they actually\ncontrol rather than the browser key they were given. The vault handles\nthat move inside PromoteToNFTRecord.\n\nCallable by the record's current owner only. Anyone may PAY for\nsomebody else's upgrade in the sense that they can hand over the money,\nbut they cannot perform it — a stranger promoting your name into a token\nand choosing where it lands is a theft with a receipt.\n*/\nfunc UpgradeFreeName(cur realm, label, domainLabel string, newOwner address) {\n\tcaller := cur.Previous().Address()\n\tif nsdata.GetNameOwner(label, domainLabel) != caller {\n\t\tpanic(\"nslogic: restricted to name owner\")\n\t}\n\tif !nsdata.IsNameValid(label, domainLabel) {\n\t\tpanic(\"nslogic: this name has expired\")\n\t}\n\tif newOwner == \"\" {\n\t\tnewOwner = caller\n\t}\n\tif !newOwner.IsValid() {\n\t\tpanic(\"nslogic: invalid destination address\")\n\t}\n\n\t// Full list price, resolved now rather than at grant time — the\n\t// name's worth is what it is worth today, and a free name held for a\n\t// year should not be upgradable at last year's rate.\n\ttakePaymentSplit(cur, NamePrice(domainLabel, label), \"\", 0)\n\n\tif err := nsdata.PromoteToNFTRecord(cross(cur), label, domainLabel, newOwner); err != nil {\n\t\tpanic(err)\n\t}\n}\n\n/*\nGrantFreeRenewal extends a free-tier name at no charge.\n\nTHE DEAL IS THE DISPLAY, and it is checked off chain: the issuer signs\nsomebody in on X, reads their display name, and finds the name in it.\nThat single act proves both halves at once — that a real account still\nexists, and that it is still carrying the name — which is why renewal is\none click a year rather than a form.\n\nFREE-TIER RECORDS ONLY. A paid name renewed for nothing is revenue given\naway by accident, and the two are told apart by whether a token was ever\nminted. isNFT is the flag, and it is the vault's, not ours.\n\nNO OWNERSHIP PROOF IS ASKED FOR, and that is deliberate rather than\nmissing. Renewing somebody else's name is a GIFT — it takes nothing,\nmoves nothing, and grants the caller no power over it. The only cost of\nallowing it is ours, and the vault's renewal cap already bounds how far\nany single name can be pushed. A proof requirement here would buy\nnothing and would cost the one-click renewal that makes the deal work.\n\nRenewNameRecord insists the caller is the owner, so the owner is looked\nup and passed. That is not a bypass: this realm is the trusted logic,\nand what it is asserting is exactly true — this renewal is on that\nowner's behalf.\n*/\nfunc GrantFreeRenewal(cur realm, label, domainLabel string) {\n\tassertNoSend(cur)\n\tassertMayGrant(cur)\n\n\tif !nsdata.NameExists(label, domainLabel) {\n\t\tpanic(\"nslogic: no such name\")\n\t}\n\towner, _, _, _, _, isNFT := nsdata.GetNameInfo(label, domainLabel)\n\tif isNFT {\n\t\tpanic(\"nslogic: \" + label + \"*\" + domainLabel +\n\t\t\t\" is a paid name — free renewal is for free-tier records\")\n\t}\n\tif RenewalsLeft(label, domainLabel) \u003c= 0 {\n\t\tpanic(\"nslogic: this name is already paid as far ahead as a name may run\")\n\t}\n\tif err := nsdata.RenewNameRecord(cross(cur), label, domainLabel, owner,\n\t\ttime.Now().Unix()); err != nil {\n\t\tpanic(err)\n\t}\n}\n\n// IsFreeTier reports whether a name is a record without a token — the\n// free tier. Exported because the site and the issuer both need to ask\n// before offering a free renewal, and neither should infer it.\nfunc IsFreeTier(label, domainLabel string) bool {\n\tif !nsdata.NameExists(label, domainLabel) {\n\t\treturn false\n\t}\n\t_, _, _, _, _, isNFT := nsdata.GetNameInfo(label, domainLabel)\n\treturn !isNFT\n}\n"},{"name":"gnomod.toml","body":"module = \"gno.land/r/g1xr6tgxnpled50h74eafmvxway7z0ytr5rsmeme/nslogic/v3\"\ngno = \"0.9\"\n"},{"name":"holdings.gno","body":"package nslogic\n\nimport (\n\t\"strings\"\n\n\t\"gno.land/p/nt/ufmt/v0\"\n\t\"gno.land/r/g1xr6tgxnpled50h74eafmvxway7z0ytr5rsmeme/nsdata/v1\"\n)\n\n// Answering \"what does this address hold\" in ONE read.\n//\n// The site used to ask the chain for every name in the REGISTRY and then\n// throw away the ones that did not match — one HTTP round trip per name\n// in the world to draw one person's collection. Six names took three\n// seconds, and it gets linearly worse for everybody every time anybody\n// else registers anything.\n//\n// The scan itself is unavoidable: nsdata keys names by domain*label and\n// has no index by owner, and nsdata is the permanent vault so no index\n// can be added to it now. But the scan does not have to happen across\n// the network. nslogic is swappable and can walk the same records inside\n// a single query, which turns N round trips into one.\n//\n// Nothing is cached and nothing is trusted from the caller: every row is\n// read from the vault at the moment of the query, exactly as before.\n//\n// Paged, because a query has a gas ceiling of its own and a registry\n// with fifty thousand names in it must not become one enormous read that\n// nobody can complete. `after` is the vault's own cursor, so paging here\n// is the same paging ExportNames already does.\nconst maxHoldingsPage = 200\n\n// NamesOwnedBy returns rows of `label*domain,registered,expires,streak,frozen`\n// separated by \";\", plus the cursor to continue from. Labels are\n// restricted to [a-z0-9-] so neither separator can appear inside a field.\n//\n// `next` is empty when the scan has reached the end of the registry. It\n// is NOT empty merely because this page found no matches: a page of two\n// hundred names belonging to other people returns no rows and a cursor,\n// and the caller keeps going.\nfunc NamesOwnedBy(owner address, after string, limit int) (rows, next string) {\n\tif limit \u003c= 0 || limit \u003e maxHoldingsPage {\n\t\tlimit = maxHoldingsPage\n\t}\n\tkeys, nxt := nsdata.ExportNames(\"\", after, limit)\n\n\tvar b strings.Builder\n\tfor _, key := range strings.Split(keys, \",\") {\n\t\tif key == \"\" {\n\t\t\tcontinue\n\t\t}\n\t\t// The tree key is domain*label; the token ID is label*domain.\n\t\tat := strings.Index(key, \"*\")\n\t\tif at \u003c 0 {\n\t\t\tcontinue\n\t\t}\n\t\tdomainLabel, label := key[:at], key[at+1:]\n\n\t\to, registered, expires, streak, frozen, _ := nsdata.GetNameInfo(label, domainLabel)\n\t\tif o != owner {\n\t\t\tcontinue\n\t\t}\n\t\tif b.Len() \u003e 0 {\n\t\t\tb.WriteString(\";\")\n\t\t}\n\t\tb.WriteString(ufmt.Sprintf(\"%s*%s,%d,%d,%d,%t\",\n\t\t\tlabel, domainLabel, registered, expires, streak, frozen))\n\t}\n\treturn b.String(), nxt\n}\n\n// CountNamesOwnedBy is the same walk without the strings, for a header\n// that wants a number before the list has finished arriving.\nfunc CountNamesOwnedBy(owner address, after string, limit int) (n int, next string) {\n\tif limit \u003c= 0 || limit \u003e maxHoldingsPage {\n\t\tlimit = maxHoldingsPage\n\t}\n\tkeys, nxt := nsdata.ExportNames(\"\", after, limit)\n\tfor _, key := range strings.Split(keys, \",\") {\n\t\tif key == \"\" {\n\t\t\tcontinue\n\t\t}\n\t\tat := strings.Index(key, \"*\")\n\t\tif at \u003c 0 {\n\t\t\tcontinue\n\t\t}\n\t\tif nsdata.GetNameOwner(key[at+1:], key[:at]) == owner {\n\t\t\tn++\n\t\t}\n\t}\n\treturn n, nxt\n}\n\n// Everything the profile page draws, in ONE read.\n//\n// It was making seventeen queries: three to establish that the name\n// exists and when it expires, then fourteen more — one per extension\n// slot — that could not start until the first three had come back. Two\n// waves, and the node serves only a few at a time, so a profile took\n// seconds to assemble out of values that all live in the same record.\n//\n// Values come back in the ORDER THE KEYS WERE ASKED FOR rather than as\n// key=value pairs. An extension key is allowed to contain \"=\" (only\n// commas and newlines are forbidden), so a pair encoding would have an\n// ambiguous split; positional has none.\n//\n// Line 0 is the record: owner, registered, expires, streak, frozen, and\n// the grace period, which is global but is one more query the page would\n// otherwise have to make on its own.\n//\n// An empty return means the name is not registered. That is a real\n// answer rather than an error: asking about a name nobody owns is the\n// normal case on a profile URL somebody typed.\nfunc ProfileOf(label, domainLabel, keys string) string {\n\tif !nsdata.NameExists(label, domainLabel) {\n\t\treturn \"\"\n\t}\n\towner, registered, expires, streak, frozen, _ := nsdata.GetNameInfo(label, domainLabel)\n\n\tvar b strings.Builder\n\tb.WriteString(ufmt.Sprintf(\"%s,%d,%d,%d,%t,%d\",\n\t\towner.String(), registered, expires, streak, frozen, nsdata.GetGracePeriod()))\n\n\tfor _, k := range strings.Split(keys, \",\") {\n\t\tb.WriteString(\"\\n\")\n\t\tif k == \"\" {\n\t\t\tcontinue\n\t\t}\n\t\tv := nsdata.GetNameExtra(label, domainLabel, k)\n\n\t\t// THE GNO.LAND ADDRESS ANSWERS EVEN WHEN NOBODY SAVED ONE.\n\t\t//\n\t\t// A name is an NFT held by an address, so the owner is already\n\t\t// public — it is the first field of the record above. Returning\n\t\t// it here as well costs no storage and means a name can be paid\n\t\t// without its holder ever having opened a profile editor, which\n\t\t// is most of them, and without a transaction to back-fill the\n\t\t// ones registered before this existed.\n\t\t//\n\t\t// Doing it in the realm rather than in the website is the whole\n\t\t// point: any client asking this realm for the gno key gets the\n\t\t// same answer our pages do. A front end that derived it locally\n\t\t// would leave every other reader with nothing.\n\t\t//\n\t\t// \"off\" is the opt-out, and it has to be a sentinel. An empty\n\t\t// value is indistinguishable from never having set one, so an\n\t\t// empty gno field would simply hand the derived address back on\n\t\t// the next read.\n\t\tif k == \"gno\" {\n\t\t\tif v == \"off\" {\n\t\t\t\tv = \"\"\n\t\t\t} else if v == \"\" {\n\t\t\t\tv = owner.String()\n\t\t\t}\n\t\t}\n\t\tb.WriteString(escapeValue(v))\n\t}\n\treturn b.String()\n}\n\n// Extension VALUES are free text — a bio can hold anything a person can\n// type, newlines included — so they cannot be newline-separated raw.\n// Escaping is done here rather than stripping, because silently eating a\n// line break out of somebody's bio is data loss on a read path.\nfunc escapeValue(s string) string {\n\ts = strings.ReplaceAll(s, `\\`, `\\\\`)\n\ts = strings.ReplaceAll(s, \"\\n\", `\\n`)\n\treturn strings.ReplaceAll(s, \"\\r\", `\\r`)\n}\n\n// RecentNames answers \"what has just been registered\" in one read.\n//\n// The site used to build this by listing every key and then reading each\n// record one at a time — the same shape NamesOwnedBy replaced for a\n// wallet's holdings, and the same problem: fine at thirteen names, one\n// query per name forever after. A feed that gets slower every time\n// somebody registers is a feed that eventually stops loading on the busy\n// day you most wanted it.\n//\n// Returns rows of \"label*domain,registered,expires,owner\", newest first,\n// and a cursor. Sorting happens across a PAGE, not the whole registry:\n// the vault stores names alphabetically and there is no index by date,\n// so a caller that wants the true newest reads every page and merges.\n// That is one query per 200 names instead of one per name — enough of a\n// difference to stop mattering, without pretending the vault has an\n// index it does not have.\nfunc RecentNames(after string, limit int) (rows, next string) {\n\tif limit \u003c= 0 || limit \u003e maxHoldingsPage {\n\t\tlimit = maxHoldingsPage\n\t}\n\tkeys, nxt := nsdata.ExportNames(\"\", after, limit)\n\n\ttype row struct {\n\t\ttext string\n\t\tat   int64\n\t}\n\tvar found []row\n\tfor _, key := range strings.Split(keys, \",\") {\n\t\tif key == \"\" {\n\t\t\tcontinue\n\t\t}\n\t\t// The tree key is domain*label; the token ID is label*domain.\n\t\tat := strings.Index(key, \"*\")\n\t\tif at \u003c 1 {\n\t\t\tcontinue\n\t\t}\n\t\tdomainLabel, label := key[:at], key[at+1:]\n\t\towner, registered, expires, _, _, _ := nsdata.GetNameInfo(label, domainLabel)\n\t\tfound = append(found, row{\n\t\t\ttext: ufmt.Sprintf(\"%s*%s,%d,%d,%s\", label, domainLabel, registered, expires, owner.String()),\n\t\t\tat:   registered,\n\t\t})\n\t}\n\n\t// Insertion sort, newest first. The page is at most 200 entries and\n\t// gno has no sort in scope here; anything cleverer would be more code\n\t// than the problem deserves.\n\tfor i := 1; i \u003c len(found); i++ {\n\t\tv := found[i]\n\t\tj := i - 1\n\t\tfor j \u003e= 0 \u0026\u0026 found[j].at \u003c v.at {\n\t\t\tfound[j+1] = found[j]\n\t\t\tj--\n\t\t}\n\t\tfound[j+1] = v\n\t}\n\n\tvar b strings.Builder\n\tfor _, r := range found {\n\t\tif b.Len() \u003e 0 {\n\t\t\tb.WriteString(\";\")\n\t\t}\n\t\tb.WriteString(r.text)\n\t}\n\treturn b.String(), nxt\n}\n"},{"name":"name.gno","body":"package nslogic\n\nimport (\n\t\"strings\"\n\t\"time\"\n\n\t\"gno.land/r/g1xr6tgxnpled50h74eafmvxway7z0ytr5rsmeme/nsdata/v1\"\n)\n\n// Profile field names inside a name's extension slot. The vault stores\n// them as opaque strings and bounds their size; what they MEAN is this\n// realm's convention, so new profile fields need no vault change.\nconst (\n\tkeyDisplayName = \"displayName\"\n\tkeyBio         = \"bio\"\n\tkeyWebsite     = \"website\"\n\tkeyAvatar      = \"avatar\"\n)\n\n// RegisterName mints `label*domain` under an existing domain. Anyone may\n// call this. Minting still works while the parent domain is past its own\n// due date, as long as its grace period hasn't fully lapsed and it isn't\n// frozen — nsdata's RegisterNameRecord enforces that.\nfunc RegisterName(cur realm, label, domainLabel string) {\n\tregisterName(cur, label, domainLabel, \"\")\n}\n\n// RegisterNameRef is RegisterName with a referral code — the NAME of\n// whoever sent the buyer here, as it appears in the link.\n//\n// It is a second entry point rather than a third argument on the first\n// one because the first one is already deployed and already being called\n// by wallets and by the site. Changing its arity would break every one\n// of those callers at the same instant, which is exactly the failure the\n// voting realm produced last time; adding a function breaks nothing and\n// lets the front end pick whichever the deployed realm actually has.\n//\n// A code that does not resolve — misspelt, lapsed, or the buyer's own\n// name — is IGNORED, not rejected. The registration goes through at\n// list price. Refusing to sell somebody a name because they mistyped\n// somebody else's would be an absurd way to lose a sale.\nfunc RegisterNameRef(cur realm, label, domainLabel, refName string) {\n\tregisterName(cur, label, domainLabel, refName)\n}\n\nfunc registerName(cur realm, label, domainLabel, refName string) {\n\tvalidateLabel(label)\n\tif !DomainOpen(domainLabel) {\n\t\tpanic(\"nslogic: this domain is not accepting new names\")\n\t}\n\tcaller := cur.Previous().Address()\n\n\t// Resolved BEFORE anything is written, so the tier and the discount\n\t// are read from the world the buyer was quoted in. Registering first\n\t// would change the buyer's own primary name and, on a self-referral\n\t// that slipped through, their tenure with it.\n\tref := resolveReferrer(refName, caller)\n\tprice := NamePrice(domainLabel, label)\n\tif ref != \"\" {\n\t\tif pct := tierPercent(RefDiscountRules(), TenureYears(ref)); pct \u003e 0 {\n\t\t\tprice -= price * pct / 100\n\t\t}\n\t}\n\tpayTo, cut := referralCut(ref, price)\n\ttakePaymentSplit(cur, price, payTo, cut)\n\n\tif err := nsdata.RegisterNameRecord(cross(cur), label, domainLabel, caller, time.Now().Unix(), true); err != nil {\n\t\tpanic(err)\n\t}\n\n\t// Written after the record exists, because the extension slot is\n\t// created by registration. Only recorded when the cut was actually\n\t// paid: a referrer who earned nothing on the sale has no claim on\n\t// the renewals either, and storing them would say otherwise.\n\tif ref != \"\" \u0026\u0026 cut \u003e 0 {\n\t\tsetExtra(cur, label, domainLabel, keyRef, ref.String())\n\t\tcreditReferral(cur, ref)\n\t}\n}\n\n// RenewName — same anchoring rule as RenewDomain: extends from the\n// previous due date, not from the payment date. Deliberately does not\n// call validateLabel — an already-registered name must keep renewing\n// under whatever pattern was in force when it was minted.\nfunc RenewName(cur realm, label, domainLabel string) {\n\t// No new code is accepted here and none is needed: the referrer was\n\t// fixed into this name when it was registered, and renewals keep\n\t// paying them for as long as the name survives unbroken. If it\n\t// lapses or is sold the vault destroys the record's extras, so the\n\t// link ends without this realm having to notice.\n\t//\n\t// Renewals pay the FULL price. The discount was a reason to arrive;\n\t// it is not a permanent lower rate, and compounding it every year\n\t// would make the referred name cheaper forever than the same name\n\t// bought directly.\n\tassertRoomToRenew(label, domainLabel)\n\tprice := NamePrice(domainLabel, label)\n\tref := ReferrerOf(label, domainLabel)\n\tpayTo, cut := referralCut(ref, price)\n\ttakePaymentSplit(cur, price, payTo, cut)\n\n\tcaller := cur.Previous().Address()\n\tif err := nsdata.RenewNameRecord(cross(cur), label, domainLabel, caller, time.Now().Unix()); err != nil {\n\t\tpanic(err)\n\t}\n\tif cut \u003e 0 {\n\t\tcreditReferral(cur, ref)\n\t}\n}\n\n// SetProfile writes the four conventional profile fields. Each lands in\n// the name's extension slot, where the vault enforces size bounds.\nfunc SetProfile(cur realm, label, domainLabel, displayName, bio, website, avatar string) {\n\tassertNoSend(cur)\n\tassertNameOwner(cur, label, domainLabel)\n\tsetExtra(cur, label, domainLabel, keyDisplayName, displayName)\n\tsetExtra(cur, label, domainLabel, keyBio, bio)\n\tsetExtra(cur, label, domainLabel, keyWebsite, website)\n\tsetExtra(cur, label, domainLabel, keyAvatar, avatar)\n}\n\n// SetNameField writes a single arbitrary key — cross-chain addresses and\n// anything else a future feature needs, with no vault change required.\n// Keys used by this realm's own machinery are reserved.\nfunc SetNameField(cur realm, label, domainLabel, key, value string) {\n\tassertNoSend(cur)\n\tassertNameOwner(cur, label, domainLabel)\n\tif strings.HasPrefix(key, \"_\") {\n\t\tpanic(\"nslogic: keys beginning with '_' are reserved\")\n\t}\n\t// `ref` decides who gets paid out of every renewal of this name. A\n\t// holder who could write it would set it to an address they control\n\t// and take a permanent cut of their own renewals out of the\n\t// treasury — so the one key that moves money is the one key its\n\t// owner may not touch.\n\tif key == keyRef {\n\t\tpanic(\"nslogic: '\" + keyRef + \"' is set at registration and cannot be edited\")\n\t}\n\tsetExtra(cur, label, domainLabel, key, value)\n}\n\nfunc setExtra(cur realm, label, domainLabel, key, value string) {\n\tif err := nsdata.SetNameExtraRecord(cross(cur), label, domainLabel, key, value); err != nil {\n\t\tpanic(err)\n\t}\n}\n\nfunc assertNameOwner(cur realm, label, domainLabel string) {\n\tif nsdata.GetNameOwner(label, domainLabel) != cur.Previous().Address() {\n\t\tpanic(\"nslogic: restricted to name owner\")\n\t}\n}\n\n// SetPrimaryName lets a wallet explicitly choose (and later change) which\n// of its names is used for reverse resolution.\nfunc SetPrimaryName(cur realm, label, domainLabel string) {\n\tassertNoSend(cur)\n\tcaller := cur.Previous().Address()\n\tif nsdata.GetNameOwner(label, domainLabel) != caller {\n\t\tpanic(\"nslogic: restricted to name owner\")\n\t}\n\tif err := nsdata.SetPrimaryRecord(cross(cur), caller, label, domainLabel); err != nil {\n\t\tpanic(err)\n\t}\n}\n\n/*\nClearPrimaryName switches reverse resolution OFF for the caller.\n\nWHY THIS HAS TO EXIST, and it is a hole rather than a feature request.\n\nnsdata sets the reverse pointer AUTOMATICALLY on a wallet's first\nregistration — RegisterNameRecord does it, and nothing asked the person\nfirst. From that moment their address answers with their name, so anybody\nwho sees that address anywhere can find the persona attached to it, and\nevery transaction it has ever made becomes attributable. Retroactively:\nthe chain is permanent and indexers keep copies.\n\nAnd until now there was NO WAY OUT. ClearPrimaryRecord is gated to\ntrusted logic, and no logic realm exposed it, so the one person the\npointer is about could not remove it. A name service is a bridge between\na persona and a wallet; publishing that bridge without consent and\nwithout an exit is the worst version of it.\n\nThis is the exit. It takes no arguments on purpose: a wallet has exactly\none reverse pointer, and the only address it can clear is its own, so\nthere is nothing to pass and nothing to get wrong.\n\nWHAT IT DOES NOT DO, said plainly because a promise we cannot keep would\nbe worse than the hole: clearing stops FUTURE lookups. It does not\nunpublish what was already visible, and it cannot — the history is on\nchain and other people have copies. The honest sequence is to choose\nbefore publishing, which is why the site now shows what the link exposes\nbefore it offers to make it.\n*/\nfunc ClearPrimaryName(cur realm) {\n\tassertNoSend(cur)\n\tnsdata.ClearPrimaryRecord(cross(cur), cur.Previous().Address())\n}\n\n// SetNameTTL is the owner's cache policy for their own name, in\n// seconds, where 0 MEANS NEVER CACHE — copied from the ENS registry so\n// integrators need not read anything of ours to handle it.\n//\n// The owner sets it rather than the registry because only they know the\n// risk: a name being used to receive payments wants 0, since a cached\n// answer after a sale pays the previous owner; a name used as a display\n// handle can safely be cached for a long time.\nfunc SetNameTTL(cur realm, label, domainLabel string, seconds int64) {\n\tassertNoSend(cur)\n\tassertNameOwner(cur, label, domainLabel)\n\tif seconds \u003c 0 {\n\t\tpanic(\"nslogic: ttl may not be negative\")\n\t}\n\tif err := nsdata.SetNameTTLRecord(cross(cur), label, domainLabel, seconds); err != nil {\n\t\tpanic(err)\n\t}\n}\n\nfunc FreezeName(cur realm, label, domainLabel string) {\n\tassertNoSend(cur)\n\tassertIsAdmin(cur)\n\tnsdata.SetNameFrozen(cross(cur), label, domainLabel, true)\n}\n\nfunc UnfreezeName(cur realm, label, domainLabel string) {\n\tassertNoSend(cur)\n\tassertIsAdmin(cur)\n\tnsdata.SetNameFrozen(cross(cur), label, domainLabel, false)\n}\n\n// -- resolution --\n\nfunc ResolveName(label, domainLabel string) address {\n\tif !nsdata.IsNameValid(label, domainLabel) {\n\t\tpanic(\"nslogic: name is expired\")\n\t}\n\treturn nsdata.GetNameOwner(label, domainLabel)\n}\n\n// ResolveAddress performs reverse resolution: address -\u003e name.\n//\n// It must verify the ownership it is asserting, not merely that the\n// pointed-at name exists and is unexpired. nsdata's `primary` tree is a\n// record of what an address last SELECTED, not proof of what it still\n// owns — and although nsdata now clears the pointer on transfer and\n// reclaim, this check is the half that lives in the swappable realm and\n// therefore stays correct even against pointers written by an older\n// version of the logic. Without it, an address that sold or lost a name\n// kept reverse-resolving to it, impersonating the new owner.\nfunc ResolveAddress(addr address) string {\n\ttid := nsdata.GetPrimary(addr)\n\tif tid == \"\" {\n\t\treturn \"\"\n\t}\n\tlabel, domainLabel := splitTokenID(tid)\n\tif !nsdata.IsNameValid(label, domainLabel) {\n\t\treturn \"\"\n\t}\n\tif nsdata.GetNameOwner(label, domainLabel) != addr {\n\t\treturn \"\"\n\t}\n\treturn tid\n}\n\n// splitTokenID splits a `label*domain` token ID.\nfunc splitTokenID(tid string) (label, domainLabel string) {\n\tfor i := 0; i \u003c len(tid); i++ {\n\t\tif tid[i] == '*' {\n\t\t\treturn tid[:i], tid[i+1:]\n\t\t}\n\t}\n\treturn tid, \"\"\n}\n"},{"name":"nft.gno","body":"package nslogic\n\nimport (\n\t\"strings\"\n\n\t\"gno.land/r/g1xr6tgxnpled50h74eafmvxway7z0ytr5rsmeme/nsdata/v1\"\n)\n\n// This realm only exposes the WRITE side of the GRC-721 surface. Reads\n// (BalanceOf, OwnerOf, TokenURI, GetApproved, IsApprovedForAll, and\n// every listing/status/profile query) have no authorization check to\n// perform and are served directly by nsdata — no reason to duplicate\n// them here.\n\nfunc isDomainToken(tid string) bool {\n\treturn strings.HasPrefix(tid, \"*\")\n}\n\n// isTokenValid checks the underlying domain/name's expiry via nsdata's\n// own validity reads — the anti-scam pattern (an expired name must not\n// be sellable) enforced at the transfer boundary, not just at read time.\nfunc isTokenValid(tid string) bool {\n\tif isDomainToken(tid) {\n\t\treturn nsdata.IsDomainValid(tid[1:])\n\t}\n\tlabel, domainLabel := splitTokenID(tid)\n\treturn nsdata.IsNameValid(label, domainLabel)\n}\n\nfunc Approve(cur realm, to address, tid string) {\n\tassertNoSend(cur)\n\tif isDomainToken(tid) {\n\t\tpanic(\"nslogic: domains are project-owned and non-transferable\")\n\t}\n\tif !isTokenValid(tid) {\n\t\tpanic(\"nslogic: cannot approve an expired token\")\n\t}\n\tcaller := cur.Previous().Address()\n\tif err := nsdata.RawApprove(cross(cur), caller, to, tid); err != nil {\n\t\tpanic(err)\n\t}\n}\n\nfunc SetApprovalForAll(cur realm, operator address, approved bool) {\n\tassertNoSend(cur)\n\tcaller := cur.Previous().Address()\n\tif err := nsdata.RawSetApprovalForAll(cross(cur), caller, operator, approved); err != nil {\n\t\tpanic(err)\n\t}\n}\n\n// TransferFrom / SafeTransferFrom are the actual enforcement point for\n// both (a) the anti-scam pattern — blocking transfer of an expired token\n// closes off third-party marketplace sales without blocking the owner's\n// own ability to reclaim by renewing (renewal never goes through these)\n// — and (b) the domains-are-never-transferable policy: blocking it here,\n// not just at RegisterDomain, is what actually closes the loophole.\n// (ReassignDomain in domain.gno is the deliberate admin-only exception;\n// see its comment.)\nfunc TransferFrom(cur realm, from, to address, tid string) {\n\tassertNoSend(cur)\n\tif isDomainToken(tid) {\n\t\tpanic(\"nslogic: domains are project-owned and non-transferable\")\n\t}\n\tif !isTokenValid(tid) {\n\t\tpanic(\"nslogic: cannot transfer an expired token\")\n\t}\n\tlabel, domainLabel := splitTokenID(tid)\n\tcaller := cur.Previous().Address()\n\t// A free-tier record has no GRC-721 token to move, so the token-layer\n\t// transfer would fail; the vault exposes a record-only path for it.\n\tif !nsdata.IsNameNFT(label, domainLabel) {\n\t\tif caller != from || nsdata.GetNameOwner(label, domainLabel) != caller {\n\t\t\tpanic(\"nslogic: restricted to name owner\")\n\t\t}\n\t\tif err := nsdata.TransferRecordOwner(cross(cur), label, domainLabel, to); err != nil {\n\t\t\tpanic(err)\n\t\t}\n\t\treturn\n\t}\n\tif err := nsdata.RawTransferNFT(cross(cur), caller, from, to, tid); err != nil {\n\t\tpanic(err)\n\t}\n}\n\n// nsdata only exposes one raw transfer primitive (RawTransferNFT, backed\n// by grc721's plain TransferFrom) — the safe-receiver-interface check\n// grc721's SafeTransferFrom adds doesn't carry much weight for a\n// name-service NFT, so both entry points converge on the same path here.\nfunc SafeTransferFrom(cur realm, from, to address, tid string) {\n\tTransferFrom(cur, from, to, tid)\n}\n"},{"name":"payment.gno","body":"package nslogic\n\nimport (\n\t\"chain\"\n\t\"chain/banker\"\n\t\"chain/runtime/unsafe\"\n\n\t\"gno.land/p/nt/ufmt/v0\"\n\t\"gno.land/r/g1xr6tgxnpled50h74eafmvxway7z0ytr5rsmeme/nsdata/v1\"\n)\n\n// takePayment verifies the attached send-envelope covers `price` ugnot,\n// forwards the price to the treasury, and refunds any overpayment to the\n// caller in the same transaction. Nothing is ever retained: every ugnot\n// that arrives leaves again, as either payment or refund.\n//\n// It used to take a `beneficiary` and split the price fee/remainder,\n// routing the remainder to treasury only while `beneficiary == admin`.\n// That comparison was a proxy for \"is this a project-owned domain\", and\n// it silently broke the moment `admin` rotated — the planned EOA-to-\n// multisig step — because a domain's owner is frozen into its record at\n// registration time. After a rotation, every registration's remainder\n// would have been paid to the retired key instead of the treasury, with\n// no error and no log line.\n//\n// The heuristic is gone rather than patched. Domains are admin-only to\n// create and permanently non-transferable, so every domain is\n// project-owned by construction and 100% of every payment belongs to the\n// treasury. `platformFeeBps` stays in nsdata's config for a future logic\n// realm that reintroduces third-party domain ownership; reinstating the\n// split is then a logic-realm swap, not a migration.\nfunc takePayment(cur realm, price int64) {\n\ttakePaymentSplit(cur, price, \"\", 0)\n}\n\n// takePaymentSplit is takePayment with a referrer's cut carved out of the\n// same price. The buyer pays exactly what they were quoted; the cut is\n// not added on top, it is a share of what the treasury would otherwise\n// have received.\n//\n// All three legs settle inside the buyer's own transaction — treasury,\n// referrer, refund — so this realm's balance is the same after the call\n// as before it. That is not a stylistic preference: holding a referrer's\n// cut until they claim it would make this an account you can have a\n// balance in, and that is a different kind of thing to operate, subject\n// to a different set of rules, run by people with licences. The cut is\n// paid now or it is not paid.\nfunc takePaymentSplit(cur realm, price int64, cutTo address, cut int64) {\n\tif !cur.Previous().IsUserCall() {\n\t\tpanic(\"nslogic: only direct wallet calls may attach payment\")\n\t}\n\tcaller := cur.Previous().Address()\n\tsent := unsafe.OriginSend()\n\tassertOnlyUgnot(sent)\n\n\tpaid := sent.AmountOf(ugnot)\n\tif paid \u003c price {\n\t\tpanic(ufmt.Sprintf(\"nslogic: insufficient payment: need %d ugnot, got %d\", price, paid))\n\t}\n\texcess := paid - price\n\n\t// Belt and braces. referralCut already caps the percentage, but this\n\t// is the line where the arithmetic becomes money leaving an address,\n\t// and a bug upstream must not be able to pay out more than came in.\n\tif cut \u003c 0 || cut \u003e price || cutTo == \"\" {\n\t\tcut = 0\n\t}\n\n\tb := banker.NewBanker(banker.BankerTypeRealmSend, cur)\n\tpkgAddr := cur.Address()\n\tif cut \u003e 0 {\n\t\tb.SendCoins(pkgAddr, cutTo, chain.Coins{chain.NewCoin(ugnot, cut)})\n\t}\n\tif price-cut \u003e 0 {\n\t\tb.SendCoins(pkgAddr, nsdata.GetTreasury(), chain.Coins{chain.NewCoin(ugnot, price-cut)})\n\t}\n\tif excess \u003e 0 {\n\t\tb.SendCoins(pkgAddr, caller, chain.Coins{chain.NewCoin(ugnot, excess)})\n\t}\n}\n\n// assertOnlyUgnot rejects any other denomination outright. Only the\n// ugnot amount was ever accounted for, and neither realm has a\n// withdrawal path, so a foreign coin sent alongside a payment would have\n// been stranded in this realm's address permanently. Refusing the whole\n// transaction is simpler than sweeping it back and keeps the\n// \"this realm never retains funds\" invariant absolute.\nfunc assertOnlyUgnot(sent chain.Coins) {\n\tfor _, c := range sent {\n\t\tif c.Denom != ugnot {\n\t\t\tpanic(\"nslogic: only ugnot is accepted, got: \" + c.Denom)\n\t\t}\n\t}\n}\n\n// assertNoSend guards every entry point that has no price. Without it,\n// coins attached to a free call (a renewal of a project domain, a\n// profile edit, an admin setter) would land in this realm's address with\n// no way back out.\n//\n// IT APPLIES TO DIRECT WALLET CALLS ONLY, and that qualification is not\n// a loosening — it is the guard finally matching its own reason.\n//\n// OriginSend() reports what the TRANSACTION carried, not what this call\n// received. Measured on sapphire-1 with a pair of probe realms: a user\n// attaches 500000ugnot to realm A, A calls realm B, and B's OriginSend()\n// reads 500000ugnot even though every one of those coins is sitting in\n// A's address. B never saw them and cannot strand them.\n//\n// So for a realm-to-realm call this was rejecting a payment this realm\n// never received. It is the reason a marketplace could not transfer a\n// name inside the same transaction that pays for it — the transfer is a\n// free call, the transaction is not, and the two are unrelated. A\n// two-transaction sale is the alternative, and that means somebody's\n// money or somebody's name sitting somewhere in between. Not here.\nfunc assertNoSend(cur realm) {\n\tif !cur.Previous().IsUserCall() {\n\t\treturn // the coins went to whoever called us, not to us\n\t}\n\tif !unsafe.OriginSend().IsZero() {\n\t\tpanic(\"nslogic: this function takes no payment; do not attach coins\")\n\t}\n}\n"},{"name":"premium.gno","body":"package nslogic\n\nimport (\n\t\"strconv\"\n\t\"strings\"\n\n\t\"gno.land/r/g1xr6tgxnpled50h74eafmvxway7z0ytr5rsmeme/nsdata/v1\"\n)\n\n// Global pricing layers that sit ACROSS every domain rather than inside\n// one — see SPEC.md §27.\n//\n// Two of them, and they work at different points:\n//\n//   GLOBAL LENGTH RULES are a FLOOR beneath each domain's own length\n//   rules, consulted only when the domain has nothing to say about that\n//   length. \"three-character names cost $5 everywhere\" becomes one entry\n//   instead of an edit to all twenty-seven domains, and a domain that\n//   wants its own answer still overrides it.\n//\n//   GLOBAL NAME PREMIUMS are an ADJUSTMENT applied after the price has\n//   already resolved. Making `og` premium is one entry covering\n//   `og*degen`, `og*meme` and every domain invented later. Deliberately\n//   not the per-name override, which REPLACES a price outright: \"this\n//   name costs exactly X here\" and \"this name is worth more everywhere\"\n//   are different tools and both are worth having.\n//\n// Full order, most specific first:\n//\n//   1. per-name override      (domain \"nm\")\n//   2. per-domain length rule (domain \"len\")\n//   3. global length rule     (global \"glen\")\n//   4. domain base price      (domain \"price\")\n//   5. global default         (global \"defaultPrice\")\n//   then the global name premium modifies the result,\n//   then any referral discount comes off what is left.\nconst (\n\tkeyGlobalLens = \"glen\"    // global length rules\n\tkeyPremium    = \"premium\" // premium1, premium2, ... see below\n\n\t// The vault caps an extension value at maxExtraValueLen (512 bytes),\n\t// which is roughly 40-60 premium entries. The list is therefore split\n\t// across numbered keys rather than pretending one will do; a list\n\t// that silently truncated at the 512th byte would drop exactly the\n\t// entries nobody checked.\n\tmaxPremiumKeys = 8\n)\n\n// GlobalLengthRule returns the global rule for a label length, if any.\nfunc GlobalLengthRule(n int) (string, bool) {\n\treturn lookupRule(nsdata.GetGlobal(keyGlobalLens), strconv.Itoa(n))\n}\n\n// PremiumFor returns the raw modifier for a name across all premium\n// keys, or \"\" when the name is not premium anywhere.\nfunc PremiumFor(label string) string {\n\tfor i := 1; i \u003c= maxPremiumKeys; i++ {\n\t\tcsv := nsdata.GetGlobal(keyPremium + strconv.Itoa(i))\n\t\tif csv == \"\" {\n\t\t\tcontinue\n\t\t}\n\t\tif v, ok := lookupRule(csv, label); ok {\n\t\t\treturn v\n\t\t}\n\t}\n\treturn \"\"\n}\n\n// applyPremium adjusts an already-resolved price.\n//\n// ZERO ALWAYS STAYS ZERO. A fixed \"+$2.00\" would otherwise silently undo\n// a giveaway tier set months earlier, and a free tier that quietly stops\n// being free is worse than never having offered one. This is decided\n// explicitly rather than inherited from the percentage case happening to\n// work out at zero.\n//\n// Forms: \"%5\" / \"+%5\" five percent more · \"-%10\" ten percent less ·\n// \"2.00\" two dollars more · \"-0.25\" twenty-five cents less ·\n// \"=5.00\" exactly five dollars, ignoring everything resolved above.\nfunc applyPremium(mod string, price int64) int64 {\n\tmod = strings.TrimSpace(mod)\n\tif mod == \"\" || price \u003c= 0 {\n\t\treturn price\n\t}\n\n\t// \"=X\" replaces outright. Checked before the sign handling because\n\t// an exact price is not a delta and has no direction.\n\tif strings.HasPrefix(mod, \"=\") {\n\t\tv, ok := parseUSD(mod[1:])\n\t\tif !ok || v \u003c 0 {\n\t\t\treturn price\n\t\t}\n\t\treturn v\n\t}\n\n\tneg := false\n\tswitch {\n\tcase strings.HasPrefix(mod, \"-\"):\n\t\tneg, mod = true, mod[1:]\n\tcase strings.HasPrefix(mod, \"+\"):\n\t\tmod = mod[1:]\n\t}\n\n\tvar delta int64\n\tif strings.HasPrefix(mod, \"%\") {\n\t\tpct, err := strconv.ParseInt(mod[1:], 10, 64)\n\t\tif err != nil || pct \u003c 0 {\n\t\t\treturn price\n\t\t}\n\t\tdelta = price * pct / 100\n\t} else {\n\t\tv, ok := parseUSD(mod)\n\t\tif !ok || v \u003c 0 {\n\t\t\treturn price\n\t\t}\n\t\tdelta = v\n\t}\n\n\tif neg {\n\t\t// A discount may take a name to free, but never below it: a\n\t\t// negative price would be a payout, and this realm never pays\n\t\t// anybody to register.\n\t\tif delta \u003e= price {\n\t\t\treturn 0\n\t\t}\n\t\treturn price - delta\n\t}\n\treturn price + delta\n}\n\n// parseUSD reads a human dollar figure — \"2\", \"2.5\", \"2.50\", \"0.069\" —\n// into micro-USD. Written by hand rather than with floats: a float would\n// make \"0.07\" cost a different number of micro-dollars depending on the\n// platform, and prices must be exact.\nfunc parseUSD(s string) (int64, bool) {\n\ts = strings.TrimSpace(s)\n\tif s == \"\" {\n\t\treturn 0, false\n\t}\n\twhole, frac := s, \"\"\n\tif i := strings.IndexByte(s, '.'); i \u003e= 0 {\n\t\twhole, frac = s[:i], s[i+1:]\n\t}\n\tif whole == \"\" {\n\t\twhole = \"0\"\n\t}\n\tw, err := strconv.ParseInt(whole, 10, 64)\n\tif err != nil || w \u003c 0 {\n\t\treturn 0, false\n\t}\n\t// Pad or truncate the fraction to exactly six digits of micro-USD.\n\tif len(frac) \u003e 6 {\n\t\tfrac = frac[:6]\n\t}\n\tfor len(frac) \u003c 6 {\n\t\tfrac += \"0\"\n\t}\n\tvar f int64\n\tif frac != \"\" {\n\t\tf, err = strconv.ParseInt(frac, 10, 64)\n\t\tif err != nil || f \u003c 0 {\n\t\t\treturn 0, false\n\t\t}\n\t}\n\treturn w*microUSD + f, true\n}\n\n// normalizePremiums validates a premium list before storage. Same\n// reasoning as normalizeRules: a malformed entry must never reach the\n// vault, because every later lookup would carry it.\nfunc normalizePremiums(csv string) string {\n\tif strings.TrimSpace(csv) == \"\" {\n\t\treturn \"\"\n\t}\n\tvar out []string\n\tseen := map[string]bool{}\n\tfor _, pair := range strings.Split(csv, \",\") {\n\t\tpair = strings.TrimSpace(pair)\n\t\tif pair == \"\" {\n\t\t\tcontinue\n\t\t}\n\t\tparts := strings.SplitN(pair, \":\", 2)\n\t\tif len(parts) != 2 {\n\t\t\tpanic(\"nslogic: bad premium, want name:modifier — got \" + pair)\n\t\t}\n\t\tk, v := strings.TrimSpace(parts[0]), strings.TrimSpace(parts[1])\n\t\tif k == \"\" || strings.ContainsAny(k, \"*, \\t\\n\") {\n\t\t\tpanic(\"nslogic: invalid name in premium — \" + k)\n\t\t}\n\t\tif seen[k] {\n\t\t\tpanic(\"nslogic: duplicate premium name — \" + k)\n\t\t}\n\t\tif !validPremiumValue(v) {\n\t\t\tpanic(\"nslogic: bad premium modifier for \" + k + \" — \" + v)\n\t\t}\n\t\tseen[k] = true\n\t\tout = append(out, k+\":\"+v)\n\t}\n\treturn strings.Join(out, \",\")\n}\n\nfunc validPremiumValue(v string) bool {\n\tif v == \"\" {\n\t\treturn false\n\t}\n\tif strings.HasPrefix(v, \"=\") {\n\t\t_, ok := parseUSD(v[1:])\n\t\treturn ok\n\t}\n\tif strings.HasPrefix(v, \"-\") || strings.HasPrefix(v, \"+\") {\n\t\tv = v[1:]\n\t}\n\tif strings.HasPrefix(v, \"%\") {\n\t\tp, err := strconv.ParseInt(v[1:], 10, 64)\n\t\treturn err == nil \u0026\u0026 p \u003e= 0\n\t}\n\t_, ok := parseUSD(v)\n\treturn ok\n}\n"},{"name":"pricing.gno","body":"package nslogic\n\nimport (\n\t\"strconv\"\n\t\"strings\"\n\n\t\"gno.land/r/g1xr6tgxnpled50h74eafmvxway7z0ytr5rsmeme/nsdata/v1\"\n)\n\n// Pricing lives here, not in the vault. The vault stores numbers as\n// opaque strings and has no opinion about what they mean, so the whole\n// model can be replaced by swapping this realm.\n//\n// PRICES ARE DENOMINATED IN USD, held as MICRO-USD (1 USD = 1_000_000)\n// so fractions of a cent are expressible — a name that costs about a\n// gumball needs that. Nothing is stored in ugnot: the chain's token is\n// just the settlement rail, converted at payment time from a stored\n// rate. Accepting a second token later is then only a second rate, not\n// a second price list.\n//\n// PRECEDENCE — most specific wins:\n//\n//   1. per-name override      (keyNames: \"420:5000000,gm:%500\")\n//   2. per-length rule        (keyLens:  \"3:2000000,4:%150,5:0\")\n//   3. GLOBAL length rule     (global keyGlobalLens — see premium.gno)\n//   4. domain base price      (keyPrice: \"500000\"  = $0.50)\n//   5. global default         (global keyDefaultPrice)\n//\n// then the GLOBAL NAME PREMIUM adjusts the result, and a referral\n// discount comes off last. Both live in premium.gno.\n//\n// A rule value is either an absolute micro-USD amount or \"%N\", meaning N\n// percent of that domain's base — so repricing a domain once carries\n// every relative rule under it. \"0\" means free, which is how giveaway\n// tiers are expressed: \"all five-character names under *pleb are free\"\n// is just a length rule resolving to zero.\n//\n// Rules are stored as CSV in ONE key each rather than one key per rule.\n// The vault caps extension keys at 32 bytes and 24 entries per record; a\n// key like \"nm:somelongname\" breaks the first and a few dozen premium\n// names break the second. Two CSV keys sidestep both.\nconst (\n\tkeyPrice        = \"price\" // domain base, micro-USD\n\tkeyLens         = \"len\"   // length rules\n\tkeyNames        = \"nm\"    // per-name overrides\n\tkeyOpen         = \"open\"  // \"0\" closes the domain to new names\n\tkeyDefaultPrice = \"defaultPrice\"\n\tkeyRate         = \"rateUgnotPerUsd\" // ugnot equal to 1 USD\n\n\tmicroUSD          = int64(1_000_000)\n\tfallbackPriceUSD  = int64(500_000) // $0.50\n\t// TESTNET PEG: $0.069 per GNOT, so 1 USD = 1/0.069 GNOT = 14.492753\n\t// GNOT = 14_492_753 ugnot. Deliberately a fixed number while we are on\n\t// a testnet whose token has no market price at all -- a \"real\" rate\n\t// there would be fiction dressed as precision. Replaced with a genuine\n\t// conversion before mainnet; tracked in MAINNET.md.\n\tfallbackRate      = int64(14_492_753)\n\tmaxRules          = 64\n)\n\n// DomainOpen reports whether names may currently be registered under a\n// domain. An absent key means open — a domain created before this\n// existed must not silently close.\nfunc DomainOpen(domainLabel string) bool {\n\treturn nsdata.GetDomainExtra(domainLabel, keyOpen) != \"0\"\n}\n\n// -- USD side --\n\nfunc DefaultPriceUSD() int64 {\n\tif v := nsdata.GetGlobal(keyDefaultPrice); v != \"\" {\n\t\tif p, err := strconv.ParseInt(v, 10, 64); err == nil \u0026\u0026 p \u003e= 0 {\n\t\t\treturn p\n\t\t}\n\t}\n\treturn fallbackPriceUSD\n}\n\nfunc DomainBasePriceUSD(domainLabel string) int64 {\n\tif v := nsdata.GetDomainExtra(domainLabel, keyPrice); v != \"\" {\n\t\tif p, err := strconv.ParseInt(v, 10, 64); err == nil \u0026\u0026 p \u003e= 0 {\n\t\t\treturn p\n\t\t}\n\t}\n\treturn DefaultPriceUSD()\n}\n\n// NamePriceUSD resolves the four inputs in precedence order and returns\n// micro-USD. This is the authoritative price; ugnot is derived from it.\nfunc NamePriceUSD(domainLabel, label string) int64 {\n\treturn applyPremium(PremiumFor(label), resolveBeforePremium(domainLabel, label))\n}\n\n// resolveBeforePremium runs steps 1-5. Separated from NamePriceUSD so\n// the admin panel can show WHY a price is what it is: the figure before\n// the premium and the figure after are both meaningful, and a single\n// function returning only the total makes a wrong price impossible to\n// diagnose.\nfunc resolveBeforePremium(domainLabel, label string) int64 {\n\tbase := DomainBasePriceUSD(domainLabel)\n\tif v, ok := lookupRule(nsdata.GetDomainExtra(domainLabel, keyNames), label); ok {\n\t\treturn applyRule(v, base)\n\t}\n\tif v, ok := lookupRule(nsdata.GetDomainExtra(domainLabel, keyLens), strconv.Itoa(len(label))); ok {\n\t\treturn applyRule(v, base)\n\t}\n\t// The global length rule is a floor, not a replacement: it applies\n\t// only where the domain itself had nothing to say about this length.\n\tif v, ok := GlobalLengthRule(len(label)); ok {\n\t\treturn applyRule(v, base)\n\t}\n\treturn base\n}\n\n// ExplainPriceUSD returns each stage of the resolution, so the control\n// panel can show the arithmetic rather than assert a total. Returns the\n// price before any premium, the premium modifier that applied (empty if\n// none), and the final price.\nfunc ExplainPriceUSD(domainLabel, label string) (beforePremium int64, modifier string, final int64) {\n\tbefore := resolveBeforePremium(domainLabel, label)\n\tmod := PremiumFor(label)\n\treturn before, mod, applyPremium(mod, before)\n}\n\n// -- settlement side --\n\n// RateUgnotPerUsd is how many ugnot one USD buys.\n//\n// This is a MANUALLY MAINTAINED rate, not a market oracle: an admin\n// writes it and it sits there until rewritten. That is a real\n// operational hazard — a stale rate during a sharp move sells names at\n// the wrong price in whichever direction hurts — and the mitigation is\n// keeping it fresh, not anything this code can do. Kept as one global so\n// a future logic realm can swap in a real oracle without touching the\n// vault or restating a single price.\nfunc RateUgnotPerUsd() int64 {\n\tif v := nsdata.GetGlobal(keyRate); v != \"\" {\n\t\tif r, err := strconv.ParseInt(v, 10, 64); err == nil \u0026\u0026 r \u003e 0 {\n\t\t\treturn r\n\t\t}\n\t}\n\treturn fallbackRate\n}\n\n// NamePrice converts the USD price to ugnot for settlement. Rounds UP,\n// so rounding never sells a name below its listed price; a free name\n// stays exactly free.\nfunc NamePrice(domainLabel, label string) int64 {\n\tusd := NamePriceUSD(domainLabel, label)\n\tif usd \u003c= 0 {\n\t\treturn 0\n\t}\n\trate := RateUgnotPerUsd()\n\treturn (usd*rate + microUSD - 1) / microUSD\n}\n\n// -- rule plumbing --\n\nfunc applyRule(v string, base int64) int64 {\n\tif strings.HasPrefix(v, \"%\") {\n\t\tpct, err := strconv.ParseInt(v[1:], 10, 64)\n\t\tif err != nil || pct \u003c 0 {\n\t\t\treturn base\n\t\t}\n\t\treturn base * pct / 100\n\t}\n\tn, err := strconv.ParseInt(v, 10, 64)\n\tif err != nil || n \u003c 0 {\n\t\treturn base\n\t}\n\treturn n\n}\n\nfunc lookupRule(csv, want string) (string, bool) {\n\tif csv == \"\" {\n\t\treturn \"\", false\n\t}\n\tfor _, pair := range strings.Split(csv, \",\") {\n\t\tparts := strings.SplitN(strings.TrimSpace(pair), \":\", 2)\n\t\tif len(parts) != 2 {\n\t\t\tcontinue\n\t\t}\n\t\tif strings.TrimSpace(parts[0]) == want {\n\t\t\treturn strings.TrimSpace(parts[1]), true\n\t\t}\n\t}\n\treturn \"\", false\n}\n\n// normalizeRules validates and canonicalises a rule string before it is\n// stored, so a malformed entry can never reach storage and poison every\n// later lookup.\nfunc normalizeRules(csv string, numericKeys bool) string {\n\tif strings.TrimSpace(csv) == \"\" {\n\t\treturn \"\"\n\t}\n\tvar out []string\n\tseen := map[string]bool{}\n\tfor _, pair := range strings.Split(csv, \",\") {\n\t\tpair = strings.TrimSpace(pair)\n\t\tif pair == \"\" {\n\t\t\tcontinue\n\t\t}\n\t\tparts := strings.SplitN(pair, \":\", 2)\n\t\tif len(parts) != 2 {\n\t\t\tpanic(\"nslogic: bad rule, want key:value — got \" + pair)\n\t\t}\n\t\tk, v := strings.TrimSpace(parts[0]), strings.TrimSpace(parts[1])\n\t\tif k == \"\" {\n\t\t\tpanic(\"nslogic: empty rule key\")\n\t\t}\n\t\tif numericKeys {\n\t\t\tif n, err := strconv.Atoi(k); err != nil || n \u003c= 0 {\n\t\t\t\tpanic(\"nslogic: length must be a positive number — got \" + k)\n\t\t\t}\n\t\t} else if strings.ContainsAny(k, \"*, \\t\\n\") {\n\t\t\tpanic(\"nslogic: invalid name in rule — \" + k)\n\t\t}\n\t\tif strings.HasPrefix(v, \"%\") {\n\t\t\tif p, err := strconv.ParseInt(v[1:], 10, 64); err != nil || p \u003c 0 {\n\t\t\t\tpanic(\"nslogic: bad percentage — \" + v)\n\t\t\t}\n\t\t} else if n, err := strconv.ParseInt(v, 10, 64); err != nil || n \u003c 0 {\n\t\t\tpanic(\"nslogic: bad amount — \" + v)\n\t\t}\n\t\tif seen[k] {\n\t\t\tpanic(\"nslogic: duplicate rule key — \" + k)\n\t\t}\n\t\tseen[k] = true\n\t\tout = append(out, k+\":\"+v)\n\t\tif len(out) \u003e maxRules {\n\t\t\tpanic(\"nslogic: too many rules (max 64)\")\n\t\t}\n\t}\n\treturn strings.Join(out, \",\")\n}\n"},{"name":"referral.gno","body":"package nslogic\n\nimport (\n\t\"strconv\"\n\t\"strings\"\n\n\t\"gno.land/r/g1xr6tgxnpled50h74eafmvxway7z0ytr5rsmeme/nsdata/v1\"\n)\n\n// The referral programme.\n//\n// SINGLE LEVEL, ALWAYS. One buyer, one referrer, one cut, taken out of\n// an actual sale. There is no downline, no override on somebody else's\n// referrals, and no bonus for recruiting other referrers — and there is\n// no code here that could be extended into one without being obvious\n// about it. That is the line between an affiliate programme and the\n// other thing, and this realm stays on the near side of it.\n//\n// WHERE THE LINK LIVES. The referrer is recorded on the REFERRED NAME,\n// in its extension slot, at the moment it is registered. That placement\n// does the work of two rules for free, because nsdata already destroys a\n// name's extras when a lapsed name is re-registered (names.Remove, then\n// a fresh record) and when a name changes hands (wipeOnHandover):\n//\n//\ta name that lapses loses its referrer\n//\ta name that is sold loses its referrer\n//\n// Neither needs code to enforce. Both would need code to PREVENT.\n//\n// WHO GETS PAID. An address, not a name. The referrer's wallet is fixed\n// into the referred name at registration, so they may let their own name\n// go, buy a different one, or hold ten — the commission does not move.\n// What they cannot do is leave: paying out requires the referrer to\n// still hold at least one name, checked at payment time.\n//\n// A SKIPPED PAYOUT IS NOT A DEBT. If the referrer holds nothing when a\n// renewal comes through, the cut is simply not taken — the buyer pays\n// less and the treasury keeps the difference. Nothing accrues, because\n// accruing would mean holding somebody else's money until they qualify,\n// and this realm holds nobody's money at any point.\n//\n// SELF-REFERRAL IS REFUSED. Otherwise the informed apply their own code\n// forever and the cut becomes a silent discount for people who read the\n// source.\n//\n// EVERYTHING SETTLES IN ONE TRANSACTION. Buyer pays, treasury is paid,\n// referrer is paid, overpayment goes back — all inside the message the\n// buyer signed. No escrow, no float, not for a block.\nconst (\n\tkeyRef     = \"ref\"     // on a NAME: the referrer's address\n\tkeyRefCut  = \"refcut\"  // global: tier table, referrer's percentage\n\tkeyRefDisc = \"refdisc\" // global: tier table, buyer's discount\n\tkeyRefStat = \"ref:\"    // global, per address: how many referred\n\tkeyRefWin  = \"refwin\"  // global: days a captured code stays valid\n\n\tmaxRefPercent = 50 // a cut plus a discount above this stops being a business\n)\n\n// -- tier tables --\n\n// A tier table reads like the length rules do: \"0:5,2:8,5:12\" means five\n// percent from the start, eight once the referrer has held a name two\n// years, twelve at five. The highest tier at or below their tenure wins,\n// so gaps are fine and the table never has to be exhaustive. An empty\n// table means the programme pays nothing, which is also how it is\n// switched off.\nfunc tierPercent(spec string, years int64) int64 {\n\tbest := int64(0)\n\tbestAt := int64(-1) // the threshold of the tier `best` came from\n\tfor _, part := range strings.Split(spec, \",\") {\n\t\tkv := strings.SplitN(strings.TrimSpace(part), \":\", 2)\n\t\tif len(kv) != 2 {\n\t\t\tcontinue\n\t\t}\n\t\tat, err1 := strconv.ParseInt(strings.TrimSpace(kv[0]), 10, 64)\n\t\tpct, err2 := strconv.ParseInt(strings.TrimSpace(kv[1]), 10, 64)\n\t\tif err1 != nil || err2 != nil || at \u003c 0 || pct \u003c 0 {\n\t\t\tcontinue\n\t\t}\n\t\tif pct \u003e maxRefPercent {\n\t\t\tpct = maxRefPercent\n\t\t}\n\t\t/* THE HIGHEST TIER THAT APPLIES, not the biggest number in the\n\t\t   table. This compared percentages, so a table where a later\n\t\t   tier pays LESS — the shape a taper needs, and the shape the\n\t\t   buyer-discount table is most likely to take — silently paid\n\t\t   the earlier, larger rate forever. Tracking the tier's\n\t\t   threshold instead makes the table mean what it reads as. */\n\t\tif years \u003e= at \u0026\u0026 at \u003e= bestAt {\n\t\t\tbestAt = at\n\t\t\tbest = pct\n\t\t}\n\t}\n\treturn best\n}\n\nfunc RefCutRules() string      { return nsdata.GetGlobal(keyRefCut) }\nfunc RefDiscountRules() string { return nsdata.GetGlobal(keyRefDisc) }\n\n// RefWindowDays is how long a clicked link keeps counting for. It is\n// enforced by the browser that captured the code, not by this realm —\n// the chain never sees the click, only the registration that quotes a\n// code — so it is stored here purely to be ONE number rather than a\n// constant baked separately into the site, the FAQ and the rules page.\n// Zero means the default; the site treats an absent value the same way.\nfunc RefWindowDays() int64 {\n\td := parseInt64(nsdata.GetGlobal(keyRefWin))\n\tif d \u003c= 0 {\n\t\treturn 30\n\t}\n\treturn d\n}\n\nfunc SetReferralWindow(cur realm, days int64) {\n\tassertNoSend(cur)\n\tassertIsAdmin(cur)\n\tif days \u003c 0 || days \u003e 3650 {\n\t\tpanic(\"nslogic: window must be 0-3650 days\")\n\t}\n\tsetGlobal(cur, keyRefWin, strconv.FormatInt(days, 10))\n}\n\n// ReferralQuote is what a code is worth, answered before anybody signs\n// anything. One call rather than four, because the site needs all of it\n// at once to show a price and every extra round trip is a moment where\n// the number on screen is not the number the chain will charge.\n//\n// A referrer of \"\" means the code did not resolve, and the two\n// percentages are then both zero.\nfunc ReferralQuote(refName string, buyer address) (referrer address, tenure, discountPct, cutPct int64) {\n\tref := resolveReferrer(refName, buyer)\n\tif ref == \"\" {\n\t\treturn \"\", 0, 0, 0\n\t}\n\tyears := TenureYears(ref)\n\tcut := int64(0)\n\t// The cut is zero for a referrer holding nothing, and saying so up\n\t// front is better than showing a percentage that silently will not\n\t// be paid when the transaction lands.\n\tif nsdata.BalanceOf(ref) \u003e 0 {\n\t\tcut = tierPercent(RefCutRules(), years)\n\t}\n\treturn ref, years, tierPercent(RefDiscountRules(), years), cut\n}\n\n// SetReferralRules replaces both tier tables at once, because setting one\n// without the other is how a cut ends up larger than the margin. Passing\n// two empty strings turns the programme off: no discount, no commission,\n// and registrations stop writing a referrer at all.\nfunc SetReferralRules(cur realm, cutSpec, discountSpec string) {\n\tassertNoSend(cur)\n\tassertIsAdmin(cur)\n\tcut := normalizeTiers(cutSpec)\n\tdisc := normalizeTiers(discountSpec)\n\t// The two are paid out of the same list price, so their sum is what\n\t// actually leaves the treasury. Checked at every tenure either table\n\t// mentions rather than only at tier zero, since the tables need not\n\t// step at the same points.\n\tfor _, y := range tierYears(cut, disc) {\n\t\tif tierPercent(cut, y)+tierPercent(disc, y) \u003e maxRefPercent {\n\t\t\tpanic(\"nslogic: cut plus discount exceeds 50% at year \" + strconv.FormatInt(y, 10))\n\t\t}\n\t}\n\tsetGlobal(cur, keyRefCut, cut)\n\tsetGlobal(cur, keyRefDisc, disc)\n}\n\nfunc tierYears(specs ...string) []int64 {\n\tvar out []int64\n\tseen := map[int64]bool{}\n\tfor _, spec := range specs {\n\t\tfor _, part := range strings.Split(spec, \",\") {\n\t\t\tkv := strings.SplitN(strings.TrimSpace(part), \":\", 2)\n\t\t\tif len(kv) != 2 {\n\t\t\t\tcontinue\n\t\t\t}\n\t\t\tif y, err := strconv.ParseInt(strings.TrimSpace(kv[0]), 10, 64); err == nil \u0026\u0026 !seen[y] {\n\t\t\t\tseen[y] = true\n\t\t\t\tout = append(out, y)\n\t\t\t}\n\t\t}\n\t}\n\tif len(out) == 0 {\n\t\tout = append(out, 0)\n\t}\n\treturn out\n}\n\n// normalizeTiers rejects a malformed table before it is stored, for the\n// same reason normalizeRules does: a bad entry that reaches storage is\n// then read silently and wrongly on every sale afterwards.\nfunc normalizeTiers(spec string) string {\n\tif strings.TrimSpace(spec) == \"\" {\n\t\treturn \"\"\n\t}\n\tvar out []string\n\tseen := map[int64]bool{}\n\tfor _, part := range strings.Split(spec, \",\") {\n\t\tpart = strings.TrimSpace(part)\n\t\tif part == \"\" {\n\t\t\tcontinue\n\t\t}\n\t\tkv := strings.SplitN(part, \":\", 2)\n\t\tif len(kv) != 2 {\n\t\t\tpanic(\"nslogic: bad tier, want years:percent — got \" + part)\n\t\t}\n\t\ty, err := strconv.ParseInt(strings.TrimSpace(kv[0]), 10, 64)\n\t\tif err != nil || y \u003c 0 || y \u003e 100 {\n\t\t\tpanic(\"nslogic: tier years must be 0-100 — got \" + kv[0])\n\t\t}\n\t\tp, err := strconv.ParseInt(strings.TrimSpace(kv[1]), 10, 64)\n\t\tif err != nil || p \u003c 0 || p \u003e maxRefPercent {\n\t\t\tpanic(\"nslogic: tier percent must be 0-50 — got \" + kv[1])\n\t\t}\n\t\tif seen[y] {\n\t\t\tpanic(\"nslogic: duplicate tier year — \" + kv[0])\n\t\t}\n\t\tseen[y] = true\n\t\tout = append(out, strconv.FormatInt(y, 10)+\":\"+strconv.FormatInt(p, 10))\n\t\tif len(out) \u003e 16 {\n\t\t\tpanic(\"nslogic: too many tiers (max 16)\")\n\t\t}\n\t}\n\treturn strings.Join(out, \",\")\n}\n\n// -- tenure --\n\n// TenureYears is how long a wallet counts as having been here, measured\n// from its PRIMARY name and nothing else.\n//\n// The honest alternative — the oldest name the wallet holds — means\n// walking the whole registry on every quote, which is a read that gets\n// slower every time somebody registers and eventually stops working.\n// Primary is one lookup and never gets slower. It is also already the\n// wallet's declared identity, it is set automatically at a wallet's first\n// registration, and a holder who wants an older name counted only has to\n// make it their primary. The site says so on the referral page.\n//\n// A lapsed or missing primary is zero years, not an error: tier zero.\nfunc TenureYears(who address) int64 {\n\ttid := nsdata.GetPrimary(who)\n\tat := strings.Index(tid, \"*\")\n\tif at \u003c 1 { // \"\" or \"*domain\" — a domain primary has no name streak\n\t\treturn 0\n\t}\n\tlabel, domainLabel := tid[:at], tid[at+1:]\n\tif !nsdata.NameExists(label, domainLabel) || !nsdata.IsNameValid(label, domainLabel) {\n\t\treturn 0\n\t}\n\t_, _, _, streak, _, _ := nsdata.GetNameInfo(label, domainLabel)\n\treturn streak\n}\n\n// -- where it gets paid --\n\n// Commissions go to the referring wallet. There is no way to redirect\n// them, and that is a decision rather than an omission.\n//\n// There WAS a redirect — SetReferralPayout, \"a forwarding address, not a\n// transferable right\". It does not survive the obvious question: how\n// would this realm tell somebody moving money between their own two\n// wallets from somebody who has sold the stream to a stranger? It\n// cannot. Nothing on chain distinguishes those, and a rule that cannot\n// be enforced is not a rule, it is a sentence in a document.\n//\n// What settled it is that the redirect did not even serve the case it\n// was written for. Calling it required signing from the ORIGINAL wallet,\n// so \"I lost access to my wallet\" was never covered; the only wallet\n// that could redirect was one whose owner could equally have received\n// the money and forwarded it. It gave nothing to the honest case and\n// gave the shape of an assignable income stream to the other one.\n//\n// So the destination is the referrer's own address, fixed. Selling the\n// entitlement now means handing over private keys, which is a deterrent\n// no contract clause could match.\nfunc PayoutFor(who address) address { return who }\n\n// -- the record on a name --\n\n// ReferrerOf reads the wallet credited with a name, or the empty address.\nfunc ReferrerOf(label, domainLabel string) address {\n\tv := nsdata.GetNameExtra(label, domainLabel, keyRef)\n\tif a := address(v); v != \"\" \u0026\u0026 a.IsValid() {\n\t\treturn a\n\t}\n\treturn \"\"\n}\n\n// resolveReferrer turns a claimed referring NAME into the wallet to\n// credit, or the empty address if the claim does not stand up. Every\n// rejection is silent on purpose: a mistyped or self-referral should not\n// fail somebody's registration, it should simply earn nobody anything.\nfunc resolveReferrer(refName string, buyer address) address {\n\tat := strings.Index(refName, \"*\")\n\tif refName == \"\" || at \u003c 1 || at == len(refName)-1 {\n\t\treturn \"\"\n\t}\n\tlabel, domainLabel := refName[:at], refName[at+1:]\n\tif !nsdata.NameExists(label, domainLabel) || !nsdata.IsNameValid(label, domainLabel) {\n\t\treturn \"\" // a lapsed name is not a working code\n\t}\n\towner := nsdata.GetNameOwner(label, domainLabel)\n\tif owner == buyer {\n\t\treturn \"\" // no referring yourself\n\t}\n\treturn owner\n}\n\n// -- the money --\n\n// PriceWithReferral is what a buyer actually pays given the code they\n// arrived with. Exported so the site can display the number the chain\n// will charge instead of computing its own and hoping they agree.\nfunc PriceWithReferral(domainLabel, label, refName string, buyer address) int64 {\n\tprice := NamePrice(domainLabel, label)\n\tif price \u003c= 0 {\n\t\treturn 0 // free is free, and a percentage of nothing is nothing\n\t}\n\tref := resolveReferrer(refName, buyer)\n\tif ref == \"\" {\n\t\treturn price\n\t}\n\tpct := tierPercent(RefDiscountRules(), TenureYears(ref))\n\tif pct \u003c= 0 {\n\t\treturn price\n\t}\n\treturn price - (price * pct / 100)\n}\n\n// referralCut is what leaves for the referrer out of a payment of\n// `price`, and is zero unless they still hold a name. Returns the\n// destination address and the amount; the caller pays it in the same\n// banker call that pays the treasury.\nfunc referralCut(ref address, price int64) (address, int64) {\n\tif ref == \"\" || price \u003c= 0 {\n\t\treturn \"\", 0\n\t}\n\t/* STILL HERE, AND STILL VALID.\n\t   This tested nsdata.BalanceOf, which has no expiry filter — and a\n\t   token is burned only when somebody else re-registers the label. So\n\t   a referrer who bought one cheap name, earned credits and walked\n\t   away kept a balance of one forever, and every renewal of every\n\t   name they ever referred kept paying them out of treasury revenue.\n\t   That is exactly the standing charge the note above says the check\n\t   prevents.\n\t   ResolveAddress is the honest test: it returns a name only when the\n\t   address still owns it AND it has not expired. TenureYears does the\n\t   same lookup two lines below, so this costs nothing extra. */\n\tif ResolveAddress(ref) == \"\" {\n\t\treturn \"\", 0\n\t}\n\tpct := tierPercent(RefCutRules(), TenureYears(ref))\n\tif pct \u003c= 0 {\n\t\treturn \"\", 0\n\t}\n\tcut := price * pct / 100\n\tif cut \u003c= 0 {\n\t\treturn \"\", 0\n\t}\n\treturn PayoutFor(ref), cut\n}\n\n// -- the scoreboard --\n\n// creditReferral records one paid referral against a wallet.\n//\n// The COUNT and nothing else. There was a running total of micro-USD\n// beside it, and it is gone, because a number stored in the vault is a\n// number anyone with an RPC endpoint can read — there is no such thing\n// as an admin-only global on a public chain, and building the site as\n// though there were would be the kind of privacy that works right up\n// until somebody checks.\n//\n// Not storing it is the only real reduction available. The payments are\n// still in the block history and always will be, so anyone determined\n// can add them up; what we can decline to do is publish a convenient\n// running total ourselves. tools/referrals.py reconstructs it from the\n// chain when we want it, which costs no storage and publishes nothing.\n//\n// The registrant's transaction pays for this entry. It is about forty\n// bytes, written once per referrer and overwritten after, and it is the\n// price of the discount they just received.\nfunc creditReferral(cur realm, ref address) {\n\tif ref == \"\" {\n\t\treturn\n\t}\n\tsetGlobal(cur, keyRefStat+ref.String(),\n\t\tstrconv.FormatInt(ReferralCount(ref)+1, 10))\n}\n\n// ReferralCount is how many paid registrations a wallet has been\n// credited with. Public, like every other global here.\nfunc ReferralCount(who address) int64 {\n\treturn parseInt64(nsdata.GetGlobal(keyRefStat + who.String()))\n}\n\nfunc parseInt64(s string) int64 {\n\tv, err := strconv.ParseInt(strings.TrimSpace(s), 10, 64)\n\tif err != nil {\n\t\treturn 0\n\t}\n\treturn v\n}\n\n// ReferralBoard pages through every credited wallet for the\n// leaderboard: one \"address,count\" row per line. The vault stores\n// globals in a sorted tree, so the \"ref:\" entries are one contiguous run\n// and the walk stops at the first key past it rather than reading the\n// rest of the config.\nfunc ReferralBoard(after string, limit int) (rows, next string) {\n\tif limit \u003c= 0 || limit \u003e 200 {\n\t\tlimit = 200\n\t}\n\tfrom := keyRefStat\n\tif after != \"\" {\n\t\tfrom = keyRefStat + after\n\t}\n\tvar out []string\n\tfor len(out) \u003c limit {\n\t\tkeys, more := nsdata.ExportGlobalKeys(from, limit)\n\t\tif keys == \"\" {\n\t\t\tbreak\n\t\t}\n\t\tstop := false\n\t\tfor _, k := range strings.Split(keys, \",\") {\n\t\t\tif !strings.HasPrefix(k, keyRefStat) {\n\t\t\t\tstop = true\n\t\t\t\tbreak\n\t\t\t}\n\t\t\taddr := k[len(keyRefStat):]\n\t\t\tn := ReferralCount(address(addr))\n\t\t\tif n \u003c= 0 {\n\t\t\t\tcontinue\n\t\t\t}\n\t\t\tout = append(out, addr+\",\"+strconv.FormatInt(n, 10))\n\t\t\tif len(out) \u003e= limit {\n\t\t\t\tnext = addr\n\t\t\t\tbreak\n\t\t\t}\n\t\t}\n\t\tif stop || more == \"\" || more == from {\n\t\t\tbreak\n\t\t}\n\t\tfrom = more\n\t}\n\treturn strings.Join(out, \"\\n\"), next\n}\n"},{"name":"reserved.gno","body":"package nslogic\n\nimport (\n\t\"strconv\"\n\n\t\"gno.land/r/g1xr6tgxnpled50h74eafmvxway7z0ytr5rsmeme/nsdata/v1\"\n)\n\n// GENERATED BY tools/gen-reserved.py FROM collections/reserved-labels.json.\n// Do not edit by hand: the site reads the same source, and a change made\n// in one place only is a label the site offers and the chain refuses.\n//\n// STRUCTURAL RULES ONLY. The wordlists live off chain, in that JSON,\n// because storage is 100 ugnot per byte and a ten-thousand-word\n// dictionary is about 57 GNOT paid forever to encode a marketing\n// decision that will change. This is the backstop against a bug in the\n// issuer, not the place the policy lives.\n//\n// THE NUMBERS ARE SETTABLE, the shape is not. A minimum length is a\n// promotion knob and belongs in the panel — redeploying this realm to\n// change a 5 to a 4 costs about 22 GNOT. So the lengths read from the\n// vault's globals and fall back to the values generated here; the\n// pattern rules are structural facts about what a handle looks like and\n// stay in code.\n\nconst (\n\tdefaultFreeMinLength = 5\n\tdefaultFreeMaxLength = 63\n\n\tkeyFreeMin = \"freemin\"\n\tkeyFreeMax = \"freemax\"\n)\n\n// FreeMinLength is the shortest label the free tier will issue. Settable\n// from the panel, because which lengths are worth money is a commercial\n// judgement that moves.\nfunc FreeMinLength() int {\n\tif v := nsdata.GetGlobal(keyFreeMin); v != \"\" {\n\t\tif n, err := strconv.Atoi(v); err == nil \u0026\u0026 n \u003e 0 \u0026\u0026 n \u003c= 63 {\n\t\t\treturn n\n\t\t}\n\t}\n\treturn defaultFreeMinLength\n}\n\nfunc FreeMaxLength() int {\n\tif v := nsdata.GetGlobal(keyFreeMax); v != \"\" {\n\t\tif n, err := strconv.Atoi(v); err == nil \u0026\u0026 n \u003e 0 \u0026\u0026 n \u003c= 63 {\n\t\t\treturn n\n\t\t}\n\t}\n\treturn defaultFreeMaxLength\n}\n\n/*\nFreeLabelAllowed reports whether a label may be ISSUED on the free tier.\n\nISSUED, and never renewed. This is called by GrantFreeName and by\nnothing else, deliberately: if the minimum length rises from three to\nfive, somebody who already holds a three-character free name keeps it\nand keeps renewing it free. Tightening a rule must not reach backwards\nand take away what was already given — that is the single fastest way to\nmake a giveaway cost more trust than it ever bought.\n\nA name that LAPSES and is registered again is a new issuance, and gets\nwhatever the rules say that day. That is not retroactive; it is a\ndifferent name event.\n*/\nfunc FreeLabelAllowed(label string) bool {\n\tn := len(label)\n\tif n \u003c FreeMinLength() || n \u003e FreeMaxLength() {\n\t\treturn false\n\t}\n\t// Pure digits: 1337, 420, years. Collectible, and no handle is one.\n\tdigits := true\n\tfor i := 0; i \u003c n; i++ {\n\t\tif label[i] \u003c '0' || label[i] \u003e '9' {\n\t\t\tdigits = false\n\t\t\tbreak\n\t\t}\n\t}\n\tif digits {\n\t\treturn false\n\t}\n\t// aaaa — every character the same.\n\tsame := true\n\tfor i := 1; i \u003c n; i++ {\n\t\tif label[i] != label[0] {\n\t\t\tsame = false\n\t\t\tbreak\n\t\t}\n\t}\n\tif same {\n\t\treturn false\n\t}\n\t// abab — a two-character cycle. Also collectible, also not a handle.\n\tif n \u003e= 4 {\n\t\tcycle := true\n\t\tfor i := 2; i \u003c n; i++ {\n\t\t\tif label[i] != label[i-2] {\n\t\t\t\tcycle = false\n\t\t\t\tbreak\n\t\t\t}\n\t\t}\n\t\tif cycle {\n\t\t\treturn false\n\t\t}\n\t}\n\treturn true\n}\n"},{"name":"restore.gno","body":"package nslogic\n\nimport (\n\t\"strconv\"\n\t\"strings\"\n\t\"time\"\n\n\t\"gno.land/r/g1xr6tgxnpled50h74eafmvxway7z0ytr5rsmeme/nsdata/v1\"\n)\n\n/*\nREBUILDING THE REGISTRY ON A NEW CHAIN.\n\nWHY THIS EXISTS. gno.land replaces testnets rather than preserving them:\ntopaz-1 was superseded by sapphire-1 on twenty-four hours' notice and\nevery name registered on it went to zero. tools/snapshot.js keeps a\ncomplete off-chain copy of the vault for exactly that morning. This is\nthe other half — the path that turns a snapshot back into a live\nregistry, so that people who paid for a name get that name back rather\nthan an apology.\n\nTHIS IS A BACKDOOR, AND IT IS TREATED AS ONE.\n\nA function that can mint any name, to any address, with any registration\ndate, is precisely the thing that would make every name here worthless\nif it could be reached whenever the admin felt like it. Ownership means\nnothing if history is editable on demand. So the capability is not\nmerely admin-gated — admin-gated is what everything else here already\nis, and it is not enough for this. It is a ONE-SHOT:\n\n  - it starts closed, and on a chain where nothing needs restoring it is\n    sealed immediately and can never be used at all;\n  - opening it is a deliberate, separate, announced act with a deadline\n    attached, so forgetting to close it is not the same as leaving it\n    open forever;\n  - sealing is IRREVERSIBLE. Sealed cannot be reopened. There is no\n    argument that unseals it, including a good one;\n  - it refuses to touch a name that already exists. It fills gaps in an\n    empty registry; it cannot rewrite a live one.\n\nWHERE THE SEAL LIVES, AND WHY IT IS NOT HERE. This realm holds no state\nat all — that is the whole reason prices and names survive a logic\nupgrade untouched. If the seal lived in this realm's variables it would\nreset to \"never opened\" the moment a new generation was deployed, which\nwould make \"irreversible\" a lie with a two-hour shelf life. It lives in\nthe vault's global slot instead, so a new logic generation inherits a\nseal it cannot talk its way out of.\n\nWHAT THAT DOES AND DOES NOT GUARANTEE. It is a real barrier against\nevery accident and against this realm being reached later for something\nit was not built for. It is not a barrier against the admin themselves,\nwho can deploy a further logic realm and have the vault trust it — but\nthat is already total control by definition, and no flag in a realm the\nadmin can replace was ever going to change it. What the seal gives is\nthe thing that actually matters in practice: the state is public, it is\ncheckable by anybody at any time through RestoreStatus, and moving it\nbackwards leaves a permanent trace on chain.\n\nWHAT IT DELIBERATELY DOES NOT RESTORE. Marketplace listings and open\nvotes. Those live in their own realms, are intentions rather than facts,\nand are re-creatable by the people who made them in one transaction\neach. Re-materialising somebody's month-old asking price as though they\nhad just set it would be inventing a decision on their behalf.\n*/\n\n// The vault key holding the state of this whole mechanism.\nconst keyRestore = \"restore\"\n\n// Restore states, stored as a string in the vault's global slot.\nconst (\n\trestoreNever  = \"\"       // never opened; the normal state forever\n\trestoreSealed = \"sealed\" // closed permanently; cannot be reopened\n\trestoreOpen   = \"open:\"  // followed by the unix second it lapses\n)\n\n// The longest a restore window may be left open. A restore is a day of\n// work, not a season, and an unbounded window is just \"open forever\"\n// spelled differently — the failure here is nobody remembering to seal\n// it, so the deadline seals it for them.\nconst maxRestoreDays = 30\n\n// RestoreStatus reports the state of the restore mechanism in a form a\n// person can read, and is deliberately PUBLIC AND UNAUTHENTICATED.\n// Anybody at all can check whether this registry currently has a door in\n// it, without asking us and without trusting the answer we would give.\n//\n// Returns the state (\"never\", \"open\", \"expired\" or \"sealed\"), the unix\n// second an open window lapses (0 otherwise), and how many records have\n// been restored in the lifetime of this vault.\nfunc RestoreStatus() (state string, deadline int64, restored int64) {\n\tst, d := readRestoreState(nsdata.GetGlobal(keyRestore), time.Now().Unix())\n\treturn st, d, restoredCount()\n}\n\n// readRestoreState is the state machine on its own, with the clock passed\n// in. Split out so the one piece of logic that decides whether the door\n// is open can be tested exhaustively without a chain under it.\nfunc readRestoreState(raw string, now int64) (state string, deadline int64) {\n\tswitch {\n\tcase raw == restoreSealed:\n\t\treturn \"sealed\", 0\n\tcase strings.HasPrefix(raw, restoreOpen):\n\t\td, err := strconv.ParseInt(raw[len(restoreOpen):], 10, 64)\n\t\tif err != nil || d \u003c= 0 {\n\t\t\t// An unparseable deadline is read as NO permission. The safe\n\t\t\t// reading of a door whose lock is broken is \"shut\" — a corrupt\n\t\t\t// value must never widen what somebody is allowed to do.\n\t\t\treturn \"sealed\", 0\n\t\t}\n\t\tif now \u003e= d {\n\t\t\treturn \"expired\", d\n\t\t}\n\t\treturn \"open\", d\n\t}\n\treturn \"never\", 0\n}\n\nconst keyRestoredCount = \"restored\"\n\nfunc restoredCount() int64 {\n\tif v := nsdata.GetGlobal(keyRestoredCount); v != \"\" {\n\t\tif n, err := strconv.ParseInt(v, 10, 64); err == nil {\n\t\t\treturn n\n\t\t}\n\t}\n\treturn 0\n}\n\n/*\nTHE RESERVED-KEY DOOR DOES NOT EXIST ON THE DEPLOYED VAULT, so these\nwrites go through the ordinary setter.\n\nnsdata/v1 is live and permanent, and its SetGlobalRecord takes any key\nwith no reserved-key guard at all. The two-door design — a general setter\nthat refuses \"restore\"/\"restored\" plus SetReservedGlobalRecord for the\nmachinery — exists only in the local nsdata source, which can never be\ndeployed over a vault holding real records. Calling for a door that is\nnot in the wall does not fail at review; it fails at deploy, after the\ndeposit is spent.\n\nSo the seal is guarded in ONE place: setGlobal in admin.gno, which panics\non both keys. That is weaker than the intended belt-and-braces — a future\nlogic realm could drop the guard by forgetting it, and the vault would\nnot catch it — and it is the strongest version available on this chain.\nDo not \"restore\" the reserved call: it will not link.\n*/\nfunc bumpRestored(cur realm, n int64) {\n\tnsdata.SetGlobalRecord(cross(cur), keyRestoredCount,\n\t\tstrconv.FormatInt(restoredCount()+n, 10))\n}\n\n// assertRestoring is the gate every Restore* function passes through.\nfunc assertRestoring(cur realm) {\n\tassertNoSend(cur)\n\tassertIsAdmin(cur)\n\tstate, _, _ := RestoreStatus()\n\tswitch state {\n\tcase \"open\":\n\t\treturn\n\tcase \"sealed\":\n\t\tpanic(\"nslogic: restore was sealed and cannot be reopened. This is \" +\n\t\t\t\"not recoverable by design — a registry whose history can be \" +\n\t\t\t\"rewritten on request does not have owners, it has guests.\")\n\tcase \"expired\":\n\t\tpanic(\"nslogic: the restore window has lapsed. Deploy a fresh vault \" +\n\t\t\t\"and open a new window there; an expired one does not reopen.\")\n\t}\n\tpanic(\"nslogic: restore has never been opened. Call OpenRestore first, \" +\n\t\t\"and mean it.\")\n}\n\n// OpenRestore opens the window, once, for a bounded number of days.\n//\n// Only ever valid on a chain where the registry is EMPTY because the\n// previous chain was retired. It cannot be called on a vault that has\n// been sealed, and it cannot be called twice.\nfunc OpenRestore(cur realm, days int64) {\n\tassertNoSend(cur)\n\tassertIsAdmin(cur)\n\n\tstate, _, _ := RestoreStatus()\n\tif state != \"never\" {\n\t\tpanic(\"nslogic: restore may be opened exactly once per vault; this \" +\n\t\t\t\"one is already \" + state + \". A door that reopens is a door.\")\n\t}\n\tif days \u003c 1 || days \u003e maxRestoreDays {\n\t\tpanic(\"nslogic: a restore window must be between 1 and \" +\n\t\t\tstrconv.Itoa(maxRestoreDays) + \" days\")\n\t}\n\t/* THE PRECONDITION THE COMMENT ABOVE ALREADY CLAIMS.\n\t   \"Only ever valid on a chain where the registry is EMPTY\" was a\n\t   statement of intent that nothing enforced, so the window was\n\t   openable on a populated vault. The record restorers all refuse an\n\t   existing record, but RestoreNameField does not — it writes any\n\t   extension key on any EXISTING name, which includes the payment\n\t   addresses the site renders on a name's pay page. That is a way to\n\t   redirect somebody else's incoming money using only the admin key,\n\t   without the timelock and public event that granting a fresh logic\n\t   realm would cost. Now it is what it said it was. */\n\tif d, n, _, _ := nsdata.Sizes(); d != 0 || n != 0 {\n\t\tpanic(\"nslogic: restore is for an empty vault on a fresh chain; \" +\n\t\t\t\"this one already holds records\")\n\t}\n\tdeadline := time.Now().Unix() + days*86400\n\tnsdata.SetGlobalRecord(cross(cur), keyRestore,\n\t\trestoreOpen+strconv.FormatInt(deadline, 10))\n}\n\n// SealRestore closes the door permanently. Valid from any state,\n// including \"never\" — which is how it should be called on a chain that\n// has nothing to restore, so that the capability is provably dead rather\n// than merely unused.\n//\n// There is no matching unseal, and adding one later would defeat every\n// word written at the top of this file.\nfunc SealRestore(cur realm) {\n\tassertNoSend(cur)\n\tassertIsAdmin(cur)\n\tnsdata.SetGlobalRecord(cross(cur), keyRestore, restoreSealed)\n}\n\n/*\nRestoreDomain re-creates one domain with its original dates.\n\nORDER MATTERS: domains before their names. The vault refuses to register\na name under a domain that does not exist or had lapsed at the moment\nthe name claims to have been registered, which is correct behaviour and\nalso exactly the order a snapshot should be replayed in.\n\nThe vault's own registration path sets `registered` to whatever instant\nit is handed and derives the expiry from the term length, so the\nregistration date is restored by passing the ORIGINAL one, and the\nexpiry is then corrected to the recorded value rather than a computed\none. Anything else would quietly hand everybody a fresh term.\n*/\nfunc RestoreDomain(cur realm, label string, owner address, registered, expires, ttl int64, frozen bool) {\n\tassertRestoring(cur)\n\tif nsdata.DomainExists(label) {\n\t\tpanic(\"nslogic: \" + label + \" already exists; restore fills an empty \" +\n\t\t\t\"registry and never overwrites a live record\")\n\t}\n\tif registered \u003c= 0 || expires \u003c= 0 {\n\t\tpanic(\"nslogic: a restored record needs its original dates\")\n\t}\n\tif err := nsdata.RegisterDomainRecord(cross(cur), label, owner, registered); err != nil {\n\t\tpanic(\"nslogic: \" + err.Error())\n\t}\n\tnsdata.SetDomainExpiryRecord(cross(cur), label, expires)\n\tif ttl != 0 {\n\t\tif err := nsdata.SetDomainTTLRecord(cross(cur), label, ttl); err != nil {\n\t\t\tpanic(\"nslogic: \" + err.Error())\n\t\t}\n\t}\n\tif frozen {\n\t\tnsdata.SetDomainFrozen(cross(cur), label, true)\n\t}\n\tbumpRestored(cur, 1)\n}\n\n// RestoreName re-creates one name. See RestoreDomain on ordering and on\n// why the dates are passed rather than computed.\n//\n// isNFT is carried across rather than assumed: the free tier registers\n// names without a token, and minting one for somebody who never had it\n// would silently upgrade their record and desynchronise the two\n// ownership ledgers.\nfunc RestoreName(cur realm, label, domainLabel string, owner address, registered, expires, ttl int64, frozen, isNFT bool) {\n\tassertRestoring(cur)\n\tif nsdata.NameExists(label, domainLabel) {\n\t\tpanic(\"nslogic: \" + label + \"*\" + domainLabel + \" already exists; \" +\n\t\t\t\"restore fills an empty registry and never overwrites a live record\")\n\t}\n\tif registered \u003c= 0 || expires \u003c= 0 {\n\t\tpanic(\"nslogic: a restored record needs its original dates\")\n\t}\n\tif err := nsdata.RegisterNameRecord(cross(cur), label, domainLabel, owner, registered, isNFT); err != nil {\n\t\tpanic(\"nslogic: \" + err.Error())\n\t}\n\tnsdata.SetNameExpiryRecord(cross(cur), label, domainLabel, expires)\n\tif ttl != 0 {\n\t\tif err := nsdata.SetNameTTLRecord(cross(cur), label, domainLabel, ttl); err != nil {\n\t\t\tpanic(\"nslogic: \" + err.Error())\n\t\t}\n\t}\n\tif frozen {\n\t\tnsdata.SetNameFrozen(cross(cur), label, domainLabel, true)\n\t}\n\tbumpRestored(cur, 1)\n}\n\n/*\nRestoreNames replays many names in ONE transaction.\n\nWHY A BLOB AND NOT A LOOP OF CALLS. A restore is done in one sitting\nagainst a clock: the window is bounded and the people whose names are\nmissing are watching. One transaction per name is one signature per\nname, and at a few hundred names that is an afternoon of clicking during\nwhich a single mistimed refresh loses the thread. The compact form here\nis unpleasant to read and it is the difference between a restore that\ngets finished and one that gets abandoned half way.\n\nThe format is records separated by \"|\", fields separated by \",\":\n\n\tlabel,domain,owner,registered,expires,ttl,frozen,isNFT\n\nNeither a label nor a domain may contain \",\" or \"|\" — the character set\nis enforced at registration — and an address contains neither, so the\nsplit is unambiguous. tools/snapshot.js writes the JSON this is built\nfrom; see tools/restore-blob.js for the conversion.\n\nIt stops at the first bad record and panics, taking the whole\ntransaction with it. That is deliberate: a partial batch that silently\nskipped four names would leave four people without theirs and nothing\nanywhere saying which four.\n*/\nfunc RestoreNames(cur realm, blob string) int {\n\tassertRestoring(cur)\n\tif blob == \"\" {\n\t\tpanic(\"nslogic: nothing to restore\")\n\t}\n\trecs := strings.Split(blob, \"|\")\n\tif len(recs) \u003e 50 {\n\t\tpanic(\"nslogic: at most 50 records per call, or the transaction \" +\n\t\t\t\"runs out of gas half way and it is unclear what landed\")\n\t}\n\tdone := 0\n\tfor i, rec := range recs {\n\t\tif rec == \"\" {\n\t\t\tcontinue\n\t\t}\n\t\tr := parseRestoreRecord(rec, i)\n\t\tif nsdata.NameExists(r.label, r.domain) {\n\t\t\tpanic(\"nslogic: record \" + strconv.Itoa(i) + \" (\" + r.label + \"*\" + r.domain +\n\t\t\t\t\") already exists; restore never overwrites a live record\")\n\t\t}\n\t\tif err := nsdata.RegisterNameRecord(cross(cur), r.label, r.domain, r.owner, r.registered, r.isNFT); err != nil {\n\t\t\tpanic(\"nslogic: record \" + strconv.Itoa(i) + \": \" + err.Error())\n\t\t}\n\t\tnsdata.SetNameExpiryRecord(cross(cur), r.label, r.domain, r.expires)\n\t\tif r.ttl != 0 {\n\t\t\tif err := nsdata.SetNameTTLRecord(cross(cur), r.label, r.domain, r.ttl); err != nil {\n\t\t\t\tpanic(\"nslogic: record \" + strconv.Itoa(i) + \": \" + err.Error())\n\t\t\t}\n\t\t}\n\t\tif r.frozen {\n\t\t\tnsdata.SetNameFrozen(cross(cur), r.label, r.domain, true)\n\t\t}\n\t\tdone++\n\t}\n\tbumpRestored(cur, int64(done))\n\treturn done\n}\n\n// restoreRec is one parsed line of the blob.\ntype restoreRec struct {\n\tlabel, domain     string\n\towner             address\n\tregistered        int64\n\texpires, ttl      int64\n\tfrozen, isNFT     bool\n}\n\n/*\nparseRestoreRecord reads one record and refuses anything it is not sure\nabout.\n\nEVERY CHECK HERE IS A PANIC AND NOT A SKIP. The blob is machine-written\nfrom a snapshot, so a malformed record does not mean \"this one name is\nodd\", it means the file is not what it was believed to be — and the\nworst outcome available is a restore that appears to succeed while\nquietly dropping the records it could not read. Somebody would find out\nmonths later when one person asked where their name went.\n*/\nfunc parseRestoreRecord(rec string, i int) restoreRec {\n\tf := strings.Split(rec, \",\")\n\tif len(f) != 8 {\n\t\tpanic(\"nslogic: record \" + strconv.Itoa(i) + \" has \" +\n\t\t\tstrconv.Itoa(len(f)) + \" fields, expected 8 \" +\n\t\t\t\"(label,domain,owner,registered,expires,ttl,frozen,isNFT)\")\n\t}\n\tif f[0] == \"\" || f[1] == \"\" {\n\t\tpanic(\"nslogic: record \" + strconv.Itoa(i) + \" has no name\")\n\t}\n\tout := restoreRec{\n\t\tlabel:      f[0],\n\t\tdomain:     f[1],\n\t\towner:      address(f[2]),\n\t\tregistered: mustSecs(f[3], i, \"registered\"),\n\t\texpires:    mustSecs(f[4], i, \"expires\"),\n\t\tttl:        mustSecs(f[5], i, \"ttl\"),\n\t\tfrozen:     f[6] == \"1\",\n\t\tisNFT:      f[7] == \"1\",\n\t}\n\tif !out.owner.IsValid() {\n\t\tpanic(\"nslogic: record \" + strconv.Itoa(i) + \" has an invalid owner address\")\n\t}\n\tif out.registered \u003c= 0 || out.expires \u003c= 0 {\n\t\tpanic(\"nslogic: record \" + strconv.Itoa(i) + \" is missing its original dates\")\n\t}\n\t// An expiry before the registration is not a record, it is a\n\t// corruption, and restoring it would create a name that was already\n\t// reclaimable the day it was bought.\n\tif out.expires \u003c out.registered {\n\t\tpanic(\"nslogic: record \" + strconv.Itoa(i) + \" expires before it was registered\")\n\t}\n\treturn out\n}\n\nfunc mustSecs(s string, i int, what string) int64 {\n\tv, err := strconv.ParseInt(s, 10, 64)\n\tif err != nil || v \u003c 0 {\n\t\tpanic(\"nslogic: record \" + strconv.Itoa(i) + \" has a bad \" + what)\n\t}\n\treturn v\n}\n\n// RestoreNameField and RestoreDomainField put back a record's extension\n// keys — profiles, cross-chain addresses, tier markers, and a domain's\n// pricing rules. Separate calls because the extension slot has no fixed\n// shape by design, which is the same reason the snapshot captures it by\n// enumeration rather than by schema.\nfunc RestoreNameField(cur realm, label, domainLabel, key, value string) {\n\tassertRestoring(cur)\n\tif err := nsdata.SetNameExtraRecord(cross(cur), label, domainLabel, key, value); err != nil {\n\t\tpanic(\"nslogic: \" + err.Error())\n\t}\n}\n\nfunc RestoreDomainField(cur realm, label, key, value string) {\n\tassertRestoring(cur)\n\tif err := nsdata.SetDomainExtraRecord(cross(cur), label, key, value); err != nil {\n\t\tpanic(\"nslogic: \" + err.Error())\n\t}\n}\n\n// RestoreGlobal puts back the vault's global slot — where the default\n// price, the referral tiers and the seasonal card effects live.\n//\n// It refuses to write the restore keys themselves. Without that, a\n// snapshot taken while a window was open would, on being replayed,\n// cheerfully restore the open window along with everything else — and a\n// seal that a backup file can undo is not a seal.\nfunc RestoreGlobal(cur realm, key, value string) {\n\tassertRestoring(cur)\n\tif key == keyRestore || key == keyRestoredCount {\n\t\tpanic(\"nslogic: the restore mechanism does not restore itself\")\n\t}\n\tif err := nsdata.SetGlobalRecord(cross(cur), key, value); err != nil {\n\t\tpanic(\"nslogic: \" + err.Error())\n\t}\n}\n\n// RestorePrimary puts back reverse resolution — which name an address\n// answers to. Registration already sets this for an owner who had none,\n// so this exists for the owners who had chosen a different one.\nfunc RestorePrimary(cur realm, owner address, label, domainLabel string) {\n\tassertRestoring(cur)\n\tif err := nsdata.SetPrimaryRecord(cross(cur), owner, label, domainLabel); err != nil {\n\t\tpanic(\"nslogic: \" + err.Error())\n\t}\n}\n"},{"name":"term.gno","body":"package nslogic\n\nimport (\n\t\"strconv\"\n\t\"time\"\n\n\t\"gno.land/p/nt/ufmt/v0\"\n\t\"gno.land/r/g1xr6tgxnpled50h74eafmvxway7z0ytr5rsmeme/nsdata/v1\"\n)\n\n// How far ahead a name may be paid.\n//\n// Renewal was unbounded. RenewNameRecord adds one term to the previous\n// due date and asks no further questions, so a holder pressing renew\n// seven times bought seven years of runway, and nothing anywhere said\n// that was not intended. It is a bug in two directions at once:\n//\n//   - Free domains have a base price of zero. Renewal is free too, so a\n//     script could take every good name under *gnoland and hold it for a\n//     century for the cost of gas. There is no economic brake on a free\n//     name at all; the term limit IS the brake.\n//   - A paid name held fifty years ahead is fifty years of a promise we\n//     have made against a rate written by hand and a testnet that will\n//     be replaced. Selling that is easy and honouring it is not.\n//\n// The limit is RUNWAY, not total lifetime: at most N years may be\n// outstanding at any moment. Nobody is stopped from holding a name\n// forever — they renew as the runway burns down, which is the same\n// arrangement every domain registrar has always used and the one people\n// already understand.\n//\n// Per domain first, then a project-wide default, so a free domain can be\n// capped at one year without capping anything else.\nconst (\n\tkeyMaxTerm       = \"maxterm\" // domain extra AND global: years of runway\n\tdefaultMaxTerm   = int64(5)\n\tmaxMaxTermYears  = int64(100)\n)\n\n// MaxTermYears is the cap that applies to names under this domain.\nfunc MaxTermYears(domainLabel string) int64 {\n\tif v := nsdata.GetDomainExtra(domainLabel, keyMaxTerm); v != \"\" {\n\t\tif y, err := strconv.ParseInt(v, 10, 64); err == nil \u0026\u0026 y \u003e 0 {\n\t\t\treturn y\n\t\t}\n\t}\n\tif v := nsdata.GetGlobal(keyMaxTerm); v != \"\" {\n\t\tif y, err := strconv.ParseInt(v, 10, 64); err == nil \u0026\u0026 y \u003e 0 {\n\t\t\treturn y\n\t\t}\n\t}\n\treturn defaultMaxTerm\n}\n\n// SetMaxTerm caps a single domain, or the whole project when the domain\n// label is empty. A domain's own cap always wins, so tightening one free\n// domain does not disturb the rest.\nfunc SetMaxTerm(cur realm, domainLabel string, years int64) {\n\tassertNoSend(cur)\n\tassertIsAdmin(cur)\n\tif years \u003c 1 || years \u003e maxMaxTermYears {\n\t\tpanic(\"nslogic: term cap must be 1-100 years\")\n\t}\n\tv := strconv.FormatInt(years, 10)\n\tif domainLabel == \"\" {\n\t\tsetGlobal(cur, keyMaxTerm, v)\n\t\treturn\n\t}\n\tif !nsdata.DomainExists(domainLabel) {\n\t\tpanic(\"nslogic: unknown domain\")\n\t}\n\tif err := nsdata.SetDomainExtraRecord(cross(cur), domainLabel, keyMaxTerm, v); err != nil {\n\t\tpanic(err)\n\t}\n}\n\n// RenewalsLeft is how many more years may be added to a name right now.\n// Zero means the renew button should be off, and the site reads this\n// rather than doing the arithmetic itself — the cap can be per domain,\n// and a page that guesses it will guess wrong on exactly the domains\n// where it matters.\n//\n// An unregistered name reports the full allowance: nothing is holding\n// runway yet.\nfunc RenewalsLeft(label, domainLabel string) int64 {\n\tmaxYears := MaxTermYears(domainLabel)\n\tif !nsdata.NameExists(label, domainLabel) {\n\t\treturn maxYears\n\t}\n\tterm := nsdata.GetTermLength()\n\tif term \u003c= 0 {\n\t\treturn 0\n\t}\n\t_, _, expires, _, _, _ := nsdata.GetNameInfo(label, domainLabel)\n\treturn renewalsLeft(expires, time.Now().Unix(), term, maxYears)\n}\n\n// renewalsLeft is the arithmetic on its own, so it can be tested without\n// a chain under it.\n//\n// Runway is measured from NOW, not from the registration date, so a name\n// deep in its grace period counts as zero rather than as a negative that\n// would then read as extra room. Years already bought round UP: a name\n// with thirteen months left is holding two of its five, because the\n// alternative lets somebody sit permanently at four-years-and-eleven-\n// months and renew every month forever.\nfunc renewalsLeft(expires, now, term, maxYears int64) int64 {\n\tif term \u003c= 0 {\n\t\treturn 0\n\t}\n\trunway := expires - now\n\tif runway \u003c 0 {\n\t\trunway = 0\n\t}\n\tleft := maxYears - (runway+term-1)/term\n\tif left \u003c 0 {\n\t\treturn 0\n\t}\n\treturn left\n}\n\n// assertRoomToRenew is the enforcement. It runs before payment is taken,\n// so a rejected renewal costs gas and nothing else — taking the money\n// first and reverting would work too, but only because a panic reverts,\n// and depending on that for a refund is a habit that eventually meets a\n// path where it is not true.\nfunc assertRoomToRenew(label, domainLabel string) {\n\tif RenewalsLeft(label, domainLabel) \u003e 0 {\n\t\treturn\n\t}\n\tpanic(ufmt.Sprintf(\n\t\t\"nslogic: %s*%s is already paid %d years ahead, which is the limit. Renew again when it has run down.\",\n\t\tlabel, domainLabel, MaxTermYears(domainLabel)))\n}\n"},{"name":"types.gno","body":"// Package nslogic is the swappable \"business rules\" half of the split\n// (NOTES.md §9t). It holds every piece of policy that might reasonably\n// change — pricing, character validation, payment/refund, freeze rules,\n// transfer restrictions — and no persistent records of its own.\n//\n// Every mutation here ends by calling into nsdata's narrow Record/Raw\n// API via cross(cur). nsdata trusts whichever address is currently wired\n// up as trustedLogic to have done its own authorization checks\n// correctly, so THIS realm is where \"is the caller allowed to do this\"\n// actually lives. If a bug is found here, admin deploys a fixed nslogic\n// and re-points the vault at it (ProposeTrustedLogicGrant, then\n// ExecutePending after the timelock) — no data migration,\n// records and config untouched.\n//\n// Deliberately reads config fresh from nsdata on every call rather than\n// caching it. The one thing that cannot be read fresh is a COMPILED\n// regex — avl trees store the pattern string, not the compiled object —\n// so that one value is cached, and the cache is validated against\n// nsdata on every use.\npackage nslogic\n\n\nconst (\n\tugnot    = \"ugnot\"\n\tbpsDenom = 10_000\n)\n\n\n"},{"name":"validate.gno","body":"package nslogic\n\nimport (\n\t\"strings\"\n\n\t\"gno.land/p/nt/ufmt/v0\"\n\t\"gno.land/r/g1xr6tgxnpled50h74eafmvxway7z0ytr5rsmeme/nsdata/v1\"\n)\n\n// THE LABEL RULE IS WRITTEN OUT, NOT COMPILED.\n//\n// It used to be a regexp, and that regexp was the single most expensive\n// thing in this realm — by a wide margin, and not by a little. Measured\n// on sapphire-1 with vm/qstorage against purpose-built probe realms:\n//\n//\ta realm with code and no package-level state      2,711 bytes\n//\tthe same realm plus ONE compiled regexp         154,877 bytes\n//\n// A compiled regexp is an object graph, and gno charges 100 ugnot for\n// every byte of it, forever. That one variable was 15.2 GNOT of the\n// 22.5 GNOT it costs to deploy this realm — about 68% of the bill, to\n// enforce a rule that fits in thirty lines.\n//\n// The second regexp was worse, because it was compiled LAZILY and cached\n// in a package-level var. That does not avoid the cost, it MOVES it: the\n// first person to register a name after a fresh deploy would have had\n// their transaction grow this realm's storage by 150KB and been charged\n// ~15 GNOT of deposit for it. Confirmed by probe, not inferred — a realm\n// deployed at 2,711 bytes went to 154,149 the moment one call compiled\n// and cached a regexp, billed to the caller. Somebody buying a ten-cent\n// name would have been asked for fifteen GNOT.\n//\n// The hand-written version is also 6x cheaper in gas on every call\n// (1.06M against 6.67M), so there is no axis on which the regexp was\n// the better choice. It read better. That was the whole of its case.\n//\n// WHAT THIS COSTS US. The pattern is no longer admin-tunable at runtime:\n// changing the rule now means redeploying this realm. That is a fair\n// trade at 7 GNOT a deploy, it was never a rule that should change\n// casually, and SetLabelPattern refuses anything but the implemented\n// pattern so the stored string can never describe rules the code does\n// not enforce.\n\n// labelRule is the pattern this realm implements, kept as a string so\n// nsdata, the error messages and the site all quote the same thing.\nconst labelRule = `^[a-z0-9]+(-[a-z0-9]+)*$`\n\n// matchesLabelRule is that pattern, by hand: lowercase letters and\n// digits, single hyphens between them, none at either end.\nfunc matchesLabelRule(s string) bool {\n\tif s == \"\" {\n\t\treturn false\n\t}\n\tprevHyphen := true // a leading hyphen is not allowed\n\tfor i := 0; i \u003c len(s); i++ {\n\t\tc := s[i]\n\t\tswitch {\n\t\tcase c \u003e= 'a' \u0026\u0026 c \u003c= 'z', c \u003e= '0' \u0026\u0026 c \u003c= '9':\n\t\t\tprevHyphen = false\n\t\tcase c == '-':\n\t\t\tif prevHyphen {\n\t\t\t\treturn false // leading, or two in a row\n\t\t\t}\n\t\t\tprevHyphen = true\n\t\tdefault:\n\t\t\treturn false\n\t\t}\n\t}\n\treturn !prevHyphen // nor a trailing one\n}\n\n// looksLikeAddress was `^g1[a-z0-9]{20,38}$`. Anything shaped like a real\n// gno.land address is refused as a label, so a name can never be mistaken\n// for a raw wallet address in a UI.\nfunc looksLikeAddress(s string) bool {\n\tif len(s) \u003c 22 || len(s) \u003e 40 || !strings.HasPrefix(s, \"g1\") {\n\t\treturn false\n\t}\n\tfor i := 2; i \u003c len(s); i++ {\n\t\tc := s[i]\n\t\tif !(c \u003e= 'a' \u0026\u0026 c \u003c= 'z') \u0026\u0026 !(c \u003e= '0' \u0026\u0026 c \u003c= '9') {\n\t\t\treturn false\n\t\t}\n\t}\n\treturn true\n}\n\n// validateLabel — same policy as the original single-realm build (see\n// NOTES.md for the ENS/Starname/r/sys/users research behind it). The\n// length bounds are still read from nsdata, so they survive a swap of\n// this realm; the character rule is now this realm's own.\n//\n// Call sites, and why: RegisterName/RegisterDomain only (both the\n// first-ever mint AND reclaiming a past-grace slot — a reclaim is a\n// genuine new mint with a new owner). RenewName/RenewDomain deliberately\n// do NOT call this — an already-registered name must keep renewing under\n// whatever rule was in force when it was minted.\nfunc validateLabel(label string) {\n\tminLen, maxLen := nsdata.GetLabelLength()\n\tif len(label) \u003c minLen || len(label) \u003e maxLen {\n\t\tpanic(ufmt.Sprintf(\"nslogic: label must be %d-%d characters\", minLen, maxLen))\n\t}\n\t// Structural invariant, enforced regardless of what the pattern\n\t// happens to allow: \"*\" is the separator in both the token ID\n\t// (label*domain) and nsdata's storage key (domain*label). A label or\n\t// domain containing one would make those encodings ambiguous and let\n\t// two different names collide onto a single record.\n\tif strings.Contains(label, \"*\") {\n\t\tpanic(\"nslogic: label may not contain '*'\")\n\t}\n\tif !matchesLabelRule(label) {\n\t\tpanic(\"nslogic: a name is lowercase letters, digits and single hyphens between them — \" + labelRule)\n\t}\n\tif looksLikeAddress(label) {\n\t\tpanic(\"nslogic: label looks like a wallet address, not allowed\")\n\t}\n}\n"}],"type":{"@type":"/gno.MemPackageType","value":"MPUserAll"}},"send":"","max_deposit":""}],"fee":{"gas_wanted":"200000000","gas_fee":"1000000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"Auy1VwQmQltziWh2LuRJj7xKSpD9chk7NirrueqIJYrk"},"signature":"d/ww1JJ9hj4RxOsbqNBqnO4F7r2y2GfwBJit/2kNDNkE0a5bEQA+ogQ6z9mJV/2VtFCt3J/pS5EORTkiTeDcPg=="}],"memo":""},"metadata":{"timestamp":"1788745292"}}
{"tx":{"msg":[{"@type":"/vm.m_call","caller":"g1xr6tgxnpled50h74eafmvxway7z0ytr5rsmeme","send":"","max_deposit":"","pkg_path":"gno.land/r/g1xr6tgxnpled50h74eafmvxway7z0ytr5rsmeme/nsdata/v1","func":"ProposeTrustedLogicGrant","args":["g15h7eqr29wfukf757s45art3m9xrjzpjp8y5u79"]}],"fee":{"gas_wanted":"80000000","gas_fee":"80000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"Auy1VwQmQltziWh2LuRJj7xKSpD9chk7NirrueqIJYrk"},"signature":"CNrk6JIob6iKeowUM/AyjwKhbvBTQCVmc7onfm9QbRQzdyKqzyvo9EVwur1aihuSWFvIMmAEU1yn+6Wq3/awwA=="}],"memo":""},"metadata":{"timestamp":"1788745317"}}
{"tx":{"msg":[{"@type":"/vm.m_call","caller":"g1xr6tgxnpled50h74eafmvxway7z0ytr5rsmeme","send":"","max_deposit":"","pkg_path":"gno.land/r/g1xr6tgxnpled50h74eafmvxway7z0ytr5rsmeme/nsdata/v1","func":"ExecutePending","args":["grantLogic"]}],"fee":{"gas_wanted":"80000000","gas_fee":"80000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"Auy1VwQmQltziWh2LuRJj7xKSpD9chk7NirrueqIJYrk"},"signature":"zRg0zaEjHMz7XiQEYzrDeHaUEX+/8AYmWcCcKr0v85ZF9Hya35UWvSk+t9kLasish3NEgh/bsWF6AUnntNORqA=="}],"memo":""},"metadata":{"timestamp":"1788745320"}}
{"tx":{"msg":[{"@type":"/vm.m_call","caller":"g1xr6tgxnpled50h74eafmvxway7z0ytr5rsmeme","send":"","max_deposit":"","pkg_path":"gno.land/r/g1xr6tgxnpled50h74eafmvxway7z0ytr5rsmeme/nslogic/v3","func":"SetGranter","args":["g1xr6tgxnpled50h74eafmvxway7z0ytr5rsmeme"]}],"fee":{"gas_wanted":"80000000","gas_fee":"80000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"Auy1VwQmQltziWh2LuRJj7xKSpD9chk7NirrueqIJYrk"},"signature":"MMcf6j4bRrvKbG5QuxlH+NgmF2UNEEm8mjmnMDyOMxEZwZpU24wi+NgzQ+6bH77GP6pGJxk20sWvYXwD2+FlcQ=="}],"memo":""},"metadata":{"timestamp":"1788745324"}}
{"tx":{"msg":[{"@type":"/vm.m_call","caller":"g1xr6tgxnpled50h74eafmvxway7z0ytr5rsmeme","send":"","max_deposit":"","pkg_path":"gno.land/r/g1xr6tgxnpled50h74eafmvxway7z0ytr5rsmeme/nslogic/v3","func":"SetDomainFree","args":["meme","true"]}],"fee":{"gas_wanted":"80000000","gas_fee":"80000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"Auy1VwQmQltziWh2LuRJj7xKSpD9chk7NirrueqIJYrk"},"signature":"mk4PSmgI5ixuFdK28TK+ag47JzcPmTiZLQzjiEgr0TJBUD2PKPcWaZTesNSQZm08JoKTvF1cOFtkVqyOufthog=="}],"memo":""},"metadata":{"timestamp":"1788745327"}}
{"tx":{"msg":[{"@type":"/bank.MsgSend","from_address":"g18qhq2fl54lszhmxeyqlvxnwjzc3xpu4nnakclp","to_address":"g19mqecalmtrqr6jx98dack56g44g6pm08efsrrt","amount":"300000000ugnot"}],"fee":{"gas_wanted":"5000000","gas_fee":"1000000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"AhUuKSwiL4XKy0BM9cK0UJFToerTzvgLTHFBvYk/xOtY"},"signature":"CxWJO5VPoIiHDy1wkK4UKIc6kor/RHn/ETlg3yRvoy99fElghJHb37sgCNnNyj3vfU+AGaSsCuPgnjpzdwD7ww=="}],"memo":""},"metadata":{"timestamp":"1788745369"}}
{"tx":{"msg":[{"@type":"/bank.MsgSend","from_address":"g18qhq2fl54lszhmxeyqlvxnwjzc3xpu4nnakclp","to_address":"g1sayx6dnqrhhhl8w8umd7543wu8azt9x7y5uzgs","amount":"300000000ugnot"}],"fee":{"gas_wanted":"5000000","gas_fee":"1000000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"AhUuKSwiL4XKy0BM9cK0UJFToerTzvgLTHFBvYk/xOtY"},"signature":"NNGT2W7p4993+MHQYXasJLgWZh5c+5HnVHRAKl+WgZYZQM2Eorih86ldYx+f2MwZXSazHpsuOr5+HVIscnJ8xQ=="}],"memo":""},"metadata":{"timestamp":"1788745435"}}
{"tx":{"msg":[{"@type":"/bank.MsgSend","from_address":"g18qhq2fl54lszhmxeyqlvxnwjzc3xpu4nnakclp","to_address":"g182qfdgk2sf66s879mgxxmcmc0804rtzfaezaea","amount":"300000000ugnot"}],"fee":{"gas_wanted":"5000000","gas_fee":"1000000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"AhUuKSwiL4XKy0BM9cK0UJFToerTzvgLTHFBvYk/xOtY"},"signature":"tvGmONoA++H1EnDIPHvq5ucioiF5YvjaGoGL0qK3pToQzrjPFFFY8Ge3dMmvXlNYxo1+Dgm/1+4c+4siBGcsxg=="}],"memo":""},"metadata":{"timestamp":"1788745439"}}
{"tx":{"msg":[{"@type":"/bank.MsgSend","from_address":"g18qhq2fl54lszhmxeyqlvxnwjzc3xpu4nnakclp","to_address":"g1rc7qqa603xxx8ttkp0aa7p7vunhuxt2hmwsl4s","amount":"300000000ugnot"}],"fee":{"gas_wanted":"5000000","gas_fee":"1000000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"AhUuKSwiL4XKy0BM9cK0UJFToerTzvgLTHFBvYk/xOtY"},"signature":"fHhA/UCbU/AVpir33R/998nTUPsImVjY6g0V509Er/ch87s2nZYaM50Py3TWjfciQX9/47Hy2G3gIsXy73BreA=="}],"memo":""},"metadata":{"timestamp":"1788745570"}}
{"tx":{"msg":[{"@type":"/bank.MsgSend","from_address":"g18qhq2fl54lszhmxeyqlvxnwjzc3xpu4nnakclp","to_address":"g14mw4te2wz0uegs6t5rz5tlu3p292g7k7vyufe0","amount":"300000000ugnot"}],"fee":{"gas_wanted":"5000000","gas_fee":"1000000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"AhUuKSwiL4XKy0BM9cK0UJFToerTzvgLTHFBvYk/xOtY"},"signature":"vAPiJPM+RxCgeykRqUnPKMvSJDR6bPCzLGARJl4pefwiuNvvN36HUy4m2NnnHklukyjdohrj76iC8VnwS+CEpQ=="}],"memo":""},"metadata":{"timestamp":"1788745581"}}
{"tx":{"msg":[{"@type":"/bank.MsgSend","from_address":"g18qhq2fl54lszhmxeyqlvxnwjzc3xpu4nnakclp","to_address":"g1nkqgugze0h280klvjnun76dwptzfarax2lcaew","amount":"300000000ugnot"}],"fee":{"gas_wanted":"5000000","gas_fee":"1000000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"AhUuKSwiL4XKy0BM9cK0UJFToerTzvgLTHFBvYk/xOtY"},"signature":"Qc/RbjZnfs0WeAQc7WOcXfkUhszaUpCJnuP7KZ1xmnM0G/eAD+IrMvybyGUw+Dk7JbUhUTYzFjbcn6VRFAbXrg=="}],"memo":""},"metadata":{"timestamp":"1788745693"}}
{"tx":{"msg":[{"@type":"/vm.m_call","caller":"g1xr6tgxnpled50h74eafmvxway7z0ytr5rsmeme","send":"","max_deposit":"","pkg_path":"gno.land/r/g1xr6tgxnpled50h74eafmvxway7z0ytr5rsmeme/nsdata/v1","func":"RevokeTrustedLogic","args":["g177shjadfa5ltlpvme2wt8d8xlex4mj5t8nrx69"]}],"fee":{"gas_wanted":"80000000","gas_fee":"80000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"Auy1VwQmQltziWh2LuRJj7xKSpD9chk7NirrueqIJYrk"},"signature":"lyrfDdDsW8LJLALrFHPnlprhedaprD+aS4IqWdxT0FwcOYBkyra3p9W7aKHSkMZrskPC7PV8as1QGYAY/3CzYA=="}],"memo":""},"metadata":{"timestamp":"1788745794"}}
{"tx":{"msg":[{"@type":"/vm.m_call","caller":"g1xr6tgxnpled50h74eafmvxway7z0ytr5rsmeme","send":"","max_deposit":"","pkg_path":"gno.land/r/g1xr6tgxnpled50h74eafmvxway7z0ytr5rsmeme/nslogic/v3","func":"SetDomainOpen","args":["frog","false"]}],"fee":{"gas_wanted":"80000000","gas_fee":"80000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"Auy1VwQmQltziWh2LuRJj7xKSpD9chk7NirrueqIJYrk"},"signature":"+6jcj/m5y8bH7U4mhcMS6ov6xHV6rFTtpop1eIuYngZmV4+Y9x4hqyA8wIuN6VkoLFgZln69SCe9UQU/NgcS6Q=="}],"memo":""},"metadata":{"timestamp":"1788745797"}}
{"tx":{"msg":[{"@type":"/vm.m_call","caller":"g1xr6tgxnpled50h74eafmvxway7z0ytr5rsmeme","send":"","max_deposit":"","pkg_path":"gno.land/r/g1xr6tgxnpled50h74eafmvxway7z0ytr5rsmeme/nslogic/v3","func":"RegisterDomain","args":["troll"]}],"fee":{"gas_wanted":"80000000","gas_fee":"80000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"Auy1VwQmQltziWh2LuRJj7xKSpD9chk7NirrueqIJYrk"},"signature":"6XXNZ9asAF8Y+vG8fEPIVgnKCFMZXOWB6/Ka9TgfkhhSAaYn6CMAbqlv3WAQ7R3LxNQGBWU8df5/wxRrfQ2WLw=="}],"memo":""},"metadata":{"timestamp":"1788745801"}}
{"tx":{"msg":[{"@type":"/vm.m_call","caller":"g1xr6tgxnpled50h74eafmvxway7z0ytr5rsmeme","send":"","max_deposit":"","pkg_path":"gno.land/r/g1xr6tgxnpled50h74eafmvxway7z0ytr5rsmeme/nslogic/v3","func":"RegisterDomain","args":["alpha"]}],"fee":{"gas_wanted":"80000000","gas_fee":"80000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"Auy1VwQmQltziWh2LuRJj7xKSpD9chk7NirrueqIJYrk"},"signature":"8AvrHXuxlp+s6PUWLMtFr2u4W71HbShXwdDbxUD3Kro3rfc39eQ5PsWZFiqEd0WzCfFSNxKvhRq/ztWlz3oReg=="}],"memo":""},"metadata":{"timestamp":"1788745804"}}
{"tx":{"msg":[{"@type":"/vm.m_call","caller":"g1xr6tgxnpled50h74eafmvxway7z0ytr5rsmeme","send":"","max_deposit":"","pkg_path":"gno.land/r/g1xr6tgxnpled50h74eafmvxway7z0ytr5rsmeme/nslogic/v3","func":"RegisterDomain","args":["beta"]}],"fee":{"gas_wanted":"80000000","gas_fee":"80000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"Auy1VwQmQltziWh2LuRJj7xKSpD9chk7NirrueqIJYrk"},"signature":"lPma7n2mGGa1lr5lprICadn7oQtDf7IyHn/6vm4G6Bhhqof1rLf09LG5ID9XOVSoD3hqvA9dgp0m/ko1apK6sw=="}],"memo":""},"metadata":{"timestamp":"1788745808"}}
{"tx":{"msg":[{"@type":"/vm.m_call","caller":"g1xr6tgxnpled50h74eafmvxway7z0ytr5rsmeme","send":"","max_deposit":"","pkg_path":"gno.land/r/g1xr6tgxnpled50h74eafmvxway7z0ytr5rsmeme/nslogic/v3","func":"RegisterDomain","args":["omega"]}],"fee":{"gas_wanted":"80000000","gas_fee":"80000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"Auy1VwQmQltziWh2LuRJj7xKSpD9chk7NirrueqIJYrk"},"signature":"Iud/qqtaVMVUq8sS7m21ub4cgPSGiqfrw+8MI2kM0hRBiWHKqDT4hkd+XssHWpEwL9dLL8hFeF4BC7d1Z8/DUg=="}],"memo":""},"metadata":{"timestamp":"1788745811"}}
{"tx":{"msg":[{"@type":"/bank.MsgSend","from_address":"g18qhq2fl54lszhmxeyqlvxnwjzc3xpu4nnakclp","to_address":"g17zsfettrlxah4v9vxwrr6cmtaw2txfqg3t5avs","amount":"300000000ugnot"}],"fee":{"gas_wanted":"5000000","gas_fee":"1000000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"AhUuKSwiL4XKy0BM9cK0UJFToerTzvgLTHFBvYk/xOtY"},"signature":"HFjlKn/7/BASm2/BgdXujniL5XQ6VDFwzNmPv62etyBUfNMDyk0R06v9x9XcXDIt+YNV/PIcpka6qMNaC7YMvQ=="}],"memo":""},"metadata":{"timestamp":"1788745874"}}
{"tx":{"msg":[{"@type":"/bank.MsgSend","from_address":"g18qhq2fl54lszhmxeyqlvxnwjzc3xpu4nnakclp","to_address":"g1afqpjnyeewhuw6htyesrt07hq87vke7xgrq87q","amount":"300000000ugnot"}],"fee":{"gas_wanted":"5000000","gas_fee":"1000000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"AhUuKSwiL4XKy0BM9cK0UJFToerTzvgLTHFBvYk/xOtY"},"signature":"+SS3pQj8eYLkCnUdVvsdtS2jI/VB9FRMXWG2YxtFTRlpXZDAbGgn4A/lhf5HxEWoAsn3fNXwqk9cGmTs4P3h+w=="}],"memo":""},"metadata":{"timestamp":"1788745884"}}
{"tx":{"msg":[{"@type":"/bank.MsgSend","from_address":"g18qhq2fl54lszhmxeyqlvxnwjzc3xpu4nnakclp","to_address":"g18wnp4agad7w02x302a0dw9wtwwh684lrre8t67","amount":"300000000ugnot"}],"fee":{"gas_wanted":"5000000","gas_fee":"1000000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"AhUuKSwiL4XKy0BM9cK0UJFToerTzvgLTHFBvYk/xOtY"},"signature":"GDKaAo6aEv+UsZyesD+oSF4I3G0kFvjp2WjALDfJnHZZ1Z2qZKfad4tMzCaJyk016bD9qCjkA9S5prF1kANuBg=="}],"memo":""},"metadata":{"timestamp":"1788745916"}}
{"tx":{"msg":[{"@type":"/bank.MsgSend","from_address":"g18qhq2fl54lszhmxeyqlvxnwjzc3xpu4nnakclp","to_address":"g1ydvvspa888y43qhs65jdyk9wvq9v9quk6gjx6z","amount":"300000000ugnot"}],"fee":{"gas_wanted":"5000000","gas_fee":"1000000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"AhUuKSwiL4XKy0BM9cK0UJFToerTzvgLTHFBvYk/xOtY"},"signature":"Hh38oKHjXl3Iedu6q43fPXpcr6nK51F6t8EFmaQTz5YFP7IwR/wWzqk1k0u7mntjFe70aybdKgD1AmKAGilA9Q=="}],"memo":""},"metadata":{"timestamp":"1788745950"}}
{"tx":{"msg":[{"@type":"/bank.MsgSend","from_address":"g18qhq2fl54lszhmxeyqlvxnwjzc3xpu4nnakclp","to_address":"g18nh7kqy6cykf94ng6htz2te0tx4kxhucyqx9ux","amount":"300000000ugnot"}],"fee":{"gas_wanted":"5000000","gas_fee":"1000000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"AhUuKSwiL4XKy0BM9cK0UJFToerTzvgLTHFBvYk/xOtY"},"signature":"bmXNIFanrq0v9M+r1xIPix+5zQg7eZRCfTLDdWtF3XdPV6wkTgQtRi1yTjdA3TSmTlOnw+UIjAYHUw4ohP2QAA=="}],"memo":""},"metadata":{"timestamp":"1788745985"}}
{"tx":{"msg":[{"@type":"/bank.MsgSend","from_address":"g18qhq2fl54lszhmxeyqlvxnwjzc3xpu4nnakclp","to_address":"g1d623w4mfcxlkzruzwjas2x6t5xm7cpw33fp0u3","amount":"300000000ugnot"}],"fee":{"gas_wanted":"5000000","gas_fee":"1000000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"AhUuKSwiL4XKy0BM9cK0UJFToerTzvgLTHFBvYk/xOtY"},"signature":"dDd79IUoEG2VUTLvaTYSym0Yltv2AIfue5gCKdobvTdBlus2P05rNUeyoYQo62qDftYTCjs453/XEJUvtsC5lw=="}],"memo":""},"metadata":{"timestamp":"1788746091"}}
{"tx":{"msg":[{"@type":"/bank.MsgSend","from_address":"g18qhq2fl54lszhmxeyqlvxnwjzc3xpu4nnakclp","to_address":"g1vcezgqxpemlwmjtmc73v4pyn3lappd3kv5s79z","amount":"300000000ugnot"}],"fee":{"gas_wanted":"5000000","gas_fee":"1000000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"AhUuKSwiL4XKy0BM9cK0UJFToerTzvgLTHFBvYk/xOtY"},"signature":"xVZH+/9ZgDTQzB7ubgbvwoGJzJn2Fv3mjQv1pVGxVY49GH2AE4ZbQ2vugyupce5OjFmPEWc+Z7Yel6WUO1eQrA=="}],"memo":""},"metadata":{"timestamp":"1788746222"}}
{"tx":{"msg":[{"@type":"/bank.MsgSend","from_address":"g18qhq2fl54lszhmxeyqlvxnwjzc3xpu4nnakclp","to_address":"g1vdc9wqqrxmyhcrqgp6k3zrkxs7vm2a5e4kggeu","amount":"300000000ugnot"}],"fee":{"gas_wanted":"5000000","gas_fee":"1000000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"AhUuKSwiL4XKy0BM9cK0UJFToerTzvgLTHFBvYk/xOtY"},"signature":"kYG8UmZX3oslBNfuCpFxu53fonIaVEQtw+UX25iFW8ESXigFvWW47azmax897D6lhLMyaAdCCXrQk5FuYqRILw=="}],"memo":""},"metadata":{"timestamp":"1788746299"}}
{"tx":{"msg":[{"@type":"/bank.MsgSend","from_address":"g18qhq2fl54lszhmxeyqlvxnwjzc3xpu4nnakclp","to_address":"g1yzd33tg3x6mz2cdrekp9e8x2xff8gcnxq2ep9e","amount":"300000000ugnot"}],"fee":{"gas_wanted":"5000000","gas_fee":"1000000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"AhUuKSwiL4XKy0BM9cK0UJFToerTzvgLTHFBvYk/xOtY"},"signature":"6PF9uRR+195q13ZIySFgFp59sCYkJpgEpFqovI+F0OE4Arhf4Kn7GbQ6CqcN5l+Xv6tGOOIAtPjrPpYmqujJew=="}],"memo":""},"metadata":{"timestamp":"1788746333"}}
{"tx":{"msg":[{"@type":"/vm.m_run","caller":"g169d3un6y4sqxxmzwt7le3l5pawlfrhg5aa3xvp","send":"","max_deposit":"","package":{"name":"main","path":"","files":[{"name":"main.gno","body":"package main\n\nimport (\n\tgrc20reg \"gno.land/r/demo/defi/grc20reg\"\n)\n\nfunc main(cur realm) {\n\tgrc20reg.Approve(0, cur, \"gno.land/r/gnoswap/gns.GNS\", address(\"g1vc883gshu5z7ytk5cdynhc8c2dh67pdp4cszkp\"), 100000000)\n}\n"}]}},{"@type":"/vm.m_call","caller":"g169d3un6y4sqxxmzwt7le3l5pawlfrhg5aa3xvp","send":"","max_deposit":"","pkg_path":"gno.land/r/gnoswap/router","func":"ExactInSwapRoute","args":["gno.land/r/gnoswap/gns.GNS","gno.land/r/gnoswap/test_token/test_trx.TRX","100000000","gno.land/r/gnoswap/gns.GNS:gno.land/r/gnoswap/test_token/test_usdc.USDC:3000*POOL*gno.land/r/gnoswap/test_token/test_usdc.USDC:gno.land/r/gnoswap/test_token/test_trx.TRX:3000","100","393383018","1788746767",""]},{"@type":"/vm.m_run","caller":"g169d3un6y4sqxxmzwt7le3l5pawlfrhg5aa3xvp","send":"","max_deposit":"","package":{"name":"main","path":"","files":[{"name":"main.gno","body":"package main\n\nimport (\n\tgrc20reg \"gno.land/r/demo/defi/grc20reg\"\n)\n\nfunc main(cur realm) {\n\tgrc20reg.Approve(0, cur, \"gno.land/r/gnoswap/gns.GNS\", address(\"g1vc883gshu5z7ytk5cdynhc8c2dh67pdp4cszkp\"), 0)\n}\n"}]}}],"fee":{"gas_wanted":"195388274","gas_fee":"195389ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"A+FwLm4upty6jrOUKvPWMtxesfw3fytyPVyMKb9/GLoE"},"signature":"oAAVkdOLAwrtKYXXRs4IpvVmAZMn7ZL9elOATIqNiJBQ6+sx7nLnJRKOl/pmw0cB4wv71eBpM2D+XQUhQOlcsA=="}],"memo":"Executed through gnoswap.io"},"metadata":{"timestamp":"1788746466"}}
{"tx":{"msg":[{"@type":"/vm.m_run","caller":"g169d3un6y4sqxxmzwt7le3l5pawlfrhg5aa3xvp","send":"","max_deposit":"","package":{"name":"main","path":"","files":[{"name":"main.gno","body":"package main\n\nimport (\n\tgrc20reg \"gno.land/r/demo/defi/grc20reg\"\n)\n\nfunc main(cur realm) {\n\tgrc20reg.Approve(0, cur, \"gno.land/r/gnoswap/gns.GNS\", address(\"g1vc883gshu5z7ytk5cdynhc8c2dh67pdp4cszkp\"), 10000000000)\n}\n"}]}},{"@type":"/vm.m_call","caller":"g169d3un6y4sqxxmzwt7le3l5pawlfrhg5aa3xvp","send":"","max_deposit":"","pkg_path":"gno.land/r/gnoswap/router","func":"ExactInSwapRoute","args":["gno.land/r/gnoswap/gns.GNS","gno.land/r/gnoswap/test_token/test_trx.TRX","10000000000","gno.land/r/gnoswap/gns.GNS:gno.land/r/gnoswap/test_token/test_usdc.USDC:3000*POOL*gno.land/r/gnoswap/test_token/test_usdc.USDC:gno.land/r/gnoswap/test_token/test_trx.TRX:3000","100","32831060674","1788746783",""]},{"@type":"/vm.m_run","caller":"g169d3un6y4sqxxmzwt7le3l5pawlfrhg5aa3xvp","send":"","max_deposit":"","package":{"name":"main","path":"","files":[{"name":"main.gno","body":"package main\n\nimport (\n\tgrc20reg \"gno.land/r/demo/defi/grc20reg\"\n)\n\nfunc main(cur realm) {\n\tgrc20reg.Approve(0, cur, \"gno.land/r/gnoswap/gns.GNS\", address(\"g1vc883gshu5z7ytk5cdynhc8c2dh67pdp4cszkp\"), 0)\n}\n"}]}}],"fee":{"gas_wanted":"286358861","gas_fee":"286359ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"A+FwLm4upty6jrOUKvPWMtxesfw3fytyPVyMKb9/GLoE"},"signature":"F68hlmEMKvoJIZeSy5OJ8n6/TV55RI31EeL5s3Zg7PJhv9ia+LkXzjrqPZkDiI0d8g24uWFMrJ41EWSaBoChGw=="}],"memo":"Executed through gnoswap.io"},"metadata":{"timestamp":"1788746484"}}
{"tx":{"msg":[{"@type":"/vm.m_run","caller":"g169d3un6y4sqxxmzwt7le3l5pawlfrhg5aa3xvp","send":"","max_deposit":"","package":{"name":"main","path":"","files":[{"name":"main.gno","body":"package main\n\nimport (\n\tgrc20reg \"gno.land/r/demo/defi/grc20reg\"\n)\n\nfunc main(cur realm) {\n\tgrc20reg.Approve(0, cur, \"gno.land/r/gnoswap/test_token/test_trx.TRX\", address(\"g1vc883gshu5z7ytk5cdynhc8c2dh67pdp4cszkp\"), 1000000000)\n}\n"}]}},{"@type":"/vm.m_call","caller":"g169d3un6y4sqxxmzwt7le3l5pawlfrhg5aa3xvp","send":"","max_deposit":"","pkg_path":"gno.land/r/gnoswap/router","func":"ExactInSwapRoute","args":["gno.land/r/gnoswap/test_token/test_trx.TRX","gno.land/r/gnoswap/test_token/test_usdc.USDC","1000000000","gno.land/r/gnoswap/test_token/test_trx.TRX:gno.land/r/gnoswap/test_token/test_usdc.USDC:3000","100","1197855726","1788746799",""]},{"@type":"/vm.m_run","caller":"g169d3un6y4sqxxmzwt7le3l5pawlfrhg5aa3xvp","send":"","max_deposit":"","package":{"name":"main","path":"","files":[{"name":"main.gno","body":"package main\n\nimport (\n\tgrc20reg \"gno.land/r/demo/defi/grc20reg\"\n)\n\nfunc main(cur realm) {\n\tgrc20reg.Approve(0, cur, \"gno.land/r/gnoswap/test_token/test_trx.TRX\", address(\"g1vc883gshu5z7ytk5cdynhc8c2dh67pdp4cszkp\"), 0)\n}\n"}]}}],"fee":{"gas_wanted":"154986546","gas_fee":"154987ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"A+FwLm4upty6jrOUKvPWMtxesfw3fytyPVyMKb9/GLoE"},"signature":"KLHmPLpajTXxZuJCC2zCPTa1DqYW6bG06W4+tZ+Ww0Na4YF22ZB9jpSoZzvGeHdjQwRYkZUkuX7quM+NVMpw2Q=="}],"memo":"Executed through gnoswap.io"},"metadata":{"timestamp":"1788746501"}}
{"tx":{"msg":[{"@type":"/bank.MsgSend","from_address":"g18qhq2fl54lszhmxeyqlvxnwjzc3xpu4nnakclp","to_address":"g17kfz24rtedmtsjq84zl4kmcdsdtfwfg3aqktfu","amount":"300000000ugnot"}],"fee":{"gas_wanted":"5000000","gas_fee":"1000000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"AhUuKSwiL4XKy0BM9cK0UJFToerTzvgLTHFBvYk/xOtY"},"signature":"6dPlLSGuBVHlnY481ZaFN3Q89B0NBJCSmCKRIoo/2LZTXzPC/yiJx2f+4oM+OwsnVc7H66DbojrEDIo0sHythQ=="}],"memo":""},"metadata":{"timestamp":"1788746542"}}
{"tx":{"msg":[{"@type":"/vm.m_run","caller":"g169d3un6y4sqxxmzwt7le3l5pawlfrhg5aa3xvp","send":"","max_deposit":"","package":{"name":"main","path":"","files":[{"name":"main.gno","body":"package main\n\nimport (\n\tgrc20reg \"gno.land/r/demo/defi/grc20reg\"\n)\n\nfunc main(cur realm) {\n\tgrc20reg.Approve(0, cur, \"gno.land/r/gnoswap/test_token/test_trx.TRX\", address(\"g1vc883gshu5z7ytk5cdynhc8c2dh67pdp4cszkp\"), 555000000)\n}\n"}]}},{"@type":"/vm.m_call","caller":"g169d3un6y4sqxxmzwt7le3l5pawlfrhg5aa3xvp","send":"","max_deposit":"","pkg_path":"gno.land/r/gnoswap/router","func":"ExactInSwapRoute","args":["gno.land/r/gnoswap/test_token/test_trx.TRX","gno.land/r/gnoswap/test_token/test_usdc.USDC","555000000","gno.land/r/gnoswap/test_token/test_trx.TRX:gno.land/r/gnoswap/test_token/test_usdc.USDC:3000","100","658760657","1788746880",""]},{"@type":"/vm.m_run","caller":"g169d3un6y4sqxxmzwt7le3l5pawlfrhg5aa3xvp","send":"","max_deposit":"","package":{"name":"main","path":"","files":[{"name":"main.gno","body":"package main\n\nimport (\n\tgrc20reg \"gno.land/r/demo/defi/grc20reg\"\n)\n\nfunc main(cur realm) {\n\tgrc20reg.Approve(0, cur, \"gno.land/r/gnoswap/test_token/test_trx.TRX\", address(\"g1vc883gshu5z7ytk5cdynhc8c2dh67pdp4cszkp\"), 0)\n}\n"}]}}],"fee":{"gas_wanted":"154966268","gas_fee":"154967ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"A+FwLm4upty6jrOUKvPWMtxesfw3fytyPVyMKb9/GLoE"},"signature":"tpEORDr0PjZtdqxXgHJ2WJpLuftb+uHTeBxVO4bq5QQWgU66AP6FE2aCC82TbjManzIOq3TDM4+TDtXLyXlfZA=="}],"memo":"Executed through gnoswap.io"},"metadata":{"timestamp":"1788746586"}}
{"tx":{"msg":[{"@type":"/bank.MsgSend","from_address":"g18qhq2fl54lszhmxeyqlvxnwjzc3xpu4nnakclp","to_address":"g193yax8xqu93fe0ug6dt2l2w7ukzr8x8l3eyqnr","amount":"300000000ugnot"}],"fee":{"gas_wanted":"5000000","gas_fee":"1000000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"AhUuKSwiL4XKy0BM9cK0UJFToerTzvgLTHFBvYk/xOtY"},"signature":"lvxLg4nGBigGBu2UGf4hJH/fOORg/0nnS2dvASw8GR9gdXHjx2oltp3t8pG/gv3liigKDeoGpklZP54rPD7/Mw=="}],"memo":""},"metadata":{"timestamp":"1788746656"}}
{"tx":{"msg":[{"@type":"/bank.MsgSend","from_address":"g18qhq2fl54lszhmxeyqlvxnwjzc3xpu4nnakclp","to_address":"g1cqwhqxwdvff6r9q46wv7uk2njtfl640cjw3ful","amount":"300000000ugnot"}],"fee":{"gas_wanted":"5000000","gas_fee":"1000000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"AhUuKSwiL4XKy0BM9cK0UJFToerTzvgLTHFBvYk/xOtY"},"signature":"hbTN+MKfLoJGjbcTC3s4PF4M5FShYjSERdKUWzK75r5ZUSfuwh/6o15wLGlDs7gPVW6Cc57tr3P/f9b+qr9pvw=="}],"memo":""},"metadata":{"timestamp":"1788746727"}}
{"tx":{"msg":[{"@type":"/bank.MsgSend","from_address":"g18qhq2fl54lszhmxeyqlvxnwjzc3xpu4nnakclp","to_address":"g1qrrclvx2mmsnrzlz6qdygngjtj9dpnrtmrhflq","amount":"300000000ugnot"}],"fee":{"gas_wanted":"5000000","gas_fee":"1000000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"AhUuKSwiL4XKy0BM9cK0UJFToerTzvgLTHFBvYk/xOtY"},"signature":"mviYP0McQI81Irk+FMc8XXbTzhfCismkzg6PbDgdPaheub7xgTxAsNNr1/q3eTDKoUHYQY7XcX+zs0KEI4XfmA=="}],"memo":""},"metadata":{"timestamp":"1788746887"}}
{"tx":{"msg":[{"@type":"/bank.MsgSend","from_address":"g18qhq2fl54lszhmxeyqlvxnwjzc3xpu4nnakclp","to_address":"g1mqqlee8kanrsdm2klpwc6gef0ct7806fg8nfpe","amount":"300000000ugnot"}],"fee":{"gas_wanted":"5000000","gas_fee":"1000000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"AhUuKSwiL4XKy0BM9cK0UJFToerTzvgLTHFBvYk/xOtY"},"signature":"m5nOo+hswaG80cD09MfMuvF1Ek+s2RqswwMOYyfJJQkwYmUbPd/pnZrXJBIdKFTmexO17MIEug1teAzuMaykUQ=="}],"memo":""},"metadata":{"timestamp":"1788747250"}}
{"tx":{"msg":[{"@type":"/bank.MsgSend","from_address":"g18qhq2fl54lszhmxeyqlvxnwjzc3xpu4nnakclp","to_address":"g1mkv0y0vw28m2rkce0vly6p6yawma9y9xayxxn2","amount":"300000000ugnot"}],"fee":{"gas_wanted":"5000000","gas_fee":"1000000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"AhUuKSwiL4XKy0BM9cK0UJFToerTzvgLTHFBvYk/xOtY"},"signature":"9KdVdo71D8WhGTpClvob0xp2fcQf907pdSs/Ya0KBmhRhIWHRXw8NIs2y0k1kQtcbJjWxpsjrpSiqVDFxiyXOg=="}],"memo":""},"metadata":{"timestamp":"1788747277"}}
{"tx":{"msg":[{"@type":"/bank.MsgSend","from_address":"g18qhq2fl54lszhmxeyqlvxnwjzc3xpu4nnakclp","to_address":"g14kjr5sz33vt0cv43t35e79wf4peq7um52y6753","amount":"300000000ugnot"}],"fee":{"gas_wanted":"5000000","gas_fee":"1000000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"AhUuKSwiL4XKy0BM9cK0UJFToerTzvgLTHFBvYk/xOtY"},"signature":"B1AZE130efIdWVekhh6dw3IwN6+302o/FWiIgiWS2z0qj6j+ulXJbCy1UNAGEru4bIooEJLMNWAHBj76NbbHlQ=="}],"memo":""},"metadata":{"timestamp":"1788747305"}}
{"tx":{"msg":[{"@type":"/bank.MsgSend","from_address":"g18qhq2fl54lszhmxeyqlvxnwjzc3xpu4nnakclp","to_address":"g1fnvpyhaln2ffs7nldar47qsqcrf05h66mgwrys","amount":"300000000ugnot"}],"fee":{"gas_wanted":"5000000","gas_fee":"1000000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"AhUuKSwiL4XKy0BM9cK0UJFToerTzvgLTHFBvYk/xOtY"},"signature":"qUxvh7t2xGEgUn4aPG/9/PonlKHThpXs6GEvjxXLN+1e3nmxJoW2ZhA/Ws/SGwDECvTGTN+8cG41rUbHA9RmlA=="}],"memo":""},"metadata":{"timestamp":"1788747441"}}
{"tx":{"msg":[{"@type":"/bank.MsgSend","from_address":"g18qhq2fl54lszhmxeyqlvxnwjzc3xpu4nnakclp","to_address":"g1dc4drzw4xdllpc74xpuzcqdty5hnvn5fer4yr0","amount":"300000000ugnot"}],"fee":{"gas_wanted":"5000000","gas_fee":"1000000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"AhUuKSwiL4XKy0BM9cK0UJFToerTzvgLTHFBvYk/xOtY"},"signature":"Ye51E8JAWkEdDI9PWCdj7DfsDoCkAMQ4nXtTXAIf62N2RVmwFkfn3Be8m9oVNNy36ZF8Aiexkoa+7reCt89+Kw=="}],"memo":""},"metadata":{"timestamp":"1788747449"}}
{"tx":{"msg":[{"@type":"/bank.MsgSend","from_address":"g18qhq2fl54lszhmxeyqlvxnwjzc3xpu4nnakclp","to_address":"g1j0rrxlgc34falg9xhs0xsp0gj267ne8zcj66uv","amount":"300000000ugnot"}],"fee":{"gas_wanted":"5000000","gas_fee":"1000000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"AhUuKSwiL4XKy0BM9cK0UJFToerTzvgLTHFBvYk/xOtY"},"signature":"mXOZgZyyb5f6ptYgh2zSg9Y4MhpePZRXD7jifWC7UK9c0CL890wsZ5+zeeFxzucASxLr/9+31xrNtrbo02tX4Q=="}],"memo":""},"metadata":{"timestamp":"1788747575"}}
{"tx":{"msg":[{"@type":"/bank.MsgSend","from_address":"g18qhq2fl54lszhmxeyqlvxnwjzc3xpu4nnakclp","to_address":"g1gyfa29tprhexwry2ky5fvehw5jvwjm5ldvp8sz","amount":"300000000ugnot"}],"fee":{"gas_wanted":"5000000","gas_fee":"1000000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"AhUuKSwiL4XKy0BM9cK0UJFToerTzvgLTHFBvYk/xOtY"},"signature":"mxMq5zfC+58VZtXXMhGhoaPoRIpeUhwnyeF3ZxeuVQYDL1uW4MJdgSNHsNtpdLRkQ9A2FR4F+axXsIgEiZBleA=="}],"memo":""},"metadata":{"timestamp":"1788747608"}}
{"tx":{"msg":[{"@type":"/bank.MsgSend","from_address":"g18qhq2fl54lszhmxeyqlvxnwjzc3xpu4nnakclp","to_address":"g1v4a86j8pfjpgjeu06gpnxlm7aa7s5hzj5vfu33","amount":"300000000ugnot"}],"fee":{"gas_wanted":"5000000","gas_fee":"1000000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"AhUuKSwiL4XKy0BM9cK0UJFToerTzvgLTHFBvYk/xOtY"},"signature":"f0+davL2k/qUaxD5YUM1euSUbawnKvU2T/mMGo2jBdUw8BOxHEInIFjPipuOVqsEsXli+eSYhI83yiIkggjcvA=="}],"memo":""},"metadata":{"timestamp":"1788747943"}}
{"tx":{"msg":[{"@type":"/bank.MsgSend","from_address":"g18qhq2fl54lszhmxeyqlvxnwjzc3xpu4nnakclp","to_address":"g1rz8rquk7j52ha5zyc677sdlshmdh67l85xyngr","amount":"300000000ugnot"}],"fee":{"gas_wanted":"5000000","gas_fee":"1000000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"AhUuKSwiL4XKy0BM9cK0UJFToerTzvgLTHFBvYk/xOtY"},"signature":"oUM2WZizhwhMWk55W8DUB/VEpJRayfHXdrQDrRLgBbQluSIemZxSRZEZJ58FAdAICSMXpMhOD5xlh6P7vKX32g=="}],"memo":""},"metadata":{"timestamp":"1788748045"}}
{"tx":{"msg":[{"@type":"/bank.MsgSend","from_address":"g18qhq2fl54lszhmxeyqlvxnwjzc3xpu4nnakclp","to_address":"g1yskdcmdsudnjmr7l9c5gm38z3weysn8zcsc0hr","amount":"300000000ugnot"}],"fee":{"gas_wanted":"5000000","gas_fee":"1000000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"AhUuKSwiL4XKy0BM9cK0UJFToerTzvgLTHFBvYk/xOtY"},"signature":"vZPqoddbK8smFonDBNKQTBqnAEZyQk+0hC77NI6FHU0kLorJuPefYG5JwvpC48iauHjm1GgCDsgTzn5sZ2LV4Q=="}],"memo":""},"metadata":{"timestamp":"1788748083"}}
{"tx":{"msg":[{"@type":"/bank.MsgSend","from_address":"g18qhq2fl54lszhmxeyqlvxnwjzc3xpu4nnakclp","to_address":"g1tlljkncyxtwmfdl9vpm38uq2mq55mdesek2ttr","amount":"300000000ugnot"}],"fee":{"gas_wanted":"5000000","gas_fee":"1000000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"AhUuKSwiL4XKy0BM9cK0UJFToerTzvgLTHFBvYk/xOtY"},"signature":"yx/0saKjR2kiUWoSPi6y8zKnDN43iEE9AKlZQsyLwahS2/DFizyBNIL6/shoVVDS5R1pZJU6lfArtSs4MwjpuA=="}],"memo":""},"metadata":{"timestamp":"1788748124"}}
{"tx":{"msg":[{"@type":"/bank.MsgSend","from_address":"g18qhq2fl54lszhmxeyqlvxnwjzc3xpu4nnakclp","to_address":"g1nkvynxc24gqu48f6zuuutu2t0hey7wq6hklmxs","amount":"300000000ugnot"}],"fee":{"gas_wanted":"5000000","gas_fee":"1000000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"AhUuKSwiL4XKy0BM9cK0UJFToerTzvgLTHFBvYk/xOtY"},"signature":"qYXA+IsYNryl1siGSwTbukq1PgWMkETl8xp8w8sqdBl/DNO36c8soD8OErU/gg1ls4a405mnT1PjxRWieyEXIQ=="}],"memo":""},"metadata":{"timestamp":"1788748343"}}
{"tx":{"msg":[{"@type":"/bank.MsgSend","from_address":"g18qhq2fl54lszhmxeyqlvxnwjzc3xpu4nnakclp","to_address":"g1kvedxf8q9ryljkaufakmsjh2jajqvmpkaxxlx0","amount":"300000000ugnot"}],"fee":{"gas_wanted":"5000000","gas_fee":"1000000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"AhUuKSwiL4XKy0BM9cK0UJFToerTzvgLTHFBvYk/xOtY"},"signature":"Q+EF4EiMsCHHKQ9qwDmWxetWlMmjKM2zcyo3qIC9w0YAjCtAtMRNEasF2gtOhwejYmljPTxvYSJ0gJA5GVoLCQ=="}],"memo":""},"metadata":{"timestamp":"1788748709"}}
{"tx":{"msg":[{"@type":"/bank.MsgSend","from_address":"g18qhq2fl54lszhmxeyqlvxnwjzc3xpu4nnakclp","to_address":"g14wghw60terp8fuzel7kk6pf6598gmedtk3vfvq","amount":"300000000ugnot"}],"fee":{"gas_wanted":"5000000","gas_fee":"1000000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"AhUuKSwiL4XKy0BM9cK0UJFToerTzvgLTHFBvYk/xOtY"},"signature":"SQtF3reOGg6NvuxF3UfrBX378heDlMYdKZh+5635gJ0G4TzJDdK3DIik1klZqV97P+0SVQfaBF07dqC9OudV0Q=="}],"memo":""},"metadata":{"timestamp":"1788748785"}}
{"tx":{"msg":[{"@type":"/bank.MsgSend","from_address":"g18qhq2fl54lszhmxeyqlvxnwjzc3xpu4nnakclp","to_address":"g1xes3qhrsv76pnzqsl5zx9tmp4njc8t6tgrwprh","amount":"300000000ugnot"}],"fee":{"gas_wanted":"5000000","gas_fee":"1000000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"AhUuKSwiL4XKy0BM9cK0UJFToerTzvgLTHFBvYk/xOtY"},"signature":"j7seZtpzmgTOvvouu4beY0EeNwxiZyuQM8aztiBUUj1kLGgserDOWDirvguTSrbiZ5wnvlJNXCKhUi3SDHIsXw=="}],"memo":""},"metadata":{"timestamp":"1788748894"}}
{"tx":{"msg":[{"@type":"/bank.MsgSend","from_address":"g18qhq2fl54lszhmxeyqlvxnwjzc3xpu4nnakclp","to_address":"g1hmr09qruygcyj5qgpwwxzlg86ruaphegdqzx8m","amount":"300000000ugnot"}],"fee":{"gas_wanted":"5000000","gas_fee":"1000000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"AhUuKSwiL4XKy0BM9cK0UJFToerTzvgLTHFBvYk/xOtY"},"signature":"SmnUipqpXM20uqy3umUst8MNLE2xR+cUzyqZpzR2CewD+7w++53jY8Cwe8PEP5WUNl9F1OsOo1c1tkZ0QbWMkA=="}],"memo":""},"metadata":{"timestamp":"1788748918"}}
{"tx":{"msg":[{"@type":"/bank.MsgSend","from_address":"g18qhq2fl54lszhmxeyqlvxnwjzc3xpu4nnakclp","to_address":"g1qc73uke2tdcu949fjpqk7sh4trl48t0elmj6mm","amount":"300000000ugnot"}],"fee":{"gas_wanted":"5000000","gas_fee":"1000000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"AhUuKSwiL4XKy0BM9cK0UJFToerTzvgLTHFBvYk/xOtY"},"signature":"j+V36oAass5PosoygPjtLYunGzhMA3MVVhzCTgpGH4dyVVemhx93a9gERuSfBnC8u9qAUE0wZy/7rd0MFwM55A=="}],"memo":""},"metadata":{"timestamp":"1788749042"}}
{"tx":{"msg":[{"@type":"/bank.MsgSend","from_address":"g18qhq2fl54lszhmxeyqlvxnwjzc3xpu4nnakclp","to_address":"g1wnl737d9g9nc7eqwglvtxhh53kh9z5f2787h3h","amount":"300000000ugnot"}],"fee":{"gas_wanted":"5000000","gas_fee":"1000000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"AhUuKSwiL4XKy0BM9cK0UJFToerTzvgLTHFBvYk/xOtY"},"signature":"5Sexusch837ZCa4m2bxEqsZQ+RclrqTGiqQuriV75/cAwz+ubiAe8Y9GIDD9tJCEywOkYULDlaQEruEkwEmPOQ=="}],"memo":""},"metadata":{"timestamp":"1788749127"}}
{"tx":{"msg":[{"@type":"/bank.MsgSend","from_address":"g18qhq2fl54lszhmxeyqlvxnwjzc3xpu4nnakclp","to_address":"g1kujyauulx4st7dz37ldjkxek806khlae3zntv8","amount":"300000000ugnot"}],"fee":{"gas_wanted":"5000000","gas_fee":"1000000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"AhUuKSwiL4XKy0BM9cK0UJFToerTzvgLTHFBvYk/xOtY"},"signature":"y/gXT1q0i75rxccMILjK/S334gdtRECyol1hCuEK/gNy4agLRbhwc2yUbfrDI/JP79DsCQldXlCEENoIUZmYVw=="}],"memo":""},"metadata":{"timestamp":"1788749165"}}
{"tx":{"msg":[{"@type":"/vm.m_addpkg","creator":"g1xr6tgxnpled50h74eafmvxway7z0ytr5rsmeme","package":{"name":"nslogic","path":"gno.land/r/g1xr6tgxnpled50h74eafmvxway7z0ytr5rsmeme/nslogic/v4","files":[{"name":"admin.gno","body":"package nslogic\n\nimport (\n\t\"strconv\"\n\t\"strings\"\n\n\t\"gno.land/r/g1xr6tgxnpled50h74eafmvxway7z0ytr5rsmeme/nsdata/v1\"\n)\n\n// assertIsAdmin reads nsdata.GetAdmin() fresh every call — correct even\n// immediately after a TransferAdmin, and correct for a freshly-deployed\n// logic realm with no local state of its own to go stale.\n//\n// Note: admin authority to swap WHICH realm is trustedLogic, or to\n// transfer admin itself, is deliberately NOT exposed here — those live\n// only in nsdata's own admin.gno, callable solely by a direct wallet\n// call from the current admin address, never proxied through logic.\n// That's the one part of the system a compromised or buggy logic realm\n// cannot touch.\nfunc assertIsAdmin(cur realm) {\n\tif cur.Previous().Address() != nsdata.GetAdmin() {\n\t\tpanic(\"nslogic: restricted to project admin\")\n\t}\n}\n\n// SetLabelPattern no longer changes anything, and refuses anything but\n// the rule this realm actually implements.\n//\n// It used to compile an arbitrary regex and store it, with validateLabel\n// reading it back. That flexibility cost 15 GNOT of permanent storage\n// per deploy — see the measurements in validate.gno — for a rule that\n// has never changed and should not change casually.\n//\n// It is kept rather than deleted so that an admin who reaches for it\n// gets told what happened instead of a \"function not found\". And it\n// refuses a different pattern rather than accepting one it will not\n// enforce: a stored string describing rules the code does not apply is\n// the exact silent divergence the caching version was written to avoid.\nfunc SetLabelPattern(cur realm, pattern string) {\n\tassertNoSend(cur)\n\tassertIsAdmin(cur)\n\tif pattern != labelRule {\n\t\tpanic(\"nslogic: the label rule is fixed in this realm (\" + labelRule +\n\t\t\t\"). Changing it means deploying a new logic realm — which is deliberate, \" +\n\t\t\t\"because a stored pattern the code does not enforce is worse than no setting at all.\")\n\t}\n\tnsdata.SetLabelPatternRecord(cross(cur), pattern)\n}\n\nfunc SetLabelLengthLimits(cur realm, minLen, maxLen int) {\n\tassertNoSend(cur)\n\tassertIsAdmin(cur)\n\tif minLen \u003c 1 || maxLen \u003c minLen {\n\t\tpanic(\"nslogic: invalid length bounds\")\n\t}\n\tnsdata.SetLabelLengthRecord(cross(cur), minLen, maxLen)\n}\n\n// SetDefaultNamePrice sets the fallback price in micro-USD, used by any\n// domain that has not set its own base.\nfunc SetDefaultNamePrice(cur realm, microUsd int64) {\n\tassertNoSend(cur)\n\tassertIsAdmin(cur)\n\tif microUsd \u003c 0 {\n\t\tpanic(\"nslogic: price cannot be negative\")\n\t}\n\tsetGlobal(cur, keyDefaultPrice, strconv.FormatInt(microUsd, 10))\n}\n\n// SetUsdRate records how many ugnot one USD buys — the bridge between\n// the USD price list and settlement on this chain.\n//\n// It is a MANUAL rate, not a market feed: it sits where it was last\n// written. A stale rate during a sharp move sells names at the wrong\n// price, and no amount of contract logic fixes that — keeping it current\n// is an operational duty. Isolated as one global so a future logic realm\n// can swap in a real oracle without touching the vault or restating a\n// single price.\nfunc SetUsdRate(cur realm, ugnotPerUsd int64) {\n\tassertNoSend(cur)\n\tassertIsAdmin(cur)\n\tif ugnotPerUsd \u003c= 0 {\n\t\tpanic(\"nslogic: rate must be positive\")\n\t}\n\tsetGlobal(cur, keyRate, strconv.FormatInt(ugnotPerUsd, 10))\n}\n\n// SetTermLength / SetGracePeriod retune the lifecycle itself. Both were\n// hardcoded constants in the vault until 2026-08-21 — meaning the 1-year\n// term and 90-day grace could never have been changed, on a realm that\n// is never redeployed. There was no reason to accept that.\n// maxLifecycleDays bounds both setters. time.Duration is int64\n// NANOSECONDS, so `days * 24h` overflows above ~106,751 days and wraps to\n// a NEGATIVE duration — which would install a negative term and brick\n// every registration and renewal in the registry. Guarding the input is\n// what keeps the multiplication in range.\nconst maxLifecycleDays = 36500 // 100 years\n\nfunc SetTermLength(cur realm, days int64) {\n\tassertNoSend(cur)\n\tassertIsAdmin(cur)\n\tif days \u003c= 0 || days \u003e maxLifecycleDays {\n\t\tpanic(\"nslogic: term out of range (1..36500 days)\")\n\t}\n\tterm := days * 24 * 60 * 60\n\t// Keep gracePeriod \u003c= termLength. Anchoring a renewal to the previous\n\t// due date is only coherent while a renewal cannot happen more than\n\t// one full term after it; the vault clamps forward defensively, but\n\t// the combination should not be reachable in the first place.\n\tif nsdata.GetGracePeriod() \u003e term {\n\t\tpanic(\"nslogic: term must be at least the current grace period\")\n\t}\n\tnsdata.SetTermLengthRecord(cross(cur), term)\n}\n\nfunc SetGracePeriod(cur realm, days int64) {\n\tassertNoSend(cur)\n\tassertIsAdmin(cur)\n\tif days \u003c 0 || days \u003e maxLifecycleDays {\n\t\tpanic(\"nslogic: grace period out of range (0..36500 days)\")\n\t}\n\tgrace := days * 24 * 60 * 60\n\tif grace \u003e nsdata.GetTermLength() {\n\t\tpanic(\"nslogic: grace period may not exceed the term length\")\n\t}\n\tnsdata.SetGracePeriodRecord(cross(cur), grace)\n}\n\n// SetTokenURIBase re-points TokenURI at a different renderer, so the NFT\n// artwork can be replaced without touching the vault.\nfunc SetTokenURIBase(cur realm, base string) {\n\tassertNoSend(cur)\n\tassertIsAdmin(cur)\n\tnsdata.SetTokenURIBaseRecord(cross(cur), base)\n}\n\nfunc SetExtraLimits(cur realm, keyLen, valueLen, entries int) {\n\tassertNoSend(cur)\n\tassertIsAdmin(cur)\n\tif keyLen \u003c 1 || valueLen \u003c 1 || entries \u003c 1 {\n\t\tpanic(\"nslogic: limits must be positive\")\n\t}\n\tnsdata.SetExtraLimitsRecord(cross(cur), keyLen, valueLen, entries)\n}\n\n/*\nSetMarketFee is nsmarket's fee control, living here because this is the\nrealm the vault trusts.\n\nnsmarket.SetFee could never work: it wrote through nsdata.SetGlobalRecord\nfrom a realm deliberately outside the trusted set, so it passed its own\nadmin check and then panicked inside the vault for every caller. Rather\nthan grant nsmarket vault privileges — which would surrender the property\nthat realm is built around — the setter moved to the realm that already\nhas the grant, and the validation moved with it.\n\nThe spec grammar is nsmarket's, restated here rather than imported so\nthis realm does not take a dependency on the marketplace to set a number:\nan amount in ugnot (\"1000000\"), or a percentage (\"%2.5\"), or \"\" for none.\nA malformed spec would otherwise be read on every sale and resolve\nsilently to zero.\n*/\nfunc SetMarketFee(cur realm, spec string) {\n\tassertNoSend(cur)\n\tassertIsAdmin(cur)\n\tassertValidFeeSpec(spec)\n\tsetGlobal(cur, \"marketfee\", strings.TrimSpace(spec))\n}\n\n// assertValidFeeSpec mirrors nsmarket.parseFee. Percentages are carried\n// as basis points so a fee is expressible without floating point\n// anywhere near money.\nfunc assertValidFeeSpec(spec string) {\n\tspec = strings.TrimSpace(spec)\n\tif spec == \"\" {\n\t\treturn\n\t}\n\tif !strings.HasPrefix(spec, \"%\") {\n\t\tn, err := strconv.ParseInt(spec, 10, 64)\n\t\tif err != nil || n \u003c 0 {\n\t\t\tpanic(\"nslogic: a fee is an amount in ugnot or a percentage like %2.5 — got \" + spec)\n\t\t}\n\t\treturn\n\t}\n\tbody := spec[1:]\n\twhole, frac := body, \"\"\n\tif dot := strings.Index(body, \".\"); dot \u003e= 0 {\n\t\twhole, frac = body[:dot], body[dot+1:]\n\t}\n\t// ONE decimal, matching nsmarket.parseFee exactly. I wrote two here\n\t// first, which made this validator MORE permissive than the parser\n\t// that has to read the value back — and a spec nsmarket refuses is\n\t// read as a zero fee on every sale, silently, forever. A validator\n\t// looser than its consumer is worse than no validator: it converts a\n\t// rejected input into a silent revenue leak.\n\tif len(frac) \u003e 1 {\n\t\tpanic(\"nslogic: a fee percentage carries at most one decimal — got \" + spec)\n\t}\n\tfor _, part := range []string{whole, frac} {\n\t\tfor i := 0; i \u003c len(part); i++ {\n\t\t\tif part[i] \u003c '0' || part[i] \u003e '9' {\n\t\t\t\tpanic(\"nslogic: a fee percentage must be digits — got \" + spec)\n\t\t\t}\n\t\t}\n\t}\n\tif whole == \"\" \u0026\u0026 frac == \"\" {\n\t\tpanic(\"nslogic: a fee percentage needs a number — got \" + spec)\n\t}\n\t// Basis points the same way parseFee computes them: whole*100 plus\n\t// one tenth-digit worth ten. Not two digits, and not a 100% ceiling\n\t// — nsmarket caps at maxFeeBps, which is 50%.\n\tw, _ := strconv.ParseInt(\"0\"+whole, 10, 64)\n\tbp := w * 100\n\tif frac != \"\" {\n\t\tf, _ := strconv.ParseInt(\"0\"+frac, 10, 64)\n\t\tbp += f * 10\n\t}\n\tif bp \u003c= 0 || bp \u003e 5000 {\n\t\tpanic(\"nslogic: a fee percentage must be above zero and at most 50 — got \" + spec)\n\t}\n}\n\n// SetGlobal writes an arbitrary project-wide key into the vault's\n// package-level extension slot — the escape hatch for global state that\n// does not exist yet.\nfunc SetGlobal(cur realm, key, value string) {\n\tassertNoSend(cur)\n\tassertIsAdmin(cur)\n\tsetGlobal(cur, key, value)\n}\n\nfunc setGlobal(cur realm, key, value string) {\n\t/* THE RESTORE SEAL IS NOT AN ORDINARY GLOBAL.\n\t   restore.gno says sealing is \"IRREVERSIBLE… There is no argument\n\t   that unseals it\", and RestoreGlobal carries a reserved-key guard\n\t   to make that true. This function did not, so one admin call\n\t   writing key \"restore\" reopened the seal and the claim in that file\n\t   was simply false. The admin key is trusted and already holds\n\t   worse powers, so this is documentation drift rather than an\n\t   escalation — but a permanent-sounding promise that a two-word call\n\t   undoes is the kind of thing an auditor and a court both notice.\n\t   The durable version of this guard belongs in the vault, where a\n\t   logic swap cannot drop it; this is the half that ships today. */\n\tif key == keyRestore || key == keyRestoredCount {\n\t\tpanic(\"nslogic: \" + key + \" is written by the restore machinery, not by SetGlobal\")\n\t}\n\tif err := nsdata.SetGlobalRecord(cross(cur), key, value); err != nil {\n\t\tpanic(err)\n\t}\n}\n\n// SetTreasury moves the revenue-receiving role — independent of\n// nsdata's TransferAdmin, same reasoning as the original build's\n// admin/treasury separation.\nfunc SetTreasury(cur realm, newTreasury address) {\n\tassertNoSend(cur)\n\tassertIsAdmin(cur)\n\tif !newTreasury.IsValid() {\n\t\tpanic(\"nslogic: invalid treasury address\")\n\t}\n\tnsdata.SetTreasuryRecord(cross(cur), newTreasury)\n}\n\n// SelfAddress reports this deployed realm's own address — needed once,\n// at wiring time, so admin knows what to pass to\n// nsdata.ProposeTrustedLogicGrant.\nfunc SelfAddress(cur realm) address {\n\treturn cur.Address()\n}\n"},{"name":"blocked.gno","body":"package nslogic\n\nimport (\n\t\"strings\"\n\n\t\"gno.land/r/g1xr6tgxnpled50h74eafmvxway7z0ytr5rsmeme/nsdata/v1\"\n)\n\n/*\nTHE BLOCK LIST: labels nobody may hold, at any price.\n\nThis is NOT the reserved list, and confusing the two would undo both.\n\n\treserved (collections/reserved-labels.json, 232 labels) says\n\t\"blocked from the free tier, buyable at list price like anything\n\telse\". admin*meme being purchasable is deliberate: the harm it\n\tguards against is handing a phishing address out for nothing, not\n\tthe existence of the name. That list is enforced by the issuer,\n\twhich is the only place that decides what is free.\n\n\tTHIS list says \"not available, full stop\". It is the small set where\n\tthe answer does not change when somebody offers money: names that\n\timpersonate the registry itself, and the registry's own names.\n\nWHY IT LIVES IN THE VAULT'S GLOBALS RATHER THAN IN THIS FILE.\n\nBaking it in would mean a logic deploy to block a word, and the words\nworth blocking are exactly the ones discovered after launch — the label\nsomebody tried yesterday that nobody thought of. Globals are writable by\nthe admin, survive a logic swap because they belong to the vault, and\ncost storage only for what is actually stored.\n\nChunked because a global value is capped at 512 bytes. Eight chunks is\nabout four kilobytes, or several hundred labels, which is far more than\nthis list should ever need — if it grows past that, the thing being\nbuilt is a content policy and it belongs off chain where it can be\nupdated without a transaction.\n\nFAILS OPEN, and that is the uncomfortable choice, so it is written down.\nAn unreadable chunk blocks nothing rather than blocking everything.\nFailing closed here would mean one malformed global takes the whole\nregistry offline — every registration, for everyone, until an admin\nnotices. The harm from a blocked label slipping through is one bad name\nthat an admin can freeze; the harm from failing closed is total. The\nlist is also not the only defence: the issuer holds the reserved list,\nand FreezeName exists for anything that gets past both.\n*/\n\nconst (\n\tblockChunks   = 8\n\tblockKeyStem  = \"block\"\n\tblockChunkMax = 512\n)\n\n// blockKey is the global name for one chunk: block0 … block7.\nfunc blockKey(i int) string {\n\treturn blockKeyStem + string(rune('0'+i))\n}\n\n/*\nBlockedLabel reports whether a label may not be registered at all.\n\nCompares against the label as the registry stores it — lowercase, already\nvalidated — so a caller cannot slip past with different casing. Every\ncomparison is exact: this list holds whole labels, not substrings,\nbecause \"contains\" matching turns a list of a dozen words into an\nunpredictable filter that refuses names nobody meant to refuse.\n*/\nfunc BlockedLabel(label string) bool {\n\tif label == \"\" {\n\t\treturn false\n\t}\n\twant := strings.ToLower(label)\n\tfor i := 0; i \u003c blockChunks; i++ {\n\t\tv := nsdata.GetGlobal(blockKey(i))\n\t\tif v == \"\" {\n\t\t\tcontinue\n\t\t}\n\t\tfor _, e := range strings.Split(v, \",\") {\n\t\t\tif strings.ToLower(strings.TrimSpace(e)) == want {\n\t\t\t\treturn true\n\t\t\t}\n\t\t}\n\t}\n\treturn false\n}\n\n// BlockList returns one chunk, so the panel can show and edit what is\n// actually stored rather than what somebody believes is stored.\nfunc BlockList(i int) string {\n\tif i \u003c 0 || i \u003e= blockChunks {\n\t\treturn \"\"\n\t}\n\treturn nsdata.GetGlobal(blockKey(i))\n}\n\n// BlockChunks is the number of chunks, so the panel does not hard-code a\n// constant that this file might later change.\nfunc BlockChunks() int { return blockChunks }\n\n/*\nSetBlockList replaces one chunk. Admin only.\n\nReplaces rather than appends: an append-only list has no way to correct a\nmistake, and a mistake here refuses somebody a name for reasons nobody\ncan see. The panel reads the chunk, edits it, and writes it back whole.\n*/\nfunc SetBlockList(cur realm, i int, csv string) {\n\tassertNoSend(cur)\n\tassertIsAdmin(cur)\n\tif i \u003c 0 || i \u003e= blockChunks {\n\t\tpanic(\"nslogic: chunk out of range\")\n\t}\n\tif len(csv) \u003e blockChunkMax {\n\t\tpanic(\"nslogic: that chunk is longer than a global may be\")\n\t}\n\t// Normalised on the way in, so the comparison above never has to\n\t// account for whatever spacing somebody typed into a form.\n\tparts := strings.Split(csv, \",\")\n\tout := []string{}\n\tfor _, p := range parts {\n\t\tp = strings.ToLower(strings.TrimSpace(p))\n\t\tif p != \"\" {\n\t\t\tout = append(out, p)\n\t\t}\n\t}\n\tsetGlobal(cur, blockKey(i), strings.Join(out, \",\"))\n}\n\n// assertNotBlocked is the gate both doors pass through — the paid\n// registration and the free grant. One function, called twice, because\n// a block that only covers one route is not a block.\nfunc assertNotBlocked(label string) {\n\tif BlockedLabel(label) {\n\t\tpanic(\"nslogic: \" + label + \" is not available\")\n\t}\n}\n"},{"name":"domain.gno","body":"package nslogic\n\nimport (\n\t\"strconv\"\n\t\"strings\"\n\t\"time\"\n\n\t\"gno.land/r/g1xr6tgxnpled50h74eafmvxway7z0ytr5rsmeme/nsdata/v1\"\n)\n\n// RegisterDomain mints a new `*domain` — ADMIN-ONLY (2026-08-15 policy:\n// domains are never sold or owned by third parties). No payment: the\n// project isn't paying itself. Domains are also permanently\n// non-transferable by users (see nft.gno's TransferFrom).\nfunc RegisterDomain(cur realm, label string) {\n\tassertNoSend(cur)\n\tassertIsAdmin(cur)\n\tvalidateLabel(label)\n\tcaller := cur.Previous().Address()\n\tif err := nsdata.RegisterDomainRecord(cross(cur), label, caller, time.Now().Unix()); err != nil {\n\t\tpanic(err)\n\t}\n}\n\n// RenewDomain extends a domain's expiry by exactly one year from its\n// PREVIOUS due date, not from the payment date. No payment — domains are\n// always project-owned, so this is self-service upkeep, not a sale.\n// Deliberately does not call validateLabel — a tightened pattern only\n// gates new registrations, never renewals of what's already registered.\nfunc RenewDomain(cur realm, label string) {\n\tassertNoSend(cur)\n\tcaller := cur.Previous().Address()\n\tif err := nsdata.RenewDomainRecord(cross(cur), label, caller, time.Now().Unix()); err != nil {\n\t\tpanic(err)\n\t}\n}\n\n// ReassignDomain moves a domain record to a new owner. ADMIN-ONLY, and\n// the deliberate exception to \"domains are non-transferable\".\n//\n// It exists because domain ownership is snapshotted into the record at\n// registration time, while `admin` is a rotatable role. After the\n// planned EOA-to-multisig rotation, the incoming admin could not renew\n// or reprice any domain the outgoing key had registered — and if the\n// rotation happened *because* the old key was compromised, the attacker\n// could still reprice those domains afterwards. This is the recovery\n// lever for exactly that situation.\n//\n// Not a loophole in the non-transferability policy: that policy exists\n// to stop domains reaching third parties as tradeable assets, and this\n// path is reachable only by the current admin, moving a project domain\n// between project-controlled addresses.\nfunc ReassignDomain(cur realm, label string, newOwner address) {\n\tassertNoSend(cur)\n\tassertIsAdmin(cur)\n\towner := nsdata.GetDomainOwner(label)\n\n\t/* THE TRANSFER WIPES THE DOMAIN'S CONFIGURATION, so it is read back\n\t   and rewritten around the call.\n\t   RawTransferNFT applies wipeOnHandover, whose long rationale is\n\t   entirely about NAMES — clearing a seller's payment addresses so a\n\t   buyer does not inherit them. A domain's extra slot is not user\n\t   data: it is the domain's settings. price, len, nm, open and\n\t   maxterm all live there, so a deliberately CLOSED domain silently\n\t   reopened, pricing fell back to the global default, and the\n\t   per-domain term cap released to the project default — the brake\n\t   that exists to stop a script hoarding every good name under a free\n\t   domain. Nothing errored and nothing was emitted, and this fires\n\t   during a compromised-key recovery, when the operator is least able\n\t   to notice.\n\t   The durable fix is a vault function that moves the owner and\n\t   leaves extra alone; domains are project-owned and never reach a\n\t   third party, so there is no previous-owner data to protect. This\n\t   is the stopgap that works against a vault already deployed. */\n\tkeep := []string{\"price\", \"len\", \"nm\", \"open\", \"maxterm\"}\n\tsaved := make([]string, len(keep))\n\tfor i, k := range keep {\n\t\tsaved[i] = nsdata.GetDomainExtra(label, k)\n\t}\n\n\tif err := nsdata.RawTransferNFT(cross(cur), owner, owner, newOwner, \"*\"+label); err != nil {\n\t\tpanic(err)\n\t}\n\n\tfor i, k := range keep {\n\t\tif saved[i] == \"\" {\n\t\t\tcontinue // absent before, absent after — writing \"\" would store a blank\n\t\t}\n\t\tif err := nsdata.SetDomainExtraRecord(cross(cur), label, k, saved[i]); err != nil {\n\t\t\tpanic(err)\n\t\t}\n\t}\n}\n\n// -- pricing controls (admin) --\n//\n// Every amount here is MICRO-USD (1 USD = 1_000_000). Nothing is priced\n// in the chain token; ugnot is derived at payment time from the stored\n// rate, so a second settlement token later is a second rate rather than\n// a second price list.\n\n// SetDomainOpen enables or disables registration of new names under a\n// domain. Existing names are untouched: closing a domain stops new\n// mints, it does not seize anything already held.\nfunc SetDomainOpen(cur realm, label string, open bool) {\n\tassertNoSend(cur)\n\tassertIsAdmin(cur)\n\tv := \"1\"\n\tif !open {\n\t\tv = \"0\"\n\t}\n\tsetDomainExtra(cur, label, keyOpen, v)\n}\n\n// SetDomainBasePrice sets the domain's base price in micro-USD. Every\n// \"%N\" rule under the domain is relative to this, so one call reprices\n// the whole domain.\nfunc SetDomainBasePrice(cur realm, label string, microUsd int64) {\n\tassertNoSend(cur)\n\tassertIsAdmin(cur)\n\tif microUsd \u003c 0 {\n\t\tpanic(\"nslogic: price cannot be negative\")\n\t}\n\tsetDomainExtra(cur, label, keyPrice, strconv.FormatInt(microUsd, 10))\n}\n\n// SetDomainLengthRules prices by label length: \"3:2000000,4:%150,5:0\"\n// means 3-char names cost $2.00, 4-char names cost 150% of the domain\n// base, and 5-char names are free.\nfunc SetDomainLengthRules(cur realm, label, rules string) {\n\tassertNoSend(cur)\n\tassertIsAdmin(cur)\n\tsetDomainExtra(cur, label, keyLens, normalizeRules(rules, true))\n}\n\n// SetDomainNameOverrides prices specific labels: \"420:5000000,gm:%500\".\n// Highest precedence — this beats length rules and the base.\nfunc SetDomainNameOverrides(cur realm, label, rules string) {\n\tassertNoSend(cur)\n\tassertIsAdmin(cur)\n\tsetDomainExtra(cur, label, keyNames, normalizeRules(rules, false))\n}\n\n// SetDomainField writes an arbitrary key on a domain, for whatever a\n// future feature needs without a vault change.\nfunc SetDomainField(cur realm, label, key, value string) {\n\tassertNoSend(cur)\n\tassertIsAdmin(cur)\n\tif strings.HasPrefix(key, \"_\") {\n\t\tpanic(\"nslogic: keys beginning with '_' are reserved\")\n\t}\n\tsetDomainExtra(cur, label, key, value)\n}\n\nfunc setDomainExtra(cur realm, label, key, value string) {\n\tif err := nsdata.SetDomainExtraRecord(cross(cur), label, key, value); err != nil {\n\t\tpanic(err)\n\t}\n}\n\n// FreezeDomain / UnfreezeDomain: project-only power to permanently block\n// re-registration of a domain even after its grace period lapses.\n// DeleteDomain destroys a domain and every name beneath it.\n//\n// THE VAULT PRIMITIVE EXISTED BEFORE THIS DID, AND THAT MADE IT\n// UNREACHABLE. nsdata.DeleteDomainRecord gates on assertIsTrustedLogic,\n// so the only way to reach it is a function in this realm — and there\n// was not one. The capability shipped in the vault and could not be\n// used, which is worse than not shipping it, because it reads as\n// present.\n//\n// Bounded by the vault at maxDeletePerCall names per call. It returns\n// how many it removed and whether it finished, so a large domain is\n// several calls rather than a transaction that cannot fit in a block.\n// Repeat until done is true; calling it on a domain that is already gone\n// reports done rather than panicking, so a loop terminates cleanly.\n//\n// ADMIN ONLY, and deliberately not owner-callable: domains are\n// project-owned (SPEC.md §17b) and this destroys other people's names.\nfunc DeleteDomain(cur realm, label string) (removed int64, done bool) {\n\tassertNoSend(cur)\n\tassertIsAdmin(cur)\n\treturn nsdata.DeleteDomainRecord(cross(cur), label)\n}\n\n// SetDomainTTL sets how long a resolver may cache answers about this\n// domain, in seconds, where 0 means never cache — the ENS rule. Admin\n// only, since domains are project-owned.\nfunc SetDomainTTL(cur realm, label string, seconds int64) {\n\tassertNoSend(cur)\n\tassertIsAdmin(cur)\n\tif seconds \u003c 0 {\n\t\tpanic(\"nslogic: ttl may not be negative\")\n\t}\n\tif err := nsdata.SetDomainTTLRecord(cross(cur), label, seconds); err != nil {\n\t\tpanic(err)\n\t}\n}\n\nfunc FreezeDomain(cur realm, label string) {\n\tassertNoSend(cur)\n\tassertIsAdmin(cur)\n\tnsdata.SetDomainFrozen(cross(cur), label, true)\n}\n\nfunc UnfreezeDomain(cur realm, label string) {\n\tassertNoSend(cur)\n\tassertIsAdmin(cur)\n\tnsdata.SetDomainFrozen(cross(cur), label, false)\n}\n"},{"name":"freetier.gno","body":"package nslogic\n\nimport (\n\t\"strconv\"\n\t\"time\"\n\n\t\"gno.land/r/g1xr6tgxnpled50h74eafmvxway7z0ytr5rsmeme/nsdata/v1\"\n)\n\n/*\nTHE FREE TIER: a name somebody can hold without ever owning a wallet.\n\nThe vault has supported this since it was written — RegisterNameRecord\ntakes mintNFT=false and stores a record with no GRC-721 token, and\nPromoteToNFTRecord is described there as \"the paid free-tier upgrade\".\nNothing in the logic realm ever reached either. This is that wiring.\n\nWHY A RECORD WITHOUT A TOKEN IS THE RIGHT SHAPE, and not a compromise:\n\n\tIT IS A REAL NAME. Owned, resolvable, renewable, on chain, visible\n\tto anyone. Not a promise, not a queue position, not a row in our\n\tdatabase. Everything the paid tier does except be sold.\n\n\tIT CANNOT BE SOLD, and that is what makes giving it away safe.\n\tnsmarket escrows a TOKEN, and there is no token — so a squatter who\n\tfarms a thousand of these has farmed a thousand things nobody can\n\tbuy from them. The anti-abuse property falls out of the data model\n\trather than out of a rule somebody has to enforce.\n\n\tIT BECOMES SELLABLE BY BECOMING PAID. UpgradeFreeName takes the full\n\tlist price and mints the token. So the same name costs nothing and\n\tcosts full price, and which one depends entirely on whether you want\n\tto sell it.\n\nWHY THE PROJECT PAYS. The whole point is somebody with no GNOT, and\npossibly no wallet, who is going to put this name in front of an\naudience. Measured on Pearl: a registration is about 0.04 GNOT of fee\nplus a small storage deposit, and a free-tier record is smaller than a\npaid one because there is no token and no ledger entry. That is a\nmarketing cost per name, bounded by a daily cap we apply off chain,\nwhere it can be changed without a deploy.\n\nWHAT IS DELIBERATELY NOT HERE. No wordlist: storage is 100 ugnot a byte,\nso a ten-thousand-word dictionary is about 57 GNOT paid forever to\nencode a marketing decision that will change. The contract carries the\nstructural rules in reserved.gno and nothing else; the lists live off\nchain with the issuer, which is us, because the free path is granted\nrather than bought. See tools/gen-reserved.py.\n*/\n\nconst (\n\tkeyFreeDomain = \"free\"      // per-domain: \"1\" means the free tier is open here\n\tkeyGrantEnd   = \"grantend\"  // global: unix seconds, or \"never\"\n\tkeyGranter    = \"granter\"   // global: the address allowed to issue grants\n)\n\n/*\nTHE GRANTER IS NOT THE ADMIN, and that separation is the point.\n\nFree names are issued by a machine — something has to sign the grant\nafter a person proves an X account, and that key has to sit somewhere\nwarm. If that key were the admin key, a compromise of the issuing\nmachine would be a compromise of the registry: DeleteDomain destroys\nevery name under a domain and burns their tokens, instantly, with no\ntimelock.\n\nSo there is a second role that can do exactly one thing. Worst case for\na stolen granter key is that somebody gives away free names until we\nnotice and clear the role — a bounded, reversible, embarrassing loss\nrather than an unbounded one.\n\nStored in the vault's globals rather than in this realm, so it survives\na logic redeploy. Unset means nobody but the admin may grant, which is\nthe right default for a role that exists to be delegated deliberately.\n*/\nfunc Granter() address {\n\treturn address(nsdata.GetGlobal(keyGranter))\n}\n\n// SetGranter delegates grant issuance, or withdraws it. Passing the\n// empty address is the revocation, and it is the first thing to reach\n// for if the issuing machine is ever in doubt.\nfunc SetGranter(cur realm, a address) {\n\tassertNoSend(cur)\n\tassertIsAdmin(cur)\n\tif a != \"\" \u0026\u0026 !a.IsValid() {\n\t\tpanic(\"nslogic: not an address\")\n\t}\n\tsetGlobal(cur, keyGranter, a.String())\n}\n\n/*\nSetFreeLengths tunes which lengths the free tier will issue.\n\nIn the panel rather than in a redeploy: nslogic is about 22 GNOT to\ndeploy, and \"should four-character names be free this month\" is a\nmarketing question that should cost a signature, not a deployment.\n\nIT DOES NOT REACH BACKWARDS. Raising the minimum stops NEW issuance of\nshort names; every one already granted stays granted and keeps renewing\nfree. FreeLabelAllowed is called by GrantFreeName and by nothing else,\nwhich is what makes that true — see the note on it in reserved.gno.\n*/\nfunc SetFreeLengths(cur realm, min, max int) {\n\tassertNoSend(cur)\n\tassertIsAdmin(cur)\n\tif min \u003c 1 || max \u003e 63 || min \u003e max {\n\t\tpanic(\"nslogic: a free-tier length range must sit inside 1..63 and be the right way round\")\n\t}\n\tsetGlobal(cur, keyFreeMin, strconv.Itoa(min))\n\tsetGlobal(cur, keyFreeMax, strconv.Itoa(max))\n}\n\nfunc assertMayGrant(cur realm) {\n\tcaller := cur.Previous().Address()\n\tif caller == nsdata.GetAdmin() {\n\t\treturn\n\t}\n\tg := Granter()\n\tif g == \"\" || caller != g {\n\t\tpanic(\"nslogic: only the admin or the granter may issue free names\")\n\t}\n}\n\n// DomainFree reports whether a domain accepts free-tier registrations.\n// Off by default, and per-domain on purpose: the free tier is a\n// promotion aimed at a few domains, not a property of the registry.\nfunc DomainFree(domainLabel string) bool {\n\treturn nsdata.GetDomainExtra(domainLabel, keyFreeDomain) == \"1\"\n}\n\n// SetDomainFree opens or closes the free tier for one domain.\nfunc SetDomainFree(cur realm, domainLabel string, on bool) {\n\tassertNoSend(cur)\n\tassertIsAdmin(cur)\n\tif !nsdata.DomainExists(domainLabel) {\n\t\tpanic(\"nslogic: no such domain\")\n\t}\n\tv := \"\"\n\tif on {\n\t\tv = \"1\"\n\t}\n\tif err := nsdata.SetDomainExtraRecord(cross(cur), domainLabel, keyFreeDomain, v); err != nil {\n\t\tpanic(err)\n\t}\n}\n\n/*\nGrantWindowOpen reports whether free grants are being issued at all.\n\nA single global switch that closes everything at once, because the\nfailure this guards against is not a clever attack — it is noticing on a\nSunday that something is wrong and wanting one call that stops it. An\nunset value means open; \"never\" means open with no end date, which is\nwhat the admin panel already writes.\n*/\nfunc GrantWindowOpen() bool {\n\treturn readGrantWindow(nsdata.GetGlobal(keyGrantEnd), time.Now().Unix())\n}\n\n// readGrantWindow is GrantWindowOpen with the clock and the stored value\n// handed in, so the reading can be tested without a chain.\nfunc readGrantWindow(v string, now int64) bool {\n\tif v == \"\" || v == \"never\" {\n\t\treturn true\n\t}\n\tend, err := strconv.ParseInt(v, 10, 64)\n\tif err != nil {\n\t\t// An unparseable window is a misconfiguration, and the safe\n\t\t// reading of a misconfigured gate is \"closed\". Giving names away\n\t\t// because a setting was mistyped is the wrong way to fail.\n\t\treturn false\n\t}\n\treturn now \u003c end\n}\n\n/*\nGrantFreeName registers a free-tier record to somebody else.\n\nADMIN OR GRANTER, and that is the whole trust model: the eligibility rules —\nwhich Twitter account, how old, how many followers, whether the label is\na dictionary word — live off chain with the issuer. Putting them here\nwould freeze a marketing policy into a realm and cost storage to do it.\nWhat the chain enforces is the part that must not depend on our\ncorrectness: the label is structurally allowed, the domain opted in, and\nthe window is open.\n\n`owner` may be an address the recipient generated in their browser\nseconds ago and has never funded. That is the point.\n*/\nfunc GrantFreeName(cur realm, label, domainLabel string, owner address) {\n\tassertNoSend(cur)\n\tassertMayGrant(cur)\n\n\tif !GrantWindowOpen() {\n\t\tpanic(\"nslogic: free grants are closed\")\n\t}\n\tif !DomainFree(domainLabel) {\n\t\tpanic(\"nslogic: *\" + domainLabel + \" does not offer free names\")\n\t}\n\tif !DomainOpen(domainLabel) {\n\t\tpanic(\"nslogic: this domain is not accepting new names\")\n\t}\n\tvalidateLabel(label)\n\tassertNotBlocked(label)\n\t// The structural backstop. The issuer has already applied the full\n\t// policy; this is what stops a bug there giving away a four-letter\n\t// name or a pure-numeric one.\n\tif !FreeLabelAllowed(label) {\n\t\tpanic(\"nslogic: \" + label + \" is not available on the free tier\")\n\t}\n\tif !owner.IsValid() {\n\t\tpanic(\"nslogic: a grant needs a recipient\")\n\t}\n\n\t// mintNFT=false — a record, not a token. See the note at the top.\n\tif err := nsdata.RegisterNameRecord(cross(cur), label, domainLabel, owner,\n\t\ttime.Now().Unix(), false); err != nil {\n\t\tpanic(err)\n\t}\n}\n\n/*\nUpgradeFreeName mints the token for a free-tier record, at full price.\n\nThis is the only way a free name becomes sellable, and the price is the\nordinary list price with no referral discount — a discount is for\nbringing somebody new, and this person is already here.\n\n`newOwner` exists because the common case is somebody who has decided to\nsell and, in the same moment, wants the name in a wallet they actually\ncontrol rather than the browser key they were given. The vault handles\nthat move inside PromoteToNFTRecord.\n\nCallable by the record's current owner only. Anyone may PAY for\nsomebody else's upgrade in the sense that they can hand over the money,\nbut they cannot perform it — a stranger promoting your name into a token\nand choosing where it lands is a theft with a receipt.\n*/\nfunc UpgradeFreeName(cur realm, label, domainLabel string, newOwner address) {\n\tcaller := cur.Previous().Address()\n\tif nsdata.GetNameOwner(label, domainLabel) != caller {\n\t\tpanic(\"nslogic: restricted to name owner\")\n\t}\n\tif !nsdata.IsNameValid(label, domainLabel) {\n\t\tpanic(\"nslogic: this name has expired\")\n\t}\n\tif newOwner == \"\" {\n\t\tnewOwner = caller\n\t}\n\tif !newOwner.IsValid() {\n\t\tpanic(\"nslogic: invalid destination address\")\n\t}\n\n\t// Full list price, resolved now rather than at grant time — the\n\t// name's worth is what it is worth today, and a free name held for a\n\t// year should not be upgradable at last year's rate.\n\ttakePaymentSplit(cur, NamePrice(domainLabel, label), \"\", 0)\n\n\tif err := nsdata.PromoteToNFTRecord(cross(cur), label, domainLabel, newOwner); err != nil {\n\t\tpanic(err)\n\t}\n}\n\n/*\nGrantFreeRenewal extends a free-tier name at no charge.\n\nTHE DEAL IS THE DISPLAY, and it is checked off chain: the issuer signs\nsomebody in on X, reads their display name, and finds the name in it.\nThat single act proves both halves at once — that a real account still\nexists, and that it is still carrying the name — which is why renewal is\none click a year rather than a form.\n\nFREE-TIER RECORDS ONLY. A paid name renewed for nothing is revenue given\naway by accident, and the two are told apart by whether a token was ever\nminted. isNFT is the flag, and it is the vault's, not ours.\n\nNO OWNERSHIP PROOF IS ASKED FOR, and that is deliberate rather than\nmissing. Renewing somebody else's name is a GIFT — it takes nothing,\nmoves nothing, and grants the caller no power over it. The only cost of\nallowing it is ours, and the vault's renewal cap already bounds how far\nany single name can be pushed. A proof requirement here would buy\nnothing and would cost the one-click renewal that makes the deal work.\n\nRenewNameRecord insists the caller is the owner, so the owner is looked\nup and passed. That is not a bypass: this realm is the trusted logic,\nand what it is asserting is exactly true — this renewal is on that\nowner's behalf.\n*/\nfunc GrantFreeRenewal(cur realm, label, domainLabel string) {\n\tassertNoSend(cur)\n\tassertMayGrant(cur)\n\n\tif !nsdata.NameExists(label, domainLabel) {\n\t\tpanic(\"nslogic: no such name\")\n\t}\n\towner, _, _, _, _, isNFT := nsdata.GetNameInfo(label, domainLabel)\n\tif isNFT {\n\t\tpanic(\"nslogic: \" + label + \"*\" + domainLabel +\n\t\t\t\" is a paid name — free renewal is for free-tier records\")\n\t}\n\tif RenewalsLeft(label, domainLabel) \u003c= 0 {\n\t\tpanic(\"nslogic: this name is already paid as far ahead as a name may run\")\n\t}\n\tif err := nsdata.RenewNameRecord(cross(cur), label, domainLabel, owner,\n\t\ttime.Now().Unix()); err != nil {\n\t\tpanic(err)\n\t}\n}\n\n// IsFreeTier reports whether a name is a record without a token — the\n// free tier. Exported because the site and the issuer both need to ask\n// before offering a free renewal, and neither should infer it.\nfunc IsFreeTier(label, domainLabel string) bool {\n\tif !nsdata.NameExists(label, domainLabel) {\n\t\treturn false\n\t}\n\t_, _, _, _, _, isNFT := nsdata.GetNameInfo(label, domainLabel)\n\treturn !isNFT\n}\n"},{"name":"gnomod.toml","body":"module = \"gno.land/r/g1xr6tgxnpled50h74eafmvxway7z0ytr5rsmeme/nslogic/v4\"\ngno = \"0.9\"\n"},{"name":"holdings.gno","body":"package nslogic\n\nimport (\n\t\"strings\"\n\n\t\"gno.land/p/nt/ufmt/v0\"\n\t\"gno.land/r/g1xr6tgxnpled50h74eafmvxway7z0ytr5rsmeme/nsdata/v1\"\n)\n\n// Answering \"what does this address hold\" in ONE read.\n//\n// The site used to ask the chain for every name in the REGISTRY and then\n// throw away the ones that did not match — one HTTP round trip per name\n// in the world to draw one person's collection. Six names took three\n// seconds, and it gets linearly worse for everybody every time anybody\n// else registers anything.\n//\n// The scan itself is unavoidable: nsdata keys names by domain*label and\n// has no index by owner, and nsdata is the permanent vault so no index\n// can be added to it now. But the scan does not have to happen across\n// the network. nslogic is swappable and can walk the same records inside\n// a single query, which turns N round trips into one.\n//\n// Nothing is cached and nothing is trusted from the caller: every row is\n// read from the vault at the moment of the query, exactly as before.\n//\n// Paged, because a query has a gas ceiling of its own and a registry\n// with fifty thousand names in it must not become one enormous read that\n// nobody can complete. `after` is the vault's own cursor, so paging here\n// is the same paging ExportNames already does.\nconst maxHoldingsPage = 200\n\n// NamesOwnedBy returns rows of `label*domain,registered,expires,streak,frozen`\n// separated by \";\", plus the cursor to continue from. Labels are\n// restricted to [a-z0-9-] so neither separator can appear inside a field.\n//\n// `next` is empty when the scan has reached the end of the registry. It\n// is NOT empty merely because this page found no matches: a page of two\n// hundred names belonging to other people returns no rows and a cursor,\n// and the caller keeps going.\nfunc NamesOwnedBy(owner address, after string, limit int) (rows, next string) {\n\tif limit \u003c= 0 || limit \u003e maxHoldingsPage {\n\t\tlimit = maxHoldingsPage\n\t}\n\tkeys, nxt := nsdata.ExportNames(\"\", after, limit)\n\n\tvar b strings.Builder\n\tfor _, key := range strings.Split(keys, \",\") {\n\t\tif key == \"\" {\n\t\t\tcontinue\n\t\t}\n\t\t// The tree key is domain*label; the token ID is label*domain.\n\t\tat := strings.Index(key, \"*\")\n\t\tif at \u003c 0 {\n\t\t\tcontinue\n\t\t}\n\t\tdomainLabel, label := key[:at], key[at+1:]\n\n\t\to, registered, expires, streak, frozen, _ := nsdata.GetNameInfo(label, domainLabel)\n\t\tif o != owner {\n\t\t\tcontinue\n\t\t}\n\t\tif b.Len() \u003e 0 {\n\t\t\tb.WriteString(\";\")\n\t\t}\n\t\tb.WriteString(ufmt.Sprintf(\"%s*%s,%d,%d,%d,%t\",\n\t\t\tlabel, domainLabel, registered, expires, streak, frozen))\n\t}\n\treturn b.String(), nxt\n}\n\n// CountNamesOwnedBy is the same walk without the strings, for a header\n// that wants a number before the list has finished arriving.\nfunc CountNamesOwnedBy(owner address, after string, limit int) (n int, next string) {\n\tif limit \u003c= 0 || limit \u003e maxHoldingsPage {\n\t\tlimit = maxHoldingsPage\n\t}\n\tkeys, nxt := nsdata.ExportNames(\"\", after, limit)\n\tfor _, key := range strings.Split(keys, \",\") {\n\t\tif key == \"\" {\n\t\t\tcontinue\n\t\t}\n\t\tat := strings.Index(key, \"*\")\n\t\tif at \u003c 0 {\n\t\t\tcontinue\n\t\t}\n\t\tif nsdata.GetNameOwner(key[at+1:], key[:at]) == owner {\n\t\t\tn++\n\t\t}\n\t}\n\treturn n, nxt\n}\n\n// Everything the profile page draws, in ONE read.\n//\n// It was making seventeen queries: three to establish that the name\n// exists and when it expires, then fourteen more — one per extension\n// slot — that could not start until the first three had come back. Two\n// waves, and the node serves only a few at a time, so a profile took\n// seconds to assemble out of values that all live in the same record.\n//\n// Values come back in the ORDER THE KEYS WERE ASKED FOR rather than as\n// key=value pairs. An extension key is allowed to contain \"=\" (only\n// commas and newlines are forbidden), so a pair encoding would have an\n// ambiguous split; positional has none.\n//\n// Line 0 is the record: owner, registered, expires, streak, frozen, and\n// the grace period, which is global but is one more query the page would\n// otherwise have to make on its own.\n//\n// An empty return means the name is not registered. That is a real\n// answer rather than an error: asking about a name nobody owns is the\n// normal case on a profile URL somebody typed.\nfunc ProfileOf(label, domainLabel, keys string) string {\n\tif !nsdata.NameExists(label, domainLabel) {\n\t\treturn \"\"\n\t}\n\towner, registered, expires, streak, frozen, _ := nsdata.GetNameInfo(label, domainLabel)\n\n\tvar b strings.Builder\n\tb.WriteString(ufmt.Sprintf(\"%s,%d,%d,%d,%t,%d\",\n\t\towner.String(), registered, expires, streak, frozen, nsdata.GetGracePeriod()))\n\n\tfor _, k := range strings.Split(keys, \",\") {\n\t\tb.WriteString(\"\\n\")\n\t\tif k == \"\" {\n\t\t\tcontinue\n\t\t}\n\t\tv := nsdata.GetNameExtra(label, domainLabel, k)\n\n\t\t// THE GNO.LAND ADDRESS ANSWERS EVEN WHEN NOBODY SAVED ONE.\n\t\t//\n\t\t// A name is an NFT held by an address, so the owner is already\n\t\t// public — it is the first field of the record above. Returning\n\t\t// it here as well costs no storage and means a name can be paid\n\t\t// without its holder ever having opened a profile editor, which\n\t\t// is most of them, and without a transaction to back-fill the\n\t\t// ones registered before this existed.\n\t\t//\n\t\t// Doing it in the realm rather than in the website is the whole\n\t\t// point: any client asking this realm for the gno key gets the\n\t\t// same answer our pages do. A front end that derived it locally\n\t\t// would leave every other reader with nothing.\n\t\t//\n\t\t// \"off\" is the opt-out, and it has to be a sentinel. An empty\n\t\t// value is indistinguishable from never having set one, so an\n\t\t// empty gno field would simply hand the derived address back on\n\t\t// the next read.\n\t\tif k == \"gno\" {\n\t\t\tif v == \"off\" {\n\t\t\t\tv = \"\"\n\t\t\t} else if v == \"\" {\n\t\t\t\tv = owner.String()\n\t\t\t}\n\t\t}\n\t\tb.WriteString(escapeValue(v))\n\t}\n\treturn b.String()\n}\n\n// Extension VALUES are free text — a bio can hold anything a person can\n// type, newlines included — so they cannot be newline-separated raw.\n// Escaping is done here rather than stripping, because silently eating a\n// line break out of somebody's bio is data loss on a read path.\nfunc escapeValue(s string) string {\n\ts = strings.ReplaceAll(s, `\\`, `\\\\`)\n\ts = strings.ReplaceAll(s, \"\\n\", `\\n`)\n\treturn strings.ReplaceAll(s, \"\\r\", `\\r`)\n}\n\n// RecentNames answers \"what has just been registered\" in one read.\n//\n// The site used to build this by listing every key and then reading each\n// record one at a time — the same shape NamesOwnedBy replaced for a\n// wallet's holdings, and the same problem: fine at thirteen names, one\n// query per name forever after. A feed that gets slower every time\n// somebody registers is a feed that eventually stops loading on the busy\n// day you most wanted it.\n//\n// Returns rows of \"label*domain,registered,expires,owner\", newest first,\n// and a cursor. Sorting happens across a PAGE, not the whole registry:\n// the vault stores names alphabetically and there is no index by date,\n// so a caller that wants the true newest reads every page and merges.\n// That is one query per 200 names instead of one per name — enough of a\n// difference to stop mattering, without pretending the vault has an\n// index it does not have.\nfunc RecentNames(after string, limit int) (rows, next string) {\n\tif limit \u003c= 0 || limit \u003e maxHoldingsPage {\n\t\tlimit = maxHoldingsPage\n\t}\n\tkeys, nxt := nsdata.ExportNames(\"\", after, limit)\n\n\ttype row struct {\n\t\ttext string\n\t\tat   int64\n\t}\n\tvar found []row\n\tfor _, key := range strings.Split(keys, \",\") {\n\t\tif key == \"\" {\n\t\t\tcontinue\n\t\t}\n\t\t// The tree key is domain*label; the token ID is label*domain.\n\t\tat := strings.Index(key, \"*\")\n\t\tif at \u003c 1 {\n\t\t\tcontinue\n\t\t}\n\t\tdomainLabel, label := key[:at], key[at+1:]\n\t\towner, registered, expires, _, _, _ := nsdata.GetNameInfo(label, domainLabel)\n\t\tfound = append(found, row{\n\t\t\ttext: ufmt.Sprintf(\"%s*%s,%d,%d,%s\", label, domainLabel, registered, expires, owner.String()),\n\t\t\tat:   registered,\n\t\t})\n\t}\n\n\t// Insertion sort, newest first. The page is at most 200 entries and\n\t// gno has no sort in scope here; anything cleverer would be more code\n\t// than the problem deserves.\n\tfor i := 1; i \u003c len(found); i++ {\n\t\tv := found[i]\n\t\tj := i - 1\n\t\tfor j \u003e= 0 \u0026\u0026 found[j].at \u003c v.at {\n\t\t\tfound[j+1] = found[j]\n\t\t\tj--\n\t\t}\n\t\tfound[j+1] = v\n\t}\n\n\tvar b strings.Builder\n\tfor _, r := range found {\n\t\tif b.Len() \u003e 0 {\n\t\t\tb.WriteString(\";\")\n\t\t}\n\t\tb.WriteString(r.text)\n\t}\n\treturn b.String(), nxt\n}\n"},{"name":"name.gno","body":"package nslogic\n\nimport (\n\t\"strings\"\n\t\"time\"\n\n\t\"gno.land/r/g1xr6tgxnpled50h74eafmvxway7z0ytr5rsmeme/nsdata/v1\"\n)\n\n// Profile field names inside a name's extension slot. The vault stores\n// them as opaque strings and bounds their size; what they MEAN is this\n// realm's convention, so new profile fields need no vault change.\nconst (\n\tkeyDisplayName = \"displayName\"\n\tkeyBio         = \"bio\"\n\tkeyWebsite     = \"website\"\n\tkeyAvatar      = \"avatar\"\n)\n\n// RegisterName mints `label*domain` under an existing domain. Anyone may\n// call this. Minting still works while the parent domain is past its own\n// due date, as long as its grace period hasn't fully lapsed and it isn't\n// frozen — nsdata's RegisterNameRecord enforces that.\nfunc RegisterName(cur realm, label, domainLabel string) {\n\tregisterName(cur, label, domainLabel, \"\")\n}\n\n// RegisterNameRef is RegisterName with a referral code — the NAME of\n// whoever sent the buyer here, as it appears in the link.\n//\n// It is a second entry point rather than a third argument on the first\n// one because the first one is already deployed and already being called\n// by wallets and by the site. Changing its arity would break every one\n// of those callers at the same instant, which is exactly the failure the\n// voting realm produced last time; adding a function breaks nothing and\n// lets the front end pick whichever the deployed realm actually has.\n//\n// A code that does not resolve — misspelt, lapsed, or the buyer's own\n// name — is IGNORED, not rejected. The registration goes through at\n// list price. Refusing to sell somebody a name because they mistyped\n// somebody else's would be an absurd way to lose a sale.\nfunc RegisterNameRef(cur realm, label, domainLabel, refName string) {\n\tregisterName(cur, label, domainLabel, refName)\n}\n\nfunc registerName(cur realm, label, domainLabel, refName string) {\n\tvalidateLabel(label)\n\tassertNotBlocked(label)\n\tif !DomainOpen(domainLabel) {\n\t\tpanic(\"nslogic: this domain is not accepting new names\")\n\t}\n\tcaller := cur.Previous().Address()\n\n\t// Resolved BEFORE anything is written, so the tier and the discount\n\t// are read from the world the buyer was quoted in. Registering first\n\t// would change the buyer's own primary name and, on a self-referral\n\t// that slipped through, their tenure with it.\n\tref := resolveReferrer(refName, caller)\n\tprice := NamePrice(domainLabel, label)\n\tif ref != \"\" {\n\t\tif pct := tierPercent(RefDiscountRules(), TenureYears(ref)); pct \u003e 0 {\n\t\t\tprice -= price * pct / 100\n\t\t}\n\t}\n\tpayTo, cut := referralCut(ref, price)\n\ttakePaymentSplit(cur, price, payTo, cut)\n\n\tif err := nsdata.RegisterNameRecord(cross(cur), label, domainLabel, caller, time.Now().Unix(), true); err != nil {\n\t\tpanic(err)\n\t}\n\n\t// Written after the record exists, because the extension slot is\n\t// created by registration. Only recorded when the cut was actually\n\t// paid: a referrer who earned nothing on the sale has no claim on\n\t// the renewals either, and storing them would say otherwise.\n\tif ref != \"\" \u0026\u0026 cut \u003e 0 {\n\t\tsetExtra(cur, label, domainLabel, keyRef, ref.String())\n\t\tcreditReferral(cur, ref)\n\t}\n}\n\n// RenewName — same anchoring rule as RenewDomain: extends from the\n// previous due date, not from the payment date. Deliberately does not\n// call validateLabel — an already-registered name must keep renewing\n// under whatever pattern was in force when it was minted.\nfunc RenewName(cur realm, label, domainLabel string) {\n\t// No new code is accepted here and none is needed: the referrer was\n\t// fixed into this name when it was registered, and renewals keep\n\t// paying them for as long as the name survives unbroken. If it\n\t// lapses or is sold the vault destroys the record's extras, so the\n\t// link ends without this realm having to notice.\n\t//\n\t// Renewals pay the FULL price. The discount was a reason to arrive;\n\t// it is not a permanent lower rate, and compounding it every year\n\t// would make the referred name cheaper forever than the same name\n\t// bought directly.\n\tassertRoomToRenew(label, domainLabel)\n\tprice := NamePrice(domainLabel, label)\n\tref := ReferrerOf(label, domainLabel)\n\tpayTo, cut := referralCut(ref, price)\n\ttakePaymentSplit(cur, price, payTo, cut)\n\n\tcaller := cur.Previous().Address()\n\tif err := nsdata.RenewNameRecord(cross(cur), label, domainLabel, caller, time.Now().Unix()); err != nil {\n\t\tpanic(err)\n\t}\n\tif cut \u003e 0 {\n\t\tcreditReferral(cur, ref)\n\t}\n}\n\n// SetProfile writes the four conventional profile fields. Each lands in\n// the name's extension slot, where the vault enforces size bounds.\nfunc SetProfile(cur realm, label, domainLabel, displayName, bio, website, avatar string) {\n\tassertNoSend(cur)\n\tassertNameOwner(cur, label, domainLabel)\n\tsetExtra(cur, label, domainLabel, keyDisplayName, displayName)\n\tsetExtra(cur, label, domainLabel, keyBio, bio)\n\tsetExtra(cur, label, domainLabel, keyWebsite, website)\n\tsetExtra(cur, label, domainLabel, keyAvatar, avatar)\n}\n\n// SetNameField writes a single arbitrary key — cross-chain addresses and\n// anything else a future feature needs, with no vault change required.\n// Keys used by this realm's own machinery are reserved.\nfunc SetNameField(cur realm, label, domainLabel, key, value string) {\n\tassertNoSend(cur)\n\tassertNameOwner(cur, label, domainLabel)\n\tif strings.HasPrefix(key, \"_\") {\n\t\tpanic(\"nslogic: keys beginning with '_' are reserved\")\n\t}\n\t// `ref` decides who gets paid out of every renewal of this name. A\n\t// holder who could write it would set it to an address they control\n\t// and take a permanent cut of their own renewals out of the\n\t// treasury — so the one key that moves money is the one key its\n\t// owner may not touch.\n\tif key == keyRef {\n\t\tpanic(\"nslogic: '\" + keyRef + \"' is set at registration and cannot be edited\")\n\t}\n\tsetExtra(cur, label, domainLabel, key, value)\n}\n\nfunc setExtra(cur realm, label, domainLabel, key, value string) {\n\tif err := nsdata.SetNameExtraRecord(cross(cur), label, domainLabel, key, value); err != nil {\n\t\tpanic(err)\n\t}\n}\n\nfunc assertNameOwner(cur realm, label, domainLabel string) {\n\tif nsdata.GetNameOwner(label, domainLabel) != cur.Previous().Address() {\n\t\tpanic(\"nslogic: restricted to name owner\")\n\t}\n}\n\n// SetPrimaryName lets a wallet explicitly choose (and later change) which\n// of its names is used for reverse resolution.\nfunc SetPrimaryName(cur realm, label, domainLabel string) {\n\tassertNoSend(cur)\n\tcaller := cur.Previous().Address()\n\tif nsdata.GetNameOwner(label, domainLabel) != caller {\n\t\tpanic(\"nslogic: restricted to name owner\")\n\t}\n\tif err := nsdata.SetPrimaryRecord(cross(cur), caller, label, domainLabel); err != nil {\n\t\tpanic(err)\n\t}\n}\n\n/*\nClearPrimaryName switches reverse resolution OFF for the caller.\n\nWHY THIS HAS TO EXIST, and it is a hole rather than a feature request.\n\nnsdata sets the reverse pointer AUTOMATICALLY on a wallet's first\nregistration — RegisterNameRecord does it, and nothing asked the person\nfirst. From that moment their address answers with their name, so anybody\nwho sees that address anywhere can find the persona attached to it, and\nevery transaction it has ever made becomes attributable. Retroactively:\nthe chain is permanent and indexers keep copies.\n\nAnd until now there was NO WAY OUT. ClearPrimaryRecord is gated to\ntrusted logic, and no logic realm exposed it, so the one person the\npointer is about could not remove it. A name service is a bridge between\na persona and a wallet; publishing that bridge without consent and\nwithout an exit is the worst version of it.\n\nThis is the exit. It takes no arguments on purpose: a wallet has exactly\none reverse pointer, and the only address it can clear is its own, so\nthere is nothing to pass and nothing to get wrong.\n\nWHAT IT DOES NOT DO, said plainly because a promise we cannot keep would\nbe worse than the hole: clearing stops FUTURE lookups. It does not\nunpublish what was already visible, and it cannot — the history is on\nchain and other people have copies. The honest sequence is to choose\nbefore publishing, which is why the site now shows what the link exposes\nbefore it offers to make it.\n*/\nfunc ClearPrimaryName(cur realm) {\n\tassertNoSend(cur)\n\tnsdata.ClearPrimaryRecord(cross(cur), cur.Previous().Address())\n}\n\n// SetNameTTL is the owner's cache policy for their own name, in\n// seconds, where 0 MEANS NEVER CACHE — copied from the ENS registry so\n// integrators need not read anything of ours to handle it.\n//\n// The owner sets it rather than the registry because only they know the\n// risk: a name being used to receive payments wants 0, since a cached\n// answer after a sale pays the previous owner; a name used as a display\n// handle can safely be cached for a long time.\nfunc SetNameTTL(cur realm, label, domainLabel string, seconds int64) {\n\tassertNoSend(cur)\n\tassertNameOwner(cur, label, domainLabel)\n\tif seconds \u003c 0 {\n\t\tpanic(\"nslogic: ttl may not be negative\")\n\t}\n\tif err := nsdata.SetNameTTLRecord(cross(cur), label, domainLabel, seconds); err != nil {\n\t\tpanic(err)\n\t}\n}\n\nfunc FreezeName(cur realm, label, domainLabel string) {\n\tassertNoSend(cur)\n\tassertIsAdmin(cur)\n\tnsdata.SetNameFrozen(cross(cur), label, domainLabel, true)\n}\n\nfunc UnfreezeName(cur realm, label, domainLabel string) {\n\tassertNoSend(cur)\n\tassertIsAdmin(cur)\n\tnsdata.SetNameFrozen(cross(cur), label, domainLabel, false)\n}\n\n// -- resolution --\n\nfunc ResolveName(label, domainLabel string) address {\n\tif !nsdata.IsNameValid(label, domainLabel) {\n\t\tpanic(\"nslogic: name is expired\")\n\t}\n\treturn nsdata.GetNameOwner(label, domainLabel)\n}\n\n// ResolveAddress performs reverse resolution: address -\u003e name.\n//\n// It must verify the ownership it is asserting, not merely that the\n// pointed-at name exists and is unexpired. nsdata's `primary` tree is a\n// record of what an address last SELECTED, not proof of what it still\n// owns — and although nsdata now clears the pointer on transfer and\n// reclaim, this check is the half that lives in the swappable realm and\n// therefore stays correct even against pointers written by an older\n// version of the logic. Without it, an address that sold or lost a name\n// kept reverse-resolving to it, impersonating the new owner.\nfunc ResolveAddress(addr address) string {\n\ttid := nsdata.GetPrimary(addr)\n\tif tid == \"\" {\n\t\treturn \"\"\n\t}\n\tlabel, domainLabel := splitTokenID(tid)\n\tif !nsdata.IsNameValid(label, domainLabel) {\n\t\treturn \"\"\n\t}\n\tif nsdata.GetNameOwner(label, domainLabel) != addr {\n\t\treturn \"\"\n\t}\n\treturn tid\n}\n\n// splitTokenID splits a `label*domain` token ID.\nfunc splitTokenID(tid string) (label, domainLabel string) {\n\tfor i := 0; i \u003c len(tid); i++ {\n\t\tif tid[i] == '*' {\n\t\t\treturn tid[:i], tid[i+1:]\n\t\t}\n\t}\n\treturn tid, \"\"\n}\n"},{"name":"nft.gno","body":"package nslogic\n\nimport (\n\t\"strings\"\n\n\t\"gno.land/r/g1xr6tgxnpled50h74eafmvxway7z0ytr5rsmeme/nsdata/v1\"\n)\n\n// This realm only exposes the WRITE side of the GRC-721 surface. Reads\n// (BalanceOf, OwnerOf, TokenURI, GetApproved, IsApprovedForAll, and\n// every listing/status/profile query) have no authorization check to\n// perform and are served directly by nsdata — no reason to duplicate\n// them here.\n\nfunc isDomainToken(tid string) bool {\n\treturn strings.HasPrefix(tid, \"*\")\n}\n\n// isTokenValid checks the underlying domain/name's expiry via nsdata's\n// own validity reads — the anti-scam pattern (an expired name must not\n// be sellable) enforced at the transfer boundary, not just at read time.\nfunc isTokenValid(tid string) bool {\n\tif isDomainToken(tid) {\n\t\treturn nsdata.IsDomainValid(tid[1:])\n\t}\n\tlabel, domainLabel := splitTokenID(tid)\n\treturn nsdata.IsNameValid(label, domainLabel)\n}\n\nfunc Approve(cur realm, to address, tid string) {\n\tassertNoSend(cur)\n\tif isDomainToken(tid) {\n\t\tpanic(\"nslogic: domains are project-owned and non-transferable\")\n\t}\n\tif !isTokenValid(tid) {\n\t\tpanic(\"nslogic: cannot approve an expired token\")\n\t}\n\tcaller := cur.Previous().Address()\n\tif err := nsdata.RawApprove(cross(cur), caller, to, tid); err != nil {\n\t\tpanic(err)\n\t}\n}\n\nfunc SetApprovalForAll(cur realm, operator address, approved bool) {\n\tassertNoSend(cur)\n\tcaller := cur.Previous().Address()\n\tif err := nsdata.RawSetApprovalForAll(cross(cur), caller, operator, approved); err != nil {\n\t\tpanic(err)\n\t}\n}\n\n// TransferFrom / SafeTransferFrom are the actual enforcement point for\n// both (a) the anti-scam pattern — blocking transfer of an expired token\n// closes off third-party marketplace sales without blocking the owner's\n// own ability to reclaim by renewing (renewal never goes through these)\n// — and (b) the domains-are-never-transferable policy: blocking it here,\n// not just at RegisterDomain, is what actually closes the loophole.\n// (ReassignDomain in domain.gno is the deliberate admin-only exception;\n// see its comment.)\nfunc TransferFrom(cur realm, from, to address, tid string) {\n\tassertNoSend(cur)\n\tif isDomainToken(tid) {\n\t\tpanic(\"nslogic: domains are project-owned and non-transferable\")\n\t}\n\tif !isTokenValid(tid) {\n\t\tpanic(\"nslogic: cannot transfer an expired token\")\n\t}\n\tlabel, domainLabel := splitTokenID(tid)\n\tcaller := cur.Previous().Address()\n\t// A free-tier record has no GRC-721 token to move, so the token-layer\n\t// transfer would fail; the vault exposes a record-only path for it.\n\tif !nsdata.IsNameNFT(label, domainLabel) {\n\t\tif caller != from || nsdata.GetNameOwner(label, domainLabel) != caller {\n\t\t\tpanic(\"nslogic: restricted to name owner\")\n\t\t}\n\t\tif err := nsdata.TransferRecordOwner(cross(cur), label, domainLabel, to); err != nil {\n\t\t\tpanic(err)\n\t\t}\n\t\treturn\n\t}\n\tif err := nsdata.RawTransferNFT(cross(cur), caller, from, to, tid); err != nil {\n\t\tpanic(err)\n\t}\n}\n\n// nsdata only exposes one raw transfer primitive (RawTransferNFT, backed\n// by grc721's plain TransferFrom) — the safe-receiver-interface check\n// grc721's SafeTransferFrom adds doesn't carry much weight for a\n// name-service NFT, so both entry points converge on the same path here.\nfunc SafeTransferFrom(cur realm, from, to address, tid string) {\n\tTransferFrom(cur, from, to, tid)\n}\n"},{"name":"payment.gno","body":"package nslogic\n\nimport (\n\t\"chain\"\n\t\"chain/banker\"\n\t\"chain/runtime/unsafe\"\n\n\t\"gno.land/p/nt/ufmt/v0\"\n\t\"gno.land/r/g1xr6tgxnpled50h74eafmvxway7z0ytr5rsmeme/nsdata/v1\"\n)\n\n// takePayment verifies the attached send-envelope covers `price` ugnot,\n// forwards the price to the treasury, and refunds any overpayment to the\n// caller in the same transaction. Nothing is ever retained: every ugnot\n// that arrives leaves again, as either payment or refund.\n//\n// It used to take a `beneficiary` and split the price fee/remainder,\n// routing the remainder to treasury only while `beneficiary == admin`.\n// That comparison was a proxy for \"is this a project-owned domain\", and\n// it silently broke the moment `admin` rotated — the planned EOA-to-\n// multisig step — because a domain's owner is frozen into its record at\n// registration time. After a rotation, every registration's remainder\n// would have been paid to the retired key instead of the treasury, with\n// no error and no log line.\n//\n// The heuristic is gone rather than patched. Domains are admin-only to\n// create and permanently non-transferable, so every domain is\n// project-owned by construction and 100% of every payment belongs to the\n// treasury. `platformFeeBps` stays in nsdata's config for a future logic\n// realm that reintroduces third-party domain ownership; reinstating the\n// split is then a logic-realm swap, not a migration.\nfunc takePayment(cur realm, price int64) {\n\ttakePaymentSplit(cur, price, \"\", 0)\n}\n\n// takePaymentSplit is takePayment with a referrer's cut carved out of the\n// same price. The buyer pays exactly what they were quoted; the cut is\n// not added on top, it is a share of what the treasury would otherwise\n// have received.\n//\n// All three legs settle inside the buyer's own transaction — treasury,\n// referrer, refund — so this realm's balance is the same after the call\n// as before it. That is not a stylistic preference: holding a referrer's\n// cut until they claim it would make this an account you can have a\n// balance in, and that is a different kind of thing to operate, subject\n// to a different set of rules, run by people with licences. The cut is\n// paid now or it is not paid.\nfunc takePaymentSplit(cur realm, price int64, cutTo address, cut int64) {\n\tif !cur.Previous().IsUserCall() {\n\t\tpanic(\"nslogic: only direct wallet calls may attach payment\")\n\t}\n\tcaller := cur.Previous().Address()\n\tsent := unsafe.OriginSend()\n\tassertOnlyUgnot(sent)\n\n\tpaid := sent.AmountOf(ugnot)\n\tif paid \u003c price {\n\t\tpanic(ufmt.Sprintf(\"nslogic: insufficient payment: need %d ugnot, got %d\", price, paid))\n\t}\n\texcess := paid - price\n\n\t// Belt and braces. referralCut already caps the percentage, but this\n\t// is the line where the arithmetic becomes money leaving an address,\n\t// and a bug upstream must not be able to pay out more than came in.\n\tif cut \u003c 0 || cut \u003e price || cutTo == \"\" {\n\t\tcut = 0\n\t}\n\n\tb := banker.NewBanker(banker.BankerTypeRealmSend, cur)\n\tpkgAddr := cur.Address()\n\tif cut \u003e 0 {\n\t\tb.SendCoins(pkgAddr, cutTo, chain.Coins{chain.NewCoin(ugnot, cut)})\n\t}\n\tif price-cut \u003e 0 {\n\t\tb.SendCoins(pkgAddr, nsdata.GetTreasury(), chain.Coins{chain.NewCoin(ugnot, price-cut)})\n\t}\n\tif excess \u003e 0 {\n\t\tb.SendCoins(pkgAddr, caller, chain.Coins{chain.NewCoin(ugnot, excess)})\n\t}\n}\n\n// assertOnlyUgnot rejects any other denomination outright. Only the\n// ugnot amount was ever accounted for, and neither realm has a\n// withdrawal path, so a foreign coin sent alongside a payment would have\n// been stranded in this realm's address permanently. Refusing the whole\n// transaction is simpler than sweeping it back and keeps the\n// \"this realm never retains funds\" invariant absolute.\nfunc assertOnlyUgnot(sent chain.Coins) {\n\tfor _, c := range sent {\n\t\tif c.Denom != ugnot {\n\t\t\tpanic(\"nslogic: only ugnot is accepted, got: \" + c.Denom)\n\t\t}\n\t}\n}\n\n// assertNoSend guards every entry point that has no price. Without it,\n// coins attached to a free call (a renewal of a project domain, a\n// profile edit, an admin setter) would land in this realm's address with\n// no way back out.\n//\n// IT APPLIES TO DIRECT WALLET CALLS ONLY, and that qualification is not\n// a loosening — it is the guard finally matching its own reason.\n//\n// OriginSend() reports what the TRANSACTION carried, not what this call\n// received. Measured on sapphire-1 with a pair of probe realms: a user\n// attaches 500000ugnot to realm A, A calls realm B, and B's OriginSend()\n// reads 500000ugnot even though every one of those coins is sitting in\n// A's address. B never saw them and cannot strand them.\n//\n// So for a realm-to-realm call this was rejecting a payment this realm\n// never received. It is the reason a marketplace could not transfer a\n// name inside the same transaction that pays for it — the transfer is a\n// free call, the transaction is not, and the two are unrelated. A\n// two-transaction sale is the alternative, and that means somebody's\n// money or somebody's name sitting somewhere in between. Not here.\nfunc assertNoSend(cur realm) {\n\tif !cur.Previous().IsUserCall() {\n\t\treturn // the coins went to whoever called us, not to us\n\t}\n\tif !unsafe.OriginSend().IsZero() {\n\t\tpanic(\"nslogic: this function takes no payment; do not attach coins\")\n\t}\n}\n"},{"name":"premium.gno","body":"package nslogic\n\nimport (\n\t\"strconv\"\n\t\"strings\"\n\n\t\"gno.land/r/g1xr6tgxnpled50h74eafmvxway7z0ytr5rsmeme/nsdata/v1\"\n)\n\n// Global pricing layers that sit ACROSS every domain rather than inside\n// one — see SPEC.md §27.\n//\n// Two of them, and they work at different points:\n//\n//   GLOBAL LENGTH RULES are a FLOOR beneath each domain's own length\n//   rules, consulted only when the domain has nothing to say about that\n//   length. \"three-character names cost $5 everywhere\" becomes one entry\n//   instead of an edit to all twenty-seven domains, and a domain that\n//   wants its own answer still overrides it.\n//\n//   GLOBAL NAME PREMIUMS are an ADJUSTMENT applied after the price has\n//   already resolved. Making `og` premium is one entry covering\n//   `og*degen`, `og*meme` and every domain invented later. Deliberately\n//   not the per-name override, which REPLACES a price outright: \"this\n//   name costs exactly X here\" and \"this name is worth more everywhere\"\n//   are different tools and both are worth having.\n//\n// Full order, most specific first:\n//\n//   1. per-name override      (domain \"nm\")\n//   2. per-domain length rule (domain \"len\")\n//   3. global length rule     (global \"glen\")\n//   4. domain base price      (domain \"price\")\n//   5. global default         (global \"defaultPrice\")\n//   then the global name premium modifies the result,\n//   then any referral discount comes off what is left.\nconst (\n\tkeyGlobalLens = \"glen\"    // global length rules\n\tkeyPremium    = \"premium\" // premium1, premium2, ... see below\n\n\t// The vault caps an extension value at maxExtraValueLen (512 bytes),\n\t// which is roughly 40-60 premium entries. The list is therefore split\n\t// across numbered keys rather than pretending one will do; a list\n\t// that silently truncated at the 512th byte would drop exactly the\n\t// entries nobody checked.\n\tmaxPremiumKeys = 8\n)\n\n// GlobalLengthRule returns the global rule for a label length, if any.\nfunc GlobalLengthRule(n int) (string, bool) {\n\treturn lookupRule(nsdata.GetGlobal(keyGlobalLens), strconv.Itoa(n))\n}\n\n// PremiumFor returns the raw modifier for a name across all premium\n// keys, or \"\" when the name is not premium anywhere.\nfunc PremiumFor(label string) string {\n\tfor i := 1; i \u003c= maxPremiumKeys; i++ {\n\t\tcsv := nsdata.GetGlobal(keyPremium + strconv.Itoa(i))\n\t\tif csv == \"\" {\n\t\t\tcontinue\n\t\t}\n\t\tif v, ok := lookupRule(csv, label); ok {\n\t\t\treturn v\n\t\t}\n\t}\n\treturn \"\"\n}\n\n// applyPremium adjusts an already-resolved price.\n//\n// ZERO ALWAYS STAYS ZERO. A fixed \"+$2.00\" would otherwise silently undo\n// a giveaway tier set months earlier, and a free tier that quietly stops\n// being free is worse than never having offered one. This is decided\n// explicitly rather than inherited from the percentage case happening to\n// work out at zero.\n//\n// Forms: \"%5\" / \"+%5\" five percent more · \"-%10\" ten percent less ·\n// \"2.00\" two dollars more · \"-0.25\" twenty-five cents less ·\n// \"=5.00\" exactly five dollars, ignoring everything resolved above.\nfunc applyPremium(mod string, price int64) int64 {\n\tmod = strings.TrimSpace(mod)\n\tif mod == \"\" || price \u003c= 0 {\n\t\treturn price\n\t}\n\n\t// \"=X\" replaces outright. Checked before the sign handling because\n\t// an exact price is not a delta and has no direction.\n\tif strings.HasPrefix(mod, \"=\") {\n\t\tv, ok := parseUSD(mod[1:])\n\t\tif !ok || v \u003c 0 {\n\t\t\treturn price\n\t\t}\n\t\treturn v\n\t}\n\n\tneg := false\n\tswitch {\n\tcase strings.HasPrefix(mod, \"-\"):\n\t\tneg, mod = true, mod[1:]\n\tcase strings.HasPrefix(mod, \"+\"):\n\t\tmod = mod[1:]\n\t}\n\n\tvar delta int64\n\tif strings.HasPrefix(mod, \"%\") {\n\t\tpct, err := strconv.ParseInt(mod[1:], 10, 64)\n\t\tif err != nil || pct \u003c 0 {\n\t\t\treturn price\n\t\t}\n\t\tdelta = price * pct / 100\n\t} else {\n\t\tv, ok := parseUSD(mod)\n\t\tif !ok || v \u003c 0 {\n\t\t\treturn price\n\t\t}\n\t\tdelta = v\n\t}\n\n\tif neg {\n\t\t// A discount may take a name to free, but never below it: a\n\t\t// negative price would be a payout, and this realm never pays\n\t\t// anybody to register.\n\t\tif delta \u003e= price {\n\t\t\treturn 0\n\t\t}\n\t\treturn price - delta\n\t}\n\treturn price + delta\n}\n\n// parseUSD reads a human dollar figure — \"2\", \"2.5\", \"2.50\", \"0.069\" —\n// into micro-USD. Written by hand rather than with floats: a float would\n// make \"0.07\" cost a different number of micro-dollars depending on the\n// platform, and prices must be exact.\nfunc parseUSD(s string) (int64, bool) {\n\ts = strings.TrimSpace(s)\n\tif s == \"\" {\n\t\treturn 0, false\n\t}\n\twhole, frac := s, \"\"\n\tif i := strings.IndexByte(s, '.'); i \u003e= 0 {\n\t\twhole, frac = s[:i], s[i+1:]\n\t}\n\tif whole == \"\" {\n\t\twhole = \"0\"\n\t}\n\tw, err := strconv.ParseInt(whole, 10, 64)\n\tif err != nil || w \u003c 0 {\n\t\treturn 0, false\n\t}\n\t// Pad or truncate the fraction to exactly six digits of micro-USD.\n\tif len(frac) \u003e 6 {\n\t\tfrac = frac[:6]\n\t}\n\tfor len(frac) \u003c 6 {\n\t\tfrac += \"0\"\n\t}\n\tvar f int64\n\tif frac != \"\" {\n\t\tf, err = strconv.ParseInt(frac, 10, 64)\n\t\tif err != nil || f \u003c 0 {\n\t\t\treturn 0, false\n\t\t}\n\t}\n\treturn w*microUSD + f, true\n}\n\n// normalizePremiums validates a premium list before storage. Same\n// reasoning as normalizeRules: a malformed entry must never reach the\n// vault, because every later lookup would carry it.\nfunc normalizePremiums(csv string) string {\n\tif strings.TrimSpace(csv) == \"\" {\n\t\treturn \"\"\n\t}\n\tvar out []string\n\tseen := map[string]bool{}\n\tfor _, pair := range strings.Split(csv, \",\") {\n\t\tpair = strings.TrimSpace(pair)\n\t\tif pair == \"\" {\n\t\t\tcontinue\n\t\t}\n\t\tparts := strings.SplitN(pair, \":\", 2)\n\t\tif len(parts) != 2 {\n\t\t\tpanic(\"nslogic: bad premium, want name:modifier — got \" + pair)\n\t\t}\n\t\tk, v := strings.TrimSpace(parts[0]), strings.TrimSpace(parts[1])\n\t\tif k == \"\" || strings.ContainsAny(k, \"*, \\t\\n\") {\n\t\t\tpanic(\"nslogic: invalid name in premium — \" + k)\n\t\t}\n\t\tif seen[k] {\n\t\t\tpanic(\"nslogic: duplicate premium name — \" + k)\n\t\t}\n\t\tif !validPremiumValue(v) {\n\t\t\tpanic(\"nslogic: bad premium modifier for \" + k + \" — \" + v)\n\t\t}\n\t\tseen[k] = true\n\t\tout = append(out, k+\":\"+v)\n\t}\n\treturn strings.Join(out, \",\")\n}\n\nfunc validPremiumValue(v string) bool {\n\tif v == \"\" {\n\t\treturn false\n\t}\n\tif strings.HasPrefix(v, \"=\") {\n\t\t_, ok := parseUSD(v[1:])\n\t\treturn ok\n\t}\n\tif strings.HasPrefix(v, \"-\") || strings.HasPrefix(v, \"+\") {\n\t\tv = v[1:]\n\t}\n\tif strings.HasPrefix(v, \"%\") {\n\t\tp, err := strconv.ParseInt(v[1:], 10, 64)\n\t\treturn err == nil \u0026\u0026 p \u003e= 0\n\t}\n\t_, ok := parseUSD(v)\n\treturn ok\n}\n"},{"name":"pricing.gno","body":"package nslogic\n\nimport (\n\t\"strconv\"\n\t\"strings\"\n\n\t\"gno.land/r/g1xr6tgxnpled50h74eafmvxway7z0ytr5rsmeme/nsdata/v1\"\n)\n\n// Pricing lives here, not in the vault. The vault stores numbers as\n// opaque strings and has no opinion about what they mean, so the whole\n// model can be replaced by swapping this realm.\n//\n// PRICES ARE DENOMINATED IN USD, held as MICRO-USD (1 USD = 1_000_000)\n// so fractions of a cent are expressible — a name that costs about a\n// gumball needs that. Nothing is stored in ugnot: the chain's token is\n// just the settlement rail, converted at payment time from a stored\n// rate. Accepting a second token later is then only a second rate, not\n// a second price list.\n//\n// PRECEDENCE — most specific wins:\n//\n//   1. per-name override      (keyNames: \"420:5000000,gm:%500\")\n//   2. per-length rule        (keyLens:  \"3:2000000,4:%150,5:0\")\n//   3. GLOBAL length rule     (global keyGlobalLens — see premium.gno)\n//   4. domain base price      (keyPrice: \"500000\"  = $0.50)\n//   5. global default         (global keyDefaultPrice)\n//\n// then the GLOBAL NAME PREMIUM adjusts the result, and a referral\n// discount comes off last. Both live in premium.gno.\n//\n// A rule value is either an absolute micro-USD amount or \"%N\", meaning N\n// percent of that domain's base — so repricing a domain once carries\n// every relative rule under it. \"0\" means free, which is how giveaway\n// tiers are expressed: \"all five-character names under *pleb are free\"\n// is just a length rule resolving to zero.\n//\n// Rules are stored as CSV in ONE key each rather than one key per rule.\n// The vault caps extension keys at 32 bytes and 24 entries per record; a\n// key like \"nm:somelongname\" breaks the first and a few dozen premium\n// names break the second. Two CSV keys sidestep both.\nconst (\n\tkeyPrice        = \"price\" // domain base, micro-USD\n\tkeyLens         = \"len\"   // length rules\n\tkeyNames        = \"nm\"    // per-name overrides\n\tkeyOpen         = \"open\"  // \"0\" closes the domain to new names\n\tkeyDefaultPrice = \"defaultPrice\"\n\tkeyRate         = \"rateUgnotPerUsd\" // ugnot equal to 1 USD\n\n\tmicroUSD          = int64(1_000_000)\n\tfallbackPriceUSD  = int64(500_000) // $0.50\n\t// TESTNET PEG: $0.069 per GNOT, so 1 USD = 1/0.069 GNOT = 14.492753\n\t// GNOT = 14_492_753 ugnot. Deliberately a fixed number while we are on\n\t// a testnet whose token has no market price at all -- a \"real\" rate\n\t// there would be fiction dressed as precision. Replaced with a genuine\n\t// conversion before mainnet; tracked in MAINNET.md.\n\tfallbackRate      = int64(14_492_753)\n\tmaxRules          = 64\n)\n\n// DomainOpen reports whether names may currently be registered under a\n// domain. An absent key means open — a domain created before this\n// existed must not silently close.\nfunc DomainOpen(domainLabel string) bool {\n\treturn nsdata.GetDomainExtra(domainLabel, keyOpen) != \"0\"\n}\n\n// -- USD side --\n\nfunc DefaultPriceUSD() int64 {\n\tif v := nsdata.GetGlobal(keyDefaultPrice); v != \"\" {\n\t\tif p, err := strconv.ParseInt(v, 10, 64); err == nil \u0026\u0026 p \u003e= 0 {\n\t\t\treturn p\n\t\t}\n\t}\n\treturn fallbackPriceUSD\n}\n\nfunc DomainBasePriceUSD(domainLabel string) int64 {\n\tif v := nsdata.GetDomainExtra(domainLabel, keyPrice); v != \"\" {\n\t\tif p, err := strconv.ParseInt(v, 10, 64); err == nil \u0026\u0026 p \u003e= 0 {\n\t\t\treturn p\n\t\t}\n\t}\n\treturn DefaultPriceUSD()\n}\n\n// NamePriceUSD resolves the four inputs in precedence order and returns\n// micro-USD. This is the authoritative price; ugnot is derived from it.\nfunc NamePriceUSD(domainLabel, label string) int64 {\n\treturn applyPremium(PremiumFor(label), resolveBeforePremium(domainLabel, label))\n}\n\n// resolveBeforePremium runs steps 1-5. Separated from NamePriceUSD so\n// the admin panel can show WHY a price is what it is: the figure before\n// the premium and the figure after are both meaningful, and a single\n// function returning only the total makes a wrong price impossible to\n// diagnose.\nfunc resolveBeforePremium(domainLabel, label string) int64 {\n\tbase := DomainBasePriceUSD(domainLabel)\n\tif v, ok := lookupRule(nsdata.GetDomainExtra(domainLabel, keyNames), label); ok {\n\t\treturn applyRule(v, base)\n\t}\n\tif v, ok := lookupRule(nsdata.GetDomainExtra(domainLabel, keyLens), strconv.Itoa(len(label))); ok {\n\t\treturn applyRule(v, base)\n\t}\n\t// The global length rule is a floor, not a replacement: it applies\n\t// only where the domain itself had nothing to say about this length.\n\tif v, ok := GlobalLengthRule(len(label)); ok {\n\t\treturn applyRule(v, base)\n\t}\n\treturn base\n}\n\n// ExplainPriceUSD returns each stage of the resolution, so the control\n// panel can show the arithmetic rather than assert a total. Returns the\n// price before any premium, the premium modifier that applied (empty if\n// none), and the final price.\nfunc ExplainPriceUSD(domainLabel, label string) (beforePremium int64, modifier string, final int64) {\n\tbefore := resolveBeforePremium(domainLabel, label)\n\tmod := PremiumFor(label)\n\treturn before, mod, applyPremium(mod, before)\n}\n\n// -- settlement side --\n\n// RateUgnotPerUsd is how many ugnot one USD buys.\n//\n// This is a MANUALLY MAINTAINED rate, not a market oracle: an admin\n// writes it and it sits there until rewritten. That is a real\n// operational hazard — a stale rate during a sharp move sells names at\n// the wrong price in whichever direction hurts — and the mitigation is\n// keeping it fresh, not anything this code can do. Kept as one global so\n// a future logic realm can swap in a real oracle without touching the\n// vault or restating a single price.\nfunc RateUgnotPerUsd() int64 {\n\tif v := nsdata.GetGlobal(keyRate); v != \"\" {\n\t\tif r, err := strconv.ParseInt(v, 10, 64); err == nil \u0026\u0026 r \u003e 0 {\n\t\t\treturn r\n\t\t}\n\t}\n\treturn fallbackRate\n}\n\n// NamePrice converts the USD price to ugnot for settlement. Rounds UP,\n// so rounding never sells a name below its listed price; a free name\n// stays exactly free.\nfunc NamePrice(domainLabel, label string) int64 {\n\tusd := NamePriceUSD(domainLabel, label)\n\tif usd \u003c= 0 {\n\t\treturn 0\n\t}\n\trate := RateUgnotPerUsd()\n\treturn (usd*rate + microUSD - 1) / microUSD\n}\n\n// -- rule plumbing --\n\nfunc applyRule(v string, base int64) int64 {\n\tif strings.HasPrefix(v, \"%\") {\n\t\tpct, err := strconv.ParseInt(v[1:], 10, 64)\n\t\tif err != nil || pct \u003c 0 {\n\t\t\treturn base\n\t\t}\n\t\treturn base * pct / 100\n\t}\n\tn, err := strconv.ParseInt(v, 10, 64)\n\tif err != nil || n \u003c 0 {\n\t\treturn base\n\t}\n\treturn n\n}\n\nfunc lookupRule(csv, want string) (string, bool) {\n\tif csv == \"\" {\n\t\treturn \"\", false\n\t}\n\tfor _, pair := range strings.Split(csv, \",\") {\n\t\tparts := strings.SplitN(strings.TrimSpace(pair), \":\", 2)\n\t\tif len(parts) != 2 {\n\t\t\tcontinue\n\t\t}\n\t\tif strings.TrimSpace(parts[0]) == want {\n\t\t\treturn strings.TrimSpace(parts[1]), true\n\t\t}\n\t}\n\treturn \"\", false\n}\n\n// normalizeRules validates and canonicalises a rule string before it is\n// stored, so a malformed entry can never reach storage and poison every\n// later lookup.\nfunc normalizeRules(csv string, numericKeys bool) string {\n\tif strings.TrimSpace(csv) == \"\" {\n\t\treturn \"\"\n\t}\n\tvar out []string\n\tseen := map[string]bool{}\n\tfor _, pair := range strings.Split(csv, \",\") {\n\t\tpair = strings.TrimSpace(pair)\n\t\tif pair == \"\" {\n\t\t\tcontinue\n\t\t}\n\t\tparts := strings.SplitN(pair, \":\", 2)\n\t\tif len(parts) != 2 {\n\t\t\tpanic(\"nslogic: bad rule, want key:value — got \" + pair)\n\t\t}\n\t\tk, v := strings.TrimSpace(parts[0]), strings.TrimSpace(parts[1])\n\t\tif k == \"\" {\n\t\t\tpanic(\"nslogic: empty rule key\")\n\t\t}\n\t\tif numericKeys {\n\t\t\tif n, err := strconv.Atoi(k); err != nil || n \u003c= 0 {\n\t\t\t\tpanic(\"nslogic: length must be a positive number — got \" + k)\n\t\t\t}\n\t\t} else if strings.ContainsAny(k, \"*, \\t\\n\") {\n\t\t\tpanic(\"nslogic: invalid name in rule — \" + k)\n\t\t}\n\t\tif strings.HasPrefix(v, \"%\") {\n\t\t\tif p, err := strconv.ParseInt(v[1:], 10, 64); err != nil || p \u003c 0 {\n\t\t\t\tpanic(\"nslogic: bad percentage — \" + v)\n\t\t\t}\n\t\t} else if n, err := strconv.ParseInt(v, 10, 64); err != nil || n \u003c 0 {\n\t\t\tpanic(\"nslogic: bad amount — \" + v)\n\t\t}\n\t\tif seen[k] {\n\t\t\tpanic(\"nslogic: duplicate rule key — \" + k)\n\t\t}\n\t\tseen[k] = true\n\t\tout = append(out, k+\":\"+v)\n\t\tif len(out) \u003e maxRules {\n\t\t\tpanic(\"nslogic: too many rules (max 64)\")\n\t\t}\n\t}\n\treturn strings.Join(out, \",\")\n}\n"},{"name":"referral.gno","body":"package nslogic\n\nimport (\n\t\"strconv\"\n\t\"strings\"\n\n\t\"gno.land/r/g1xr6tgxnpled50h74eafmvxway7z0ytr5rsmeme/nsdata/v1\"\n)\n\n// The referral programme.\n//\n// SINGLE LEVEL, ALWAYS. One buyer, one referrer, one cut, taken out of\n// an actual sale. There is no downline, no override on somebody else's\n// referrals, and no bonus for recruiting other referrers — and there is\n// no code here that could be extended into one without being obvious\n// about it. That is the line between an affiliate programme and the\n// other thing, and this realm stays on the near side of it.\n//\n// WHERE THE LINK LIVES. The referrer is recorded on the REFERRED NAME,\n// in its extension slot, at the moment it is registered. That placement\n// does the work of two rules for free, because nsdata already destroys a\n// name's extras when a lapsed name is re-registered (names.Remove, then\n// a fresh record) and when a name changes hands (wipeOnHandover):\n//\n//\ta name that lapses loses its referrer\n//\ta name that is sold loses its referrer\n//\n// Neither needs code to enforce. Both would need code to PREVENT.\n//\n// WHO GETS PAID. An address, not a name. The referrer's wallet is fixed\n// into the referred name at registration, so they may let their own name\n// go, buy a different one, or hold ten — the commission does not move.\n// What they cannot do is leave: paying out requires the referrer to\n// still hold at least one name, checked at payment time.\n//\n// A SKIPPED PAYOUT IS NOT A DEBT. If the referrer holds nothing when a\n// renewal comes through, the cut is simply not taken — the buyer pays\n// less and the treasury keeps the difference. Nothing accrues, because\n// accruing would mean holding somebody else's money until they qualify,\n// and this realm holds nobody's money at any point.\n//\n// SELF-REFERRAL IS REFUSED. Otherwise the informed apply their own code\n// forever and the cut becomes a silent discount for people who read the\n// source.\n//\n// EVERYTHING SETTLES IN ONE TRANSACTION. Buyer pays, treasury is paid,\n// referrer is paid, overpayment goes back — all inside the message the\n// buyer signed. No escrow, no float, not for a block.\nconst (\n\tkeyRef     = \"ref\"     // on a NAME: the referrer's address\n\tkeyRefCut  = \"refcut\"  // global: tier table, referrer's percentage\n\tkeyRefDisc = \"refdisc\" // global: tier table, buyer's discount\n\tkeyRefStat = \"ref:\"    // global, per address: how many referred\n\tkeyRefWin  = \"refwin\"  // global: days a captured code stays valid\n\n\tmaxRefPercent = 50 // a cut plus a discount above this stops being a business\n)\n\n// -- tier tables --\n\n// A tier table reads like the length rules do: \"0:5,2:8,5:12\" means five\n// percent from the start, eight once the referrer has held a name two\n// years, twelve at five. The highest tier at or below their tenure wins,\n// so gaps are fine and the table never has to be exhaustive. An empty\n// table means the programme pays nothing, which is also how it is\n// switched off.\nfunc tierPercent(spec string, years int64) int64 {\n\tbest := int64(0)\n\tbestAt := int64(-1) // the threshold of the tier `best` came from\n\tfor _, part := range strings.Split(spec, \",\") {\n\t\tkv := strings.SplitN(strings.TrimSpace(part), \":\", 2)\n\t\tif len(kv) != 2 {\n\t\t\tcontinue\n\t\t}\n\t\tat, err1 := strconv.ParseInt(strings.TrimSpace(kv[0]), 10, 64)\n\t\tpct, err2 := strconv.ParseInt(strings.TrimSpace(kv[1]), 10, 64)\n\t\tif err1 != nil || err2 != nil || at \u003c 0 || pct \u003c 0 {\n\t\t\tcontinue\n\t\t}\n\t\tif pct \u003e maxRefPercent {\n\t\t\tpct = maxRefPercent\n\t\t}\n\t\t/* THE HIGHEST TIER THAT APPLIES, not the biggest number in the\n\t\t   table. This compared percentages, so a table where a later\n\t\t   tier pays LESS — the shape a taper needs, and the shape the\n\t\t   buyer-discount table is most likely to take — silently paid\n\t\t   the earlier, larger rate forever. Tracking the tier's\n\t\t   threshold instead makes the table mean what it reads as. */\n\t\tif years \u003e= at \u0026\u0026 at \u003e= bestAt {\n\t\t\tbestAt = at\n\t\t\tbest = pct\n\t\t}\n\t}\n\treturn best\n}\n\nfunc RefCutRules() string      { return nsdata.GetGlobal(keyRefCut) }\nfunc RefDiscountRules() string { return nsdata.GetGlobal(keyRefDisc) }\n\n// RefWindowDays is how long a clicked link keeps counting for. It is\n// enforced by the browser that captured the code, not by this realm —\n// the chain never sees the click, only the registration that quotes a\n// code — so it is stored here purely to be ONE number rather than a\n// constant baked separately into the site, the FAQ and the rules page.\n// Zero means the default; the site treats an absent value the same way.\nfunc RefWindowDays() int64 {\n\td := parseInt64(nsdata.GetGlobal(keyRefWin))\n\tif d \u003c= 0 {\n\t\treturn 30\n\t}\n\treturn d\n}\n\nfunc SetReferralWindow(cur realm, days int64) {\n\tassertNoSend(cur)\n\tassertIsAdmin(cur)\n\tif days \u003c 0 || days \u003e 3650 {\n\t\tpanic(\"nslogic: window must be 0-3650 days\")\n\t}\n\tsetGlobal(cur, keyRefWin, strconv.FormatInt(days, 10))\n}\n\n// ReferralQuote is what a code is worth, answered before anybody signs\n// anything. One call rather than four, because the site needs all of it\n// at once to show a price and every extra round trip is a moment where\n// the number on screen is not the number the chain will charge.\n//\n// A referrer of \"\" means the code did not resolve, and the two\n// percentages are then both zero.\nfunc ReferralQuote(refName string, buyer address) (referrer address, tenure, discountPct, cutPct int64) {\n\tref := resolveReferrer(refName, buyer)\n\tif ref == \"\" {\n\t\treturn \"\", 0, 0, 0\n\t}\n\tyears := TenureYears(ref)\n\tcut := int64(0)\n\t// The cut is zero for a referrer holding nothing, and saying so up\n\t// front is better than showing a percentage that silently will not\n\t// be paid when the transaction lands.\n\tif nsdata.BalanceOf(ref) \u003e 0 {\n\t\tcut = tierPercent(RefCutRules(), years)\n\t}\n\treturn ref, years, tierPercent(RefDiscountRules(), years), cut\n}\n\n// SetReferralRules replaces both tier tables at once, because setting one\n// without the other is how a cut ends up larger than the margin. Passing\n// two empty strings turns the programme off: no discount, no commission,\n// and registrations stop writing a referrer at all.\nfunc SetReferralRules(cur realm, cutSpec, discountSpec string) {\n\tassertNoSend(cur)\n\tassertIsAdmin(cur)\n\tcut := normalizeTiers(cutSpec)\n\tdisc := normalizeTiers(discountSpec)\n\t// The two are paid out of the same list price, so their sum is what\n\t// actually leaves the treasury. Checked at every tenure either table\n\t// mentions rather than only at tier zero, since the tables need not\n\t// step at the same points.\n\tfor _, y := range tierYears(cut, disc) {\n\t\tif tierPercent(cut, y)+tierPercent(disc, y) \u003e maxRefPercent {\n\t\t\tpanic(\"nslogic: cut plus discount exceeds 50% at year \" + strconv.FormatInt(y, 10))\n\t\t}\n\t}\n\tsetGlobal(cur, keyRefCut, cut)\n\tsetGlobal(cur, keyRefDisc, disc)\n}\n\nfunc tierYears(specs ...string) []int64 {\n\tvar out []int64\n\tseen := map[int64]bool{}\n\tfor _, spec := range specs {\n\t\tfor _, part := range strings.Split(spec, \",\") {\n\t\t\tkv := strings.SplitN(strings.TrimSpace(part), \":\", 2)\n\t\t\tif len(kv) != 2 {\n\t\t\t\tcontinue\n\t\t\t}\n\t\t\tif y, err := strconv.ParseInt(strings.TrimSpace(kv[0]), 10, 64); err == nil \u0026\u0026 !seen[y] {\n\t\t\t\tseen[y] = true\n\t\t\t\tout = append(out, y)\n\t\t\t}\n\t\t}\n\t}\n\tif len(out) == 0 {\n\t\tout = append(out, 0)\n\t}\n\treturn out\n}\n\n// normalizeTiers rejects a malformed table before it is stored, for the\n// same reason normalizeRules does: a bad entry that reaches storage is\n// then read silently and wrongly on every sale afterwards.\nfunc normalizeTiers(spec string) string {\n\tif strings.TrimSpace(spec) == \"\" {\n\t\treturn \"\"\n\t}\n\tvar out []string\n\tseen := map[int64]bool{}\n\tfor _, part := range strings.Split(spec, \",\") {\n\t\tpart = strings.TrimSpace(part)\n\t\tif part == \"\" {\n\t\t\tcontinue\n\t\t}\n\t\tkv := strings.SplitN(part, \":\", 2)\n\t\tif len(kv) != 2 {\n\t\t\tpanic(\"nslogic: bad tier, want years:percent — got \" + part)\n\t\t}\n\t\ty, err := strconv.ParseInt(strings.TrimSpace(kv[0]), 10, 64)\n\t\tif err != nil || y \u003c 0 || y \u003e 100 {\n\t\t\tpanic(\"nslogic: tier years must be 0-100 — got \" + kv[0])\n\t\t}\n\t\tp, err := strconv.ParseInt(strings.TrimSpace(kv[1]), 10, 64)\n\t\tif err != nil || p \u003c 0 || p \u003e maxRefPercent {\n\t\t\tpanic(\"nslogic: tier percent must be 0-50 — got \" + kv[1])\n\t\t}\n\t\tif seen[y] {\n\t\t\tpanic(\"nslogic: duplicate tier year — \" + kv[0])\n\t\t}\n\t\tseen[y] = true\n\t\tout = append(out, strconv.FormatInt(y, 10)+\":\"+strconv.FormatInt(p, 10))\n\t\tif len(out) \u003e 16 {\n\t\t\tpanic(\"nslogic: too many tiers (max 16)\")\n\t\t}\n\t}\n\treturn strings.Join(out, \",\")\n}\n\n// -- tenure --\n\n// TenureYears is how long a wallet counts as having been here, measured\n// from its PRIMARY name and nothing else.\n//\n// The honest alternative — the oldest name the wallet holds — means\n// walking the whole registry on every quote, which is a read that gets\n// slower every time somebody registers and eventually stops working.\n// Primary is one lookup and never gets slower. It is also already the\n// wallet's declared identity, it is set automatically at a wallet's first\n// registration, and a holder who wants an older name counted only has to\n// make it their primary. The site says so on the referral page.\n//\n// A lapsed or missing primary is zero years, not an error: tier zero.\nfunc TenureYears(who address) int64 {\n\ttid := nsdata.GetPrimary(who)\n\tat := strings.Index(tid, \"*\")\n\tif at \u003c 1 { // \"\" or \"*domain\" — a domain primary has no name streak\n\t\treturn 0\n\t}\n\tlabel, domainLabel := tid[:at], tid[at+1:]\n\tif !nsdata.NameExists(label, domainLabel) || !nsdata.IsNameValid(label, domainLabel) {\n\t\treturn 0\n\t}\n\t_, _, _, streak, _, _ := nsdata.GetNameInfo(label, domainLabel)\n\treturn streak\n}\n\n// -- where it gets paid --\n\n// Commissions go to the referring wallet. There is no way to redirect\n// them, and that is a decision rather than an omission.\n//\n// There WAS a redirect — SetReferralPayout, \"a forwarding address, not a\n// transferable right\". It does not survive the obvious question: how\n// would this realm tell somebody moving money between their own two\n// wallets from somebody who has sold the stream to a stranger? It\n// cannot. Nothing on chain distinguishes those, and a rule that cannot\n// be enforced is not a rule, it is a sentence in a document.\n//\n// What settled it is that the redirect did not even serve the case it\n// was written for. Calling it required signing from the ORIGINAL wallet,\n// so \"I lost access to my wallet\" was never covered; the only wallet\n// that could redirect was one whose owner could equally have received\n// the money and forwarded it. It gave nothing to the honest case and\n// gave the shape of an assignable income stream to the other one.\n//\n// So the destination is the referrer's own address, fixed. Selling the\n// entitlement now means handing over private keys, which is a deterrent\n// no contract clause could match.\nfunc PayoutFor(who address) address { return who }\n\n// -- the record on a name --\n\n// ReferrerOf reads the wallet credited with a name, or the empty address.\nfunc ReferrerOf(label, domainLabel string) address {\n\tv := nsdata.GetNameExtra(label, domainLabel, keyRef)\n\tif a := address(v); v != \"\" \u0026\u0026 a.IsValid() {\n\t\treturn a\n\t}\n\treturn \"\"\n}\n\n// resolveReferrer turns a claimed referring NAME into the wallet to\n// credit, or the empty address if the claim does not stand up. Every\n// rejection is silent on purpose: a mistyped or self-referral should not\n// fail somebody's registration, it should simply earn nobody anything.\nfunc resolveReferrer(refName string, buyer address) address {\n\tat := strings.Index(refName, \"*\")\n\tif refName == \"\" || at \u003c 1 || at == len(refName)-1 {\n\t\treturn \"\"\n\t}\n\tlabel, domainLabel := refName[:at], refName[at+1:]\n\tif !nsdata.NameExists(label, domainLabel) || !nsdata.IsNameValid(label, domainLabel) {\n\t\treturn \"\" // a lapsed name is not a working code\n\t}\n\towner := nsdata.GetNameOwner(label, domainLabel)\n\tif owner == buyer {\n\t\treturn \"\" // no referring yourself\n\t}\n\treturn owner\n}\n\n// -- the money --\n\n// PriceWithReferral is what a buyer actually pays given the code they\n// arrived with. Exported so the site can display the number the chain\n// will charge instead of computing its own and hoping they agree.\nfunc PriceWithReferral(domainLabel, label, refName string, buyer address) int64 {\n\tprice := NamePrice(domainLabel, label)\n\tif price \u003c= 0 {\n\t\treturn 0 // free is free, and a percentage of nothing is nothing\n\t}\n\tref := resolveReferrer(refName, buyer)\n\tif ref == \"\" {\n\t\treturn price\n\t}\n\tpct := tierPercent(RefDiscountRules(), TenureYears(ref))\n\tif pct \u003c= 0 {\n\t\treturn price\n\t}\n\treturn price - (price * pct / 100)\n}\n\n// referralCut is what leaves for the referrer out of a payment of\n// `price`, and is zero unless they still hold a name. Returns the\n// destination address and the amount; the caller pays it in the same\n// banker call that pays the treasury.\nfunc referralCut(ref address, price int64) (address, int64) {\n\tif ref == \"\" || price \u003c= 0 {\n\t\treturn \"\", 0\n\t}\n\t/* STILL HERE, AND STILL VALID.\n\t   This tested nsdata.BalanceOf, which has no expiry filter — and a\n\t   token is burned only when somebody else re-registers the label. So\n\t   a referrer who bought one cheap name, earned credits and walked\n\t   away kept a balance of one forever, and every renewal of every\n\t   name they ever referred kept paying them out of treasury revenue.\n\t   That is exactly the standing charge the note above says the check\n\t   prevents.\n\t   ResolveAddress is the honest test: it returns a name only when the\n\t   address still owns it AND it has not expired. TenureYears does the\n\t   same lookup two lines below, so this costs nothing extra. */\n\tif ResolveAddress(ref) == \"\" {\n\t\treturn \"\", 0\n\t}\n\tpct := tierPercent(RefCutRules(), TenureYears(ref))\n\tif pct \u003c= 0 {\n\t\treturn \"\", 0\n\t}\n\tcut := price * pct / 100\n\tif cut \u003c= 0 {\n\t\treturn \"\", 0\n\t}\n\treturn PayoutFor(ref), cut\n}\n\n// -- the scoreboard --\n\n// creditReferral records one paid referral against a wallet.\n//\n// The COUNT and nothing else. There was a running total of micro-USD\n// beside it, and it is gone, because a number stored in the vault is a\n// number anyone with an RPC endpoint can read — there is no such thing\n// as an admin-only global on a public chain, and building the site as\n// though there were would be the kind of privacy that works right up\n// until somebody checks.\n//\n// Not storing it is the only real reduction available. The payments are\n// still in the block history and always will be, so anyone determined\n// can add them up; what we can decline to do is publish a convenient\n// running total ourselves. tools/referrals.py reconstructs it from the\n// chain when we want it, which costs no storage and publishes nothing.\n//\n// The registrant's transaction pays for this entry. It is about forty\n// bytes, written once per referrer and overwritten after, and it is the\n// price of the discount they just received.\nfunc creditReferral(cur realm, ref address) {\n\tif ref == \"\" {\n\t\treturn\n\t}\n\tsetGlobal(cur, keyRefStat+ref.String(),\n\t\tstrconv.FormatInt(ReferralCount(ref)+1, 10))\n}\n\n// ReferralCount is how many paid registrations a wallet has been\n// credited with. Public, like every other global here.\nfunc ReferralCount(who address) int64 {\n\treturn parseInt64(nsdata.GetGlobal(keyRefStat + who.String()))\n}\n\nfunc parseInt64(s string) int64 {\n\tv, err := strconv.ParseInt(strings.TrimSpace(s), 10, 64)\n\tif err != nil {\n\t\treturn 0\n\t}\n\treturn v\n}\n\n// ReferralBoard pages through every credited wallet for the\n// leaderboard: one \"address,count\" row per line. The vault stores\n// globals in a sorted tree, so the \"ref:\" entries are one contiguous run\n// and the walk stops at the first key past it rather than reading the\n// rest of the config.\nfunc ReferralBoard(after string, limit int) (rows, next string) {\n\tif limit \u003c= 0 || limit \u003e 200 {\n\t\tlimit = 200\n\t}\n\tfrom := keyRefStat\n\tif after != \"\" {\n\t\tfrom = keyRefStat + after\n\t}\n\tvar out []string\n\tfor len(out) \u003c limit {\n\t\tkeys, more := nsdata.ExportGlobalKeys(from, limit)\n\t\tif keys == \"\" {\n\t\t\tbreak\n\t\t}\n\t\tstop := false\n\t\tfor _, k := range strings.Split(keys, \",\") {\n\t\t\tif !strings.HasPrefix(k, keyRefStat) {\n\t\t\t\tstop = true\n\t\t\t\tbreak\n\t\t\t}\n\t\t\taddr := k[len(keyRefStat):]\n\t\t\tn := ReferralCount(address(addr))\n\t\t\tif n \u003c= 0 {\n\t\t\t\tcontinue\n\t\t\t}\n\t\t\tout = append(out, addr+\",\"+strconv.FormatInt(n, 10))\n\t\t\tif len(out) \u003e= limit {\n\t\t\t\tnext = addr\n\t\t\t\tbreak\n\t\t\t}\n\t\t}\n\t\tif stop || more == \"\" || more == from {\n\t\t\tbreak\n\t\t}\n\t\tfrom = more\n\t}\n\treturn strings.Join(out, \"\\n\"), next\n}\n"},{"name":"reserved.gno","body":"package nslogic\n\nimport (\n\t\"strconv\"\n\n\t\"gno.land/r/g1xr6tgxnpled50h74eafmvxway7z0ytr5rsmeme/nsdata/v1\"\n)\n\n// GENERATED BY tools/gen-reserved.py FROM collections/reserved-labels.json.\n// Do not edit by hand: the site reads the same source, and a change made\n// in one place only is a label the site offers and the chain refuses.\n//\n// STRUCTURAL RULES ONLY. The wordlists live off chain, in that JSON,\n// because storage is 100 ugnot per byte and a ten-thousand-word\n// dictionary is about 57 GNOT paid forever to encode a marketing\n// decision that will change. This is the backstop against a bug in the\n// issuer, not the place the policy lives.\n//\n// THE NUMBERS ARE SETTABLE, the shape is not. A minimum length is a\n// promotion knob and belongs in the panel — redeploying this realm to\n// change a 5 to a 4 costs about 22 GNOT. So the lengths read from the\n// vault's globals and fall back to the values generated here; the\n// pattern rules are structural facts about what a handle looks like and\n// stay in code.\n\nconst (\n\tdefaultFreeMinLength = 5\n\tdefaultFreeMaxLength = 63\n\n\tkeyFreeMin = \"freemin\"\n\tkeyFreeMax = \"freemax\"\n)\n\n// FreeMinLength is the shortest label the free tier will issue. Settable\n// from the panel, because which lengths are worth money is a commercial\n// judgement that moves.\nfunc FreeMinLength() int {\n\tif v := nsdata.GetGlobal(keyFreeMin); v != \"\" {\n\t\tif n, err := strconv.Atoi(v); err == nil \u0026\u0026 n \u003e 0 \u0026\u0026 n \u003c= 63 {\n\t\t\treturn n\n\t\t}\n\t}\n\treturn defaultFreeMinLength\n}\n\nfunc FreeMaxLength() int {\n\tif v := nsdata.GetGlobal(keyFreeMax); v != \"\" {\n\t\tif n, err := strconv.Atoi(v); err == nil \u0026\u0026 n \u003e 0 \u0026\u0026 n \u003c= 63 {\n\t\t\treturn n\n\t\t}\n\t}\n\treturn defaultFreeMaxLength\n}\n\n/*\nFreeLabelAllowed reports whether a label may be ISSUED on the free tier.\n\nISSUED, and never renewed. This is called by GrantFreeName and by\nnothing else, deliberately: if the minimum length rises from three to\nfive, somebody who already holds a three-character free name keeps it\nand keeps renewing it free. Tightening a rule must not reach backwards\nand take away what was already given — that is the single fastest way to\nmake a giveaway cost more trust than it ever bought.\n\nA name that LAPSES and is registered again is a new issuance, and gets\nwhatever the rules say that day. That is not retroactive; it is a\ndifferent name event.\n*/\nfunc FreeLabelAllowed(label string) bool {\n\tn := len(label)\n\tif n \u003c FreeMinLength() || n \u003e FreeMaxLength() {\n\t\treturn false\n\t}\n\t// Pure digits: 1337, 420, years. Collectible, and no handle is one.\n\tdigits := true\n\tfor i := 0; i \u003c n; i++ {\n\t\tif label[i] \u003c '0' || label[i] \u003e '9' {\n\t\t\tdigits = false\n\t\t\tbreak\n\t\t}\n\t}\n\tif digits {\n\t\treturn false\n\t}\n\t// aaaa — every character the same.\n\tsame := true\n\tfor i := 1; i \u003c n; i++ {\n\t\tif label[i] != label[0] {\n\t\t\tsame = false\n\t\t\tbreak\n\t\t}\n\t}\n\tif same {\n\t\treturn false\n\t}\n\t// abab — a two-character cycle. Also collectible, also not a handle.\n\tif n \u003e= 4 {\n\t\tcycle := true\n\t\tfor i := 2; i \u003c n; i++ {\n\t\t\tif label[i] != label[i-2] {\n\t\t\t\tcycle = false\n\t\t\t\tbreak\n\t\t\t}\n\t\t}\n\t\tif cycle {\n\t\t\treturn false\n\t\t}\n\t}\n\treturn true\n}\n"},{"name":"restore.gno","body":"package nslogic\n\nimport (\n\t\"strconv\"\n\t\"strings\"\n\t\"time\"\n\n\t\"gno.land/r/g1xr6tgxnpled50h74eafmvxway7z0ytr5rsmeme/nsdata/v1\"\n)\n\n/*\nREBUILDING THE REGISTRY ON A NEW CHAIN.\n\nWHY THIS EXISTS. gno.land replaces testnets rather than preserving them:\ntopaz-1 was superseded by sapphire-1 on twenty-four hours' notice and\nevery name registered on it went to zero. tools/snapshot.js keeps a\ncomplete off-chain copy of the vault for exactly that morning. This is\nthe other half — the path that turns a snapshot back into a live\nregistry, so that people who paid for a name get that name back rather\nthan an apology.\n\nTHIS IS A BACKDOOR, AND IT IS TREATED AS ONE.\n\nA function that can mint any name, to any address, with any registration\ndate, is precisely the thing that would make every name here worthless\nif it could be reached whenever the admin felt like it. Ownership means\nnothing if history is editable on demand. So the capability is not\nmerely admin-gated — admin-gated is what everything else here already\nis, and it is not enough for this. It is a ONE-SHOT:\n\n  - it starts closed, and on a chain where nothing needs restoring it is\n    sealed immediately and can never be used at all;\n  - opening it is a deliberate, separate, announced act with a deadline\n    attached, so forgetting to close it is not the same as leaving it\n    open forever;\n  - sealing is IRREVERSIBLE. Sealed cannot be reopened. There is no\n    argument that unseals it, including a good one;\n  - it refuses to touch a name that already exists. It fills gaps in an\n    empty registry; it cannot rewrite a live one.\n\nWHERE THE SEAL LIVES, AND WHY IT IS NOT HERE. This realm holds no state\nat all — that is the whole reason prices and names survive a logic\nupgrade untouched. If the seal lived in this realm's variables it would\nreset to \"never opened\" the moment a new generation was deployed, which\nwould make \"irreversible\" a lie with a two-hour shelf life. It lives in\nthe vault's global slot instead, so a new logic generation inherits a\nseal it cannot talk its way out of.\n\nWHAT THAT DOES AND DOES NOT GUARANTEE. It is a real barrier against\nevery accident and against this realm being reached later for something\nit was not built for. It is not a barrier against the admin themselves,\nwho can deploy a further logic realm and have the vault trust it — but\nthat is already total control by definition, and no flag in a realm the\nadmin can replace was ever going to change it. What the seal gives is\nthe thing that actually matters in practice: the state is public, it is\ncheckable by anybody at any time through RestoreStatus, and moving it\nbackwards leaves a permanent trace on chain.\n\nWHAT IT DELIBERATELY DOES NOT RESTORE. Marketplace listings and open\nvotes. Those live in their own realms, are intentions rather than facts,\nand are re-creatable by the people who made them in one transaction\neach. Re-materialising somebody's month-old asking price as though they\nhad just set it would be inventing a decision on their behalf.\n*/\n\n// The vault key holding the state of this whole mechanism.\nconst keyRestore = \"restore\"\n\n// Restore states, stored as a string in the vault's global slot.\nconst (\n\trestoreNever  = \"\"       // never opened; the normal state forever\n\trestoreSealed = \"sealed\" // closed permanently; cannot be reopened\n\trestoreOpen   = \"open:\"  // followed by the unix second it lapses\n)\n\n// The longest a restore window may be left open. A restore is a day of\n// work, not a season, and an unbounded window is just \"open forever\"\n// spelled differently — the failure here is nobody remembering to seal\n// it, so the deadline seals it for them.\nconst maxRestoreDays = 30\n\n// RestoreStatus reports the state of the restore mechanism in a form a\n// person can read, and is deliberately PUBLIC AND UNAUTHENTICATED.\n// Anybody at all can check whether this registry currently has a door in\n// it, without asking us and without trusting the answer we would give.\n//\n// Returns the state (\"never\", \"open\", \"expired\" or \"sealed\"), the unix\n// second an open window lapses (0 otherwise), and how many records have\n// been restored in the lifetime of this vault.\nfunc RestoreStatus() (state string, deadline int64, restored int64) {\n\tst, d := readRestoreState(nsdata.GetGlobal(keyRestore), time.Now().Unix())\n\treturn st, d, restoredCount()\n}\n\n// readRestoreState is the state machine on its own, with the clock passed\n// in. Split out so the one piece of logic that decides whether the door\n// is open can be tested exhaustively without a chain under it.\nfunc readRestoreState(raw string, now int64) (state string, deadline int64) {\n\tswitch {\n\tcase raw == restoreSealed:\n\t\treturn \"sealed\", 0\n\tcase strings.HasPrefix(raw, restoreOpen):\n\t\td, err := strconv.ParseInt(raw[len(restoreOpen):], 10, 64)\n\t\tif err != nil || d \u003c= 0 {\n\t\t\t// An unparseable deadline is read as NO permission. The safe\n\t\t\t// reading of a door whose lock is broken is \"shut\" — a corrupt\n\t\t\t// value must never widen what somebody is allowed to do.\n\t\t\treturn \"sealed\", 0\n\t\t}\n\t\tif now \u003e= d {\n\t\t\treturn \"expired\", d\n\t\t}\n\t\treturn \"open\", d\n\t}\n\treturn \"never\", 0\n}\n\nconst keyRestoredCount = \"restored\"\n\nfunc restoredCount() int64 {\n\tif v := nsdata.GetGlobal(keyRestoredCount); v != \"\" {\n\t\tif n, err := strconv.ParseInt(v, 10, 64); err == nil {\n\t\t\treturn n\n\t\t}\n\t}\n\treturn 0\n}\n\n/*\nTHE RESERVED-KEY DOOR DOES NOT EXIST ON THE DEPLOYED VAULT, so these\nwrites go through the ordinary setter.\n\nnsdata/v1 is live and permanent, and its SetGlobalRecord takes any key\nwith no reserved-key guard at all. The two-door design — a general setter\nthat refuses \"restore\"/\"restored\" plus SetReservedGlobalRecord for the\nmachinery — exists only in the local nsdata source, which can never be\ndeployed over a vault holding real records. Calling for a door that is\nnot in the wall does not fail at review; it fails at deploy, after the\ndeposit is spent.\n\nSo the seal is guarded in ONE place: setGlobal in admin.gno, which panics\non both keys. That is weaker than the intended belt-and-braces — a future\nlogic realm could drop the guard by forgetting it, and the vault would\nnot catch it — and it is the strongest version available on this chain.\nDo not \"restore\" the reserved call: it will not link.\n*/\nfunc bumpRestored(cur realm, n int64) {\n\tnsdata.SetGlobalRecord(cross(cur), keyRestoredCount,\n\t\tstrconv.FormatInt(restoredCount()+n, 10))\n}\n\n// assertRestoring is the gate every Restore* function passes through.\nfunc assertRestoring(cur realm) {\n\tassertNoSend(cur)\n\tassertIsAdmin(cur)\n\tstate, _, _ := RestoreStatus()\n\tswitch state {\n\tcase \"open\":\n\t\treturn\n\tcase \"sealed\":\n\t\tpanic(\"nslogic: restore was sealed and cannot be reopened. This is \" +\n\t\t\t\"not recoverable by design — a registry whose history can be \" +\n\t\t\t\"rewritten on request does not have owners, it has guests.\")\n\tcase \"expired\":\n\t\tpanic(\"nslogic: the restore window has lapsed. Deploy a fresh vault \" +\n\t\t\t\"and open a new window there; an expired one does not reopen.\")\n\t}\n\tpanic(\"nslogic: restore has never been opened. Call OpenRestore first, \" +\n\t\t\"and mean it.\")\n}\n\n// OpenRestore opens the window, once, for a bounded number of days.\n//\n// Only ever valid on a chain where the registry is EMPTY because the\n// previous chain was retired. It cannot be called on a vault that has\n// been sealed, and it cannot be called twice.\nfunc OpenRestore(cur realm, days int64) {\n\tassertNoSend(cur)\n\tassertIsAdmin(cur)\n\n\tstate, _, _ := RestoreStatus()\n\tif state != \"never\" {\n\t\tpanic(\"nslogic: restore may be opened exactly once per vault; this \" +\n\t\t\t\"one is already \" + state + \". A door that reopens is a door.\")\n\t}\n\tif days \u003c 1 || days \u003e maxRestoreDays {\n\t\tpanic(\"nslogic: a restore window must be between 1 and \" +\n\t\t\tstrconv.Itoa(maxRestoreDays) + \" days\")\n\t}\n\t/* THE PRECONDITION THE COMMENT ABOVE ALREADY CLAIMS.\n\t   \"Only ever valid on a chain where the registry is EMPTY\" was a\n\t   statement of intent that nothing enforced, so the window was\n\t   openable on a populated vault. The record restorers all refuse an\n\t   existing record, but RestoreNameField does not — it writes any\n\t   extension key on any EXISTING name, which includes the payment\n\t   addresses the site renders on a name's pay page. That is a way to\n\t   redirect somebody else's incoming money using only the admin key,\n\t   without the timelock and public event that granting a fresh logic\n\t   realm would cost. Now it is what it said it was. */\n\tif d, n, _, _ := nsdata.Sizes(); d != 0 || n != 0 {\n\t\tpanic(\"nslogic: restore is for an empty vault on a fresh chain; \" +\n\t\t\t\"this one already holds records\")\n\t}\n\tdeadline := time.Now().Unix() + days*86400\n\tnsdata.SetGlobalRecord(cross(cur), keyRestore,\n\t\trestoreOpen+strconv.FormatInt(deadline, 10))\n}\n\n// SealRestore closes the door permanently. Valid from any state,\n// including \"never\" — which is how it should be called on a chain that\n// has nothing to restore, so that the capability is provably dead rather\n// than merely unused.\n//\n// There is no matching unseal, and adding one later would defeat every\n// word written at the top of this file.\nfunc SealRestore(cur realm) {\n\tassertNoSend(cur)\n\tassertIsAdmin(cur)\n\tnsdata.SetGlobalRecord(cross(cur), keyRestore, restoreSealed)\n}\n\n/*\nRestoreDomain re-creates one domain with its original dates.\n\nORDER MATTERS: domains before their names. The vault refuses to register\na name under a domain that does not exist or had lapsed at the moment\nthe name claims to have been registered, which is correct behaviour and\nalso exactly the order a snapshot should be replayed in.\n\nThe vault's own registration path sets `registered` to whatever instant\nit is handed and derives the expiry from the term length, so the\nregistration date is restored by passing the ORIGINAL one, and the\nexpiry is then corrected to the recorded value rather than a computed\none. Anything else would quietly hand everybody a fresh term.\n*/\nfunc RestoreDomain(cur realm, label string, owner address, registered, expires, ttl int64, frozen bool) {\n\tassertRestoring(cur)\n\tif nsdata.DomainExists(label) {\n\t\tpanic(\"nslogic: \" + label + \" already exists; restore fills an empty \" +\n\t\t\t\"registry and never overwrites a live record\")\n\t}\n\tif registered \u003c= 0 || expires \u003c= 0 {\n\t\tpanic(\"nslogic: a restored record needs its original dates\")\n\t}\n\tif err := nsdata.RegisterDomainRecord(cross(cur), label, owner, registered); err != nil {\n\t\tpanic(\"nslogic: \" + err.Error())\n\t}\n\tnsdata.SetDomainExpiryRecord(cross(cur), label, expires)\n\tif ttl != 0 {\n\t\tif err := nsdata.SetDomainTTLRecord(cross(cur), label, ttl); err != nil {\n\t\t\tpanic(\"nslogic: \" + err.Error())\n\t\t}\n\t}\n\tif frozen {\n\t\tnsdata.SetDomainFrozen(cross(cur), label, true)\n\t}\n\tbumpRestored(cur, 1)\n}\n\n// RestoreName re-creates one name. See RestoreDomain on ordering and on\n// why the dates are passed rather than computed.\n//\n// isNFT is carried across rather than assumed: the free tier registers\n// names without a token, and minting one for somebody who never had it\n// would silently upgrade their record and desynchronise the two\n// ownership ledgers.\nfunc RestoreName(cur realm, label, domainLabel string, owner address, registered, expires, ttl int64, frozen, isNFT bool) {\n\tassertRestoring(cur)\n\tif nsdata.NameExists(label, domainLabel) {\n\t\tpanic(\"nslogic: \" + label + \"*\" + domainLabel + \" already exists; \" +\n\t\t\t\"restore fills an empty registry and never overwrites a live record\")\n\t}\n\tif registered \u003c= 0 || expires \u003c= 0 {\n\t\tpanic(\"nslogic: a restored record needs its original dates\")\n\t}\n\tif err := nsdata.RegisterNameRecord(cross(cur), label, domainLabel, owner, registered, isNFT); err != nil {\n\t\tpanic(\"nslogic: \" + err.Error())\n\t}\n\tnsdata.SetNameExpiryRecord(cross(cur), label, domainLabel, expires)\n\tif ttl != 0 {\n\t\tif err := nsdata.SetNameTTLRecord(cross(cur), label, domainLabel, ttl); err != nil {\n\t\t\tpanic(\"nslogic: \" + err.Error())\n\t\t}\n\t}\n\tif frozen {\n\t\tnsdata.SetNameFrozen(cross(cur), label, domainLabel, true)\n\t}\n\tbumpRestored(cur, 1)\n}\n\n/*\nRestoreNames replays many names in ONE transaction.\n\nWHY A BLOB AND NOT A LOOP OF CALLS. A restore is done in one sitting\nagainst a clock: the window is bounded and the people whose names are\nmissing are watching. One transaction per name is one signature per\nname, and at a few hundred names that is an afternoon of clicking during\nwhich a single mistimed refresh loses the thread. The compact form here\nis unpleasant to read and it is the difference between a restore that\ngets finished and one that gets abandoned half way.\n\nThe format is records separated by \"|\", fields separated by \",\":\n\n\tlabel,domain,owner,registered,expires,ttl,frozen,isNFT\n\nNeither a label nor a domain may contain \",\" or \"|\" — the character set\nis enforced at registration — and an address contains neither, so the\nsplit is unambiguous. tools/snapshot.js writes the JSON this is built\nfrom; see tools/restore-blob.js for the conversion.\n\nIt stops at the first bad record and panics, taking the whole\ntransaction with it. That is deliberate: a partial batch that silently\nskipped four names would leave four people without theirs and nothing\nanywhere saying which four.\n*/\nfunc RestoreNames(cur realm, blob string) int {\n\tassertRestoring(cur)\n\tif blob == \"\" {\n\t\tpanic(\"nslogic: nothing to restore\")\n\t}\n\trecs := strings.Split(blob, \"|\")\n\tif len(recs) \u003e 50 {\n\t\tpanic(\"nslogic: at most 50 records per call, or the transaction \" +\n\t\t\t\"runs out of gas half way and it is unclear what landed\")\n\t}\n\tdone := 0\n\tfor i, rec := range recs {\n\t\tif rec == \"\" {\n\t\t\tcontinue\n\t\t}\n\t\tr := parseRestoreRecord(rec, i)\n\t\tif nsdata.NameExists(r.label, r.domain) {\n\t\t\tpanic(\"nslogic: record \" + strconv.Itoa(i) + \" (\" + r.label + \"*\" + r.domain +\n\t\t\t\t\") already exists; restore never overwrites a live record\")\n\t\t}\n\t\tif err := nsdata.RegisterNameRecord(cross(cur), r.label, r.domain, r.owner, r.registered, r.isNFT); err != nil {\n\t\t\tpanic(\"nslogic: record \" + strconv.Itoa(i) + \": \" + err.Error())\n\t\t}\n\t\tnsdata.SetNameExpiryRecord(cross(cur), r.label, r.domain, r.expires)\n\t\tif r.ttl != 0 {\n\t\t\tif err := nsdata.SetNameTTLRecord(cross(cur), r.label, r.domain, r.ttl); err != nil {\n\t\t\t\tpanic(\"nslogic: record \" + strconv.Itoa(i) + \": \" + err.Error())\n\t\t\t}\n\t\t}\n\t\tif r.frozen {\n\t\t\tnsdata.SetNameFrozen(cross(cur), r.label, r.domain, true)\n\t\t}\n\t\tdone++\n\t}\n\tbumpRestored(cur, int64(done))\n\treturn done\n}\n\n// restoreRec is one parsed line of the blob.\ntype restoreRec struct {\n\tlabel, domain     string\n\towner             address\n\tregistered        int64\n\texpires, ttl      int64\n\tfrozen, isNFT     bool\n}\n\n/*\nparseRestoreRecord reads one record and refuses anything it is not sure\nabout.\n\nEVERY CHECK HERE IS A PANIC AND NOT A SKIP. The blob is machine-written\nfrom a snapshot, so a malformed record does not mean \"this one name is\nodd\", it means the file is not what it was believed to be — and the\nworst outcome available is a restore that appears to succeed while\nquietly dropping the records it could not read. Somebody would find out\nmonths later when one person asked where their name went.\n*/\nfunc parseRestoreRecord(rec string, i int) restoreRec {\n\tf := strings.Split(rec, \",\")\n\tif len(f) != 8 {\n\t\tpanic(\"nslogic: record \" + strconv.Itoa(i) + \" has \" +\n\t\t\tstrconv.Itoa(len(f)) + \" fields, expected 8 \" +\n\t\t\t\"(label,domain,owner,registered,expires,ttl,frozen,isNFT)\")\n\t}\n\tif f[0] == \"\" || f[1] == \"\" {\n\t\tpanic(\"nslogic: record \" + strconv.Itoa(i) + \" has no name\")\n\t}\n\tout := restoreRec{\n\t\tlabel:      f[0],\n\t\tdomain:     f[1],\n\t\towner:      address(f[2]),\n\t\tregistered: mustSecs(f[3], i, \"registered\"),\n\t\texpires:    mustSecs(f[4], i, \"expires\"),\n\t\tttl:        mustSecs(f[5], i, \"ttl\"),\n\t\tfrozen:     f[6] == \"1\",\n\t\tisNFT:      f[7] == \"1\",\n\t}\n\tif !out.owner.IsValid() {\n\t\tpanic(\"nslogic: record \" + strconv.Itoa(i) + \" has an invalid owner address\")\n\t}\n\tif out.registered \u003c= 0 || out.expires \u003c= 0 {\n\t\tpanic(\"nslogic: record \" + strconv.Itoa(i) + \" is missing its original dates\")\n\t}\n\t// An expiry before the registration is not a record, it is a\n\t// corruption, and restoring it would create a name that was already\n\t// reclaimable the day it was bought.\n\tif out.expires \u003c out.registered {\n\t\tpanic(\"nslogic: record \" + strconv.Itoa(i) + \" expires before it was registered\")\n\t}\n\treturn out\n}\n\nfunc mustSecs(s string, i int, what string) int64 {\n\tv, err := strconv.ParseInt(s, 10, 64)\n\tif err != nil || v \u003c 0 {\n\t\tpanic(\"nslogic: record \" + strconv.Itoa(i) + \" has a bad \" + what)\n\t}\n\treturn v\n}\n\n// RestoreNameField and RestoreDomainField put back a record's extension\n// keys — profiles, cross-chain addresses, tier markers, and a domain's\n// pricing rules. Separate calls because the extension slot has no fixed\n// shape by design, which is the same reason the snapshot captures it by\n// enumeration rather than by schema.\nfunc RestoreNameField(cur realm, label, domainLabel, key, value string) {\n\tassertRestoring(cur)\n\tif err := nsdata.SetNameExtraRecord(cross(cur), label, domainLabel, key, value); err != nil {\n\t\tpanic(\"nslogic: \" + err.Error())\n\t}\n}\n\nfunc RestoreDomainField(cur realm, label, key, value string) {\n\tassertRestoring(cur)\n\tif err := nsdata.SetDomainExtraRecord(cross(cur), label, key, value); err != nil {\n\t\tpanic(\"nslogic: \" + err.Error())\n\t}\n}\n\n// RestoreGlobal puts back the vault's global slot — where the default\n// price, the referral tiers and the seasonal card effects live.\n//\n// It refuses to write the restore keys themselves. Without that, a\n// snapshot taken while a window was open would, on being replayed,\n// cheerfully restore the open window along with everything else — and a\n// seal that a backup file can undo is not a seal.\nfunc RestoreGlobal(cur realm, key, value string) {\n\tassertRestoring(cur)\n\tif key == keyRestore || key == keyRestoredCount {\n\t\tpanic(\"nslogic: the restore mechanism does not restore itself\")\n\t}\n\tif err := nsdata.SetGlobalRecord(cross(cur), key, value); err != nil {\n\t\tpanic(\"nslogic: \" + err.Error())\n\t}\n}\n\n// RestorePrimary puts back reverse resolution — which name an address\n// answers to. Registration already sets this for an owner who had none,\n// so this exists for the owners who had chosen a different one.\nfunc RestorePrimary(cur realm, owner address, label, domainLabel string) {\n\tassertRestoring(cur)\n\tif err := nsdata.SetPrimaryRecord(cross(cur), owner, label, domainLabel); err != nil {\n\t\tpanic(\"nslogic: \" + err.Error())\n\t}\n}\n"},{"name":"term.gno","body":"package nslogic\n\nimport (\n\t\"strconv\"\n\t\"time\"\n\n\t\"gno.land/p/nt/ufmt/v0\"\n\t\"gno.land/r/g1xr6tgxnpled50h74eafmvxway7z0ytr5rsmeme/nsdata/v1\"\n)\n\n// How far ahead a name may be paid.\n//\n// Renewal was unbounded. RenewNameRecord adds one term to the previous\n// due date and asks no further questions, so a holder pressing renew\n// seven times bought seven years of runway, and nothing anywhere said\n// that was not intended. It is a bug in two directions at once:\n//\n//   - Free domains have a base price of zero. Renewal is free too, so a\n//     script could take every good name under *gnoland and hold it for a\n//     century for the cost of gas. There is no economic brake on a free\n//     name at all; the term limit IS the brake.\n//   - A paid name held fifty years ahead is fifty years of a promise we\n//     have made against a rate written by hand and a testnet that will\n//     be replaced. Selling that is easy and honouring it is not.\n//\n// The limit is RUNWAY, not total lifetime: at most N years may be\n// outstanding at any moment. Nobody is stopped from holding a name\n// forever — they renew as the runway burns down, which is the same\n// arrangement every domain registrar has always used and the one people\n// already understand.\n//\n// Per domain first, then a project-wide default, so a free domain can be\n// capped at one year without capping anything else.\nconst (\n\tkeyMaxTerm       = \"maxterm\" // domain extra AND global: years of runway\n\tdefaultMaxTerm   = int64(5)\n\tmaxMaxTermYears  = int64(100)\n)\n\n// MaxTermYears is the cap that applies to names under this domain.\nfunc MaxTermYears(domainLabel string) int64 {\n\tif v := nsdata.GetDomainExtra(domainLabel, keyMaxTerm); v != \"\" {\n\t\tif y, err := strconv.ParseInt(v, 10, 64); err == nil \u0026\u0026 y \u003e 0 {\n\t\t\treturn y\n\t\t}\n\t}\n\tif v := nsdata.GetGlobal(keyMaxTerm); v != \"\" {\n\t\tif y, err := strconv.ParseInt(v, 10, 64); err == nil \u0026\u0026 y \u003e 0 {\n\t\t\treturn y\n\t\t}\n\t}\n\treturn defaultMaxTerm\n}\n\n// SetMaxTerm caps a single domain, or the whole project when the domain\n// label is empty. A domain's own cap always wins, so tightening one free\n// domain does not disturb the rest.\nfunc SetMaxTerm(cur realm, domainLabel string, years int64) {\n\tassertNoSend(cur)\n\tassertIsAdmin(cur)\n\tif years \u003c 1 || years \u003e maxMaxTermYears {\n\t\tpanic(\"nslogic: term cap must be 1-100 years\")\n\t}\n\tv := strconv.FormatInt(years, 10)\n\tif domainLabel == \"\" {\n\t\tsetGlobal(cur, keyMaxTerm, v)\n\t\treturn\n\t}\n\tif !nsdata.DomainExists(domainLabel) {\n\t\tpanic(\"nslogic: unknown domain\")\n\t}\n\tif err := nsdata.SetDomainExtraRecord(cross(cur), domainLabel, keyMaxTerm, v); err != nil {\n\t\tpanic(err)\n\t}\n}\n\n// RenewalsLeft is how many more years may be added to a name right now.\n// Zero means the renew button should be off, and the site reads this\n// rather than doing the arithmetic itself — the cap can be per domain,\n// and a page that guesses it will guess wrong on exactly the domains\n// where it matters.\n//\n// An unregistered name reports the full allowance: nothing is holding\n// runway yet.\nfunc RenewalsLeft(label, domainLabel string) int64 {\n\tmaxYears := MaxTermYears(domainLabel)\n\tif !nsdata.NameExists(label, domainLabel) {\n\t\treturn maxYears\n\t}\n\tterm := nsdata.GetTermLength()\n\tif term \u003c= 0 {\n\t\treturn 0\n\t}\n\t_, _, expires, _, _, _ := nsdata.GetNameInfo(label, domainLabel)\n\treturn renewalsLeft(expires, time.Now().Unix(), term, maxYears)\n}\n\n// renewalsLeft is the arithmetic on its own, so it can be tested without\n// a chain under it.\n//\n// Runway is measured from NOW, not from the registration date, so a name\n// deep in its grace period counts as zero rather than as a negative that\n// would then read as extra room. Years already bought round UP: a name\n// with thirteen months left is holding two of its five, because the\n// alternative lets somebody sit permanently at four-years-and-eleven-\n// months and renew every month forever.\nfunc renewalsLeft(expires, now, term, maxYears int64) int64 {\n\tif term \u003c= 0 {\n\t\treturn 0\n\t}\n\trunway := expires - now\n\tif runway \u003c 0 {\n\t\trunway = 0\n\t}\n\tleft := maxYears - (runway+term-1)/term\n\tif left \u003c 0 {\n\t\treturn 0\n\t}\n\treturn left\n}\n\n// assertRoomToRenew is the enforcement. It runs before payment is taken,\n// so a rejected renewal costs gas and nothing else — taking the money\n// first and reverting would work too, but only because a panic reverts,\n// and depending on that for a refund is a habit that eventually meets a\n// path where it is not true.\nfunc assertRoomToRenew(label, domainLabel string) {\n\tif RenewalsLeft(label, domainLabel) \u003e 0 {\n\t\treturn\n\t}\n\tpanic(ufmt.Sprintf(\n\t\t\"nslogic: %s*%s is already paid %d years ahead, which is the limit. Renew again when it has run down.\",\n\t\tlabel, domainLabel, MaxTermYears(domainLabel)))\n}\n"},{"name":"types.gno","body":"// Package nslogic is the swappable \"business rules\" half of the split\n// (NOTES.md §9t). It holds every piece of policy that might reasonably\n// change — pricing, character validation, payment/refund, freeze rules,\n// transfer restrictions — and no persistent records of its own.\n//\n// Every mutation here ends by calling into nsdata's narrow Record/Raw\n// API via cross(cur). nsdata trusts whichever address is currently wired\n// up as trustedLogic to have done its own authorization checks\n// correctly, so THIS realm is where \"is the caller allowed to do this\"\n// actually lives. If a bug is found here, admin deploys a fixed nslogic\n// and re-points the vault at it (ProposeTrustedLogicGrant, then\n// ExecutePending after the timelock) — no data migration,\n// records and config untouched.\n//\n// Deliberately reads config fresh from nsdata on every call rather than\n// caching it. The one thing that cannot be read fresh is a COMPILED\n// regex — avl trees store the pattern string, not the compiled object —\n// so that one value is cached, and the cache is validated against\n// nsdata on every use.\npackage nslogic\n\n\nconst (\n\tugnot    = \"ugnot\"\n\tbpsDenom = 10_000\n)\n\n\n"},{"name":"validate.gno","body":"package nslogic\n\nimport (\n\t\"strings\"\n\n\t\"gno.land/p/nt/ufmt/v0\"\n\t\"gno.land/r/g1xr6tgxnpled50h74eafmvxway7z0ytr5rsmeme/nsdata/v1\"\n)\n\n// THE LABEL RULE IS WRITTEN OUT, NOT COMPILED.\n//\n// It used to be a regexp, and that regexp was the single most expensive\n// thing in this realm — by a wide margin, and not by a little. Measured\n// on sapphire-1 with vm/qstorage against purpose-built probe realms:\n//\n//\ta realm with code and no package-level state      2,711 bytes\n//\tthe same realm plus ONE compiled regexp         154,877 bytes\n//\n// A compiled regexp is an object graph, and gno charges 100 ugnot for\n// every byte of it, forever. That one variable was 15.2 GNOT of the\n// 22.5 GNOT it costs to deploy this realm — about 68% of the bill, to\n// enforce a rule that fits in thirty lines.\n//\n// The second regexp was worse, because it was compiled LAZILY and cached\n// in a package-level var. That does not avoid the cost, it MOVES it: the\n// first person to register a name after a fresh deploy would have had\n// their transaction grow this realm's storage by 150KB and been charged\n// ~15 GNOT of deposit for it. Confirmed by probe, not inferred — a realm\n// deployed at 2,711 bytes went to 154,149 the moment one call compiled\n// and cached a regexp, billed to the caller. Somebody buying a ten-cent\n// name would have been asked for fifteen GNOT.\n//\n// The hand-written version is also 6x cheaper in gas on every call\n// (1.06M against 6.67M), so there is no axis on which the regexp was\n// the better choice. It read better. That was the whole of its case.\n//\n// WHAT THIS COSTS US. The pattern is no longer admin-tunable at runtime:\n// changing the rule now means redeploying this realm. That is a fair\n// trade at 7 GNOT a deploy, it was never a rule that should change\n// casually, and SetLabelPattern refuses anything but the implemented\n// pattern so the stored string can never describe rules the code does\n// not enforce.\n\n// labelRule is the pattern this realm implements, kept as a string so\n// nsdata, the error messages and the site all quote the same thing.\nconst labelRule = `^[a-z0-9]+(-[a-z0-9]+)*$`\n\n// matchesLabelRule is that pattern, by hand: lowercase letters and\n// digits, single hyphens between them, none at either end.\nfunc matchesLabelRule(s string) bool {\n\tif s == \"\" {\n\t\treturn false\n\t}\n\tprevHyphen := true // a leading hyphen is not allowed\n\tfor i := 0; i \u003c len(s); i++ {\n\t\tc := s[i]\n\t\tswitch {\n\t\tcase c \u003e= 'a' \u0026\u0026 c \u003c= 'z', c \u003e= '0' \u0026\u0026 c \u003c= '9':\n\t\t\tprevHyphen = false\n\t\tcase c == '-':\n\t\t\tif prevHyphen {\n\t\t\t\treturn false // leading, or two in a row\n\t\t\t}\n\t\t\tprevHyphen = true\n\t\tdefault:\n\t\t\treturn false\n\t\t}\n\t}\n\treturn !prevHyphen // nor a trailing one\n}\n\n// looksLikeAddress was `^g1[a-z0-9]{20,38}$`. Anything shaped like a real\n// gno.land address is refused as a label, so a name can never be mistaken\n// for a raw wallet address in a UI.\nfunc looksLikeAddress(s string) bool {\n\tif len(s) \u003c 22 || len(s) \u003e 40 || !strings.HasPrefix(s, \"g1\") {\n\t\treturn false\n\t}\n\tfor i := 2; i \u003c len(s); i++ {\n\t\tc := s[i]\n\t\tif !(c \u003e= 'a' \u0026\u0026 c \u003c= 'z') \u0026\u0026 !(c \u003e= '0' \u0026\u0026 c \u003c= '9') {\n\t\t\treturn false\n\t\t}\n\t}\n\treturn true\n}\n\n// validateLabel — same policy as the original single-realm build (see\n// NOTES.md for the ENS/Starname/r/sys/users research behind it). The\n// length bounds are still read from nsdata, so they survive a swap of\n// this realm; the character rule is now this realm's own.\n//\n// Call sites, and why: RegisterName/RegisterDomain only (both the\n// first-ever mint AND reclaiming a past-grace slot — a reclaim is a\n// genuine new mint with a new owner). RenewName/RenewDomain deliberately\n// do NOT call this — an already-registered name must keep renewing under\n// whatever rule was in force when it was minted.\nfunc validateLabel(label string) {\n\tminLen, maxLen := nsdata.GetLabelLength()\n\tif len(label) \u003c minLen || len(label) \u003e maxLen {\n\t\tpanic(ufmt.Sprintf(\"nslogic: label must be %d-%d characters\", minLen, maxLen))\n\t}\n\t// Structural invariant, enforced regardless of what the pattern\n\t// happens to allow: \"*\" is the separator in both the token ID\n\t// (label*domain) and nsdata's storage key (domain*label). A label or\n\t// domain containing one would make those encodings ambiguous and let\n\t// two different names collide onto a single record.\n\tif strings.Contains(label, \"*\") {\n\t\tpanic(\"nslogic: label may not contain '*'\")\n\t}\n\tif !matchesLabelRule(label) {\n\t\tpanic(\"nslogic: a name is lowercase letters, digits and single hyphens between them — \" + labelRule)\n\t}\n\tif looksLikeAddress(label) {\n\t\tpanic(\"nslogic: label looks like a wallet address, not allowed\")\n\t}\n}\n"}],"type":{"@type":"/gno.MemPackageType","value":"MPUserAll"}},"send":"","max_deposit":""}],"fee":{"gas_wanted":"200000000","gas_fee":"1000000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"Auy1VwQmQltziWh2LuRJj7xKSpD9chk7NirrueqIJYrk"},"signature":"Jl+V4wG+U9x6m+Y97ZxlV5McElsfkqpEz5NaVsE/TpJj/95vdowMU9dh2nVzvqAX/EX7lSRa+CrsqF32oP34zw=="}],"memo":""},"metadata":{"timestamp":"1788749266"}}
{"tx":{"msg":[{"@type":"/vm.m_call","caller":"g1v55fc8t2n8uv3jd0pqxadsjazg39v3fdzn5v4d","send":"1000000ugnot","max_deposit":"","pkg_path":"gno.land/r/gnoland/wugnot","func":"Deposit"}],"fee":{"gas_wanted":"13887432","gas_fee":"13888ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"A9niebEik8gRtWfb443DW7Z5SsXEG61MlDfR0UIAbmOh"},"signature":"HynqbKYmuXMvadf3JFwlN1bRvgFw/RceYU8aReHIhVM9HD03vGCRtDyRyFaA8Pnlf0jIaGTfgvbojIeCn/Htng=="}],"memo":"Executed through gnoswap.io"},"metadata":{"timestamp":"1788749332"}}
{"tx":{"msg":[{"@type":"/bank.MsgSend","from_address":"g18qhq2fl54lszhmxeyqlvxnwjzc3xpu4nnakclp","to_address":"g14z645actt63fmrxsqdkc4aaq4mut9cmz8d4tez","amount":"300000000ugnot"}],"fee":{"gas_wanted":"5000000","gas_fee":"1000000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"AhUuKSwiL4XKy0BM9cK0UJFToerTzvgLTHFBvYk/xOtY"},"signature":"lWo05cNFG2HVnN6BGh6AOVEoDd1f2cR5Laz9HUjWOTJhKCbLtdhbQmzpSiabV+jHTw1wOCooCT+9zTaclaUERA=="}],"memo":""},"metadata":{"timestamp":"1788749360"}}
{"tx":{"msg":[{"@type":"/vm.m_call","caller":"g1xr6tgxnpled50h74eafmvxway7z0ytr5rsmeme","send":"","max_deposit":"","pkg_path":"gno.land/r/g1xr6tgxnpled50h74eafmvxway7z0ytr5rsmeme/nsdata/v1","func":"ProposeTrustedLogicGrant","args":["g1rrmeeed3qtwe6vwf9n3prpfgczswtv8wnyfnjv"]}],"fee":{"gas_wanted":"80000000","gas_fee":"80000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"Auy1VwQmQltziWh2LuRJj7xKSpD9chk7NirrueqIJYrk"},"signature":"A2Zc7Vyp/KHAoAB31BpYDgUIoMuD73FehCZf6EoaSNMP8nS3QvhT5D358b7tH1nw4lhhHw6YbapwN6XO1NHdUA=="}],"memo":""},"metadata":{"timestamp":"1788749384"}}
{"tx":{"msg":[{"@type":"/vm.m_call","caller":"g1xr6tgxnpled50h74eafmvxway7z0ytr5rsmeme","send":"","max_deposit":"","pkg_path":"gno.land/r/g1xr6tgxnpled50h74eafmvxway7z0ytr5rsmeme/nsdata/v1","func":"ExecutePending","args":["grantLogic"]}],"fee":{"gas_wanted":"80000000","gas_fee":"80000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"Auy1VwQmQltziWh2LuRJj7xKSpD9chk7NirrueqIJYrk"},"signature":"yDaCVH5OST/XkPc5svcOiWj6de5/KNq4ibw2FnI4dJp3WZZHD3Db6l81C2nRAqToLKk03+XxajBaZeiltfJSiA=="}],"memo":""},"metadata":{"timestamp":"1788749388"}}
{"tx":{"msg":[{"@type":"/vm.m_call","caller":"g1xr6tgxnpled50h74eafmvxway7z0ytr5rsmeme","send":"","max_deposit":"","pkg_path":"gno.land/r/g1xr6tgxnpled50h74eafmvxway7z0ytr5rsmeme/nslogic/v4","func":"SetGranter","args":["g1xr6tgxnpled50h74eafmvxway7z0ytr5rsmeme"]}],"fee":{"gas_wanted":"80000000","gas_fee":"80000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"Auy1VwQmQltziWh2LuRJj7xKSpD9chk7NirrueqIJYrk"},"signature":"DDZeHPkIUYAknJP1qRpbyoqPGe8fh5c/7eei5pDwzsVssnaTzlhW65XU5/XcXIj+0Th871q26HaJkgn+H/LixA=="}],"memo":""},"metadata":{"timestamp":"1788749391"}}
{"tx":{"msg":[{"@type":"/bank.MsgSend","from_address":"g18qhq2fl54lszhmxeyqlvxnwjzc3xpu4nnakclp","to_address":"g1lxjcz8pesu7xv7fys74ndlptg542k8l839mphj","amount":"300000000ugnot"}],"fee":{"gas_wanted":"5000000","gas_fee":"1000000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"AhUuKSwiL4XKy0BM9cK0UJFToerTzvgLTHFBvYk/xOtY"},"signature":"8OFpoDc2FqGw7yU2mnwpnnTrtkm7cX7fKxgyIuvDa8V+PU4Eu+w5W99um8XZw8na6p1Evt1R/TgaOZ/YPQqvxw=="}],"memo":""},"metadata":{"timestamp":"1788749391"}}
{"tx":{"msg":[{"@type":"/vm.m_call","caller":"g1xr6tgxnpled50h74eafmvxway7z0ytr5rsmeme","send":"","max_deposit":"","pkg_path":"gno.land/r/g1xr6tgxnpled50h74eafmvxway7z0ytr5rsmeme/nslogic/v4","func":"SetDomainFree","args":["meme","true"]}],"fee":{"gas_wanted":"80000000","gas_fee":"80000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"Auy1VwQmQltziWh2LuRJj7xKSpD9chk7NirrueqIJYrk"},"signature":"Y0/QkXSugKgjUFcn6/UbtJhDw8Ndhp7VB+YrzlVFXCVZ05IESLVOFNGp/DsZWlCIRcfBx//4+V0FkCAyCLsMJw=="}],"memo":""},"metadata":{"timestamp":"1788749395"}}
{"tx":{"msg":[{"@type":"/vm.m_call","caller":"g1n4y3l58k3wfa4r0nxhvz3f8g6a9d699pm9nent","send":"7101963ugnot","max_deposit":"","pkg_path":"gno.land/r/gnoland/wugnot","func":"Deposit"}],"fee":{"gas_wanted":"345600000","gas_fee":"3456000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"ArpR6V19tMzRzGMbcvC1U3OGD3MtTgJk2VxNGmucRIPD"},"signature":"nXiELEHa6MtP2qgy8Fr9wKTPydsMZqtNwrfJf7uguk48cMonRpK+AxLh80OsQbPY3iq92ktYinr7IBiLJsaOLw=="}],"memo":""},"metadata":{"timestamp":"1788749417"}}
{"tx":{"msg":[{"@type":"/vm.m_call","caller":"g1vc4verl8sfs6j3dp65tqqtlheuvuguseulqg3j","send":"3196933ugnot","max_deposit":"","pkg_path":"gno.land/r/gnoland/wugnot","func":"Deposit"}],"fee":{"gas_wanted":"342400000","gas_fee":"3424000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"AqRByAyiZS5vENGjPhk9WJIJH8snSvCJSSz5ocLwLAfc"},"signature":"V7DAVmoP/mg1Rn9b06VdCPkRpkrTccrEVwPxGkyErDtP/oU4nqJuWKWO/QV2+RoiXEvxqTdmHpUL316SYxET3Q=="}],"memo":""},"metadata":{"timestamp":"1788749480"}}
{"tx":{"msg":[{"@type":"/vm.m_call","caller":"g1pjszhr99wfd7ax0y6xykvs79n7yv9elzekcaav","send":"","max_deposit":"","pkg_path":"gno.land/r/gnoland/wugnot","func":"Withdraw","args":["11729344"]}],"fee":{"gas_wanted":"368000000","gas_fee":"3680000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"AmLZxPzwjzORqu7wqCWEtLp79nwTs6CQhgZA3TBQj2wU"},"signature":"2VXrKc0pMr6mG6iE3J8jpjORj9O/XDDoIeesVcJftW4H3N4aSjHNO5Gpn4xjKiGdngXQsuoLweGlT76Dplw+Nw=="}],"memo":""},"metadata":{"timestamp":"1788749490"}}
{"tx":{"msg":[{"@type":"/bank.MsgSend","from_address":"g18qhq2fl54lszhmxeyqlvxnwjzc3xpu4nnakclp","to_address":"g14cu67nat5p7nhyjuf08qczg7j39uuz5r84h8sp","amount":"300000000ugnot"}],"fee":{"gas_wanted":"5000000","gas_fee":"1000000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"AhUuKSwiL4XKy0BM9cK0UJFToerTzvgLTHFBvYk/xOtY"},"signature":"Jnh5MjX+a9sBkW10SteSBa/1BYrv9woKN/RCW9e3setrdQ0fsyXlLLpue3nh1kkfXN4lXZ72V503mwZCCMg1oA=="}],"memo":""},"metadata":{"timestamp":"1788749518"}}
{"tx":{"msg":[{"@type":"/bank.MsgSend","from_address":"g18qhq2fl54lszhmxeyqlvxnwjzc3xpu4nnakclp","to_address":"g1tdlsp93c5cecagnylyh7q4u3w0m8v74wtft0p4","amount":"300000000ugnot"}],"fee":{"gas_wanted":"5000000","gas_fee":"1000000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"AhUuKSwiL4XKy0BM9cK0UJFToerTzvgLTHFBvYk/xOtY"},"signature":"VPT2hIPhtVqP5dK5K6KuaEuJJ1MrUdpJtVysVssJ15pTWX0aTtSMjd3qC5DFBy3nuKwuiVKftHFoL1axc9O9BA=="}],"memo":""},"metadata":{"timestamp":"1788749559"}}
{"tx":{"msg":[{"@type":"/vm.m_call","caller":"g1f70ew7zqeg6cvczfxt0wvzy7rnkal0kwpy22x3","send":"","max_deposit":"","pkg_path":"gno.land/r/gnoland/wugnot","func":"Withdraw","args":["8572519"]}],"fee":{"gas_wanted":"336000000","gas_fee":"3360000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"Al4lhc3CCMZcUwr6mj7xuqabDCQD/Jt4vklR7yo0e7FM"},"signature":"9mxrpF2WXq++yj/Han6IJ94NLumBx8Wa6rPGvQoD0nYDtyux/WhbRFdhjjlXseIlMYuiQiMfdIrZVkqeo2l1ZA=="}],"memo":""},"metadata":{"timestamp":"1788749587"}}
{"tx":{"msg":[{"@type":"/vm.m_call","caller":"g135wfkgjhca3z7g87x4463xxll5f680rpljt2ux","send":"5862282ugnot","max_deposit":"","pkg_path":"gno.land/r/gnoland/wugnot","func":"Deposit"}],"fee":{"gas_wanted":"387200000","gas_fee":"3872000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"A+whKhvpRJqpOljyCp/E923gnzFlV1fqWmGXkGv3avhy"},"signature":"T0a1NVRqn3sP1deZKa72MXTDmKxqLrwJ6UzaJXDX6NVncP6akwHZeKcucR9DOHPp928tazACm+8oZ14RVThIxg=="}],"memo":""},"metadata":{"timestamp":"1788749607"}}
{"tx":{"msg":[{"@type":"/vm.m_call","caller":"g1nxgjwpypjczfyq3atafxtucpcuh4czw5qp7c9m","send":"22998517ugnot","max_deposit":"","pkg_path":"gno.land/r/gnoland/wugnot","func":"Deposit"}],"fee":{"gas_wanted":"361600000","gas_fee":"3616000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"AkxeNJqnFLUsf9ozEM8L3dj3h2Fl5dc03cKLtP1YNRbe"},"signature":"lR5mWsjVGEtHcBzVRWNlSVqdOBIi37Gzf3mcB3UTBBw7jdjIMwPuwXpxWxcwEdGeUABTp/qzq47s2RNJXYtwAA=="}],"memo":""},"metadata":{"timestamp":"1788749621"}}
{"tx":{"msg":[{"@type":"/vm.m_call","caller":"g135wfkgjhca3z7g87x4463xxll5f680rpljt2ux","send":"","max_deposit":"","pkg_path":"gno.land/r/gnoland/wugnot","func":"Withdraw","args":["2053949"]}],"fee":{"gas_wanted":"400000000","gas_fee":"4000000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"A+whKhvpRJqpOljyCp/E923gnzFlV1fqWmGXkGv3avhy"},"signature":"Neqk9Qq2m21UAqmIBsoidpkAvJaZNWzOWbV28RP1s5gB2MyZpl7PoWAq4m0/FTiMqhRlOxsYcf39EFwQMAEUOg=="}],"memo":""},"metadata":{"timestamp":"1788749621"}}
{"tx":{"msg":[{"@type":"/vm.m_call","caller":"g1eut9gpfc639hpe67mh0kftzveadw934h87quh6","send":"","max_deposit":"","pkg_path":"gno.land/r/gnoland/wugnot","func":"Withdraw","args":["7709754"]}],"fee":{"gas_wanted":"377600000","gas_fee":"3776000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"At3kbJJJsd4M60dXpLJubVux+DM88zQ4AKHWC1sSgW42"},"signature":"oqRlXdG/hZaT8gDwEAy7213dOY87i3hXp1ba2flL7k1xQ8k2XyoZmi8Vuj4VL1gPdXIJVBcMrmVXArqH+/qrFw=="}],"memo":""},"metadata":{"timestamp":"1788749632"}}
{"tx":{"msg":[{"@type":"/vm.m_call","caller":"g10ack634rfrgph0zxza4temuxmgpf4p33tdzax5","send":"7537113ugnot","max_deposit":"","pkg_path":"gno.land/r/gnoland/wugnot","func":"Deposit"}],"fee":{"gas_wanted":"323200000","gas_fee":"3232000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"AreqH7Df+tZZOIff/kXTjLoJB6Hsn/q9nYSkI9yDJ9MW"},"signature":"oqMj6WwXvtvSvygte2Ve2w5V1aWnwfhEMW5ge5DN7OlMFj2nD3vuO2WES3/gUiaAmFbIhmSXTUk7F9jNu1gO2w=="}],"memo":""},"metadata":{"timestamp":"1788749740"}}
{"tx":{"msg":[{"@type":"/vm.m_call","caller":"g17l333d24cwqka2ja6qjxk7hnp39jvneznd7p2m","send":"2161528ugnot","max_deposit":"","pkg_path":"gno.land/r/gnoland/wugnot","func":"Deposit"}],"fee":{"gas_wanted":"371200000","gas_fee":"3712000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"Ahw6rbi7t9IJWU47usP5rQf+iO1vTFVCbrCJMHCF9h4L"},"signature":"FQN4zVtQYzWh40W89RbcVzYTR3jinL0hWZE4m0nHCfBljJWxBydQPQ3B33akkJPwv5vyjO+sGJD5oLAhSsySlw=="}],"memo":""},"metadata":{"timestamp":"1788749799"}}
{"tx":{"msg":[{"@type":"/vm.m_call","caller":"g1eqv0sxu9ygeard6fe9xgeat750cxu0w9q7hpdf","send":"1302346ugnot","max_deposit":"","pkg_path":"gno.land/r/gnoland/wugnot","func":"Deposit"}],"fee":{"gas_wanted":"326400000","gas_fee":"3264000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"A542gl7YL30pbPkW5abA7FHDJldSvspis7Il7FZ+Jc51"},"signature":"/NOf7BK1HP9BSdHEJUIuX1jRyx185N891YU/b7BUC50j/tOEQ1frIEdXhejdSAVugkQJlyQRQxQhSlrjPTiung=="}],"memo":""},"metadata":{"timestamp":"1788749803"}}
{"tx":{"msg":[{"@type":"/vm.m_call","caller":"g17l333d24cwqka2ja6qjxk7hnp39jvneznd7p2m","send":"2035092ugnot","max_deposit":"","pkg_path":"gno.land/r/gnoland/wugnot","func":"Deposit"}],"fee":{"gas_wanted":"387200000","gas_fee":"3872000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"Ahw6rbi7t9IJWU47usP5rQf+iO1vTFVCbrCJMHCF9h4L"},"signature":"oF0jO20vWcjv2SSL3NSPgzs+EWZeBf1DasQdWiS6pPdintHGEk3kRDF1hNDHuLN3i90ffuilyVIevap64e061g=="}],"memo":""},"metadata":{"timestamp":"1788749813"}}
{"tx":{"msg":[{"@type":"/vm.m_call","caller":"g1dh344545ksldha40fg35xxylf9x5ull78hcqnr","send":"2614438ugnot","max_deposit":"","pkg_path":"gno.land/r/gnoland/wugnot","func":"Deposit"}],"fee":{"gas_wanted":"374400000","gas_fee":"3744000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"A+D75aPqViaVv5xsXl46ZBm44OnKUy+1wzswhZ1xxiqH"},"signature":"LOLkYZsu0LbdP+gC7eQeqMT8JiyuiQfMtj+UrXKzryAEvzQ+5BStQikTzuvm2AA28J9mdj3mNuLdrjiQLeD6BA=="}],"memo":""},"metadata":{"timestamp":"1788749817"}}
{"tx":{"msg":[{"@type":"/vm.m_call","caller":"g1zcs8vht4t6p2tcjck7mz3y3k250c5klre6j33q","send":"","max_deposit":"","pkg_path":"gno.land/r/gnoland/wugnot","func":"Withdraw","args":["3757190"]}],"fee":{"gas_wanted":"368000000","gas_fee":"3680000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"A0Y7Atvf1f655p+YbQmjVEA0oq4sVlzf82jIDB03YzV5"},"signature":"DkGOEI7Px+xkhpopJiEk8fQEJeiEknbeeGxuyE8Q3zwHE1FSP6dx0Tkq1tv0CLEE8kNemUm+OhNVmauO4dEl7A=="}],"memo":""},"metadata":{"timestamp":"1788749821"}}
{"tx":{"msg":[{"@type":"/vm.m_call","caller":"g1q9qyl0x02qz56ejqz2rxzt3tsmcd8epwnxggfg","send":"","max_deposit":"","pkg_path":"gno.land/r/gnoland/wugnot","func":"Withdraw","args":["9397793"]}],"fee":{"gas_wanted":"380800000","gas_fee":"3808000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"AnxWDpSMj3kR9Yj8N9nkclROl4Lr1Ah0imMUzH7DWciF"},"signature":"UwLVMLoOJBGwqfmSBCvCoyVvvNgjD7xY5nt0veG1GtdGUnzcmdFEkqcWMGrl+K7SekrDaxajy7E2l7P/cBnKOQ=="}],"memo":""},"metadata":{"timestamp":"1788749824"}}
{"tx":{"msg":[{"@type":"/vm.m_call","caller":"g1r2wvz5vhutmwd8g9q4hxcrs8wyy4yf6tuds50g","send":"6346797ugnot","max_deposit":"","pkg_path":"gno.land/r/gnoland/wugnot","func":"Deposit"}],"fee":{"gas_wanted":"358400000","gas_fee":"3584000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"Arc+knwLYSkB+6BqpqsqBlf0ynXp1coLCYfraRW49pFE"},"signature":"u+kYrztLmxfRU2hON03CW0yrU7qH0QxDcslAUoHks3BnGuJV+akjFq1a8qiM26xypuRTVqezSCf7iMddU7h6DQ=="}],"memo":""},"metadata":{"timestamp":"1788749831"}}
{"tx":{"msg":[{"@type":"/vm.m_call","caller":"g1q9qyl0x02qz56ejqz2rxzt3tsmcd8epwnxggfg","send":"","max_deposit":"","pkg_path":"gno.land/r/gnoland/wugnot","func":"Withdraw","args":["7745917"]}],"fee":{"gas_wanted":"387200000","gas_fee":"3872000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"AnxWDpSMj3kR9Yj8N9nkclROl4Lr1Ah0imMUzH7DWciF"},"signature":"PlvR/mvL5mvuuxpy3xG4cd4UQWm7VZeX+pK7UTR5Cj1imXc5U1ycsfmvHGzsZRtaRPl576klM1c4nRWsqkXQOw=="}],"memo":""},"metadata":{"timestamp":"1788749834"}}
{"tx":{"msg":[{"@type":"/vm.m_call","caller":"g1r2wvz5vhutmwd8g9q4hxcrs8wyy4yf6tuds50g","send":"","max_deposit":"","pkg_path":"gno.land/r/gnoland/wugnot","func":"Withdraw","args":["3958561"]}],"fee":{"gas_wanted":"348800000","gas_fee":"3488000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"Arc+knwLYSkB+6BqpqsqBlf0ynXp1coLCYfraRW49pFE"},"signature":"65KeD9Ie9ODeiMpbOzVqkJwKrczPxxTxNmxKHMMZq14KL39dqUdlln0wUxy5z6/0tj0ncNDi4mGJXMX/AninZw=="}],"memo":""},"metadata":{"timestamp":"1788749841"}}
{"tx":{"msg":[{"@type":"/vm.m_call","caller":"g1hd5he9seme6kcz2cxxfp32rpnhqh79z9fvscxy","send":"","max_deposit":"","pkg_path":"gno.land/r/gnoland/wugnot","func":"Withdraw","args":["5437355"]}],"fee":{"gas_wanted":"361600000","gas_fee":"3616000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"A35yGhgJ3kCtEjj2DDlBSD/CJcLwkgB+s5QXdc1ugTh/"},"signature":"oo6k9MlZCB2IKkYtgZQ79EtqFMjlmAgmaQ4T5aVn7vwSNGtD/IfxOCOESi+HJ0qMlm4xrW2MKP0em/SKovzXcQ=="}],"memo":""},"metadata":{"timestamp":"1788749865"}}
{"tx":{"msg":[{"@type":"/vm.m_call","caller":"g1qdraegrgyyzqr7nq45cwgaezqlghpt400yjd55","send":"","max_deposit":"","pkg_path":"gno.land/r/gnoland/wugnot","func":"Withdraw","args":["4253403"]}],"fee":{"gas_wanted":"339200000","gas_fee":"3392000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"AupR9oLdsipcZZ12CCK+2pI7SHwezh4E78naX4iaxsZ2"},"signature":"O1cvdxBq0L96gsb2Hh0vGw8O3AcsRpL/1UIYZTRbTpBoi5FeJprr9JSwPyry4IDpbdmNaNFQag7zNrtWVhDD3A=="}],"memo":""},"metadata":{"timestamp":"1788749869"}}
{"tx":{"msg":[{"@type":"/vm.m_call","caller":"g1fypteuk8849uu8skl2k0x76cev35pyu63vc0w6","send":"18041927ugnot","max_deposit":"","pkg_path":"gno.land/r/gnoland/wugnot","func":"Deposit"}],"fee":{"gas_wanted":"393600000","gas_fee":"3936000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"AkGQw/db0qrmU5YxdmMcUWQUCmohWg3lX1mdcIdJczsj"},"signature":"si7NivN18g6+nADjXcjAjgWQI8R6ElD5aEKpTOxli2JPcg2mMW6uQly99x75ROkRTuqZIlwWnT+//12RWPZyHg=="}],"memo":""},"metadata":{"timestamp":"1788749872"}}
{"tx":{"msg":[{"@type":"/vm.m_call","caller":"g1hd5he9seme6kcz2cxxfp32rpnhqh79z9fvscxy","send":"","max_deposit":"","pkg_path":"gno.land/r/gnoland/wugnot","func":"Withdraw","args":["4304400"]}],"fee":{"gas_wanted":"342400000","gas_fee":"3424000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"A35yGhgJ3kCtEjj2DDlBSD/CJcLwkgB+s5QXdc1ugTh/"},"signature":"UTH66nbaqe+QMM8q5kj4bDEKqlv/giu0F2mcHTg9dlM3ys6Qb3yHkQb7Ro+tVMAK4/UIeOK5HVUi5SqJ3fTt6A=="}],"memo":""},"metadata":{"timestamp":"1788749876"}}
{"tx":{"msg":[{"@type":"/vm.m_call","caller":"g1fypteuk8849uu8skl2k0x76cev35pyu63vc0w6","send":"18552795ugnot","max_deposit":"","pkg_path":"gno.land/r/gnoland/wugnot","func":"Deposit"}],"fee":{"gas_wanted":"361600000","gas_fee":"3616000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"AkGQw/db0qrmU5YxdmMcUWQUCmohWg3lX1mdcIdJczsj"},"signature":"5+S3bItE+ELe69Se3oFE/1NPy8pw3UeJew2hozGdSWJlDvvjCGfDAtfE6eqaIj3o+Vu04eI47DQvdtCZVXuZsA=="}],"memo":""},"metadata":{"timestamp":"1788749881"}}
{"tx":{"msg":[{"@type":"/vm.m_call","caller":"g1unhstutkzujaq9tjtxsea9w84fnlhtrwpr0987","send":"2008927ugnot","max_deposit":"","pkg_path":"gno.land/r/gnoland/wugnot","func":"Deposit"}],"fee":{"gas_wanted":"384000000","gas_fee":"3840000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"A9ZkhOlC+DFdcl+EJ/fTCrx/vundd5OodsLkIFkrcRbI"},"signature":"IWmewElK1fAZsM0CNiobu9kVpAeAgujBa0Fv+Kdf6ZkdZgwVr8AEXA+SaOrK/2CcNjQvqt/mkQtnAuo8GgZ0Kw=="}],"memo":""},"metadata":{"timestamp":"1788749891"}}
{"tx":{"msg":[{"@type":"/vm.m_call","caller":"g1unhstutkzujaq9tjtxsea9w84fnlhtrwpr0987","send":"","max_deposit":"","pkg_path":"gno.land/r/gnoland/wugnot","func":"Withdraw","args":["2205951"]}],"fee":{"gas_wanted":"323200000","gas_fee":"3232000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"A9ZkhOlC+DFdcl+EJ/fTCrx/vundd5OodsLkIFkrcRbI"},"signature":"50wUznvKcATpIIKOc9DdwcJeHtFDcXOEsPdLI5gPJ5oDqPpBc2KtyWYPOBPAF4SGSRMRAmaM2fTGZfa9/QtSgA=="}],"memo":""},"metadata":{"timestamp":"1788749902"}}
{"tx":{"msg":[{"@type":"/bank.MsgSend","from_address":"g18qhq2fl54lszhmxeyqlvxnwjzc3xpu4nnakclp","to_address":"g1up675fxk3wv5s0ldjlqglpgrkdrmzvg5e4rcse","amount":"300000000ugnot"}],"fee":{"gas_wanted":"5000000","gas_fee":"1000000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"AhUuKSwiL4XKy0BM9cK0UJFToerTzvgLTHFBvYk/xOtY"},"signature":"5hYowc7ci+yNK31Mw7C/ZvYhqbEbGbyylMUDh0srsqpgo4mjsH37XTvhj7Cl3lWJHPv/0SlEN4TXV2pYVqLdow=="}],"memo":""},"metadata":{"timestamp":"1788749972"}}
{"tx":{"msg":[{"@type":"/vm.m_call","caller":"g14m2uegqfeptx05vsw9pj5g3vvcvgta0zyducvw","send":"7437433ugnot","max_deposit":"","pkg_path":"gno.land/r/gnoland/wugnot","func":"Deposit"}],"fee":{"gas_wanted":"390400000","gas_fee":"3904000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"A46ODEnpX4gemzy1RcohVcccc3JCcqrMnbizBw6e/yb9"},"signature":"2cQwdVjxybPiza/qaeL+rxv9ZYR2JBKwN7gOHn+KrOV+CVeY2ijds/lJcF4g7ZYieHW18UQU/74pQJ9cuh92XA=="}],"memo":""},"metadata":{"timestamp":"1788749999"}}
{"tx":{"msg":[{"@type":"/vm.m_call","caller":"g13y4y2anjnrkze0qjzy3alnny3cyaw96q635lru","send":"","max_deposit":"","pkg_path":"gno.land/r/gnoland/wugnot","func":"Withdraw","args":["5937028"]}],"fee":{"gas_wanted":"339200000","gas_fee":"3392000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"AycQIGNj1REl+P4uZKtZsjQ6BIXgDpEB7ETYYha52da2"},"signature":"on+GQUVY9kPHLGzUEj1RVQBZvLHtmPxMguNEA1lDwQtLyBJ71244pSP2xYYIY98dS83YjbrsAkqHM1OXVa4ARg=="}],"memo":""},"metadata":{"timestamp":"1788750012"}}
{"tx":{"msg":[{"@type":"/vm.m_call","caller":"g14m2uegqfeptx05vsw9pj5g3vvcvgta0zyducvw","send":"4804687ugnot","max_deposit":"","pkg_path":"gno.land/r/gnoland/wugnot","func":"Deposit"}],"fee":{"gas_wanted":"342400000","gas_fee":"3424000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"A46ODEnpX4gemzy1RcohVcccc3JCcqrMnbizBw6e/yb9"},"signature":"aq/WDq4zBQMKq+VhiFMUd7ewo+l41mkPycDbPFSXkr8BBGddfDxlfrfpwYK7mRZ58qQdtYBQp+7qmqn3zbEX4Q=="}],"memo":""},"metadata":{"timestamp":"1788750016"}}
{"tx":{"msg":[{"@type":"/vm.m_call","caller":"g18mxufyz2gjll7xxcfe5g9k6azt5q2s2c3h08t4","send":"2777185ugnot","max_deposit":"","pkg_path":"gno.land/r/gnoland/wugnot","func":"Deposit"}],"fee":{"gas_wanted":"336000000","gas_fee":"3360000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"AroCGMuUyu3409MHim9+TjUDDFLslThYOyVWv6va3xwg"},"signature":"o7Qs7c3s+eeKfBWMCBFoTZVhTuV874xhY7ISbI4+NapsYXdUPJtYuOxgRYWoR6zWOVvCO0DAn7cCBydAYJoxEA=="}],"memo":""},"metadata":{"timestamp":"1788750071"}}
{"tx":{"msg":[{"@type":"/bank.MsgSend","from_address":"g18qhq2fl54lszhmxeyqlvxnwjzc3xpu4nnakclp","to_address":"g1w4u95v4d9z4hgvjf6f2yzd0n74a50tpe7ykwpf","amount":"300000000ugnot"}],"fee":{"gas_wanted":"5000000","gas_fee":"1000000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"AhUuKSwiL4XKy0BM9cK0UJFToerTzvgLTHFBvYk/xOtY"},"signature":"3c7lxSvNNvxTlb5Q437CFN6Ky0mFx9SSkQ7BBYxQFE5EgAaEeE0jvZoMS4clg2vt8uDexSLcO+IAZSojIaQgig=="}],"memo":""},"metadata":{"timestamp":"1788750078"}}
{"tx":{"msg":[{"@type":"/vm.m_call","caller":"g18mxufyz2gjll7xxcfe5g9k6azt5q2s2c3h08t4","send":"","max_deposit":"","pkg_path":"gno.land/r/gnoland/wugnot","func":"Withdraw","args":["4324569"]}],"fee":{"gas_wanted":"390400000","gas_fee":"3904000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"AroCGMuUyu3409MHim9+TjUDDFLslThYOyVWv6va3xwg"},"signature":"1BpvB8Wn/OEruAvjyHQF7Kv4Lr71BV0DK4gbi6kRTxVZSfVax2XmvG9QkCqL7EXPKlwyBQ78CXSTI0OKy2sUWw=="}],"memo":""},"metadata":{"timestamp":"1788750089"}}
{"tx":{"msg":[{"@type":"/vm.m_call","caller":"g1fphls8xgp8krdve6y89s2a06npjpx4ywlkg6gm","send":"2395825ugnot","max_deposit":"","pkg_path":"gno.land/r/gnoland/wugnot","func":"Deposit"}],"fee":{"gas_wanted":"352000000","gas_fee":"3520000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"Ax6sbApfJhNzAIwtr8AnVOoWwK0E6WniyQeJTkB4j2V3"},"signature":"L9CkIn5qqUPtCPYJp9KboXKysrhP+w//iGye3l0xjO0JilzGa8EfxSlTm7KrAPeC/y8aUbPQT5JnZhOa3PF9sg=="}],"memo":""},"metadata":{"timestamp":"1788750095"}}
{"tx":{"msg":[{"@type":"/vm.m_call","caller":"g1fphls8xgp8krdve6y89s2a06npjpx4ywlkg6gm","send":"","max_deposit":"","pkg_path":"gno.land/r/gnoland/wugnot","func":"Withdraw","args":["1068456"]}],"fee":{"gas_wanted":"323200000","gas_fee":"3232000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"Ax6sbApfJhNzAIwtr8AnVOoWwK0E6WniyQeJTkB4j2V3"},"signature":"UXJM4mHo6QlfQDyhmS807l9/hVolc5NUkbhI1nBrtdpUhzyyVtycJbVTF+XnkfLW1cMzne6iySC3qmc6IdTn9w=="}],"memo":""},"metadata":{"timestamp":"1788750106"}}
{"tx":{"msg":[{"@type":"/bank.MsgSend","from_address":"g1jg8mtutu9khhfwc4nxmuhcpftf0pajdhfvsqf5","to_address":"g18tml4dnf8rmexdgkcj8ukgdjtcsz8kk0rww4aw","amount":"100000ugnot"}],"fee":{"gas_wanted":"1025141","gas_fee":"1026ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"A+FhNtsXHjLfSJk1lB8FbiL4mGPjc50Kt81J7EKDnJ2y"},"signature":"10E6Wi7+ja6I7L/oCce45NkyKO1vS/XhJiEuKNjUddVCJrjGUqqd2qYoOz2RWBb2cly6l1MRCMsQS6JpowT0sA=="}],"memo":""},"metadata":{"timestamp":"1788750148"}}
{"tx":{"msg":[{"@type":"/vm.m_call","caller":"g1dgnqfq4p28zqpdg5tt0cp2hx96k207kxqnc52e","send":"1724073ugnot","max_deposit":"","pkg_path":"gno.land/r/gnoland/wugnot","func":"Deposit"}],"fee":{"gas_wanted":"355200000","gas_fee":"3552000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"A9/gTiU2HruIP2eQbHJjuvuuTLE70TuO19A2cM3tGawH"},"signature":"yPanlM4awW5t3RacmxwNOTmvjhTspw6L/1ln7HB99H4AB18XFVKPznsk2WFqzW6zFZ6tE+Wml1klfMRQHLbq8w=="}],"memo":""},"metadata":{"timestamp":"1788750200"}}
{"tx":{"msg":[{"@type":"/vm.m_call","caller":"g1dgnqfq4p28zqpdg5tt0cp2hx96k207kxqnc52e","send":"","max_deposit":"","pkg_path":"gno.land/r/gnoland/wugnot","func":"Withdraw","args":["5318353"]}],"fee":{"gas_wanted":"387200000","gas_fee":"3872000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"A9/gTiU2HruIP2eQbHJjuvuuTLE70TuO19A2cM3tGawH"},"signature":"8TxPBB7Dw5mhNh3VB5idXbnskUmX4hgcfASUhIuyqORfcyTp84EDzO9mEYe+GgtVlgY+8fK1cCBZVNUoQaxFIQ=="}],"memo":""},"metadata":{"timestamp":"1788750210"}}
{"tx":{"msg":[{"@type":"/bank.MsgSend","from_address":"g1jg8mtutu9khhfwc4nxmuhcpftf0pajdhfvsqf5","to_address":"g18tml4dnf8rmexdgkcj8ukgdjtcsz8kk0rww4aw","amount":"100000ugnot"}],"fee":{"gas_wanted":"2000000","gas_fee":"1000000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"A+FhNtsXHjLfSJk1lB8FbiL4mGPjc50Kt81J7EKDnJ2y"},"signature":"8pqhvMMxGU+iCesCKNKivdEt7/SsMTx/m/8e3LfnIgBSODTTsg/VFfQ4EoDee1cVoDBbh4fL5GhCFo0thSiL3A=="}],"memo":""},"metadata":{"timestamp":"1788750228"}}
{"tx":{"msg":[{"@type":"/bank.MsgSend","from_address":"g1jg8mtutu9khhfwc4nxmuhcpftf0pajdhfvsqf5","to_address":"g18tml4dnf8rmexdgkcj8ukgdjtcsz8kk0rww4aw","amount":"100000ugnot"}],"fee":{"gas_wanted":"2000000","gas_fee":"1000000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"A+FhNtsXHjLfSJk1lB8FbiL4mGPjc50Kt81J7EKDnJ2y"},"signature":"Y/L6hlSgC96/93YAaxMvQ7Hzhjcj9VEey1dFLVF0HSMPOrG6JGiDwQiNeVkvnoLy/M2ewM6yr3G92Ku9fB9dtg=="}],"memo":""},"metadata":{"timestamp":"1788750241"}}
{"tx":{"msg":[{"@type":"/bank.MsgSend","from_address":"g1jg8mtutu9khhfwc4nxmuhcpftf0pajdhfvsqf5","to_address":"g18tml4dnf8rmexdgkcj8ukgdjtcsz8kk0rww4aw","amount":"100000ugnot"}],"fee":{"gas_wanted":"2000000","gas_fee":"1000000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"A+FhNtsXHjLfSJk1lB8FbiL4mGPjc50Kt81J7EKDnJ2y"},"signature":"4x0w7bLb4lNssoejUwTt+48fhmsS1HsSuUceV9EiOcNrnC6zn3LXFv3PEKidGkcUsYjilL+lu4uKjl0MRfKx1g=="}],"memo":""},"metadata":{"timestamp":"1788750245"}}
{"tx":{"msg":[{"@type":"/bank.MsgSend","from_address":"g18qhq2fl54lszhmxeyqlvxnwjzc3xpu4nnakclp","to_address":"g15qm7aruvs8r3n6fdjh2e7hntvt8klfqkrxxcmy","amount":"300000000ugnot"}],"fee":{"gas_wanted":"5000000","gas_fee":"1000000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"AhUuKSwiL4XKy0BM9cK0UJFToerTzvgLTHFBvYk/xOtY"},"signature":"TqzIABN2v6crcwTvz3OBMLcJvQFuk+LBmKTi0X76is9UnP+rWJuHrOZW5WXLZ+BRXo6Wc9lgyA1wuTIp0/JlHg=="}],"memo":""},"metadata":{"timestamp":"1788750294"}}
{"tx":{"msg":[{"@type":"/vm.m_call","caller":"g12qerfzxw4e6nxt6ksgzmatxc4gt3udv47umeea","send":"8973808ugnot","max_deposit":"","pkg_path":"gno.land/r/gnoland/wugnot","func":"Deposit"}],"fee":{"gas_wanted":"377600000","gas_fee":"3776000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"A1fHnQ5SUZTaAVfmvfeMlnh9veKNRZoHJSM2kSf8dae6"},"signature":"OXqZbV3Cou72NoyYxv0s5pTsR93MLnXcA74EMpFJbuQhhcaT9rONjcBLzMRI7k7kiUJHS62O56GCxfDrLoxqUw=="}],"memo":""},"metadata":{"timestamp":"1788750297"}}
{"tx":{"msg":[{"@type":"/vm.m_call","caller":"g1hzjuzgfl4f3ytcgzwpgwk3kuj4p6nhhs3ydhn8","send":"5134546ugnot","max_deposit":"","pkg_path":"gno.land/r/gnoland/wugnot","func":"Deposit"}],"fee":{"gas_wanted":"336000000","gas_fee":"3360000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"AkslXXYltDJHomIrEWT/2GXkbT8f8vW/ihCb0/As+MMr"},"signature":"89Ec6pkUBPT6UJxRaPtxOs3APZQRh+ckx7mPLK86yhZ+jk69zhZK19xcqyIQM8eqU62Xzv9vhm0SR+bjY40cUw=="}],"memo":""},"metadata":{"timestamp":"1788750304"}}
{"tx":{"msg":[{"@type":"/vm.m_call","caller":"g12qerfzxw4e6nxt6ksgzmatxc4gt3udv47umeea","send":"7042820ugnot","max_deposit":"","pkg_path":"gno.land/r/gnoland/wugnot","func":"Deposit"}],"fee":{"gas_wanted":"393600000","gas_fee":"3936000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"A1fHnQ5SUZTaAVfmvfeMlnh9veKNRZoHJSM2kSf8dae6"},"signature":"vfD/KA51ZXQYDGYWIQid4nNf7rkO5hlBC6uoEdgUQJBQpi9XXfFmxgG60qdTQNhbBScfGKpVEXTSNtXGRg0xdA=="}],"memo":""},"metadata":{"timestamp":"1788750307"}}
{"tx":{"msg":[{"@type":"/vm.m_call","caller":"g1nshunncxpln0pn3rgrek5cgcra9w90chptrdn3","send":"3565833ugnot","max_deposit":"","pkg_path":"gno.land/r/gnoland/wugnot","func":"Deposit"}],"fee":{"gas_wanted":"323200000","gas_fee":"3232000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"Ai2p3VJENxkhPniXTEBJwS3SbF1qubLgMnxBsZ1aFbjR"},"signature":"5yjfkW/X207qsb6C3HsX9X3QEARKVztXg0+SwXtG+aNrHPVQ8JM9YFcSKw5trvYuYc7eQCeC+vL/ZOf+SOc/Sg=="}],"memo":""},"metadata":{"timestamp":"1788750328"}}
{"tx":{"msg":[{"@type":"/vm.m_call","caller":"g1tmtw39s4rcvk6rdhnfmu74nyyw0tdtrec4tk90","send":"1640408ugnot","max_deposit":"","pkg_path":"gno.land/r/gnoland/wugnot","func":"Deposit"}],"fee":{"gas_wanted":"384000000","gas_fee":"3840000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"A3C74nVYEb8pCI/W5aF7Qrjm3LmbuBWJbKn7Z0NMLc0/"},"signature":"7w/bX0ttj07bk1ayJMs0hWQBAyi5udF25PSRUKtf6qR0WGKOovww9L2H6qjaLHju/3gMxzxE3xAmuvNNzlErlw=="}],"memo":""},"metadata":{"timestamp":"1788750354"}}
{"tx":{"msg":[{"@type":"/vm.m_call","caller":"g1tmtw39s4rcvk6rdhnfmu74nyyw0tdtrec4tk90","send":"","max_deposit":"","pkg_path":"gno.land/r/gnoland/wugnot","func":"Withdraw","args":["4885825"]}],"fee":{"gas_wanted":"320000000","gas_fee":"3200000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"A3C74nVYEb8pCI/W5aF7Qrjm3LmbuBWJbKn7Z0NMLc0/"},"signature":"sw8wEVHcXQStj/+dNrd+qPYMlisoSoplAETW9foQdsUOoezLsA3DN+y1B4sHlo0LubM/vdPnvljyL2cbAmCitg=="}],"memo":""},"metadata":{"timestamp":"1788750364"}}
{"tx":{"msg":[{"@type":"/bank.MsgSend","from_address":"g18qhq2fl54lszhmxeyqlvxnwjzc3xpu4nnakclp","to_address":"g1gvfqa7t9qpx9fgt0t4m0v7t0jwkmfs0unq0g4z","amount":"300000000ugnot"}],"fee":{"gas_wanted":"5000000","gas_fee":"1000000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"AhUuKSwiL4XKy0BM9cK0UJFToerTzvgLTHFBvYk/xOtY"},"signature":"QGmxPBKjdV8BxvOH5WFPuUHOVsDn59gS+IbpnQOznDB8+sEeMVONFJzSElVR8njTZfZAl2niJLRPtQ+ldTjnYw=="}],"memo":""},"metadata":{"timestamp":"1788750364"}}
{"tx":{"msg":[{"@type":"/bank.MsgSend","from_address":"g18qhq2fl54lszhmxeyqlvxnwjzc3xpu4nnakclp","to_address":"g1h98ucsv37zhsn8sfulzvzkacqfkfvw5n46t9s2","amount":"300000000ugnot"}],"fee":{"gas_wanted":"5000000","gas_fee":"1000000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"AhUuKSwiL4XKy0BM9cK0UJFToerTzvgLTHFBvYk/xOtY"},"signature":"mA7ulk1gRWZsmsWKbH4yEPwL7HbeH3AcPwVlrWUM5nRO48Yk/o82cMBuY3dDZzpRXAT8+bM5hJ3Yb/fWthrWpw=="}],"memo":""},"metadata":{"timestamp":"1788750416"}}
{"tx":{"msg":[{"@type":"/bank.MsgSend","from_address":"g18qhq2fl54lszhmxeyqlvxnwjzc3xpu4nnakclp","to_address":"g1qnghmez83a7rucp0anhnamstfyd8dffaa40wn5","amount":"300000000ugnot"}],"fee":{"gas_wanted":"5000000","gas_fee":"1000000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"AhUuKSwiL4XKy0BM9cK0UJFToerTzvgLTHFBvYk/xOtY"},"signature":"m2JdBFBycCfaTVjdW3qA5vrSLptb4M+ChAzoa4cRs9klSzFnHaqsDvvLSYkVgcZoQXug8l+BqSOt57QUEzdbqQ=="}],"memo":""},"metadata":{"timestamp":"1788750438"}}
{"tx":{"msg":[{"@type":"/vm.m_call","caller":"g1g2hs53eq6jhvckggj6a2jx6yxzlj8v87n370g8","send":"","max_deposit":"","pkg_path":"gno.land/r/gnoland/wugnot","func":"Withdraw","args":["10597656"]}],"fee":{"gas_wanted":"396800000","gas_fee":"3968000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"A/e+lN5oU0IREE0xqGDuVxUW/DD5e+yW4ieNvMrJufoX"},"signature":"GYVgOrrphzvkYJwadvuduZjRg5y91+/NKMOBsdGdHuV4SjiDRd6+P+AAYjWNZqXJ2aCjBIvYikKZTYE2tbYwvg=="}],"memo":""},"metadata":{"timestamp":"1788750441"}}
{"tx":{"msg":[{"@type":"/bank.MsgSend","from_address":"g18qhq2fl54lszhmxeyqlvxnwjzc3xpu4nnakclp","to_address":"g10fex8hlq405un3hk7nq4w70jphxhgcmgjq89na","amount":"300000000ugnot"}],"fee":{"gas_wanted":"5000000","gas_fee":"1000000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"AhUuKSwiL4XKy0BM9cK0UJFToerTzvgLTHFBvYk/xOtY"},"signature":"lSDZS5kYb7XlXuf0wCmzvHgyGVyEsydOwqOO8RQXQzQhu2s8MR7LihXbslLvs43pCDjYRQHvz/1ifFG0Mq+23Q=="}],"memo":""},"metadata":{"timestamp":"1788750462"}}
{"tx":{"msg":[{"@type":"/vm.m_call","caller":"g1qyuk8e7flf9nwxs7f43mft5jc0srmc6fmtne8j","send":"1701752ugnot","max_deposit":"","pkg_path":"gno.land/r/gnoland/wugnot","func":"Deposit"}],"fee":{"gas_wanted":"393600000","gas_fee":"3936000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"AygAzns5pyzUyR+fqmu7MffykERMOibtXb+rjDwJ8okI"},"signature":"Dmxh2vXN8yIUN2zqVrXrJoZOdGgl4IM2jE1eDC0r+IFi8c+U5+ZRb2qhEf8iwkbzbAjnzGQmDlPeRYR4ZsXBXg=="}],"memo":""},"metadata":{"timestamp":"1788750523"}}
{"tx":{"msg":[{"@type":"/vm.m_call","caller":"g1ypq5d2kqf6c6q47m5spvs349nwkjzmd9q2prjz","send":"","max_deposit":"","pkg_path":"gno.land/r/gnoland/wugnot","func":"Withdraw","args":["8303418"]}],"fee":{"gas_wanted":"348800000","gas_fee":"3488000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"A5F5p0vP0deIJFiEQ4QmtIcA9dibBU32Q00xCirIrqj7"},"signature":"Ybuc25+P5JqfXLow7wPkErlPi9xvzztHXWstJMFT8Xtth12DFL91NF7dHZ4js/EVshkUGdDnVRv57R53YM67Yw=="}],"memo":""},"metadata":{"timestamp":"1788750551"}}
{"tx":{"msg":[{"@type":"/vm.m_call","caller":"g14qhqmm8rz5sl25exs9x0jn8up49hj5ullwhqt9","send":"1103946ugnot","max_deposit":"","pkg_path":"gno.land/r/gnoland/wugnot","func":"Deposit"}],"fee":{"gas_wanted":"364800000","gas_fee":"3648000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"AynmhZZj/Cn6OGQZcSBahYgezeTFOMRf44cjFXG6/UE0"},"signature":"yGzyfKnbLEEJuv7CL0UgORh5cvfkinpesOBL1LFtuOoCWxsgam/g2vw2oVt0Q8tjc+Bm2MiJel1Mn7QJUphcYQ=="}],"memo":""},"metadata":{"timestamp":"1788750558"}}
{"tx":{"msg":[{"@type":"/vm.m_addpkg","creator":"g1xr6tgxnpled50h74eafmvxway7z0ytr5rsmeme","package":{"name":"nslogic","path":"gno.land/r/g1xr6tgxnpled50h74eafmvxway7z0ytr5rsmeme/nslogic/v5","files":[{"name":"admin.gno","body":"package nslogic\n\nimport (\n\t\"strconv\"\n\t\"strings\"\n\n\t\"gno.land/r/g1xr6tgxnpled50h74eafmvxway7z0ytr5rsmeme/nsdata/v2\"\n)\n\n// assertIsAdmin reads nsdata.GetAdmin() fresh every call — correct even\n// immediately after a TransferAdmin, and correct for a freshly-deployed\n// logic realm with no local state of its own to go stale.\n//\n// Note: admin authority to swap WHICH realm is trustedLogic, or to\n// transfer admin itself, is deliberately NOT exposed here — those live\n// only in nsdata's own admin.gno, callable solely by a direct wallet\n// call from the current admin address, never proxied through logic.\n// That's the one part of the system a compromised or buggy logic realm\n// cannot touch.\nfunc assertIsAdmin(cur realm) {\n\tif cur.Previous().Address() != nsdata.GetAdmin() {\n\t\tpanic(\"nslogic: restricted to project admin\")\n\t}\n}\n\n// SetLabelPattern no longer changes anything, and refuses anything but\n// the rule this realm actually implements.\n//\n// It used to compile an arbitrary regex and store it, with validateLabel\n// reading it back. That flexibility cost 15 GNOT of permanent storage\n// per deploy — see the measurements in validate.gno — for a rule that\n// has never changed and should not change casually.\n//\n// It is kept rather than deleted so that an admin who reaches for it\n// gets told what happened instead of a \"function not found\". And it\n// refuses a different pattern rather than accepting one it will not\n// enforce: a stored string describing rules the code does not apply is\n// the exact silent divergence the caching version was written to avoid.\nfunc SetLabelPattern(cur realm, pattern string) {\n\tassertNoSend(cur)\n\tassertIsAdmin(cur)\n\tif pattern != labelRule {\n\t\tpanic(\"nslogic: the label rule is fixed in this realm (\" + labelRule +\n\t\t\t\"). Changing it means deploying a new logic realm — which is deliberate, \" +\n\t\t\t\"because a stored pattern the code does not enforce is worse than no setting at all.\")\n\t}\n\tnsdata.SetLabelPatternRecord(cross(cur), pattern)\n}\n\nfunc SetLabelLengthLimits(cur realm, minLen, maxLen int) {\n\tassertNoSend(cur)\n\tassertIsAdmin(cur)\n\tif minLen \u003c 1 || maxLen \u003c minLen {\n\t\tpanic(\"nslogic: invalid length bounds\")\n\t}\n\tnsdata.SetLabelLengthRecord(cross(cur), minLen, maxLen)\n}\n\n// SetDefaultNamePrice sets the fallback price in micro-USD, used by any\n// domain that has not set its own base.\nfunc SetDefaultNamePrice(cur realm, microUsd int64) {\n\tassertNoSend(cur)\n\tassertIsAdmin(cur)\n\tif microUsd \u003c 0 {\n\t\tpanic(\"nslogic: price cannot be negative\")\n\t}\n\tsetGlobal(cur, keyDefaultPrice, strconv.FormatInt(microUsd, 10))\n}\n\n// SetUsdRate records how many ugnot one USD buys — the bridge between\n// the USD price list and settlement on this chain.\n//\n// It is a MANUAL rate, not a market feed: it sits where it was last\n// written. A stale rate during a sharp move sells names at the wrong\n// price, and no amount of contract logic fixes that — keeping it current\n// is an operational duty. Isolated as one global so a future logic realm\n// can swap in a real oracle without touching the vault or restating a\n// single price.\nfunc SetUsdRate(cur realm, ugnotPerUsd int64) {\n\tassertNoSend(cur)\n\tassertIsAdmin(cur)\n\tif ugnotPerUsd \u003c= 0 {\n\t\tpanic(\"nslogic: rate must be positive\")\n\t}\n\tsetGlobal(cur, keyRate, strconv.FormatInt(ugnotPerUsd, 10))\n}\n\n// SetTermLength / SetGracePeriod retune the lifecycle itself. Both were\n// hardcoded constants in the vault until 2026-08-21 — meaning the 1-year\n// term and 90-day grace could never have been changed, on a realm that\n// is never redeployed. There was no reason to accept that.\n// maxLifecycleDays bounds both setters. time.Duration is int64\n// NANOSECONDS, so `days * 24h` overflows above ~106,751 days and wraps to\n// a NEGATIVE duration — which would install a negative term and brick\n// every registration and renewal in the registry. Guarding the input is\n// what keeps the multiplication in range.\nconst maxLifecycleDays = 36500 // 100 years\n\nfunc SetTermLength(cur realm, days int64) {\n\tassertNoSend(cur)\n\tassertIsAdmin(cur)\n\tif days \u003c= 0 || days \u003e maxLifecycleDays {\n\t\tpanic(\"nslogic: term out of range (1..36500 days)\")\n\t}\n\tterm := days * 24 * 60 * 60\n\t// Keep gracePeriod \u003c= termLength. Anchoring a renewal to the previous\n\t// due date is only coherent while a renewal cannot happen more than\n\t// one full term after it; the vault clamps forward defensively, but\n\t// the combination should not be reachable in the first place.\n\tif nsdata.GetGracePeriod() \u003e term {\n\t\tpanic(\"nslogic: term must be at least the current grace period\")\n\t}\n\tnsdata.SetTermLengthRecord(cross(cur), term)\n}\n\nfunc SetGracePeriod(cur realm, days int64) {\n\tassertNoSend(cur)\n\tassertIsAdmin(cur)\n\tif days \u003c 0 || days \u003e maxLifecycleDays {\n\t\tpanic(\"nslogic: grace period out of range (0..36500 days)\")\n\t}\n\tgrace := days * 24 * 60 * 60\n\tif grace \u003e nsdata.GetTermLength() {\n\t\tpanic(\"nslogic: grace period may not exceed the term length\")\n\t}\n\tnsdata.SetGracePeriodRecord(cross(cur), grace)\n}\n\n// SetTokenURIBase re-points TokenURI at a different renderer, so the NFT\n// artwork can be replaced without touching the vault.\nfunc SetTokenURIBase(cur realm, base string) {\n\tassertNoSend(cur)\n\tassertIsAdmin(cur)\n\tnsdata.SetTokenURIBaseRecord(cross(cur), base)\n}\n\nfunc SetExtraLimits(cur realm, keyLen, valueLen, entries int) {\n\tassertNoSend(cur)\n\tassertIsAdmin(cur)\n\tif keyLen \u003c 1 || valueLen \u003c 1 || entries \u003c 1 {\n\t\tpanic(\"nslogic: limits must be positive\")\n\t}\n\tnsdata.SetExtraLimitsRecord(cross(cur), keyLen, valueLen, entries)\n}\n\n/*\nSetMarketFee is nsmarket's fee control, living here because this is the\nrealm the vault trusts.\n\nnsmarket.SetFee could never work: it wrote through nsdata.SetGlobalRecord\nfrom a realm deliberately outside the trusted set, so it passed its own\nadmin check and then panicked inside the vault for every caller. Rather\nthan grant nsmarket vault privileges — which would surrender the property\nthat realm is built around — the setter moved to the realm that already\nhas the grant, and the validation moved with it.\n\nThe spec grammar is nsmarket's, restated here rather than imported so\nthis realm does not take a dependency on the marketplace to set a number:\nan amount in ugnot (\"1000000\"), or a percentage (\"%2.5\"), or \"\" for none.\nA malformed spec would otherwise be read on every sale and resolve\nsilently to zero.\n*/\nfunc SetMarketFee(cur realm, spec string) {\n\tassertNoSend(cur)\n\tassertIsAdmin(cur)\n\tassertValidFeeSpec(spec)\n\tsetGlobal(cur, \"marketfee\", strings.TrimSpace(spec))\n}\n\n// assertValidFeeSpec mirrors nsmarket.parseFee. Percentages are carried\n// as basis points so a fee is expressible without floating point\n// anywhere near money.\nfunc assertValidFeeSpec(spec string) {\n\tspec = strings.TrimSpace(spec)\n\tif spec == \"\" {\n\t\treturn\n\t}\n\tif !strings.HasPrefix(spec, \"%\") {\n\t\tn, err := strconv.ParseInt(spec, 10, 64)\n\t\tif err != nil || n \u003c 0 {\n\t\t\tpanic(\"nslogic: a fee is an amount in ugnot or a percentage like %2.5 — got \" + spec)\n\t\t}\n\t\treturn\n\t}\n\tbody := spec[1:]\n\twhole, frac := body, \"\"\n\tif dot := strings.Index(body, \".\"); dot \u003e= 0 {\n\t\twhole, frac = body[:dot], body[dot+1:]\n\t}\n\t// ONE decimal, matching nsmarket.parseFee exactly. I wrote two here\n\t// first, which made this validator MORE permissive than the parser\n\t// that has to read the value back — and a spec nsmarket refuses is\n\t// read as a zero fee on every sale, silently, forever. A validator\n\t// looser than its consumer is worse than no validator: it converts a\n\t// rejected input into a silent revenue leak.\n\tif len(frac) \u003e 1 {\n\t\tpanic(\"nslogic: a fee percentage carries at most one decimal — got \" + spec)\n\t}\n\tfor _, part := range []string{whole, frac} {\n\t\tfor i := 0; i \u003c len(part); i++ {\n\t\t\tif part[i] \u003c '0' || part[i] \u003e '9' {\n\t\t\t\tpanic(\"nslogic: a fee percentage must be digits — got \" + spec)\n\t\t\t}\n\t\t}\n\t}\n\tif whole == \"\" \u0026\u0026 frac == \"\" {\n\t\tpanic(\"nslogic: a fee percentage needs a number — got \" + spec)\n\t}\n\t// Basis points the same way parseFee computes them: whole*100 plus\n\t// one tenth-digit worth ten. Not two digits, and not a 100% ceiling\n\t// — nsmarket caps at maxFeeBps, which is 50%.\n\tw, _ := strconv.ParseInt(\"0\"+whole, 10, 64)\n\tbp := w * 100\n\tif frac != \"\" {\n\t\tf, _ := strconv.ParseInt(\"0\"+frac, 10, 64)\n\t\tbp += f * 10\n\t}\n\tif bp \u003c= 0 || bp \u003e 5000 {\n\t\tpanic(\"nslogic: a fee percentage must be above zero and at most 50 — got \" + spec)\n\t}\n}\n\n// SetGlobal writes an arbitrary project-wide key into the vault's\n// package-level extension slot — the escape hatch for global state that\n// does not exist yet.\nfunc SetGlobal(cur realm, key, value string) {\n\tassertNoSend(cur)\n\tassertIsAdmin(cur)\n\tsetGlobal(cur, key, value)\n}\n\nfunc setGlobal(cur realm, key, value string) {\n\t/* THE RESTORE SEAL IS NOT AN ORDINARY GLOBAL.\n\t   restore.gno says sealing is \"IRREVERSIBLE… There is no argument\n\t   that unseals it\", and RestoreGlobal carries a reserved-key guard\n\t   to make that true. This function did not, so one admin call\n\t   writing key \"restore\" reopened the seal and the claim in that file\n\t   was simply false. The admin key is trusted and already holds\n\t   worse powers, so this is documentation drift rather than an\n\t   escalation — but a permanent-sounding promise that a two-word call\n\t   undoes is the kind of thing an auditor and a court both notice.\n\t   The durable version of this guard belongs in the vault, where a\n\t   logic swap cannot drop it; this is the half that ships today. */\n\tif key == keyRestore || key == keyRestoredCount {\n\t\tpanic(\"nslogic: \" + key + \" is written by the restore machinery, not by SetGlobal\")\n\t}\n\tif err := nsdata.SetGlobalRecord(cross(cur), key, value); err != nil {\n\t\tpanic(err)\n\t}\n}\n\n// SetTreasury moves the revenue-receiving role — independent of\n// nsdata's TransferAdmin, same reasoning as the original build's\n// admin/treasury separation.\nfunc SetTreasury(cur realm, newTreasury address) {\n\tassertNoSend(cur)\n\tassertIsAdmin(cur)\n\tif !newTreasury.IsValid() {\n\t\tpanic(\"nslogic: invalid treasury address\")\n\t}\n\tnsdata.SetTreasuryRecord(cross(cur), newTreasury)\n}\n\n// SelfAddress reports this deployed realm's own address — needed once,\n// at wiring time, so admin knows what to pass to\n// nsdata.ProposeTrustedLogicGrant.\nfunc SelfAddress(cur realm) address {\n\treturn cur.Address()\n}\n"},{"name":"blocked.gno","body":"package nslogic\n\nimport (\n\t\"strings\"\n\n\t\"gno.land/r/g1xr6tgxnpled50h74eafmvxway7z0ytr5rsmeme/nsdata/v2\"\n)\n\n/*\nTHE BLOCK LIST: labels nobody may hold, at any price.\n\nThis is NOT the reserved list, and confusing the two would undo both.\n\n\treserved (collections/reserved-labels.json, 232 labels) says\n\t\"blocked from the free tier, buyable at list price like anything\n\telse\". admin*meme being purchasable is deliberate: the harm it\n\tguards against is handing a phishing address out for nothing, not\n\tthe existence of the name. That list is enforced by the issuer,\n\twhich is the only place that decides what is free.\n\n\tTHIS list says \"not available, full stop\". It is the small set where\n\tthe answer does not change when somebody offers money: names that\n\timpersonate the registry itself, and the registry's own names.\n\nWHY IT LIVES IN THE VAULT'S GLOBALS RATHER THAN IN THIS FILE.\n\nBaking it in would mean a logic deploy to block a word, and the words\nworth blocking are exactly the ones discovered after launch — the label\nsomebody tried yesterday that nobody thought of. Globals are writable by\nthe admin, survive a logic swap because they belong to the vault, and\ncost storage only for what is actually stored.\n\nChunked because a global value is capped at 512 bytes. Eight chunks is\nabout four kilobytes, or several hundred labels, which is far more than\nthis list should ever need — if it grows past that, the thing being\nbuilt is a content policy and it belongs off chain where it can be\nupdated without a transaction.\n\nFAILS OPEN, and that is the uncomfortable choice, so it is written down.\nAn unreadable chunk blocks nothing rather than blocking everything.\nFailing closed here would mean one malformed global takes the whole\nregistry offline — every registration, for everyone, until an admin\nnotices. The harm from a blocked label slipping through is one bad name\nthat an admin can freeze; the harm from failing closed is total. The\nlist is also not the only defence: the issuer holds the reserved list,\nand FreezeName exists for anything that gets past both.\n*/\n\nconst (\n\tblockChunks   = 8\n\tblockKeyStem  = \"block\"\n\tblockChunkMax = 512\n)\n\n// blockKey is the global name for one chunk: block0 … block7.\nfunc blockKey(i int) string {\n\treturn blockKeyStem + string(rune('0'+i))\n}\n\n/*\nBlockedLabel reports whether a label may not be registered at all.\n\nCompares against the label as the registry stores it — lowercase, already\nvalidated — so a caller cannot slip past with different casing. Every\ncomparison is exact: this list holds whole labels, not substrings,\nbecause \"contains\" matching turns a list of a dozen words into an\nunpredictable filter that refuses names nobody meant to refuse.\n*/\nfunc BlockedLabel(label string) bool {\n\tif label == \"\" {\n\t\treturn false\n\t}\n\twant := strings.ToLower(label)\n\tfor i := 0; i \u003c blockChunks; i++ {\n\t\tv := nsdata.GetGlobal(blockKey(i))\n\t\tif v == \"\" {\n\t\t\tcontinue\n\t\t}\n\t\tfor _, e := range strings.Split(v, \",\") {\n\t\t\tif strings.ToLower(strings.TrimSpace(e)) == want {\n\t\t\t\treturn true\n\t\t\t}\n\t\t}\n\t}\n\treturn false\n}\n\n// BlockList returns one chunk, so the panel can show and edit what is\n// actually stored rather than what somebody believes is stored.\nfunc BlockList(i int) string {\n\tif i \u003c 0 || i \u003e= blockChunks {\n\t\treturn \"\"\n\t}\n\treturn nsdata.GetGlobal(blockKey(i))\n}\n\n// BlockChunks is the number of chunks, so the panel does not hard-code a\n// constant that this file might later change.\nfunc BlockChunks() int { return blockChunks }\n\n/*\nSetBlockList replaces one chunk. Admin only.\n\nReplaces rather than appends: an append-only list has no way to correct a\nmistake, and a mistake here refuses somebody a name for reasons nobody\ncan see. The panel reads the chunk, edits it, and writes it back whole.\n*/\nfunc SetBlockList(cur realm, i int, csv string) {\n\tassertNoSend(cur)\n\tassertIsAdmin(cur)\n\tif i \u003c 0 || i \u003e= blockChunks {\n\t\tpanic(\"nslogic: chunk out of range\")\n\t}\n\tif len(csv) \u003e blockChunkMax {\n\t\tpanic(\"nslogic: that chunk is longer than a global may be\")\n\t}\n\t// Normalised on the way in, so the comparison above never has to\n\t// account for whatever spacing somebody typed into a form.\n\tparts := strings.Split(csv, \",\")\n\tout := []string{}\n\tfor _, p := range parts {\n\t\tp = strings.ToLower(strings.TrimSpace(p))\n\t\tif p != \"\" {\n\t\t\tout = append(out, p)\n\t\t}\n\t}\n\tsetGlobal(cur, blockKey(i), strings.Join(out, \",\"))\n}\n\n// assertNotBlocked is the gate both doors pass through — the paid\n// registration and the free grant. One function, called twice, because\n// a block that only covers one route is not a block.\nfunc assertNotBlocked(label string) {\n\tif BlockedLabel(label) {\n\t\tpanic(\"nslogic: \" + label + \" is not available\")\n\t}\n}\n"},{"name":"domain.gno","body":"package nslogic\n\nimport (\n\t\"strconv\"\n\t\"strings\"\n\t\"time\"\n\n\t\"gno.land/r/g1xr6tgxnpled50h74eafmvxway7z0ytr5rsmeme/nsdata/v2\"\n)\n\n// RegisterDomain mints a new `*domain` — ADMIN-ONLY (2026-08-15 policy:\n// domains are never sold or owned by third parties). No payment: the\n// project isn't paying itself. Domains are also permanently\n// non-transferable by users (see nft.gno's TransferFrom).\nfunc RegisterDomain(cur realm, label string) {\n\tassertNoSend(cur)\n\tassertIsAdmin(cur)\n\tvalidateLabel(label)\n\tcaller := cur.Previous().Address()\n\tif err := nsdata.RegisterDomainRecord(cross(cur), label, caller, time.Now().Unix()); err != nil {\n\t\tpanic(err)\n\t}\n}\n\n// RenewDomain extends a domain's expiry by exactly one year from its\n// PREVIOUS due date, not from the payment date. No payment — domains are\n// always project-owned, so this is self-service upkeep, not a sale.\n// Deliberately does not call validateLabel — a tightened pattern only\n// gates new registrations, never renewals of what's already registered.\nfunc RenewDomain(cur realm, label string) {\n\tassertNoSend(cur)\n\tcaller := cur.Previous().Address()\n\tif err := nsdata.RenewDomainRecord(cross(cur), label, caller, time.Now().Unix()); err != nil {\n\t\tpanic(err)\n\t}\n}\n\n// ReassignDomain moves a domain record to a new owner. ADMIN-ONLY, and\n// the deliberate exception to \"domains are non-transferable\".\n//\n// It exists because domain ownership is snapshotted into the record at\n// registration time, while `admin` is a rotatable role. After the\n// planned EOA-to-multisig rotation, the incoming admin could not renew\n// or reprice any domain the outgoing key had registered — and if the\n// rotation happened *because* the old key was compromised, the attacker\n// could still reprice those domains afterwards. This is the recovery\n// lever for exactly that situation.\n//\n// Not a loophole in the non-transferability policy: that policy exists\n// to stop domains reaching third parties as tradeable assets, and this\n// path is reachable only by the current admin, moving a project domain\n// between project-controlled addresses.\nfunc ReassignDomain(cur realm, label string, newOwner address) {\n\tassertNoSend(cur)\n\tassertIsAdmin(cur)\n\towner := nsdata.GetDomainOwner(label)\n\n\t/* THE TRANSFER WIPES THE DOMAIN'S CONFIGURATION, so it is read back\n\t   and rewritten around the call.\n\t   RawTransferNFT applies wipeOnHandover, whose long rationale is\n\t   entirely about NAMES — clearing a seller's payment addresses so a\n\t   buyer does not inherit them. A domain's extra slot is not user\n\t   data: it is the domain's settings. price, len, nm, open and\n\t   maxterm all live there, so a deliberately CLOSED domain silently\n\t   reopened, pricing fell back to the global default, and the\n\t   per-domain term cap released to the project default — the brake\n\t   that exists to stop a script hoarding every good name under a free\n\t   domain. Nothing errored and nothing was emitted, and this fires\n\t   during a compromised-key recovery, when the operator is least able\n\t   to notice.\n\t   The durable fix is a vault function that moves the owner and\n\t   leaves extra alone; domains are project-owned and never reach a\n\t   third party, so there is no previous-owner data to protect. This\n\t   is the stopgap that works against a vault already deployed. */\n\tkeep := []string{\"price\", \"len\", \"nm\", \"open\", \"maxterm\"}\n\tsaved := make([]string, len(keep))\n\tfor i, k := range keep {\n\t\tsaved[i] = nsdata.GetDomainExtra(label, k)\n\t}\n\n\tif err := nsdata.RawTransferNFT(cross(cur), owner, owner, newOwner, \"*\"+label); err != nil {\n\t\tpanic(err)\n\t}\n\n\tfor i, k := range keep {\n\t\tif saved[i] == \"\" {\n\t\t\tcontinue // absent before, absent after — writing \"\" would store a blank\n\t\t}\n\t\tif err := nsdata.SetDomainExtraRecord(cross(cur), label, k, saved[i]); err != nil {\n\t\t\tpanic(err)\n\t\t}\n\t}\n}\n\n// -- pricing controls (admin) --\n//\n// Every amount here is MICRO-USD (1 USD = 1_000_000). Nothing is priced\n// in the chain token; ugnot is derived at payment time from the stored\n// rate, so a second settlement token later is a second rate rather than\n// a second price list.\n\n// SetDomainOpen enables or disables registration of new names under a\n// domain. Existing names are untouched: closing a domain stops new\n// mints, it does not seize anything already held.\nfunc SetDomainOpen(cur realm, label string, open bool) {\n\tassertNoSend(cur)\n\tassertIsAdmin(cur)\n\tv := \"1\"\n\tif !open {\n\t\tv = \"0\"\n\t}\n\tsetDomainExtra(cur, label, keyOpen, v)\n}\n\n// SetDomainBasePrice sets the domain's base price in micro-USD. Every\n// \"%N\" rule under the domain is relative to this, so one call reprices\n// the whole domain.\nfunc SetDomainBasePrice(cur realm, label string, microUsd int64) {\n\tassertNoSend(cur)\n\tassertIsAdmin(cur)\n\tif microUsd \u003c 0 {\n\t\tpanic(\"nslogic: price cannot be negative\")\n\t}\n\tsetDomainExtra(cur, label, keyPrice, strconv.FormatInt(microUsd, 10))\n}\n\n// SetDomainLengthRules prices by label length: \"3:2000000,4:%150,5:0\"\n// means 3-char names cost $2.00, 4-char names cost 150% of the domain\n// base, and 5-char names are free.\nfunc SetDomainLengthRules(cur realm, label, rules string) {\n\tassertNoSend(cur)\n\tassertIsAdmin(cur)\n\tsetDomainExtra(cur, label, keyLens, normalizeRules(rules, true))\n}\n\n// SetDomainNameOverrides prices specific labels: \"420:5000000,gm:%500\".\n// Highest precedence — this beats length rules and the base.\nfunc SetDomainNameOverrides(cur realm, label, rules string) {\n\tassertNoSend(cur)\n\tassertIsAdmin(cur)\n\tsetDomainExtra(cur, label, keyNames, normalizeRules(rules, false))\n}\n\n// SetDomainField writes an arbitrary key on a domain, for whatever a\n// future feature needs without a vault change.\nfunc SetDomainField(cur realm, label, key, value string) {\n\tassertNoSend(cur)\n\tassertIsAdmin(cur)\n\tif strings.HasPrefix(key, \"_\") {\n\t\tpanic(\"nslogic: keys beginning with '_' are reserved\")\n\t}\n\tsetDomainExtra(cur, label, key, value)\n}\n\nfunc setDomainExtra(cur realm, label, key, value string) {\n\tif err := nsdata.SetDomainExtraRecord(cross(cur), label, key, value); err != nil {\n\t\tpanic(err)\n\t}\n}\n\n// FreezeDomain / UnfreezeDomain: project-only power to permanently block\n// re-registration of a domain even after its grace period lapses.\n// DeleteDomain destroys a domain and every name beneath it.\n//\n// THE VAULT PRIMITIVE EXISTED BEFORE THIS DID, AND THAT MADE IT\n// UNREACHABLE. nsdata.DeleteDomainRecord gates on assertIsTrustedLogic,\n// so the only way to reach it is a function in this realm — and there\n// was not one. The capability shipped in the vault and could not be\n// used, which is worse than not shipping it, because it reads as\n// present.\n//\n// Bounded by the vault at maxDeletePerCall names per call. It returns\n// how many it removed and whether it finished, so a large domain is\n// several calls rather than a transaction that cannot fit in a block.\n// Repeat until done is true; calling it on a domain that is already gone\n// reports done rather than panicking, so a loop terminates cleanly.\n//\n// ADMIN ONLY, and deliberately not owner-callable: domains are\n// project-owned (SPEC.md §17b) and this destroys other people's names.\nfunc DeleteDomain(cur realm, label string) (removed int64, done bool) {\n\tassertNoSend(cur)\n\tassertIsAdmin(cur)\n\treturn nsdata.DeleteDomainRecord(cross(cur), label)\n}\n\n// SetDomainTTL sets how long a resolver may cache answers about this\n// domain, in seconds, where 0 means never cache — the ENS rule. Admin\n// only, since domains are project-owned.\nfunc SetDomainTTL(cur realm, label string, seconds int64) {\n\tassertNoSend(cur)\n\tassertIsAdmin(cur)\n\tif seconds \u003c 0 {\n\t\tpanic(\"nslogic: ttl may not be negative\")\n\t}\n\tif err := nsdata.SetDomainTTLRecord(cross(cur), label, seconds); err != nil {\n\t\tpanic(err)\n\t}\n}\n\nfunc FreezeDomain(cur realm, label string) {\n\tassertNoSend(cur)\n\tassertIsAdmin(cur)\n\tnsdata.SetDomainFrozen(cross(cur), label, true)\n}\n\nfunc UnfreezeDomain(cur realm, label string) {\n\tassertNoSend(cur)\n\tassertIsAdmin(cur)\n\tnsdata.SetDomainFrozen(cross(cur), label, false)\n}\n"},{"name":"freetier.gno","body":"package nslogic\n\nimport (\n\t\"strconv\"\n\t\"time\"\n\n\t\"gno.land/r/g1xr6tgxnpled50h74eafmvxway7z0ytr5rsmeme/nsdata/v2\"\n)\n\n/*\nTHE FREE TIER: a name somebody can hold without ever owning a wallet.\n\nThe vault has supported this since it was written — RegisterNameRecord\ntakes mintNFT=false and stores a record with no GRC-721 token, and\nPromoteToNFTRecord is described there as \"the paid free-tier upgrade\".\nNothing in the logic realm ever reached either. This is that wiring.\n\nWHY A RECORD WITHOUT A TOKEN IS THE RIGHT SHAPE, and not a compromise:\n\n\tIT IS A REAL NAME. Owned, resolvable, renewable, on chain, visible\n\tto anyone. Not a promise, not a queue position, not a row in our\n\tdatabase. Everything the paid tier does except be sold.\n\n\tIT CANNOT BE SOLD, and that is what makes giving it away safe.\n\tnsmarket escrows a TOKEN, and there is no token — so a squatter who\n\tfarms a thousand of these has farmed a thousand things nobody can\n\tbuy from them. The anti-abuse property falls out of the data model\n\trather than out of a rule somebody has to enforce.\n\n\tIT BECOMES SELLABLE BY BECOMING PAID. UpgradeFreeName takes the full\n\tlist price and mints the token. So the same name costs nothing and\n\tcosts full price, and which one depends entirely on whether you want\n\tto sell it.\n\nWHY THE PROJECT PAYS. The whole point is somebody with no GNOT, and\npossibly no wallet, who is going to put this name in front of an\naudience. Measured on Pearl: a registration is about 0.04 GNOT of fee\nplus a small storage deposit, and a free-tier record is smaller than a\npaid one because there is no token and no ledger entry. That is a\nmarketing cost per name, bounded by a daily cap we apply off chain,\nwhere it can be changed without a deploy.\n\nWHAT IS DELIBERATELY NOT HERE. No wordlist: storage is 100 ugnot a byte,\nso a ten-thousand-word dictionary is about 57 GNOT paid forever to\nencode a marketing decision that will change. The contract carries the\nstructural rules in reserved.gno and nothing else; the lists live off\nchain with the issuer, which is us, because the free path is granted\nrather than bought. See tools/gen-reserved.py.\n*/\n\nconst (\n\tkeyFreeDomain = \"free\"      // per-domain: \"1\" means the free tier is open here\n\tkeyGrantEnd   = \"grantend\"  // global: unix seconds, or \"never\"\n\tkeyGranter    = \"granter\"   // global: the address allowed to issue grants\n)\n\n/*\nTHE GRANTER IS NOT THE ADMIN, and that separation is the point.\n\nFree names are issued by a machine — something has to sign the grant\nafter a person proves an X account, and that key has to sit somewhere\nwarm. If that key were the admin key, a compromise of the issuing\nmachine would be a compromise of the registry: DeleteDomain destroys\nevery name under a domain and burns their tokens, instantly, with no\ntimelock.\n\nSo there is a second role that can do exactly one thing. Worst case for\na stolen granter key is that somebody gives away free names until we\nnotice and clear the role — a bounded, reversible, embarrassing loss\nrather than an unbounded one.\n\nStored in the vault's globals rather than in this realm, so it survives\na logic redeploy. Unset means nobody but the admin may grant, which is\nthe right default for a role that exists to be delegated deliberately.\n*/\nfunc Granter() address {\n\treturn address(nsdata.GetGlobal(keyGranter))\n}\n\n// SetGranter delegates grant issuance, or withdraws it. Passing the\n// empty address is the revocation, and it is the first thing to reach\n// for if the issuing machine is ever in doubt.\nfunc SetGranter(cur realm, a address) {\n\tassertNoSend(cur)\n\tassertIsAdmin(cur)\n\tif a != \"\" \u0026\u0026 !a.IsValid() {\n\t\tpanic(\"nslogic: not an address\")\n\t}\n\tsetGlobal(cur, keyGranter, a.String())\n}\n\n/*\nSetFreeLengths tunes which lengths the free tier will issue.\n\nIn the panel rather than in a redeploy: nslogic is about 22 GNOT to\ndeploy, and \"should four-character names be free this month\" is a\nmarketing question that should cost a signature, not a deployment.\n\nIT DOES NOT REACH BACKWARDS. Raising the minimum stops NEW issuance of\nshort names; every one already granted stays granted and keeps renewing\nfree. FreeLabelAllowed is called by GrantFreeName and by nothing else,\nwhich is what makes that true — see the note on it in reserved.gno.\n*/\nfunc SetFreeLengths(cur realm, min, max int) {\n\tassertNoSend(cur)\n\tassertIsAdmin(cur)\n\tif min \u003c 1 || max \u003e 63 || min \u003e max {\n\t\tpanic(\"nslogic: a free-tier length range must sit inside 1..63 and be the right way round\")\n\t}\n\tsetGlobal(cur, keyFreeMin, strconv.Itoa(min))\n\tsetGlobal(cur, keyFreeMax, strconv.Itoa(max))\n}\n\nfunc assertMayGrant(cur realm) {\n\tcaller := cur.Previous().Address()\n\tif caller == nsdata.GetAdmin() {\n\t\treturn\n\t}\n\tg := Granter()\n\tif g == \"\" || caller != g {\n\t\tpanic(\"nslogic: only the admin or the granter may issue free names\")\n\t}\n}\n\n// DomainFree reports whether a domain accepts free-tier registrations.\n// Off by default, and per-domain on purpose: the free tier is a\n// promotion aimed at a few domains, not a property of the registry.\nfunc DomainFree(domainLabel string) bool {\n\treturn nsdata.GetDomainExtra(domainLabel, keyFreeDomain) == \"1\"\n}\n\n// SetDomainFree opens or closes the free tier for one domain.\nfunc SetDomainFree(cur realm, domainLabel string, on bool) {\n\tassertNoSend(cur)\n\tassertIsAdmin(cur)\n\tif !nsdata.DomainExists(domainLabel) {\n\t\tpanic(\"nslogic: no such domain\")\n\t}\n\tv := \"\"\n\tif on {\n\t\tv = \"1\"\n\t}\n\tif err := nsdata.SetDomainExtraRecord(cross(cur), domainLabel, keyFreeDomain, v); err != nil {\n\t\tpanic(err)\n\t}\n}\n\n/*\nGrantWindowOpen reports whether free grants are being issued at all.\n\nA single global switch that closes everything at once, because the\nfailure this guards against is not a clever attack — it is noticing on a\nSunday that something is wrong and wanting one call that stops it. An\nunset value means open; \"never\" means open with no end date, which is\nwhat the admin panel already writes.\n*/\nfunc GrantWindowOpen() bool {\n\treturn readGrantWindow(nsdata.GetGlobal(keyGrantEnd), time.Now().Unix())\n}\n\n// readGrantWindow is GrantWindowOpen with the clock and the stored value\n// handed in, so the reading can be tested without a chain.\nfunc readGrantWindow(v string, now int64) bool {\n\tif v == \"\" || v == \"never\" {\n\t\treturn true\n\t}\n\tend, err := strconv.ParseInt(v, 10, 64)\n\tif err != nil {\n\t\t// An unparseable window is a misconfiguration, and the safe\n\t\t// reading of a misconfigured gate is \"closed\". Giving names away\n\t\t// because a setting was mistyped is the wrong way to fail.\n\t\treturn false\n\t}\n\treturn now \u003c end\n}\n\n/*\nGrantFreeName registers a free-tier record to somebody else.\n\nADMIN OR GRANTER, and that is the whole trust model: the eligibility rules —\nwhich Twitter account, how old, how many followers, whether the label is\na dictionary word — live off chain with the issuer. Putting them here\nwould freeze a marketing policy into a realm and cost storage to do it.\nWhat the chain enforces is the part that must not depend on our\ncorrectness: the label is structurally allowed, the domain opted in, and\nthe window is open.\n\n`owner` may be an address the recipient generated in their browser\nseconds ago and has never funded. That is the point.\n*/\nfunc GrantFreeName(cur realm, label, domainLabel string, owner address) {\n\tassertNoSend(cur)\n\tassertMayGrant(cur)\n\n\tif !GrantWindowOpen() {\n\t\tpanic(\"nslogic: free grants are closed\")\n\t}\n\tif !DomainFree(domainLabel) {\n\t\tpanic(\"nslogic: *\" + domainLabel + \" does not offer free names\")\n\t}\n\tif !DomainOpen(domainLabel) {\n\t\tpanic(\"nslogic: this domain is not accepting new names\")\n\t}\n\tvalidateLabel(label)\n\tassertNotBlocked(label)\n\t// The structural backstop. The issuer has already applied the full\n\t// policy; this is what stops a bug there giving away a four-letter\n\t// name or a pure-numeric one.\n\tif !FreeLabelAllowed(label) {\n\t\tpanic(\"nslogic: \" + label + \" is not available on the free tier\")\n\t}\n\tif !owner.IsValid() {\n\t\tpanic(\"nslogic: a grant needs a recipient\")\n\t}\n\n\t// mintNFT=false — a record, not a token. See the note at the top.\n\tif err := nsdata.RegisterNameRecord(cross(cur), label, domainLabel, owner,\n\t\ttime.Now().Unix(), false); err != nil {\n\t\tpanic(err)\n\t}\n}\n\n/*\nUpgradeFreeName mints the token for a free-tier record, at full price.\n\nThis is the only way a free name becomes sellable, and the price is the\nordinary list price with no referral discount — a discount is for\nbringing somebody new, and this person is already here.\n\n`newOwner` exists because the common case is somebody who has decided to\nsell and, in the same moment, wants the name in a wallet they actually\ncontrol rather than the browser key they were given. The vault handles\nthat move inside PromoteToNFTRecord.\n\nCallable by the record's current owner only. Anyone may PAY for\nsomebody else's upgrade in the sense that they can hand over the money,\nbut they cannot perform it — a stranger promoting your name into a token\nand choosing where it lands is a theft with a receipt.\n*/\nfunc UpgradeFreeName(cur realm, label, domainLabel string, newOwner address) {\n\tcaller := cur.Previous().Address()\n\tif nsdata.GetNameOwner(label, domainLabel) != caller {\n\t\tpanic(\"nslogic: restricted to name owner\")\n\t}\n\tif !nsdata.IsNameValid(label, domainLabel) {\n\t\tpanic(\"nslogic: this name has expired\")\n\t}\n\tif newOwner == \"\" {\n\t\tnewOwner = caller\n\t}\n\tif !newOwner.IsValid() {\n\t\tpanic(\"nslogic: invalid destination address\")\n\t}\n\n\t// Full list price, resolved now rather than at grant time — the\n\t// name's worth is what it is worth today, and a free name held for a\n\t// year should not be upgradable at last year's rate.\n\ttakePaymentSplit(cur, NamePrice(domainLabel, label), \"\", 0)\n\n\tif err := nsdata.PromoteToNFTRecord(cross(cur), label, domainLabel, newOwner); err != nil {\n\t\tpanic(err)\n\t}\n}\n\n/*\nGrantFreeRenewal extends a free-tier name at no charge.\n\nTHE DEAL IS THE DISPLAY, and it is checked off chain: the issuer signs\nsomebody in on X, reads their display name, and finds the name in it.\nThat single act proves both halves at once — that a real account still\nexists, and that it is still carrying the name — which is why renewal is\none click a year rather than a form.\n\nFREE-TIER RECORDS ONLY. A paid name renewed for nothing is revenue given\naway by accident, and the two are told apart by whether a token was ever\nminted. isNFT is the flag, and it is the vault's, not ours.\n\nNO OWNERSHIP PROOF IS ASKED FOR, and that is deliberate rather than\nmissing. Renewing somebody else's name is a GIFT — it takes nothing,\nmoves nothing, and grants the caller no power over it. The only cost of\nallowing it is ours, and the vault's renewal cap already bounds how far\nany single name can be pushed. A proof requirement here would buy\nnothing and would cost the one-click renewal that makes the deal work.\n\nRenewNameRecord insists the caller is the owner, so the owner is looked\nup and passed. That is not a bypass: this realm is the trusted logic,\nand what it is asserting is exactly true — this renewal is on that\nowner's behalf.\n*/\nfunc GrantFreeRenewal(cur realm, label, domainLabel string) {\n\tassertNoSend(cur)\n\tassertMayGrant(cur)\n\n\tif !nsdata.NameExists(label, domainLabel) {\n\t\tpanic(\"nslogic: no such name\")\n\t}\n\towner, _, _, _, _, isNFT := nsdata.GetNameInfo(label, domainLabel)\n\tif isNFT {\n\t\tpanic(\"nslogic: \" + label + \"*\" + domainLabel +\n\t\t\t\" is a paid name — free renewal is for free-tier records\")\n\t}\n\tif RenewalsLeft(label, domainLabel) \u003c= 0 {\n\t\tpanic(\"nslogic: this name is already paid as far ahead as a name may run\")\n\t}\n\tif err := nsdata.RenewNameRecord(cross(cur), label, domainLabel, owner,\n\t\ttime.Now().Unix()); err != nil {\n\t\tpanic(err)\n\t}\n}\n\n// IsFreeTier reports whether a name is a record without a token — the\n// free tier. Exported because the site and the issuer both need to ask\n// before offering a free renewal, and neither should infer it.\nfunc IsFreeTier(label, domainLabel string) bool {\n\tif !nsdata.NameExists(label, domainLabel) {\n\t\treturn false\n\t}\n\t_, _, _, _, _, isNFT := nsdata.GetNameInfo(label, domainLabel)\n\treturn !isNFT\n}\n"},{"name":"gnomod.toml","body":"module = \"gno.land/r/g1xr6tgxnpled50h74eafmvxway7z0ytr5rsmeme/nslogic/v5\"\ngno = \"0.9\"\n"},{"name":"holdings.gno","body":"package nslogic\n\nimport (\n\t\"strings\"\n\n\t\"gno.land/p/nt/ufmt/v0\"\n\t\"gno.land/r/g1xr6tgxnpled50h74eafmvxway7z0ytr5rsmeme/nsdata/v2\"\n)\n\n// Answering \"what does this address hold\" in ONE read.\n//\n// The site used to ask the chain for every name in the REGISTRY and then\n// throw away the ones that did not match — one HTTP round trip per name\n// in the world to draw one person's collection. Six names took three\n// seconds, and it gets linearly worse for everybody every time anybody\n// else registers anything.\n//\n// The scan itself is unavoidable: nsdata keys names by domain*label and\n// has no index by owner, and nsdata is the permanent vault so no index\n// can be added to it now. But the scan does not have to happen across\n// the network. nslogic is swappable and can walk the same records inside\n// a single query, which turns N round trips into one.\n//\n// Nothing is cached and nothing is trusted from the caller: every row is\n// read from the vault at the moment of the query, exactly as before.\n//\n// Paged, because a query has a gas ceiling of its own and a registry\n// with fifty thousand names in it must not become one enormous read that\n// nobody can complete. `after` is the vault's own cursor, so paging here\n// is the same paging ExportNames already does.\nconst maxHoldingsPage = 200\n\n// NamesOwnedBy returns rows of `label*domain,registered,expires,streak,frozen`\n// separated by \";\", plus the cursor to continue from. Labels are\n// restricted to [a-z0-9-] so neither separator can appear inside a field.\n//\n// `next` is empty when the scan has reached the end of the registry. It\n// is NOT empty merely because this page found no matches: a page of two\n// hundred names belonging to other people returns no rows and a cursor,\n// and the caller keeps going.\nfunc NamesOwnedBy(owner address, after string, limit int) (rows, next string) {\n\tif limit \u003c= 0 || limit \u003e maxHoldingsPage {\n\t\tlimit = maxHoldingsPage\n\t}\n\tkeys, nxt := nsdata.ExportNames(\"\", after, limit)\n\n\tvar b strings.Builder\n\tfor _, key := range strings.Split(keys, \",\") {\n\t\tif key == \"\" {\n\t\t\tcontinue\n\t\t}\n\t\t// The tree key is domain*label; the token ID is label*domain.\n\t\tat := strings.Index(key, \"*\")\n\t\tif at \u003c 0 {\n\t\t\tcontinue\n\t\t}\n\t\tdomainLabel, label := key[:at], key[at+1:]\n\n\t\to, registered, expires, streak, frozen, _ := nsdata.GetNameInfo(label, domainLabel)\n\t\tif o != owner {\n\t\t\tcontinue\n\t\t}\n\t\tif b.Len() \u003e 0 {\n\t\t\tb.WriteString(\";\")\n\t\t}\n\t\tb.WriteString(ufmt.Sprintf(\"%s*%s,%d,%d,%d,%t\",\n\t\t\tlabel, domainLabel, registered, expires, streak, frozen))\n\t}\n\treturn b.String(), nxt\n}\n\n// CountNamesOwnedBy is the same walk without the strings, for a header\n// that wants a number before the list has finished arriving.\nfunc CountNamesOwnedBy(owner address, after string, limit int) (n int, next string) {\n\tif limit \u003c= 0 || limit \u003e maxHoldingsPage {\n\t\tlimit = maxHoldingsPage\n\t}\n\tkeys, nxt := nsdata.ExportNames(\"\", after, limit)\n\tfor _, key := range strings.Split(keys, \",\") {\n\t\tif key == \"\" {\n\t\t\tcontinue\n\t\t}\n\t\tat := strings.Index(key, \"*\")\n\t\tif at \u003c 0 {\n\t\t\tcontinue\n\t\t}\n\t\tif nsdata.GetNameOwner(key[at+1:], key[:at]) == owner {\n\t\t\tn++\n\t\t}\n\t}\n\treturn n, nxt\n}\n\n// Everything the profile page draws, in ONE read.\n//\n// It was making seventeen queries: three to establish that the name\n// exists and when it expires, then fourteen more — one per extension\n// slot — that could not start until the first three had come back. Two\n// waves, and the node serves only a few at a time, so a profile took\n// seconds to assemble out of values that all live in the same record.\n//\n// Values come back in the ORDER THE KEYS WERE ASKED FOR rather than as\n// key=value pairs. An extension key is allowed to contain \"=\" (only\n// commas and newlines are forbidden), so a pair encoding would have an\n// ambiguous split; positional has none.\n//\n// Line 0 is the record: owner, registered, expires, streak, frozen, and\n// the grace period, which is global but is one more query the page would\n// otherwise have to make on its own.\n//\n// An empty return means the name is not registered. That is a real\n// answer rather than an error: asking about a name nobody owns is the\n// normal case on a profile URL somebody typed.\nfunc ProfileOf(label, domainLabel, keys string) string {\n\tif !nsdata.NameExists(label, domainLabel) {\n\t\treturn \"\"\n\t}\n\towner, registered, expires, streak, frozen, _ := nsdata.GetNameInfo(label, domainLabel)\n\n\tvar b strings.Builder\n\tb.WriteString(ufmt.Sprintf(\"%s,%d,%d,%d,%t,%d\",\n\t\towner.String(), registered, expires, streak, frozen, nsdata.GetGracePeriod()))\n\n\tfor _, k := range strings.Split(keys, \",\") {\n\t\tb.WriteString(\"\\n\")\n\t\tif k == \"\" {\n\t\t\tcontinue\n\t\t}\n\t\tv := nsdata.GetNameExtra(label, domainLabel, k)\n\n\t\t// THE GNO.LAND ADDRESS ANSWERS EVEN WHEN NOBODY SAVED ONE.\n\t\t//\n\t\t// A name is an NFT held by an address, so the owner is already\n\t\t// public — it is the first field of the record above. Returning\n\t\t// it here as well costs no storage and means a name can be paid\n\t\t// without its holder ever having opened a profile editor, which\n\t\t// is most of them, and without a transaction to back-fill the\n\t\t// ones registered before this existed.\n\t\t//\n\t\t// Doing it in the realm rather than in the website is the whole\n\t\t// point: any client asking this realm for the gno key gets the\n\t\t// same answer our pages do. A front end that derived it locally\n\t\t// would leave every other reader with nothing.\n\t\t//\n\t\t// \"off\" is the opt-out, and it has to be a sentinel. An empty\n\t\t// value is indistinguishable from never having set one, so an\n\t\t// empty gno field would simply hand the derived address back on\n\t\t// the next read.\n\t\tif k == \"gno\" {\n\t\t\tif v == \"off\" {\n\t\t\t\tv = \"\"\n\t\t\t} else if v == \"\" {\n\t\t\t\tv = owner.String()\n\t\t\t}\n\t\t}\n\t\tb.WriteString(escapeValue(v))\n\t}\n\treturn b.String()\n}\n\n// Extension VALUES are free text — a bio can hold anything a person can\n// type, newlines included — so they cannot be newline-separated raw.\n// Escaping is done here rather than stripping, because silently eating a\n// line break out of somebody's bio is data loss on a read path.\nfunc escapeValue(s string) string {\n\ts = strings.ReplaceAll(s, `\\`, `\\\\`)\n\ts = strings.ReplaceAll(s, \"\\n\", `\\n`)\n\treturn strings.ReplaceAll(s, \"\\r\", `\\r`)\n}\n\n// RecentNames answers \"what has just been registered\" in one read.\n//\n// The site used to build this by listing every key and then reading each\n// record one at a time — the same shape NamesOwnedBy replaced for a\n// wallet's holdings, and the same problem: fine at thirteen names, one\n// query per name forever after. A feed that gets slower every time\n// somebody registers is a feed that eventually stops loading on the busy\n// day you most wanted it.\n//\n// Returns rows of \"label*domain,registered,expires,owner\", newest first,\n// and a cursor. Sorting happens across a PAGE, not the whole registry:\n// the vault stores names alphabetically and there is no index by date,\n// so a caller that wants the true newest reads every page and merges.\n// That is one query per 200 names instead of one per name — enough of a\n// difference to stop mattering, without pretending the vault has an\n// index it does not have.\nfunc RecentNames(after string, limit int) (rows, next string) {\n\tif limit \u003c= 0 || limit \u003e maxHoldingsPage {\n\t\tlimit = maxHoldingsPage\n\t}\n\tkeys, nxt := nsdata.ExportNames(\"\", after, limit)\n\n\ttype row struct {\n\t\ttext string\n\t\tat   int64\n\t}\n\tvar found []row\n\tfor _, key := range strings.Split(keys, \",\") {\n\t\tif key == \"\" {\n\t\t\tcontinue\n\t\t}\n\t\t// The tree key is domain*label; the token ID is label*domain.\n\t\tat := strings.Index(key, \"*\")\n\t\tif at \u003c 1 {\n\t\t\tcontinue\n\t\t}\n\t\tdomainLabel, label := key[:at], key[at+1:]\n\t\towner, registered, expires, _, _, _ := nsdata.GetNameInfo(label, domainLabel)\n\t\tfound = append(found, row{\n\t\t\ttext: ufmt.Sprintf(\"%s*%s,%d,%d,%s\", label, domainLabel, registered, expires, owner.String()),\n\t\t\tat:   registered,\n\t\t})\n\t}\n\n\t// Insertion sort, newest first. The page is at most 200 entries and\n\t// gno has no sort in scope here; anything cleverer would be more code\n\t// than the problem deserves.\n\tfor i := 1; i \u003c len(found); i++ {\n\t\tv := found[i]\n\t\tj := i - 1\n\t\tfor j \u003e= 0 \u0026\u0026 found[j].at \u003c v.at {\n\t\t\tfound[j+1] = found[j]\n\t\t\tj--\n\t\t}\n\t\tfound[j+1] = v\n\t}\n\n\tvar b strings.Builder\n\tfor _, r := range found {\n\t\tif b.Len() \u003e 0 {\n\t\t\tb.WriteString(\";\")\n\t\t}\n\t\tb.WriteString(r.text)\n\t}\n\treturn b.String(), nxt\n}\n"},{"name":"name.gno","body":"package nslogic\n\nimport (\n\t\"strings\"\n\t\"time\"\n\n\t\"gno.land/r/g1xr6tgxnpled50h74eafmvxway7z0ytr5rsmeme/nsdata/v2\"\n)\n\n// Profile field names inside a name's extension slot. The vault stores\n// them as opaque strings and bounds their size; what they MEAN is this\n// realm's convention, so new profile fields need no vault change.\nconst (\n\tkeyDisplayName = \"displayName\"\n\tkeyBio         = \"bio\"\n\tkeyWebsite     = \"website\"\n\tkeyAvatar      = \"avatar\"\n)\n\n// RegisterName mints `label*domain` under an existing domain. Anyone may\n// call this. Minting still works while the parent domain is past its own\n// due date, as long as its grace period hasn't fully lapsed and it isn't\n// frozen — nsdata's RegisterNameRecord enforces that.\nfunc RegisterName(cur realm, label, domainLabel string) {\n\tregisterName(cur, label, domainLabel, \"\")\n}\n\n// RegisterNameRef is RegisterName with a referral code — the NAME of\n// whoever sent the buyer here, as it appears in the link.\n//\n// It is a second entry point rather than a third argument on the first\n// one because the first one is already deployed and already being called\n// by wallets and by the site. Changing its arity would break every one\n// of those callers at the same instant, which is exactly the failure the\n// voting realm produced last time; adding a function breaks nothing and\n// lets the front end pick whichever the deployed realm actually has.\n//\n// A code that does not resolve — misspelt, lapsed, or the buyer's own\n// name — is IGNORED, not rejected. The registration goes through at\n// list price. Refusing to sell somebody a name because they mistyped\n// somebody else's would be an absurd way to lose a sale.\nfunc RegisterNameRef(cur realm, label, domainLabel, refName string) {\n\tregisterName(cur, label, domainLabel, refName)\n}\n\nfunc registerName(cur realm, label, domainLabel, refName string) {\n\tvalidateLabel(label)\n\tassertNotBlocked(label)\n\tif !DomainOpen(domainLabel) {\n\t\tpanic(\"nslogic: this domain is not accepting new names\")\n\t}\n\tcaller := cur.Previous().Address()\n\n\t// Resolved BEFORE anything is written, so the tier and the discount\n\t// are read from the world the buyer was quoted in. Registering first\n\t// would change the buyer's own primary name and, on a self-referral\n\t// that slipped through, their tenure with it.\n\tref := resolveReferrer(refName, caller)\n\tprice := NamePrice(domainLabel, label)\n\tif ref != \"\" {\n\t\tif pct := tierPercent(RefDiscountRules(), TenureYears(ref)); pct \u003e 0 {\n\t\t\tprice -= price * pct / 100\n\t\t}\n\t}\n\tpayTo, cut := referralCut(ref, price)\n\ttakePaymentSplit(cur, price, payTo, cut)\n\n\tif err := nsdata.RegisterNameRecord(cross(cur), label, domainLabel, caller, time.Now().Unix(), true); err != nil {\n\t\tpanic(err)\n\t}\n\n\t// Written after the record exists, because the extension slot is\n\t// created by registration. Only recorded when the cut was actually\n\t// paid: a referrer who earned nothing on the sale has no claim on\n\t// the renewals either, and storing them would say otherwise.\n\tif ref != \"\" \u0026\u0026 cut \u003e 0 {\n\t\tsetExtra(cur, label, domainLabel, keyRef, ref.String())\n\t\tcreditReferral(cur, ref)\n\t}\n}\n\n// RenewName — same anchoring rule as RenewDomain: extends from the\n// previous due date, not from the payment date. Deliberately does not\n// call validateLabel — an already-registered name must keep renewing\n// under whatever pattern was in force when it was minted.\nfunc RenewName(cur realm, label, domainLabel string) {\n\t// No new code is accepted here and none is needed: the referrer was\n\t// fixed into this name when it was registered, and renewals keep\n\t// paying them for as long as the name survives unbroken. If it\n\t// lapses or is sold the vault destroys the record's extras, so the\n\t// link ends without this realm having to notice.\n\t//\n\t// Renewals pay the FULL price. The discount was a reason to arrive;\n\t// it is not a permanent lower rate, and compounding it every year\n\t// would make the referred name cheaper forever than the same name\n\t// bought directly.\n\tassertRoomToRenew(label, domainLabel)\n\tprice := NamePrice(domainLabel, label)\n\tref := ReferrerOf(label, domainLabel)\n\tpayTo, cut := referralCut(ref, price)\n\ttakePaymentSplit(cur, price, payTo, cut)\n\n\tcaller := cur.Previous().Address()\n\tif err := nsdata.RenewNameRecord(cross(cur), label, domainLabel, caller, time.Now().Unix()); err != nil {\n\t\tpanic(err)\n\t}\n\tif cut \u003e 0 {\n\t\tcreditReferral(cur, ref)\n\t}\n}\n\n// SetProfile writes the four conventional profile fields. Each lands in\n// the name's extension slot, where the vault enforces size bounds.\nfunc SetProfile(cur realm, label, domainLabel, displayName, bio, website, avatar string) {\n\tassertNoSend(cur)\n\tassertNameOwner(cur, label, domainLabel)\n\tsetExtra(cur, label, domainLabel, keyDisplayName, displayName)\n\tsetExtra(cur, label, domainLabel, keyBio, bio)\n\tsetExtra(cur, label, domainLabel, keyWebsite, website)\n\tsetExtra(cur, label, domainLabel, keyAvatar, avatar)\n}\n\n// SetNameField writes a single arbitrary key — cross-chain addresses and\n// anything else a future feature needs, with no vault change required.\n// Keys used by this realm's own machinery are reserved.\nfunc SetNameField(cur realm, label, domainLabel, key, value string) {\n\tassertNoSend(cur)\n\tassertNameOwner(cur, label, domainLabel)\n\tif strings.HasPrefix(key, \"_\") {\n\t\tpanic(\"nslogic: keys beginning with '_' are reserved\")\n\t}\n\t// `ref` decides who gets paid out of every renewal of this name. A\n\t// holder who could write it would set it to an address they control\n\t// and take a permanent cut of their own renewals out of the\n\t// treasury — so the one key that moves money is the one key its\n\t// owner may not touch.\n\tif key == keyRef {\n\t\tpanic(\"nslogic: '\" + keyRef + \"' is set at registration and cannot be edited\")\n\t}\n\tsetExtra(cur, label, domainLabel, key, value)\n}\n\nfunc setExtra(cur realm, label, domainLabel, key, value string) {\n\tif err := nsdata.SetNameExtraRecord(cross(cur), label, domainLabel, key, value); err != nil {\n\t\tpanic(err)\n\t}\n}\n\nfunc assertNameOwner(cur realm, label, domainLabel string) {\n\tif nsdata.GetNameOwner(label, domainLabel) != cur.Previous().Address() {\n\t\tpanic(\"nslogic: restricted to name owner\")\n\t}\n}\n\n// SetPrimaryName lets a wallet explicitly choose (and later change) which\n// of its names is used for reverse resolution.\nfunc SetPrimaryName(cur realm, label, domainLabel string) {\n\tassertNoSend(cur)\n\tcaller := cur.Previous().Address()\n\tif nsdata.GetNameOwner(label, domainLabel) != caller {\n\t\tpanic(\"nslogic: restricted to name owner\")\n\t}\n\tif err := nsdata.SetPrimaryRecord(cross(cur), caller, label, domainLabel); err != nil {\n\t\tpanic(err)\n\t}\n}\n\n/*\nClearPrimaryName switches reverse resolution OFF for the caller.\n\nWHY THIS HAS TO EXIST, and it is a hole rather than a feature request.\n\nnsdata sets the reverse pointer AUTOMATICALLY on a wallet's first\nregistration — RegisterNameRecord does it, and nothing asked the person\nfirst. From that moment their address answers with their name, so anybody\nwho sees that address anywhere can find the persona attached to it, and\nevery transaction it has ever made becomes attributable. Retroactively:\nthe chain is permanent and indexers keep copies.\n\nAnd until now there was NO WAY OUT. ClearPrimaryRecord is gated to\ntrusted logic, and no logic realm exposed it, so the one person the\npointer is about could not remove it. A name service is a bridge between\na persona and a wallet; publishing that bridge without consent and\nwithout an exit is the worst version of it.\n\nThis is the exit. It takes no arguments on purpose: a wallet has exactly\none reverse pointer, and the only address it can clear is its own, so\nthere is nothing to pass and nothing to get wrong.\n\nWHAT IT DOES NOT DO, said plainly because a promise we cannot keep would\nbe worse than the hole: clearing stops FUTURE lookups. It does not\nunpublish what was already visible, and it cannot — the history is on\nchain and other people have copies. The honest sequence is to choose\nbefore publishing, which is why the site now shows what the link exposes\nbefore it offers to make it.\n*/\nfunc ClearPrimaryName(cur realm) {\n\tassertNoSend(cur)\n\tnsdata.ClearPrimaryRecord(cross(cur), cur.Previous().Address())\n}\n\n// SetNameTTL is the owner's cache policy for their own name, in\n// seconds, where 0 MEANS NEVER CACHE — copied from the ENS registry so\n// integrators need not read anything of ours to handle it.\n//\n// The owner sets it rather than the registry because only they know the\n// risk: a name being used to receive payments wants 0, since a cached\n// answer after a sale pays the previous owner; a name used as a display\n// handle can safely be cached for a long time.\nfunc SetNameTTL(cur realm, label, domainLabel string, seconds int64) {\n\tassertNoSend(cur)\n\tassertNameOwner(cur, label, domainLabel)\n\tif seconds \u003c 0 {\n\t\tpanic(\"nslogic: ttl may not be negative\")\n\t}\n\tif err := nsdata.SetNameTTLRecord(cross(cur), label, domainLabel, seconds); err != nil {\n\t\tpanic(err)\n\t}\n}\n\nfunc FreezeName(cur realm, label, domainLabel string) {\n\tassertNoSend(cur)\n\tassertIsAdmin(cur)\n\tnsdata.SetNameFrozen(cross(cur), label, domainLabel, true)\n}\n\nfunc UnfreezeName(cur realm, label, domainLabel string) {\n\tassertNoSend(cur)\n\tassertIsAdmin(cur)\n\tnsdata.SetNameFrozen(cross(cur), label, domainLabel, false)\n}\n\n// -- resolution --\n\nfunc ResolveName(label, domainLabel string) address {\n\tif !nsdata.IsNameValid(label, domainLabel) {\n\t\tpanic(\"nslogic: name is expired\")\n\t}\n\treturn nsdata.GetNameOwner(label, domainLabel)\n}\n\n// ResolveAddress performs reverse resolution: address -\u003e name.\n//\n// It must verify the ownership it is asserting, not merely that the\n// pointed-at name exists and is unexpired. nsdata's `primary` tree is a\n// record of what an address last SELECTED, not proof of what it still\n// owns — and although nsdata now clears the pointer on transfer and\n// reclaim, this check is the half that lives in the swappable realm and\n// therefore stays correct even against pointers written by an older\n// version of the logic. Without it, an address that sold or lost a name\n// kept reverse-resolving to it, impersonating the new owner.\nfunc ResolveAddress(addr address) string {\n\ttid := nsdata.GetPrimary(addr)\n\tif tid == \"\" {\n\t\treturn \"\"\n\t}\n\tlabel, domainLabel := splitTokenID(tid)\n\tif !nsdata.IsNameValid(label, domainLabel) {\n\t\treturn \"\"\n\t}\n\tif nsdata.GetNameOwner(label, domainLabel) != addr {\n\t\treturn \"\"\n\t}\n\treturn tid\n}\n\n// splitTokenID splits a `label*domain` token ID.\nfunc splitTokenID(tid string) (label, domainLabel string) {\n\tfor i := 0; i \u003c len(tid); i++ {\n\t\tif tid[i] == '*' {\n\t\t\treturn tid[:i], tid[i+1:]\n\t\t}\n\t}\n\treturn tid, \"\"\n}\n"},{"name":"nft.gno","body":"package nslogic\n\nimport (\n\t\"strings\"\n\n\t\"gno.land/r/g1xr6tgxnpled50h74eafmvxway7z0ytr5rsmeme/nsdata/v2\"\n)\n\n// This realm only exposes the WRITE side of the GRC-721 surface. Reads\n// (BalanceOf, OwnerOf, TokenURI, GetApproved, IsApprovedForAll, and\n// every listing/status/profile query) have no authorization check to\n// perform and are served directly by nsdata — no reason to duplicate\n// them here.\n\nfunc isDomainToken(tid string) bool {\n\treturn strings.HasPrefix(tid, \"*\")\n}\n\n// isTokenValid checks the underlying domain/name's expiry via nsdata's\n// own validity reads — the anti-scam pattern (an expired name must not\n// be sellable) enforced at the transfer boundary, not just at read time.\nfunc isTokenValid(tid string) bool {\n\tif isDomainToken(tid) {\n\t\treturn nsdata.IsDomainValid(tid[1:])\n\t}\n\tlabel, domainLabel := splitTokenID(tid)\n\treturn nsdata.IsNameValid(label, domainLabel)\n}\n\nfunc Approve(cur realm, to address, tid string) {\n\tassertNoSend(cur)\n\tif isDomainToken(tid) {\n\t\tpanic(\"nslogic: domains are project-owned and non-transferable\")\n\t}\n\tif !isTokenValid(tid) {\n\t\tpanic(\"nslogic: cannot approve an expired token\")\n\t}\n\tcaller := cur.Previous().Address()\n\tif err := nsdata.RawApprove(cross(cur), caller, to, tid); err != nil {\n\t\tpanic(err)\n\t}\n}\n\nfunc SetApprovalForAll(cur realm, operator address, approved bool) {\n\tassertNoSend(cur)\n\tcaller := cur.Previous().Address()\n\tif err := nsdata.RawSetApprovalForAll(cross(cur), caller, operator, approved); err != nil {\n\t\tpanic(err)\n\t}\n}\n\n// TransferFrom / SafeTransferFrom are the actual enforcement point for\n// both (a) the anti-scam pattern — blocking transfer of an expired token\n// closes off third-party marketplace sales without blocking the owner's\n// own ability to reclaim by renewing (renewal never goes through these)\n// — and (b) the domains-are-never-transferable policy: blocking it here,\n// not just at RegisterDomain, is what actually closes the loophole.\n// (ReassignDomain in domain.gno is the deliberate admin-only exception;\n// see its comment.)\nfunc TransferFrom(cur realm, from, to address, tid string) {\n\tassertNoSend(cur)\n\tif isDomainToken(tid) {\n\t\tpanic(\"nslogic: domains are project-owned and non-transferable\")\n\t}\n\tif !isTokenValid(tid) {\n\t\tpanic(\"nslogic: cannot transfer an expired token\")\n\t}\n\tlabel, domainLabel := splitTokenID(tid)\n\tcaller := cur.Previous().Address()\n\t// A free-tier record has no GRC-721 token to move, so the token-layer\n\t// transfer would fail; the vault exposes a record-only path for it.\n\tif !nsdata.IsNameNFT(label, domainLabel) {\n\t\tif caller != from || nsdata.GetNameOwner(label, domainLabel) != caller {\n\t\t\tpanic(\"nslogic: restricted to name owner\")\n\t\t}\n\t\tif err := nsdata.TransferRecordOwner(cross(cur), label, domainLabel, to); err != nil {\n\t\t\tpanic(err)\n\t\t}\n\t\treturn\n\t}\n\tif err := nsdata.RawTransferNFT(cross(cur), caller, from, to, tid); err != nil {\n\t\tpanic(err)\n\t}\n}\n\n// nsdata only exposes one raw transfer primitive (RawTransferNFT, backed\n// by grc721's plain TransferFrom) — the safe-receiver-interface check\n// grc721's SafeTransferFrom adds doesn't carry much weight for a\n// name-service NFT, so both entry points converge on the same path here.\nfunc SafeTransferFrom(cur realm, from, to address, tid string) {\n\tTransferFrom(cur, from, to, tid)\n}\n"},{"name":"payment.gno","body":"package nslogic\n\nimport (\n\t\"chain\"\n\t\"chain/banker\"\n\t\"chain/runtime/unsafe\"\n\n\t\"gno.land/p/nt/ufmt/v0\"\n\t\"gno.land/r/g1xr6tgxnpled50h74eafmvxway7z0ytr5rsmeme/nsdata/v2\"\n)\n\n// takePayment verifies the attached send-envelope covers `price` ugnot,\n// forwards the price to the treasury, and refunds any overpayment to the\n// caller in the same transaction. Nothing is ever retained: every ugnot\n// that arrives leaves again, as either payment or refund.\n//\n// It used to take a `beneficiary` and split the price fee/remainder,\n// routing the remainder to treasury only while `beneficiary == admin`.\n// That comparison was a proxy for \"is this a project-owned domain\", and\n// it silently broke the moment `admin` rotated — the planned EOA-to-\n// multisig step — because a domain's owner is frozen into its record at\n// registration time. After a rotation, every registration's remainder\n// would have been paid to the retired key instead of the treasury, with\n// no error and no log line.\n//\n// The heuristic is gone rather than patched. Domains are admin-only to\n// create and permanently non-transferable, so every domain is\n// project-owned by construction and 100% of every payment belongs to the\n// treasury. `platformFeeBps` stays in nsdata's config for a future logic\n// realm that reintroduces third-party domain ownership; reinstating the\n// split is then a logic-realm swap, not a migration.\nfunc takePayment(cur realm, price int64) {\n\ttakePaymentSplit(cur, price, \"\", 0)\n}\n\n// takePaymentSplit is takePayment with a referrer's cut carved out of the\n// same price. The buyer pays exactly what they were quoted; the cut is\n// not added on top, it is a share of what the treasury would otherwise\n// have received.\n//\n// All three legs settle inside the buyer's own transaction — treasury,\n// referrer, refund — so this realm's balance is the same after the call\n// as before it. That is not a stylistic preference: holding a referrer's\n// cut until they claim it would make this an account you can have a\n// balance in, and that is a different kind of thing to operate, subject\n// to a different set of rules, run by people with licences. The cut is\n// paid now or it is not paid.\nfunc takePaymentSplit(cur realm, price int64, cutTo address, cut int64) {\n\tif !cur.Previous().IsUserCall() {\n\t\tpanic(\"nslogic: only direct wallet calls may attach payment\")\n\t}\n\tcaller := cur.Previous().Address()\n\tsent := unsafe.OriginSend()\n\tassertOnlyUgnot(sent)\n\n\tpaid := sent.AmountOf(ugnot)\n\tif paid \u003c price {\n\t\tpanic(ufmt.Sprintf(\"nslogic: insufficient payment: need %d ugnot, got %d\", price, paid))\n\t}\n\texcess := paid - price\n\n\t// Belt and braces. referralCut already caps the percentage, but this\n\t// is the line where the arithmetic becomes money leaving an address,\n\t// and a bug upstream must not be able to pay out more than came in.\n\tif cut \u003c 0 || cut \u003e price || cutTo == \"\" {\n\t\tcut = 0\n\t}\n\n\tb := banker.NewBanker(banker.BankerTypeRealmSend, cur)\n\tpkgAddr := cur.Address()\n\tif cut \u003e 0 {\n\t\tb.SendCoins(pkgAddr, cutTo, chain.Coins{chain.NewCoin(ugnot, cut)})\n\t}\n\tif price-cut \u003e 0 {\n\t\tb.SendCoins(pkgAddr, nsdata.GetTreasury(), chain.Coins{chain.NewCoin(ugnot, price-cut)})\n\t}\n\tif excess \u003e 0 {\n\t\tb.SendCoins(pkgAddr, caller, chain.Coins{chain.NewCoin(ugnot, excess)})\n\t}\n}\n\n// assertOnlyUgnot rejects any other denomination outright. Only the\n// ugnot amount was ever accounted for, and neither realm has a\n// withdrawal path, so a foreign coin sent alongside a payment would have\n// been stranded in this realm's address permanently. Refusing the whole\n// transaction is simpler than sweeping it back and keeps the\n// \"this realm never retains funds\" invariant absolute.\nfunc assertOnlyUgnot(sent chain.Coins) {\n\tfor _, c := range sent {\n\t\tif c.Denom != ugnot {\n\t\t\tpanic(\"nslogic: only ugnot is accepted, got: \" + c.Denom)\n\t\t}\n\t}\n}\n\n// assertNoSend guards every entry point that has no price. Without it,\n// coins attached to a free call (a renewal of a project domain, a\n// profile edit, an admin setter) would land in this realm's address with\n// no way back out.\n//\n// IT APPLIES TO DIRECT WALLET CALLS ONLY, and that qualification is not\n// a loosening — it is the guard finally matching its own reason.\n//\n// OriginSend() reports what the TRANSACTION carried, not what this call\n// received. Measured on sapphire-1 with a pair of probe realms: a user\n// attaches 500000ugnot to realm A, A calls realm B, and B's OriginSend()\n// reads 500000ugnot even though every one of those coins is sitting in\n// A's address. B never saw them and cannot strand them.\n//\n// So for a realm-to-realm call this was rejecting a payment this realm\n// never received. It is the reason a marketplace could not transfer a\n// name inside the same transaction that pays for it — the transfer is a\n// free call, the transaction is not, and the two are unrelated. A\n// two-transaction sale is the alternative, and that means somebody's\n// money or somebody's name sitting somewhere in between. Not here.\nfunc assertNoSend(cur realm) {\n\tif !cur.Previous().IsUserCall() {\n\t\treturn // the coins went to whoever called us, not to us\n\t}\n\tif !unsafe.OriginSend().IsZero() {\n\t\tpanic(\"nslogic: this function takes no payment; do not attach coins\")\n\t}\n}\n"},{"name":"premium.gno","body":"package nslogic\n\nimport (\n\t\"strconv\"\n\t\"strings\"\n\n\t\"gno.land/r/g1xr6tgxnpled50h74eafmvxway7z0ytr5rsmeme/nsdata/v2\"\n)\n\n// Global pricing layers that sit ACROSS every domain rather than inside\n// one — see SPEC.md §27.\n//\n// Two of them, and they work at different points:\n//\n//   GLOBAL LENGTH RULES are a FLOOR beneath each domain's own length\n//   rules, consulted only when the domain has nothing to say about that\n//   length. \"three-character names cost $5 everywhere\" becomes one entry\n//   instead of an edit to all twenty-seven domains, and a domain that\n//   wants its own answer still overrides it.\n//\n//   GLOBAL NAME PREMIUMS are an ADJUSTMENT applied after the price has\n//   already resolved. Making `og` premium is one entry covering\n//   `og*degen`, `og*meme` and every domain invented later. Deliberately\n//   not the per-name override, which REPLACES a price outright: \"this\n//   name costs exactly X here\" and \"this name is worth more everywhere\"\n//   are different tools and both are worth having.\n//\n// Full order, most specific first:\n//\n//   1. per-name override      (domain \"nm\")\n//   2. per-domain length rule (domain \"len\")\n//   3. global length rule     (global \"glen\")\n//   4. domain base price      (domain \"price\")\n//   5. global default         (global \"defaultPrice\")\n//   then the global name premium modifies the result,\n//   then any referral discount comes off what is left.\nconst (\n\tkeyGlobalLens = \"glen\"    // global length rules\n\tkeyPremium    = \"premium\" // premium1, premium2, ... see below\n\n\t// The vault caps an extension value at maxExtraValueLen (512 bytes),\n\t// which is roughly 40-60 premium entries. The list is therefore split\n\t// across numbered keys rather than pretending one will do; a list\n\t// that silently truncated at the 512th byte would drop exactly the\n\t// entries nobody checked.\n\tmaxPremiumKeys = 8\n)\n\n// GlobalLengthRule returns the global rule for a label length, if any.\nfunc GlobalLengthRule(n int) (string, bool) {\n\treturn lookupRule(nsdata.GetGlobal(keyGlobalLens), strconv.Itoa(n))\n}\n\n// PremiumFor returns the raw modifier for a name across all premium\n// keys, or \"\" when the name is not premium anywhere.\nfunc PremiumFor(label string) string {\n\tfor i := 1; i \u003c= maxPremiumKeys; i++ {\n\t\tcsv := nsdata.GetGlobal(keyPremium + strconv.Itoa(i))\n\t\tif csv == \"\" {\n\t\t\tcontinue\n\t\t}\n\t\tif v, ok := lookupRule(csv, label); ok {\n\t\t\treturn v\n\t\t}\n\t}\n\treturn \"\"\n}\n\n// applyPremium adjusts an already-resolved price.\n//\n// ZERO ALWAYS STAYS ZERO. A fixed \"+$2.00\" would otherwise silently undo\n// a giveaway tier set months earlier, and a free tier that quietly stops\n// being free is worse than never having offered one. This is decided\n// explicitly rather than inherited from the percentage case happening to\n// work out at zero.\n//\n// Forms: \"%5\" / \"+%5\" five percent more · \"-%10\" ten percent less ·\n// \"2.00\" two dollars more · \"-0.25\" twenty-five cents less ·\n// \"=5.00\" exactly five dollars, ignoring everything resolved above.\nfunc applyPremium(mod string, price int64) int64 {\n\tmod = strings.TrimSpace(mod)\n\tif mod == \"\" || price \u003c= 0 {\n\t\treturn price\n\t}\n\n\t// \"=X\" replaces outright. Checked before the sign handling because\n\t// an exact price is not a delta and has no direction.\n\tif strings.HasPrefix(mod, \"=\") {\n\t\tv, ok := parseUSD(mod[1:])\n\t\tif !ok || v \u003c 0 {\n\t\t\treturn price\n\t\t}\n\t\treturn v\n\t}\n\n\tneg := false\n\tswitch {\n\tcase strings.HasPrefix(mod, \"-\"):\n\t\tneg, mod = true, mod[1:]\n\tcase strings.HasPrefix(mod, \"+\"):\n\t\tmod = mod[1:]\n\t}\n\n\tvar delta int64\n\tif strings.HasPrefix(mod, \"%\") {\n\t\tpct, err := strconv.ParseInt(mod[1:], 10, 64)\n\t\tif err != nil || pct \u003c 0 {\n\t\t\treturn price\n\t\t}\n\t\tdelta = price * pct / 100\n\t} else {\n\t\tv, ok := parseUSD(mod)\n\t\tif !ok || v \u003c 0 {\n\t\t\treturn price\n\t\t}\n\t\tdelta = v\n\t}\n\n\tif neg {\n\t\t// A discount may take a name to free, but never below it: a\n\t\t// negative price would be a payout, and this realm never pays\n\t\t// anybody to register.\n\t\tif delta \u003e= price {\n\t\t\treturn 0\n\t\t}\n\t\treturn price - delta\n\t}\n\treturn price + delta\n}\n\n// parseUSD reads a human dollar figure — \"2\", \"2.5\", \"2.50\", \"0.069\" —\n// into micro-USD. Written by hand rather than with floats: a float would\n// make \"0.07\" cost a different number of micro-dollars depending on the\n// platform, and prices must be exact.\nfunc parseUSD(s string) (int64, bool) {\n\ts = strings.TrimSpace(s)\n\tif s == \"\" {\n\t\treturn 0, false\n\t}\n\twhole, frac := s, \"\"\n\tif i := strings.IndexByte(s, '.'); i \u003e= 0 {\n\t\twhole, frac = s[:i], s[i+1:]\n\t}\n\tif whole == \"\" {\n\t\twhole = \"0\"\n\t}\n\tw, err := strconv.ParseInt(whole, 10, 64)\n\tif err != nil || w \u003c 0 {\n\t\treturn 0, false\n\t}\n\t// Pad or truncate the fraction to exactly six digits of micro-USD.\n\tif len(frac) \u003e 6 {\n\t\tfrac = frac[:6]\n\t}\n\tfor len(frac) \u003c 6 {\n\t\tfrac += \"0\"\n\t}\n\tvar f int64\n\tif frac != \"\" {\n\t\tf, err = strconv.ParseInt(frac, 10, 64)\n\t\tif err != nil || f \u003c 0 {\n\t\t\treturn 0, false\n\t\t}\n\t}\n\treturn w*microUSD + f, true\n}\n\n// normalizePremiums validates a premium list before storage. Same\n// reasoning as normalizeRules: a malformed entry must never reach the\n// vault, because every later lookup would carry it.\nfunc normalizePremiums(csv string) string {\n\tif strings.TrimSpace(csv) == \"\" {\n\t\treturn \"\"\n\t}\n\tvar out []string\n\tseen := map[string]bool{}\n\tfor _, pair := range strings.Split(csv, \",\") {\n\t\tpair = strings.TrimSpace(pair)\n\t\tif pair == \"\" {\n\t\t\tcontinue\n\t\t}\n\t\tparts := strings.SplitN(pair, \":\", 2)\n\t\tif len(parts) != 2 {\n\t\t\tpanic(\"nslogic: bad premium, want name:modifier — got \" + pair)\n\t\t}\n\t\tk, v := strings.TrimSpace(parts[0]), strings.TrimSpace(parts[1])\n\t\tif k == \"\" || strings.ContainsAny(k, \"*, \\t\\n\") {\n\t\t\tpanic(\"nslogic: invalid name in premium — \" + k)\n\t\t}\n\t\tif seen[k] {\n\t\t\tpanic(\"nslogic: duplicate premium name — \" + k)\n\t\t}\n\t\tif !validPremiumValue(v) {\n\t\t\tpanic(\"nslogic: bad premium modifier for \" + k + \" — \" + v)\n\t\t}\n\t\tseen[k] = true\n\t\tout = append(out, k+\":\"+v)\n\t}\n\treturn strings.Join(out, \",\")\n}\n\nfunc validPremiumValue(v string) bool {\n\tif v == \"\" {\n\t\treturn false\n\t}\n\tif strings.HasPrefix(v, \"=\") {\n\t\t_, ok := parseUSD(v[1:])\n\t\treturn ok\n\t}\n\tif strings.HasPrefix(v, \"-\") || strings.HasPrefix(v, \"+\") {\n\t\tv = v[1:]\n\t}\n\tif strings.HasPrefix(v, \"%\") {\n\t\tp, err := strconv.ParseInt(v[1:], 10, 64)\n\t\treturn err == nil \u0026\u0026 p \u003e= 0\n\t}\n\t_, ok := parseUSD(v)\n\treturn ok\n}\n"},{"name":"pricing.gno","body":"package nslogic\n\nimport (\n\t\"strconv\"\n\t\"strings\"\n\n\t\"gno.land/r/g1xr6tgxnpled50h74eafmvxway7z0ytr5rsmeme/nsdata/v2\"\n)\n\n// Pricing lives here, not in the vault. The vault stores numbers as\n// opaque strings and has no opinion about what they mean, so the whole\n// model can be replaced by swapping this realm.\n//\n// PRICES ARE DENOMINATED IN USD, held as MICRO-USD (1 USD = 1_000_000)\n// so fractions of a cent are expressible — a name that costs about a\n// gumball needs that. Nothing is stored in ugnot: the chain's token is\n// just the settlement rail, converted at payment time from a stored\n// rate. Accepting a second token later is then only a second rate, not\n// a second price list.\n//\n// PRECEDENCE — most specific wins:\n//\n//   1. per-name override      (keyNames: \"420:5000000,gm:%500\")\n//   2. per-length rule        (keyLens:  \"3:2000000,4:%150,5:0\")\n//   3. GLOBAL length rule     (global keyGlobalLens — see premium.gno)\n//   4. domain base price      (keyPrice: \"500000\"  = $0.50)\n//   5. global default         (global keyDefaultPrice)\n//\n// then the GLOBAL NAME PREMIUM adjusts the result, and a referral\n// discount comes off last. Both live in premium.gno.\n//\n// A rule value is either an absolute micro-USD amount or \"%N\", meaning N\n// percent of that domain's base — so repricing a domain once carries\n// every relative rule under it. \"0\" means free, which is how giveaway\n// tiers are expressed: \"all five-character names under *pleb are free\"\n// is just a length rule resolving to zero.\n//\n// Rules are stored as CSV in ONE key each rather than one key per rule.\n// The vault caps extension keys at 32 bytes and 24 entries per record; a\n// key like \"nm:somelongname\" breaks the first and a few dozen premium\n// names break the second. Two CSV keys sidestep both.\nconst (\n\tkeyPrice        = \"price\" // domain base, micro-USD\n\tkeyLens         = \"len\"   // length rules\n\tkeyNames        = \"nm\"    // per-name overrides\n\tkeyOpen         = \"open\"  // \"0\" closes the domain to new names\n\tkeyDefaultPrice = \"defaultPrice\"\n\tkeyRate         = \"rateUgnotPerUsd\" // ugnot equal to 1 USD\n\n\tmicroUSD          = int64(1_000_000)\n\tfallbackPriceUSD  = int64(500_000) // $0.50\n\t// TESTNET PEG: $0.069 per GNOT, so 1 USD = 1/0.069 GNOT = 14.492753\n\t// GNOT = 14_492_753 ugnot. Deliberately a fixed number while we are on\n\t// a testnet whose token has no market price at all -- a \"real\" rate\n\t// there would be fiction dressed as precision. Replaced with a genuine\n\t// conversion before mainnet; tracked in MAINNET.md.\n\tfallbackRate      = int64(14_492_753)\n\tmaxRules          = 64\n)\n\n// DomainOpen reports whether names may currently be registered under a\n// domain. An absent key means open — a domain created before this\n// existed must not silently close.\nfunc DomainOpen(domainLabel string) bool {\n\treturn nsdata.GetDomainExtra(domainLabel, keyOpen) != \"0\"\n}\n\n// -- USD side --\n\nfunc DefaultPriceUSD() int64 {\n\tif v := nsdata.GetGlobal(keyDefaultPrice); v != \"\" {\n\t\tif p, err := strconv.ParseInt(v, 10, 64); err == nil \u0026\u0026 p \u003e= 0 {\n\t\t\treturn p\n\t\t}\n\t}\n\treturn fallbackPriceUSD\n}\n\nfunc DomainBasePriceUSD(domainLabel string) int64 {\n\tif v := nsdata.GetDomainExtra(domainLabel, keyPrice); v != \"\" {\n\t\tif p, err := strconv.ParseInt(v, 10, 64); err == nil \u0026\u0026 p \u003e= 0 {\n\t\t\treturn p\n\t\t}\n\t}\n\treturn DefaultPriceUSD()\n}\n\n// NamePriceUSD resolves the four inputs in precedence order and returns\n// micro-USD. This is the authoritative price; ugnot is derived from it.\nfunc NamePriceUSD(domainLabel, label string) int64 {\n\treturn applyPremium(PremiumFor(label), resolveBeforePremium(domainLabel, label))\n}\n\n// resolveBeforePremium runs steps 1-5. Separated from NamePriceUSD so\n// the admin panel can show WHY a price is what it is: the figure before\n// the premium and the figure after are both meaningful, and a single\n// function returning only the total makes a wrong price impossible to\n// diagnose.\nfunc resolveBeforePremium(domainLabel, label string) int64 {\n\tbase := DomainBasePriceUSD(domainLabel)\n\tif v, ok := lookupRule(nsdata.GetDomainExtra(domainLabel, keyNames), label); ok {\n\t\treturn applyRule(v, base)\n\t}\n\tif v, ok := lookupRule(nsdata.GetDomainExtra(domainLabel, keyLens), strconv.Itoa(len(label))); ok {\n\t\treturn applyRule(v, base)\n\t}\n\t// The global length rule is a floor, not a replacement: it applies\n\t// only where the domain itself had nothing to say about this length.\n\tif v, ok := GlobalLengthRule(len(label)); ok {\n\t\treturn applyRule(v, base)\n\t}\n\treturn base\n}\n\n// ExplainPriceUSD returns each stage of the resolution, so the control\n// panel can show the arithmetic rather than assert a total. Returns the\n// price before any premium, the premium modifier that applied (empty if\n// none), and the final price.\nfunc ExplainPriceUSD(domainLabel, label string) (beforePremium int64, modifier string, final int64) {\n\tbefore := resolveBeforePremium(domainLabel, label)\n\tmod := PremiumFor(label)\n\treturn before, mod, applyPremium(mod, before)\n}\n\n// -- settlement side --\n\n// RateUgnotPerUsd is how many ugnot one USD buys.\n//\n// This is a MANUALLY MAINTAINED rate, not a market oracle: an admin\n// writes it and it sits there until rewritten. That is a real\n// operational hazard — a stale rate during a sharp move sells names at\n// the wrong price in whichever direction hurts — and the mitigation is\n// keeping it fresh, not anything this code can do. Kept as one global so\n// a future logic realm can swap in a real oracle without touching the\n// vault or restating a single price.\nfunc RateUgnotPerUsd() int64 {\n\tif v := nsdata.GetGlobal(keyRate); v != \"\" {\n\t\tif r, err := strconv.ParseInt(v, 10, 64); err == nil \u0026\u0026 r \u003e 0 {\n\t\t\treturn r\n\t\t}\n\t}\n\treturn fallbackRate\n}\n\n// NamePrice converts the USD price to ugnot for settlement. Rounds UP,\n// so rounding never sells a name below its listed price; a free name\n// stays exactly free.\nfunc NamePrice(domainLabel, label string) int64 {\n\tusd := NamePriceUSD(domainLabel, label)\n\tif usd \u003c= 0 {\n\t\treturn 0\n\t}\n\trate := RateUgnotPerUsd()\n\treturn (usd*rate + microUSD - 1) / microUSD\n}\n\n// -- rule plumbing --\n\nfunc applyRule(v string, base int64) int64 {\n\tif strings.HasPrefix(v, \"%\") {\n\t\tpct, err := strconv.ParseInt(v[1:], 10, 64)\n\t\tif err != nil || pct \u003c 0 {\n\t\t\treturn base\n\t\t}\n\t\treturn base * pct / 100\n\t}\n\tn, err := strconv.ParseInt(v, 10, 64)\n\tif err != nil || n \u003c 0 {\n\t\treturn base\n\t}\n\treturn n\n}\n\nfunc lookupRule(csv, want string) (string, bool) {\n\tif csv == \"\" {\n\t\treturn \"\", false\n\t}\n\tfor _, pair := range strings.Split(csv, \",\") {\n\t\tparts := strings.SplitN(strings.TrimSpace(pair), \":\", 2)\n\t\tif len(parts) != 2 {\n\t\t\tcontinue\n\t\t}\n\t\tif strings.TrimSpace(parts[0]) == want {\n\t\t\treturn strings.TrimSpace(parts[1]), true\n\t\t}\n\t}\n\treturn \"\", false\n}\n\n// normalizeRules validates and canonicalises a rule string before it is\n// stored, so a malformed entry can never reach storage and poison every\n// later lookup.\nfunc normalizeRules(csv string, numericKeys bool) string {\n\tif strings.TrimSpace(csv) == \"\" {\n\t\treturn \"\"\n\t}\n\tvar out []string\n\tseen := map[string]bool{}\n\tfor _, pair := range strings.Split(csv, \",\") {\n\t\tpair = strings.TrimSpace(pair)\n\t\tif pair == \"\" {\n\t\t\tcontinue\n\t\t}\n\t\tparts := strings.SplitN(pair, \":\", 2)\n\t\tif len(parts) != 2 {\n\t\t\tpanic(\"nslogic: bad rule, want key:value — got \" + pair)\n\t\t}\n\t\tk, v := strings.TrimSpace(parts[0]), strings.TrimSpace(parts[1])\n\t\tif k == \"\" {\n\t\t\tpanic(\"nslogic: empty rule key\")\n\t\t}\n\t\tif numericKeys {\n\t\t\tif n, err := strconv.Atoi(k); err != nil || n \u003c= 0 {\n\t\t\t\tpanic(\"nslogic: length must be a positive number — got \" + k)\n\t\t\t}\n\t\t} else if strings.ContainsAny(k, \"*, \\t\\n\") {\n\t\t\tpanic(\"nslogic: invalid name in rule — \" + k)\n\t\t}\n\t\tif strings.HasPrefix(v, \"%\") {\n\t\t\tif p, err := strconv.ParseInt(v[1:], 10, 64); err != nil || p \u003c 0 {\n\t\t\t\tpanic(\"nslogic: bad percentage — \" + v)\n\t\t\t}\n\t\t} else if n, err := strconv.ParseInt(v, 10, 64); err != nil || n \u003c 0 {\n\t\t\tpanic(\"nslogic: bad amount — \" + v)\n\t\t}\n\t\tif seen[k] {\n\t\t\tpanic(\"nslogic: duplicate rule key — \" + k)\n\t\t}\n\t\tseen[k] = true\n\t\tout = append(out, k+\":\"+v)\n\t\tif len(out) \u003e maxRules {\n\t\t\tpanic(\"nslogic: too many rules (max 64)\")\n\t\t}\n\t}\n\treturn strings.Join(out, \",\")\n}\n"},{"name":"referral.gno","body":"package nslogic\n\nimport (\n\t\"strconv\"\n\t\"strings\"\n\n\t\"gno.land/r/g1xr6tgxnpled50h74eafmvxway7z0ytr5rsmeme/nsdata/v2\"\n)\n\n// The referral programme.\n//\n// SINGLE LEVEL, ALWAYS. One buyer, one referrer, one cut, taken out of\n// an actual sale. There is no downline, no override on somebody else's\n// referrals, and no bonus for recruiting other referrers — and there is\n// no code here that could be extended into one without being obvious\n// about it. That is the line between an affiliate programme and the\n// other thing, and this realm stays on the near side of it.\n//\n// WHERE THE LINK LIVES. The referrer is recorded on the REFERRED NAME,\n// in its extension slot, at the moment it is registered. That placement\n// does the work of two rules for free, because nsdata already destroys a\n// name's extras when a lapsed name is re-registered (names.Remove, then\n// a fresh record) and when a name changes hands (wipeOnHandover):\n//\n//\ta name that lapses loses its referrer\n//\ta name that is sold loses its referrer\n//\n// Neither needs code to enforce. Both would need code to PREVENT.\n//\n// WHO GETS PAID. An address, not a name. The referrer's wallet is fixed\n// into the referred name at registration, so they may let their own name\n// go, buy a different one, or hold ten — the commission does not move.\n// What they cannot do is leave: paying out requires the referrer to\n// still hold at least one name, checked at payment time.\n//\n// A SKIPPED PAYOUT IS NOT A DEBT. If the referrer holds nothing when a\n// renewal comes through, the cut is simply not taken — the buyer pays\n// less and the treasury keeps the difference. Nothing accrues, because\n// accruing would mean holding somebody else's money until they qualify,\n// and this realm holds nobody's money at any point.\n//\n// SELF-REFERRAL IS REFUSED. Otherwise the informed apply their own code\n// forever and the cut becomes a silent discount for people who read the\n// source.\n//\n// EVERYTHING SETTLES IN ONE TRANSACTION. Buyer pays, treasury is paid,\n// referrer is paid, overpayment goes back — all inside the message the\n// buyer signed. No escrow, no float, not for a block.\nconst (\n\tkeyRef     = \"ref\"     // on a NAME: the referrer's address\n\tkeyRefCut  = \"refcut\"  // global: tier table, referrer's percentage\n\tkeyRefDisc = \"refdisc\" // global: tier table, buyer's discount\n\tkeyRefStat = \"ref:\"    // global, per address: how many referred\n\tkeyRefWin  = \"refwin\"  // global: days a captured code stays valid\n\n\tmaxRefPercent = 50 // a cut plus a discount above this stops being a business\n)\n\n// -- tier tables --\n\n// A tier table reads like the length rules do: \"0:5,2:8,5:12\" means five\n// percent from the start, eight once the referrer has held a name two\n// years, twelve at five. The highest tier at or below their tenure wins,\n// so gaps are fine and the table never has to be exhaustive. An empty\n// table means the programme pays nothing, which is also how it is\n// switched off.\nfunc tierPercent(spec string, years int64) int64 {\n\tbest := int64(0)\n\tbestAt := int64(-1) // the threshold of the tier `best` came from\n\tfor _, part := range strings.Split(spec, \",\") {\n\t\tkv := strings.SplitN(strings.TrimSpace(part), \":\", 2)\n\t\tif len(kv) != 2 {\n\t\t\tcontinue\n\t\t}\n\t\tat, err1 := strconv.ParseInt(strings.TrimSpace(kv[0]), 10, 64)\n\t\tpct, err2 := strconv.ParseInt(strings.TrimSpace(kv[1]), 10, 64)\n\t\tif err1 != nil || err2 != nil || at \u003c 0 || pct \u003c 0 {\n\t\t\tcontinue\n\t\t}\n\t\tif pct \u003e maxRefPercent {\n\t\t\tpct = maxRefPercent\n\t\t}\n\t\t/* THE HIGHEST TIER THAT APPLIES, not the biggest number in the\n\t\t   table. This compared percentages, so a table where a later\n\t\t   tier pays LESS — the shape a taper needs, and the shape the\n\t\t   buyer-discount table is most likely to take — silently paid\n\t\t   the earlier, larger rate forever. Tracking the tier's\n\t\t   threshold instead makes the table mean what it reads as. */\n\t\tif years \u003e= at \u0026\u0026 at \u003e= bestAt {\n\t\t\tbestAt = at\n\t\t\tbest = pct\n\t\t}\n\t}\n\treturn best\n}\n\nfunc RefCutRules() string      { return nsdata.GetGlobal(keyRefCut) }\nfunc RefDiscountRules() string { return nsdata.GetGlobal(keyRefDisc) }\n\n// RefWindowDays is how long a clicked link keeps counting for. It is\n// enforced by the browser that captured the code, not by this realm —\n// the chain never sees the click, only the registration that quotes a\n// code — so it is stored here purely to be ONE number rather than a\n// constant baked separately into the site, the FAQ and the rules page.\n// Zero means the default; the site treats an absent value the same way.\nfunc RefWindowDays() int64 {\n\td := parseInt64(nsdata.GetGlobal(keyRefWin))\n\tif d \u003c= 0 {\n\t\treturn 30\n\t}\n\treturn d\n}\n\nfunc SetReferralWindow(cur realm, days int64) {\n\tassertNoSend(cur)\n\tassertIsAdmin(cur)\n\tif days \u003c 0 || days \u003e 3650 {\n\t\tpanic(\"nslogic: window must be 0-3650 days\")\n\t}\n\tsetGlobal(cur, keyRefWin, strconv.FormatInt(days, 10))\n}\n\n// ReferralQuote is what a code is worth, answered before anybody signs\n// anything. One call rather than four, because the site needs all of it\n// at once to show a price and every extra round trip is a moment where\n// the number on screen is not the number the chain will charge.\n//\n// A referrer of \"\" means the code did not resolve, and the two\n// percentages are then both zero.\nfunc ReferralQuote(refName string, buyer address) (referrer address, tenure, discountPct, cutPct int64) {\n\tref := resolveReferrer(refName, buyer)\n\tif ref == \"\" {\n\t\treturn \"\", 0, 0, 0\n\t}\n\tyears := TenureYears(ref)\n\tcut := int64(0)\n\t// The cut is zero for a referrer holding nothing, and saying so up\n\t// front is better than showing a percentage that silently will not\n\t// be paid when the transaction lands.\n\tif nsdata.BalanceOf(ref) \u003e 0 {\n\t\tcut = tierPercent(RefCutRules(), years)\n\t}\n\treturn ref, years, tierPercent(RefDiscountRules(), years), cut\n}\n\n// SetReferralRules replaces both tier tables at once, because setting one\n// without the other is how a cut ends up larger than the margin. Passing\n// two empty strings turns the programme off: no discount, no commission,\n// and registrations stop writing a referrer at all.\nfunc SetReferralRules(cur realm, cutSpec, discountSpec string) {\n\tassertNoSend(cur)\n\tassertIsAdmin(cur)\n\tcut := normalizeTiers(cutSpec)\n\tdisc := normalizeTiers(discountSpec)\n\t// The two are paid out of the same list price, so their sum is what\n\t// actually leaves the treasury. Checked at every tenure either table\n\t// mentions rather than only at tier zero, since the tables need not\n\t// step at the same points.\n\tfor _, y := range tierYears(cut, disc) {\n\t\tif tierPercent(cut, y)+tierPercent(disc, y) \u003e maxRefPercent {\n\t\t\tpanic(\"nslogic: cut plus discount exceeds 50% at year \" + strconv.FormatInt(y, 10))\n\t\t}\n\t}\n\tsetGlobal(cur, keyRefCut, cut)\n\tsetGlobal(cur, keyRefDisc, disc)\n}\n\nfunc tierYears(specs ...string) []int64 {\n\tvar out []int64\n\tseen := map[int64]bool{}\n\tfor _, spec := range specs {\n\t\tfor _, part := range strings.Split(spec, \",\") {\n\t\t\tkv := strings.SplitN(strings.TrimSpace(part), \":\", 2)\n\t\t\tif len(kv) != 2 {\n\t\t\t\tcontinue\n\t\t\t}\n\t\t\tif y, err := strconv.ParseInt(strings.TrimSpace(kv[0]), 10, 64); err == nil \u0026\u0026 !seen[y] {\n\t\t\t\tseen[y] = true\n\t\t\t\tout = append(out, y)\n\t\t\t}\n\t\t}\n\t}\n\tif len(out) == 0 {\n\t\tout = append(out, 0)\n\t}\n\treturn out\n}\n\n// normalizeTiers rejects a malformed table before it is stored, for the\n// same reason normalizeRules does: a bad entry that reaches storage is\n// then read silently and wrongly on every sale afterwards.\nfunc normalizeTiers(spec string) string {\n\tif strings.TrimSpace(spec) == \"\" {\n\t\treturn \"\"\n\t}\n\tvar out []string\n\tseen := map[int64]bool{}\n\tfor _, part := range strings.Split(spec, \",\") {\n\t\tpart = strings.TrimSpace(part)\n\t\tif part == \"\" {\n\t\t\tcontinue\n\t\t}\n\t\tkv := strings.SplitN(part, \":\", 2)\n\t\tif len(kv) != 2 {\n\t\t\tpanic(\"nslogic: bad tier, want years:percent — got \" + part)\n\t\t}\n\t\ty, err := strconv.ParseInt(strings.TrimSpace(kv[0]), 10, 64)\n\t\tif err != nil || y \u003c 0 || y \u003e 100 {\n\t\t\tpanic(\"nslogic: tier years must be 0-100 — got \" + kv[0])\n\t\t}\n\t\tp, err := strconv.ParseInt(strings.TrimSpace(kv[1]), 10, 64)\n\t\tif err != nil || p \u003c 0 || p \u003e maxRefPercent {\n\t\t\tpanic(\"nslogic: tier percent must be 0-50 — got \" + kv[1])\n\t\t}\n\t\tif seen[y] {\n\t\t\tpanic(\"nslogic: duplicate tier year — \" + kv[0])\n\t\t}\n\t\tseen[y] = true\n\t\tout = append(out, strconv.FormatInt(y, 10)+\":\"+strconv.FormatInt(p, 10))\n\t\tif len(out) \u003e 16 {\n\t\t\tpanic(\"nslogic: too many tiers (max 16)\")\n\t\t}\n\t}\n\treturn strings.Join(out, \",\")\n}\n\n// -- tenure --\n\n// TenureYears is how long a wallet counts as having been here, measured\n// from its PRIMARY name and nothing else.\n//\n// The honest alternative — the oldest name the wallet holds — means\n// walking the whole registry on every quote, which is a read that gets\n// slower every time somebody registers and eventually stops working.\n// Primary is one lookup and never gets slower. It is also already the\n// wallet's declared identity, it is set automatically at a wallet's first\n// registration, and a holder who wants an older name counted only has to\n// make it their primary. The site says so on the referral page.\n//\n// A lapsed or missing primary is zero years, not an error: tier zero.\nfunc TenureYears(who address) int64 {\n\ttid := nsdata.GetPrimary(who)\n\tat := strings.Index(tid, \"*\")\n\tif at \u003c 1 { // \"\" or \"*domain\" — a domain primary has no name streak\n\t\treturn 0\n\t}\n\tlabel, domainLabel := tid[:at], tid[at+1:]\n\tif !nsdata.NameExists(label, domainLabel) || !nsdata.IsNameValid(label, domainLabel) {\n\t\treturn 0\n\t}\n\t_, _, _, streak, _, _ := nsdata.GetNameInfo(label, domainLabel)\n\treturn streak\n}\n\n// -- where it gets paid --\n\n// Commissions go to the referring wallet. There is no way to redirect\n// them, and that is a decision rather than an omission.\n//\n// There WAS a redirect — SetReferralPayout, \"a forwarding address, not a\n// transferable right\". It does not survive the obvious question: how\n// would this realm tell somebody moving money between their own two\n// wallets from somebody who has sold the stream to a stranger? It\n// cannot. Nothing on chain distinguishes those, and a rule that cannot\n// be enforced is not a rule, it is a sentence in a document.\n//\n// What settled it is that the redirect did not even serve the case it\n// was written for. Calling it required signing from the ORIGINAL wallet,\n// so \"I lost access to my wallet\" was never covered; the only wallet\n// that could redirect was one whose owner could equally have received\n// the money and forwarded it. It gave nothing to the honest case and\n// gave the shape of an assignable income stream to the other one.\n//\n// So the destination is the referrer's own address, fixed. Selling the\n// entitlement now means handing over private keys, which is a deterrent\n// no contract clause could match.\nfunc PayoutFor(who address) address { return who }\n\n// -- the record on a name --\n\n// ReferrerOf reads the wallet credited with a name, or the empty address.\nfunc ReferrerOf(label, domainLabel string) address {\n\tv := nsdata.GetNameExtra(label, domainLabel, keyRef)\n\tif a := address(v); v != \"\" \u0026\u0026 a.IsValid() {\n\t\treturn a\n\t}\n\treturn \"\"\n}\n\n// resolveReferrer turns a claimed referring NAME into the wallet to\n// credit, or the empty address if the claim does not stand up. Every\n// rejection is silent on purpose: a mistyped or self-referral should not\n// fail somebody's registration, it should simply earn nobody anything.\nfunc resolveReferrer(refName string, buyer address) address {\n\tat := strings.Index(refName, \"*\")\n\tif refName == \"\" || at \u003c 1 || at == len(refName)-1 {\n\t\treturn \"\"\n\t}\n\tlabel, domainLabel := refName[:at], refName[at+1:]\n\tif !nsdata.NameExists(label, domainLabel) || !nsdata.IsNameValid(label, domainLabel) {\n\t\treturn \"\" // a lapsed name is not a working code\n\t}\n\towner := nsdata.GetNameOwner(label, domainLabel)\n\tif owner == buyer {\n\t\treturn \"\" // no referring yourself\n\t}\n\treturn owner\n}\n\n// -- the money --\n\n// PriceWithReferral is what a buyer actually pays given the code they\n// arrived with. Exported so the site can display the number the chain\n// will charge instead of computing its own and hoping they agree.\nfunc PriceWithReferral(domainLabel, label, refName string, buyer address) int64 {\n\tprice := NamePrice(domainLabel, label)\n\tif price \u003c= 0 {\n\t\treturn 0 // free is free, and a percentage of nothing is nothing\n\t}\n\tref := resolveReferrer(refName, buyer)\n\tif ref == \"\" {\n\t\treturn price\n\t}\n\tpct := tierPercent(RefDiscountRules(), TenureYears(ref))\n\tif pct \u003c= 0 {\n\t\treturn price\n\t}\n\treturn price - (price * pct / 100)\n}\n\n// referralCut is what leaves for the referrer out of a payment of\n// `price`, and is zero unless they still hold a name. Returns the\n// destination address and the amount; the caller pays it in the same\n// banker call that pays the treasury.\nfunc referralCut(ref address, price int64) (address, int64) {\n\tif ref == \"\" || price \u003c= 0 {\n\t\treturn \"\", 0\n\t}\n\t/* STILL HERE, AND STILL VALID.\n\t   This tested nsdata.BalanceOf, which has no expiry filter — and a\n\t   token is burned only when somebody else re-registers the label. So\n\t   a referrer who bought one cheap name, earned credits and walked\n\t   away kept a balance of one forever, and every renewal of every\n\t   name they ever referred kept paying them out of treasury revenue.\n\t   That is exactly the standing charge the note above says the check\n\t   prevents.\n\t   ResolveAddress is the honest test: it returns a name only when the\n\t   address still owns it AND it has not expired. TenureYears does the\n\t   same lookup two lines below, so this costs nothing extra. */\n\tif ResolveAddress(ref) == \"\" {\n\t\treturn \"\", 0\n\t}\n\tpct := tierPercent(RefCutRules(), TenureYears(ref))\n\tif pct \u003c= 0 {\n\t\treturn \"\", 0\n\t}\n\tcut := price * pct / 100\n\tif cut \u003c= 0 {\n\t\treturn \"\", 0\n\t}\n\treturn PayoutFor(ref), cut\n}\n\n// -- the scoreboard --\n\n// creditReferral records one paid referral against a wallet.\n//\n// The COUNT and nothing else. There was a running total of micro-USD\n// beside it, and it is gone, because a number stored in the vault is a\n// number anyone with an RPC endpoint can read — there is no such thing\n// as an admin-only global on a public chain, and building the site as\n// though there were would be the kind of privacy that works right up\n// until somebody checks.\n//\n// Not storing it is the only real reduction available. The payments are\n// still in the block history and always will be, so anyone determined\n// can add them up; what we can decline to do is publish a convenient\n// running total ourselves. tools/referrals.py reconstructs it from the\n// chain when we want it, which costs no storage and publishes nothing.\n//\n// The registrant's transaction pays for this entry. It is about forty\n// bytes, written once per referrer and overwritten after, and it is the\n// price of the discount they just received.\nfunc creditReferral(cur realm, ref address) {\n\tif ref == \"\" {\n\t\treturn\n\t}\n\tsetGlobal(cur, keyRefStat+ref.String(),\n\t\tstrconv.FormatInt(ReferralCount(ref)+1, 10))\n}\n\n// ReferralCount is how many paid registrations a wallet has been\n// credited with. Public, like every other global here.\nfunc ReferralCount(who address) int64 {\n\treturn parseInt64(nsdata.GetGlobal(keyRefStat + who.String()))\n}\n\nfunc parseInt64(s string) int64 {\n\tv, err := strconv.ParseInt(strings.TrimSpace(s), 10, 64)\n\tif err != nil {\n\t\treturn 0\n\t}\n\treturn v\n}\n\n// ReferralBoard pages through every credited wallet for the\n// leaderboard: one \"address,count\" row per line. The vault stores\n// globals in a sorted tree, so the \"ref:\" entries are one contiguous run\n// and the walk stops at the first key past it rather than reading the\n// rest of the config.\nfunc ReferralBoard(after string, limit int) (rows, next string) {\n\tif limit \u003c= 0 || limit \u003e 200 {\n\t\tlimit = 200\n\t}\n\tfrom := keyRefStat\n\tif after != \"\" {\n\t\tfrom = keyRefStat + after\n\t}\n\tvar out []string\n\tfor len(out) \u003c limit {\n\t\tkeys, more := nsdata.ExportGlobalKeys(from, limit)\n\t\tif keys == \"\" {\n\t\t\tbreak\n\t\t}\n\t\tstop := false\n\t\tfor _, k := range strings.Split(keys, \",\") {\n\t\t\tif !strings.HasPrefix(k, keyRefStat) {\n\t\t\t\tstop = true\n\t\t\t\tbreak\n\t\t\t}\n\t\t\taddr := k[len(keyRefStat):]\n\t\t\tn := ReferralCount(address(addr))\n\t\t\tif n \u003c= 0 {\n\t\t\t\tcontinue\n\t\t\t}\n\t\t\tout = append(out, addr+\",\"+strconv.FormatInt(n, 10))\n\t\t\tif len(out) \u003e= limit {\n\t\t\t\tnext = addr\n\t\t\t\tbreak\n\t\t\t}\n\t\t}\n\t\tif stop || more == \"\" || more == from {\n\t\t\tbreak\n\t\t}\n\t\tfrom = more\n\t}\n\treturn strings.Join(out, \"\\n\"), next\n}\n"},{"name":"reserved.gno","body":"package nslogic\n\nimport (\n\t\"strconv\"\n\n\t\"gno.land/r/g1xr6tgxnpled50h74eafmvxway7z0ytr5rsmeme/nsdata/v2\"\n)\n\n// GENERATED BY tools/gen-reserved.py FROM collections/reserved-labels.json.\n// Do not edit by hand: the site reads the same source, and a change made\n// in one place only is a label the site offers and the chain refuses.\n//\n// STRUCTURAL RULES ONLY. The wordlists live off chain, in that JSON,\n// because storage is 100 ugnot per byte and a ten-thousand-word\n// dictionary is about 57 GNOT paid forever to encode a marketing\n// decision that will change. This is the backstop against a bug in the\n// issuer, not the place the policy lives.\n//\n// THE NUMBERS ARE SETTABLE, the shape is not. A minimum length is a\n// promotion knob and belongs in the panel — redeploying this realm to\n// change a 5 to a 4 costs about 22 GNOT. So the lengths read from the\n// vault's globals and fall back to the values generated here; the\n// pattern rules are structural facts about what a handle looks like and\n// stay in code.\n\nconst (\n\tdefaultFreeMinLength = 5\n\tdefaultFreeMaxLength = 63\n\n\tkeyFreeMin = \"freemin\"\n\tkeyFreeMax = \"freemax\"\n)\n\n// FreeMinLength is the shortest label the free tier will issue. Settable\n// from the panel, because which lengths are worth money is a commercial\n// judgement that moves.\nfunc FreeMinLength() int {\n\tif v := nsdata.GetGlobal(keyFreeMin); v != \"\" {\n\t\tif n, err := strconv.Atoi(v); err == nil \u0026\u0026 n \u003e 0 \u0026\u0026 n \u003c= 63 {\n\t\t\treturn n\n\t\t}\n\t}\n\treturn defaultFreeMinLength\n}\n\nfunc FreeMaxLength() int {\n\tif v := nsdata.GetGlobal(keyFreeMax); v != \"\" {\n\t\tif n, err := strconv.Atoi(v); err == nil \u0026\u0026 n \u003e 0 \u0026\u0026 n \u003c= 63 {\n\t\t\treturn n\n\t\t}\n\t}\n\treturn defaultFreeMaxLength\n}\n\n/*\nFreeLabelAllowed reports whether a label may be ISSUED on the free tier.\n\nISSUED, and never renewed. This is called by GrantFreeName and by\nnothing else, deliberately: if the minimum length rises from three to\nfive, somebody who already holds a three-character free name keeps it\nand keeps renewing it free. Tightening a rule must not reach backwards\nand take away what was already given — that is the single fastest way to\nmake a giveaway cost more trust than it ever bought.\n\nA name that LAPSES and is registered again is a new issuance, and gets\nwhatever the rules say that day. That is not retroactive; it is a\ndifferent name event.\n*/\nfunc FreeLabelAllowed(label string) bool {\n\tn := len(label)\n\tif n \u003c FreeMinLength() || n \u003e FreeMaxLength() {\n\t\treturn false\n\t}\n\t// Pure digits: 1337, 420, years. Collectible, and no handle is one.\n\tdigits := true\n\tfor i := 0; i \u003c n; i++ {\n\t\tif label[i] \u003c '0' || label[i] \u003e '9' {\n\t\t\tdigits = false\n\t\t\tbreak\n\t\t}\n\t}\n\tif digits {\n\t\treturn false\n\t}\n\t// aaaa — every character the same.\n\tsame := true\n\tfor i := 1; i \u003c n; i++ {\n\t\tif label[i] != label[0] {\n\t\t\tsame = false\n\t\t\tbreak\n\t\t}\n\t}\n\tif same {\n\t\treturn false\n\t}\n\t// abab — a two-character cycle. Also collectible, also not a handle.\n\tif n \u003e= 4 {\n\t\tcycle := true\n\t\tfor i := 2; i \u003c n; i++ {\n\t\t\tif label[i] != label[i-2] {\n\t\t\t\tcycle = false\n\t\t\t\tbreak\n\t\t\t}\n\t\t}\n\t\tif cycle {\n\t\t\treturn false\n\t\t}\n\t}\n\treturn true\n}\n"},{"name":"restore.gno","body":"package nslogic\n\nimport (\n\t\"strconv\"\n\t\"strings\"\n\t\"time\"\n\n\t\"gno.land/r/g1xr6tgxnpled50h74eafmvxway7z0ytr5rsmeme/nsdata/v2\"\n)\n\n/*\nREBUILDING THE REGISTRY ON A NEW CHAIN.\n\nWHY THIS EXISTS. gno.land replaces testnets rather than preserving them:\ntopaz-1 was superseded by sapphire-1 on twenty-four hours' notice and\nevery name registered on it went to zero. tools/snapshot.js keeps a\ncomplete off-chain copy of the vault for exactly that morning. This is\nthe other half — the path that turns a snapshot back into a live\nregistry, so that people who paid for a name get that name back rather\nthan an apology.\n\nTHIS IS A BACKDOOR, AND IT IS TREATED AS ONE.\n\nA function that can mint any name, to any address, with any registration\ndate, is precisely the thing that would make every name here worthless\nif it could be reached whenever the admin felt like it. Ownership means\nnothing if history is editable on demand. So the capability is not\nmerely admin-gated — admin-gated is what everything else here already\nis, and it is not enough for this. It is a ONE-SHOT:\n\n  - it starts closed, and on a chain where nothing needs restoring it is\n    sealed immediately and can never be used at all;\n  - opening it is a deliberate, separate, announced act with a deadline\n    attached, so forgetting to close it is not the same as leaving it\n    open forever;\n  - sealing is IRREVERSIBLE. Sealed cannot be reopened. There is no\n    argument that unseals it, including a good one;\n  - it refuses to touch a name that already exists. It fills gaps in an\n    empty registry; it cannot rewrite a live one.\n\nWHERE THE SEAL LIVES, AND WHY IT IS NOT HERE. This realm holds no state\nat all — that is the whole reason prices and names survive a logic\nupgrade untouched. If the seal lived in this realm's variables it would\nreset to \"never opened\" the moment a new generation was deployed, which\nwould make \"irreversible\" a lie with a two-hour shelf life. It lives in\nthe vault's global slot instead, so a new logic generation inherits a\nseal it cannot talk its way out of.\n\nWHAT THAT DOES AND DOES NOT GUARANTEE. It is a real barrier against\nevery accident and against this realm being reached later for something\nit was not built for. It is not a barrier against the admin themselves,\nwho can deploy a further logic realm and have the vault trust it — but\nthat is already total control by definition, and no flag in a realm the\nadmin can replace was ever going to change it. What the seal gives is\nthe thing that actually matters in practice: the state is public, it is\ncheckable by anybody at any time through RestoreStatus, and moving it\nbackwards leaves a permanent trace on chain.\n\nWHAT IT DELIBERATELY DOES NOT RESTORE. Marketplace listings and open\nvotes. Those live in their own realms, are intentions rather than facts,\nand are re-creatable by the people who made them in one transaction\neach. Re-materialising somebody's month-old asking price as though they\nhad just set it would be inventing a decision on their behalf.\n*/\n\n// The vault key holding the state of this whole mechanism.\nconst keyRestore = \"restore\"\n\n// Restore states, stored as a string in the vault's global slot.\nconst (\n\trestoreNever  = \"\"       // never opened; the normal state forever\n\trestoreSealed = \"sealed\" // closed permanently; cannot be reopened\n\trestoreOpen   = \"open:\"  // followed by the unix second it lapses\n)\n\n// The longest a restore window may be left open. A restore is a day of\n// work, not a season, and an unbounded window is just \"open forever\"\n// spelled differently — the failure here is nobody remembering to seal\n// it, so the deadline seals it for them.\nconst maxRestoreDays = 30\n\n// RestoreStatus reports the state of the restore mechanism in a form a\n// person can read, and is deliberately PUBLIC AND UNAUTHENTICATED.\n// Anybody at all can check whether this registry currently has a door in\n// it, without asking us and without trusting the answer we would give.\n//\n// Returns the state (\"never\", \"open\", \"expired\" or \"sealed\"), the unix\n// second an open window lapses (0 otherwise), and how many records have\n// been restored in the lifetime of this vault.\nfunc RestoreStatus() (state string, deadline int64, restored int64) {\n\tst, d := readRestoreState(nsdata.GetGlobal(keyRestore), time.Now().Unix())\n\treturn st, d, restoredCount()\n}\n\n// readRestoreState is the state machine on its own, with the clock passed\n// in. Split out so the one piece of logic that decides whether the door\n// is open can be tested exhaustively without a chain under it.\nfunc readRestoreState(raw string, now int64) (state string, deadline int64) {\n\tswitch {\n\tcase raw == restoreSealed:\n\t\treturn \"sealed\", 0\n\tcase strings.HasPrefix(raw, restoreOpen):\n\t\td, err := strconv.ParseInt(raw[len(restoreOpen):], 10, 64)\n\t\tif err != nil || d \u003c= 0 {\n\t\t\t// An unparseable deadline is read as NO permission. The safe\n\t\t\t// reading of a door whose lock is broken is \"shut\" — a corrupt\n\t\t\t// value must never widen what somebody is allowed to do.\n\t\t\treturn \"sealed\", 0\n\t\t}\n\t\tif now \u003e= d {\n\t\t\treturn \"expired\", d\n\t\t}\n\t\treturn \"open\", d\n\t}\n\treturn \"never\", 0\n}\n\nconst keyRestoredCount = \"restored\"\n\nfunc restoredCount() int64 {\n\tif v := nsdata.GetGlobal(keyRestoredCount); v != \"\" {\n\t\tif n, err := strconv.ParseInt(v, 10, 64); err == nil {\n\t\t\treturn n\n\t\t}\n\t}\n\treturn 0\n}\n\n/*\nTHE RESERVED-KEY DOOR DOES NOT EXIST ON THE DEPLOYED VAULT, so these\nwrites go through the ordinary setter.\n\nnsdata/v1 is live and permanent, and its SetGlobalRecord takes any key\nwith no reserved-key guard at all. The two-door design — a general setter\nthat refuses \"restore\"/\"restored\" plus SetReservedGlobalRecord for the\nmachinery — exists only in the local nsdata source, which can never be\ndeployed over a vault holding real records. Calling for a door that is\nnot in the wall does not fail at review; it fails at deploy, after the\ndeposit is spent.\n\nSo the seal is guarded in ONE place: setGlobal in admin.gno, which panics\non both keys. That is weaker than the intended belt-and-braces — a future\nlogic realm could drop the guard by forgetting it, and the vault would\nnot catch it — and it is the strongest version available on this chain.\nDo not \"restore\" the reserved call: it will not link.\n*/\nfunc bumpRestored(cur realm, n int64) {\n\tnsdata.SetGlobalRecord(cross(cur), keyRestoredCount,\n\t\tstrconv.FormatInt(restoredCount()+n, 10))\n}\n\n// assertRestoring is the gate every Restore* function passes through.\nfunc assertRestoring(cur realm) {\n\tassertNoSend(cur)\n\tassertIsAdmin(cur)\n\tstate, _, _ := RestoreStatus()\n\tswitch state {\n\tcase \"open\":\n\t\treturn\n\tcase \"sealed\":\n\t\tpanic(\"nslogic: restore was sealed and cannot be reopened. This is \" +\n\t\t\t\"not recoverable by design — a registry whose history can be \" +\n\t\t\t\"rewritten on request does not have owners, it has guests.\")\n\tcase \"expired\":\n\t\tpanic(\"nslogic: the restore window has lapsed. Deploy a fresh vault \" +\n\t\t\t\"and open a new window there; an expired one does not reopen.\")\n\t}\n\tpanic(\"nslogic: restore has never been opened. Call OpenRestore first, \" +\n\t\t\"and mean it.\")\n}\n\n// OpenRestore opens the window, once, for a bounded number of days.\n//\n// Only ever valid on a chain where the registry is EMPTY because the\n// previous chain was retired. It cannot be called on a vault that has\n// been sealed, and it cannot be called twice.\nfunc OpenRestore(cur realm, days int64) {\n\tassertNoSend(cur)\n\tassertIsAdmin(cur)\n\n\tstate, _, _ := RestoreStatus()\n\tif state != \"never\" {\n\t\tpanic(\"nslogic: restore may be opened exactly once per vault; this \" +\n\t\t\t\"one is already \" + state + \". A door that reopens is a door.\")\n\t}\n\tif days \u003c 1 || days \u003e maxRestoreDays {\n\t\tpanic(\"nslogic: a restore window must be between 1 and \" +\n\t\t\tstrconv.Itoa(maxRestoreDays) + \" days\")\n\t}\n\t/* THE PRECONDITION THE COMMENT ABOVE ALREADY CLAIMS.\n\t   \"Only ever valid on a chain where the registry is EMPTY\" was a\n\t   statement of intent that nothing enforced, so the window was\n\t   openable on a populated vault. The record restorers all refuse an\n\t   existing record, but RestoreNameField does not — it writes any\n\t   extension key on any EXISTING name, which includes the payment\n\t   addresses the site renders on a name's pay page. That is a way to\n\t   redirect somebody else's incoming money using only the admin key,\n\t   without the timelock and public event that granting a fresh logic\n\t   realm would cost. Now it is what it said it was. */\n\tif d, n, _, _ := nsdata.Sizes(); d != 0 || n != 0 {\n\t\tpanic(\"nslogic: restore is for an empty vault on a fresh chain; \" +\n\t\t\t\"this one already holds records\")\n\t}\n\tdeadline := time.Now().Unix() + days*86400\n\tnsdata.SetGlobalRecord(cross(cur), keyRestore,\n\t\trestoreOpen+strconv.FormatInt(deadline, 10))\n}\n\n// SealRestore closes the door permanently. Valid from any state,\n// including \"never\" — which is how it should be called on a chain that\n// has nothing to restore, so that the capability is provably dead rather\n// than merely unused.\n//\n// There is no matching unseal, and adding one later would defeat every\n// word written at the top of this file.\nfunc SealRestore(cur realm) {\n\tassertNoSend(cur)\n\tassertIsAdmin(cur)\n\tnsdata.SetGlobalRecord(cross(cur), keyRestore, restoreSealed)\n}\n\n/*\nRestoreDomain re-creates one domain with its original dates.\n\nORDER MATTERS: domains before their names. The vault refuses to register\na name under a domain that does not exist or had lapsed at the moment\nthe name claims to have been registered, which is correct behaviour and\nalso exactly the order a snapshot should be replayed in.\n\nThe vault's own registration path sets `registered` to whatever instant\nit is handed and derives the expiry from the term length, so the\nregistration date is restored by passing the ORIGINAL one, and the\nexpiry is then corrected to the recorded value rather than a computed\none. Anything else would quietly hand everybody a fresh term.\n*/\nfunc RestoreDomain(cur realm, label string, owner address, registered, expires, ttl int64, frozen bool) {\n\tassertRestoring(cur)\n\tif nsdata.DomainExists(label) {\n\t\tpanic(\"nslogic: \" + label + \" already exists; restore fills an empty \" +\n\t\t\t\"registry and never overwrites a live record\")\n\t}\n\tif registered \u003c= 0 || expires \u003c= 0 {\n\t\tpanic(\"nslogic: a restored record needs its original dates\")\n\t}\n\tif err := nsdata.RegisterDomainRecord(cross(cur), label, owner, registered); err != nil {\n\t\tpanic(\"nslogic: \" + err.Error())\n\t}\n\tnsdata.SetDomainExpiryRecord(cross(cur), label, expires)\n\tif ttl != 0 {\n\t\tif err := nsdata.SetDomainTTLRecord(cross(cur), label, ttl); err != nil {\n\t\t\tpanic(\"nslogic: \" + err.Error())\n\t\t}\n\t}\n\tif frozen {\n\t\tnsdata.SetDomainFrozen(cross(cur), label, true)\n\t}\n\tbumpRestored(cur, 1)\n}\n\n// RestoreName re-creates one name. See RestoreDomain on ordering and on\n// why the dates are passed rather than computed.\n//\n// isNFT is carried across rather than assumed: the free tier registers\n// names without a token, and minting one for somebody who never had it\n// would silently upgrade their record and desynchronise the two\n// ownership ledgers.\nfunc RestoreName(cur realm, label, domainLabel string, owner address, registered, expires, ttl int64, frozen, isNFT bool) {\n\tassertRestoring(cur)\n\tif nsdata.NameExists(label, domainLabel) {\n\t\tpanic(\"nslogic: \" + label + \"*\" + domainLabel + \" already exists; \" +\n\t\t\t\"restore fills an empty registry and never overwrites a live record\")\n\t}\n\tif registered \u003c= 0 || expires \u003c= 0 {\n\t\tpanic(\"nslogic: a restored record needs its original dates\")\n\t}\n\tif err := nsdata.RegisterNameRecord(cross(cur), label, domainLabel, owner, registered, isNFT); err != nil {\n\t\tpanic(\"nslogic: \" + err.Error())\n\t}\n\tnsdata.SetNameExpiryRecord(cross(cur), label, domainLabel, expires)\n\tif ttl != 0 {\n\t\tif err := nsdata.SetNameTTLRecord(cross(cur), label, domainLabel, ttl); err != nil {\n\t\t\tpanic(\"nslogic: \" + err.Error())\n\t\t}\n\t}\n\tif frozen {\n\t\tnsdata.SetNameFrozen(cross(cur), label, domainLabel, true)\n\t}\n\tbumpRestored(cur, 1)\n}\n\n/*\nRestoreNames replays many names in ONE transaction.\n\nWHY A BLOB AND NOT A LOOP OF CALLS. A restore is done in one sitting\nagainst a clock: the window is bounded and the people whose names are\nmissing are watching. One transaction per name is one signature per\nname, and at a few hundred names that is an afternoon of clicking during\nwhich a single mistimed refresh loses the thread. The compact form here\nis unpleasant to read and it is the difference between a restore that\ngets finished and one that gets abandoned half way.\n\nThe format is records separated by \"|\", fields separated by \",\":\n\n\tlabel,domain,owner,registered,expires,ttl,frozen,isNFT\n\nNeither a label nor a domain may contain \",\" or \"|\" — the character set\nis enforced at registration — and an address contains neither, so the\nsplit is unambiguous. tools/snapshot.js writes the JSON this is built\nfrom; see tools/restore-blob.js for the conversion.\n\nIt stops at the first bad record and panics, taking the whole\ntransaction with it. That is deliberate: a partial batch that silently\nskipped four names would leave four people without theirs and nothing\nanywhere saying which four.\n*/\nfunc RestoreNames(cur realm, blob string) int {\n\tassertRestoring(cur)\n\tif blob == \"\" {\n\t\tpanic(\"nslogic: nothing to restore\")\n\t}\n\trecs := strings.Split(blob, \"|\")\n\tif len(recs) \u003e 50 {\n\t\tpanic(\"nslogic: at most 50 records per call, or the transaction \" +\n\t\t\t\"runs out of gas half way and it is unclear what landed\")\n\t}\n\tdone := 0\n\tfor i, rec := range recs {\n\t\tif rec == \"\" {\n\t\t\tcontinue\n\t\t}\n\t\tr := parseRestoreRecord(rec, i)\n\t\tif nsdata.NameExists(r.label, r.domain) {\n\t\t\tpanic(\"nslogic: record \" + strconv.Itoa(i) + \" (\" + r.label + \"*\" + r.domain +\n\t\t\t\t\") already exists; restore never overwrites a live record\")\n\t\t}\n\t\tif err := nsdata.RegisterNameRecord(cross(cur), r.label, r.domain, r.owner, r.registered, r.isNFT); err != nil {\n\t\t\tpanic(\"nslogic: record \" + strconv.Itoa(i) + \": \" + err.Error())\n\t\t}\n\t\tnsdata.SetNameExpiryRecord(cross(cur), r.label, r.domain, r.expires)\n\t\tif r.ttl != 0 {\n\t\t\tif err := nsdata.SetNameTTLRecord(cross(cur), r.label, r.domain, r.ttl); err != nil {\n\t\t\t\tpanic(\"nslogic: record \" + strconv.Itoa(i) + \": \" + err.Error())\n\t\t\t}\n\t\t}\n\t\tif r.frozen {\n\t\t\tnsdata.SetNameFrozen(cross(cur), r.label, r.domain, true)\n\t\t}\n\t\tdone++\n\t}\n\tbumpRestored(cur, int64(done))\n\treturn done\n}\n\n// restoreRec is one parsed line of the blob.\ntype restoreRec struct {\n\tlabel, domain     string\n\towner             address\n\tregistered        int64\n\texpires, ttl      int64\n\tfrozen, isNFT     bool\n}\n\n/*\nparseRestoreRecord reads one record and refuses anything it is not sure\nabout.\n\nEVERY CHECK HERE IS A PANIC AND NOT A SKIP. The blob is machine-written\nfrom a snapshot, so a malformed record does not mean \"this one name is\nodd\", it means the file is not what it was believed to be — and the\nworst outcome available is a restore that appears to succeed while\nquietly dropping the records it could not read. Somebody would find out\nmonths later when one person asked where their name went.\n*/\nfunc parseRestoreRecord(rec string, i int) restoreRec {\n\tf := strings.Split(rec, \",\")\n\tif len(f) != 8 {\n\t\tpanic(\"nslogic: record \" + strconv.Itoa(i) + \" has \" +\n\t\t\tstrconv.Itoa(len(f)) + \" fields, expected 8 \" +\n\t\t\t\"(label,domain,owner,registered,expires,ttl,frozen,isNFT)\")\n\t}\n\tif f[0] == \"\" || f[1] == \"\" {\n\t\tpanic(\"nslogic: record \" + strconv.Itoa(i) + \" has no name\")\n\t}\n\tout := restoreRec{\n\t\tlabel:      f[0],\n\t\tdomain:     f[1],\n\t\towner:      address(f[2]),\n\t\tregistered: mustSecs(f[3], i, \"registered\"),\n\t\texpires:    mustSecs(f[4], i, \"expires\"),\n\t\tttl:        mustSecs(f[5], i, \"ttl\"),\n\t\tfrozen:     f[6] == \"1\",\n\t\tisNFT:      f[7] == \"1\",\n\t}\n\tif !out.owner.IsValid() {\n\t\tpanic(\"nslogic: record \" + strconv.Itoa(i) + \" has an invalid owner address\")\n\t}\n\tif out.registered \u003c= 0 || out.expires \u003c= 0 {\n\t\tpanic(\"nslogic: record \" + strconv.Itoa(i) + \" is missing its original dates\")\n\t}\n\t// An expiry before the registration is not a record, it is a\n\t// corruption, and restoring it would create a name that was already\n\t// reclaimable the day it was bought.\n\tif out.expires \u003c out.registered {\n\t\tpanic(\"nslogic: record \" + strconv.Itoa(i) + \" expires before it was registered\")\n\t}\n\treturn out\n}\n\nfunc mustSecs(s string, i int, what string) int64 {\n\tv, err := strconv.ParseInt(s, 10, 64)\n\tif err != nil || v \u003c 0 {\n\t\tpanic(\"nslogic: record \" + strconv.Itoa(i) + \" has a bad \" + what)\n\t}\n\treturn v\n}\n\n// RestoreNameField and RestoreDomainField put back a record's extension\n// keys — profiles, cross-chain addresses, tier markers, and a domain's\n// pricing rules. Separate calls because the extension slot has no fixed\n// shape by design, which is the same reason the snapshot captures it by\n// enumeration rather than by schema.\nfunc RestoreNameField(cur realm, label, domainLabel, key, value string) {\n\tassertRestoring(cur)\n\tif err := nsdata.SetNameExtraRecord(cross(cur), label, domainLabel, key, value); err != nil {\n\t\tpanic(\"nslogic: \" + err.Error())\n\t}\n}\n\nfunc RestoreDomainField(cur realm, label, key, value string) {\n\tassertRestoring(cur)\n\tif err := nsdata.SetDomainExtraRecord(cross(cur), label, key, value); err != nil {\n\t\tpanic(\"nslogic: \" + err.Error())\n\t}\n}\n\n// RestoreGlobal puts back the vault's global slot — where the default\n// price, the referral tiers and the seasonal card effects live.\n//\n// It refuses to write the restore keys themselves. Without that, a\n// snapshot taken while a window was open would, on being replayed,\n// cheerfully restore the open window along with everything else — and a\n// seal that a backup file can undo is not a seal.\nfunc RestoreGlobal(cur realm, key, value string) {\n\tassertRestoring(cur)\n\tif key == keyRestore || key == keyRestoredCount {\n\t\tpanic(\"nslogic: the restore mechanism does not restore itself\")\n\t}\n\tif err := nsdata.SetGlobalRecord(cross(cur), key, value); err != nil {\n\t\tpanic(\"nslogic: \" + err.Error())\n\t}\n}\n\n// RestorePrimary puts back reverse resolution — which name an address\n// answers to. Registration already sets this for an owner who had none,\n// so this exists for the owners who had chosen a different one.\nfunc RestorePrimary(cur realm, owner address, label, domainLabel string) {\n\tassertRestoring(cur)\n\tif err := nsdata.SetPrimaryRecord(cross(cur), owner, label, domainLabel); err != nil {\n\t\tpanic(\"nslogic: \" + err.Error())\n\t}\n}\n"},{"name":"term.gno","body":"package nslogic\n\nimport (\n\t\"strconv\"\n\t\"time\"\n\n\t\"gno.land/p/nt/ufmt/v0\"\n\t\"gno.land/r/g1xr6tgxnpled50h74eafmvxway7z0ytr5rsmeme/nsdata/v2\"\n)\n\n// How far ahead a name may be paid.\n//\n// Renewal was unbounded. RenewNameRecord adds one term to the previous\n// due date and asks no further questions, so a holder pressing renew\n// seven times bought seven years of runway, and nothing anywhere said\n// that was not intended. It is a bug in two directions at once:\n//\n//   - Free domains have a base price of zero. Renewal is free too, so a\n//     script could take every good name under *gnoland and hold it for a\n//     century for the cost of gas. There is no economic brake on a free\n//     name at all; the term limit IS the brake.\n//   - A paid name held fifty years ahead is fifty years of a promise we\n//     have made against a rate written by hand and a testnet that will\n//     be replaced. Selling that is easy and honouring it is not.\n//\n// The limit is RUNWAY, not total lifetime: at most N years may be\n// outstanding at any moment. Nobody is stopped from holding a name\n// forever — they renew as the runway burns down, which is the same\n// arrangement every domain registrar has always used and the one people\n// already understand.\n//\n// Per domain first, then a project-wide default, so a free domain can be\n// capped at one year without capping anything else.\nconst (\n\tkeyMaxTerm       = \"maxterm\" // domain extra AND global: years of runway\n\tdefaultMaxTerm   = int64(5)\n\tmaxMaxTermYears  = int64(100)\n)\n\n// MaxTermYears is the cap that applies to names under this domain.\nfunc MaxTermYears(domainLabel string) int64 {\n\tif v := nsdata.GetDomainExtra(domainLabel, keyMaxTerm); v != \"\" {\n\t\tif y, err := strconv.ParseInt(v, 10, 64); err == nil \u0026\u0026 y \u003e 0 {\n\t\t\treturn y\n\t\t}\n\t}\n\tif v := nsdata.GetGlobal(keyMaxTerm); v != \"\" {\n\t\tif y, err := strconv.ParseInt(v, 10, 64); err == nil \u0026\u0026 y \u003e 0 {\n\t\t\treturn y\n\t\t}\n\t}\n\treturn defaultMaxTerm\n}\n\n// SetMaxTerm caps a single domain, or the whole project when the domain\n// label is empty. A domain's own cap always wins, so tightening one free\n// domain does not disturb the rest.\nfunc SetMaxTerm(cur realm, domainLabel string, years int64) {\n\tassertNoSend(cur)\n\tassertIsAdmin(cur)\n\tif years \u003c 1 || years \u003e maxMaxTermYears {\n\t\tpanic(\"nslogic: term cap must be 1-100 years\")\n\t}\n\tv := strconv.FormatInt(years, 10)\n\tif domainLabel == \"\" {\n\t\tsetGlobal(cur, keyMaxTerm, v)\n\t\treturn\n\t}\n\tif !nsdata.DomainExists(domainLabel) {\n\t\tpanic(\"nslogic: unknown domain\")\n\t}\n\tif err := nsdata.SetDomainExtraRecord(cross(cur), domainLabel, keyMaxTerm, v); err != nil {\n\t\tpanic(err)\n\t}\n}\n\n// RenewalsLeft is how many more years may be added to a name right now.\n// Zero means the renew button should be off, and the site reads this\n// rather than doing the arithmetic itself — the cap can be per domain,\n// and a page that guesses it will guess wrong on exactly the domains\n// where it matters.\n//\n// An unregistered name reports the full allowance: nothing is holding\n// runway yet.\nfunc RenewalsLeft(label, domainLabel string) int64 {\n\tmaxYears := MaxTermYears(domainLabel)\n\tif !nsdata.NameExists(label, domainLabel) {\n\t\treturn maxYears\n\t}\n\tterm := nsdata.GetTermLength()\n\tif term \u003c= 0 {\n\t\treturn 0\n\t}\n\t_, _, expires, _, _, _ := nsdata.GetNameInfo(label, domainLabel)\n\treturn renewalsLeft(expires, time.Now().Unix(), term, maxYears)\n}\n\n// renewalsLeft is the arithmetic on its own, so it can be tested without\n// a chain under it.\n//\n// Runway is measured from NOW, not from the registration date, so a name\n// deep in its grace period counts as zero rather than as a negative that\n// would then read as extra room. Years already bought round UP: a name\n// with thirteen months left is holding two of its five, because the\n// alternative lets somebody sit permanently at four-years-and-eleven-\n// months and renew every month forever.\nfunc renewalsLeft(expires, now, term, maxYears int64) int64 {\n\tif term \u003c= 0 {\n\t\treturn 0\n\t}\n\trunway := expires - now\n\tif runway \u003c 0 {\n\t\trunway = 0\n\t}\n\tleft := maxYears - (runway+term-1)/term\n\tif left \u003c 0 {\n\t\treturn 0\n\t}\n\treturn left\n}\n\n// assertRoomToRenew is the enforcement. It runs before payment is taken,\n// so a rejected renewal costs gas and nothing else — taking the money\n// first and reverting would work too, but only because a panic reverts,\n// and depending on that for a refund is a habit that eventually meets a\n// path where it is not true.\nfunc assertRoomToRenew(label, domainLabel string) {\n\tif RenewalsLeft(label, domainLabel) \u003e 0 {\n\t\treturn\n\t}\n\tpanic(ufmt.Sprintf(\n\t\t\"nslogic: %s*%s is already paid %d years ahead, which is the limit. Renew again when it has run down.\",\n\t\tlabel, domainLabel, MaxTermYears(domainLabel)))\n}\n"},{"name":"types.gno","body":"// Package nslogic is the swappable \"business rules\" half of the split\n// (NOTES.md §9t). It holds every piece of policy that might reasonably\n// change — pricing, character validation, payment/refund, freeze rules,\n// transfer restrictions — and no persistent records of its own.\n//\n// Every mutation here ends by calling into nsdata's narrow Record/Raw\n// API via cross(cur). nsdata trusts whichever address is currently wired\n// up as trustedLogic to have done its own authorization checks\n// correctly, so THIS realm is where \"is the caller allowed to do this\"\n// actually lives. If a bug is found here, admin deploys a fixed nslogic\n// and re-points the vault at it (ProposeTrustedLogicGrant, then\n// ExecutePending after the timelock) — no data migration,\n// records and config untouched.\n//\n// Deliberately reads config fresh from nsdata on every call rather than\n// caching it. The one thing that cannot be read fresh is a COMPILED\n// regex — avl trees store the pattern string, not the compiled object —\n// so that one value is cached, and the cache is validated against\n// nsdata on every use.\npackage nslogic\n\n\nconst (\n\tugnot    = \"ugnot\"\n\tbpsDenom = 10_000\n)\n\n\n"},{"name":"validate.gno","body":"package nslogic\n\nimport (\n\t\"strings\"\n\n\t\"gno.land/p/nt/ufmt/v0\"\n\t\"gno.land/r/g1xr6tgxnpled50h74eafmvxway7z0ytr5rsmeme/nsdata/v2\"\n)\n\n// THE LABEL RULE IS WRITTEN OUT, NOT COMPILED.\n//\n// It used to be a regexp, and that regexp was the single most expensive\n// thing in this realm — by a wide margin, and not by a little. Measured\n// on sapphire-1 with vm/qstorage against purpose-built probe realms:\n//\n//\ta realm with code and no package-level state      2,711 bytes\n//\tthe same realm plus ONE compiled regexp         154,877 bytes\n//\n// A compiled regexp is an object graph, and gno charges 100 ugnot for\n// every byte of it, forever. That one variable was 15.2 GNOT of the\n// 22.5 GNOT it costs to deploy this realm — about 68% of the bill, to\n// enforce a rule that fits in thirty lines.\n//\n// The second regexp was worse, because it was compiled LAZILY and cached\n// in a package-level var. That does not avoid the cost, it MOVES it: the\n// first person to register a name after a fresh deploy would have had\n// their transaction grow this realm's storage by 150KB and been charged\n// ~15 GNOT of deposit for it. Confirmed by probe, not inferred — a realm\n// deployed at 2,711 bytes went to 154,149 the moment one call compiled\n// and cached a regexp, billed to the caller. Somebody buying a ten-cent\n// name would have been asked for fifteen GNOT.\n//\n// The hand-written version is also 6x cheaper in gas on every call\n// (1.06M against 6.67M), so there is no axis on which the regexp was\n// the better choice. It read better. That was the whole of its case.\n//\n// WHAT THIS COSTS US. The pattern is no longer admin-tunable at runtime:\n// changing the rule now means redeploying this realm. That is a fair\n// trade at 7 GNOT a deploy, it was never a rule that should change\n// casually, and SetLabelPattern refuses anything but the implemented\n// pattern so the stored string can never describe rules the code does\n// not enforce.\n\n// labelRule is the pattern this realm implements, kept as a string so\n// nsdata, the error messages and the site all quote the same thing.\nconst labelRule = `^[a-z0-9]+(-[a-z0-9]+)*$`\n\n// matchesLabelRule is that pattern, by hand: lowercase letters and\n// digits, single hyphens between them, none at either end.\nfunc matchesLabelRule(s string) bool {\n\tif s == \"\" {\n\t\treturn false\n\t}\n\tprevHyphen := true // a leading hyphen is not allowed\n\tfor i := 0; i \u003c len(s); i++ {\n\t\tc := s[i]\n\t\tswitch {\n\t\tcase c \u003e= 'a' \u0026\u0026 c \u003c= 'z', c \u003e= '0' \u0026\u0026 c \u003c= '9':\n\t\t\tprevHyphen = false\n\t\tcase c == '-':\n\t\t\tif prevHyphen {\n\t\t\t\treturn false // leading, or two in a row\n\t\t\t}\n\t\t\tprevHyphen = true\n\t\tdefault:\n\t\t\treturn false\n\t\t}\n\t}\n\treturn !prevHyphen // nor a trailing one\n}\n\n// looksLikeAddress was `^g1[a-z0-9]{20,38}$`. Anything shaped like a real\n// gno.land address is refused as a label, so a name can never be mistaken\n// for a raw wallet address in a UI.\nfunc looksLikeAddress(s string) bool {\n\tif len(s) \u003c 22 || len(s) \u003e 40 || !strings.HasPrefix(s, \"g1\") {\n\t\treturn false\n\t}\n\tfor i := 2; i \u003c len(s); i++ {\n\t\tc := s[i]\n\t\tif !(c \u003e= 'a' \u0026\u0026 c \u003c= 'z') \u0026\u0026 !(c \u003e= '0' \u0026\u0026 c \u003c= '9') {\n\t\t\treturn false\n\t\t}\n\t}\n\treturn true\n}\n\n// validateLabel — same policy as the original single-realm build (see\n// NOTES.md for the ENS/Starname/r/sys/users research behind it). The\n// length bounds are still read from nsdata, so they survive a swap of\n// this realm; the character rule is now this realm's own.\n//\n// Call sites, and why: RegisterName/RegisterDomain only (both the\n// first-ever mint AND reclaiming a past-grace slot — a reclaim is a\n// genuine new mint with a new owner). RenewName/RenewDomain deliberately\n// do NOT call this — an already-registered name must keep renewing under\n// whatever rule was in force when it was minted.\nfunc validateLabel(label string) {\n\tminLen, maxLen := nsdata.GetLabelLength()\n\tif len(label) \u003c minLen || len(label) \u003e maxLen {\n\t\tpanic(ufmt.Sprintf(\"nslogic: label must be %d-%d characters\", minLen, maxLen))\n\t}\n\t// Structural invariant, enforced regardless of what the pattern\n\t// happens to allow: \"*\" is the separator in both the token ID\n\t// (label*domain) and nsdata's storage key (domain*label). A label or\n\t// domain containing one would make those encodings ambiguous and let\n\t// two different names collide onto a single record.\n\tif strings.Contains(label, \"*\") {\n\t\tpanic(\"nslogic: label may not contain '*'\")\n\t}\n\tif !matchesLabelRule(label) {\n\t\tpanic(\"nslogic: a name is lowercase letters, digits and single hyphens between them — \" + labelRule)\n\t}\n\tif looksLikeAddress(label) {\n\t\tpanic(\"nslogic: label looks like a wallet address, not allowed\")\n\t}\n}\n"}]},"send":"","max_deposit":"37667700ugnot"}],"fee":{"gas_wanted":"199761202","gas_fee":"199762ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"Auy1VwQmQltziWh2LuRJj7xKSpD9chk7NirrueqIJYrk"},"signature":"la+RGhFKI2fUMZgXhzdW/ye8LVV3doqLQwp3sbmXqWxQ1+IpKRB/2Yrl/YCo3jn4ceuAuBhV2yaXom4n4z1/lQ=="}],"memo":"mns: nslogic"},"metadata":{"timestamp":"1788750561"}}
{"tx":{"msg":[{"@type":"/vm.m_call","caller":"g174j0dhfww0aam3e3nwk3ys5re7ql4karphlgc6","send":"1183664ugnot","max_deposit":"","pkg_path":"gno.land/r/gnoland/wugnot","func":"Deposit"}],"fee":{"gas_wanted":"336000000","gas_fee":"3360000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"AtgmHtTPMLgcvhAe3eLulrhUBrUN3h7C0+7qY2venYeS"},"signature":"6FjwpeSzCLUO6Z2Lkgk8BEnBwvQVji4vngdTVEkwVVA8P4oxrBf8EOWNd7/qwDxwSOp68GRTnPKbh5h4Xf1NwQ=="}],"memo":""},"metadata":{"timestamp":"1788750614"}}
{"tx":{"msg":[{"@type":"/bank.MsgSend","from_address":"g18qhq2fl54lszhmxeyqlvxnwjzc3xpu4nnakclp","to_address":"g1xr6tgxnpled50h74eafmvxway7z0ytr5rsmeme","amount":"300000000ugnot"}],"fee":{"gas_wanted":"5000000","gas_fee":"1000000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"AhUuKSwiL4XKy0BM9cK0UJFToerTzvgLTHFBvYk/xOtY"},"signature":"Ef1ACOsY/xEG+dkHlWWgM8ScGA8QMELD/zbWGI6XsWtkwWChNLnJYQEM9ViOyr98YoVNwRoqA8brh/vbhlPuYQ=="}],"memo":""},"metadata":{"timestamp":"1788750628"}}
{"tx":{"msg":[{"@type":"/vm.m_call","caller":"g1edv88vm0nsjhgrdudfyezqmat0fhmfpddk3dqw","send":"","max_deposit":"","pkg_path":"gno.land/r/gnoland/wugnot","func":"Withdraw","args":["8170770"]}],"fee":{"gas_wanted":"326400000","gas_fee":"3264000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"A1JP4gqMVa6LVHzkVzUuL03dLDP8w5Q1LIU3CN+sF9o2"},"signature":"sZGjJORnTSccA0jf+cFTByw7D9nCflBQ2bjq1i0YQ+oZl7sZFb0hjcBefr3hJQ47Z3QoOXESM/xn3BTC0djlzA=="}],"memo":""},"metadata":{"timestamp":"1788750638"}}
{"tx":{"msg":[{"@type":"/bank.MsgSend","from_address":"g18qhq2fl54lszhmxeyqlvxnwjzc3xpu4nnakclp","to_address":"g15y9s2wk89m0zfpulhunkkwsvhtksm6842ty2p6","amount":"300000000ugnot"}],"fee":{"gas_wanted":"5000000","gas_fee":"1000000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"AhUuKSwiL4XKy0BM9cK0UJFToerTzvgLTHFBvYk/xOtY"},"signature":"AFSEjFEUPvBcclnysaga8zaJoZw9DmG05wmL/DB6izsg4w9iJtB5A+9A1KWRhDdFfmelPW0QiXWB60VfyqpubA=="}],"memo":""},"metadata":{"timestamp":"1788750705"}}
{"tx":{"msg":[{"@type":"/vm.m_call","caller":"g1lcuw9wfsr4nrn7gldu3zegfauuxd89v8m7v5dv","send":"1165459ugnot","max_deposit":"","pkg_path":"gno.land/r/gnoland/wugnot","func":"Deposit"}],"fee":{"gas_wanted":"390400000","gas_fee":"3904000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"A/pnhKV8PyAJd3Fa9yiWTB+WbPcv2H9d2x95JakKX0cC"},"signature":"5WTDy65UGS8QyZ/CBxhlMjxVNT9QcYc90nw10tVRYxB85WWZmgA+mrAN5rFMUax6cbeoJulsyuF735xt5xCRsg=="}],"memo":""},"metadata":{"timestamp":"1788750757"}}
{"tx":{"msg":[{"@type":"/vm.m_call","caller":"g1s74ffd3uyqze6468rjan4lk7pc7htpdc8zy5d3","send":"2461108ugnot","max_deposit":"","pkg_path":"gno.land/r/gnoland/wugnot","func":"Deposit"}],"fee":{"gas_wanted":"323200000","gas_fee":"3232000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"A67iZsiWQvlHKbXSbGSXvb4zwfaMQ3a2EZwJbek1nSB1"},"signature":"QL9ZBPh4SdvDaJXkne4T4CaKmOdXFnuYyNiNZabRpYZOGhQO72hGPBJloScJ0KER3KUCxTgyM+D7ywygoJgf0g=="}],"memo":""},"metadata":{"timestamp":"1788750922"}}
{"tx":{"msg":[{"@type":"/vm.m_call","caller":"g17txg50ewlzw8hfftx9kj4fxs2xnup6jvk3rl5v","send":"","max_deposit":"","pkg_path":"gno.land/r/gnoland/wugnot","func":"Withdraw","args":["6955049"]}],"fee":{"gas_wanted":"336000000","gas_fee":"3360000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"A9ee4+zhIv5gVGmpNa2uNh86piiTg6VMXXhIIU+wSanB"},"signature":"sSDdB0SywtBcXUR+46CBV9Qm159g9ZVo/k1pPvzS30k6ukEiWBKUHm3oAP5pgVU5KWmrxdrr4cQn7WT/t506nQ=="}],"memo":""},"metadata":{"timestamp":"1788750928"}}
{"tx":{"msg":[{"@type":"/vm.m_call","caller":"g1s74ffd3uyqze6468rjan4lk7pc7htpdc8zy5d3","send":"8552162ugnot","max_deposit":"","pkg_path":"gno.land/r/gnoland/wugnot","func":"Deposit"}],"fee":{"gas_wanted":"361600000","gas_fee":"3616000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"A67iZsiWQvlHKbXSbGSXvb4zwfaMQ3a2EZwJbek1nSB1"},"signature":"LUkO+FmZpC54Lci1FynHcj26fDi52bDArLYiIpt1OJ4zoaZYCk69IxHd06NZNS2Pc/dzBaxMsIo4h09Z8XNc2w=="}],"memo":""},"metadata":{"timestamp":"1788750936"}}
{"tx":{"msg":[{"@type":"/vm.m_call","caller":"g17txg50ewlzw8hfftx9kj4fxs2xnup6jvk3rl5v","send":"2538509ugnot","max_deposit":"","pkg_path":"gno.land/r/gnoland/wugnot","func":"Deposit"}],"fee":{"gas_wanted":"352000000","gas_fee":"3520000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"A9ee4+zhIv5gVGmpNa2uNh86piiTg6VMXXhIIU+wSanB"},"signature":"dCJCRA+RY3TpaNv6Lhxg2QtyLimAQ8+T8D/lwlBR69k0ITyGNG5J19BpRpPA/fFZemj0Hsb6nAEXGz10pkzBmw=="}],"memo":""},"metadata":{"timestamp":"1788750942"}}
{"tx":{"msg":[{"@type":"/bank.MsgSend","from_address":"g18qhq2fl54lszhmxeyqlvxnwjzc3xpu4nnakclp","to_address":"g1z5ampvtqpnxa3hy2zt4st4jlnf9ran7cxyvk4p","amount":"300000000ugnot"}],"fee":{"gas_wanted":"5000000","gas_fee":"1000000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"AhUuKSwiL4XKy0BM9cK0UJFToerTzvgLTHFBvYk/xOtY"},"signature":"bekV33xyHdh5VFc/BoFuj8CaUPaawXQvyralA9Sv0opi2ka3ptMlxJxMJOOL+5wxHfzpOZb3qIoDbzb8WOvBrw=="}],"memo":""},"metadata":{"timestamp":"1788750950"}}
{"tx":{"msg":[{"@type":"/vm.m_call","caller":"g1mfe3du75krzuzgc69d3l2sp2xdvd5qty57l76n","send":"7942578ugnot","max_deposit":"","pkg_path":"gno.land/r/gnoland/wugnot","func":"Deposit"}],"fee":{"gas_wanted":"368000000","gas_fee":"3680000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"AgeRPjntak30O3qui6kCPoV3rBeqaq1MneG5SrnFkAD0"},"signature":"eRRd57wHM+/k8p65XkeYlCHbW/eNotnWJFxK3u6LAUQl3BYqWVhyG9Hx/G5wlCsNxVyFmpW2wRMkxv75HNd4EQ=="}],"memo":""},"metadata":{"timestamp":"1788750990"}}
{"tx":{"msg":[{"@type":"/vm.m_call","caller":"g194tnqwtefz5myaz8fg2wxsdmed393ft9jet4ht","send":"2364725ugnot","max_deposit":"","pkg_path":"gno.land/r/gnoland/wugnot","func":"Deposit"}],"fee":{"gas_wanted":"323200000","gas_fee":"3232000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"AnsoxR2yjrEWDAfth+989spVD6ah4FE7ZQKUvCycQy6b"},"signature":"e2T2i6FDA2RZnTvVcIsaji0YuQae9Jjq7OtdFxpRXM41swqrM1vAAaj9n17w3GxF/I0hPUAI1eAahUxyBSkS4Q=="}],"memo":""},"metadata":{"timestamp":"1788751045"}}
{"tx":{"msg":[{"@type":"/vm.m_call","caller":"g194tnqwtefz5myaz8fg2wxsdmed393ft9jet4ht","send":"","max_deposit":"","pkg_path":"gno.land/r/gnoland/wugnot","func":"Withdraw","args":["3004366"]}],"fee":{"gas_wanted":"377600000","gas_fee":"3776000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"AnsoxR2yjrEWDAfth+989spVD6ah4FE7ZQKUvCycQy6b"},"signature":"eo5Mq1SHaDQZkku7q5R2asbe5AMSBzjvwPauZaqMYy4JG1GfGjgSDXpqBNxkh8hu7KDFvfYVmZLsTWTBIFnTnA=="}],"memo":""},"metadata":{"timestamp":"1788751059"}}
{"tx":{"msg":[{"@type":"/vm.m_call","caller":"g1ag9dgr74hapsahpw5yxj3lmvtm6q3a9cxygkg5","send":"6689808ugnot","max_deposit":"","pkg_path":"gno.land/r/gnoland/wugnot","func":"Deposit"}],"fee":{"gas_wanted":"323200000","gas_fee":"3232000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"Ast0z9c0HD0Wd8e6B5H+8Z7Kh8AtBiiIIWMDPUOymCvD"},"signature":"WRjjW0x5OjvyYxUt1jqA0cO6UcKPH6BidOLysgMu08hm0qFSBRq8iHp4Svrxc+Ln3NzUi9+O7BJC8Uug2oWLbQ=="}],"memo":""},"metadata":{"timestamp":"1788751080"}}
{"tx":{"msg":[{"@type":"/vm.m_call","caller":"g1ag9dgr74hapsahpw5yxj3lmvtm6q3a9cxygkg5","send":"2975231ugnot","max_deposit":"","pkg_path":"gno.land/r/gnoland/wugnot","func":"Deposit"}],"fee":{"gas_wanted":"361600000","gas_fee":"3616000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"Ast0z9c0HD0Wd8e6B5H+8Z7Kh8AtBiiIIWMDPUOymCvD"},"signature":"D9oGnJFOHlWTYA/gkStiKrQWO1b1NYf2JcGH67n/nwFZSOTJEOn/if3UGxCTwQjxwELX/qeF5cz2fvUyQyq4XQ=="}],"memo":""},"metadata":{"timestamp":"1788751090"}}
{"tx":{"msg":[{"@type":"/bank.MsgSend","from_address":"g18qhq2fl54lszhmxeyqlvxnwjzc3xpu4nnakclp","to_address":"g1pkx4mp46j0tdevjtcsel49usm20g824dlmn58z","amount":"300000000ugnot"}],"fee":{"gas_wanted":"5000000","gas_fee":"1000000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"AhUuKSwiL4XKy0BM9cK0UJFToerTzvgLTHFBvYk/xOtY"},"signature":"/QJrlK0ryE7Hz/xUXe6YVfvDXYbMOXtfLJbXyDkWFN0mpazZcnbM+5OApE8XAM4KIPIC01olUiqRQo4e8zgfUw=="}],"memo":""},"metadata":{"timestamp":"1788751097"}}
{"tx":{"msg":[{"@type":"/bank.MsgSend","from_address":"g18qhq2fl54lszhmxeyqlvxnwjzc3xpu4nnakclp","to_address":"g1f28qjlyyn0gy0amst77r4v8te3jxhhuhj77wtc","amount":"300000000ugnot"}],"fee":{"gas_wanted":"5000000","gas_fee":"1000000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"AhUuKSwiL4XKy0BM9cK0UJFToerTzvgLTHFBvYk/xOtY"},"signature":"iafBCmNCeclMZz8lJjFupIKc8nXLx86H5GhQPfIYZL5trFqXZjkmHLSDvzl8F6niZNTm7hWzJ6zA5XMNVadcfg=="}],"memo":""},"metadata":{"timestamp":"1788751111"}}
{"tx":{"msg":[{"@type":"/vm.m_call","caller":"g1qnap4e4va2ynp92y3jjx7uxsekyga4alp5nfv8","send":"20365212ugnot","max_deposit":"","pkg_path":"gno.land/r/gnoland/wugnot","func":"Deposit"}],"fee":{"gas_wanted":"364800000","gas_fee":"3648000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"A2Yo5a3N7z8VjF+Qguul/bgl7n2+/DLybLIsT9mmZf2c"},"signature":"Xoyp5pvN1weY66VlKTJv53kyXvHGMb6EOvGOAIxtTnYZ8XOjW4B+hcTG7NxyC7ow+8Ki6ytHtCr2yreV4d/zpA=="}],"memo":""},"metadata":{"timestamp":"1788751119"}}
{"tx":{"msg":[{"@type":"/bank.MsgSend","from_address":"g18qhq2fl54lszhmxeyqlvxnwjzc3xpu4nnakclp","to_address":"g1yjnz5g4tr3zvus4wlvh839zj7e49vw2dfz79en","amount":"300000000ugnot"}],"fee":{"gas_wanted":"5000000","gas_fee":"1000000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"AhUuKSwiL4XKy0BM9cK0UJFToerTzvgLTHFBvYk/xOtY"},"signature":"NczRJSoy/RihXRVYH7zlq5qpyibXYP4DAmkOzr6IM14sJr8pUo+zcPzUUYrg45aVTQiLt5vHPIHb+jZIuvjb0w=="}],"memo":""},"metadata":{"timestamp":"1788751157"}}
{"tx":{"msg":[{"@type":"/vm.m_call","caller":"g158fug5682fy2z3j20jp36zm0vzn0u0xcfjrvqk","send":"11195935ugnot","max_deposit":"","pkg_path":"gno.land/r/gnoland/wugnot","func":"Deposit"}],"fee":{"gas_wanted":"352000000","gas_fee":"3520000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"AuIENP8r+yRA/+yNyUFR2nKsNWqohhNFeVoXKxH0kvDu"},"signature":"vWOGXm2R0FtvD78l8sJCQYKGGE0M24AZ3f//V6wtZ+F+VH7+sONLvGgJ+YjliK6JEbjz8p7rkcNaM3dz8Z4yJg=="}],"memo":""},"metadata":{"timestamp":"1788751295"}}
{"tx":{"msg":[{"@type":"/vm.m_call","caller":"g1tx07rdup48d6m3aejh7565642ht6ytcjnw542s","send":"3518009ugnot","max_deposit":"","pkg_path":"gno.land/r/gnoland/wugnot","func":"Deposit"}],"fee":{"gas_wanted":"345600000","gas_fee":"3456000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"Azcy1NoR03jqXHCrozWPmn1uwpGay6eICNMPX/uKtMhg"},"signature":"Nw/D/o/2yun1z8PJjDVnMLM7o38cWegn52BrcgJqCg9yg54EwiGDqoOaaiSGAOZql83cSjgd/dqgRWqajQ9IgA=="}],"memo":""},"metadata":{"timestamp":"1788751295"}}
{"tx":{"msg":[{"@type":"/vm.m_call","caller":"g1tx07rdup48d6m3aejh7565642ht6ytcjnw542s","send":"6832798ugnot","max_deposit":"","pkg_path":"gno.land/r/gnoland/wugnot","func":"Deposit"}],"fee":{"gas_wanted":"342400000","gas_fee":"3424000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"Azcy1NoR03jqXHCrozWPmn1uwpGay6eICNMPX/uKtMhg"},"signature":"yuSftT2WlzgYaCFKfipf3ZKrJCVxCRNTChGdjP4t9ZsyjAPcjfKKz05g/5l1HzQqxEqzc8lfPQr1fNiINkzyww=="}],"memo":""},"metadata":{"timestamp":"1788751304"}}
{"tx":{"msg":[{"@type":"/vm.m_call","caller":"g1rzg6p03ycqh6rrpq0ty7vxg67ed8scqr3vs4n8","send":"","max_deposit":"","pkg_path":"gno.land/r/gnoland/wugnot","func":"Withdraw","args":["3771259"]}],"fee":{"gas_wanted":"352000000","gas_fee":"3520000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"AvNp3rfHWYvUNgN0iWEWx1ypYbWU7Lp+G6mXHsujyoiZ"},"signature":"IsGbaZ2jNW4h3ZNLIKAp3BF195D/bzOEY1F1OqJ/vl4ROywpVZb5fm2tIxhxlbkZWCdOYmzycwJlc41XFq2MTA=="}],"memo":""},"metadata":{"timestamp":"1788751318"}}
{"tx":{"msg":[{"@type":"/bank.MsgSend","from_address":"g18qhq2fl54lszhmxeyqlvxnwjzc3xpu4nnakclp","to_address":"g1kjl7fm86kpa7ntq29v7643cv5ss87nz5gk7g4c","amount":"300000000ugnot"}],"fee":{"gas_wanted":"5000000","gas_fee":"1000000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"AhUuKSwiL4XKy0BM9cK0UJFToerTzvgLTHFBvYk/xOtY"},"signature":"2DcKFeJJyM+pKGMXLCUTR+SiJvyS0+lUK4CQzw+God1Tv6w/9M/C/JV8bRXDfGPDE2BJtdsw+o4NZDh1d0yUjg=="}],"memo":""},"metadata":{"timestamp":"1788751349"}}
{"tx":{"msg":[{"@type":"/vm.m_call","caller":"g1umk6qsmfre9fe06hg4798tlsdcqj20yy3hep6z","send":"1905267ugnot","max_deposit":"","pkg_path":"gno.land/r/gnoland/wugnot","func":"Deposit"}],"fee":{"gas_wanted":"393600000","gas_fee":"3936000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"A/O/+dA70GO93tLxuIA4Hgtc26upqCVmL+T3ldwJ2gHB"},"signature":"ioG8exWnsHnPVGAGmZOE7x9P10Z6eljl5UsXD42++Uh4cehsGWc7jCOXEB6pbM+kOiBPQpyJR7ogFBIwaZdIXA=="}],"memo":""},"metadata":{"timestamp":"1788751412"}}
{"tx":{"msg":[{"@type":"/vm.m_call","caller":"g1umk6qsmfre9fe06hg4798tlsdcqj20yy3hep6z","send":"2423458ugnot","max_deposit":"","pkg_path":"gno.land/r/gnoland/wugnot","func":"Deposit"}],"fee":{"gas_wanted":"348800000","gas_fee":"3488000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"A/O/+dA70GO93tLxuIA4Hgtc26upqCVmL+T3ldwJ2gHB"},"signature":"+FUq9z2eCJD/sLSDd3nzbdyYMYp4yyvL2lzvGVpyMIhiYK3/aNmItkeapQNwj+WsrG61AtoJqdUApb4JTe97Qw=="}],"memo":""},"metadata":{"timestamp":"1788751422"}}
{"tx":{"msg":[{"@type":"/vm.m_call","caller":"g1k2dctt2ez4ywcg6yteupk4kxt7ls6ay76fluks","send":"9097317ugnot","max_deposit":"","pkg_path":"gno.land/r/gnoland/wugnot","func":"Deposit"}],"fee":{"gas_wanted":"368000000","gas_fee":"3680000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"Aw0vCW3IUJkaM7ivSW1HcS6nDfQAtwpY3bgHLIC7+f3T"},"signature":"ZijhU1SHcTzRuuv4pTtumS5GaRf57Ssk+W1943+M+hFiAhSILAegyH1498psosRnYvXKRxAEAz/aJ62K1rBKAQ=="}],"memo":""},"metadata":{"timestamp":"1788751452"}}
{"tx":{"msg":[{"@type":"/vm.m_call","caller":"g1yzuw807fxchnhgjt66wfky55s83f92qdzdevr4","send":"1178310ugnot","max_deposit":"","pkg_path":"gno.land/r/gnoland/wugnot","func":"Deposit"}],"fee":{"gas_wanted":"361600000","gas_fee":"3616000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"AxJZ5Mc5Vv/rIsTUwWgSxJobBqM0qjJfPf+zdtvOLBn1"},"signature":"AcROIruV9vQmLoL0thIxp16iqxU8SG946C0oGn8ZNZJA8HoDHUJxBXfsiq2Vl+WEXJYkksxc4PCgCjnaT1dmXw=="}],"memo":""},"metadata":{"timestamp":"1788751456"}}
{"tx":{"msg":[{"@type":"/vm.m_call","caller":"g1k2dctt2ez4ywcg6yteupk4kxt7ls6ay76fluks","send":"13059999ugnot","max_deposit":"","pkg_path":"gno.land/r/gnoland/wugnot","func":"Deposit"}],"fee":{"gas_wanted":"374400000","gas_fee":"3744000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"Aw0vCW3IUJkaM7ivSW1HcS6nDfQAtwpY3bgHLIC7+f3T"},"signature":"hqT4UHlPwV2j0SkEQeWHa3C/9O5pUhNLcprRsP86h3VquaWPpZqXE49DKBdGO0CiVtX/b/P5xRWzGGsq61imYQ=="}],"memo":""},"metadata":{"timestamp":"1788751463"}}
{"tx":{"msg":[{"@type":"/vm.m_call","caller":"g1yzuw807fxchnhgjt66wfky55s83f92qdzdevr4","send":"3929066ugnot","max_deposit":"","pkg_path":"gno.land/r/gnoland/wugnot","func":"Deposit"}],"fee":{"gas_wanted":"390400000","gas_fee":"3904000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"AxJZ5Mc5Vv/rIsTUwWgSxJobBqM0qjJfPf+zdtvOLBn1"},"signature":"nbGKdHRPO1lekjPQ2bQbtK35AAWQRkqfo8A1bFz06pxeBvwLJ50YjrvtnkXOuPWloA3szkajoCGiCbbxlAQOKw=="}],"memo":""},"metadata":{"timestamp":"1788751469"}}
{"tx":{"msg":[{"@type":"/vm.m_call","caller":"g1zx3lrxrf7t69zwaah7f7l2phmhklvm8j7e7mjv","send":"","max_deposit":"","pkg_path":"gno.land/r/gnoland/wugnot","func":"Withdraw","args":["6015940"]}],"fee":{"gas_wanted":"352000000","gas_fee":"3520000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"Apu2lW0qau7Y/FBh+AUI65IGQieRaS8NNl9D9n9ImADb"},"signature":"8io/e6ujk4OG+QwKFgPAbS4xAb2ZyqVDJfxeEVOADOl8r1xd0gqNLTj1YpTissVhfILGjj+WWdpS/3pxkz8zSg=="}],"memo":""},"metadata":{"timestamp":"1788751532"}}
{"tx":{"msg":[{"@type":"/vm.m_call","caller":"g1zx3lrxrf7t69zwaah7f7l2phmhklvm8j7e7mjv","send":"","max_deposit":"","pkg_path":"gno.land/r/gnoland/wugnot","func":"Withdraw","args":["1100206"]}],"fee":{"gas_wanted":"352000000","gas_fee":"3520000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"Apu2lW0qau7Y/FBh+AUI65IGQieRaS8NNl9D9n9ImADb"},"signature":"9unQG3LJ9Kytf7K5+M0pkcB273z6DLY36LMVe1v/41osZs8/YoQZwlvu2z7boBzlbWZV0DlcOLF6gXN0t+ZkTQ=="}],"memo":""},"metadata":{"timestamp":"1788751542"}}
{"tx":{"msg":[{"@type":"/vm.m_call","caller":"g17szjdkj73eqfmdda9y4czm39atkmgc2mvx3r72","send":"21866720ugnot","max_deposit":"","pkg_path":"gno.land/r/gnoland/wugnot","func":"Deposit"}],"fee":{"gas_wanted":"393600000","gas_fee":"3936000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"Aqi+3cz785E/ZciBTlMO5E09kbrk/ceXcJK/V5sljHCM"},"signature":"fmEQwgMRqtPc8TyTvsxaXxkEC7uaoLCbk8AE9IHf9/5ad8ndhzAzkCBxfhnBIZkP2S58lOwfizrSSPBHP2cRMA=="}],"memo":""},"metadata":{"timestamp":"1788751574"}}
{"tx":{"msg":[{"@type":"/vm.m_call","caller":"g17szjdkj73eqfmdda9y4czm39atkmgc2mvx3r72","send":"6773771ugnot","max_deposit":"","pkg_path":"gno.land/r/gnoland/wugnot","func":"Deposit"}],"fee":{"gas_wanted":"371200000","gas_fee":"3712000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"Aqi+3cz785E/ZciBTlMO5E09kbrk/ceXcJK/V5sljHCM"},"signature":"Omkkh/xFF66S0zmn+2coeqRFE71u5mkIrS8aZa9zEI5TfMzvc78Swqp+2EM1b7OtakXs+qXMjJsJTscLoorejg=="}],"memo":""},"metadata":{"timestamp":"1788751584"}}
{"tx":{"msg":[{"@type":"/bank.MsgSend","from_address":"g18qhq2fl54lszhmxeyqlvxnwjzc3xpu4nnakclp","to_address":"g1dx2fuy3vlzg3znrgh9zuhzk96ezazmmwf6425x","amount":"300000000ugnot"}],"fee":{"gas_wanted":"5000000","gas_fee":"1000000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"AhUuKSwiL4XKy0BM9cK0UJFToerTzvgLTHFBvYk/xOtY"},"signature":"10fOWqvQzn01RigKBK8+X2s9mYjG1t+W2EQ2c26r6Z8pF9IbgxKhBRicc/Oo7YuDZ6JTSa+Rma9Uu+wkvnijyA=="}],"memo":""},"metadata":{"timestamp":"1788751639"}}
{"tx":{"msg":[{"@type":"/bank.MsgSend","from_address":"g18qhq2fl54lszhmxeyqlvxnwjzc3xpu4nnakclp","to_address":"g1tyyk7lmgcw70wn38y039lc4gzwg0ltxd2c0h39","amount":"300000000ugnot"}],"fee":{"gas_wanted":"5000000","gas_fee":"1000000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"AhUuKSwiL4XKy0BM9cK0UJFToerTzvgLTHFBvYk/xOtY"},"signature":"s3pEwdp6TmsZXRX2MkLVq86C/FqpGB/yyLpN7+IBoJ0OtE+t+K8PA0SdFZeDm/zviC0El5FBH68onV74z+Ipew=="}],"memo":""},"metadata":{"timestamp":"1788751692"}}
{"tx":{"msg":[{"@type":"/vm.m_call","caller":"g1ns5wn04wgwjnwwe0gytg98pp2kd9ad2wvlsf4c","send":"","max_deposit":"","pkg_path":"gno.land/r/gnoland/wugnot","func":"Withdraw","args":["1487656"]}],"fee":{"gas_wanted":"390400000","gas_fee":"3904000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"A2LMS85Zp+WVAWlC/DxxUe4U536XjuSdFKsuhQFawfRD"},"signature":"zlenGRylec3xG9d6sd6Hilzmkw/GS95fuXj9IeoTsXpyxA4xpXO9jH6sPf2Wd05v7/M9ki4TUEB3Zxq7B1/sGA=="}],"memo":""},"metadata":{"timestamp":"1788751706"}}
{"tx":{"msg":[{"@type":"/vm.m_call","caller":"g1fscz904w27pc4aulfseqxtlw8uk83wwxqn4fej","send":"","max_deposit":"","pkg_path":"gno.land/r/gnoland/wugnot","func":"Withdraw","args":["11990863"]}],"fee":{"gas_wanted":"371200000","gas_fee":"3712000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"A7ZV0NHQUWSjj143TPDEGiSLcto/LwE4VVdhjSIa9+gu"},"signature":"pbDDFO9oA5FmNYKG0+OYnHGxS4tq65ya3nc9n5rjGOpKIFAWoc80oR6DY+cGg2HFuyACgOD4MLYxgvrgaG92VA=="}],"memo":""},"metadata":{"timestamp":"1788751744"}}
{"tx":{"msg":[{"@type":"/vm.m_call","caller":"g1ssurvpkre7hqdzel5rya4xhrd4ecjrg9430y3f","send":"5747461ugnot","max_deposit":"","pkg_path":"gno.land/r/gnoland/wugnot","func":"Deposit"}],"fee":{"gas_wanted":"400000000","gas_fee":"4000000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"Amc5REB4cfUuAApVCZmCInywAkUzTtgEV5R2AlGB1zAU"},"signature":"5g9wq+Otnf8aPDdAFrpzg+hpa/W1L5rxyJiy2akVI2MJFlD/719GiPW+NcyAUiDzxm0S2wvY6UX/KysZj1ptmQ=="}],"memo":""},"metadata":{"timestamp":"1788751758"}}
{"tx":{"msg":[{"@type":"/vm.m_call","caller":"g1fscz904w27pc4aulfseqxtlw8uk83wwxqn4fej","send":"","max_deposit":"","pkg_path":"gno.land/r/gnoland/wugnot","func":"Withdraw","args":["11641185"]}],"fee":{"gas_wanted":"400000000","gas_fee":"4000000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"A7ZV0NHQUWSjj143TPDEGiSLcto/LwE4VVdhjSIa9+gu"},"signature":"i2nJefPLO3ouPnxzM5YMvPMwp49PpW/96QnZDa7my397vwr+eHRKY5nj8L2P7qTxp7lesIeiDtv2qg9SfC+2mQ=="}],"memo":""},"metadata":{"timestamp":"1788751758"}}
{"tx":{"msg":[{"@type":"/bank.MsgSend","from_address":"g18qhq2fl54lszhmxeyqlvxnwjzc3xpu4nnakclp","to_address":"g1vd50l8rzv33nkhrty59xmptz3754tty4yaajkn","amount":"300000000ugnot"}],"fee":{"gas_wanted":"5000000","gas_fee":"1000000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"AhUuKSwiL4XKy0BM9cK0UJFToerTzvgLTHFBvYk/xOtY"},"signature":"UoqokOtho8ana7Aqd3LIwoHRkhz8aRupIAClbEWhPEI901GBvXCzXV31ni3XldZ2XA7QJWcCAy7EWiAXY9fmTA=="}],"memo":""},"metadata":{"timestamp":"1788751791"}}
{"tx":{"msg":[{"@type":"/vm.m_call","caller":"g1mrf9zdmug9qsardrl87vcyk6ad8z4ty5l26tht","send":"","max_deposit":"","pkg_path":"gno.land/r/gnoland/wugnot","func":"Withdraw","args":["4800338"]}],"fee":{"gas_wanted":"329600000","gas_fee":"3296000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"A3mo2nlbsU+dNDaFyWPgetl+ltsYf1zdC+XMzXLaCYFH"},"signature":"PYxZmOBwX21TnGJ7YqqP3KWAfylIPqM7l5iSq3ypDjNBKEMT48B2K+4PGdp/sfb5J7LKIGwrXFiF3NoaoIOuow=="}],"memo":""},"metadata":{"timestamp":"1788751895"}}
{"tx":{"msg":[{"@type":"/vm.m_call","caller":"g1mrf9zdmug9qsardrl87vcyk6ad8z4ty5l26tht","send":"12146496ugnot","max_deposit":"","pkg_path":"gno.land/r/gnoland/wugnot","func":"Deposit"}],"fee":{"gas_wanted":"377600000","gas_fee":"3776000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"A3mo2nlbsU+dNDaFyWPgetl+ltsYf1zdC+XMzXLaCYFH"},"signature":"aT5Wgtf4JvXQpnu4LK0+32kOn0gZUgb9jctltTU574ADatK6/Ux0YBKYaoekET6Z+8RZXRTBLl7CiMhi3XpODw=="}],"memo":""},"metadata":{"timestamp":"1788751911"}}
{"tx":{"msg":[{"@type":"/vm.m_call","caller":"g1chtejncjy86r978qhgzcfefv9f7s9pkhc0emdt","send":"12602942ugnot","max_deposit":"","pkg_path":"gno.land/r/gnoland/wugnot","func":"Deposit"}],"fee":{"gas_wanted":"326400000","gas_fee":"3264000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"AkFO2zRw7jRFoBX3vumkkXIPJYczfJBJfmnyXwuPzbJ0"},"signature":"p19S5IPftYItzH13LwyQxc4ZpIGgmX8CX57YLFy1+m8Yy0PKuDLFzAbgfypZ5K9p+LtazOJjnQ/OmpwhGXI5Ww=="}],"memo":""},"metadata":{"timestamp":"1788751935"}}
{"tx":{"msg":[{"@type":"/vm.m_call","caller":"g1kszk3wd5r6l5f0l7va7uy6ael95t6a8fu3y7l2","send":"12330458ugnot","max_deposit":"","pkg_path":"gno.land/r/gnoland/wugnot","func":"Deposit"}],"fee":{"gas_wanted":"364800000","gas_fee":"3648000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"A2AfcHH/gW+QrVhF9ykVRb/Cn5zwZGIVlgNzsnRuZTrL"},"signature":"Kf69MA/sSr5sCgfNjOZqCCqDnQnFOmu2V0kbdLxmcFp/Y2zwKe11kDus8KyOqj3iVLa4+gZ/I7g/I/XLrqCJfA=="}],"memo":""},"metadata":{"timestamp":"1788751935"}}
{"tx":{"msg":[{"@type":"/vm.m_call","caller":"g1qsmlzeqppf9klmhn9h4fg6l05hnn09auxqme83","send":"12758958ugnot","max_deposit":"","pkg_path":"gno.land/r/gnoland/wugnot","func":"Deposit"}],"fee":{"gas_wanted":"336000000","gas_fee":"3360000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"AyUksj5tLAqDuaaFhpf3uixQRyzfCBAD//DW44Uhbb+q"},"signature":"KhGiUMl3XTzcvDw31B3VVkStidAl77YUIu6zKuea1ZUqj2nnd0YdQB860H8sE0scr4gAVK2OrjYIV35h0JYxsA=="}],"memo":""},"metadata":{"timestamp":"1788751939"}}
{"tx":{"msg":[{"@type":"/vm.m_call","caller":"g1chtejncjy86r978qhgzcfefv9f7s9pkhc0emdt","send":"","max_deposit":"","pkg_path":"gno.land/r/gnoland/wugnot","func":"Withdraw","args":["3577700"]}],"fee":{"gas_wanted":"358400000","gas_fee":"3584000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"AkFO2zRw7jRFoBX3vumkkXIPJYczfJBJfmnyXwuPzbJ0"},"signature":"mGozpPkm6rUhjBKqwzlQdeGszL9AViyCn4mJn/6dnMwaMVPvUcJhQXZN0BntsD+XkR/zJoxdux38iBbffC4HIQ=="}],"memo":""},"metadata":{"timestamp":"1788751942"}}
{"tx":{"msg":[{"@type":"/vm.m_call","caller":"g1kszk3wd5r6l5f0l7va7uy6ael95t6a8fu3y7l2","send":"","max_deposit":"","pkg_path":"gno.land/r/gnoland/wugnot","func":"Withdraw","args":["8282542"]}],"fee":{"gas_wanted":"361600000","gas_fee":"3616000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"A2AfcHH/gW+QrVhF9ykVRb/Cn5zwZGIVlgNzsnRuZTrL"},"signature":"gOZfngpH3mwj24Bw3qdATniW8tKNbjp631KJn2+0rfQlMFofxxcYxE3/8JFYmPwlo7vj6OMG5pP0JKVXIO8mNA=="}],"memo":""},"metadata":{"timestamp":"1788751949"}}
{"tx":{"msg":[{"@type":"/bank.MsgSend","from_address":"g18qhq2fl54lszhmxeyqlvxnwjzc3xpu4nnakclp","to_address":"g1tlljkncyxtwmfdl9vpm38uq2mq55mdesek2ttr","amount":"300000000ugnot"}],"fee":{"gas_wanted":"5000000","gas_fee":"1000000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"AhUuKSwiL4XKy0BM9cK0UJFToerTzvgLTHFBvYk/xOtY"},"signature":"3gEYrmF5amSql46kkw5dukzKPUI9k12Ld5BjO2zM/hAQ3gNwLHpWxo/K9/tgnEN+3Dg12wcxvSI27KPTbheu4Q=="}],"memo":""},"metadata":{"timestamp":"1788751963"}}
{"tx":{"msg":[{"@type":"/vm.m_call","caller":"g1zapgwvpa72t9tgw95lwulz2ncptgy4mhfkajkm","send":"2300494ugnot","max_deposit":"","pkg_path":"gno.land/r/gnoland/wugnot","func":"Deposit"}],"fee":{"gas_wanted":"396800000","gas_fee":"3968000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"ArT/GLVVMet3EhSKlaSXttAwJ3lIU5GHVFHan57/VbVr"},"signature":"9CLpxGjCDWNAUG43ZUKe4P6djcOpA9N/bTLtWWrIa8RbmW1Z1N/Ng+e2ZecHtGbpv1VyCFex7zfhho1eOqO/Aw=="}],"memo":""},"metadata":{"timestamp":"1788751994"}}
{"tx":{"msg":[{"@type":"/vm.m_call","caller":"g1zapgwvpa72t9tgw95lwulz2ncptgy4mhfkajkm","send":"","max_deposit":"","pkg_path":"gno.land/r/gnoland/wugnot","func":"Withdraw","args":["3469383"]}],"fee":{"gas_wanted":"377600000","gas_fee":"3776000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"ArT/GLVVMet3EhSKlaSXttAwJ3lIU5GHVFHan57/VbVr"},"signature":"PKSmWt5oNG3I0XKZWwMu7KDJias4WfydaZTs05HINCJ+6avd6pnfAFDDXMjSO4zs0rJHbWC6ZX3bsQtDt2Oc6A=="}],"memo":""},"metadata":{"timestamp":"1788752004"}}
{"tx":{"msg":[{"@type":"/bank.MsgSend","from_address":"g18qhq2fl54lszhmxeyqlvxnwjzc3xpu4nnakclp","to_address":"g1n70hju0sew704tmzfxkhy64uer6prjsm2xwlxs","amount":"300000000ugnot"}],"fee":{"gas_wanted":"5000000","gas_fee":"1000000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"AhUuKSwiL4XKy0BM9cK0UJFToerTzvgLTHFBvYk/xOtY"},"signature":"uqltNeJTvgxb/MzkaCg5ipuI2egyc7lNZnKx6DpO4gVaI5FznVhs2zxRHD5N6hmINbWAjCF4muW+PSzn+9j4bQ=="}],"memo":""},"metadata":{"timestamp":"1788752039"}}
{"tx":{"msg":[{"@type":"/vm.m_call","caller":"g18e95q83hyevdfraphkvx6hpdpdjeck2cgyspaq","send":"","max_deposit":"","pkg_path":"gno.land/r/gnoland/wugnot","func":"Withdraw","args":["2414380"]}],"fee":{"gas_wanted":"393600000","gas_fee":"3936000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"A2AgBF2R20vNJuaekSSdWNweoYymb1rYWUqM2MEQeQjD"},"signature":"Cm9vaePIaAWyrGnHsh3wWSAJrVUG7J0GpjVKf9gpn01/Gd0tErGf5c/zcQQyEhAMEk8g4SkSQoxLWsm+R7ZO9A=="}],"memo":""},"metadata":{"timestamp":"1788752049"}}
{"tx":{"msg":[{"@type":"/vm.m_call","caller":"g1vrh6d3pmdz0tsyp4lj9raglyy3suw9skzyf365","send":"1931732ugnot","max_deposit":"","pkg_path":"gno.land/r/gnoland/wugnot","func":"Deposit"}],"fee":{"gas_wanted":"361600000","gas_fee":"3616000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"AzMRrAGSs0lL/ID+x1Ch4NhKpnjSNKfDW+/RTS+Xupnp"},"signature":"1o69obKV4gCX3GLZn82VJ/HmqbCe9ys8qC8rUtO6eqhnQrQgoNOmBvERlyKH42B/e6xbwdwFJ7730L4qK7kotg=="}],"memo":""},"metadata":{"timestamp":"1788752088"}}
{"tx":{"msg":[{"@type":"/vm.m_call","caller":"g1kfr9z249p57j6lst2vgs0t33qjpngyuv7vcnql","send":"18386783ugnot","max_deposit":"","pkg_path":"gno.land/r/gnoland/wugnot","func":"Deposit"}],"fee":{"gas_wanted":"384000000","gas_fee":"3840000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"AwVNa3fIQSc87qliWxYeFtDogatehoiaP1PU6EzqjgTG"},"signature":"uDNYFgqRgCtVdTmIewuR8HSPHNVS/sdreuTkv6goHKZi556pDN1tKChn3+CnQ78YrA3wV+doVCF2lIYSSkCcRQ=="}],"memo":""},"metadata":{"timestamp":"1788752095"}}
{"tx":{"msg":[{"@type":"/vm.m_call","caller":"g1kfr9z249p57j6lst2vgs0t33qjpngyuv7vcnql","send":"","max_deposit":"","pkg_path":"gno.land/r/gnoland/wugnot","func":"Withdraw","args":["11048941"]}],"fee":{"gas_wanted":"371200000","gas_fee":"3712000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"AwVNa3fIQSc87qliWxYeFtDogatehoiaP1PU6EzqjgTG"},"signature":"/y850NMww+Z2mrUpq5MP1vBkVuymwVVVO/XqlFhkjuoQyPNpXebYsxCrMbAGyRkfObVr9qp/N56LfjNInh9pSA=="}],"memo":""},"metadata":{"timestamp":"1788752105"}}
{"tx":{"msg":[{"@type":"/vm.m_call","caller":"g13kasye4lrj5c5vzzzjgf68r897v2st7s04855g","send":"2610003ugnot","max_deposit":"","pkg_path":"gno.land/r/gnoland/wugnot","func":"Deposit"}],"fee":{"gas_wanted":"326400000","gas_fee":"3264000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"Av3EbEAMHWed+D5qT1N7u6BlDPlpFB1KuSRwNSq9fd7u"},"signature":"o3hBBR7b1DA3tzXKOjiJw0xCOkzXEGmKLZVVvwu1gR9O8JtC1xD1iDfmNa+eirCeUE1BkCmnqbBJXNv/AlGnGw=="}],"memo":""},"metadata":{"timestamp":"1788752112"}}
{"tx":{"msg":[{"@type":"/vm.m_call","caller":"g13kasye4lrj5c5vzzzjgf68r897v2st7s04855g","send":"","max_deposit":"","pkg_path":"gno.land/r/gnoland/wugnot","func":"Withdraw","args":["8763648"]}],"fee":{"gas_wanted":"361600000","gas_fee":"3616000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"Av3EbEAMHWed+D5qT1N7u6BlDPlpFB1KuSRwNSq9fd7u"},"signature":"Blf5/J5tmgw6pPL0Z8ged5wG4foy6IDGqXzMVLuLD3toFuM67hG5S57JBn2JKD5qaOjJ64TVQt0+K2Msl7IHqg=="}],"memo":""},"metadata":{"timestamp":"1788752126"}}
{"tx":{"msg":[{"@type":"/bank.MsgSend","from_address":"g18qhq2fl54lszhmxeyqlvxnwjzc3xpu4nnakclp","to_address":"g13h0yvu24grhsa9r9cw30x37sx9k6lau38c5kek","amount":"300000000ugnot"}],"fee":{"gas_wanted":"5000000","gas_fee":"1000000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"AhUuKSwiL4XKy0BM9cK0UJFToerTzvgLTHFBvYk/xOtY"},"signature":"wUGZ8SeYGE+nKmJTPEVNMroAr+epLGmHu0Rv4PsdQXB6s4H3IREn73jaAvQdojn6pHon85+HueB6OoybRuwSJA=="}],"memo":""},"metadata":{"timestamp":"1788752240"}}
{"tx":{"msg":[{"@type":"/bank.MsgSend","from_address":"g18qhq2fl54lszhmxeyqlvxnwjzc3xpu4nnakclp","to_address":"g1ufnwykqpvuagshlzx7d88frpgsd2sfcfsravus","amount":"300000000ugnot"}],"fee":{"gas_wanted":"5000000","gas_fee":"1000000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"AhUuKSwiL4XKy0BM9cK0UJFToerTzvgLTHFBvYk/xOtY"},"signature":"qwAo4PtEft/GHdNUeBqIv72UZVK40pThL7mIWF1OKX0Q57xo4HdwlfFS1wPsLzKC2iCfXUZbBAm/NAv91JCP+A=="}],"memo":""},"metadata":{"timestamp":"1788752277"}}
{"tx":{"msg":[{"@type":"/vm.m_call","caller":"g1wmrn0whw5yv3p5fs977hhjx79kr5zgxeh70vtl","send":"11296799ugnot","max_deposit":"","pkg_path":"gno.land/r/gnoland/wugnot","func":"Deposit"}],"fee":{"gas_wanted":"332800000","gas_fee":"3328000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"ApU7iNL4Y8VFMXt/hBjYTpkc2O+JwVYTpeWdCmfblLX4"},"signature":"6AOc3KuvZZnGIn6vKKXIb8Q0bZ731z5ZywS7spftL6tlqH5Op7MUdmeKOohSGUg7bPQn/h1BGtFLBReHC4gCmw=="}],"memo":""},"metadata":{"timestamp":"1788752280"}}
{"tx":{"msg":[{"@type":"/vm.m_call","caller":"g1wmrn0whw5yv3p5fs977hhjx79kr5zgxeh70vtl","send":"17481147ugnot","max_deposit":"","pkg_path":"gno.land/r/gnoland/wugnot","func":"Deposit"}],"fee":{"gas_wanted":"374400000","gas_fee":"3744000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"ApU7iNL4Y8VFMXt/hBjYTpkc2O+JwVYTpeWdCmfblLX4"},"signature":"st8XqdmLsCimmB77VmVUgMPacXssKQUZWeX5ynNOafUwCwsnwRfxNYweZ5j/RwgnfLGej3w5hspQQkpHPD8i3Q=="}],"memo":""},"metadata":{"timestamp":"1788752294"}}
{"tx":{"msg":[{"@type":"/vm.m_call","caller":"g1pw8ty6ldcraxsqp3hfqlagqwqgc5vant0lfd2x","send":"5477896ugnot","max_deposit":"","pkg_path":"gno.land/r/gnoland/wugnot","func":"Deposit"}],"fee":{"gas_wanted":"355200000","gas_fee":"3552000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"A4E1HWJePsvytoyI8Ef+grqRpiQ/Zb2AuOEhxU1qFXr8"},"signature":"njyln/zdUKq8NkfkJMbfXLVwGwDfPFLzz7d/TE0tgJFR9kAiuGMfRUb9mSqX5OJ7Fm1b83sRKTyBb2KCz0fN9A=="}],"memo":""},"metadata":{"timestamp":"1788752329"}}
{"tx":{"msg":[{"@type":"/vm.m_call","caller":"g1pw8ty6ldcraxsqp3hfqlagqwqgc5vant0lfd2x","send":"","max_deposit":"","pkg_path":"gno.land/r/gnoland/wugnot","func":"Withdraw","args":["6617442"]}],"fee":{"gas_wanted":"332800000","gas_fee":"3328000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"A4E1HWJePsvytoyI8Ef+grqRpiQ/Zb2AuOEhxU1qFXr8"},"signature":"gySGiD424oVT7EzwUxjjVPAvShPC517fIIBABU0Gjvxdd366uKlX3KAihww3CeosC0e43x293dTkUhxEukPu5g=="}],"memo":""},"metadata":{"timestamp":"1788752343"}}
{"tx":{"msg":[{"@type":"/bank.MsgSend","from_address":"g18qhq2fl54lszhmxeyqlvxnwjzc3xpu4nnakclp","to_address":"g1ve7vkc2cmnn4kfj9yh3xa0u7gavrm52g2c3r9s","amount":"300000000ugnot"}],"fee":{"gas_wanted":"5000000","gas_fee":"1000000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"AhUuKSwiL4XKy0BM9cK0UJFToerTzvgLTHFBvYk/xOtY"},"signature":"+u0xfAmx3VV97fIcKcu563tHnQs9WqcIGeXC8z/aQV1EHH6M+kTvjytKC4Db9xhJvfrtCMyj/jbVMOWdkVLRLg=="}],"memo":""},"metadata":{"timestamp":"1788752377"}}
{"tx":{"msg":[{"@type":"/vm.m_call","caller":"g1dhhcgygcjtk2337ey609s0u654lkaqwm8ez9fl","send":"","max_deposit":"","pkg_path":"gno.land/r/gnoland/wugnot","func":"Withdraw","args":["6531967"]}],"fee":{"gas_wanted":"393600000","gas_fee":"3936000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"AtQq4gGmrEmuO02cdvywaTVxTjyQKDZrslH4betNUyC7"},"signature":"gB+jEKqSZl02MB3v0h/PmvI8rNzsTD61vMb/ZaKsFG4ltkFh0ErbaK1a+PGbGqOHRAD8fQbt9XUdTbFnAtMfHA=="}],"memo":""},"metadata":{"timestamp":"1788752450"}}
{"tx":{"msg":[{"@type":"/vm.m_call","caller":"g1dhhcgygcjtk2337ey609s0u654lkaqwm8ez9fl","send":"","max_deposit":"","pkg_path":"gno.land/r/gnoland/wugnot","func":"Withdraw","args":["5205529"]}],"fee":{"gas_wanted":"387200000","gas_fee":"3872000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"AtQq4gGmrEmuO02cdvywaTVxTjyQKDZrslH4betNUyC7"},"signature":"S6mp48yLMKQOJVLL3eNRkDmPSv5Aj5un9ZqUmm+Kjk4sWGEM75fctWbl+q4vYxHxctmeI+RmgRn038qHMdsXYw=="}],"memo":""},"metadata":{"timestamp":"1788752460"}}
{"tx":{"msg":[{"@type":"/vm.m_call","caller":"g1h0ektm7xm8lykctxlqllh55vpwd7vyrkawt90l","send":"","max_deposit":"","pkg_path":"gno.land/r/gnoland/wugnot","func":"Withdraw","args":["5941919"]}],"fee":{"gas_wanted":"396800000","gas_fee":"3968000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"Ajwyq8Ie4l+XszHj6M8bk538UbQsj3s4Ql25AKqvoANX"},"signature":"AveaFdqC1g7bhitPsjvQTom28zm59d+NMAYzUBuKlYh2k+7fCi9h9jtkRfMmfW15O2M5Tc1MoS4M71RAg+GvVw=="}],"memo":""},"metadata":{"timestamp":"1788752464"}}
{"tx":{"msg":[{"@type":"/vm.m_call","caller":"g1p8htnsydtwu3g6qqn5qt09830zqje8n33gpx6p","send":"20333403ugnot","max_deposit":"","pkg_path":"gno.land/r/gnoland/wugnot","func":"Deposit"}],"fee":{"gas_wanted":"329600000","gas_fee":"3296000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"A3VSes//hRhg2Yyllq/usetCpC1K+IU5AtFCXQcP+UtU"},"signature":"gzgPgzJWUC1ENdxUNBtI+vYEaClD/WajXNhmHw5KYg465QhYG9qRWjeZbeAGV0qzrNrkfPj5CQbVooXtcMHcYQ=="}],"memo":""},"metadata":{"timestamp":"1788752474"}}
{"tx":{"msg":[{"@type":"/vm.m_call","caller":"g1h0ektm7xm8lykctxlqllh55vpwd7vyrkawt90l","send":"7289571ugnot","max_deposit":"","pkg_path":"gno.land/r/gnoland/wugnot","func":"Deposit"}],"fee":{"gas_wanted":"380800000","gas_fee":"3808000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"Ajwyq8Ie4l+XszHj6M8bk538UbQsj3s4Ql25AKqvoANX"},"signature":"3v+XTlAD7pZfHZVtUNMwEVJGZmWu6NtfDI0qrfZ9xTonLTsr3sqFVhoUoUThE2/g13sPNkXdp3UxEqjvjZlcSw=="}],"memo":""},"metadata":{"timestamp":"1788752477"}}
{"tx":{"msg":[{"@type":"/vm.m_call","caller":"g10f7zcd72y93jy2ss3wqxva0r4ld7sh2acddz9r","send":"","max_deposit":"","pkg_path":"gno.land/r/gnoland/wugnot","func":"Withdraw","args":["9307997"]}],"fee":{"gas_wanted":"352000000","gas_fee":"3520000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"Awla9Dt8SNEBYmqtof+9kgi5XakNVu1JtLC5CdosI2bO"},"signature":"eAo9SUsY/yEs/cd2brHw4pbvqZ8BI5rrVT8suEWuDN9v8ufi/qLWIkQ5q1XXlmvU3DHvpDsIshVZMTD7qliNgA=="}],"memo":""},"metadata":{"timestamp":"1788752509"}}
{"tx":{"msg":[{"@type":"/bank.MsgSend","from_address":"g18qhq2fl54lszhmxeyqlvxnwjzc3xpu4nnakclp","to_address":"g1wrh0j6p4u4hgswpf80zt87nxpe3yg0g7wsqvrt","amount":"300000000ugnot"}],"fee":{"gas_wanted":"5000000","gas_fee":"1000000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"AhUuKSwiL4XKy0BM9cK0UJFToerTzvgLTHFBvYk/xOtY"},"signature":"eYiid4TfHs0g8ynLxM++lLT/zuXB5RTbUZMoYM10Iw5Dgt0/3sjPO67ZHPc8mMgAmlEbfJ5vxoGpI/9lvP1xig=="}],"memo":""},"metadata":{"timestamp":"1788752515"}}
{"tx":{"msg":[{"@type":"/vm.m_call","caller":"g10d35aqwna72yzsl6304jem6xdlf3gye4n2zyls","send":"1008432ugnot","max_deposit":"","pkg_path":"gno.land/r/gnoland/wugnot","func":"Deposit"}],"fee":{"gas_wanted":"371200000","gas_fee":"3712000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"Ah+jN3gvc+p0QEhhH1APhLXMdT/879z0d4+8+RzVF1tx"},"signature":"/mSBVQHnE13IOFl1CPijM8+ac1OUtlbprLDoZ1MqBLQKy5pLf1HqZdSlMeFTadF9iOys/RdDGy39zJwyFs2GbA=="}],"memo":""},"metadata":{"timestamp":"1788752606"}}
{"tx":{"msg":[{"@type":"/vm.m_call","caller":"g10d35aqwna72yzsl6304jem6xdlf3gye4n2zyls","send":"4754720ugnot","max_deposit":"","pkg_path":"gno.land/r/gnoland/wugnot","func":"Deposit"}],"fee":{"gas_wanted":"329600000","gas_fee":"3296000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"Ah+jN3gvc+p0QEhhH1APhLXMdT/879z0d4+8+RzVF1tx"},"signature":"D/pobEvqDgQaPtqQrAhiAFg33l0x2n69gxL5uCWE/7prjgSqciJRZ4IjcRA4lLXnIjHQNSFz6fRQlyYnvXpXtA=="}],"memo":""},"metadata":{"timestamp":"1788752617"}}
{"tx":{"msg":[{"@type":"/bank.MsgSend","from_address":"g18qhq2fl54lszhmxeyqlvxnwjzc3xpu4nnakclp","to_address":"g179kg8k3nxr2wuq9863xl5uqge4vzsewz3tr5f2","amount":"300000000ugnot"}],"fee":{"gas_wanted":"5000000","gas_fee":"1000000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"AhUuKSwiL4XKy0BM9cK0UJFToerTzvgLTHFBvYk/xOtY"},"signature":"IQK6bv/yRqW4nxoo108cpwqJdf1TY7+LIfB2IAKCZVZ5ZcqPQAbax0tcAzrkIONjJJ/I+du9GCzltejVxkS4cg=="}],"memo":""},"metadata":{"timestamp":"1788752740"}}
{"tx":{"msg":[{"@type":"/vm.m_call","caller":"g1vklywnkmnsqdurqmwp4kynr777sm8dahcq4zr5","send":"17475284ugnot","max_deposit":"","pkg_path":"gno.land/r/gnoland/wugnot","func":"Deposit"}],"fee":{"gas_wanted":"371200000","gas_fee":"3712000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"AkbS4L2W2MgF7QeqhzqED2G08wJgwWhbOcQm5C/QAd/i"},"signature":"ANKRsSChrXBqZH2QgTu+fAOAmVLPs1mIJbTl7cctCVUzCMvmWS+uSYqnwQTmaqK3BzzH7kSUORdeeKlxA8Fp5w=="}],"memo":""},"metadata":{"timestamp":"1788752761"}}
{"tx":{"msg":[{"@type":"/vm.m_call","caller":"g16g38utef784epg0tlq59gaqkhclgfd48qrvzrv","send":"1573692ugnot","max_deposit":"","pkg_path":"gno.land/r/gnoland/wugnot","func":"Deposit"}],"fee":{"gas_wanted":"377600000","gas_fee":"3776000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"AgbYcsGjabW/ImZfiGFQDgyeigN1PMMbqDFfCH1+PCoS"},"signature":"QVpEmLGeesnYLYyafcSMMby7iygdMFfKfaKW2/9RahNjRyepQ6TiHWXSmg/hT/pdk+n8+zDVTndsBydpvl0YZQ=="}],"memo":""},"metadata":{"timestamp":"1788752785"}}
{"tx":{"msg":[{"@type":"/vm.m_call","caller":"g1l34hft7ztgu0sfvdzk0h6yxsnalp2halsqfuxg","send":"2756181ugnot","max_deposit":"","pkg_path":"gno.land/r/gnoland/wugnot","func":"Deposit"}],"fee":{"gas_wanted":"380800000","gas_fee":"3808000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"AyTECPkbOV5HhPKncNDlzDXgwV+c6HVIKrN+KjalGsFG"},"signature":"K+o8/Koyq12cNMVF7wVTnSWbqMpYf33rnG9A3Wnl1TQpPpgUUAc9Id/8lGeSE4WTMCO2Z7hLPFDdumGKodA8QQ=="}],"memo":""},"metadata":{"timestamp":"1788752803"}}
{"tx":{"msg":[{"@type":"/vm.m_call","caller":"g1l34hft7ztgu0sfvdzk0h6yxsnalp2halsqfuxg","send":"3459102ugnot","max_deposit":"","pkg_path":"gno.land/r/gnoland/wugnot","func":"Deposit"}],"fee":{"gas_wanted":"364800000","gas_fee":"3648000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"AyTECPkbOV5HhPKncNDlzDXgwV+c6HVIKrN+KjalGsFG"},"signature":"rQi1pvORNbstn1ZkSNrNAHlvnqwGOh5nJinozZwrA/lDnpkxQ9GGGERpLois1pEIkV+qIJs2mAiGofBOiqyfgQ=="}],"memo":""},"metadata":{"timestamp":"1788752813"}}
{"tx":{"msg":[{"@type":"/vm.m_call","caller":"g1f3mdu406xrjlpeay4d0tr829x9nkr0a5ekgdl7","send":"18392394ugnot","max_deposit":"","pkg_path":"gno.land/r/gnoland/wugnot","func":"Deposit"}],"fee":{"gas_wanted":"374400000","gas_fee":"3744000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"A6dkAqCYOeE53R71vOf6CTf3xfAAscj0znf4eCCxC4A3"},"signature":"iNaWLw+un46pYnl9pdNKLvJlDTMmqAjimy6j3LMx9Tx5u3X7XD8DqQupwlQK07DRWQlbMbHOkaM6LpXSweS3nw=="}],"memo":""},"metadata":{"timestamp":"1788752868"}}
{"tx":{"msg":[{"@type":"/vm.m_call","caller":"g1f3mdu406xrjlpeay4d0tr829x9nkr0a5ekgdl7","send":"","max_deposit":"","pkg_path":"gno.land/r/gnoland/wugnot","func":"Withdraw","args":["9293231"]}],"fee":{"gas_wanted":"361600000","gas_fee":"3616000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"A6dkAqCYOeE53R71vOf6CTf3xfAAscj0znf4eCCxC4A3"},"signature":"LFdPm32DoS22BE8bNeJgpJbSV/j6Kg04fCd8GkgrvAskaBW1iGNSZ6YPqMsIod5lRQHiR+KnG1B/tFHuyNM0GA=="}],"memo":""},"metadata":{"timestamp":"1788752878"}}
{"tx":{"msg":[{"@type":"/vm.m_call","caller":"g1pl27m7wmyhww09wzscj5lyy7jpsluvseaaws4z","send":"3606217ugnot","max_deposit":"","pkg_path":"gno.land/r/gnoland/wugnot","func":"Deposit"}],"fee":{"gas_wanted":"336000000","gas_fee":"3360000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"A+gGwnGj4AyvvhlU7ca09UFanmCWL/5eFRqaLDSvprl3"},"signature":"U1/8jegsyYn0Uo7tjaFMfhmNzgJMbR1LiojnxPiMpxMaBaguBCDybxEKCpxdpJLgkg7oC82aFjSLfAX2f6ArUw=="}],"memo":""},"metadata":{"timestamp":"1788752927"}}
{"tx":{"msg":[{"@type":"/bank.MsgSend","from_address":"g18qhq2fl54lszhmxeyqlvxnwjzc3xpu4nnakclp","to_address":"g1dedgr5xuqqlrj6ng7dtu7txf87fpx83t2wzrv9","amount":"300000000ugnot"}],"fee":{"gas_wanted":"5000000","gas_fee":"1000000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"AhUuKSwiL4XKy0BM9cK0UJFToerTzvgLTHFBvYk/xOtY"},"signature":"bGWelIG5uf3Nv2MAyGwXX8sOy78ZrAEJrpXotbu3r6U9+FWbPVcIARW3RJDYibbOdVRiroISoeyaWmZaMVStOw=="}],"memo":""},"metadata":{"timestamp":"1788752940"}}
{"tx":{"msg":[{"@type":"/bank.MsgSend","from_address":"g18qhq2fl54lszhmxeyqlvxnwjzc3xpu4nnakclp","to_address":"g1pu66decmvjezwvcmka86r36nwsuv65snnaxa3v","amount":"300000000ugnot"}],"fee":{"gas_wanted":"5000000","gas_fee":"1000000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"AhUuKSwiL4XKy0BM9cK0UJFToerTzvgLTHFBvYk/xOtY"},"signature":"qv3JGhslx1uBy3ee6Whx4P+vMhQJ39XgJtb9kJIHIEtRBIMAUkUwZs0UexnEWVvzZbdXpXsloa0UV8JOXi5wkw=="}],"memo":""},"metadata":{"timestamp":"1788752982"}}
{"tx":{"msg":[{"@type":"/bank.MsgSend","from_address":"g18qhq2fl54lszhmxeyqlvxnwjzc3xpu4nnakclp","to_address":"g1eeavxx6n5jgvrphdn5uh69qmkqjayfwxg9r330","amount":"300000000ugnot"}],"fee":{"gas_wanted":"5000000","gas_fee":"1000000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"AhUuKSwiL4XKy0BM9cK0UJFToerTzvgLTHFBvYk/xOtY"},"signature":"A/o5Fg+hl2Qtn5VxYf5Z5Dhr97QlNnQDVzrQWXRe28VZ4yiotTNZouAs+9K30K5w/yrtSqgGRnKrQ8p/omfH+A=="}],"memo":""},"metadata":{"timestamp":"1788753014"}}
{"tx":{"msg":[{"@type":"/bank.MsgSend","from_address":"g18qhq2fl54lszhmxeyqlvxnwjzc3xpu4nnakclp","to_address":"g12yr0nzj4q2dhpjcjs35q83sgfmk964hwm6yd29","amount":"300000000ugnot"}],"fee":{"gas_wanted":"5000000","gas_fee":"1000000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"AhUuKSwiL4XKy0BM9cK0UJFToerTzvgLTHFBvYk/xOtY"},"signature":"qN6fVyIAqLFjBVKXKg8Wc0tI3RUnz3bG0/ntcVD8g44HKpd9Fm2rBxrhdKEwWpSxZtLnRWaBGgXzuZFJArKSEg=="}],"memo":""},"metadata":{"timestamp":"1788753058"}}
{"tx":{"msg":[{"@type":"/bank.MsgSend","from_address":"g18qhq2fl54lszhmxeyqlvxnwjzc3xpu4nnakclp","to_address":"g1r5muk8te0z2url8kntgjst7wygnfmr8xhsn7m3","amount":"300000000ugnot"}],"fee":{"gas_wanted":"5000000","gas_fee":"1000000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"AhUuKSwiL4XKy0BM9cK0UJFToerTzvgLTHFBvYk/xOtY"},"signature":"PmkSqEpwKgSvrFCi0j06Fg7FcFnYWfuej2S2UWz3FBMQTGROKzWqabiCVGYoFKIMhOTYAPma498bFU4MqgCx8w=="}],"memo":""},"metadata":{"timestamp":"1788753079"}}
{"tx":{"msg":[{"@type":"/vm.m_call","caller":"g1lkfsafls64dydqnjlvcw0jarlumdcsklfh4ny3","send":"","max_deposit":"","pkg_path":"gno.land/r/gnoland/wugnot","func":"Withdraw","args":["2753570"]}],"fee":{"gas_wanted":"364800000","gas_fee":"3648000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"Ax2xATYYu3PKyow/KX9FxzggNxzsN8y7LTHps3Ez/OkA"},"signature":"1GQe+DLYPL9+kkmV7NLNNH4doLAprKphjcjYHK9ZKQE36hOVY4MH1Cjm1cmHu6njG/N2KjHGcUbOBX9K3yemiA=="}],"memo":""},"metadata":{"timestamp":"1788753146"}}
{"tx":{"msg":[{"@type":"/vm.m_call","caller":"g1lkfsafls64dydqnjlvcw0jarlumdcsklfh4ny3","send":"12668593ugnot","max_deposit":"","pkg_path":"gno.land/r/gnoland/wugnot","func":"Deposit"}],"fee":{"gas_wanted":"348800000","gas_fee":"3488000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"Ax2xATYYu3PKyow/KX9FxzggNxzsN8y7LTHps3Ez/OkA"},"signature":"V+doQXurP+Sb6z2ZIHa5KEEiKwVWpMIQEGaNjts/MrBMkXmFVTZzu3bTQ9gdphEV/Ij/zIryK0IWcdLT8WU48g=="}],"memo":""},"metadata":{"timestamp":"1788753163"}}
{"tx":{"msg":[{"@type":"/bank.MsgSend","from_address":"g18qhq2fl54lszhmxeyqlvxnwjzc3xpu4nnakclp","to_address":"g1ewj03wlkklz7nuwhv0unqpyalkacc3jqw8mm06","amount":"300000000ugnot"}],"fee":{"gas_wanted":"5000000","gas_fee":"1000000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"AhUuKSwiL4XKy0BM9cK0UJFToerTzvgLTHFBvYk/xOtY"},"signature":"XPMvgMRWjp5NiuTMZUu1JqhUICY45I6QiIaQE/WGfmoy0hBkYZgpgKHKueReEYCh+WLw6faJrsQ3g+347jA3Cw=="}],"memo":""},"metadata":{"timestamp":"1788753166"}}
{"tx":{"msg":[{"@type":"/vm.m_call","caller":"g1qw0eyxqmekclumgfrzdyaspxlzc3yd32d7nl86","send":"22338925ugnot","max_deposit":"","pkg_path":"gno.land/r/gnoland/wugnot","func":"Deposit"}],"fee":{"gas_wanted":"361600000","gas_fee":"3616000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"ArbF0SU4Nm/7sl6OfX8UIgJ1mKFWQAdoQPQ3o1EyQoYL"},"signature":"IUnH/BehFHm3nmjjx+3yrhtAY4ePW3SbYssaIwiZCp1g95Ft01dcsobTb0aazev2BqF6CnJctYlWwCLGgJ/ITg=="}],"memo":""},"metadata":{"timestamp":"1788753166"}}
{"tx":{"msg":[{"@type":"/vm.m_call","caller":"g12q3g7y077jtl3cxm4rxtn0l4xz8evwpr23kglc","send":"3071276ugnot","max_deposit":"","pkg_path":"gno.land/r/gnoland/wugnot","func":"Deposit"}],"fee":{"gas_wanted":"377600000","gas_fee":"3776000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"AhHADbMqSr7gfb3SYFYliWaTtdKSCXzRc6y8TXTu8IJh"},"signature":"QYkD40C33V2jgi8DUheLuO8Q1eGHK207bv4NiMUfXNZtmAiXXM9dqnG5J9apVJVU/SUc7z9A/SK0V+WLCaIc2A=="}],"memo":""},"metadata":{"timestamp":"1788753176"}}
{"tx":{"msg":[{"@type":"/vm.m_call","caller":"g12q3g7y077jtl3cxm4rxtn0l4xz8evwpr23kglc","send":"","max_deposit":"","pkg_path":"gno.land/r/gnoland/wugnot","func":"Withdraw","args":["4743606"]}],"fee":{"gas_wanted":"323200000","gas_fee":"3232000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"AhHADbMqSr7gfb3SYFYliWaTtdKSCXzRc6y8TXTu8IJh"},"signature":"jrK/qZnxev+/y7yKRk/6DeI+PZJEjUPZAGOmgmmRrvlwFOUUNrto43P9hgeCDhThm364JeQApGqi2kHUclXLJA=="}],"memo":""},"metadata":{"timestamp":"1788753187"}}
{"tx":{"msg":[{"@type":"/vm.m_call","caller":"g1qz8fmwkdnj3p39dcd048gxksq9ccwsr4ut74c3","send":"","max_deposit":"","pkg_path":"gno.land/r/gnoland/wugnot","func":"Withdraw","args":["2831215"]}],"fee":{"gas_wanted":"374400000","gas_fee":"3744000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"A8UseeQ5JbJ0QpFMkh5tb6f0maaAyNsCE9SrZxt9O2wo"},"signature":"6KkQGtYgiyWamMQKnHEwb6zvSQ9BXNyHPe1AkfltMXYcpjAU9snSRZ7IPdhOmmX9smpJPLq3oGAI0bEdexxQGA=="}],"memo":""},"metadata":{"timestamp":"1788753268"}}
{"tx":{"msg":[{"@type":"/vm.m_call","caller":"g1qz8fmwkdnj3p39dcd048gxksq9ccwsr4ut74c3","send":"","max_deposit":"","pkg_path":"gno.land/r/gnoland/wugnot","func":"Withdraw","args":["7538187"]}],"fee":{"gas_wanted":"387200000","gas_fee":"3872000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"A8UseeQ5JbJ0QpFMkh5tb6f0maaAyNsCE9SrZxt9O2wo"},"signature":"XQPEoErxFF803PJo8E9bYbXXz50BOPRLopNZTN4yMT0weQntkHPZhVLIuzcp6mAlnsQfIDZQrt6zfOAz+6iH4Q=="}],"memo":""},"metadata":{"timestamp":"1788753279"}}
{"tx":{"msg":[{"@type":"/bank.MsgSend","from_address":"g18qhq2fl54lszhmxeyqlvxnwjzc3xpu4nnakclp","to_address":"g18rhf9d9g0t3kffw2mx0tcq2lnx3qgd5ke83sfq","amount":"300000000ugnot"}],"fee":{"gas_wanted":"5000000","gas_fee":"1000000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"AhUuKSwiL4XKy0BM9cK0UJFToerTzvgLTHFBvYk/xOtY"},"signature":"j5PBLsRO4kyYvwtT8nVRCWiWH3nAtfZ7sr3mj1PJ2q4iUznFok/C8GqTJBMe/+kbbAHBdoP5XY5+tBHTDQ5bpQ=="}],"memo":""},"metadata":{"timestamp":"1788753279"}}
{"tx":{"msg":[{"@type":"/bank.MsgSend","from_address":"g18qhq2fl54lszhmxeyqlvxnwjzc3xpu4nnakclp","to_address":"g1zd43a5mrmnq65xmq3zj3ukz4vakau7rw3k369h","amount":"100000000ugnot"}],"fee":{"gas_wanted":"5000000","gas_fee":"1000000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"AhUuKSwiL4XKy0BM9cK0UJFToerTzvgLTHFBvYk/xOtY"},"signature":"EqDSiuOX+/1vpuZiL9bhBMRPkttNYNI042gRSDWmkJBqXr1QkCwNxedZDbf37tQJcpp7MsZh3Dini0ucm8Uz6Q=="}],"memo":""},"metadata":{"timestamp":"1788753340"}}
{"tx":{"msg":[{"@type":"/vm.m_call","caller":"g1kkqlpxz770t58tzzp26azcljts5ph08a6mwc9w","send":"3874124ugnot","max_deposit":"","pkg_path":"gno.land/r/gnoland/wugnot","func":"Deposit"}],"fee":{"gas_wanted":"377600000","gas_fee":"3776000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"A054Pa8+Xkpmb6EJdJsjmFN7z9tKAmZmAS332sLPf6gR"},"signature":"RCbw3F4FsioUmzdZatJfPmG48wTdC6wvs8w/z6XaymV1AOipZIcHjOFOeoVDAgy+8eQLkSKKhvls3fEwVCFdRQ=="}],"memo":""},"metadata":{"timestamp":"1788753476"}}
{"tx":{"msg":[{"@type":"/bank.MsgSend","from_address":"g18qhq2fl54lszhmxeyqlvxnwjzc3xpu4nnakclp","to_address":"g12kdg0l3w42kz5d22d9skalsf76jj0gq7p73nkd","amount":"300000000ugnot"}],"fee":{"gas_wanted":"5000000","gas_fee":"1000000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"AhUuKSwiL4XKy0BM9cK0UJFToerTzvgLTHFBvYk/xOtY"},"signature":"HGwMe4OIoRkdwZ2rZi9GVnsG0+zUyuvYRzaqTosEYLsfidHFs9j+UI9ecv9M1bHcVdKhUEbayMwEpKb82HgTgg=="}],"memo":""},"metadata":{"timestamp":"1788753494"}}
{"tx":{"msg":[{"@type":"/vm.m_call","caller":"g136hqkapzrr2xd5c9ftnasgcrl57lc2n7qsdlm4","send":"6252798ugnot","max_deposit":"","pkg_path":"gno.land/r/gnoland/wugnot","func":"Deposit"}],"fee":{"gas_wanted":"329600000","gas_fee":"3296000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"ArXf2HEYAIixlnn/8DrVR0HD/hO+rt3SxXkoDrCbdAbc"},"signature":"MuOp7aDXthyJ+BWSI5Jl/18oBalhs2v9myFk1X+gF5wOiV4YXjXshwrs27bMGRefftxYKggrlwylxUAlpv+NOg=="}],"memo":""},"metadata":{"timestamp":"1788753524"}}
{"tx":{"msg":[{"@type":"/vm.m_call","caller":"g1w5w960dpvnwdf20yn36fyla5rptzerjn829wzv","send":"","max_deposit":"","pkg_path":"gno.land/r/gnoland/wugnot","func":"Withdraw","args":["2464464"]}],"fee":{"gas_wanted":"326400000","gas_fee":"3264000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"A7FhMZJCS+AGgKkps7ceICnu/46TW3f73LQZbwoBzfkk"},"signature":"A//MMyDleMVPmLVPoeZiPkxTlL0RyH442Cbs6xs+a4tcyhhyXidc8Si9fMTNMBvi9Dptvwd7dNOzic8kUVWfUQ=="}],"memo":""},"metadata":{"timestamp":"1788753612"}}
{"tx":{"msg":[{"@type":"/vm.m_call","caller":"g1w5w960dpvnwdf20yn36fyla5rptzerjn829wzv","send":"","max_deposit":"","pkg_path":"gno.land/r/gnoland/wugnot","func":"Withdraw","args":["3391198"]}],"fee":{"gas_wanted":"396800000","gas_fee":"3968000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"A7FhMZJCS+AGgKkps7ceICnu/46TW3f73LQZbwoBzfkk"},"signature":"x/ori0JZf1yH9xaZ0iiqzFFn7CWswZyw8wZTwaq04sJ9syJiNe2IOlb2NiqRYuEV1l5Uuia3RjYA1VyKRbnGBw=="}],"memo":""},"metadata":{"timestamp":"1788753629"}}
{"tx":{"msg":[{"@type":"/bank.MsgSend","from_address":"g18qhq2fl54lszhmxeyqlvxnwjzc3xpu4nnakclp","to_address":"g1yqpe0hg8t005c9gvvzuclj58fdz43y9jvuxnap","amount":"300000000ugnot"}],"fee":{"gas_wanted":"5000000","gas_fee":"1000000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"AhUuKSwiL4XKy0BM9cK0UJFToerTzvgLTHFBvYk/xOtY"},"signature":"50ZIny3wwS1hIsVl7TXBaLefGvF5sFcI5YYrNU801ZcNup1/FRwJe0ExN1Bnlzjy/mz8yPC+4F9o6MtiK24SKg=="}],"memo":""},"metadata":{"timestamp":"1788753636"}}
{"tx":{"msg":[{"@type":"/vm.m_call","caller":"g1avtpecf0dz573p7z0dy6schqgn3ntkxc3tvzwh","send":"12531290ugnot","max_deposit":"","pkg_path":"gno.land/r/gnoland/wugnot","func":"Deposit"}],"fee":{"gas_wanted":"323200000","gas_fee":"3232000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"A2oH4kh9nRZqwq6XM/phNK8/NTYvYousuwnBUkBzslLR"},"signature":"fS97kfYYlRa0DnhDNieMtOovOPZjAfoj1meT84Eg2zhP7BNeIcHYfiTZzaRj8QMVFmU2wqPGOQIwoTxWLYjItQ=="}],"memo":""},"metadata":{"timestamp":"1788753646"}}
{"tx":{"msg":[{"@type":"/vm.m_call","caller":"g1avtpecf0dz573p7z0dy6schqgn3ntkxc3tvzwh","send":"6534652ugnot","max_deposit":"","pkg_path":"gno.land/r/gnoland/wugnot","func":"Deposit"}],"fee":{"gas_wanted":"355200000","gas_fee":"3552000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"A2oH4kh9nRZqwq6XM/phNK8/NTYvYousuwnBUkBzslLR"},"signature":"tJyXtRY+rjA4vXfkKqnrDyK86C3irFG9hqynvcBHBwA/ENfO7MsJJ+jwaDN7+/CK7hO45KzMpgYGMVnt+CBPhg=="}],"memo":""},"metadata":{"timestamp":"1788753656"}}
{"tx":{"msg":[{"@type":"/vm.m_call","caller":"g15ylnsd247lw0y4dd9mqgxz76etap60yrxdwjhv","send":"","max_deposit":"","pkg_path":"gno.land/r/gnoland/wugnot","func":"Withdraw","args":["1326332"]}],"fee":{"gas_wanted":"329600000","gas_fee":"3296000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"A72gX641bmjMvlCr2JLzUDzlihng75TKmkinN9ZQPhOh"},"signature":"0QabNnDSbfYA1tynLPfra+/ila4+KDwacpe1a/458VdkqwcaaIIL4yrvQhPxGiP6LVIGqkaVyaB+YDHm0paCdQ=="}],"memo":""},"metadata":{"timestamp":"1788753672"}}
{"tx":{"msg":[{"@type":"/vm.m_call","caller":"g15ylnsd247lw0y4dd9mqgxz76etap60yrxdwjhv","send":"","max_deposit":"","pkg_path":"gno.land/r/gnoland/wugnot","func":"Withdraw","args":["11602085"]}],"fee":{"gas_wanted":"342400000","gas_fee":"3424000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"A72gX641bmjMvlCr2JLzUDzlihng75TKmkinN9ZQPhOh"},"signature":"SRC2RGNL1CIs6dpk/GsNd48Sc4ihSh+mCdp7A3/cd3hKqDGIx4v8PjOsbq+FQdJf0zYWEXYU0Z4aiPjJJCYO6g=="}],"memo":""},"metadata":{"timestamp":"1788753682"}}
{"tx":{"msg":[{"@type":"/vm.m_call","caller":"g1sqgczghxqlxtsehvg4c97u3r9m6gag79g50865","send":"","max_deposit":"","pkg_path":"gno.land/r/gnoland/wugnot","func":"Withdraw","args":["3309640"]}],"fee":{"gas_wanted":"329600000","gas_fee":"3296000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"A1HuZ/KC/xQO1GpHNC82NCVQJs4fNKn0+HrIILEfGjqs"},"signature":"I5hsm0F+0hz5jMlkxnJ+pbvXpd0MUE/GpHZCO14nVV4uA2oiQT0HsV8oJmRp3C/H5Hdnf392It2xq24Xu3Czxw=="}],"memo":""},"metadata":{"timestamp":"1788753710"}}
{"tx":{"msg":[{"@type":"/vm.m_call","caller":"g1j62rlvz8gc47n4x5hvgp62npfe6m4k5w2c84z7","send":"11730124ugnot","max_deposit":"","pkg_path":"gno.land/r/gnoland/wugnot","func":"Deposit"}],"fee":{"gas_wanted":"323200000","gas_fee":"3232000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"AjebaXed5+iCNOdmdxCywgscnevup/BtePlfvmnH2siK"},"signature":"/QYqattLCjEVBkk/X6PLyZ7cLjTCTe7r0ii+NcoQG4Js38dGwob1PjbG6RiNLPMh7RNnj6Eigsara+ByrgNL1g=="}],"memo":""},"metadata":{"timestamp":"1788753724"}}
{"tx":{"msg":[{"@type":"/vm.m_call","caller":"g1sqgczghxqlxtsehvg4c97u3r9m6gag79g50865","send":"1849095ugnot","max_deposit":"","pkg_path":"gno.land/r/gnoland/wugnot","func":"Deposit"}],"fee":{"gas_wanted":"380800000","gas_fee":"3808000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"A1HuZ/KC/xQO1GpHNC82NCVQJs4fNKn0+HrIILEfGjqs"},"signature":"1qfzgNEYg2DP3BOEqfyQLvximsE0z9/CZLgdwZlDR/YdUWwQ85ezpSxXcS3d16IvKNNE7K+P6bWxkwNn0d3YNw=="}],"memo":""},"metadata":{"timestamp":"1788753727"}}
{"tx":{"msg":[{"@type":"/vm.m_call","caller":"g1j62rlvz8gc47n4x5hvgp62npfe6m4k5w2c84z7","send":"1703840ugnot","max_deposit":"","pkg_path":"gno.land/r/gnoland/wugnot","func":"Deposit"}],"fee":{"gas_wanted":"358400000","gas_fee":"3584000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"AjebaXed5+iCNOdmdxCywgscnevup/BtePlfvmnH2siK"},"signature":"2haCPdtwWsCPFqIZ0719ezzwz/kcHufbX/MrxQ3QlwkhUbzCgayFfnRkh6HiD2vY6Y9jjVUrkcRdd9uDdU4VRg=="}],"memo":""},"metadata":{"timestamp":"1788753731"}}
{"tx":{"msg":[{"@type":"/bank.MsgSend","from_address":"g18qhq2fl54lszhmxeyqlvxnwjzc3xpu4nnakclp","to_address":"g180vhzc9236rzxtv6xqqnhc6ujuy7uhly3cljpg","amount":"300000000ugnot"}],"fee":{"gas_wanted":"5000000","gas_fee":"1000000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"AhUuKSwiL4XKy0BM9cK0UJFToerTzvgLTHFBvYk/xOtY"},"signature":"ja+J8z2grwfhS8+ZBaKRqMw2fYZRRJbqEjT+QJBNSA5/j590HCqgycqD9ITxvDbZ0hQjyrYfXeNViqliHI6DbQ=="}],"memo":""},"metadata":{"timestamp":"1788753770"}}
{"tx":{"msg":[{"@type":"/bank.MsgSend","from_address":"g18qhq2fl54lszhmxeyqlvxnwjzc3xpu4nnakclp","to_address":"g1r86z05pj7wqtje6xdjma9aswa6s692j4exg4ct","amount":"300000000ugnot"}],"fee":{"gas_wanted":"5000000","gas_fee":"1000000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"AhUuKSwiL4XKy0BM9cK0UJFToerTzvgLTHFBvYk/xOtY"},"signature":"VcWQGBbRS9IETjffUvwfioKr+aAfP6ZicYO5JFFmGRZL4Ns2gWL6NUje8Fzzxm97II2qjg91anFxWBcxZ5S8/g=="}],"memo":""},"metadata":{"timestamp":"1788753818"}}
{"tx":{"msg":[{"@type":"/vm.m_call","caller":"g1pn7vy9k4794c49gk5dcs63q86ve4xy4m200pg8","send":"","max_deposit":"","pkg_path":"gno.land/r/gnoland/wugnot","func":"Withdraw","args":["8444870"]}],"fee":{"gas_wanted":"326400000","gas_fee":"3264000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"A2G7EsG2gc0a4bvz9ojeYkayhjj1xXZZUaeCKIG6qWQO"},"signature":"5wFYZzq4BFTO95ZZpLt6yvfF5wIuuAiafurxCWsAv/E8y28+3XlNV2TbymGuPgyS2G7QvFukceMsIugtL4Tfyg=="}],"memo":""},"metadata":{"timestamp":"1788753908"}}
{"tx":{"msg":[{"@type":"/vm.m_call","caller":"g1pn7vy9k4794c49gk5dcs63q86ve4xy4m200pg8","send":"8168519ugnot","max_deposit":"","pkg_path":"gno.land/r/gnoland/wugnot","func":"Deposit"}],"fee":{"gas_wanted":"336000000","gas_fee":"3360000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"A2G7EsG2gc0a4bvz9ojeYkayhjj1xXZZUaeCKIG6qWQO"},"signature":"MudIxLpQrqzw3kOuCPqaWYOwjLRTeBZiBbtYqxyPqvAJnAETyOm3O7YLqlaJHqx124Em/riNa6KL5NvNRocq4A=="}],"memo":""},"metadata":{"timestamp":"1788753922"}}
{"tx":{"msg":[{"@type":"/vm.m_call","caller":"g1lc7qyrcumjknqra7aahmf8tnlap663jjxdq6n3","send":"23787057ugnot","max_deposit":"","pkg_path":"gno.land/r/gnoland/wugnot","func":"Deposit"}],"fee":{"gas_wanted":"368000000","gas_fee":"3680000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"AsrwZPkgRlLcVD4IXUnjqpRvV/C63P8ix3q1mco3ifMh"},"signature":"RhepMq3tsMziX+YIwW34495HaTZ2KDRVjR02aZzXk4BroM9gGYf79Kfa4FxwGPfg8j5HSv1O3ylK8n7aXcieeA=="}],"memo":""},"metadata":{"timestamp":"1788753932"}}
{"tx":{"msg":[{"@type":"/vm.m_call","caller":"g1lc7qyrcumjknqra7aahmf8tnlap663jjxdq6n3","send":"","max_deposit":"","pkg_path":"gno.land/r/gnoland/wugnot","func":"Withdraw","args":["6637819"]}],"fee":{"gas_wanted":"371200000","gas_fee":"3712000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"AsrwZPkgRlLcVD4IXUnjqpRvV/C63P8ix3q1mco3ifMh"},"signature":"lq9UmHQZBBuQ2F9ne/XXuCf+1T88gkNKXYsNsCh937gDc2UfYzQu7GqWk+7NcsbLP8ALwYErA9PkI4Dx1Y6qjg=="}],"memo":""},"metadata":{"timestamp":"1788753943"}}
{"tx":{"msg":[{"@type":"/vm.m_call","caller":"g1s6qs7lh2rrdfuewrnf75vw59e8wcnfkzeha5w7","send":"19513768ugnot","max_deposit":"","pkg_path":"gno.land/r/gnoland/wugnot","func":"Deposit"}],"fee":{"gas_wanted":"348800000","gas_fee":"3488000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"A7ekSWCooF0rkv1B7dEdUvcblE8aLgH79Z8Wpritob6/"},"signature":"7sH7s+5OhLRrKbgjhmy0jgcAOcxmf4rs0FWyP/qupeF/HE7wKqKjA3TeF3bhhsd5VDOnAmYsV6xu8ui/EKQHOg=="}],"memo":""},"metadata":{"timestamp":"1788753998"}}
{"tx":{"msg":[{"@type":"/vm.m_call","caller":"g1mpavfcuytgmz06t4xr74lvryg5wynrx4u0f0eg","send":"2016016ugnot","max_deposit":"","pkg_path":"gno.land/r/gnoland/wugnot","func":"Deposit"}],"fee":{"gas_wanted":"377600000","gas_fee":"3776000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"Avu5WYeJH5d6tF/hc+Y4oLE68XF2VM1fNDAyyyQ9N8rV"},"signature":"W0LtjRJN2EirSzBl/eq9ITR/bqvlW8sd7sToKSiIEb9nnBGInLYYwQslnyuLgt6keUnJcLuhNDCgzuqyZxoM4g=="}],"memo":""},"metadata":{"timestamp":"1788754002"}}
{"tx":{"msg":[{"@type":"/vm.m_call","caller":"g1tgmr7h4h7uyuu9wm775xua9s8kff6fc9s2gznj","send":"15722552ugnot","max_deposit":"","pkg_path":"gno.land/r/gnoland/wugnot","func":"Deposit"}],"fee":{"gas_wanted":"352000000","gas_fee":"3520000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"A9ffLYcbJ2YnpAwtjfzeksAx36L+OZ1NR8CJiOctu8w6"},"signature":"/ZEfKmxmHVyP0akKOpK5ZSy673UCpW0UI6S2RaDEiFFJ33prxRzRIPnRBfcXSnOlou8SoleDI8gxfFj4MHRRpQ=="}],"memo":""},"metadata":{"timestamp":"1788754054"}}
{"tx":{"msg":[{"@type":"/bank.MsgSend","from_address":"g18qhq2fl54lszhmxeyqlvxnwjzc3xpu4nnakclp","to_address":"g1fke6zh2eykhcvpxl207qqgqzrprzr8ee7nw5ax","amount":"300000000ugnot"}],"fee":{"gas_wanted":"5000000","gas_fee":"1000000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"AhUuKSwiL4XKy0BM9cK0UJFToerTzvgLTHFBvYk/xOtY"},"signature":"opweuZeQbk4G0lRSeOy7E1whCSy9WePEvBOJgtAbH6dsLmFDbIGmzL7wkzyUkFA8dyy+px38SnVjmeY2WdNq4A=="}],"memo":""},"metadata":{"timestamp":"1788754082"}}
{"tx":{"msg":[{"@type":"/vm.m_call","caller":"g1xduj6twzm3x8qj33ak9zvkz03jtqltsw5gpqpm","send":"9616158ugnot","max_deposit":"","pkg_path":"gno.land/r/gnoland/wugnot","func":"Deposit"}],"fee":{"gas_wanted":"345600000","gas_fee":"3456000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"A1CovL3eJUaR+1AmfkcsPr4LhwXMWSRWd4fRF7KnTmH8"},"signature":"3WJ2IFt5h1qmfIX7BB/LM/FzWrEX4Bt/xnrAVUAfqsQzzLxWwA0ywulRhpdgm1PJC5fN7gezMawei6WfNkRiqA=="}],"memo":""},"metadata":{"timestamp":"1788754092"}}
{"tx":{"msg":[{"@type":"/bank.MsgSend","from_address":"g18qhq2fl54lszhmxeyqlvxnwjzc3xpu4nnakclp","to_address":"g1ke5sf8zw906e978afu8vw9ngdk3qxkdpl66827","amount":"300000000ugnot"}],"fee":{"gas_wanted":"5000000","gas_fee":"1000000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"AhUuKSwiL4XKy0BM9cK0UJFToerTzvgLTHFBvYk/xOtY"},"signature":"ItOTqB+Iimc6ZfS+5Usw3f2MUPH02es99G1/G64ve2Fd0ZkfVFzTQZn5z6PWv1I8Pjs79TzTxppOZF1nQSqqlg=="}],"memo":""},"metadata":{"timestamp":"1788754116"}}
{"tx":{"msg":[{"@type":"/vm.m_call","caller":"g15a8u4wmxm5xt377vj03nuwjemnyhdevjnex49l","send":"","max_deposit":"","pkg_path":"gno.land/r/gnoland/wugnot","func":"Withdraw","args":["6034313"]}],"fee":{"gas_wanted":"358400000","gas_fee":"3584000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"AjbsSPWJGVlfxwBPWsyMSGe8w3JL0MwwqR7xcuHa6+sM"},"signature":"d9UgQ9GtBF7XCfmO0NfCZajPr0bHbhxOQD/0bAWhz7ty5om6gkQ16bNMBYsTbTQzzcia/P+7/4RhSE/PyDUizA=="}],"memo":""},"metadata":{"timestamp":"1788754130"}}
{"tx":{"msg":[{"@type":"/vm.m_call","caller":"g17waznjhujxa7gkcudl3u4ezxs89r8j3j2j0f9e","send":"","max_deposit":"","pkg_path":"gno.land/r/gnoland/wugnot","func":"Withdraw","args":["4357918"]}],"fee":{"gas_wanted":"336000000","gas_fee":"3360000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"AyMjNQtV8vR9m2XapKsU7Z5zQDEmaPNrhS4NH7lhBfdk"},"signature":"faVaV1TBvLNbckC69h+cCmanFE+1W0mEvi1S8OixhA8pcvq+5Ob55fVLCPdF1h4S4b2zPRDpRETG4f5oyg6SQA=="}],"memo":""},"metadata":{"timestamp":"1788754187"}}
{"tx":{"msg":[{"@type":"/vm.m_call","caller":"g17waznjhujxa7gkcudl3u4ezxs89r8j3j2j0f9e","send":"2608883ugnot","max_deposit":"","pkg_path":"gno.land/r/gnoland/wugnot","func":"Deposit"}],"fee":{"gas_wanted":"396800000","gas_fee":"3968000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"AyMjNQtV8vR9m2XapKsU7Z5zQDEmaPNrhS4NH7lhBfdk"},"signature":"CFNVq4X/in/w6ugCF1T9FdkELGokOFoHYrMs3JUwHh16+4K9wd9f+1InfZ3dXZICIBb9jI/a3thBxGXWV2pmZw=="}],"memo":""},"metadata":{"timestamp":"1788754201"}}
{"tx":{"msg":[{"@type":"/vm.m_call","caller":"g1uweav36z8n4m57sqp99kqhjr33u68hpjruw3mf","send":"24110764ugnot","max_deposit":"","pkg_path":"gno.land/r/gnoland/wugnot","func":"Deposit"}],"fee":{"gas_wanted":"358400000","gas_fee":"3584000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"AjSIkBUNQoNPBYDzwHR4+unf0vw6YpQY6DOD/LXwhWU2"},"signature":"Mgo8gLWkBhhVXI42pYZX1CQiwAgkdOLo5MJGwjeUQMBdD43mxc1dzw4UHixYWuAoPqE4F+vnr8E1FDo+Kg7dDw=="}],"memo":""},"metadata":{"timestamp":"1788754220"}}
{"tx":{"msg":[{"@type":"/bank.MsgSend","from_address":"g18qhq2fl54lszhmxeyqlvxnwjzc3xpu4nnakclp","to_address":"g1axqud0g7mg9cnkc44f9mamsw326glh5m9w93lv","amount":"300000000ugnot"}],"fee":{"gas_wanted":"5000000","gas_fee":"1000000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"AhUuKSwiL4XKy0BM9cK0UJFToerTzvgLTHFBvYk/xOtY"},"signature":"kB4jTIxgukDoBs/dtbGX37uzz523khVDHpzmRzTttl16wySpBeA8DZCY9IKzGU5/LcjDXkpern6vnDxJnoEwVw=="}],"memo":""},"metadata":{"timestamp":"1788754274"}}
{"tx":{"msg":[{"@type":"/bank.MsgSend","from_address":"g18qhq2fl54lszhmxeyqlvxnwjzc3xpu4nnakclp","to_address":"g1yltnyqg0qz6q4v3h7hm4tlcg9nucuu2mndka3d","amount":"300000000ugnot"}],"fee":{"gas_wanted":"5000000","gas_fee":"1000000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"AhUuKSwiL4XKy0BM9cK0UJFToerTzvgLTHFBvYk/xOtY"},"signature":"9232ayk9RjzwagAQNZtMBrg0dYAxwKIqVbt/c0AYfm0d7i6qAQsUzu9tDegPgCKPYslcAPjIK7F00iDtGtspaQ=="}],"memo":""},"metadata":{"timestamp":"1788754305"}}
{"tx":{"msg":[{"@type":"/bank.MsgSend","from_address":"g18qhq2fl54lszhmxeyqlvxnwjzc3xpu4nnakclp","to_address":"g1xmy2etk8n9fyr9yuw373q3za83j53a2dh48as3","amount":"300000000ugnot"}],"fee":{"gas_wanted":"5000000","gas_fee":"1000000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"AhUuKSwiL4XKy0BM9cK0UJFToerTzvgLTHFBvYk/xOtY"},"signature":"43f57Mymi+XrQRiSqq1byyeVNeHjsZa0XvHES1iN53EncTfOAam5fAU81hp+hkLEYmmr4UWpRX4yydJl4XfP1w=="}],"memo":""},"metadata":{"timestamp":"1788754319"}}
{"tx":{"msg":[{"@type":"/bank.MsgSend","from_address":"g18qhq2fl54lszhmxeyqlvxnwjzc3xpu4nnakclp","to_address":"g10t23emdsumzlktj0vdqhj50cgtltwr7r4y3u68","amount":"300000000ugnot"}],"fee":{"gas_wanted":"5000000","gas_fee":"1000000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"AhUuKSwiL4XKy0BM9cK0UJFToerTzvgLTHFBvYk/xOtY"},"signature":"MG9w97HDpGhHE3oeuP2RwOtgcgjv7YNlZika4trLQTFDYBk7a3zCZWXqxMMNzuur6wNj5gZtO77CLWCZZQIcXA=="}],"memo":""},"metadata":{"timestamp":"1788754340"}}
{"tx":{"msg":[{"@type":"/vm.m_call","caller":"g1ak8n483tn75gfgkne2yyf59fk0s95jnu25eqwz","send":"2131708ugnot","max_deposit":"","pkg_path":"gno.land/r/gnoland/wugnot","func":"Deposit"}],"fee":{"gas_wanted":"352000000","gas_fee":"3520000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"A0i84N4pqT2uEO/zgS8tndd1sW3ygQ2yLt+sL9EPkEXL"},"signature":"t6+hjSbVjJHYN2Ywx7f5Hcx1+fWWoEPae8gfLQnbwClSbFEMGCu/CxWtzIKKv1Caw5J22Ss7GnHBzWQsGrlCgQ=="}],"memo":""},"metadata":{"timestamp":"1788754423"}}
{"tx":{"msg":[{"@type":"/vm.m_call","caller":"g1lx9jdw2l3xwe6af7apr2w5vvccgfm8a2k28uq7","send":"1491580ugnot","max_deposit":"","pkg_path":"gno.land/r/gnoland/wugnot","func":"Deposit"}],"fee":{"gas_wanted":"339200000","gas_fee":"3392000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"AuULgOw0Z2Uxmd+9BZb9qIBsw0T3BT80n5AqB39QJ8TH"},"signature":"s1M/Kx8FkHes89/a4GQw0GZnFjaBFZ9HY4IFlWbXMzFN7Bv78JsdQeeGd3t+aWYuv3Qzht24MzyZIt3K8rkdPQ=="}],"memo":""},"metadata":{"timestamp":"1788754462"}}
{"tx":{"msg":[{"@type":"/vm.m_call","caller":"g1lx9jdw2l3xwe6af7apr2w5vvccgfm8a2k28uq7","send":"13026013ugnot","max_deposit":"","pkg_path":"gno.land/r/gnoland/wugnot","func":"Deposit"}],"fee":{"gas_wanted":"326400000","gas_fee":"3264000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"AuULgOw0Z2Uxmd+9BZb9qIBsw0T3BT80n5AqB39QJ8TH"},"signature":"6bptm4r/VUnBSTQhpxiUXVIGaoqQtpMczxGTmqy5oPMzJUGvvyuzb0YFXVWA5CMoySF/gnliwAUyfFGCczD/0Q=="}],"memo":""},"metadata":{"timestamp":"1788754476"}}
{"tx":{"msg":[{"@type":"/bank.MsgSend","from_address":"g18qhq2fl54lszhmxeyqlvxnwjzc3xpu4nnakclp","to_address":"g10q46kegjthcytl9n447z5a4jxnu9wmyrcwy4pk","amount":"300000000ugnot"}],"fee":{"gas_wanted":"5000000","gas_fee":"1000000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"AhUuKSwiL4XKy0BM9cK0UJFToerTzvgLTHFBvYk/xOtY"},"signature":"Fyg/LQtE1YunG3IcZFvPAH/seg2jV8gAF29tbBeil0l2sVbAQIcACBn29ilzneAUO52kmIe4K+1Eb3S/l6b1nw=="}],"memo":""},"metadata":{"timestamp":"1788754479"}}
{"tx":{"msg":[{"@type":"/bank.MsgSend","from_address":"g18qhq2fl54lszhmxeyqlvxnwjzc3xpu4nnakclp","to_address":"g1cayju66ry9wv2r8wfdf98l37ur2v04xcqg8eqk","amount":"300000000ugnot"}],"fee":{"gas_wanted":"5000000","gas_fee":"1000000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"AhUuKSwiL4XKy0BM9cK0UJFToerTzvgLTHFBvYk/xOtY"},"signature":"du6ZOCcUwPcUkux0RDkMYCkPrACZsuMqvj63Ufmg+EdCk+hUieD8ZGdTVo8PrWoz4mVsupA6YUDG4GJRZKnmXg=="}],"memo":""},"metadata":{"timestamp":"1788754483"}}
{"tx":{"msg":[{"@type":"/vm.m_call","caller":"g1nnxuspyju2prgff5u3na6jdqjmsxawz6a823eu","send":"3875440ugnot","max_deposit":"","pkg_path":"gno.land/r/gnoland/wugnot","func":"Deposit"}],"fee":{"gas_wanted":"336000000","gas_fee":"3360000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"A4S8tR3xE+b5IBzDo93BP9UaR/BLFelMTDvJ0CeAzjN+"},"signature":"7sXSJ1yYX6S1KPsZHhjgui2tozAXuGV6tNYiB6pIDekHnczrEChxs53tFVO7sAaIs+r1EbCuypY8NHUWgSKXoA=="}],"memo":""},"metadata":{"timestamp":"1788754483"}}
{"tx":{"msg":[{"@type":"/vm.m_call","caller":"g1nnxuspyju2prgff5u3na6jdqjmsxawz6a823eu","send":"13013992ugnot","max_deposit":"","pkg_path":"gno.land/r/gnoland/wugnot","func":"Deposit"}],"fee":{"gas_wanted":"371200000","gas_fee":"3712000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"A4S8tR3xE+b5IBzDo93BP9UaR/BLFelMTDvJ0CeAzjN+"},"signature":"ptRN0Vx8lx7kXA3HFcGzbjYj8QBECCbOCzvnvJpv1GUcfS2nhqIV5NOe35CgHUlEse0GiMRGNU0RzH3iZJIRRg=="}],"memo":""},"metadata":{"timestamp":"1788754496"}}
{"tx":{"msg":[{"@type":"/bank.MsgSend","from_address":"g18qhq2fl54lszhmxeyqlvxnwjzc3xpu4nnakclp","to_address":"g14mgfgj7jx8u7jwmzq9scn0lr5eapzuz7s3q4ss","amount":"300000000ugnot"}],"fee":{"gas_wanted":"5000000","gas_fee":"1000000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"AhUuKSwiL4XKy0BM9cK0UJFToerTzvgLTHFBvYk/xOtY"},"signature":"C2VRdw2LdY/dz2muy/pmYZImw3kSlOmmLxV/PTx/sxoKL0KuRZC1NA8bhC7KDcuDpjcbbwcUd4x6lW3mVHIgSw=="}],"memo":""},"metadata":{"timestamp":"1788754507"}}
{"tx":{"msg":[{"@type":"/vm.m_call","caller":"g1930y5ldjmckj24zu3gl2pxx9e0850t2zrunqcl","send":"20130335ugnot","max_deposit":"","pkg_path":"gno.land/r/gnoland/wugnot","func":"Deposit"}],"fee":{"gas_wanted":"390400000","gas_fee":"3904000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"A9UDKCaPGq1oTtoFI94ibcSg3lV0YLcECgQvxTh+M1kG"},"signature":"KJuiDmgbSReBERDOsUn96hp7tuM/QltwRmtprCsiMsEun/NrhUwCdX34ucm0cirbbjXNrtrK9wEgB8RaB928Yg=="}],"memo":""},"metadata":{"timestamp":"1788754525"}}
{"tx":{"msg":[{"@type":"/vm.m_call","caller":"g1930y5ldjmckj24zu3gl2pxx9e0850t2zrunqcl","send":"","max_deposit":"","pkg_path":"gno.land/r/gnoland/wugnot","func":"Withdraw","args":["4182440"]}],"fee":{"gas_wanted":"387200000","gas_fee":"3872000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"A9UDKCaPGq1oTtoFI94ibcSg3lV0YLcECgQvxTh+M1kG"},"signature":"2Kf8MmPP5g5APa0EP3L7fqzUdP1Tm1EahtqwQqFYwoQQVj0H9KpGa3OAR9Aw2BAXgocafaLBjg+op/yjJNlbxw=="}],"memo":""},"metadata":{"timestamp":"1788754535"}}
{"tx":{"msg":[{"@type":"/vm.m_call","caller":"g1srl5zdx2a5r266c6qzns4u3cpth530m0q9phx5","send":"5664702ugnot","max_deposit":"","pkg_path":"gno.land/r/gnoland/wugnot","func":"Deposit"}],"fee":{"gas_wanted":"368000000","gas_fee":"3680000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"Au7vLP2h/AtE8wW1jkJp/RJz1P/odP2oy8jlN5bz8MM+"},"signature":"bOSJS93ABUrlKPfqq5/DCbAF+/2SgSxoavxoYcVJ3bUO6Z0FpA4diA8EgqSjmNlQuVIlpaJa9HxmM+In1Nru3g=="}],"memo":""},"metadata":{"timestamp":"1788754613"}}
{"tx":{"msg":[{"@type":"/vm.m_call","caller":"g1srl5zdx2a5r266c6qzns4u3cpth530m0q9phx5","send":"8311387ugnot","max_deposit":"","pkg_path":"gno.land/r/gnoland/wugnot","func":"Deposit"}],"fee":{"gas_wanted":"329600000","gas_fee":"3296000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"Au7vLP2h/AtE8wW1jkJp/RJz1P/odP2oy8jlN5bz8MM+"},"signature":"A0tgi1fcxN4S/G/KC5jmluFtgQ+OmHd2yTDCKrOiDYIfyRdLVQw8d9oz2ewYqxdRawLh/QbFe2mN9Iq+kixdfw=="}],"memo":""},"metadata":{"timestamp":"1788754624"}}
{"tx":{"msg":[{"@type":"/bank.MsgSend","from_address":"g18qhq2fl54lszhmxeyqlvxnwjzc3xpu4nnakclp","to_address":"g1dd64dmqkk7tytg90tqhnp6jr6uswz42qgtkd4q","amount":"300000000ugnot"}],"fee":{"gas_wanted":"5000000","gas_fee":"1000000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"AhUuKSwiL4XKy0BM9cK0UJFToerTzvgLTHFBvYk/xOtY"},"signature":"ctQY54hecZRDvevlw01JnO/fpYQ06+auigIHwNBVEU8LV6oRq1dJuj9MrHUk7brsj4InVteEW8R/r3qP5x5yxw=="}],"memo":""},"metadata":{"timestamp":"1788754627"}}
{"tx":{"msg":[{"@type":"/vm.m_call","caller":"g1ac0ng06auahty57xc2gv756rpj4mt9k7mdr95d","send":"","max_deposit":"","pkg_path":"gno.land/r/gnoland/wugnot","func":"Withdraw","args":["4596382"]}],"fee":{"gas_wanted":"348800000","gas_fee":"3488000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"AwUs79yWQSl5copyAbRzDiELl5oYjhcXo7mcz9TG+br9"},"signature":"f82v/U9sptvvrQc5WN8Okvq0MOmjNNFSjSDZWaDvQTsJrALCn2W4L2MMrAUlnJPCwpApLBWQOCmrp9Jr4bArgA=="}],"memo":""},"metadata":{"timestamp":"1788754711"}}
