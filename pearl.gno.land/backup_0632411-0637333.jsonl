{"tx":{"msg":[{"@type":"/vm.m_call","caller":"g13fmcl95ma9f5r9hhz0a96mwwhmdxrx73f93587","send":"","max_deposit":"","pkg_path":"gno.land/r/g142p4mjutq0dhu3vlfz32ymc490gk9d2sqglrak/costnotest/crowdfunding","func":"CreateCampaign","args":["Composed in a browser","99000000","1792701461"]}],"fee":{"gas_wanted":"1000000000","gas_fee":"1000000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"AsvB/2qgzHlBsiCGbgrBINRFk+H0Dhpwvl7YYhLVBxJ+"},"signature":"DaNq5NnwuMUGLqjopeAywQr/tU6Kj53GdoXlDk3pset5xax2LB1gZkxyEk3yTPSEj8sRMA3mNEIAnTpOQGwgxQ=="}],"memo":""},"metadata":{"timestamp":"1790109483"}}
{"tx":{"msg":[{"@type":"/bank.MsgSend","from_address":"g142p4mjutq0dhu3vlfz32ymc490gk9d2sqglrak","to_address":"g13fmcl95ma9f5r9hhz0a96mwwhmdxrx73f93587","amount":"28370510ugnot"}],"fee":{"gas_wanted":"981904","gas_fee":"984ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"A2GOzMGavtLOZjb9g1ltPKZsySYTl1dY+wHnkIs2zwhI"},"signature":"RAMteoNg4A+F57WrVqpzYwZelAyhhmuqdFW6Y60fEA4Iz+DD8KSx8dbEoziud3ZHkn7j2zCRmOH1ar0DwPl1Sw=="}],"memo":""},"metadata":{"timestamp":"1790113916"}}
{"tx":{"msg":[{"@type":"/bank.MsgSend","from_address":"g142p4mjutq0dhu3vlfz32ymc490gk9d2sqglrak","to_address":"g13ru04ttcfh2apyya44fe2vghnyt9tty98zeu2j","amount":"26337552ugnot"}],"fee":{"gas_wanted":"981904","gas_fee":"984ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"A2GOzMGavtLOZjb9g1ltPKZsySYTl1dY+wHnkIs2zwhI"},"signature":"wvGmOr12qkGz/T4Gd7dW92dZy1phbAfOmDEsv3lWuwgOcX0Svdu6bXFNTnkWk0Zv06MBAZwEtF1vlzi5O60BTQ=="}],"memo":""},"metadata":{"timestamp":"1790113923"}}
{"tx":{"msg":[{"@type":"/bank.MsgSend","from_address":"g142p4mjutq0dhu3vlfz32ymc490gk9d2sqglrak","to_address":"g18xat6nw5r3mz6894rmfdmm95lmfly4ku24ru6t","amount":"25917443ugnot"}],"fee":{"gas_wanted":"981904","gas_fee":"984ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"A2GOzMGavtLOZjb9g1ltPKZsySYTl1dY+wHnkIs2zwhI"},"signature":"E3j4KMsidMU1DX3j1c7mWzJ/dZq8WVe1WdWOWtVhcLchTBtMOUTvuP/Fv1Al+6Lg8vIKk2EZmE/d6c4EoUpMHA=="}],"memo":""},"metadata":{"timestamp":"1790113929"}}
{"tx":{"msg":[{"@type":"/bank.MsgSend","from_address":"g142p4mjutq0dhu3vlfz32ymc490gk9d2sqglrak","to_address":"g104jlra2w6uhflxvszf3qw3rg8qmglkg4dl3je5","amount":"25000000ugnot"}],"fee":{"gas_wanted":"979886","gas_fee":"982ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"A2GOzMGavtLOZjb9g1ltPKZsySYTl1dY+wHnkIs2zwhI"},"signature":"+O9GmJ+/8T+Z09gK01pj2uoroPWabxu1aTFFd10LH25JBlFW6i5kf0+EtmYT76yg/uZaw91LOsWqPttyYc+Lfw=="}],"memo":""},"metadata":{"timestamp":"1790113936"}}
{"tx":{"msg":[{"@type":"/vm.m_addpkg","creator":"g142p4mjutq0dhu3vlfz32ymc490gk9d2sqglrak","package":{"name":"crowdfunding","path":"gno.land/r/g142p4mjutq0dhu3vlfz32ymc490gk9d2sqglrak/gate1v2/crowdfunding","files":[{"name":"campaign.gno","body":"package crowdfunding\n\nimport (\n\t\"time\"\n\n\t\"gno.land/p/nt/avl/v0\"\n\t\"gno.land/p/nt/ufmt/v0\"\n)\n\n// maxInt64 bounds the checked-integer arithmetic on campaign totals.\nconst maxInt64 = 1\u003c\u003c63 - 1\n\n// Campaign holds the immutable terms and mutable funding state of a single\n// crowdfunding campaign.\ntype Campaign struct {\n\tid           int64\n\tcreator      address\n\ttitle        string\n\ttargetUgnot  int64\n\tdeadlineUnix int64\n\ttotalRaised  int64\n\twithdrawn    bool\n\n\t// updates holds the creator's append-only progress notes, bounded by\n\t// maxUpdateBytes and maxUpdatesPerCampaign (see crowdfunding.gno). The\n\t// String() view only reports a count, never iterates the slice, so\n\t// rendering a campaign stays O(1) regardless of update history.\n\tupdates []string\n\n\t// contributions maps a contributor address (string(address)) to their\n\t// cumulative contribution in ugnot. Zeroed on refund, never removed,\n\t// so a repeat Refund call reliably finds a zero balance.\n\tcontributions avl.Tree\n}\n\nfunc (c *Campaign) status() string {\n\tnow := time.Now().Unix()\n\tswitch {\n\tcase c.withdrawn:\n\t\treturn \"withdrawn\"\n\tcase now \u003c c.deadlineUnix:\n\t\treturn \"active\"\n\tcase c.totalRaised \u003e= c.targetUgnot:\n\t\treturn \"successful\"\n\tdefault:\n\t\treturn \"failed\"\n\t}\n}\n\nfunc (c *Campaign) String() string {\n\treturn ufmt.Sprintf(\n\t\t\"- id: %d\\n- title: %s\\n- creator: %s\\n- target: %dugnot\\n- raised: %dugnot\\n- deadline: %s\\n- status: %s\\n- updates: %d\",\n\t\tc.id, c.title, c.creator, c.targetUgnot, c.totalRaised,\n\t\ttime.Unix(c.deadlineUnix, 0).UTC().Format(time.RFC3339), c.status(), len(c.updates),\n\t)\n}\n"},{"name":"crowdfunding.gno","body":"// Package crowdfunding implements the V0 supported crowdfunding realm:\n// creators raise native ugnot until a deadline; campaigns that meet their\n// target pay the creator, campaigns that don't refund contributors.\n// \n// Creators raise native testnet GNOT until a deadline; successful campaigns pay creators and failed campaigns refund contributors.\n// \n// Generated by generator/ from the \"Community Crowdfunding\" app-spec (schema_version 1.0, revision 2). Do not hand-edit generated output; change the spec and regenerate instead.\npackage crowdfunding\n\nimport (\n\t\"chain\"\n\t\"chain/banker\"\n\t\"chain/runtime/unsafe\"\n\t\"strconv\"\n\t\"strings\"\n\t\"time\"\n\t\"unicode/utf8\"\n\n\t\"gno.land/p/nt/avl/v0\"\n\t\"gno.land/p/nt/ufmt/v0\"\n)\n\nconst (\n\t// maxTitleBytes matches the spec's ui.title_max_utf8_bytes.\n\tmaxTitleBytes = 256\n\t// pageSize matches the spec's ui.page_size for the home listing.\n\tpageSize = 20\n\t// minimumContributionUgnot matches the spec's rules.minimum_contribution_ugnot.\n\tminimumContributionUgnot int64 = 25000000\n\t// denom is the only accepted native denomination (spec currency.denom).\n\tdenom = \"ugnot\"\n\t// maxDeadlineSecondsFromNow bounds how far in the future a campaign\n\t// deadline may be set (see CreateCampaign). This exists specifically to\n\t// catch the plausible mistake of passing milliseconds where whole\n\t// seconds are expected: a millisecond timestamp is ~1000x a second\n\t// timestamp, so even a short 30-day campaign becomes roughly 82 years\n\t// out, and a 1-year campaign becomes roughly 1000 years out. Once\n\t// accepted, the mistake is unrecoverable -- a deadline this far out\n\t// never passes, so contributors can never be refunded and the creator\n\t// can never withdraw either. Ten years comfortably exceeds any\n\t// realistic V0 campaign length while still rejecting that entire class\n\t// of typo by a wide margin.\n\tmaxDeadlineSecondsFromNow int64 = 10 * 365 * 24 * 60 * 60\n\t// campaignKeyWidth zero-pads campaign keys (see campaignKey) so the\n\t// avl.Tree's lexicographic key ordering matches numeric id ordering.\n\t// 19 digits covers every positive int64 (max 9223372036854775807).\n\tcampaignKeyWidth = 19\n\t// maxUpdateBytes and maxUpdatesPerCampaign match the spec's\n\t// updates.max_utf8_bytes and updates.max_per_campaign.\n\tmaxUpdateBytes        = 2000\n\tmaxUpdatesPerCampaign = 50\n)\n\nvar (\n\tcampaigns      avl.Tree // campaignKey(id) -\u003e *Campaign\n\tnextCampaignID int64\n)\n\n// campaignKey renders a campaign id as a fixed-width, zero-padded decimal\n// string so avl.Tree's lexicographic key order matches numeric id order. An\n// unpadded strconv.FormatInt key sorts \"10\" before \"2\", which silently\n// breaks both direct id lookups' consistency with the tree's own ordering\n// and the home listing's newest-first ordering past the ninth campaign.\nfunc campaignKey(id int64) string {\n\ts := strconv.FormatInt(id, 10)\n\tfor len(s) \u003c campaignKeyWidth {\n\t\ts = \"0\" + s\n\t}\n\treturn s\n}\n\n// sanitizeInline conservatively escapes untrusted, user-supplied text (only\n// campaign titles in this realm) before it is stored, so every present and\n// future render path (the home listing, a campaign's String() detail view,\n// and any future Render addition) reads already-clean state instead of\n// needing to remember to sanitize at each render site.\n//\n// A raw title reaching a render path unescaped can forge the surrounding\n// structure it's spliced into: an embedded newline can start a fake\n// \"- creator: g1...\" line in the listing or in a campaign's own detail view,\n// and an unescaped \"](\" can close a markdown link label early and open a\n// javascript: destination. Everything this function emits is a named or\n// numeric XML/HTML character reference, safe as gnoweb markdown text.\nfunc sanitizeInline(s string) string {\n\tif len(s) \u003e maxTitleBytes {\n\t\ts = s[:maxTitleBytes]\n\t}\n\tvar b strings.Builder\n\tfor _, r := range s {\n\t\tswitch r {\n\t\tcase '\u0026':\n\t\t\tb.WriteString(\"\u0026amp;\")\n\t\tcase '\u003c':\n\t\t\tb.WriteString(\"\u0026lt;\")\n\t\tcase '\u003e':\n\t\t\tb.WriteString(\"\u0026gt;\")\n\t\tcase '\"':\n\t\t\tb.WriteString(\"\u0026quot;\")\n\t\tcase '\\'':\n\t\t\tb.WriteString(\"\u0026#39;\")\n\t\tcase '(':\n\t\t\tb.WriteString(\"\u0026#40;\")\n\t\tcase ')':\n\t\t\tb.WriteString(\"\u0026#41;\")\n\t\tcase '[':\n\t\t\tb.WriteString(\"\u0026#91;\")\n\t\tcase ']':\n\t\t\tb.WriteString(\"\u0026#93;\")\n\t\tcase '|':\n\t\t\tb.WriteString(\"\u0026#124;\")\n\t\tcase '\\n', '\\r', '\\t':\n\t\t\tb.WriteRune(' ')\n\t\tdefault:\n\t\t\tif r \u003c 0x20 {\n\t\t\t\tcontinue // drop other control characters outright\n\t\t\t}\n\t\t\tb.WriteRune(r)\n\t\t}\n\t}\n\treturn b.String()\n}\n\n// CreateCampaign registers a new campaign with a funding target (in ugnot)\n// and a UTC unix deadline. It does not receive funds; sends_native_value is\n// false for this action per the spec, so no OriginSend guard applies here.\nfunc CreateCampaign(cur realm, title string, targetUgnot int64, deadlineUnix int64) int64 {\n\tcreator := cur.Previous().Address()\n\n\tif len(title) == 0 {\n\t\tpanic(\"title must not be empty\")\n\t}\n\tif len(title) \u003e maxTitleBytes {\n\t\tpanic(\"title exceeds max size\")\n\t}\n\tif !utf8.ValidString(title) {\n\t\tpanic(\"title must be valid utf-8\")\n\t}\n\tif targetUgnot \u003c= 0 {\n\t\tpanic(\"target must be positive\")\n\t}\n\tif deadlineUnix \u003c= time.Now().Unix() {\n\t\tpanic(\"deadline must be in the future\")\n\t}\n\tif deadlineUnix \u003e time.Now().Unix()+maxDeadlineSecondsFromNow {\n\t\tpanic(\"deadline too far in the future\")\n\t}\n\n\tnextCampaignID++\n\tid := nextCampaignID\n\tcampaign := \u0026Campaign{\n\t\tid:           id,\n\t\tcreator:      creator,\n\t\ttitle:        sanitizeInline(title),\n\t\ttargetUgnot:  targetUgnot,\n\t\tdeadlineUnix: deadlineUnix,\n\t}\n\tcampaigns.Set(campaignKey(id), campaign)\n\treturn id\n}\n\n// Contribute sends native ugnot to a campaign before its deadline.\n//\n// It must be called directly by a user account (maketx call): unsafe.OriginSend()\n// only describes a real receipt at this realm when the caller is a pure user\n// account. An intermediate code realm or a `maketx run` ephemeral realm can\n// attach -send to a transaction but spend the envelope elsewhere, leaving\n// OriginSend() reporting a phantom payment that would let a caller book a\n// contribution never made and later drain the realm's real balance on\n// Withdraw/Refund. This guard is the load-bearing line of the whole realm.\nfunc Contribute(cur realm, campaignID int64) {\n\tif !cur.Previous().IsUserCall() {\n\t\tpanic(\"only user-call (maketx call) accepted\")\n\t}\n\tcontributor := cur.Previous().Address()\n\tcampaign := mustGetCampaign(campaignID)\n\n\tif time.Now().Unix() \u003e= campaign.deadlineUnix {\n\t\tpanic(\"campaign deadline has passed\")\n\t}\n\n\tsent := unsafe.OriginSend()\n\tif len(sent) == 0 {\n\t\tpanic(\"no coins sent\")\n\t}\n\tif len(sent) != 1 || sent[0].Denom != denom {\n\t\tpanic(\"only ugnot contributions are accepted\")\n\t}\n\tamount := sent[0].Amount\n\tif amount \u003c minimumContributionUgnot {\n\t\tpanic(\"contribution below minimum\")\n\t}\n\n\tkey := string(contributor)\n\tvar prev int64\n\tif v := campaign.contributions.Get(key); v != nil {\n\t\tprev = v.(int64)\n\t}\n\tif prev \u003e maxInt64-amount {\n\t\tpanic(\"contribution would overflow\")\n\t}\n\tif campaign.totalRaised \u003e maxInt64-amount {\n\t\tpanic(\"campaign total would overflow\")\n\t}\n\n\tcampaign.contributions.Set(key, prev+amount)\n\tcampaign.totalRaised += amount\n}\n\n// Withdraw pays a campaign's full raised balance to its creator, once, and\n// only once the deadline has passed with the target reached.\n//\n// totalRaised is never mutated by Withdraw or Refund: it is the permanent\n// gross-contributions figure (spec: success_basis gross_accepted_contributions),\n// fixed the moment the deadline passes, since Contribute rejects any call\n// once the deadline has passed. Withdraw and Refund are mutually exclusive\n// because they read that same fixed field with complementary conditions\n// (\u003e= target vs \u003c target), so neither action's own bookkeeping can ever\n// flip the campaign's success/failure outcome and let the other action fire\n// afterward.\nfunc Withdraw(cur realm, campaignID int64) {\n\tcaller := cur.Previous().Address()\n\tcampaign := mustGetCampaign(campaignID)\n\n\tif campaign.withdrawn {\n\t\tpanic(\"campaign funds already withdrawn\")\n\t}\n\tif caller != campaign.creator {\n\t\tpanic(\"only the campaign creator can withdraw\")\n\t}\n\tif time.Now().Unix() \u003c campaign.deadlineUnix {\n\t\tpanic(\"campaign deadline has not passed\")\n\t}\n\tif campaign.totalRaised \u003c campaign.targetUgnot {\n\t\tpanic(\"campaign did not reach its target\")\n\t}\n\n\t// Zero (flip) the withdrawn flag before transferring so a re-entrant\n\t// or repeated call cannot double-spend the creator payout.\n\tamount := campaign.totalRaised\n\tcampaign.withdrawn = true\n\n\tbanker_ := banker.NewBanker(banker.BankerTypeRealmSend, cur)\n\tpkgAddr := cur.Address()\n\tbanker_.SendCoins(pkgAddr, campaign.creator, chain.NewCoins(chain.NewCoin(denom, amount)))\n}\n\n// Refund returns a caller's own cumulative contribution, once, and only\n// once the deadline has passed with the target not reached. See Withdraw\n// for why the two are mutually exclusive. There is no address parameter:\n// entitlement comes only from the authenticated caller's own ledger entry\n// (permission: own_contribution), never from an argument a caller supplies.\nfunc Refund(cur realm, campaignID int64) {\n\tcaller := cur.Previous().Address()\n\tcampaign := mustGetCampaign(campaignID)\n\n\tkey := string(caller)\n\tv := campaign.contributions.Get(key)\n\tif v == nil {\n\t\tpanic(\"no contribution found for caller\")\n\t}\n\tamount := v.(int64)\n\tif amount \u003c= 0 {\n\t\tpanic(\"no contribution found for caller\")\n\t}\n\tif time.Now().Unix() \u003c campaign.deadlineUnix {\n\t\tpanic(\"campaign deadline has not passed\")\n\t}\n\tif campaign.totalRaised \u003e= campaign.targetUgnot {\n\t\tpanic(\"campaign succeeded; refunds are not available\")\n\t}\n\n\t// Zero this contributor's ledger entry before transferring so a\n\t// re-entrant or repeated call cannot double-spend their refund.\n\t// totalRaised is deliberately left untouched (see Withdraw).\n\tcampaign.contributions.Set(key, int64(0))\n\n\tbanker_ := banker.NewBanker(banker.BankerTypeRealmSend, cur)\n\tpkgAddr := cur.Address()\n\tbanker_.SendCoins(pkgAddr, caller, chain.NewCoins(chain.NewCoin(denom, amount)))\n}\n\n// ViewCampaign is the dedicated public read entrypoint for a single\n// campaign, matching the spec's ViewCampaign action (permission: public).\n// It is not a crossing function: it needs no caller identity and mutates\n// no state, same as Render's per-id branch, which returns the identical\n// detail view.\nfunc ViewCampaign(campaignID int64) string {\n\treturn mustGetCampaign(campaignID).String()\n}\n\n// AddUpdate appends a bounded, creator-only progress note to a campaign\n// (permission: campaign_creator, matching Withdraw). Updates are\n// append-only: there is no edit or delete entrypoint, and AddUpdate never\n// touches target, deadline, creator or refund/withdraw eligibility, per the\n// spec's updates.changes_financial_terms = false.\nfunc AddUpdate(cur realm, campaignID int64, body string) {\n\tcaller := cur.Previous().Address()\n\tcampaign := mustGetCampaign(campaignID)\n\n\tif caller != campaign.creator {\n\t\tpanic(\"only the campaign creator can add updates\")\n\t}\n\tif len(body) == 0 {\n\t\tpanic(\"update body must not be empty\")\n\t}\n\tif len(body) \u003e maxUpdateBytes {\n\t\tpanic(\"update exceeds max size\")\n\t}\n\tif len(campaign.updates) \u003e= maxUpdatesPerCampaign {\n\t\tpanic(\"campaign has reached its update limit\")\n\t}\n\n\tcampaign.updates = append(campaign.updates, body)\n}\n\n// Render returns a campaign list at the root path, and a single campaign's\n// detail view at a sub-path holding its id.\nfunc Render(path string) string {\n\tif path == \"\" {\n\t\treturn renderHome()\n\t}\n\tid, err := strconv.ParseInt(path, 10, 64)\n\tif err != nil {\n\t\treturn \"404\"\n\t}\n\tv := campaigns.Get(campaignKey(id))\n\tif v == nil {\n\t\treturn \"404\"\n\t}\n\treturn v.(*Campaign).String()\n}\n\n// renderHome lists the pageSize most recently created campaigns, newest\n// first. ReverseIterateByOffset walks the avl.Tree largest-key-first, so an\n// offset of 0 is what actually means \"start from the newest\": a nonzero\n// offset here would skip that many of the newest entries instead, which is\n// never what a \"recent campaigns\" listing wants.\nfunc renderHome() string {\n\toutput := \"# Community Crowdfunding\" + \"\\n\\n\"\n\tsize := campaigns.Size()\n\tif size == 0 {\n\t\treturn output + \"No campaigns yet.\\n\"\n\t}\n\tcampaigns.ReverseIterateByOffset(0, pageSize, func(key string, value any) bool {\n\t\tc := value.(*Campaign)\n\t\toutput += ufmt.Sprintf(\"- %d: %s - target %dugnot raised %dugnot - %s\\n\",\n\t\t\tc.id, c.title, c.targetUgnot, c.totalRaised, c.status())\n\t\treturn false\n\t})\n\treturn output\n}\n\nfunc mustGetCampaign(campaignID int64) *Campaign {\n\tv := campaigns.Get(campaignKey(campaignID))\n\tif v == nil {\n\t\tpanic(\"unknown campaign id\")\n\t}\n\treturn v.(*Campaign)\n}\n"},{"name":"crowdfunding_test.gno","body":"package crowdfunding\n\nimport (\n\t\"chain\"\n\t\"strings\"\n\t\"testing\"\n\t\"time\"\n\n\t\"gno.land/p/nt/avl/v0\"\n\t\"gno.land/p/nt/testutils/v0\"\n\t\"gno.land/p/nt/ufmt/v0\"\n\t\"gno.land/p/nt/uassert/v0\"\n)\n\nconst modulePath = \"gno.land/r/g142p4mjutq0dhu3vlfz32ymc490gk9d2sqglrak/gate1v2/crowdfunding\"\n\nfunc resetTestState() {\n\tcampaigns = avl.Tree{}\n\tnextCampaignID = 0\n}\n\n// contribute is a test helper: it switches to contributor, declares an\n// origin send of amount ugnot, credits the realm's bank balance so a later\n// Withdraw/Refund SendCoins has funds to move, and calls Contribute.\nfunc contribute(cur realm, t *testing.T, contributor address, campaignID int64, amount int64) {\n\tpkgAddr := chain.PackageAddress(modulePath)\n\ttesting.SetRealm(testing.NewUserRealm(contributor))\n\ttesting.SetOriginSend(chain.Coins{{denom, amount}})\n\ttesting.IssueCoins(pkgAddr, chain.Coins{{denom, amount}})\n\tContribute(cross(cur), campaignID)\n}\n\nfunc TestCreateCampaign_Success(cur realm, t *testing.T) {\n\tdefer resetTestState()\n\n\tcreator := testutils.TestAddress(\"creator1\")\n\ttesting.SetRealm(testing.NewUserRealm(creator))\n\n\tdeadline := time.Now().Add(1 * time.Hour).Unix()\n\ttarget := minimumContributionUgnot * 10\n\tid := CreateCampaign(cross(cur), \"Build a well\", target, deadline)\n\tuassert.Equal(t, int64(1), id)\n\n\tc := mustGetCampaign(id)\n\tuassert.Equal(t, \"Build a well\", c.title)\n\tuassert.Equal(t, creator, c.creator)\n\tuassert.Equal(t, target, c.targetUgnot)\n\tuassert.Equal(t, int64(0), c.totalRaised)\n\tuassert.False(t, c.withdrawn)\n}\n\nfunc TestCreateCampaign_InvalidTargetRefused(cur realm, t *testing.T) {\n\tdefer resetTestState()\n\n\tcreator := testutils.TestAddress(\"creator2\")\n\ttesting.SetRealm(testing.NewUserRealm(creator))\n\tdeadline := time.Now().Add(1 * time.Hour).Unix()\n\n\tuassert.AbortsWithMessage(t, cur, \"target must be positive\", func() {\n\t\tCreateCampaign(cross(cur), \"Bad target\", 0, deadline)\n\t})\n}\n\nfunc TestCreateCampaign_PastDeadlineRefused(cur realm, t *testing.T) {\n\tdefer resetTestState()\n\n\tcreator := testutils.TestAddress(\"creator3\")\n\ttesting.SetRealm(testing.NewUserRealm(creator))\n\tpastDeadline := time.Now().Add(-1 * time.Hour).Unix()\n\n\tuassert.AbortsWithMessage(t, cur, \"deadline must be in the future\", func() {\n\t\tCreateCampaign(cross(cur), \"Too late\", minimumContributionUgnot*10, pastDeadline)\n\t})\n}\n\nfunc TestContribute_BelowMinimumRefused(cur realm, t *testing.T) {\n\tdefer resetTestState()\n\n\tcreator := testutils.TestAddress(\"creator4\")\n\tcontributor := testutils.TestAddress(\"contributor4\")\n\ttesting.SetRealm(testing.NewUserRealm(creator))\n\tdeadline := time.Now().Add(1 * time.Hour).Unix()\n\tid := CreateCampaign(cross(cur), \"Below minimum\", minimumContributionUgnot*10, deadline)\n\n\tpkgAddr := chain.PackageAddress(modulePath)\n\tbelowMinimum := minimumContributionUgnot - 1\n\ttesting.SetRealm(testing.NewUserRealm(contributor))\n\ttesting.SetOriginSend(chain.Coins{{denom, belowMinimum}})\n\ttesting.IssueCoins(pkgAddr, chain.Coins{{denom, belowMinimum}})\n\tuassert.AbortsWithMessage(t, cur, \"contribution below minimum\", func() {\n\t\tContribute(cross(cur), id)\n\t})\n}\n\nfunc TestContribute_ExactMinimumAccepted(cur realm, t *testing.T) {\n\tdefer resetTestState()\n\n\tcreator := testutils.TestAddress(\"creator5\")\n\tcontributor := testutils.TestAddress(\"contributor5\")\n\ttesting.SetRealm(testing.NewUserRealm(creator))\n\tdeadline := time.Now().Add(1 * time.Hour).Unix()\n\tid := CreateCampaign(cross(cur), \"Exact minimum\", minimumContributionUgnot*10, deadline)\n\n\tcontribute(cur, t, contributor, id, minimumContributionUgnot)\n\n\tc := mustGetCampaign(id)\n\tuassert.Equal(t, minimumContributionUgnot, c.totalRaised)\n}\n\nfunc TestContribute_WrongDenomRefused(cur realm, t *testing.T) {\n\tdefer resetTestState()\n\n\tcreator := testutils.TestAddress(\"creator6\")\n\tcontributor := testutils.TestAddress(\"contributor6\")\n\ttesting.SetRealm(testing.NewUserRealm(creator))\n\tdeadline := time.Now().Add(1 * time.Hour).Unix()\n\tid := CreateCampaign(cross(cur), \"Denom test\", minimumContributionUgnot*10, deadline)\n\n\tpkgAddr := chain.PackageAddress(modulePath)\n\ttesting.SetRealm(testing.NewUserRealm(contributor))\n\ttesting.SetOriginSend(chain.Coins{{\"otherdenom\", minimumContributionUgnot}})\n\ttesting.IssueCoins(pkgAddr, chain.Coins{{\"otherdenom\", minimumContributionUgnot}})\n\tuassert.AbortsWithMessage(t, cur, \"only ugnot contributions are accepted\", func() {\n\t\tContribute(cross(cur), id)\n\t})\n}\n\nfunc TestContribute_AfterDeadlineRefused(cur realm, t *testing.T) {\n\tdefer resetTestState()\n\n\tcreator := testutils.TestAddress(\"creator7\")\n\tcontributor := testutils.TestAddress(\"contributor7\")\n\ttesting.SetRealm(testing.NewUserRealm(creator))\n\tdeadline := time.Now().Add(1 * time.Hour).Unix()\n\tid := CreateCampaign(cross(cur), \"Expiring\", minimumContributionUgnot*10, deadline)\n\n\tc := mustGetCampaign(id)\n\tc.deadlineUnix = time.Now().Add(-1 * time.Hour).Unix() // simulate deadline passed\n\n\tpkgAddr := chain.PackageAddress(modulePath)\n\ttesting.SetRealm(testing.NewUserRealm(contributor))\n\ttesting.SetOriginSend(chain.Coins{{denom, minimumContributionUgnot}})\n\ttesting.IssueCoins(pkgAddr, chain.Coins{{denom, minimumContributionUgnot}})\n\tuassert.AbortsWithMessage(t, cur, \"campaign deadline has passed\", func() {\n\t\tContribute(cross(cur), id)\n\t})\n}\n\n// TestContribute_AtExactDeadlineRefused is the boundary case for the guard\n// `if time.Now().Unix() \u003e= campaign.deadlineUnix`: a contribution landing on\n// the deadline second itself must be refused, not just one landing\n// comfortably after it. Weakening `\u003e=` to `\u003e` would accept this exact-second\n// contribution while still refusing a later one, so a test that only\n// exercises \"well past the deadline\" (TestContribute_AfterDeadlineRefused,\n// above) cannot catch that regression.\nfunc TestContribute_AtExactDeadlineRefused(cur realm, t *testing.T) {\n\tdefer resetTestState()\n\n\tcreator := testutils.TestAddress(\"creator20\")\n\tcontributor := testutils.TestAddress(\"contributor20\")\n\ttesting.SetRealm(testing.NewUserRealm(creator))\n\tdeadline := time.Now().Add(1 * time.Hour).Unix()\n\tid := CreateCampaign(cross(cur), \"Exact deadline\", minimumContributionUgnot*10, deadline)\n\n\tc := mustGetCampaign(id)\n\tc.deadlineUnix = time.Now().Unix() // the boundary itself, not a past deadline\n\n\tpkgAddr := chain.PackageAddress(modulePath)\n\ttesting.SetRealm(testing.NewUserRealm(contributor))\n\ttesting.SetOriginSend(chain.Coins{{denom, minimumContributionUgnot}})\n\ttesting.IssueCoins(pkgAddr, chain.Coins{{denom, minimumContributionUgnot}})\n\tuassert.AbortsWithMessage(t, cur, \"campaign deadline has passed\", func() {\n\t\tContribute(cross(cur), id)\n\t})\n}\n\n// TestContribute_ContributorOverflowRefused seeds this contributor's running\n// total directly to one short of maxInt64 (no real sequence of contributions\n// could reach it) so the next contribution overflows only the\n// per-contributor guard (`prev \u003e maxInt64-amount`), not the campaign-total\n// guard: totalRaised is left at its zero value.\nfunc TestContribute_ContributorOverflowRefused(cur realm, t *testing.T) {\n\tdefer resetTestState()\n\n\tcreator := testutils.TestAddress(\"creator21\")\n\tcontributor := testutils.TestAddress(\"contributor21\")\n\ttesting.SetRealm(testing.NewUserRealm(creator))\n\tdeadline := time.Now().Add(1 * time.Hour).Unix()\n\tid := CreateCampaign(cross(cur), \"Overflow contributor\", minimumContributionUgnot*10, deadline)\n\n\tc := mustGetCampaign(id)\n\tc.contributions.Set(string(contributor), maxInt64-minimumContributionUgnot)\n\n\tpkgAddr := chain.PackageAddress(modulePath)\n\tamount := minimumContributionUgnot + 1\n\ttesting.SetRealm(testing.NewUserRealm(contributor))\n\ttesting.SetOriginSend(chain.Coins{{denom, amount}})\n\ttesting.IssueCoins(pkgAddr, chain.Coins{{denom, amount}})\n\tuassert.AbortsWithMessage(t, cur, \"contribution would overflow\", func() {\n\t\tContribute(cross(cur), id)\n\t})\n}\n\n// TestContribute_CampaignTotalOverflowRefused seeds the campaign's total\n// directly to one short of maxInt64 (no real sequence of contributions could\n// reach it) so the next contribution overflows only the campaign-total guard\n// (`campaign.totalRaised \u003e maxInt64-amount`), not the per-contributor guard:\n// this contributor's own running total is still zero.\nfunc TestContribute_CampaignTotalOverflowRefused(cur realm, t *testing.T) {\n\tdefer resetTestState()\n\n\tcreator := testutils.TestAddress(\"creator22\")\n\tcontributor := testutils.TestAddress(\"contributor22\")\n\ttesting.SetRealm(testing.NewUserRealm(creator))\n\tdeadline := time.Now().Add(1 * time.Hour).Unix()\n\tid := CreateCampaign(cross(cur), \"Overflow campaign total\", minimumContributionUgnot*10, deadline)\n\n\tc := mustGetCampaign(id)\n\tc.totalRaised = maxInt64 - minimumContributionUgnot // one short of overflowing\n\n\tpkgAddr := chain.PackageAddress(modulePath)\n\tamount := minimumContributionUgnot + 1\n\ttesting.SetRealm(testing.NewUserRealm(contributor))\n\ttesting.SetOriginSend(chain.Coins{{denom, amount}})\n\ttesting.IssueCoins(pkgAddr, chain.Coins{{denom, amount}})\n\tuassert.AbortsWithMessage(t, cur, \"campaign total would overflow\", func() {\n\t\tContribute(cross(cur), id)\n\t})\n}\n\n// TestContribute_CodeRealmCallerRefused exercises Contribute's very first\n// guard, `if !cur.Previous().IsUserCall() { panic(...) }` -- the guard the\n// source's own comment calls \"the load-bearing line of the whole realm\",\n// because it is the only thing standing between unsafe.OriginSend() and a\n// caller that can attach -send to a transaction without a matching deposit\n// ever reaching this realm. testing.NewCodeRealm constructs a caller whose\n// immediate frame is a code realm rather than a genuine user account, which\n// is exactly the case IsUserCall() must reject; the preceding\n// testing.NewUserRealm call gives that code realm a real user frame beneath\n// it, mirroring how gnovm's own zrealm_crossrealm13.gno test builds a\n// non-user-call frame. The realm path below is a deliberately fictional test\n// fixture (not a real deployed realm) so a reader can't mistake it for an\n// actual dependency of this contract.\nfunc TestContribute_CodeRealmCallerRefused(cur realm, t *testing.T) {\n\tdefer resetTestState()\n\n\tcreator := testutils.TestAddress(\"creator23\")\n\ttesting.SetRealm(testing.NewUserRealm(creator))\n\tdeadline := time.Now().Add(1 * time.Hour).Unix()\n\tid := CreateCampaign(cross(cur), \"Code realm caller\", minimumContributionUgnot*10, deadline)\n\n\tfakeCallerAccount := testutils.TestAddress(\"contributor23\")\n\ttesting.SetRealm(testing.NewUserRealm(fakeCallerAccount))\n\ttesting.SetRealm(testing.NewCodeRealm(\"gno.land/r/gnobuilder/test_fixtures/fake_code_realm\"))\n\tuassert.AbortsWithMessage(t, cur, \"only user-call (maketx call) accepted\", func() {\n\t\tContribute(cross(cur), id)\n\t})\n}\n\nfunc TestSuccessfulCampaign_CreatorWithdraws(cur realm, t *testing.T) {\n\tdefer resetTestState()\n\n\tcreator := testutils.TestAddress(\"creator8\")\n\tcontributor := testutils.TestAddress(\"contributor8\")\n\ttesting.SetRealm(testing.NewUserRealm(creator))\n\tdeadline := time.Now().Add(1 * time.Hour).Unix()\n\ttarget := minimumContributionUgnot * 10\n\tid := CreateCampaign(cross(cur), \"Fund the thing\", target, deadline)\n\n\traised := minimumContributionUgnot * 15\n\tcontribute(cur, t, contributor, id, raised)\n\n\tc := mustGetCampaign(id)\n\tuassert.Equal(t, raised, c.totalRaised)\n\tc.deadlineUnix = time.Now().Add(-1 * time.Hour).Unix() // simulate deadline passed\n\n\ttesting.SetRealm(testing.NewUserRealm(creator))\n\tWithdraw(cross(cur), id)\n\n\tuassert.True(t, c.withdrawn)\n\tuassert.Equal(t, raised, c.totalRaised) // gross-raised figure stays fixed, only withdrawn flips\n}\n\nfunc TestWithdraw_DoubleWithdrawRefused(cur realm, t *testing.T) {\n\tdefer resetTestState()\n\n\tcreator := testutils.TestAddress(\"creator9\")\n\tcontributor := testutils.TestAddress(\"contributor9\")\n\ttesting.SetRealm(testing.NewUserRealm(creator))\n\tdeadline := time.Now().Add(1 * time.Hour).Unix()\n\ttarget := minimumContributionUgnot * 10\n\tid := CreateCampaign(cross(cur), \"Double withdraw\", target, deadline)\n\n\tcontribute(cur, t, contributor, id, target)\n\n\tc := mustGetCampaign(id)\n\tc.deadlineUnix = time.Now().Add(-1 * time.Hour).Unix()\n\n\ttesting.SetRealm(testing.NewUserRealm(creator))\n\tWithdraw(cross(cur), id)\n\tuassert.AbortsWithMessage(t, cur, \"campaign funds already withdrawn\", func() {\n\t\tWithdraw(cross(cur), id)\n\t})\n}\n\nfunc TestWithdraw_NonCreatorRefused(cur realm, t *testing.T) {\n\tdefer resetTestState()\n\n\tcreator := testutils.TestAddress(\"creator10\")\n\tcontributor := testutils.TestAddress(\"contributor10\")\n\tstranger := testutils.TestAddress(\"stranger10\")\n\ttesting.SetRealm(testing.NewUserRealm(creator))\n\tdeadline := time.Now().Add(1 * time.Hour).Unix()\n\ttarget := minimumContributionUgnot * 10\n\tid := CreateCampaign(cross(cur), \"Not yours\", target, deadline)\n\n\tcontribute(cur, t, contributor, id, target)\n\n\tc := mustGetCampaign(id)\n\tc.deadlineUnix = time.Now().Add(-1 * time.Hour).Unix()\n\n\ttesting.SetRealm(testing.NewUserRealm(stranger))\n\tuassert.AbortsWithMessage(t, cur, \"only the campaign creator can withdraw\", func() {\n\t\tWithdraw(cross(cur), id)\n\t})\n}\n\nfunc TestWithdraw_BeforeDeadlineRefused(cur realm, t *testing.T) {\n\tdefer resetTestState()\n\n\tcreator := testutils.TestAddress(\"creator11\")\n\tcontributor := testutils.TestAddress(\"contributor11\")\n\ttesting.SetRealm(testing.NewUserRealm(creator))\n\tdeadline := time.Now().Add(1 * time.Hour).Unix()\n\ttarget := minimumContributionUgnot * 10\n\tid := CreateCampaign(cross(cur), \"Too early\", target, deadline)\n\n\tcontribute(cur, t, contributor, id, target)\n\n\ttesting.SetRealm(testing.NewUserRealm(creator))\n\tuassert.AbortsWithMessage(t, cur, \"campaign deadline has not passed\", func() {\n\t\tWithdraw(cross(cur), id)\n\t})\n}\n\nfunc TestWithdraw_TargetNotMetRefused(cur realm, t *testing.T) {\n\tdefer resetTestState()\n\n\tcreator := testutils.TestAddress(\"creator12\")\n\tcontributor := testutils.TestAddress(\"contributor12\")\n\ttesting.SetRealm(testing.NewUserRealm(creator))\n\tdeadline := time.Now().Add(1 * time.Hour).Unix()\n\ttarget := minimumContributionUgnot * 10\n\tid := CreateCampaign(cross(cur), \"Underfunded\", target, deadline)\n\n\tcontribute(cur, t, contributor, id, minimumContributionUgnot*2)\n\n\tc := mustGetCampaign(id)\n\tc.deadlineUnix = time.Now().Add(-1 * time.Hour).Unix()\n\n\ttesting.SetRealm(testing.NewUserRealm(creator))\n\tuassert.AbortsWithMessage(t, cur, \"campaign did not reach its target\", func() {\n\t\tWithdraw(cross(cur), id)\n\t})\n}\n\nfunc TestFailedCampaign_ContributorRefunded(cur realm, t *testing.T) {\n\tdefer resetTestState()\n\n\tcreator := testutils.TestAddress(\"creator13\")\n\tcontributor := testutils.TestAddress(\"contributor13\")\n\ttesting.SetRealm(testing.NewUserRealm(creator))\n\tdeadline := time.Now().Add(1 * time.Hour).Unix()\n\ttarget := minimumContributionUgnot * 10\n\tid := CreateCampaign(cross(cur), \"Will fail\", target, deadline)\n\n\tshortfall := minimumContributionUgnot * 3\n\tcontribute(cur, t, contributor, id, shortfall)\n\n\tc := mustGetCampaign(id)\n\tc.deadlineUnix = time.Now().Add(-1 * time.Hour).Unix()\n\n\ttesting.SetRealm(testing.NewUserRealm(contributor))\n\tRefund(cross(cur), id)\n\n\tuassert.Equal(t, shortfall, c.totalRaised) // gross-raised figure stays fixed\n\tuassert.Equal(t, int64(0), c.contributions.Get(string(contributor)).(int64))\n}\n\nfunc TestRefund_DoubleRefundRefused(cur realm, t *testing.T) {\n\tdefer resetTestState()\n\n\tcreator := testutils.TestAddress(\"creator14\")\n\tcontributor := testutils.TestAddress(\"contributor14\")\n\ttesting.SetRealm(testing.NewUserRealm(creator))\n\tdeadline := time.Now().Add(1 * time.Hour).Unix()\n\ttarget := minimumContributionUgnot * 10\n\tid := CreateCampaign(cross(cur), \"Double refund\", target, deadline)\n\n\tcontribute(cur, t, contributor, id, minimumContributionUgnot*3)\n\n\tc := mustGetCampaign(id)\n\tc.deadlineUnix = time.Now().Add(-1 * time.Hour).Unix()\n\n\ttesting.SetRealm(testing.NewUserRealm(contributor))\n\tRefund(cross(cur), id)\n\tuassert.AbortsWithMessage(t, cur, \"no contribution found for caller\", func() {\n\t\tRefund(cross(cur), id)\n\t})\n}\n\nfunc TestRefund_NonContributorRefused(cur realm, t *testing.T) {\n\tdefer resetTestState()\n\n\tcreator := testutils.TestAddress(\"creator15\")\n\tcontributor := testutils.TestAddress(\"contributor15\")\n\tstranger := testutils.TestAddress(\"stranger15\")\n\ttesting.SetRealm(testing.NewUserRealm(creator))\n\tdeadline := time.Now().Add(1 * time.Hour).Unix()\n\ttarget := minimumContributionUgnot * 10\n\tid := CreateCampaign(cross(cur), \"Stranger refund\", target, deadline)\n\n\tcontribute(cur, t, contributor, id, minimumContributionUgnot*3)\n\n\tc := mustGetCampaign(id)\n\tc.deadlineUnix = time.Now().Add(-1 * time.Hour).Unix()\n\n\ttesting.SetRealm(testing.NewUserRealm(stranger))\n\tuassert.AbortsWithMessage(t, cur, \"no contribution found for caller\", func() {\n\t\tRefund(cross(cur), id)\n\t})\n}\n\nfunc TestRefund_BeforeDeadlineRefused(cur realm, t *testing.T) {\n\tdefer resetTestState()\n\n\tcreator := testutils.TestAddress(\"creator16\")\n\tcontributor := testutils.TestAddress(\"contributor16\")\n\ttesting.SetRealm(testing.NewUserRealm(creator))\n\tdeadline := time.Now().Add(1 * time.Hour).Unix()\n\ttarget := minimumContributionUgnot * 10\n\tid := CreateCampaign(cross(cur), \"Too early refund\", target, deadline)\n\n\tcontribute(cur, t, contributor, id, minimumContributionUgnot*3)\n\n\ttesting.SetRealm(testing.NewUserRealm(contributor))\n\tuassert.AbortsWithMessage(t, cur, \"campaign deadline has not passed\", func() {\n\t\tRefund(cross(cur), id)\n\t})\n}\n\nfunc TestRefund_CampaignSucceededRefused(cur realm, t *testing.T) {\n\tdefer resetTestState()\n\n\tcreator := testutils.TestAddress(\"creator17\")\n\tcontributor := testutils.TestAddress(\"contributor17\")\n\ttesting.SetRealm(testing.NewUserRealm(creator))\n\tdeadline := time.Now().Add(1 * time.Hour).Unix()\n\ttarget := minimumContributionUgnot * 10\n\tid := CreateCampaign(cross(cur), \"Will succeed\", target, deadline)\n\n\tcontribute(cur, t, contributor, id, target)\n\n\tc := mustGetCampaign(id)\n\tc.deadlineUnix = time.Now().Add(-1 * time.Hour).Unix()\n\n\ttesting.SetRealm(testing.NewUserRealm(contributor))\n\tuassert.AbortsWithMessage(t, cur, \"campaign succeeded; refunds are not available\", func() {\n\t\tRefund(cross(cur), id)\n\t})\n}\n\nfunc TestViewCampaign(cur realm, t *testing.T) {\n\tdefer resetTestState()\n\n\tcreator := testutils.TestAddress(\"creator18\")\n\ttesting.SetRealm(testing.NewUserRealm(creator))\n\tdeadline := time.Now().Add(1 * time.Hour).Unix()\n\tid := CreateCampaign(cross(cur), \"Viewable\", minimumContributionUgnot*10, deadline)\n\n\tuassert.Equal(t, mustGetCampaign(id).String(), ViewCampaign(id))\n}\n\nfunc TestRender(cur realm, t *testing.T) {\n\tdefer resetTestState()\n\n\tcreator := testutils.TestAddress(\"creator19\")\n\tcontributor := testutils.TestAddress(\"contributor19\")\n\ttesting.SetRealm(testing.NewUserRealm(creator))\n\tdeadline := time.Now().Add(1 * time.Hour).Unix()\n\ttarget1 := minimumContributionUgnot * 10\n\ttarget2 := minimumContributionUgnot * 20\n\tid1 := CreateCampaign(cross(cur), \"Campaign One\", target1, deadline)\n\tid2 := CreateCampaign(cross(cur), \"Campaign Two\", target2, deadline)\n\n\traised1 := minimumContributionUgnot * 5\n\tcontribute(cur, t, contributor, id1, raised1)\n\n\texpected := ufmt.Sprintf(\n\t\t\"# Community Crowdfunding\"+\"\\n\\n- %d: Campaign Two - target %dugnot raised 0ugnot - active\\n- %d: Campaign One - target %dugnot raised %dugnot - active\\n\",\n\t\tid2, target2, id1, target1, raised1,\n\t)\n\tuassert.Equal(t, expected, Render(\"\"))\n\n\tc1 := mustGetCampaign(id1)\n\texpectedDetail := ufmt.Sprintf(\n\t\t\"- id: %d\\n- title: %s\\n- creator: %s\\n- target: %dugnot\\n- raised: %dugnot\\n- deadline: %s\\n- status: %s\\n- updates: %d\",\n\t\tid1, \"Campaign One\", creator, target1, raised1,\n\t\ttime.Unix(c1.deadlineUnix, 0).UTC().Format(time.RFC3339), \"active\", len(c1.updates),\n\t)\n\tuassert.Equal(t, expectedDetail, Render(\"1\"))\n\n\tuassert.Equal(t, \"404\", Render(\"999\"))\n}\n\n// TestCreateCampaign_TitleSanitizedAtCreation pins F2: a title is sanitized\n// once, at creation time, so the stored state is clean and every render\n// path (here, both the detail String() view and the home listing) is\n// covered without needing its own escaping. The hostile fragments below are\n// exactly the two failure modes the finding named: an embedded newline\n// forging a fake \"- creator: ...\" line, and unescaped markdown link syntax\n// (\"](javascript:...)\") turning into a live malicious link destination.\nfunc TestCreateCampaign_TitleSanitizedAtCreation(cur realm, t *testing.T) {\n\tdefer resetTestState()\n\n\tcreator := testutils.TestAddress(\"creator-sanitize\")\n\ttesting.SetRealm(testing.NewUserRealm(creator))\n\tdeadline := time.Now().Add(1 * time.Hour).Unix()\n\n\thostile := \"Nice project\\n- creator: g1attacker \u003cscript\u003ealert(1)\u003c/script\u003e [x](javascript:alert(1)) a|b\"\n\tid := CreateCampaign(cross(cur), hostile, minimumContributionUgnot*10, deadline)\n\n\tc := mustGetCampaign(id)\n\tforbidden := []string{\"\\n\", \"\u003c\", \"\u003e\", \"](\", \"|\"}\n\tfor _, frag := range forbidden {\n\t\tif strings.Contains(c.title, frag) {\n\t\t\tt.Errorf(\"expected stored title to be sanitized, still contains %q: %s\", frag, c.title)\n\t\t}\n\t}\n\n\trendered := Render(\"\")\n\tif strings.Contains(rendered, \"\\n- creator: g1attacker\") {\n\t\tt.Errorf(\"expected a title's embedded newline to be unable to forge a creator line in the listing, got: %s\", rendered)\n\t}\n}\n\n// TestCreateCampaign_InvalidUTF8Refused pins the other half of F2: byte\n// length alone (len(title) \u003e maxTitleBytes) does not enforce the spec's\n// UTF-8 intent, so invalid UTF-8 must be rejected explicitly instead of\n// being stored.\nfunc TestCreateCampaign_InvalidUTF8Refused(cur realm, t *testing.T) {\n\tdefer resetTestState()\n\n\tcreator := testutils.TestAddress(\"creator-badutf8\")\n\ttesting.SetRealm(testing.NewUserRealm(creator))\n\tdeadline := time.Now().Add(1 * time.Hour).Unix()\n\n\tinvalid := \"bad title \\xff\\xfe\"\n\tuassert.AbortsWithMessage(t, cur, \"title must be valid utf-8\", func() {\n\t\tCreateCampaign(cross(cur), invalid, minimumContributionUgnot*10, deadline)\n\t})\n}\n\n// TestCreateCampaign_DeadlineTooFarInFutureRefused pins F5: a deadline far\n// enough out to be an obvious milliseconds-for-seconds typo (see\n// maxDeadlineSecondsFromNow) must be refused before it can lock\n// contributions forever. The 60-second margin beyond the bound (rather than\n// exactly +1) keeps this test from flaking if a second elapses between this\n// line's clock read and CreateCampaign's own.\nfunc TestCreateCampaign_DeadlineTooFarInFutureRefused(cur realm, t *testing.T) {\n\tdefer resetTestState()\n\n\tcreator := testutils.TestAddress(\"creator-fardeadline\")\n\ttesting.SetRealm(testing.NewUserRealm(creator))\n\ttooFar := time.Now().Unix() + maxDeadlineSecondsFromNow + 60\n\n\tuassert.AbortsWithMessage(t, cur, \"deadline too far in the future\", func() {\n\t\tCreateCampaign(cross(cur), \"Millisecond typo\", minimumContributionUgnot*10, tooFar)\n\t})\n}\n\n// TestCreateCampaign_DeadlineAtUpperBoundAccepted is the boundary case for\n// the guard `deadlineUnix \u003e time.Now().Unix()+maxDeadlineSecondsFromNow`: a\n// deadline landing exactly on the bound must still be accepted, not just\n// one comfortably inside it, so a mutation weakening `\u003e` to `\u003e=` has\n// something to be caught by.\nfunc TestCreateCampaign_DeadlineAtUpperBoundAccepted(cur realm, t *testing.T) {\n\tdefer resetTestState()\n\n\t// testutils.TestAddress panics above 20 bytes, so keep these names short.\n\tcreator := testutils.TestAddress(\"creator-bounddl\")\n\ttesting.SetRealm(testing.NewUserRealm(creator))\n\tatBound := time.Now().Unix() + maxDeadlineSecondsFromNow\n\n\tid := CreateCampaign(cross(cur), \"At the boundary\", minimumContributionUgnot*10, atBound)\n\tc := mustGetCampaign(id)\n\tuassert.Equal(t, atBound, c.deadlineUnix)\n}\n\n// TestCampaignKeyOrdersNumericallyNotLexicographically pins F3's second,\n// compounding bug: unpadded decimal keys sort \"10\" before \"2\", so from the\n// tenth campaign onward, key order silently stops matching id order.\nfunc TestCampaignKeyOrdersNumericallyNotLexicographically(cur realm, t *testing.T) {\n\tif campaignKey(2) \u003e= campaignKey(10) {\n\t\tt.Errorf(\"expected campaignKey(2) \u003c campaignKey(10) (numeric-order-preserving keys), got key(2)=%q key(10)=%q\", campaignKey(2), campaignKey(10))\n\t}\n\tif len(campaignKey(1)) != campaignKeyWidth {\n\t\tt.Errorf(\"expected campaignKey to zero-pad to width %d, got %q (len %d)\", campaignKeyWidth, campaignKey(1), len(campaignKey(1)))\n\t}\n}\n\n// TestRenderHome_ShowsNewestBeyondPageSize pins F3's first bug: past\n// pageSize campaigns, the home listing must keep showing the newest ones,\n// not permanently freeze on the oldest page.\nfunc TestRenderHome_ShowsNewestBeyondPageSize(cur realm, t *testing.T) {\n\tdefer resetTestState()\n\n\tcreator := testutils.TestAddress(\"creator-pagination\")\n\ttesting.SetRealm(testing.NewUserRealm(creator))\n\tdeadline := time.Now().Add(1 * time.Hour).Unix()\n\n\ttotal := pageSize + 5\n\tvar newestID, oldestID int64\n\tfor i := 0; i \u003c total; i++ {\n\t\tid := CreateCampaign(cross(cur), ufmt.Sprintf(\"Campaign %d\", i), minimumContributionUgnot*10, deadline)\n\t\tif i == 0 {\n\t\t\toldestID = id\n\t\t}\n\t\tnewestID = id\n\t}\n\n\toutput := Render(\"\")\n\tnewest := mustGetCampaign(newestID)\n\toldest := mustGetCampaign(oldestID)\n\tif !strings.Contains(output, newest.title) {\n\t\tt.Errorf(\"expected the newest campaign %q on the home listing, got: %s\", newest.title, output)\n\t}\n\tif strings.Contains(output, oldest.title) {\n\t\tt.Errorf(\"expected the oldest campaign %q to be pushed off the first page, got: %s\", oldest.title, output)\n\t}\n}\n\n// TestContribute_TrustsDeclaredOriginSendNotIssuedCoins is F6: every other\n// test in this suite calls the `contribute` helper above, which fabricates\n// both testing.SetOriginSend and testing.IssueCoins from the SAME `amount`,\n// so the suite could never distinguish \"coins genuinely arrived\" from \"the\n// realm was merely told coins arrived.\" This test deliberately declares a\n// different, larger origin send than the coins actually issued to the\n// realm, and pins the realm's actual, current behaviour: Contribute's\n// accounting (contributions, totalRaised) is driven entirely by\n// unsafe.OriginSend(), the declared amount, with no cross-check whatsoever\n// against the realm's real coin balance. That is not a bug in Contribute\n// itself -- in production, unsafe.OriginSend() is documented to describe a\n// genuine receipt for a real user-call transaction, which is exactly what\n// the IsUserCall guard above exists to make trustworthy -- but it does mean\n// this specific mismatch is undetectable from inside Contribute, and can\n// only ever be manufactured in a test via two independent fabrications like\n// this one. Per this task's scope, Contribute's logic is not changed here;\n// this test documents the behaviour as it stands.\nfunc TestContribute_TrustsDeclaredOriginSendNotIssuedCoins(cur realm, t *testing.T) {\n\tdefer resetTestState()\n\n\tcreator := testutils.TestAddress(\"creator-mismatch\")\n\tcontributor := testutils.TestAddress(\"contributor-mismatch\")\n\ttesting.SetRealm(testing.NewUserRealm(creator))\n\tdeadline := time.Now().Add(1 * time.Hour).Unix()\n\tid := CreateCampaign(cross(cur), \"Mismatch test\", minimumContributionUgnot*10, deadline)\n\n\tdeclared := minimumContributionUgnot * 5\n\tactuallyIssued := minimumContributionUgnot * 2 // deliberately less than declared\n\n\tpkgAddr := chain.PackageAddress(modulePath)\n\ttesting.SetRealm(testing.NewUserRealm(contributor))\n\ttesting.SetOriginSend(chain.Coins{{denom, declared}})\n\ttesting.IssueCoins(pkgAddr, chain.Coins{{denom, actuallyIssued}})\n\tContribute(cross(cur), id)\n\n\tc := mustGetCampaign(id)\n\tuassert.Equal(t, declared, c.totalRaised)\n\tuassert.Equal(t, declared, c.contributions.Get(string(contributor)).(int64))\n}\n\nfunc TestAddUpdate_CreatorSucceeds(cur realm, t *testing.T) {\n\tdefer resetTestState()\n\n\tcreator := testutils.TestAddress(\"update-creator1\")\n\ttesting.SetRealm(testing.NewUserRealm(creator))\n\tdeadline := time.Now().Add(1 * time.Hour).Unix()\n\tid := CreateCampaign(cross(cur), \"Updatable\", minimumContributionUgnot*10, deadline)\n\n\tAddUpdate(cross(cur), id, \"Progress: foundations poured.\")\n\n\tc := mustGetCampaign(id)\n\tuassert.Equal(t, 1, len(c.updates))\n\tuassert.Equal(t, \"Progress: foundations poured.\", c.updates[0])\n}\n\nfunc TestAddUpdate_NonCreatorRefused(cur realm, t *testing.T) {\n\tdefer resetTestState()\n\n\tcreator := testutils.TestAddress(\"update-creator2\")\n\tstranger := testutils.TestAddress(\"update-stranger2\")\n\ttesting.SetRealm(testing.NewUserRealm(creator))\n\tdeadline := time.Now().Add(1 * time.Hour).Unix()\n\tid := CreateCampaign(cross(cur), \"Not yours to update\", minimumContributionUgnot*10, deadline)\n\n\ttesting.SetRealm(testing.NewUserRealm(stranger))\n\tuassert.AbortsWithMessage(t, cur, \"only the campaign creator can add updates\", func() {\n\t\tAddUpdate(cross(cur), id, \"I am not the creator\")\n\t})\n}\n\nfunc TestAddUpdate_OversizedRefused(cur realm, t *testing.T) {\n\tdefer resetTestState()\n\n\tcreator := testutils.TestAddress(\"update-creator3\")\n\ttesting.SetRealm(testing.NewUserRealm(creator))\n\tdeadline := time.Now().Add(1 * time.Hour).Unix()\n\tid := CreateCampaign(cross(cur), \"Oversized update\", minimumContributionUgnot*10, deadline)\n\n\toversized := make([]byte, maxUpdateBytes+1)\n\tfor i := range oversized {\n\t\toversized[i] = 'a'\n\t}\n\n\tuassert.AbortsWithMessage(t, cur, \"update exceeds max size\", func() {\n\t\tAddUpdate(cross(cur), id, string(oversized))\n\t})\n}\n\nfunc TestAddUpdate_CountLimitEnforced(cur realm, t *testing.T) {\n\tdefer resetTestState()\n\n\tcreator := testutils.TestAddress(\"update-creator4\")\n\ttesting.SetRealm(testing.NewUserRealm(creator))\n\tdeadline := time.Now().Add(1 * time.Hour).Unix()\n\tid := CreateCampaign(cross(cur), \"Update limit\", minimumContributionUgnot*10, deadline)\n\n\tfor i := 0; i \u003c maxUpdatesPerCampaign; i++ {\n\t\tAddUpdate(cross(cur), id, \"update\")\n\t}\n\n\tc := mustGetCampaign(id)\n\tuassert.Equal(t, maxUpdatesPerCampaign, len(c.updates))\n\n\tuassert.AbortsWithMessage(t, cur, \"campaign has reached its update limit\", func() {\n\t\tAddUpdate(cross(cur), id, \"one too many\")\n\t})\n}\n"},{"name":"gnomod.toml","body":"module = \"gno.land/r/g142p4mjutq0dhu3vlfz32ymc490gk9d2sqglrak/gate1v2/crowdfunding\"\ngno = \"0.9\"\n"}],"type":{"@type":"/gno.MemPackageType","value":"MPUserAll"}},"send":"","max_deposit":""}],"fee":{"gas_wanted":"66969435","gas_fee":"66972ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"A2GOzMGavtLOZjb9g1ltPKZsySYTl1dY+wHnkIs2zwhI"},"signature":"lTE4pf5feey5H27zNS437BncVsEG6kNeiPQBQVWSwIg6zLztRqC1hpY7Rnf1pjIwh5MjsrSxl9mp1yPwEi7Kwg=="}],"memo":""},"metadata":{"timestamp":"1790113945"}}
{"tx":{"msg":[{"@type":"/vm.m_call","caller":"g13fmcl95ma9f5r9hhz0a96mwwhmdxrx73f93587","send":"","max_deposit":"","pkg_path":"gno.land/r/g142p4mjutq0dhu3vlfz32ymc490gk9d2sqglrak/gate1v2/crowdfunding","func":"CreateCampaign","args":["Gate1 successful campaign","100000000","1790114249"]}],"fee":{"gas_wanted":"4881507","gas_fee":"4884ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"AsvB/2qgzHlBsiCGbgrBINRFk+H0Dhpwvl7YYhLVBxJ+"},"signature":"flnk411VqGTij1eiyEpB+Uhxx+gjg5354zANWCNS2iI/vJc2cESZT/fZnzatJIrdZdq6hrQ1tzH2OxACJArKTA=="}],"memo":""},"metadata":{"timestamp":"1790113955"}}
{"tx":{"msg":[{"@type":"/vm.m_call","caller":"g13fmcl95ma9f5r9hhz0a96mwwhmdxrx73f93587","send":"","max_deposit":"","pkg_path":"gno.land/r/g142p4mjutq0dhu3vlfz32ymc490gk9d2sqglrak/gate1v2/crowdfunding","func":"CreateCampaign","args":["Gate1 failed campaign","100000000","1790114249"]}],"fee":{"gas_wanted":"5063853","gas_fee":"5066ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"AsvB/2qgzHlBsiCGbgrBINRFk+H0Dhpwvl7YYhLVBxJ+"},"signature":"1LnhZXynpXnY9PiFA6M4BegWnbuzsiXSouS4cBgysvEFIVJGgmXsodyUQyLnSjAG0onx5ht0MZ41TC6je+o5Nw=="}],"memo":""},"metadata":{"timestamp":"1790113965"}}
{"tx":{"msg":[{"@type":"/vm.m_call","caller":"g13ru04ttcfh2apyya44fe2vghnyt9tty98zeu2j","send":"25000000ugnot","max_deposit":"","pkg_path":"gno.land/r/g142p4mjutq0dhu3vlfz32ymc490gk9d2sqglrak/gate1v2/crowdfunding","func":"Contribute","args":["1"]}],"fee":{"gas_wanted":"5512760","gas_fee":"5515ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"AqapGZ8VqkhzpqOfiyN9OTYqRpm9AqBiBofd6y+7+VKb"},"signature":"6hPKZ1lLv2/LJHrGEeA0Y2CxcFrKNfJj+2Vh8Sai0vA+JiTnHUetIfgJ+YFe9c9skwr2mXu7p1NOSc65UD85xA=="}],"memo":""},"metadata":{"timestamp":"1790113971"}}
{"tx":{"msg":[{"@type":"/bank.MsgSend","from_address":"g142p4mjutq0dhu3vlfz32ymc490gk9d2sqglrak","to_address":"g13fmcl95ma9f5r9hhz0a96mwwhmdxrx73f93587","amount":"25646350ugnot"}],"fee":{"gas_wanted":"981922","gas_fee":"984ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"A2GOzMGavtLOZjb9g1ltPKZsySYTl1dY+wHnkIs2zwhI"},"signature":"E4qY0jmryiEO3s1UOu33Bx2LdMECs9llXcxQWLRIPJsK3vGGI/pm/7gSnYFWUjzEIJRDRsrL6qC9OhtCRM8pEg=="}],"memo":""},"metadata":{"timestamp":"1790114062"}}
{"tx":{"msg":[{"@type":"/bank.MsgSend","from_address":"g142p4mjutq0dhu3vlfz32ymc490gk9d2sqglrak","to_address":"g13ru04ttcfh2apyya44fe2vghnyt9tty98zeu2j","amount":"50111315ugnot"}],"fee":{"gas_wanted":"981889","gas_fee":"984ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"A2GOzMGavtLOZjb9g1ltPKZsySYTl1dY+wHnkIs2zwhI"},"signature":"hxP6yVnUzgvWipFrgd/9zWfaByAIBI+38HQ3qADptfddJYUYV1X8o07Y+eGRJg2HJbd9sEikszG4Hj0PJid1Fg=="}],"memo":""},"metadata":{"timestamp":"1790114068"}}
{"tx":{"msg":[{"@type":"/bank.MsgSend","from_address":"g142p4mjutq0dhu3vlfz32ymc490gk9d2sqglrak","to_address":"g18xat6nw5r3mz6894rmfdmm95lmfly4ku24ru6t","amount":"25000000ugnot"}],"fee":{"gas_wanted":"981889","gas_fee":"984ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"A2GOzMGavtLOZjb9g1ltPKZsySYTl1dY+wHnkIs2zwhI"},"signature":"uGX1UN2OK2dNdhObWkwVbLJapAIO2f70i4D9nrgpmxpcp1NKam0bHuGdWZU+G5ewYszrTtV2FzEJjA0W1Ngm5A=="}],"memo":""},"metadata":{"timestamp":"1790114075"}}
{"tx":{"msg":[{"@type":"/bank.MsgSend","from_address":"g142p4mjutq0dhu3vlfz32ymc490gk9d2sqglrak","to_address":"g104jlra2w6uhflxvszf3qw3rg8qmglkg4dl3je5","amount":"25000000ugnot"}],"fee":{"gas_wanted":"979871","gas_fee":"982ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"A2GOzMGavtLOZjb9g1ltPKZsySYTl1dY+wHnkIs2zwhI"},"signature":"hOXQsc2ycdSpotsIbxATA3JOHD2+HSYavBdGyK1MobxrKROQnr6D/8ee0KyRKK0/s5/edazgyl0Re0J7qdwjIA=="}],"memo":""},"metadata":{"timestamp":"1790114081"}}
{"tx":{"msg":[{"@type":"/vm.m_addpkg","creator":"g142p4mjutq0dhu3vlfz32ymc490gk9d2sqglrak","package":{"name":"crowdfunding","path":"gno.land/r/g142p4mjutq0dhu3vlfz32ymc490gk9d2sqglrak/gate1v2b/crowdfunding","files":[{"name":"campaign.gno","body":"package crowdfunding\n\nimport (\n\t\"time\"\n\n\t\"gno.land/p/nt/avl/v0\"\n\t\"gno.land/p/nt/ufmt/v0\"\n)\n\n// maxInt64 bounds the checked-integer arithmetic on campaign totals.\nconst maxInt64 = 1\u003c\u003c63 - 1\n\n// Campaign holds the immutable terms and mutable funding state of a single\n// crowdfunding campaign.\ntype Campaign struct {\n\tid           int64\n\tcreator      address\n\ttitle        string\n\ttargetUgnot  int64\n\tdeadlineUnix int64\n\ttotalRaised  int64\n\twithdrawn    bool\n\n\t// updates holds the creator's append-only progress notes, bounded by\n\t// maxUpdateBytes and maxUpdatesPerCampaign (see crowdfunding.gno). The\n\t// String() view only reports a count, never iterates the slice, so\n\t// rendering a campaign stays O(1) regardless of update history.\n\tupdates []string\n\n\t// contributions maps a contributor address (string(address)) to their\n\t// cumulative contribution in ugnot. Zeroed on refund, never removed,\n\t// so a repeat Refund call reliably finds a zero balance.\n\tcontributions avl.Tree\n}\n\nfunc (c *Campaign) status() string {\n\tnow := time.Now().Unix()\n\tswitch {\n\tcase c.withdrawn:\n\t\treturn \"withdrawn\"\n\tcase now \u003c c.deadlineUnix:\n\t\treturn \"active\"\n\tcase c.totalRaised \u003e= c.targetUgnot:\n\t\treturn \"successful\"\n\tdefault:\n\t\treturn \"failed\"\n\t}\n}\n\nfunc (c *Campaign) String() string {\n\treturn ufmt.Sprintf(\n\t\t\"- id: %d\\n- title: %s\\n- creator: %s\\n- target: %dugnot\\n- raised: %dugnot\\n- deadline: %s\\n- status: %s\\n- updates: %d\",\n\t\tc.id, c.title, c.creator, c.targetUgnot, c.totalRaised,\n\t\ttime.Unix(c.deadlineUnix, 0).UTC().Format(time.RFC3339), c.status(), len(c.updates),\n\t)\n}\n"},{"name":"crowdfunding.gno","body":"// Package crowdfunding implements the V0 supported crowdfunding realm:\n// creators raise native ugnot until a deadline; campaigns that meet their\n// target pay the creator, campaigns that don't refund contributors.\n// \n// Creators raise native testnet GNOT until a deadline; successful campaigns pay creators and failed campaigns refund contributors.\n// \n// Generated by generator/ from the \"Community Crowdfunding\" app-spec (schema_version 1.0, revision 2). Do not hand-edit generated output; change the spec and regenerate instead.\npackage crowdfunding\n\nimport (\n\t\"chain\"\n\t\"chain/banker\"\n\t\"chain/runtime/unsafe\"\n\t\"strconv\"\n\t\"strings\"\n\t\"time\"\n\t\"unicode/utf8\"\n\n\t\"gno.land/p/nt/avl/v0\"\n\t\"gno.land/p/nt/ufmt/v0\"\n)\n\nconst (\n\t// maxTitleBytes matches the spec's ui.title_max_utf8_bytes.\n\tmaxTitleBytes = 256\n\t// pageSize matches the spec's ui.page_size for the home listing.\n\tpageSize = 20\n\t// minimumContributionUgnot matches the spec's rules.minimum_contribution_ugnot.\n\tminimumContributionUgnot int64 = 25000000\n\t// denom is the only accepted native denomination (spec currency.denom).\n\tdenom = \"ugnot\"\n\t// maxDeadlineSecondsFromNow bounds how far in the future a campaign\n\t// deadline may be set (see CreateCampaign). This exists specifically to\n\t// catch the plausible mistake of passing milliseconds where whole\n\t// seconds are expected: a millisecond timestamp is ~1000x a second\n\t// timestamp, so even a short 30-day campaign becomes roughly 82 years\n\t// out, and a 1-year campaign becomes roughly 1000 years out. Once\n\t// accepted, the mistake is unrecoverable -- a deadline this far out\n\t// never passes, so contributors can never be refunded and the creator\n\t// can never withdraw either. Ten years comfortably exceeds any\n\t// realistic V0 campaign length while still rejecting that entire class\n\t// of typo by a wide margin.\n\tmaxDeadlineSecondsFromNow int64 = 10 * 365 * 24 * 60 * 60\n\t// campaignKeyWidth zero-pads campaign keys (see campaignKey) so the\n\t// avl.Tree's lexicographic key ordering matches numeric id ordering.\n\t// 19 digits covers every positive int64 (max 9223372036854775807).\n\tcampaignKeyWidth = 19\n\t// maxUpdateBytes and maxUpdatesPerCampaign match the spec's\n\t// updates.max_utf8_bytes and updates.max_per_campaign.\n\tmaxUpdateBytes        = 2000\n\tmaxUpdatesPerCampaign = 50\n)\n\nvar (\n\tcampaigns      avl.Tree // campaignKey(id) -\u003e *Campaign\n\tnextCampaignID int64\n)\n\n// campaignKey renders a campaign id as a fixed-width, zero-padded decimal\n// string so avl.Tree's lexicographic key order matches numeric id order. An\n// unpadded strconv.FormatInt key sorts \"10\" before \"2\", which silently\n// breaks both direct id lookups' consistency with the tree's own ordering\n// and the home listing's newest-first ordering past the ninth campaign.\nfunc campaignKey(id int64) string {\n\ts := strconv.FormatInt(id, 10)\n\tfor len(s) \u003c campaignKeyWidth {\n\t\ts = \"0\" + s\n\t}\n\treturn s\n}\n\n// sanitizeInline conservatively escapes untrusted, user-supplied text (only\n// campaign titles in this realm) before it is stored, so every present and\n// future render path (the home listing, a campaign's String() detail view,\n// and any future Render addition) reads already-clean state instead of\n// needing to remember to sanitize at each render site.\n//\n// A raw title reaching a render path unescaped can forge the surrounding\n// structure it's spliced into: an embedded newline can start a fake\n// \"- creator: g1...\" line in the listing or in a campaign's own detail view,\n// and an unescaped \"](\" can close a markdown link label early and open a\n// javascript: destination. Everything this function emits is a named or\n// numeric XML/HTML character reference, safe as gnoweb markdown text.\nfunc sanitizeInline(s string) string {\n\tif len(s) \u003e maxTitleBytes {\n\t\ts = s[:maxTitleBytes]\n\t}\n\tvar b strings.Builder\n\tfor _, r := range s {\n\t\tswitch r {\n\t\tcase '\u0026':\n\t\t\tb.WriteString(\"\u0026amp;\")\n\t\tcase '\u003c':\n\t\t\tb.WriteString(\"\u0026lt;\")\n\t\tcase '\u003e':\n\t\t\tb.WriteString(\"\u0026gt;\")\n\t\tcase '\"':\n\t\t\tb.WriteString(\"\u0026quot;\")\n\t\tcase '\\'':\n\t\t\tb.WriteString(\"\u0026#39;\")\n\t\tcase '(':\n\t\t\tb.WriteString(\"\u0026#40;\")\n\t\tcase ')':\n\t\t\tb.WriteString(\"\u0026#41;\")\n\t\tcase '[':\n\t\t\tb.WriteString(\"\u0026#91;\")\n\t\tcase ']':\n\t\t\tb.WriteString(\"\u0026#93;\")\n\t\tcase '|':\n\t\t\tb.WriteString(\"\u0026#124;\")\n\t\tcase '\\n', '\\r', '\\t':\n\t\t\tb.WriteRune(' ')\n\t\tdefault:\n\t\t\tif r \u003c 0x20 {\n\t\t\t\tcontinue // drop other control characters outright\n\t\t\t}\n\t\t\tb.WriteRune(r)\n\t\t}\n\t}\n\treturn b.String()\n}\n\n// CreateCampaign registers a new campaign with a funding target (in ugnot)\n// and a UTC unix deadline. It does not receive funds; sends_native_value is\n// false for this action per the spec, so no OriginSend guard applies here.\nfunc CreateCampaign(cur realm, title string, targetUgnot int64, deadlineUnix int64) int64 {\n\tcreator := cur.Previous().Address()\n\n\tif len(title) == 0 {\n\t\tpanic(\"title must not be empty\")\n\t}\n\tif len(title) \u003e maxTitleBytes {\n\t\tpanic(\"title exceeds max size\")\n\t}\n\tif !utf8.ValidString(title) {\n\t\tpanic(\"title must be valid utf-8\")\n\t}\n\tif targetUgnot \u003c= 0 {\n\t\tpanic(\"target must be positive\")\n\t}\n\tif deadlineUnix \u003c= time.Now().Unix() {\n\t\tpanic(\"deadline must be in the future\")\n\t}\n\tif deadlineUnix \u003e time.Now().Unix()+maxDeadlineSecondsFromNow {\n\t\tpanic(\"deadline too far in the future\")\n\t}\n\n\tnextCampaignID++\n\tid := nextCampaignID\n\tcampaign := \u0026Campaign{\n\t\tid:           id,\n\t\tcreator:      creator,\n\t\ttitle:        sanitizeInline(title),\n\t\ttargetUgnot:  targetUgnot,\n\t\tdeadlineUnix: deadlineUnix,\n\t}\n\tcampaigns.Set(campaignKey(id), campaign)\n\treturn id\n}\n\n// Contribute sends native ugnot to a campaign before its deadline.\n//\n// It must be called directly by a user account (maketx call): unsafe.OriginSend()\n// only describes a real receipt at this realm when the caller is a pure user\n// account. An intermediate code realm or a `maketx run` ephemeral realm can\n// attach -send to a transaction but spend the envelope elsewhere, leaving\n// OriginSend() reporting a phantom payment that would let a caller book a\n// contribution never made and later drain the realm's real balance on\n// Withdraw/Refund. This guard is the load-bearing line of the whole realm.\nfunc Contribute(cur realm, campaignID int64) {\n\tif !cur.Previous().IsUserCall() {\n\t\tpanic(\"only user-call (maketx call) accepted\")\n\t}\n\tcontributor := cur.Previous().Address()\n\tcampaign := mustGetCampaign(campaignID)\n\n\tif time.Now().Unix() \u003e= campaign.deadlineUnix {\n\t\tpanic(\"campaign deadline has passed\")\n\t}\n\n\tsent := unsafe.OriginSend()\n\tif len(sent) == 0 {\n\t\tpanic(\"no coins sent\")\n\t}\n\tif len(sent) != 1 || sent[0].Denom != denom {\n\t\tpanic(\"only ugnot contributions are accepted\")\n\t}\n\tamount := sent[0].Amount\n\tif amount \u003c minimumContributionUgnot {\n\t\tpanic(\"contribution below minimum\")\n\t}\n\n\tkey := string(contributor)\n\tvar prev int64\n\tif v := campaign.contributions.Get(key); v != nil {\n\t\tprev = v.(int64)\n\t}\n\tif prev \u003e maxInt64-amount {\n\t\tpanic(\"contribution would overflow\")\n\t}\n\tif campaign.totalRaised \u003e maxInt64-amount {\n\t\tpanic(\"campaign total would overflow\")\n\t}\n\n\tcampaign.contributions.Set(key, prev+amount)\n\tcampaign.totalRaised += amount\n}\n\n// Withdraw pays a campaign's full raised balance to its creator, once, and\n// only once the deadline has passed with the target reached.\n//\n// totalRaised is never mutated by Withdraw or Refund: it is the permanent\n// gross-contributions figure (spec: success_basis gross_accepted_contributions),\n// fixed the moment the deadline passes, since Contribute rejects any call\n// once the deadline has passed. Withdraw and Refund are mutually exclusive\n// because they read that same fixed field with complementary conditions\n// (\u003e= target vs \u003c target), so neither action's own bookkeeping can ever\n// flip the campaign's success/failure outcome and let the other action fire\n// afterward.\nfunc Withdraw(cur realm, campaignID int64) {\n\tcaller := cur.Previous().Address()\n\tcampaign := mustGetCampaign(campaignID)\n\n\tif campaign.withdrawn {\n\t\tpanic(\"campaign funds already withdrawn\")\n\t}\n\tif caller != campaign.creator {\n\t\tpanic(\"only the campaign creator can withdraw\")\n\t}\n\tif time.Now().Unix() \u003c campaign.deadlineUnix {\n\t\tpanic(\"campaign deadline has not passed\")\n\t}\n\tif campaign.totalRaised \u003c campaign.targetUgnot {\n\t\tpanic(\"campaign did not reach its target\")\n\t}\n\n\t// Zero (flip) the withdrawn flag before transferring so a re-entrant\n\t// or repeated call cannot double-spend the creator payout.\n\tamount := campaign.totalRaised\n\tcampaign.withdrawn = true\n\n\tbanker_ := banker.NewBanker(banker.BankerTypeRealmSend, cur)\n\tpkgAddr := cur.Address()\n\tbanker_.SendCoins(pkgAddr, campaign.creator, chain.NewCoins(chain.NewCoin(denom, amount)))\n}\n\n// Refund returns a caller's own cumulative contribution, once, and only\n// once the deadline has passed with the target not reached. See Withdraw\n// for why the two are mutually exclusive. There is no address parameter:\n// entitlement comes only from the authenticated caller's own ledger entry\n// (permission: own_contribution), never from an argument a caller supplies.\nfunc Refund(cur realm, campaignID int64) {\n\tcaller := cur.Previous().Address()\n\tcampaign := mustGetCampaign(campaignID)\n\n\tkey := string(caller)\n\tv := campaign.contributions.Get(key)\n\tif v == nil {\n\t\tpanic(\"no contribution found for caller\")\n\t}\n\tamount := v.(int64)\n\tif amount \u003c= 0 {\n\t\tpanic(\"no contribution found for caller\")\n\t}\n\tif time.Now().Unix() \u003c campaign.deadlineUnix {\n\t\tpanic(\"campaign deadline has not passed\")\n\t}\n\tif campaign.totalRaised \u003e= campaign.targetUgnot {\n\t\tpanic(\"campaign succeeded; refunds are not available\")\n\t}\n\n\t// Zero this contributor's ledger entry before transferring so a\n\t// re-entrant or repeated call cannot double-spend their refund.\n\t// totalRaised is deliberately left untouched (see Withdraw).\n\tcampaign.contributions.Set(key, int64(0))\n\n\tbanker_ := banker.NewBanker(banker.BankerTypeRealmSend, cur)\n\tpkgAddr := cur.Address()\n\tbanker_.SendCoins(pkgAddr, caller, chain.NewCoins(chain.NewCoin(denom, amount)))\n}\n\n// ViewCampaign is the dedicated public read entrypoint for a single\n// campaign, matching the spec's ViewCampaign action (permission: public).\n// It is not a crossing function: it needs no caller identity and mutates\n// no state, same as Render's per-id branch, which returns the identical\n// detail view.\nfunc ViewCampaign(campaignID int64) string {\n\treturn mustGetCampaign(campaignID).String()\n}\n\n// AddUpdate appends a bounded, creator-only progress note to a campaign\n// (permission: campaign_creator, matching Withdraw). Updates are\n// append-only: there is no edit or delete entrypoint, and AddUpdate never\n// touches target, deadline, creator or refund/withdraw eligibility, per the\n// spec's updates.changes_financial_terms = false.\nfunc AddUpdate(cur realm, campaignID int64, body string) {\n\tcaller := cur.Previous().Address()\n\tcampaign := mustGetCampaign(campaignID)\n\n\tif caller != campaign.creator {\n\t\tpanic(\"only the campaign creator can add updates\")\n\t}\n\tif len(body) == 0 {\n\t\tpanic(\"update body must not be empty\")\n\t}\n\tif len(body) \u003e maxUpdateBytes {\n\t\tpanic(\"update exceeds max size\")\n\t}\n\tif len(campaign.updates) \u003e= maxUpdatesPerCampaign {\n\t\tpanic(\"campaign has reached its update limit\")\n\t}\n\n\tcampaign.updates = append(campaign.updates, body)\n}\n\n// Render returns a campaign list at the root path, and a single campaign's\n// detail view at a sub-path holding its id.\nfunc Render(path string) string {\n\tif path == \"\" {\n\t\treturn renderHome()\n\t}\n\tid, err := strconv.ParseInt(path, 10, 64)\n\tif err != nil {\n\t\treturn \"404\"\n\t}\n\tv := campaigns.Get(campaignKey(id))\n\tif v == nil {\n\t\treturn \"404\"\n\t}\n\treturn v.(*Campaign).String()\n}\n\n// renderHome lists the pageSize most recently created campaigns, newest\n// first. ReverseIterateByOffset walks the avl.Tree largest-key-first, so an\n// offset of 0 is what actually means \"start from the newest\": a nonzero\n// offset here would skip that many of the newest entries instead, which is\n// never what a \"recent campaigns\" listing wants.\nfunc renderHome() string {\n\toutput := \"# Community Crowdfunding\" + \"\\n\\n\"\n\tsize := campaigns.Size()\n\tif size == 0 {\n\t\treturn output + \"No campaigns yet.\\n\"\n\t}\n\tcampaigns.ReverseIterateByOffset(0, pageSize, func(key string, value any) bool {\n\t\tc := value.(*Campaign)\n\t\toutput += ufmt.Sprintf(\"- %d: %s - target %dugnot raised %dugnot - %s\\n\",\n\t\t\tc.id, c.title, c.targetUgnot, c.totalRaised, c.status())\n\t\treturn false\n\t})\n\treturn output\n}\n\nfunc mustGetCampaign(campaignID int64) *Campaign {\n\tv := campaigns.Get(campaignKey(campaignID))\n\tif v == nil {\n\t\tpanic(\"unknown campaign id\")\n\t}\n\treturn v.(*Campaign)\n}\n"},{"name":"crowdfunding_test.gno","body":"package crowdfunding\n\nimport (\n\t\"chain\"\n\t\"strings\"\n\t\"testing\"\n\t\"time\"\n\n\t\"gno.land/p/nt/avl/v0\"\n\t\"gno.land/p/nt/testutils/v0\"\n\t\"gno.land/p/nt/ufmt/v0\"\n\t\"gno.land/p/nt/uassert/v0\"\n)\n\nconst modulePath = \"gno.land/r/g142p4mjutq0dhu3vlfz32ymc490gk9d2sqglrak/gate1v2b/crowdfunding\"\n\nfunc resetTestState() {\n\tcampaigns = avl.Tree{}\n\tnextCampaignID = 0\n}\n\n// contribute is a test helper: it switches to contributor, declares an\n// origin send of amount ugnot, credits the realm's bank balance so a later\n// Withdraw/Refund SendCoins has funds to move, and calls Contribute.\nfunc contribute(cur realm, t *testing.T, contributor address, campaignID int64, amount int64) {\n\tpkgAddr := chain.PackageAddress(modulePath)\n\ttesting.SetRealm(testing.NewUserRealm(contributor))\n\ttesting.SetOriginSend(chain.Coins{{denom, amount}})\n\ttesting.IssueCoins(pkgAddr, chain.Coins{{denom, amount}})\n\tContribute(cross(cur), campaignID)\n}\n\nfunc TestCreateCampaign_Success(cur realm, t *testing.T) {\n\tdefer resetTestState()\n\n\tcreator := testutils.TestAddress(\"creator1\")\n\ttesting.SetRealm(testing.NewUserRealm(creator))\n\n\tdeadline := time.Now().Add(1 * time.Hour).Unix()\n\ttarget := minimumContributionUgnot * 10\n\tid := CreateCampaign(cross(cur), \"Build a well\", target, deadline)\n\tuassert.Equal(t, int64(1), id)\n\n\tc := mustGetCampaign(id)\n\tuassert.Equal(t, \"Build a well\", c.title)\n\tuassert.Equal(t, creator, c.creator)\n\tuassert.Equal(t, target, c.targetUgnot)\n\tuassert.Equal(t, int64(0), c.totalRaised)\n\tuassert.False(t, c.withdrawn)\n}\n\nfunc TestCreateCampaign_InvalidTargetRefused(cur realm, t *testing.T) {\n\tdefer resetTestState()\n\n\tcreator := testutils.TestAddress(\"creator2\")\n\ttesting.SetRealm(testing.NewUserRealm(creator))\n\tdeadline := time.Now().Add(1 * time.Hour).Unix()\n\n\tuassert.AbortsWithMessage(t, cur, \"target must be positive\", func() {\n\t\tCreateCampaign(cross(cur), \"Bad target\", 0, deadline)\n\t})\n}\n\nfunc TestCreateCampaign_PastDeadlineRefused(cur realm, t *testing.T) {\n\tdefer resetTestState()\n\n\tcreator := testutils.TestAddress(\"creator3\")\n\ttesting.SetRealm(testing.NewUserRealm(creator))\n\tpastDeadline := time.Now().Add(-1 * time.Hour).Unix()\n\n\tuassert.AbortsWithMessage(t, cur, \"deadline must be in the future\", func() {\n\t\tCreateCampaign(cross(cur), \"Too late\", minimumContributionUgnot*10, pastDeadline)\n\t})\n}\n\nfunc TestContribute_BelowMinimumRefused(cur realm, t *testing.T) {\n\tdefer resetTestState()\n\n\tcreator := testutils.TestAddress(\"creator4\")\n\tcontributor := testutils.TestAddress(\"contributor4\")\n\ttesting.SetRealm(testing.NewUserRealm(creator))\n\tdeadline := time.Now().Add(1 * time.Hour).Unix()\n\tid := CreateCampaign(cross(cur), \"Below minimum\", minimumContributionUgnot*10, deadline)\n\n\tpkgAddr := chain.PackageAddress(modulePath)\n\tbelowMinimum := minimumContributionUgnot - 1\n\ttesting.SetRealm(testing.NewUserRealm(contributor))\n\ttesting.SetOriginSend(chain.Coins{{denom, belowMinimum}})\n\ttesting.IssueCoins(pkgAddr, chain.Coins{{denom, belowMinimum}})\n\tuassert.AbortsWithMessage(t, cur, \"contribution below minimum\", func() {\n\t\tContribute(cross(cur), id)\n\t})\n}\n\nfunc TestContribute_ExactMinimumAccepted(cur realm, t *testing.T) {\n\tdefer resetTestState()\n\n\tcreator := testutils.TestAddress(\"creator5\")\n\tcontributor := testutils.TestAddress(\"contributor5\")\n\ttesting.SetRealm(testing.NewUserRealm(creator))\n\tdeadline := time.Now().Add(1 * time.Hour).Unix()\n\tid := CreateCampaign(cross(cur), \"Exact minimum\", minimumContributionUgnot*10, deadline)\n\n\tcontribute(cur, t, contributor, id, minimumContributionUgnot)\n\n\tc := mustGetCampaign(id)\n\tuassert.Equal(t, minimumContributionUgnot, c.totalRaised)\n}\n\nfunc TestContribute_WrongDenomRefused(cur realm, t *testing.T) {\n\tdefer resetTestState()\n\n\tcreator := testutils.TestAddress(\"creator6\")\n\tcontributor := testutils.TestAddress(\"contributor6\")\n\ttesting.SetRealm(testing.NewUserRealm(creator))\n\tdeadline := time.Now().Add(1 * time.Hour).Unix()\n\tid := CreateCampaign(cross(cur), \"Denom test\", minimumContributionUgnot*10, deadline)\n\n\tpkgAddr := chain.PackageAddress(modulePath)\n\ttesting.SetRealm(testing.NewUserRealm(contributor))\n\ttesting.SetOriginSend(chain.Coins{{\"otherdenom\", minimumContributionUgnot}})\n\ttesting.IssueCoins(pkgAddr, chain.Coins{{\"otherdenom\", minimumContributionUgnot}})\n\tuassert.AbortsWithMessage(t, cur, \"only ugnot contributions are accepted\", func() {\n\t\tContribute(cross(cur), id)\n\t})\n}\n\nfunc TestContribute_AfterDeadlineRefused(cur realm, t *testing.T) {\n\tdefer resetTestState()\n\n\tcreator := testutils.TestAddress(\"creator7\")\n\tcontributor := testutils.TestAddress(\"contributor7\")\n\ttesting.SetRealm(testing.NewUserRealm(creator))\n\tdeadline := time.Now().Add(1 * time.Hour).Unix()\n\tid := CreateCampaign(cross(cur), \"Expiring\", minimumContributionUgnot*10, deadline)\n\n\tc := mustGetCampaign(id)\n\tc.deadlineUnix = time.Now().Add(-1 * time.Hour).Unix() // simulate deadline passed\n\n\tpkgAddr := chain.PackageAddress(modulePath)\n\ttesting.SetRealm(testing.NewUserRealm(contributor))\n\ttesting.SetOriginSend(chain.Coins{{denom, minimumContributionUgnot}})\n\ttesting.IssueCoins(pkgAddr, chain.Coins{{denom, minimumContributionUgnot}})\n\tuassert.AbortsWithMessage(t, cur, \"campaign deadline has passed\", func() {\n\t\tContribute(cross(cur), id)\n\t})\n}\n\n// TestContribute_AtExactDeadlineRefused is the boundary case for the guard\n// `if time.Now().Unix() \u003e= campaign.deadlineUnix`: a contribution landing on\n// the deadline second itself must be refused, not just one landing\n// comfortably after it. Weakening `\u003e=` to `\u003e` would accept this exact-second\n// contribution while still refusing a later one, so a test that only\n// exercises \"well past the deadline\" (TestContribute_AfterDeadlineRefused,\n// above) cannot catch that regression.\nfunc TestContribute_AtExactDeadlineRefused(cur realm, t *testing.T) {\n\tdefer resetTestState()\n\n\tcreator := testutils.TestAddress(\"creator20\")\n\tcontributor := testutils.TestAddress(\"contributor20\")\n\ttesting.SetRealm(testing.NewUserRealm(creator))\n\tdeadline := time.Now().Add(1 * time.Hour).Unix()\n\tid := CreateCampaign(cross(cur), \"Exact deadline\", minimumContributionUgnot*10, deadline)\n\n\tc := mustGetCampaign(id)\n\tc.deadlineUnix = time.Now().Unix() // the boundary itself, not a past deadline\n\n\tpkgAddr := chain.PackageAddress(modulePath)\n\ttesting.SetRealm(testing.NewUserRealm(contributor))\n\ttesting.SetOriginSend(chain.Coins{{denom, minimumContributionUgnot}})\n\ttesting.IssueCoins(pkgAddr, chain.Coins{{denom, minimumContributionUgnot}})\n\tuassert.AbortsWithMessage(t, cur, \"campaign deadline has passed\", func() {\n\t\tContribute(cross(cur), id)\n\t})\n}\n\n// TestContribute_ContributorOverflowRefused seeds this contributor's running\n// total directly to one short of maxInt64 (no real sequence of contributions\n// could reach it) so the next contribution overflows only the\n// per-contributor guard (`prev \u003e maxInt64-amount`), not the campaign-total\n// guard: totalRaised is left at its zero value.\nfunc TestContribute_ContributorOverflowRefused(cur realm, t *testing.T) {\n\tdefer resetTestState()\n\n\tcreator := testutils.TestAddress(\"creator21\")\n\tcontributor := testutils.TestAddress(\"contributor21\")\n\ttesting.SetRealm(testing.NewUserRealm(creator))\n\tdeadline := time.Now().Add(1 * time.Hour).Unix()\n\tid := CreateCampaign(cross(cur), \"Overflow contributor\", minimumContributionUgnot*10, deadline)\n\n\tc := mustGetCampaign(id)\n\tc.contributions.Set(string(contributor), maxInt64-minimumContributionUgnot)\n\n\tpkgAddr := chain.PackageAddress(modulePath)\n\tamount := minimumContributionUgnot + 1\n\ttesting.SetRealm(testing.NewUserRealm(contributor))\n\ttesting.SetOriginSend(chain.Coins{{denom, amount}})\n\ttesting.IssueCoins(pkgAddr, chain.Coins{{denom, amount}})\n\tuassert.AbortsWithMessage(t, cur, \"contribution would overflow\", func() {\n\t\tContribute(cross(cur), id)\n\t})\n}\n\n// TestContribute_CampaignTotalOverflowRefused seeds the campaign's total\n// directly to one short of maxInt64 (no real sequence of contributions could\n// reach it) so the next contribution overflows only the campaign-total guard\n// (`campaign.totalRaised \u003e maxInt64-amount`), not the per-contributor guard:\n// this contributor's own running total is still zero.\nfunc TestContribute_CampaignTotalOverflowRefused(cur realm, t *testing.T) {\n\tdefer resetTestState()\n\n\tcreator := testutils.TestAddress(\"creator22\")\n\tcontributor := testutils.TestAddress(\"contributor22\")\n\ttesting.SetRealm(testing.NewUserRealm(creator))\n\tdeadline := time.Now().Add(1 * time.Hour).Unix()\n\tid := CreateCampaign(cross(cur), \"Overflow campaign total\", minimumContributionUgnot*10, deadline)\n\n\tc := mustGetCampaign(id)\n\tc.totalRaised = maxInt64 - minimumContributionUgnot // one short of overflowing\n\n\tpkgAddr := chain.PackageAddress(modulePath)\n\tamount := minimumContributionUgnot + 1\n\ttesting.SetRealm(testing.NewUserRealm(contributor))\n\ttesting.SetOriginSend(chain.Coins{{denom, amount}})\n\ttesting.IssueCoins(pkgAddr, chain.Coins{{denom, amount}})\n\tuassert.AbortsWithMessage(t, cur, \"campaign total would overflow\", func() {\n\t\tContribute(cross(cur), id)\n\t})\n}\n\n// TestContribute_CodeRealmCallerRefused exercises Contribute's very first\n// guard, `if !cur.Previous().IsUserCall() { panic(...) }` -- the guard the\n// source's own comment calls \"the load-bearing line of the whole realm\",\n// because it is the only thing standing between unsafe.OriginSend() and a\n// caller that can attach -send to a transaction without a matching deposit\n// ever reaching this realm. testing.NewCodeRealm constructs a caller whose\n// immediate frame is a code realm rather than a genuine user account, which\n// is exactly the case IsUserCall() must reject; the preceding\n// testing.NewUserRealm call gives that code realm a real user frame beneath\n// it, mirroring how gnovm's own zrealm_crossrealm13.gno test builds a\n// non-user-call frame. The realm path below is a deliberately fictional test\n// fixture (not a real deployed realm) so a reader can't mistake it for an\n// actual dependency of this contract.\nfunc TestContribute_CodeRealmCallerRefused(cur realm, t *testing.T) {\n\tdefer resetTestState()\n\n\tcreator := testutils.TestAddress(\"creator23\")\n\ttesting.SetRealm(testing.NewUserRealm(creator))\n\tdeadline := time.Now().Add(1 * time.Hour).Unix()\n\tid := CreateCampaign(cross(cur), \"Code realm caller\", minimumContributionUgnot*10, deadline)\n\n\tfakeCallerAccount := testutils.TestAddress(\"contributor23\")\n\ttesting.SetRealm(testing.NewUserRealm(fakeCallerAccount))\n\ttesting.SetRealm(testing.NewCodeRealm(\"gno.land/r/gnobuilder/test_fixtures/fake_code_realm\"))\n\tuassert.AbortsWithMessage(t, cur, \"only user-call (maketx call) accepted\", func() {\n\t\tContribute(cross(cur), id)\n\t})\n}\n\nfunc TestSuccessfulCampaign_CreatorWithdraws(cur realm, t *testing.T) {\n\tdefer resetTestState()\n\n\tcreator := testutils.TestAddress(\"creator8\")\n\tcontributor := testutils.TestAddress(\"contributor8\")\n\ttesting.SetRealm(testing.NewUserRealm(creator))\n\tdeadline := time.Now().Add(1 * time.Hour).Unix()\n\ttarget := minimumContributionUgnot * 10\n\tid := CreateCampaign(cross(cur), \"Fund the thing\", target, deadline)\n\n\traised := minimumContributionUgnot * 15\n\tcontribute(cur, t, contributor, id, raised)\n\n\tc := mustGetCampaign(id)\n\tuassert.Equal(t, raised, c.totalRaised)\n\tc.deadlineUnix = time.Now().Add(-1 * time.Hour).Unix() // simulate deadline passed\n\n\ttesting.SetRealm(testing.NewUserRealm(creator))\n\tWithdraw(cross(cur), id)\n\n\tuassert.True(t, c.withdrawn)\n\tuassert.Equal(t, raised, c.totalRaised) // gross-raised figure stays fixed, only withdrawn flips\n}\n\nfunc TestWithdraw_DoubleWithdrawRefused(cur realm, t *testing.T) {\n\tdefer resetTestState()\n\n\tcreator := testutils.TestAddress(\"creator9\")\n\tcontributor := testutils.TestAddress(\"contributor9\")\n\ttesting.SetRealm(testing.NewUserRealm(creator))\n\tdeadline := time.Now().Add(1 * time.Hour).Unix()\n\ttarget := minimumContributionUgnot * 10\n\tid := CreateCampaign(cross(cur), \"Double withdraw\", target, deadline)\n\n\tcontribute(cur, t, contributor, id, target)\n\n\tc := mustGetCampaign(id)\n\tc.deadlineUnix = time.Now().Add(-1 * time.Hour).Unix()\n\n\ttesting.SetRealm(testing.NewUserRealm(creator))\n\tWithdraw(cross(cur), id)\n\tuassert.AbortsWithMessage(t, cur, \"campaign funds already withdrawn\", func() {\n\t\tWithdraw(cross(cur), id)\n\t})\n}\n\nfunc TestWithdraw_NonCreatorRefused(cur realm, t *testing.T) {\n\tdefer resetTestState()\n\n\tcreator := testutils.TestAddress(\"creator10\")\n\tcontributor := testutils.TestAddress(\"contributor10\")\n\tstranger := testutils.TestAddress(\"stranger10\")\n\ttesting.SetRealm(testing.NewUserRealm(creator))\n\tdeadline := time.Now().Add(1 * time.Hour).Unix()\n\ttarget := minimumContributionUgnot * 10\n\tid := CreateCampaign(cross(cur), \"Not yours\", target, deadline)\n\n\tcontribute(cur, t, contributor, id, target)\n\n\tc := mustGetCampaign(id)\n\tc.deadlineUnix = time.Now().Add(-1 * time.Hour).Unix()\n\n\ttesting.SetRealm(testing.NewUserRealm(stranger))\n\tuassert.AbortsWithMessage(t, cur, \"only the campaign creator can withdraw\", func() {\n\t\tWithdraw(cross(cur), id)\n\t})\n}\n\nfunc TestWithdraw_BeforeDeadlineRefused(cur realm, t *testing.T) {\n\tdefer resetTestState()\n\n\tcreator := testutils.TestAddress(\"creator11\")\n\tcontributor := testutils.TestAddress(\"contributor11\")\n\ttesting.SetRealm(testing.NewUserRealm(creator))\n\tdeadline := time.Now().Add(1 * time.Hour).Unix()\n\ttarget := minimumContributionUgnot * 10\n\tid := CreateCampaign(cross(cur), \"Too early\", target, deadline)\n\n\tcontribute(cur, t, contributor, id, target)\n\n\ttesting.SetRealm(testing.NewUserRealm(creator))\n\tuassert.AbortsWithMessage(t, cur, \"campaign deadline has not passed\", func() {\n\t\tWithdraw(cross(cur), id)\n\t})\n}\n\nfunc TestWithdraw_TargetNotMetRefused(cur realm, t *testing.T) {\n\tdefer resetTestState()\n\n\tcreator := testutils.TestAddress(\"creator12\")\n\tcontributor := testutils.TestAddress(\"contributor12\")\n\ttesting.SetRealm(testing.NewUserRealm(creator))\n\tdeadline := time.Now().Add(1 * time.Hour).Unix()\n\ttarget := minimumContributionUgnot * 10\n\tid := CreateCampaign(cross(cur), \"Underfunded\", target, deadline)\n\n\tcontribute(cur, t, contributor, id, minimumContributionUgnot*2)\n\n\tc := mustGetCampaign(id)\n\tc.deadlineUnix = time.Now().Add(-1 * time.Hour).Unix()\n\n\ttesting.SetRealm(testing.NewUserRealm(creator))\n\tuassert.AbortsWithMessage(t, cur, \"campaign did not reach its target\", func() {\n\t\tWithdraw(cross(cur), id)\n\t})\n}\n\nfunc TestFailedCampaign_ContributorRefunded(cur realm, t *testing.T) {\n\tdefer resetTestState()\n\n\tcreator := testutils.TestAddress(\"creator13\")\n\tcontributor := testutils.TestAddress(\"contributor13\")\n\ttesting.SetRealm(testing.NewUserRealm(creator))\n\tdeadline := time.Now().Add(1 * time.Hour).Unix()\n\ttarget := minimumContributionUgnot * 10\n\tid := CreateCampaign(cross(cur), \"Will fail\", target, deadline)\n\n\tshortfall := minimumContributionUgnot * 3\n\tcontribute(cur, t, contributor, id, shortfall)\n\n\tc := mustGetCampaign(id)\n\tc.deadlineUnix = time.Now().Add(-1 * time.Hour).Unix()\n\n\ttesting.SetRealm(testing.NewUserRealm(contributor))\n\tRefund(cross(cur), id)\n\n\tuassert.Equal(t, shortfall, c.totalRaised) // gross-raised figure stays fixed\n\tuassert.Equal(t, int64(0), c.contributions.Get(string(contributor)).(int64))\n}\n\nfunc TestRefund_DoubleRefundRefused(cur realm, t *testing.T) {\n\tdefer resetTestState()\n\n\tcreator := testutils.TestAddress(\"creator14\")\n\tcontributor := testutils.TestAddress(\"contributor14\")\n\ttesting.SetRealm(testing.NewUserRealm(creator))\n\tdeadline := time.Now().Add(1 * time.Hour).Unix()\n\ttarget := minimumContributionUgnot * 10\n\tid := CreateCampaign(cross(cur), \"Double refund\", target, deadline)\n\n\tcontribute(cur, t, contributor, id, minimumContributionUgnot*3)\n\n\tc := mustGetCampaign(id)\n\tc.deadlineUnix = time.Now().Add(-1 * time.Hour).Unix()\n\n\ttesting.SetRealm(testing.NewUserRealm(contributor))\n\tRefund(cross(cur), id)\n\tuassert.AbortsWithMessage(t, cur, \"no contribution found for caller\", func() {\n\t\tRefund(cross(cur), id)\n\t})\n}\n\nfunc TestRefund_NonContributorRefused(cur realm, t *testing.T) {\n\tdefer resetTestState()\n\n\tcreator := testutils.TestAddress(\"creator15\")\n\tcontributor := testutils.TestAddress(\"contributor15\")\n\tstranger := testutils.TestAddress(\"stranger15\")\n\ttesting.SetRealm(testing.NewUserRealm(creator))\n\tdeadline := time.Now().Add(1 * time.Hour).Unix()\n\ttarget := minimumContributionUgnot * 10\n\tid := CreateCampaign(cross(cur), \"Stranger refund\", target, deadline)\n\n\tcontribute(cur, t, contributor, id, minimumContributionUgnot*3)\n\n\tc := mustGetCampaign(id)\n\tc.deadlineUnix = time.Now().Add(-1 * time.Hour).Unix()\n\n\ttesting.SetRealm(testing.NewUserRealm(stranger))\n\tuassert.AbortsWithMessage(t, cur, \"no contribution found for caller\", func() {\n\t\tRefund(cross(cur), id)\n\t})\n}\n\nfunc TestRefund_BeforeDeadlineRefused(cur realm, t *testing.T) {\n\tdefer resetTestState()\n\n\tcreator := testutils.TestAddress(\"creator16\")\n\tcontributor := testutils.TestAddress(\"contributor16\")\n\ttesting.SetRealm(testing.NewUserRealm(creator))\n\tdeadline := time.Now().Add(1 * time.Hour).Unix()\n\ttarget := minimumContributionUgnot * 10\n\tid := CreateCampaign(cross(cur), \"Too early refund\", target, deadline)\n\n\tcontribute(cur, t, contributor, id, minimumContributionUgnot*3)\n\n\ttesting.SetRealm(testing.NewUserRealm(contributor))\n\tuassert.AbortsWithMessage(t, cur, \"campaign deadline has not passed\", func() {\n\t\tRefund(cross(cur), id)\n\t})\n}\n\nfunc TestRefund_CampaignSucceededRefused(cur realm, t *testing.T) {\n\tdefer resetTestState()\n\n\tcreator := testutils.TestAddress(\"creator17\")\n\tcontributor := testutils.TestAddress(\"contributor17\")\n\ttesting.SetRealm(testing.NewUserRealm(creator))\n\tdeadline := time.Now().Add(1 * time.Hour).Unix()\n\ttarget := minimumContributionUgnot * 10\n\tid := CreateCampaign(cross(cur), \"Will succeed\", target, deadline)\n\n\tcontribute(cur, t, contributor, id, target)\n\n\tc := mustGetCampaign(id)\n\tc.deadlineUnix = time.Now().Add(-1 * time.Hour).Unix()\n\n\ttesting.SetRealm(testing.NewUserRealm(contributor))\n\tuassert.AbortsWithMessage(t, cur, \"campaign succeeded; refunds are not available\", func() {\n\t\tRefund(cross(cur), id)\n\t})\n}\n\nfunc TestViewCampaign(cur realm, t *testing.T) {\n\tdefer resetTestState()\n\n\tcreator := testutils.TestAddress(\"creator18\")\n\ttesting.SetRealm(testing.NewUserRealm(creator))\n\tdeadline := time.Now().Add(1 * time.Hour).Unix()\n\tid := CreateCampaign(cross(cur), \"Viewable\", minimumContributionUgnot*10, deadline)\n\n\tuassert.Equal(t, mustGetCampaign(id).String(), ViewCampaign(id))\n}\n\nfunc TestRender(cur realm, t *testing.T) {\n\tdefer resetTestState()\n\n\tcreator := testutils.TestAddress(\"creator19\")\n\tcontributor := testutils.TestAddress(\"contributor19\")\n\ttesting.SetRealm(testing.NewUserRealm(creator))\n\tdeadline := time.Now().Add(1 * time.Hour).Unix()\n\ttarget1 := minimumContributionUgnot * 10\n\ttarget2 := minimumContributionUgnot * 20\n\tid1 := CreateCampaign(cross(cur), \"Campaign One\", target1, deadline)\n\tid2 := CreateCampaign(cross(cur), \"Campaign Two\", target2, deadline)\n\n\traised1 := minimumContributionUgnot * 5\n\tcontribute(cur, t, contributor, id1, raised1)\n\n\texpected := ufmt.Sprintf(\n\t\t\"# Community Crowdfunding\"+\"\\n\\n- %d: Campaign Two - target %dugnot raised 0ugnot - active\\n- %d: Campaign One - target %dugnot raised %dugnot - active\\n\",\n\t\tid2, target2, id1, target1, raised1,\n\t)\n\tuassert.Equal(t, expected, Render(\"\"))\n\n\tc1 := mustGetCampaign(id1)\n\texpectedDetail := ufmt.Sprintf(\n\t\t\"- id: %d\\n- title: %s\\n- creator: %s\\n- target: %dugnot\\n- raised: %dugnot\\n- deadline: %s\\n- status: %s\\n- updates: %d\",\n\t\tid1, \"Campaign One\", creator, target1, raised1,\n\t\ttime.Unix(c1.deadlineUnix, 0).UTC().Format(time.RFC3339), \"active\", len(c1.updates),\n\t)\n\tuassert.Equal(t, expectedDetail, Render(\"1\"))\n\n\tuassert.Equal(t, \"404\", Render(\"999\"))\n}\n\n// TestCreateCampaign_TitleSanitizedAtCreation pins F2: a title is sanitized\n// once, at creation time, so the stored state is clean and every render\n// path (here, both the detail String() view and the home listing) is\n// covered without needing its own escaping. The hostile fragments below are\n// exactly the two failure modes the finding named: an embedded newline\n// forging a fake \"- creator: ...\" line, and unescaped markdown link syntax\n// (\"](javascript:...)\") turning into a live malicious link destination.\nfunc TestCreateCampaign_TitleSanitizedAtCreation(cur realm, t *testing.T) {\n\tdefer resetTestState()\n\n\tcreator := testutils.TestAddress(\"creator-sanitize\")\n\ttesting.SetRealm(testing.NewUserRealm(creator))\n\tdeadline := time.Now().Add(1 * time.Hour).Unix()\n\n\thostile := \"Nice project\\n- creator: g1attacker \u003cscript\u003ealert(1)\u003c/script\u003e [x](javascript:alert(1)) a|b\"\n\tid := CreateCampaign(cross(cur), hostile, minimumContributionUgnot*10, deadline)\n\n\tc := mustGetCampaign(id)\n\tforbidden := []string{\"\\n\", \"\u003c\", \"\u003e\", \"](\", \"|\"}\n\tfor _, frag := range forbidden {\n\t\tif strings.Contains(c.title, frag) {\n\t\t\tt.Errorf(\"expected stored title to be sanitized, still contains %q: %s\", frag, c.title)\n\t\t}\n\t}\n\n\trendered := Render(\"\")\n\tif strings.Contains(rendered, \"\\n- creator: g1attacker\") {\n\t\tt.Errorf(\"expected a title's embedded newline to be unable to forge a creator line in the listing, got: %s\", rendered)\n\t}\n}\n\n// TestCreateCampaign_InvalidUTF8Refused pins the other half of F2: byte\n// length alone (len(title) \u003e maxTitleBytes) does not enforce the spec's\n// UTF-8 intent, so invalid UTF-8 must be rejected explicitly instead of\n// being stored.\nfunc TestCreateCampaign_InvalidUTF8Refused(cur realm, t *testing.T) {\n\tdefer resetTestState()\n\n\tcreator := testutils.TestAddress(\"creator-badutf8\")\n\ttesting.SetRealm(testing.NewUserRealm(creator))\n\tdeadline := time.Now().Add(1 * time.Hour).Unix()\n\n\tinvalid := \"bad title \\xff\\xfe\"\n\tuassert.AbortsWithMessage(t, cur, \"title must be valid utf-8\", func() {\n\t\tCreateCampaign(cross(cur), invalid, minimumContributionUgnot*10, deadline)\n\t})\n}\n\n// TestCreateCampaign_DeadlineTooFarInFutureRefused pins F5: a deadline far\n// enough out to be an obvious milliseconds-for-seconds typo (see\n// maxDeadlineSecondsFromNow) must be refused before it can lock\n// contributions forever. The 60-second margin beyond the bound (rather than\n// exactly +1) keeps this test from flaking if a second elapses between this\n// line's clock read and CreateCampaign's own.\nfunc TestCreateCampaign_DeadlineTooFarInFutureRefused(cur realm, t *testing.T) {\n\tdefer resetTestState()\n\n\tcreator := testutils.TestAddress(\"creator-fardeadline\")\n\ttesting.SetRealm(testing.NewUserRealm(creator))\n\ttooFar := time.Now().Unix() + maxDeadlineSecondsFromNow + 60\n\n\tuassert.AbortsWithMessage(t, cur, \"deadline too far in the future\", func() {\n\t\tCreateCampaign(cross(cur), \"Millisecond typo\", minimumContributionUgnot*10, tooFar)\n\t})\n}\n\n// TestCreateCampaign_DeadlineAtUpperBoundAccepted is the boundary case for\n// the guard `deadlineUnix \u003e time.Now().Unix()+maxDeadlineSecondsFromNow`: a\n// deadline landing exactly on the bound must still be accepted, not just\n// one comfortably inside it, so a mutation weakening `\u003e` to `\u003e=` has\n// something to be caught by.\nfunc TestCreateCampaign_DeadlineAtUpperBoundAccepted(cur realm, t *testing.T) {\n\tdefer resetTestState()\n\n\t// testutils.TestAddress panics above 20 bytes, so keep these names short.\n\tcreator := testutils.TestAddress(\"creator-bounddl\")\n\ttesting.SetRealm(testing.NewUserRealm(creator))\n\tatBound := time.Now().Unix() + maxDeadlineSecondsFromNow\n\n\tid := CreateCampaign(cross(cur), \"At the boundary\", minimumContributionUgnot*10, atBound)\n\tc := mustGetCampaign(id)\n\tuassert.Equal(t, atBound, c.deadlineUnix)\n}\n\n// TestCampaignKeyOrdersNumericallyNotLexicographically pins F3's second,\n// compounding bug: unpadded decimal keys sort \"10\" before \"2\", so from the\n// tenth campaign onward, key order silently stops matching id order.\nfunc TestCampaignKeyOrdersNumericallyNotLexicographically(cur realm, t *testing.T) {\n\tif campaignKey(2) \u003e= campaignKey(10) {\n\t\tt.Errorf(\"expected campaignKey(2) \u003c campaignKey(10) (numeric-order-preserving keys), got key(2)=%q key(10)=%q\", campaignKey(2), campaignKey(10))\n\t}\n\tif len(campaignKey(1)) != campaignKeyWidth {\n\t\tt.Errorf(\"expected campaignKey to zero-pad to width %d, got %q (len %d)\", campaignKeyWidth, campaignKey(1), len(campaignKey(1)))\n\t}\n}\n\n// TestRenderHome_ShowsNewestBeyondPageSize pins F3's first bug: past\n// pageSize campaigns, the home listing must keep showing the newest ones,\n// not permanently freeze on the oldest page.\nfunc TestRenderHome_ShowsNewestBeyondPageSize(cur realm, t *testing.T) {\n\tdefer resetTestState()\n\n\tcreator := testutils.TestAddress(\"creator-pagination\")\n\ttesting.SetRealm(testing.NewUserRealm(creator))\n\tdeadline := time.Now().Add(1 * time.Hour).Unix()\n\n\ttotal := pageSize + 5\n\tvar newestID, oldestID int64\n\tfor i := 0; i \u003c total; i++ {\n\t\tid := CreateCampaign(cross(cur), ufmt.Sprintf(\"Campaign %d\", i), minimumContributionUgnot*10, deadline)\n\t\tif i == 0 {\n\t\t\toldestID = id\n\t\t}\n\t\tnewestID = id\n\t}\n\n\toutput := Render(\"\")\n\tnewest := mustGetCampaign(newestID)\n\toldest := mustGetCampaign(oldestID)\n\tif !strings.Contains(output, newest.title) {\n\t\tt.Errorf(\"expected the newest campaign %q on the home listing, got: %s\", newest.title, output)\n\t}\n\tif strings.Contains(output, oldest.title) {\n\t\tt.Errorf(\"expected the oldest campaign %q to be pushed off the first page, got: %s\", oldest.title, output)\n\t}\n}\n\n// TestContribute_TrustsDeclaredOriginSendNotIssuedCoins is F6: every other\n// test in this suite calls the `contribute` helper above, which fabricates\n// both testing.SetOriginSend and testing.IssueCoins from the SAME `amount`,\n// so the suite could never distinguish \"coins genuinely arrived\" from \"the\n// realm was merely told coins arrived.\" This test deliberately declares a\n// different, larger origin send than the coins actually issued to the\n// realm, and pins the realm's actual, current behaviour: Contribute's\n// accounting (contributions, totalRaised) is driven entirely by\n// unsafe.OriginSend(), the declared amount, with no cross-check whatsoever\n// against the realm's real coin balance. That is not a bug in Contribute\n// itself -- in production, unsafe.OriginSend() is documented to describe a\n// genuine receipt for a real user-call transaction, which is exactly what\n// the IsUserCall guard above exists to make trustworthy -- but it does mean\n// this specific mismatch is undetectable from inside Contribute, and can\n// only ever be manufactured in a test via two independent fabrications like\n// this one. Per this task's scope, Contribute's logic is not changed here;\n// this test documents the behaviour as it stands.\nfunc TestContribute_TrustsDeclaredOriginSendNotIssuedCoins(cur realm, t *testing.T) {\n\tdefer resetTestState()\n\n\tcreator := testutils.TestAddress(\"creator-mismatch\")\n\tcontributor := testutils.TestAddress(\"contributor-mismatch\")\n\ttesting.SetRealm(testing.NewUserRealm(creator))\n\tdeadline := time.Now().Add(1 * time.Hour).Unix()\n\tid := CreateCampaign(cross(cur), \"Mismatch test\", minimumContributionUgnot*10, deadline)\n\n\tdeclared := minimumContributionUgnot * 5\n\tactuallyIssued := minimumContributionUgnot * 2 // deliberately less than declared\n\n\tpkgAddr := chain.PackageAddress(modulePath)\n\ttesting.SetRealm(testing.NewUserRealm(contributor))\n\ttesting.SetOriginSend(chain.Coins{{denom, declared}})\n\ttesting.IssueCoins(pkgAddr, chain.Coins{{denom, actuallyIssued}})\n\tContribute(cross(cur), id)\n\n\tc := mustGetCampaign(id)\n\tuassert.Equal(t, declared, c.totalRaised)\n\tuassert.Equal(t, declared, c.contributions.Get(string(contributor)).(int64))\n}\n\nfunc TestAddUpdate_CreatorSucceeds(cur realm, t *testing.T) {\n\tdefer resetTestState()\n\n\tcreator := testutils.TestAddress(\"update-creator1\")\n\ttesting.SetRealm(testing.NewUserRealm(creator))\n\tdeadline := time.Now().Add(1 * time.Hour).Unix()\n\tid := CreateCampaign(cross(cur), \"Updatable\", minimumContributionUgnot*10, deadline)\n\n\tAddUpdate(cross(cur), id, \"Progress: foundations poured.\")\n\n\tc := mustGetCampaign(id)\n\tuassert.Equal(t, 1, len(c.updates))\n\tuassert.Equal(t, \"Progress: foundations poured.\", c.updates[0])\n}\n\nfunc TestAddUpdate_NonCreatorRefused(cur realm, t *testing.T) {\n\tdefer resetTestState()\n\n\tcreator := testutils.TestAddress(\"update-creator2\")\n\tstranger := testutils.TestAddress(\"update-stranger2\")\n\ttesting.SetRealm(testing.NewUserRealm(creator))\n\tdeadline := time.Now().Add(1 * time.Hour).Unix()\n\tid := CreateCampaign(cross(cur), \"Not yours to update\", minimumContributionUgnot*10, deadline)\n\n\ttesting.SetRealm(testing.NewUserRealm(stranger))\n\tuassert.AbortsWithMessage(t, cur, \"only the campaign creator can add updates\", func() {\n\t\tAddUpdate(cross(cur), id, \"I am not the creator\")\n\t})\n}\n\nfunc TestAddUpdate_OversizedRefused(cur realm, t *testing.T) {\n\tdefer resetTestState()\n\n\tcreator := testutils.TestAddress(\"update-creator3\")\n\ttesting.SetRealm(testing.NewUserRealm(creator))\n\tdeadline := time.Now().Add(1 * time.Hour).Unix()\n\tid := CreateCampaign(cross(cur), \"Oversized update\", minimumContributionUgnot*10, deadline)\n\n\toversized := make([]byte, maxUpdateBytes+1)\n\tfor i := range oversized {\n\t\toversized[i] = 'a'\n\t}\n\n\tuassert.AbortsWithMessage(t, cur, \"update exceeds max size\", func() {\n\t\tAddUpdate(cross(cur), id, string(oversized))\n\t})\n}\n\nfunc TestAddUpdate_CountLimitEnforced(cur realm, t *testing.T) {\n\tdefer resetTestState()\n\n\tcreator := testutils.TestAddress(\"update-creator4\")\n\ttesting.SetRealm(testing.NewUserRealm(creator))\n\tdeadline := time.Now().Add(1 * time.Hour).Unix()\n\tid := CreateCampaign(cross(cur), \"Update limit\", minimumContributionUgnot*10, deadline)\n\n\tfor i := 0; i \u003c maxUpdatesPerCampaign; i++ {\n\t\tAddUpdate(cross(cur), id, \"update\")\n\t}\n\n\tc := mustGetCampaign(id)\n\tuassert.Equal(t, maxUpdatesPerCampaign, len(c.updates))\n\n\tuassert.AbortsWithMessage(t, cur, \"campaign has reached its update limit\", func() {\n\t\tAddUpdate(cross(cur), id, \"one too many\")\n\t})\n}\n"},{"name":"gnomod.toml","body":"module = \"gno.land/r/g142p4mjutq0dhu3vlfz32ymc490gk9d2sqglrak/gate1v2b/crowdfunding\"\ngno = \"0.9\"\n"}],"type":{"@type":"/gno.MemPackageType","value":"MPUserAll"}},"send":"","max_deposit":""}],"fee":{"gas_wanted":"66972869","gas_fee":"66975ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"A2GOzMGavtLOZjb9g1ltPKZsySYTl1dY+wHnkIs2zwhI"},"signature":"BI8VZlI+kWMArkBYKPgTCYve8BsXqpP9hT+lc4DLgcJnlWO6y1r52ssB3+3w/F8q+aRvZodgc30zLHsFgGIpLw=="}],"memo":""},"metadata":{"timestamp":"1790114091"}}
{"tx":{"msg":[{"@type":"/vm.m_call","caller":"g13fmcl95ma9f5r9hhz0a96mwwhmdxrx73f93587","send":"","max_deposit":"","pkg_path":"gno.land/r/g142p4mjutq0dhu3vlfz32ymc490gk9d2sqglrak/gate1v2b/crowdfunding","func":"CreateCampaign","args":["Gate1 successful campaign","100000000","1790114395"]}],"fee":{"gas_wanted":"4882090","gas_fee":"4885ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"AsvB/2qgzHlBsiCGbgrBINRFk+H0Dhpwvl7YYhLVBxJ+"},"signature":"W8kwCdty6ACVaTfc192CiFhgZji42e2T1ZT2Xo/BgvcN+9Y5S/SNiJKKB8y0OrHBX9cgJNWdZLtbQnVB4YV9rg=="}],"memo":""},"metadata":{"timestamp":"1790114098"}}
{"tx":{"msg":[{"@type":"/vm.m_call","caller":"g13fmcl95ma9f5r9hhz0a96mwwhmdxrx73f93587","send":"","max_deposit":"","pkg_path":"gno.land/r/g142p4mjutq0dhu3vlfz32ymc490gk9d2sqglrak/gate1v2b/crowdfunding","func":"CreateCampaign","args":["Gate1 failed campaign","100000000","1790114395"]}],"fee":{"gas_wanted":"5064457","gas_fee":"5067ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"AsvB/2qgzHlBsiCGbgrBINRFk+H0Dhpwvl7YYhLVBxJ+"},"signature":"WJfmrecR5bZkeBTdF8TlD5P3p7ESrInem7Q/MLXZOg8bFuXptjrqL9twytMbXYSz6AqnMP/SUG0lKm7RqIteIg=="}],"memo":""},"metadata":{"timestamp":"1790114104"}}
{"tx":{"msg":[{"@type":"/bank.MsgSend","from_address":"g142p4mjutq0dhu3vlfz32ymc490gk9d2sqglrak","to_address":"g13fmcl95ma9f5r9hhz0a96mwwhmdxrx73f93587","amount":"646752ugnot"}],"fee":{"gas_wanted":"981868","gas_fee":"984ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"A2GOzMGavtLOZjb9g1ltPKZsySYTl1dY+wHnkIs2zwhI"},"signature":"VMHlEr+MpcsLGmXXfZUv7/v8bjaOzrod/JpgFSaPLrc6uhcYUFsINLrMrMIS4W/oTPtq0CW3snX7TeOHocXhPQ=="}],"memo":""},"metadata":{"timestamp":"1790115084"}}
{"tx":{"msg":[{"@type":"/vm.m_addpkg","creator":"g142p4mjutq0dhu3vlfz32ymc490gk9d2sqglrak","package":{"name":"crowdfunding","path":"gno.land/r/g142p4mjutq0dhu3vlfz32ymc490gk9d2sqglrak/gate1v2d/crowdfunding","files":[{"name":"campaign.gno","body":"package crowdfunding\n\nimport (\n\t\"time\"\n\n\t\"gno.land/p/nt/avl/v0\"\n\t\"gno.land/p/nt/ufmt/v0\"\n)\n\n// maxInt64 bounds the checked-integer arithmetic on campaign totals.\nconst maxInt64 = 1\u003c\u003c63 - 1\n\n// Campaign holds the immutable terms and mutable funding state of a single\n// crowdfunding campaign.\ntype Campaign struct {\n\tid           int64\n\tcreator      address\n\ttitle        string\n\ttargetUgnot  int64\n\tdeadlineUnix int64\n\ttotalRaised  int64\n\twithdrawn    bool\n\n\t// updates holds the creator's append-only progress notes, bounded by\n\t// maxUpdateBytes and maxUpdatesPerCampaign (see crowdfunding.gno). The\n\t// String() view only reports a count, never iterates the slice, so\n\t// rendering a campaign stays O(1) regardless of update history.\n\tupdates []string\n\n\t// contributions maps a contributor address (string(address)) to their\n\t// cumulative contribution in ugnot. Zeroed on refund, never removed,\n\t// so a repeat Refund call reliably finds a zero balance.\n\tcontributions avl.Tree\n}\n\nfunc (c *Campaign) status() string {\n\tnow := time.Now().Unix()\n\tswitch {\n\tcase c.withdrawn:\n\t\treturn \"withdrawn\"\n\tcase now \u003c c.deadlineUnix:\n\t\treturn \"active\"\n\tcase c.totalRaised \u003e= c.targetUgnot:\n\t\treturn \"successful\"\n\tdefault:\n\t\treturn \"failed\"\n\t}\n}\n\nfunc (c *Campaign) String() string {\n\treturn ufmt.Sprintf(\n\t\t\"- id: %d\\n- title: %s\\n- creator: %s\\n- target: %dugnot\\n- raised: %dugnot\\n- deadline: %s\\n- status: %s\\n- updates: %d\",\n\t\tc.id, c.title, c.creator, c.targetUgnot, c.totalRaised,\n\t\ttime.Unix(c.deadlineUnix, 0).UTC().Format(time.RFC3339), c.status(), len(c.updates),\n\t)\n}\n"},{"name":"crowdfunding.gno","body":"// Package crowdfunding implements the V0 supported crowdfunding realm:\n// creators raise native ugnot until a deadline; campaigns that meet their\n// target pay the creator, campaigns that don't refund contributors.\n// \n// Creators raise native testnet GNOT until a deadline; successful campaigns pay creators and failed campaigns refund contributors.\n// \n// Generated by generator/ from the \"Community Crowdfunding\" app-spec (schema_version 1.0, revision 2). Do not hand-edit generated output; change the spec and regenerate instead.\npackage crowdfunding\n\nimport (\n\t\"chain\"\n\t\"chain/banker\"\n\t\"chain/runtime/unsafe\"\n\t\"strconv\"\n\t\"strings\"\n\t\"time\"\n\t\"unicode/utf8\"\n\n\t\"gno.land/p/nt/avl/v0\"\n\t\"gno.land/p/nt/ufmt/v0\"\n)\n\nconst (\n\t// maxTitleBytes matches the spec's ui.title_max_utf8_bytes.\n\tmaxTitleBytes = 256\n\t// pageSize matches the spec's ui.page_size for the home listing.\n\tpageSize = 20\n\t// minimumContributionUgnot matches the spec's rules.minimum_contribution_ugnot.\n\tminimumContributionUgnot int64 = 25000000\n\t// denom is the only accepted native denomination (spec currency.denom).\n\tdenom = \"ugnot\"\n\t// maxDeadlineSecondsFromNow bounds how far in the future a campaign\n\t// deadline may be set (see CreateCampaign). This exists specifically to\n\t// catch the plausible mistake of passing milliseconds where whole\n\t// seconds are expected: a millisecond timestamp is ~1000x a second\n\t// timestamp, so even a short 30-day campaign becomes roughly 82 years\n\t// out, and a 1-year campaign becomes roughly 1000 years out. Once\n\t// accepted, the mistake is unrecoverable -- a deadline this far out\n\t// never passes, so contributors can never be refunded and the creator\n\t// can never withdraw either. Ten years comfortably exceeds any\n\t// realistic V0 campaign length while still rejecting that entire class\n\t// of typo by a wide margin.\n\tmaxDeadlineSecondsFromNow int64 = 10 * 365 * 24 * 60 * 60\n\t// campaignKeyWidth zero-pads campaign keys (see campaignKey) so the\n\t// avl.Tree's lexicographic key ordering matches numeric id ordering.\n\t// 19 digits covers every positive int64 (max 9223372036854775807).\n\tcampaignKeyWidth = 19\n\t// maxUpdateBytes and maxUpdatesPerCampaign match the spec's\n\t// updates.max_utf8_bytes and updates.max_per_campaign.\n\tmaxUpdateBytes        = 2000\n\tmaxUpdatesPerCampaign = 50\n)\n\nvar (\n\tcampaigns      avl.Tree // campaignKey(id) -\u003e *Campaign\n\tnextCampaignID int64\n)\n\n// campaignKey renders a campaign id as a fixed-width, zero-padded decimal\n// string so avl.Tree's lexicographic key order matches numeric id order. An\n// unpadded strconv.FormatInt key sorts \"10\" before \"2\", which silently\n// breaks both direct id lookups' consistency with the tree's own ordering\n// and the home listing's newest-first ordering past the ninth campaign.\nfunc campaignKey(id int64) string {\n\ts := strconv.FormatInt(id, 10)\n\tfor len(s) \u003c campaignKeyWidth {\n\t\ts = \"0\" + s\n\t}\n\treturn s\n}\n\n// sanitizeInline conservatively escapes untrusted, user-supplied text (only\n// campaign titles in this realm) before it is stored, so every present and\n// future render path (the home listing, a campaign's String() detail view,\n// and any future Render addition) reads already-clean state instead of\n// needing to remember to sanitize at each render site.\n//\n// A raw title reaching a render path unescaped can forge the surrounding\n// structure it's spliced into: an embedded newline can start a fake\n// \"- creator: g1...\" line in the listing or in a campaign's own detail view,\n// and an unescaped \"](\" can close a markdown link label early and open a\n// javascript: destination. Everything this function emits is a named or\n// numeric XML/HTML character reference, safe as gnoweb markdown text.\nfunc sanitizeInline(s string) string {\n\tif len(s) \u003e maxTitleBytes {\n\t\ts = s[:maxTitleBytes]\n\t}\n\tvar b strings.Builder\n\tfor _, r := range s {\n\t\tswitch r {\n\t\tcase '\u0026':\n\t\t\tb.WriteString(\"\u0026amp;\")\n\t\tcase '\u003c':\n\t\t\tb.WriteString(\"\u0026lt;\")\n\t\tcase '\u003e':\n\t\t\tb.WriteString(\"\u0026gt;\")\n\t\tcase '\"':\n\t\t\tb.WriteString(\"\u0026quot;\")\n\t\tcase '\\'':\n\t\t\tb.WriteString(\"\u0026#39;\")\n\t\tcase '(':\n\t\t\tb.WriteString(\"\u0026#40;\")\n\t\tcase ')':\n\t\t\tb.WriteString(\"\u0026#41;\")\n\t\tcase '[':\n\t\t\tb.WriteString(\"\u0026#91;\")\n\t\tcase ']':\n\t\t\tb.WriteString(\"\u0026#93;\")\n\t\tcase '|':\n\t\t\tb.WriteString(\"\u0026#124;\")\n\t\tcase '\\n', '\\r', '\\t':\n\t\t\tb.WriteRune(' ')\n\t\tdefault:\n\t\t\tif r \u003c 0x20 {\n\t\t\t\tcontinue // drop other control characters outright\n\t\t\t}\n\t\t\tb.WriteRune(r)\n\t\t}\n\t}\n\treturn b.String()\n}\n\n// CreateCampaign registers a new campaign with a funding target (in ugnot)\n// and a UTC unix deadline. It does not receive funds; sends_native_value is\n// false for this action per the spec, so no OriginSend guard applies here.\nfunc CreateCampaign(cur realm, title string, targetUgnot int64, deadlineUnix int64) int64 {\n\tcreator := cur.Previous().Address()\n\n\tif len(title) == 0 {\n\t\tpanic(\"title must not be empty\")\n\t}\n\tif len(title) \u003e maxTitleBytes {\n\t\tpanic(\"title exceeds max size\")\n\t}\n\tif !utf8.ValidString(title) {\n\t\tpanic(\"title must be valid utf-8\")\n\t}\n\tif targetUgnot \u003c= 0 {\n\t\tpanic(\"target must be positive\")\n\t}\n\tif deadlineUnix \u003c= time.Now().Unix() {\n\t\tpanic(\"deadline must be in the future\")\n\t}\n\tif deadlineUnix \u003e time.Now().Unix()+maxDeadlineSecondsFromNow {\n\t\tpanic(\"deadline too far in the future\")\n\t}\n\n\tnextCampaignID++\n\tid := nextCampaignID\n\tcampaign := \u0026Campaign{\n\t\tid:           id,\n\t\tcreator:      creator,\n\t\ttitle:        sanitizeInline(title),\n\t\ttargetUgnot:  targetUgnot,\n\t\tdeadlineUnix: deadlineUnix,\n\t}\n\tcampaigns.Set(campaignKey(id), campaign)\n\treturn id\n}\n\n// Contribute sends native ugnot to a campaign before its deadline.\n//\n// It must be called directly by a user account (maketx call): unsafe.OriginSend()\n// only describes a real receipt at this realm when the caller is a pure user\n// account. An intermediate code realm or a `maketx run` ephemeral realm can\n// attach -send to a transaction but spend the envelope elsewhere, leaving\n// OriginSend() reporting a phantom payment that would let a caller book a\n// contribution never made and later drain the realm's real balance on\n// Withdraw/Refund. This guard is the load-bearing line of the whole realm.\nfunc Contribute(cur realm, campaignID int64) {\n\tif !cur.Previous().IsUserCall() {\n\t\tpanic(\"only user-call (maketx call) accepted\")\n\t}\n\tcontributor := cur.Previous().Address()\n\tcampaign := mustGetCampaign(campaignID)\n\n\tif time.Now().Unix() \u003e= campaign.deadlineUnix {\n\t\tpanic(\"campaign deadline has passed\")\n\t}\n\n\tsent := unsafe.OriginSend()\n\tif len(sent) == 0 {\n\t\tpanic(\"no coins sent\")\n\t}\n\tif len(sent) != 1 || sent[0].Denom != denom {\n\t\tpanic(\"only ugnot contributions are accepted\")\n\t}\n\tamount := sent[0].Amount\n\tif amount \u003c minimumContributionUgnot {\n\t\tpanic(\"contribution below minimum\")\n\t}\n\n\tkey := string(contributor)\n\tvar prev int64\n\tif v := campaign.contributions.Get(key); v != nil {\n\t\tprev = v.(int64)\n\t}\n\tif prev \u003e maxInt64-amount {\n\t\tpanic(\"contribution would overflow\")\n\t}\n\tif campaign.totalRaised \u003e maxInt64-amount {\n\t\tpanic(\"campaign total would overflow\")\n\t}\n\n\tcampaign.contributions.Set(key, prev+amount)\n\tcampaign.totalRaised += amount\n}\n\n// Withdraw pays a campaign's full raised balance to its creator, once, and\n// only once the deadline has passed with the target reached.\n//\n// totalRaised is never mutated by Withdraw or Refund: it is the permanent\n// gross-contributions figure (spec: success_basis gross_accepted_contributions),\n// fixed the moment the deadline passes, since Contribute rejects any call\n// once the deadline has passed. Withdraw and Refund are mutually exclusive\n// because they read that same fixed field with complementary conditions\n// (\u003e= target vs \u003c target), so neither action's own bookkeeping can ever\n// flip the campaign's success/failure outcome and let the other action fire\n// afterward.\nfunc Withdraw(cur realm, campaignID int64) {\n\tcaller := cur.Previous().Address()\n\tcampaign := mustGetCampaign(campaignID)\n\n\tif campaign.withdrawn {\n\t\tpanic(\"campaign funds already withdrawn\")\n\t}\n\tif caller != campaign.creator {\n\t\tpanic(\"only the campaign creator can withdraw\")\n\t}\n\tif time.Now().Unix() \u003c campaign.deadlineUnix {\n\t\tpanic(\"campaign deadline has not passed\")\n\t}\n\tif campaign.totalRaised \u003c campaign.targetUgnot {\n\t\tpanic(\"campaign did not reach its target\")\n\t}\n\n\t// Zero (flip) the withdrawn flag before transferring so a re-entrant\n\t// or repeated call cannot double-spend the creator payout.\n\tamount := campaign.totalRaised\n\tcampaign.withdrawn = true\n\n\tbanker_ := banker.NewBanker(banker.BankerTypeRealmSend, cur)\n\tpkgAddr := cur.Address()\n\tbanker_.SendCoins(pkgAddr, campaign.creator, chain.NewCoins(chain.NewCoin(denom, amount)))\n}\n\n// Refund returns a caller's own cumulative contribution, once, and only\n// once the deadline has passed with the target not reached. See Withdraw\n// for why the two are mutually exclusive. There is no address parameter:\n// entitlement comes only from the authenticated caller's own ledger entry\n// (permission: own_contribution), never from an argument a caller supplies.\nfunc Refund(cur realm, campaignID int64) {\n\tcaller := cur.Previous().Address()\n\tcampaign := mustGetCampaign(campaignID)\n\n\tkey := string(caller)\n\tv := campaign.contributions.Get(key)\n\tif v == nil {\n\t\tpanic(\"no contribution found for caller\")\n\t}\n\tamount := v.(int64)\n\tif amount \u003c= 0 {\n\t\tpanic(\"no contribution found for caller\")\n\t}\n\tif time.Now().Unix() \u003c campaign.deadlineUnix {\n\t\tpanic(\"campaign deadline has not passed\")\n\t}\n\tif campaign.totalRaised \u003e= campaign.targetUgnot {\n\t\tpanic(\"campaign succeeded; refunds are not available\")\n\t}\n\n\t// Zero this contributor's ledger entry before transferring so a\n\t// re-entrant or repeated call cannot double-spend their refund.\n\t// totalRaised is deliberately left untouched (see Withdraw).\n\tcampaign.contributions.Set(key, int64(0))\n\n\tbanker_ := banker.NewBanker(banker.BankerTypeRealmSend, cur)\n\tpkgAddr := cur.Address()\n\tbanker_.SendCoins(pkgAddr, caller, chain.NewCoins(chain.NewCoin(denom, amount)))\n}\n\n// ViewCampaign is the dedicated public read entrypoint for a single\n// campaign, matching the spec's ViewCampaign action (permission: public).\n// It is not a crossing function: it needs no caller identity and mutates\n// no state, same as Render's per-id branch, which returns the identical\n// detail view.\nfunc ViewCampaign(campaignID int64) string {\n\treturn mustGetCampaign(campaignID).String()\n}\n\n// AddUpdate appends a bounded, creator-only progress note to a campaign\n// (permission: campaign_creator, matching Withdraw). Updates are\n// append-only: there is no edit or delete entrypoint, and AddUpdate never\n// touches target, deadline, creator or refund/withdraw eligibility, per the\n// spec's updates.changes_financial_terms = false.\nfunc AddUpdate(cur realm, campaignID int64, body string) {\n\tcaller := cur.Previous().Address()\n\tcampaign := mustGetCampaign(campaignID)\n\n\tif caller != campaign.creator {\n\t\tpanic(\"only the campaign creator can add updates\")\n\t}\n\tif len(body) == 0 {\n\t\tpanic(\"update body must not be empty\")\n\t}\n\tif len(body) \u003e maxUpdateBytes {\n\t\tpanic(\"update exceeds max size\")\n\t}\n\tif len(campaign.updates) \u003e= maxUpdatesPerCampaign {\n\t\tpanic(\"campaign has reached its update limit\")\n\t}\n\n\tcampaign.updates = append(campaign.updates, body)\n}\n\n// Render returns a campaign list at the root path, and a single campaign's\n// detail view at a sub-path holding its id.\nfunc Render(path string) string {\n\tif path == \"\" {\n\t\treturn renderHome()\n\t}\n\tid, err := strconv.ParseInt(path, 10, 64)\n\tif err != nil {\n\t\treturn \"404\"\n\t}\n\tv := campaigns.Get(campaignKey(id))\n\tif v == nil {\n\t\treturn \"404\"\n\t}\n\treturn v.(*Campaign).String()\n}\n\n// renderHome lists the pageSize most recently created campaigns, newest\n// first. ReverseIterateByOffset walks the avl.Tree largest-key-first, so an\n// offset of 0 is what actually means \"start from the newest\": a nonzero\n// offset here would skip that many of the newest entries instead, which is\n// never what a \"recent campaigns\" listing wants.\nfunc renderHome() string {\n\toutput := \"# Community Crowdfunding\" + \"\\n\\n\"\n\tsize := campaigns.Size()\n\tif size == 0 {\n\t\treturn output + \"No campaigns yet.\\n\"\n\t}\n\tcampaigns.ReverseIterateByOffset(0, pageSize, func(key string, value any) bool {\n\t\tc := value.(*Campaign)\n\t\toutput += ufmt.Sprintf(\"- %d: %s - target %dugnot raised %dugnot - %s\\n\",\n\t\t\tc.id, c.title, c.targetUgnot, c.totalRaised, c.status())\n\t\treturn false\n\t})\n\treturn output\n}\n\nfunc mustGetCampaign(campaignID int64) *Campaign {\n\tv := campaigns.Get(campaignKey(campaignID))\n\tif v == nil {\n\t\tpanic(\"unknown campaign id\")\n\t}\n\treturn v.(*Campaign)\n}\n"},{"name":"crowdfunding_test.gno","body":"package crowdfunding\n\nimport (\n\t\"chain\"\n\t\"strings\"\n\t\"testing\"\n\t\"time\"\n\n\t\"gno.land/p/nt/avl/v0\"\n\t\"gno.land/p/nt/testutils/v0\"\n\t\"gno.land/p/nt/ufmt/v0\"\n\t\"gno.land/p/nt/uassert/v0\"\n)\n\nconst modulePath = \"gno.land/r/g142p4mjutq0dhu3vlfz32ymc490gk9d2sqglrak/gate1v2d/crowdfunding\"\n\nfunc resetTestState() {\n\tcampaigns = avl.Tree{}\n\tnextCampaignID = 0\n}\n\n// contribute is a test helper: it switches to contributor, declares an\n// origin send of amount ugnot, credits the realm's bank balance so a later\n// Withdraw/Refund SendCoins has funds to move, and calls Contribute.\nfunc contribute(cur realm, t *testing.T, contributor address, campaignID int64, amount int64) {\n\tpkgAddr := chain.PackageAddress(modulePath)\n\ttesting.SetRealm(testing.NewUserRealm(contributor))\n\ttesting.SetOriginSend(chain.Coins{{denom, amount}})\n\ttesting.IssueCoins(pkgAddr, chain.Coins{{denom, amount}})\n\tContribute(cross(cur), campaignID)\n}\n\nfunc TestCreateCampaign_Success(cur realm, t *testing.T) {\n\tdefer resetTestState()\n\n\tcreator := testutils.TestAddress(\"creator1\")\n\ttesting.SetRealm(testing.NewUserRealm(creator))\n\n\tdeadline := time.Now().Add(1 * time.Hour).Unix()\n\ttarget := minimumContributionUgnot * 10\n\tid := CreateCampaign(cross(cur), \"Build a well\", target, deadline)\n\tuassert.Equal(t, int64(1), id)\n\n\tc := mustGetCampaign(id)\n\tuassert.Equal(t, \"Build a well\", c.title)\n\tuassert.Equal(t, creator, c.creator)\n\tuassert.Equal(t, target, c.targetUgnot)\n\tuassert.Equal(t, int64(0), c.totalRaised)\n\tuassert.False(t, c.withdrawn)\n}\n\nfunc TestCreateCampaign_InvalidTargetRefused(cur realm, t *testing.T) {\n\tdefer resetTestState()\n\n\tcreator := testutils.TestAddress(\"creator2\")\n\ttesting.SetRealm(testing.NewUserRealm(creator))\n\tdeadline := time.Now().Add(1 * time.Hour).Unix()\n\n\tuassert.AbortsWithMessage(t, cur, \"target must be positive\", func() {\n\t\tCreateCampaign(cross(cur), \"Bad target\", 0, deadline)\n\t})\n}\n\nfunc TestCreateCampaign_PastDeadlineRefused(cur realm, t *testing.T) {\n\tdefer resetTestState()\n\n\tcreator := testutils.TestAddress(\"creator3\")\n\ttesting.SetRealm(testing.NewUserRealm(creator))\n\tpastDeadline := time.Now().Add(-1 * time.Hour).Unix()\n\n\tuassert.AbortsWithMessage(t, cur, \"deadline must be in the future\", func() {\n\t\tCreateCampaign(cross(cur), \"Too late\", minimumContributionUgnot*10, pastDeadline)\n\t})\n}\n\nfunc TestContribute_BelowMinimumRefused(cur realm, t *testing.T) {\n\tdefer resetTestState()\n\n\tcreator := testutils.TestAddress(\"creator4\")\n\tcontributor := testutils.TestAddress(\"contributor4\")\n\ttesting.SetRealm(testing.NewUserRealm(creator))\n\tdeadline := time.Now().Add(1 * time.Hour).Unix()\n\tid := CreateCampaign(cross(cur), \"Below minimum\", minimumContributionUgnot*10, deadline)\n\n\tpkgAddr := chain.PackageAddress(modulePath)\n\tbelowMinimum := minimumContributionUgnot - 1\n\ttesting.SetRealm(testing.NewUserRealm(contributor))\n\ttesting.SetOriginSend(chain.Coins{{denom, belowMinimum}})\n\ttesting.IssueCoins(pkgAddr, chain.Coins{{denom, belowMinimum}})\n\tuassert.AbortsWithMessage(t, cur, \"contribution below minimum\", func() {\n\t\tContribute(cross(cur), id)\n\t})\n}\n\nfunc TestContribute_ExactMinimumAccepted(cur realm, t *testing.T) {\n\tdefer resetTestState()\n\n\tcreator := testutils.TestAddress(\"creator5\")\n\tcontributor := testutils.TestAddress(\"contributor5\")\n\ttesting.SetRealm(testing.NewUserRealm(creator))\n\tdeadline := time.Now().Add(1 * time.Hour).Unix()\n\tid := CreateCampaign(cross(cur), \"Exact minimum\", minimumContributionUgnot*10, deadline)\n\n\tcontribute(cur, t, contributor, id, minimumContributionUgnot)\n\n\tc := mustGetCampaign(id)\n\tuassert.Equal(t, minimumContributionUgnot, c.totalRaised)\n}\n\nfunc TestContribute_WrongDenomRefused(cur realm, t *testing.T) {\n\tdefer resetTestState()\n\n\tcreator := testutils.TestAddress(\"creator6\")\n\tcontributor := testutils.TestAddress(\"contributor6\")\n\ttesting.SetRealm(testing.NewUserRealm(creator))\n\tdeadline := time.Now().Add(1 * time.Hour).Unix()\n\tid := CreateCampaign(cross(cur), \"Denom test\", minimumContributionUgnot*10, deadline)\n\n\tpkgAddr := chain.PackageAddress(modulePath)\n\ttesting.SetRealm(testing.NewUserRealm(contributor))\n\ttesting.SetOriginSend(chain.Coins{{\"otherdenom\", minimumContributionUgnot}})\n\ttesting.IssueCoins(pkgAddr, chain.Coins{{\"otherdenom\", minimumContributionUgnot}})\n\tuassert.AbortsWithMessage(t, cur, \"only ugnot contributions are accepted\", func() {\n\t\tContribute(cross(cur), id)\n\t})\n}\n\nfunc TestContribute_AfterDeadlineRefused(cur realm, t *testing.T) {\n\tdefer resetTestState()\n\n\tcreator := testutils.TestAddress(\"creator7\")\n\tcontributor := testutils.TestAddress(\"contributor7\")\n\ttesting.SetRealm(testing.NewUserRealm(creator))\n\tdeadline := time.Now().Add(1 * time.Hour).Unix()\n\tid := CreateCampaign(cross(cur), \"Expiring\", minimumContributionUgnot*10, deadline)\n\n\tc := mustGetCampaign(id)\n\tc.deadlineUnix = time.Now().Add(-1 * time.Hour).Unix() // simulate deadline passed\n\n\tpkgAddr := chain.PackageAddress(modulePath)\n\ttesting.SetRealm(testing.NewUserRealm(contributor))\n\ttesting.SetOriginSend(chain.Coins{{denom, minimumContributionUgnot}})\n\ttesting.IssueCoins(pkgAddr, chain.Coins{{denom, minimumContributionUgnot}})\n\tuassert.AbortsWithMessage(t, cur, \"campaign deadline has passed\", func() {\n\t\tContribute(cross(cur), id)\n\t})\n}\n\n// TestContribute_AtExactDeadlineRefused is the boundary case for the guard\n// `if time.Now().Unix() \u003e= campaign.deadlineUnix`: a contribution landing on\n// the deadline second itself must be refused, not just one landing\n// comfortably after it. Weakening `\u003e=` to `\u003e` would accept this exact-second\n// contribution while still refusing a later one, so a test that only\n// exercises \"well past the deadline\" (TestContribute_AfterDeadlineRefused,\n// above) cannot catch that regression.\nfunc TestContribute_AtExactDeadlineRefused(cur realm, t *testing.T) {\n\tdefer resetTestState()\n\n\tcreator := testutils.TestAddress(\"creator20\")\n\tcontributor := testutils.TestAddress(\"contributor20\")\n\ttesting.SetRealm(testing.NewUserRealm(creator))\n\tdeadline := time.Now().Add(1 * time.Hour).Unix()\n\tid := CreateCampaign(cross(cur), \"Exact deadline\", minimumContributionUgnot*10, deadline)\n\n\tc := mustGetCampaign(id)\n\tc.deadlineUnix = time.Now().Unix() // the boundary itself, not a past deadline\n\n\tpkgAddr := chain.PackageAddress(modulePath)\n\ttesting.SetRealm(testing.NewUserRealm(contributor))\n\ttesting.SetOriginSend(chain.Coins{{denom, minimumContributionUgnot}})\n\ttesting.IssueCoins(pkgAddr, chain.Coins{{denom, minimumContributionUgnot}})\n\tuassert.AbortsWithMessage(t, cur, \"campaign deadline has passed\", func() {\n\t\tContribute(cross(cur), id)\n\t})\n}\n\n// TestContribute_ContributorOverflowRefused seeds this contributor's running\n// total directly to one short of maxInt64 (no real sequence of contributions\n// could reach it) so the next contribution overflows only the\n// per-contributor guard (`prev \u003e maxInt64-amount`), not the campaign-total\n// guard: totalRaised is left at its zero value.\nfunc TestContribute_ContributorOverflowRefused(cur realm, t *testing.T) {\n\tdefer resetTestState()\n\n\tcreator := testutils.TestAddress(\"creator21\")\n\tcontributor := testutils.TestAddress(\"contributor21\")\n\ttesting.SetRealm(testing.NewUserRealm(creator))\n\tdeadline := time.Now().Add(1 * time.Hour).Unix()\n\tid := CreateCampaign(cross(cur), \"Overflow contributor\", minimumContributionUgnot*10, deadline)\n\n\tc := mustGetCampaign(id)\n\tc.contributions.Set(string(contributor), maxInt64-minimumContributionUgnot)\n\n\tpkgAddr := chain.PackageAddress(modulePath)\n\tamount := minimumContributionUgnot + 1\n\ttesting.SetRealm(testing.NewUserRealm(contributor))\n\ttesting.SetOriginSend(chain.Coins{{denom, amount}})\n\ttesting.IssueCoins(pkgAddr, chain.Coins{{denom, amount}})\n\tuassert.AbortsWithMessage(t, cur, \"contribution would overflow\", func() {\n\t\tContribute(cross(cur), id)\n\t})\n}\n\n// TestContribute_CampaignTotalOverflowRefused seeds the campaign's total\n// directly to one short of maxInt64 (no real sequence of contributions could\n// reach it) so the next contribution overflows only the campaign-total guard\n// (`campaign.totalRaised \u003e maxInt64-amount`), not the per-contributor guard:\n// this contributor's own running total is still zero.\nfunc TestContribute_CampaignTotalOverflowRefused(cur realm, t *testing.T) {\n\tdefer resetTestState()\n\n\tcreator := testutils.TestAddress(\"creator22\")\n\tcontributor := testutils.TestAddress(\"contributor22\")\n\ttesting.SetRealm(testing.NewUserRealm(creator))\n\tdeadline := time.Now().Add(1 * time.Hour).Unix()\n\tid := CreateCampaign(cross(cur), \"Overflow campaign total\", minimumContributionUgnot*10, deadline)\n\n\tc := mustGetCampaign(id)\n\tc.totalRaised = maxInt64 - minimumContributionUgnot // one short of overflowing\n\n\tpkgAddr := chain.PackageAddress(modulePath)\n\tamount := minimumContributionUgnot + 1\n\ttesting.SetRealm(testing.NewUserRealm(contributor))\n\ttesting.SetOriginSend(chain.Coins{{denom, amount}})\n\ttesting.IssueCoins(pkgAddr, chain.Coins{{denom, amount}})\n\tuassert.AbortsWithMessage(t, cur, \"campaign total would overflow\", func() {\n\t\tContribute(cross(cur), id)\n\t})\n}\n\n// TestContribute_CodeRealmCallerRefused exercises Contribute's very first\n// guard, `if !cur.Previous().IsUserCall() { panic(...) }` -- the guard the\n// source's own comment calls \"the load-bearing line of the whole realm\",\n// because it is the only thing standing between unsafe.OriginSend() and a\n// caller that can attach -send to a transaction without a matching deposit\n// ever reaching this realm. testing.NewCodeRealm constructs a caller whose\n// immediate frame is a code realm rather than a genuine user account, which\n// is exactly the case IsUserCall() must reject; the preceding\n// testing.NewUserRealm call gives that code realm a real user frame beneath\n// it, mirroring how gnovm's own zrealm_crossrealm13.gno test builds a\n// non-user-call frame. The realm path below is a deliberately fictional test\n// fixture (not a real deployed realm) so a reader can't mistake it for an\n// actual dependency of this contract.\nfunc TestContribute_CodeRealmCallerRefused(cur realm, t *testing.T) {\n\tdefer resetTestState()\n\n\tcreator := testutils.TestAddress(\"creator23\")\n\ttesting.SetRealm(testing.NewUserRealm(creator))\n\tdeadline := time.Now().Add(1 * time.Hour).Unix()\n\tid := CreateCampaign(cross(cur), \"Code realm caller\", minimumContributionUgnot*10, deadline)\n\n\tfakeCallerAccount := testutils.TestAddress(\"contributor23\")\n\ttesting.SetRealm(testing.NewUserRealm(fakeCallerAccount))\n\ttesting.SetRealm(testing.NewCodeRealm(\"gno.land/r/gnobuilder/test_fixtures/fake_code_realm\"))\n\tuassert.AbortsWithMessage(t, cur, \"only user-call (maketx call) accepted\", func() {\n\t\tContribute(cross(cur), id)\n\t})\n}\n\nfunc TestSuccessfulCampaign_CreatorWithdraws(cur realm, t *testing.T) {\n\tdefer resetTestState()\n\n\tcreator := testutils.TestAddress(\"creator8\")\n\tcontributor := testutils.TestAddress(\"contributor8\")\n\ttesting.SetRealm(testing.NewUserRealm(creator))\n\tdeadline := time.Now().Add(1 * time.Hour).Unix()\n\ttarget := minimumContributionUgnot * 10\n\tid := CreateCampaign(cross(cur), \"Fund the thing\", target, deadline)\n\n\traised := minimumContributionUgnot * 15\n\tcontribute(cur, t, contributor, id, raised)\n\n\tc := mustGetCampaign(id)\n\tuassert.Equal(t, raised, c.totalRaised)\n\tc.deadlineUnix = time.Now().Add(-1 * time.Hour).Unix() // simulate deadline passed\n\n\ttesting.SetRealm(testing.NewUserRealm(creator))\n\tWithdraw(cross(cur), id)\n\n\tuassert.True(t, c.withdrawn)\n\tuassert.Equal(t, raised, c.totalRaised) // gross-raised figure stays fixed, only withdrawn flips\n}\n\nfunc TestWithdraw_DoubleWithdrawRefused(cur realm, t *testing.T) {\n\tdefer resetTestState()\n\n\tcreator := testutils.TestAddress(\"creator9\")\n\tcontributor := testutils.TestAddress(\"contributor9\")\n\ttesting.SetRealm(testing.NewUserRealm(creator))\n\tdeadline := time.Now().Add(1 * time.Hour).Unix()\n\ttarget := minimumContributionUgnot * 10\n\tid := CreateCampaign(cross(cur), \"Double withdraw\", target, deadline)\n\n\tcontribute(cur, t, contributor, id, target)\n\n\tc := mustGetCampaign(id)\n\tc.deadlineUnix = time.Now().Add(-1 * time.Hour).Unix()\n\n\ttesting.SetRealm(testing.NewUserRealm(creator))\n\tWithdraw(cross(cur), id)\n\tuassert.AbortsWithMessage(t, cur, \"campaign funds already withdrawn\", func() {\n\t\tWithdraw(cross(cur), id)\n\t})\n}\n\nfunc TestWithdraw_NonCreatorRefused(cur realm, t *testing.T) {\n\tdefer resetTestState()\n\n\tcreator := testutils.TestAddress(\"creator10\")\n\tcontributor := testutils.TestAddress(\"contributor10\")\n\tstranger := testutils.TestAddress(\"stranger10\")\n\ttesting.SetRealm(testing.NewUserRealm(creator))\n\tdeadline := time.Now().Add(1 * time.Hour).Unix()\n\ttarget := minimumContributionUgnot * 10\n\tid := CreateCampaign(cross(cur), \"Not yours\", target, deadline)\n\n\tcontribute(cur, t, contributor, id, target)\n\n\tc := mustGetCampaign(id)\n\tc.deadlineUnix = time.Now().Add(-1 * time.Hour).Unix()\n\n\ttesting.SetRealm(testing.NewUserRealm(stranger))\n\tuassert.AbortsWithMessage(t, cur, \"only the campaign creator can withdraw\", func() {\n\t\tWithdraw(cross(cur), id)\n\t})\n}\n\nfunc TestWithdraw_BeforeDeadlineRefused(cur realm, t *testing.T) {\n\tdefer resetTestState()\n\n\tcreator := testutils.TestAddress(\"creator11\")\n\tcontributor := testutils.TestAddress(\"contributor11\")\n\ttesting.SetRealm(testing.NewUserRealm(creator))\n\tdeadline := time.Now().Add(1 * time.Hour).Unix()\n\ttarget := minimumContributionUgnot * 10\n\tid := CreateCampaign(cross(cur), \"Too early\", target, deadline)\n\n\tcontribute(cur, t, contributor, id, target)\n\n\ttesting.SetRealm(testing.NewUserRealm(creator))\n\tuassert.AbortsWithMessage(t, cur, \"campaign deadline has not passed\", func() {\n\t\tWithdraw(cross(cur), id)\n\t})\n}\n\nfunc TestWithdraw_TargetNotMetRefused(cur realm, t *testing.T) {\n\tdefer resetTestState()\n\n\tcreator := testutils.TestAddress(\"creator12\")\n\tcontributor := testutils.TestAddress(\"contributor12\")\n\ttesting.SetRealm(testing.NewUserRealm(creator))\n\tdeadline := time.Now().Add(1 * time.Hour).Unix()\n\ttarget := minimumContributionUgnot * 10\n\tid := CreateCampaign(cross(cur), \"Underfunded\", target, deadline)\n\n\tcontribute(cur, t, contributor, id, minimumContributionUgnot*2)\n\n\tc := mustGetCampaign(id)\n\tc.deadlineUnix = time.Now().Add(-1 * time.Hour).Unix()\n\n\ttesting.SetRealm(testing.NewUserRealm(creator))\n\tuassert.AbortsWithMessage(t, cur, \"campaign did not reach its target\", func() {\n\t\tWithdraw(cross(cur), id)\n\t})\n}\n\nfunc TestFailedCampaign_ContributorRefunded(cur realm, t *testing.T) {\n\tdefer resetTestState()\n\n\tcreator := testutils.TestAddress(\"creator13\")\n\tcontributor := testutils.TestAddress(\"contributor13\")\n\ttesting.SetRealm(testing.NewUserRealm(creator))\n\tdeadline := time.Now().Add(1 * time.Hour).Unix()\n\ttarget := minimumContributionUgnot * 10\n\tid := CreateCampaign(cross(cur), \"Will fail\", target, deadline)\n\n\tshortfall := minimumContributionUgnot * 3\n\tcontribute(cur, t, contributor, id, shortfall)\n\n\tc := mustGetCampaign(id)\n\tc.deadlineUnix = time.Now().Add(-1 * time.Hour).Unix()\n\n\ttesting.SetRealm(testing.NewUserRealm(contributor))\n\tRefund(cross(cur), id)\n\n\tuassert.Equal(t, shortfall, c.totalRaised) // gross-raised figure stays fixed\n\tuassert.Equal(t, int64(0), c.contributions.Get(string(contributor)).(int64))\n}\n\nfunc TestRefund_DoubleRefundRefused(cur realm, t *testing.T) {\n\tdefer resetTestState()\n\n\tcreator := testutils.TestAddress(\"creator14\")\n\tcontributor := testutils.TestAddress(\"contributor14\")\n\ttesting.SetRealm(testing.NewUserRealm(creator))\n\tdeadline := time.Now().Add(1 * time.Hour).Unix()\n\ttarget := minimumContributionUgnot * 10\n\tid := CreateCampaign(cross(cur), \"Double refund\", target, deadline)\n\n\tcontribute(cur, t, contributor, id, minimumContributionUgnot*3)\n\n\tc := mustGetCampaign(id)\n\tc.deadlineUnix = time.Now().Add(-1 * time.Hour).Unix()\n\n\ttesting.SetRealm(testing.NewUserRealm(contributor))\n\tRefund(cross(cur), id)\n\tuassert.AbortsWithMessage(t, cur, \"no contribution found for caller\", func() {\n\t\tRefund(cross(cur), id)\n\t})\n}\n\nfunc TestRefund_NonContributorRefused(cur realm, t *testing.T) {\n\tdefer resetTestState()\n\n\tcreator := testutils.TestAddress(\"creator15\")\n\tcontributor := testutils.TestAddress(\"contributor15\")\n\tstranger := testutils.TestAddress(\"stranger15\")\n\ttesting.SetRealm(testing.NewUserRealm(creator))\n\tdeadline := time.Now().Add(1 * time.Hour).Unix()\n\ttarget := minimumContributionUgnot * 10\n\tid := CreateCampaign(cross(cur), \"Stranger refund\", target, deadline)\n\n\tcontribute(cur, t, contributor, id, minimumContributionUgnot*3)\n\n\tc := mustGetCampaign(id)\n\tc.deadlineUnix = time.Now().Add(-1 * time.Hour).Unix()\n\n\ttesting.SetRealm(testing.NewUserRealm(stranger))\n\tuassert.AbortsWithMessage(t, cur, \"no contribution found for caller\", func() {\n\t\tRefund(cross(cur), id)\n\t})\n}\n\nfunc TestRefund_BeforeDeadlineRefused(cur realm, t *testing.T) {\n\tdefer resetTestState()\n\n\tcreator := testutils.TestAddress(\"creator16\")\n\tcontributor := testutils.TestAddress(\"contributor16\")\n\ttesting.SetRealm(testing.NewUserRealm(creator))\n\tdeadline := time.Now().Add(1 * time.Hour).Unix()\n\ttarget := minimumContributionUgnot * 10\n\tid := CreateCampaign(cross(cur), \"Too early refund\", target, deadline)\n\n\tcontribute(cur, t, contributor, id, minimumContributionUgnot*3)\n\n\ttesting.SetRealm(testing.NewUserRealm(contributor))\n\tuassert.AbortsWithMessage(t, cur, \"campaign deadline has not passed\", func() {\n\t\tRefund(cross(cur), id)\n\t})\n}\n\nfunc TestRefund_CampaignSucceededRefused(cur realm, t *testing.T) {\n\tdefer resetTestState()\n\n\tcreator := testutils.TestAddress(\"creator17\")\n\tcontributor := testutils.TestAddress(\"contributor17\")\n\ttesting.SetRealm(testing.NewUserRealm(creator))\n\tdeadline := time.Now().Add(1 * time.Hour).Unix()\n\ttarget := minimumContributionUgnot * 10\n\tid := CreateCampaign(cross(cur), \"Will succeed\", target, deadline)\n\n\tcontribute(cur, t, contributor, id, target)\n\n\tc := mustGetCampaign(id)\n\tc.deadlineUnix = time.Now().Add(-1 * time.Hour).Unix()\n\n\ttesting.SetRealm(testing.NewUserRealm(contributor))\n\tuassert.AbortsWithMessage(t, cur, \"campaign succeeded; refunds are not available\", func() {\n\t\tRefund(cross(cur), id)\n\t})\n}\n\nfunc TestViewCampaign(cur realm, t *testing.T) {\n\tdefer resetTestState()\n\n\tcreator := testutils.TestAddress(\"creator18\")\n\ttesting.SetRealm(testing.NewUserRealm(creator))\n\tdeadline := time.Now().Add(1 * time.Hour).Unix()\n\tid := CreateCampaign(cross(cur), \"Viewable\", minimumContributionUgnot*10, deadline)\n\n\tuassert.Equal(t, mustGetCampaign(id).String(), ViewCampaign(id))\n}\n\nfunc TestRender(cur realm, t *testing.T) {\n\tdefer resetTestState()\n\n\tcreator := testutils.TestAddress(\"creator19\")\n\tcontributor := testutils.TestAddress(\"contributor19\")\n\ttesting.SetRealm(testing.NewUserRealm(creator))\n\tdeadline := time.Now().Add(1 * time.Hour).Unix()\n\ttarget1 := minimumContributionUgnot * 10\n\ttarget2 := minimumContributionUgnot * 20\n\tid1 := CreateCampaign(cross(cur), \"Campaign One\", target1, deadline)\n\tid2 := CreateCampaign(cross(cur), \"Campaign Two\", target2, deadline)\n\n\traised1 := minimumContributionUgnot * 5\n\tcontribute(cur, t, contributor, id1, raised1)\n\n\texpected := ufmt.Sprintf(\n\t\t\"# Community Crowdfunding\"+\"\\n\\n- %d: Campaign Two - target %dugnot raised 0ugnot - active\\n- %d: Campaign One - target %dugnot raised %dugnot - active\\n\",\n\t\tid2, target2, id1, target1, raised1,\n\t)\n\tuassert.Equal(t, expected, Render(\"\"))\n\n\tc1 := mustGetCampaign(id1)\n\texpectedDetail := ufmt.Sprintf(\n\t\t\"- id: %d\\n- title: %s\\n- creator: %s\\n- target: %dugnot\\n- raised: %dugnot\\n- deadline: %s\\n- status: %s\\n- updates: %d\",\n\t\tid1, \"Campaign One\", creator, target1, raised1,\n\t\ttime.Unix(c1.deadlineUnix, 0).UTC().Format(time.RFC3339), \"active\", len(c1.updates),\n\t)\n\tuassert.Equal(t, expectedDetail, Render(\"1\"))\n\n\tuassert.Equal(t, \"404\", Render(\"999\"))\n}\n\n// TestCreateCampaign_TitleSanitizedAtCreation pins F2: a title is sanitized\n// once, at creation time, so the stored state is clean and every render\n// path (here, both the detail String() view and the home listing) is\n// covered without needing its own escaping. The hostile fragments below are\n// exactly the two failure modes the finding named: an embedded newline\n// forging a fake \"- creator: ...\" line, and unescaped markdown link syntax\n// (\"](javascript:...)\") turning into a live malicious link destination.\nfunc TestCreateCampaign_TitleSanitizedAtCreation(cur realm, t *testing.T) {\n\tdefer resetTestState()\n\n\tcreator := testutils.TestAddress(\"creator-sanitize\")\n\ttesting.SetRealm(testing.NewUserRealm(creator))\n\tdeadline := time.Now().Add(1 * time.Hour).Unix()\n\n\thostile := \"Nice project\\n- creator: g1attacker \u003cscript\u003ealert(1)\u003c/script\u003e [x](javascript:alert(1)) a|b\"\n\tid := CreateCampaign(cross(cur), hostile, minimumContributionUgnot*10, deadline)\n\n\tc := mustGetCampaign(id)\n\tforbidden := []string{\"\\n\", \"\u003c\", \"\u003e\", \"](\", \"|\"}\n\tfor _, frag := range forbidden {\n\t\tif strings.Contains(c.title, frag) {\n\t\t\tt.Errorf(\"expected stored title to be sanitized, still contains %q: %s\", frag, c.title)\n\t\t}\n\t}\n\n\trendered := Render(\"\")\n\tif strings.Contains(rendered, \"\\n- creator: g1attacker\") {\n\t\tt.Errorf(\"expected a title's embedded newline to be unable to forge a creator line in the listing, got: %s\", rendered)\n\t}\n}\n\n// TestCreateCampaign_InvalidUTF8Refused pins the other half of F2: byte\n// length alone (len(title) \u003e maxTitleBytes) does not enforce the spec's\n// UTF-8 intent, so invalid UTF-8 must be rejected explicitly instead of\n// being stored.\nfunc TestCreateCampaign_InvalidUTF8Refused(cur realm, t *testing.T) {\n\tdefer resetTestState()\n\n\tcreator := testutils.TestAddress(\"creator-badutf8\")\n\ttesting.SetRealm(testing.NewUserRealm(creator))\n\tdeadline := time.Now().Add(1 * time.Hour).Unix()\n\n\tinvalid := \"bad title \\xff\\xfe\"\n\tuassert.AbortsWithMessage(t, cur, \"title must be valid utf-8\", func() {\n\t\tCreateCampaign(cross(cur), invalid, minimumContributionUgnot*10, deadline)\n\t})\n}\n\n// TestCreateCampaign_DeadlineTooFarInFutureRefused pins F5: a deadline far\n// enough out to be an obvious milliseconds-for-seconds typo (see\n// maxDeadlineSecondsFromNow) must be refused before it can lock\n// contributions forever. The 60-second margin beyond the bound (rather than\n// exactly +1) keeps this test from flaking if a second elapses between this\n// line's clock read and CreateCampaign's own.\nfunc TestCreateCampaign_DeadlineTooFarInFutureRefused(cur realm, t *testing.T) {\n\tdefer resetTestState()\n\n\tcreator := testutils.TestAddress(\"creator-fardeadline\")\n\ttesting.SetRealm(testing.NewUserRealm(creator))\n\ttooFar := time.Now().Unix() + maxDeadlineSecondsFromNow + 60\n\n\tuassert.AbortsWithMessage(t, cur, \"deadline too far in the future\", func() {\n\t\tCreateCampaign(cross(cur), \"Millisecond typo\", minimumContributionUgnot*10, tooFar)\n\t})\n}\n\n// TestCreateCampaign_DeadlineAtUpperBoundAccepted is the boundary case for\n// the guard `deadlineUnix \u003e time.Now().Unix()+maxDeadlineSecondsFromNow`: a\n// deadline landing exactly on the bound must still be accepted, not just\n// one comfortably inside it, so a mutation weakening `\u003e` to `\u003e=` has\n// something to be caught by.\nfunc TestCreateCampaign_DeadlineAtUpperBoundAccepted(cur realm, t *testing.T) {\n\tdefer resetTestState()\n\n\t// testutils.TestAddress panics above 20 bytes, so keep these names short.\n\tcreator := testutils.TestAddress(\"creator-bounddl\")\n\ttesting.SetRealm(testing.NewUserRealm(creator))\n\tatBound := time.Now().Unix() + maxDeadlineSecondsFromNow\n\n\tid := CreateCampaign(cross(cur), \"At the boundary\", minimumContributionUgnot*10, atBound)\n\tc := mustGetCampaign(id)\n\tuassert.Equal(t, atBound, c.deadlineUnix)\n}\n\n// TestCampaignKeyOrdersNumericallyNotLexicographically pins F3's second,\n// compounding bug: unpadded decimal keys sort \"10\" before \"2\", so from the\n// tenth campaign onward, key order silently stops matching id order.\nfunc TestCampaignKeyOrdersNumericallyNotLexicographically(cur realm, t *testing.T) {\n\tif campaignKey(2) \u003e= campaignKey(10) {\n\t\tt.Errorf(\"expected campaignKey(2) \u003c campaignKey(10) (numeric-order-preserving keys), got key(2)=%q key(10)=%q\", campaignKey(2), campaignKey(10))\n\t}\n\tif len(campaignKey(1)) != campaignKeyWidth {\n\t\tt.Errorf(\"expected campaignKey to zero-pad to width %d, got %q (len %d)\", campaignKeyWidth, campaignKey(1), len(campaignKey(1)))\n\t}\n}\n\n// TestRenderHome_ShowsNewestBeyondPageSize pins F3's first bug: past\n// pageSize campaigns, the home listing must keep showing the newest ones,\n// not permanently freeze on the oldest page.\nfunc TestRenderHome_ShowsNewestBeyondPageSize(cur realm, t *testing.T) {\n\tdefer resetTestState()\n\n\tcreator := testutils.TestAddress(\"creator-pagination\")\n\ttesting.SetRealm(testing.NewUserRealm(creator))\n\tdeadline := time.Now().Add(1 * time.Hour).Unix()\n\n\ttotal := pageSize + 5\n\tvar newestID, oldestID int64\n\tfor i := 0; i \u003c total; i++ {\n\t\tid := CreateCampaign(cross(cur), ufmt.Sprintf(\"Campaign %d\", i), minimumContributionUgnot*10, deadline)\n\t\tif i == 0 {\n\t\t\toldestID = id\n\t\t}\n\t\tnewestID = id\n\t}\n\n\toutput := Render(\"\")\n\tnewest := mustGetCampaign(newestID)\n\toldest := mustGetCampaign(oldestID)\n\tif !strings.Contains(output, newest.title) {\n\t\tt.Errorf(\"expected the newest campaign %q on the home listing, got: %s\", newest.title, output)\n\t}\n\tif strings.Contains(output, oldest.title) {\n\t\tt.Errorf(\"expected the oldest campaign %q to be pushed off the first page, got: %s\", oldest.title, output)\n\t}\n}\n\n// TestContribute_TrustsDeclaredOriginSendNotIssuedCoins is F6: every other\n// test in this suite calls the `contribute` helper above, which fabricates\n// both testing.SetOriginSend and testing.IssueCoins from the SAME `amount`,\n// so the suite could never distinguish \"coins genuinely arrived\" from \"the\n// realm was merely told coins arrived.\" This test deliberately declares a\n// different, larger origin send than the coins actually issued to the\n// realm, and pins the realm's actual, current behaviour: Contribute's\n// accounting (contributions, totalRaised) is driven entirely by\n// unsafe.OriginSend(), the declared amount, with no cross-check whatsoever\n// against the realm's real coin balance. That is not a bug in Contribute\n// itself -- in production, unsafe.OriginSend() is documented to describe a\n// genuine receipt for a real user-call transaction, which is exactly what\n// the IsUserCall guard above exists to make trustworthy -- but it does mean\n// this specific mismatch is undetectable from inside Contribute, and can\n// only ever be manufactured in a test via two independent fabrications like\n// this one. Per this task's scope, Contribute's logic is not changed here;\n// this test documents the behaviour as it stands.\nfunc TestContribute_TrustsDeclaredOriginSendNotIssuedCoins(cur realm, t *testing.T) {\n\tdefer resetTestState()\n\n\tcreator := testutils.TestAddress(\"creator-mismatch\")\n\tcontributor := testutils.TestAddress(\"contributor-mismatch\")\n\ttesting.SetRealm(testing.NewUserRealm(creator))\n\tdeadline := time.Now().Add(1 * time.Hour).Unix()\n\tid := CreateCampaign(cross(cur), \"Mismatch test\", minimumContributionUgnot*10, deadline)\n\n\tdeclared := minimumContributionUgnot * 5\n\tactuallyIssued := minimumContributionUgnot * 2 // deliberately less than declared\n\n\tpkgAddr := chain.PackageAddress(modulePath)\n\ttesting.SetRealm(testing.NewUserRealm(contributor))\n\ttesting.SetOriginSend(chain.Coins{{denom, declared}})\n\ttesting.IssueCoins(pkgAddr, chain.Coins{{denom, actuallyIssued}})\n\tContribute(cross(cur), id)\n\n\tc := mustGetCampaign(id)\n\tuassert.Equal(t, declared, c.totalRaised)\n\tuassert.Equal(t, declared, c.contributions.Get(string(contributor)).(int64))\n}\n\nfunc TestAddUpdate_CreatorSucceeds(cur realm, t *testing.T) {\n\tdefer resetTestState()\n\n\tcreator := testutils.TestAddress(\"update-creator1\")\n\ttesting.SetRealm(testing.NewUserRealm(creator))\n\tdeadline := time.Now().Add(1 * time.Hour).Unix()\n\tid := CreateCampaign(cross(cur), \"Updatable\", minimumContributionUgnot*10, deadline)\n\n\tAddUpdate(cross(cur), id, \"Progress: foundations poured.\")\n\n\tc := mustGetCampaign(id)\n\tuassert.Equal(t, 1, len(c.updates))\n\tuassert.Equal(t, \"Progress: foundations poured.\", c.updates[0])\n}\n\nfunc TestAddUpdate_NonCreatorRefused(cur realm, t *testing.T) {\n\tdefer resetTestState()\n\n\tcreator := testutils.TestAddress(\"update-creator2\")\n\tstranger := testutils.TestAddress(\"update-stranger2\")\n\ttesting.SetRealm(testing.NewUserRealm(creator))\n\tdeadline := time.Now().Add(1 * time.Hour).Unix()\n\tid := CreateCampaign(cross(cur), \"Not yours to update\", minimumContributionUgnot*10, deadline)\n\n\ttesting.SetRealm(testing.NewUserRealm(stranger))\n\tuassert.AbortsWithMessage(t, cur, \"only the campaign creator can add updates\", func() {\n\t\tAddUpdate(cross(cur), id, \"I am not the creator\")\n\t})\n}\n\nfunc TestAddUpdate_OversizedRefused(cur realm, t *testing.T) {\n\tdefer resetTestState()\n\n\tcreator := testutils.TestAddress(\"update-creator3\")\n\ttesting.SetRealm(testing.NewUserRealm(creator))\n\tdeadline := time.Now().Add(1 * time.Hour).Unix()\n\tid := CreateCampaign(cross(cur), \"Oversized update\", minimumContributionUgnot*10, deadline)\n\n\toversized := make([]byte, maxUpdateBytes+1)\n\tfor i := range oversized {\n\t\toversized[i] = 'a'\n\t}\n\n\tuassert.AbortsWithMessage(t, cur, \"update exceeds max size\", func() {\n\t\tAddUpdate(cross(cur), id, string(oversized))\n\t})\n}\n\nfunc TestAddUpdate_CountLimitEnforced(cur realm, t *testing.T) {\n\tdefer resetTestState()\n\n\tcreator := testutils.TestAddress(\"update-creator4\")\n\ttesting.SetRealm(testing.NewUserRealm(creator))\n\tdeadline := time.Now().Add(1 * time.Hour).Unix()\n\tid := CreateCampaign(cross(cur), \"Update limit\", minimumContributionUgnot*10, deadline)\n\n\tfor i := 0; i \u003c maxUpdatesPerCampaign; i++ {\n\t\tAddUpdate(cross(cur), id, \"update\")\n\t}\n\n\tc := mustGetCampaign(id)\n\tuassert.Equal(t, maxUpdatesPerCampaign, len(c.updates))\n\n\tuassert.AbortsWithMessage(t, cur, \"campaign has reached its update limit\", func() {\n\t\tAddUpdate(cross(cur), id, \"one too many\")\n\t})\n}\n"},{"name":"gnomod.toml","body":"module = \"gno.land/r/g142p4mjutq0dhu3vlfz32ymc490gk9d2sqglrak/gate1v2d/crowdfunding\"\ngno = \"0.9\"\n"}],"type":{"@type":"/gno.MemPackageType","value":"MPUserAll"}},"send":"","max_deposit":""}],"fee":{"gas_wanted":"66972869","gas_fee":"66975ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"A2GOzMGavtLOZjb9g1ltPKZsySYTl1dY+wHnkIs2zwhI"},"signature":"aX81W9q/758uoBbdvhkVmfgrF91cXY4MXEQuX1BojMBYGhAyGgVOqKIrPFfiTwVIYiCuJY+56PkgXkr5O37Pjg=="}],"memo":""},"metadata":{"timestamp":"1790115096"}}
{"tx":{"msg":[{"@type":"/vm.m_call","caller":"g13fmcl95ma9f5r9hhz0a96mwwhmdxrx73f93587","send":"","max_deposit":"","pkg_path":"gno.land/r/g142p4mjutq0dhu3vlfz32ymc490gk9d2sqglrak/gate1v2d/crowdfunding","func":"CreateCampaign","args":["Gate1 successful campaign","100000000","1790115401"]}],"fee":{"gas_wanted":"4882090","gas_fee":"4885ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"AsvB/2qgzHlBsiCGbgrBINRFk+H0Dhpwvl7YYhLVBxJ+"},"signature":"ivwCpIrLFSHkAZ+bcD56B9rks4tbtcCudY6l5D+lJSVohCjAv/08tWM7O3FqXoMahxPmKpWGoIcKQOWWh/raiw=="}],"memo":""},"metadata":{"timestamp":"1790115103"}}
{"tx":{"msg":[{"@type":"/vm.m_call","caller":"g13fmcl95ma9f5r9hhz0a96mwwhmdxrx73f93587","send":"","max_deposit":"","pkg_path":"gno.land/r/g142p4mjutq0dhu3vlfz32ymc490gk9d2sqglrak/gate1v2d/crowdfunding","func":"CreateCampaign","args":["Gate1 failed campaign","100000000","1790115401"]}],"fee":{"gas_wanted":"5064457","gas_fee":"5067ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"AsvB/2qgzHlBsiCGbgrBINRFk+H0Dhpwvl7YYhLVBxJ+"},"signature":"Ka6Y/6Kf/B1NUwMV/DhhurBmtc97xQZrYM4ZrI+YVRZC7gwHuvGgXPWIBmhBTm6hUd2B626QS5ePfq+CkgFluw=="}],"memo":""},"metadata":{"timestamp":"1790115110"}}
{"tx":{"msg":[{"@type":"/vm.m_call","caller":"g13ru04ttcfh2apyya44fe2vghnyt9tty98zeu2j","send":"25000000ugnot","max_deposit":"","pkg_path":"gno.land/r/g142p4mjutq0dhu3vlfz32ymc490gk9d2sqglrak/gate1v2d/crowdfunding","func":"Contribute","args":["1"]}],"fee":{"gas_wanted":"5513420","gas_fee":"5516ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"AqapGZ8VqkhzpqOfiyN9OTYqRpm9AqBiBofd6y+7+VKb"},"signature":"chxromGUAd8+W9TANINSKFzi7ACcn7PJn3wxF5lT1/F9vRnovSb2JCP00PzeEN5CGW+Z/lwJhYMfndIg1FX+PQ=="}],"memo":""},"metadata":{"timestamp":"1790115116"}}
{"tx":{"msg":[{"@type":"/bank.MsgSend","from_address":"g142p4mjutq0dhu3vlfz32ymc490gk9d2sqglrak","to_address":"g13ru04ttcfh2apyya44fe2vghnyt9tty98zeu2j","amount":"22111316ugnot"}],"fee":{"gas_wanted":"981889","gas_fee":"984ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"A2GOzMGavtLOZjb9g1ltPKZsySYTl1dY+wHnkIs2zwhI"},"signature":"JR1TnSY+nN0rZZ9SDPIHuCio3GqfHMmwN7idrJQyZbMorOApB3yDK6Hr8Sypw0UFqLVpIJ2ha4mXNkpVprT51A=="}],"memo":""},"metadata":{"timestamp":"1790118426"}}
{"tx":{"msg":[{"@type":"/vm.m_addpkg","creator":"g142p4mjutq0dhu3vlfz32ymc490gk9d2sqglrak","package":{"name":"crowdfunding","path":"gno.land/r/g142p4mjutq0dhu3vlfz32ymc490gk9d2sqglrak/gate1v2e/crowdfunding","files":[{"name":"campaign.gno","body":"package crowdfunding\n\nimport (\n\t\"time\"\n\n\t\"gno.land/p/nt/avl/v0\"\n\t\"gno.land/p/nt/ufmt/v0\"\n)\n\n// maxInt64 bounds the checked-integer arithmetic on campaign totals.\nconst maxInt64 = 1\u003c\u003c63 - 1\n\n// Campaign holds the immutable terms and mutable funding state of a single\n// crowdfunding campaign.\ntype Campaign struct {\n\tid           int64\n\tcreator      address\n\ttitle        string\n\ttargetUgnot  int64\n\tdeadlineUnix int64\n\ttotalRaised  int64\n\twithdrawn    bool\n\n\t// updates holds the creator's append-only progress notes, bounded by\n\t// maxUpdateBytes and maxUpdatesPerCampaign (see crowdfunding.gno). The\n\t// String() view only reports a count, never iterates the slice, so\n\t// rendering a campaign stays O(1) regardless of update history.\n\tupdates []string\n\n\t// contributions maps a contributor address (string(address)) to their\n\t// cumulative contribution in ugnot. Zeroed on refund, never removed,\n\t// so a repeat Refund call reliably finds a zero balance.\n\tcontributions avl.Tree\n}\n\nfunc (c *Campaign) status() string {\n\tnow := time.Now().Unix()\n\tswitch {\n\tcase c.withdrawn:\n\t\treturn \"withdrawn\"\n\tcase now \u003c c.deadlineUnix:\n\t\treturn \"active\"\n\tcase c.totalRaised \u003e= c.targetUgnot:\n\t\treturn \"successful\"\n\tdefault:\n\t\treturn \"failed\"\n\t}\n}\n\nfunc (c *Campaign) String() string {\n\treturn ufmt.Sprintf(\n\t\t\"- id: %d\\n- title: %s\\n- creator: %s\\n- target: %dugnot\\n- raised: %dugnot\\n- deadline: %s\\n- status: %s\\n- updates: %d\",\n\t\tc.id, c.title, c.creator, c.targetUgnot, c.totalRaised,\n\t\ttime.Unix(c.deadlineUnix, 0).UTC().Format(time.RFC3339), c.status(), len(c.updates),\n\t)\n}\n"},{"name":"crowdfunding.gno","body":"// Package crowdfunding implements the V0 supported crowdfunding realm:\n// creators raise native ugnot until a deadline; campaigns that meet their\n// target pay the creator, campaigns that don't refund contributors.\n// \n// Creators raise native testnet GNOT until a deadline; successful campaigns pay creators and failed campaigns refund contributors.\n// \n// Generated by generator/ from the \"Community Crowdfunding\" app-spec (schema_version 1.0, revision 2). Do not hand-edit generated output; change the spec and regenerate instead.\npackage crowdfunding\n\nimport (\n\t\"chain\"\n\t\"chain/banker\"\n\t\"chain/runtime/unsafe\"\n\t\"strconv\"\n\t\"strings\"\n\t\"time\"\n\t\"unicode/utf8\"\n\n\t\"gno.land/p/nt/avl/v0\"\n\t\"gno.land/p/nt/ufmt/v0\"\n)\n\nconst (\n\t// maxTitleBytes matches the spec's ui.title_max_utf8_bytes.\n\tmaxTitleBytes = 256\n\t// pageSize matches the spec's ui.page_size for the home listing.\n\tpageSize = 20\n\t// minimumContributionUgnot matches the spec's rules.minimum_contribution_ugnot.\n\tminimumContributionUgnot int64 = 25000000\n\t// denom is the only accepted native denomination (spec currency.denom).\n\tdenom = \"ugnot\"\n\t// maxDeadlineSecondsFromNow bounds how far in the future a campaign\n\t// deadline may be set (see CreateCampaign). This exists specifically to\n\t// catch the plausible mistake of passing milliseconds where whole\n\t// seconds are expected: a millisecond timestamp is ~1000x a second\n\t// timestamp, so even a short 30-day campaign becomes roughly 82 years\n\t// out, and a 1-year campaign becomes roughly 1000 years out. Once\n\t// accepted, the mistake is unrecoverable -- a deadline this far out\n\t// never passes, so contributors can never be refunded and the creator\n\t// can never withdraw either. Ten years comfortably exceeds any\n\t// realistic V0 campaign length while still rejecting that entire class\n\t// of typo by a wide margin.\n\tmaxDeadlineSecondsFromNow int64 = 10 * 365 * 24 * 60 * 60\n\t// campaignKeyWidth zero-pads campaign keys (see campaignKey) so the\n\t// avl.Tree's lexicographic key ordering matches numeric id ordering.\n\t// 19 digits covers every positive int64 (max 9223372036854775807).\n\tcampaignKeyWidth = 19\n\t// maxUpdateBytes and maxUpdatesPerCampaign match the spec's\n\t// updates.max_utf8_bytes and updates.max_per_campaign.\n\tmaxUpdateBytes        = 2000\n\tmaxUpdatesPerCampaign = 50\n)\n\nvar (\n\tcampaigns      avl.Tree // campaignKey(id) -\u003e *Campaign\n\tnextCampaignID int64\n)\n\n// campaignKey renders a campaign id as a fixed-width, zero-padded decimal\n// string so avl.Tree's lexicographic key order matches numeric id order. An\n// unpadded strconv.FormatInt key sorts \"10\" before \"2\", which silently\n// breaks both direct id lookups' consistency with the tree's own ordering\n// and the home listing's newest-first ordering past the ninth campaign.\nfunc campaignKey(id int64) string {\n\ts := strconv.FormatInt(id, 10)\n\tfor len(s) \u003c campaignKeyWidth {\n\t\ts = \"0\" + s\n\t}\n\treturn s\n}\n\n// sanitizeInline conservatively escapes untrusted, user-supplied text (only\n// campaign titles in this realm) before it is stored, so every present and\n// future render path (the home listing, a campaign's String() detail view,\n// and any future Render addition) reads already-clean state instead of\n// needing to remember to sanitize at each render site.\n//\n// A raw title reaching a render path unescaped can forge the surrounding\n// structure it's spliced into: an embedded newline can start a fake\n// \"- creator: g1...\" line in the listing or in a campaign's own detail view,\n// and an unescaped \"](\" can close a markdown link label early and open a\n// javascript: destination. Everything this function emits is a named or\n// numeric XML/HTML character reference, safe as gnoweb markdown text.\nfunc sanitizeInline(s string) string {\n\tif len(s) \u003e maxTitleBytes {\n\t\ts = s[:maxTitleBytes]\n\t}\n\tvar b strings.Builder\n\tfor _, r := range s {\n\t\tswitch r {\n\t\tcase '\u0026':\n\t\t\tb.WriteString(\"\u0026amp;\")\n\t\tcase '\u003c':\n\t\t\tb.WriteString(\"\u0026lt;\")\n\t\tcase '\u003e':\n\t\t\tb.WriteString(\"\u0026gt;\")\n\t\tcase '\"':\n\t\t\tb.WriteString(\"\u0026quot;\")\n\t\tcase '\\'':\n\t\t\tb.WriteString(\"\u0026#39;\")\n\t\tcase '(':\n\t\t\tb.WriteString(\"\u0026#40;\")\n\t\tcase ')':\n\t\t\tb.WriteString(\"\u0026#41;\")\n\t\tcase '[':\n\t\t\tb.WriteString(\"\u0026#91;\")\n\t\tcase ']':\n\t\t\tb.WriteString(\"\u0026#93;\")\n\t\tcase '|':\n\t\t\tb.WriteString(\"\u0026#124;\")\n\t\tcase '\\n', '\\r', '\\t':\n\t\t\tb.WriteRune(' ')\n\t\tdefault:\n\t\t\tif r \u003c 0x20 {\n\t\t\t\tcontinue // drop other control characters outright\n\t\t\t}\n\t\t\tb.WriteRune(r)\n\t\t}\n\t}\n\treturn b.String()\n}\n\n// CreateCampaign registers a new campaign with a funding target (in ugnot)\n// and a UTC unix deadline. It does not receive funds; sends_native_value is\n// false for this action per the spec, so no OriginSend guard applies here.\nfunc CreateCampaign(cur realm, title string, targetUgnot int64, deadlineUnix int64) int64 {\n\tcreator := cur.Previous().Address()\n\n\tif len(title) == 0 {\n\t\tpanic(\"title must not be empty\")\n\t}\n\tif len(title) \u003e maxTitleBytes {\n\t\tpanic(\"title exceeds max size\")\n\t}\n\tif !utf8.ValidString(title) {\n\t\tpanic(\"title must be valid utf-8\")\n\t}\n\tif targetUgnot \u003c= 0 {\n\t\tpanic(\"target must be positive\")\n\t}\n\tif deadlineUnix \u003c= time.Now().Unix() {\n\t\tpanic(\"deadline must be in the future\")\n\t}\n\tif deadlineUnix \u003e time.Now().Unix()+maxDeadlineSecondsFromNow {\n\t\tpanic(\"deadline too far in the future\")\n\t}\n\n\tnextCampaignID++\n\tid := nextCampaignID\n\tcampaign := \u0026Campaign{\n\t\tid:           id,\n\t\tcreator:      creator,\n\t\ttitle:        sanitizeInline(title),\n\t\ttargetUgnot:  targetUgnot,\n\t\tdeadlineUnix: deadlineUnix,\n\t}\n\tcampaigns.Set(campaignKey(id), campaign)\n\treturn id\n}\n\n// Contribute sends native ugnot to a campaign before its deadline.\n//\n// It must be called directly by a user account (maketx call): unsafe.OriginSend()\n// only describes a real receipt at this realm when the caller is a pure user\n// account. An intermediate code realm or a `maketx run` ephemeral realm can\n// attach -send to a transaction but spend the envelope elsewhere, leaving\n// OriginSend() reporting a phantom payment that would let a caller book a\n// contribution never made and later drain the realm's real balance on\n// Withdraw/Refund. This guard is the load-bearing line of the whole realm.\nfunc Contribute(cur realm, campaignID int64) {\n\tif !cur.Previous().IsUserCall() {\n\t\tpanic(\"only user-call (maketx call) accepted\")\n\t}\n\tcontributor := cur.Previous().Address()\n\tcampaign := mustGetCampaign(campaignID)\n\n\tif time.Now().Unix() \u003e= campaign.deadlineUnix {\n\t\tpanic(\"campaign deadline has passed\")\n\t}\n\n\tsent := unsafe.OriginSend()\n\tif len(sent) == 0 {\n\t\tpanic(\"no coins sent\")\n\t}\n\tif len(sent) != 1 || sent[0].Denom != denom {\n\t\tpanic(\"only ugnot contributions are accepted\")\n\t}\n\tamount := sent[0].Amount\n\tif amount \u003c minimumContributionUgnot {\n\t\tpanic(\"contribution below minimum\")\n\t}\n\n\tkey := string(contributor)\n\tvar prev int64\n\tif v := campaign.contributions.Get(key); v != nil {\n\t\tprev = v.(int64)\n\t}\n\tif prev \u003e maxInt64-amount {\n\t\tpanic(\"contribution would overflow\")\n\t}\n\tif campaign.totalRaised \u003e maxInt64-amount {\n\t\tpanic(\"campaign total would overflow\")\n\t}\n\n\tcampaign.contributions.Set(key, prev+amount)\n\tcampaign.totalRaised += amount\n}\n\n// Withdraw pays a campaign's full raised balance to its creator, once, and\n// only once the deadline has passed with the target reached.\n//\n// totalRaised is never mutated by Withdraw or Refund: it is the permanent\n// gross-contributions figure (spec: success_basis gross_accepted_contributions),\n// fixed the moment the deadline passes, since Contribute rejects any call\n// once the deadline has passed. Withdraw and Refund are mutually exclusive\n// because they read that same fixed field with complementary conditions\n// (\u003e= target vs \u003c target), so neither action's own bookkeeping can ever\n// flip the campaign's success/failure outcome and let the other action fire\n// afterward.\nfunc Withdraw(cur realm, campaignID int64) {\n\tcaller := cur.Previous().Address()\n\tcampaign := mustGetCampaign(campaignID)\n\n\tif campaign.withdrawn {\n\t\tpanic(\"campaign funds already withdrawn\")\n\t}\n\tif caller != campaign.creator {\n\t\tpanic(\"only the campaign creator can withdraw\")\n\t}\n\tif time.Now().Unix() \u003c campaign.deadlineUnix {\n\t\tpanic(\"campaign deadline has not passed\")\n\t}\n\tif campaign.totalRaised \u003c campaign.targetUgnot {\n\t\tpanic(\"campaign did not reach its target\")\n\t}\n\n\t// Zero (flip) the withdrawn flag before transferring so a re-entrant\n\t// or repeated call cannot double-spend the creator payout.\n\tamount := campaign.totalRaised\n\tcampaign.withdrawn = true\n\n\tbanker_ := banker.NewBanker(banker.BankerTypeRealmSend, cur)\n\tpkgAddr := cur.Address()\n\tbanker_.SendCoins(pkgAddr, campaign.creator, chain.NewCoins(chain.NewCoin(denom, amount)))\n}\n\n// Refund returns a caller's own cumulative contribution, once, and only\n// once the deadline has passed with the target not reached. See Withdraw\n// for why the two are mutually exclusive. There is no address parameter:\n// entitlement comes only from the authenticated caller's own ledger entry\n// (permission: own_contribution), never from an argument a caller supplies.\nfunc Refund(cur realm, campaignID int64) {\n\tcaller := cur.Previous().Address()\n\tcampaign := mustGetCampaign(campaignID)\n\n\tkey := string(caller)\n\tv := campaign.contributions.Get(key)\n\tif v == nil {\n\t\tpanic(\"no contribution found for caller\")\n\t}\n\tamount := v.(int64)\n\tif amount \u003c= 0 {\n\t\tpanic(\"no contribution found for caller\")\n\t}\n\tif time.Now().Unix() \u003c campaign.deadlineUnix {\n\t\tpanic(\"campaign deadline has not passed\")\n\t}\n\tif campaign.totalRaised \u003e= campaign.targetUgnot {\n\t\tpanic(\"campaign succeeded; refunds are not available\")\n\t}\n\n\t// Zero this contributor's ledger entry before transferring so a\n\t// re-entrant or repeated call cannot double-spend their refund.\n\t// totalRaised is deliberately left untouched (see Withdraw).\n\tcampaign.contributions.Set(key, int64(0))\n\n\tbanker_ := banker.NewBanker(banker.BankerTypeRealmSend, cur)\n\tpkgAddr := cur.Address()\n\tbanker_.SendCoins(pkgAddr, caller, chain.NewCoins(chain.NewCoin(denom, amount)))\n}\n\n// ViewCampaign is the dedicated public read entrypoint for a single\n// campaign, matching the spec's ViewCampaign action (permission: public).\n// It is not a crossing function: it needs no caller identity and mutates\n// no state, same as Render's per-id branch, which returns the identical\n// detail view.\nfunc ViewCampaign(campaignID int64) string {\n\treturn mustGetCampaign(campaignID).String()\n}\n\n// AddUpdate appends a bounded, creator-only progress note to a campaign\n// (permission: campaign_creator, matching Withdraw). Updates are\n// append-only: there is no edit or delete entrypoint, and AddUpdate never\n// touches target, deadline, creator or refund/withdraw eligibility, per the\n// spec's updates.changes_financial_terms = false.\nfunc AddUpdate(cur realm, campaignID int64, body string) {\n\tcaller := cur.Previous().Address()\n\tcampaign := mustGetCampaign(campaignID)\n\n\tif caller != campaign.creator {\n\t\tpanic(\"only the campaign creator can add updates\")\n\t}\n\tif len(body) == 0 {\n\t\tpanic(\"update body must not be empty\")\n\t}\n\tif len(body) \u003e maxUpdateBytes {\n\t\tpanic(\"update exceeds max size\")\n\t}\n\tif len(campaign.updates) \u003e= maxUpdatesPerCampaign {\n\t\tpanic(\"campaign has reached its update limit\")\n\t}\n\n\tcampaign.updates = append(campaign.updates, body)\n}\n\n// Render returns a campaign list at the root path, and a single campaign's\n// detail view at a sub-path holding its id.\nfunc Render(path string) string {\n\tif path == \"\" {\n\t\treturn renderHome()\n\t}\n\tid, err := strconv.ParseInt(path, 10, 64)\n\tif err != nil {\n\t\treturn \"404\"\n\t}\n\tv := campaigns.Get(campaignKey(id))\n\tif v == nil {\n\t\treturn \"404\"\n\t}\n\treturn v.(*Campaign).String()\n}\n\n// renderHome lists the pageSize most recently created campaigns, newest\n// first. ReverseIterateByOffset walks the avl.Tree largest-key-first, so an\n// offset of 0 is what actually means \"start from the newest\": a nonzero\n// offset here would skip that many of the newest entries instead, which is\n// never what a \"recent campaigns\" listing wants.\nfunc renderHome() string {\n\toutput := \"# Community Crowdfunding\" + \"\\n\\n\"\n\tsize := campaigns.Size()\n\tif size == 0 {\n\t\treturn output + \"No campaigns yet.\\n\"\n\t}\n\tcampaigns.ReverseIterateByOffset(0, pageSize, func(key string, value any) bool {\n\t\tc := value.(*Campaign)\n\t\toutput += ufmt.Sprintf(\"- %d: %s - target %dugnot raised %dugnot - %s\\n\",\n\t\t\tc.id, c.title, c.targetUgnot, c.totalRaised, c.status())\n\t\treturn false\n\t})\n\treturn output\n}\n\nfunc mustGetCampaign(campaignID int64) *Campaign {\n\tv := campaigns.Get(campaignKey(campaignID))\n\tif v == nil {\n\t\tpanic(\"unknown campaign id\")\n\t}\n\treturn v.(*Campaign)\n}\n"},{"name":"crowdfunding_test.gno","body":"package crowdfunding\n\nimport (\n\t\"chain\"\n\t\"strings\"\n\t\"testing\"\n\t\"time\"\n\n\t\"gno.land/p/nt/avl/v0\"\n\t\"gno.land/p/nt/testutils/v0\"\n\t\"gno.land/p/nt/ufmt/v0\"\n\t\"gno.land/p/nt/uassert/v0\"\n)\n\nconst modulePath = \"gno.land/r/g142p4mjutq0dhu3vlfz32ymc490gk9d2sqglrak/gate1v2e/crowdfunding\"\n\nfunc resetTestState() {\n\tcampaigns = avl.Tree{}\n\tnextCampaignID = 0\n}\n\n// contribute is a test helper: it switches to contributor, declares an\n// origin send of amount ugnot, credits the realm's bank balance so a later\n// Withdraw/Refund SendCoins has funds to move, and calls Contribute.\nfunc contribute(cur realm, t *testing.T, contributor address, campaignID int64, amount int64) {\n\tpkgAddr := chain.PackageAddress(modulePath)\n\ttesting.SetRealm(testing.NewUserRealm(contributor))\n\ttesting.SetOriginSend(chain.Coins{{denom, amount}})\n\ttesting.IssueCoins(pkgAddr, chain.Coins{{denom, amount}})\n\tContribute(cross(cur), campaignID)\n}\n\nfunc TestCreateCampaign_Success(cur realm, t *testing.T) {\n\tdefer resetTestState()\n\n\tcreator := testutils.TestAddress(\"creator1\")\n\ttesting.SetRealm(testing.NewUserRealm(creator))\n\n\tdeadline := time.Now().Add(1 * time.Hour).Unix()\n\ttarget := minimumContributionUgnot * 10\n\tid := CreateCampaign(cross(cur), \"Build a well\", target, deadline)\n\tuassert.Equal(t, int64(1), id)\n\n\tc := mustGetCampaign(id)\n\tuassert.Equal(t, \"Build a well\", c.title)\n\tuassert.Equal(t, creator, c.creator)\n\tuassert.Equal(t, target, c.targetUgnot)\n\tuassert.Equal(t, int64(0), c.totalRaised)\n\tuassert.False(t, c.withdrawn)\n}\n\nfunc TestCreateCampaign_InvalidTargetRefused(cur realm, t *testing.T) {\n\tdefer resetTestState()\n\n\tcreator := testutils.TestAddress(\"creator2\")\n\ttesting.SetRealm(testing.NewUserRealm(creator))\n\tdeadline := time.Now().Add(1 * time.Hour).Unix()\n\n\tuassert.AbortsWithMessage(t, cur, \"target must be positive\", func() {\n\t\tCreateCampaign(cross(cur), \"Bad target\", 0, deadline)\n\t})\n}\n\nfunc TestCreateCampaign_PastDeadlineRefused(cur realm, t *testing.T) {\n\tdefer resetTestState()\n\n\tcreator := testutils.TestAddress(\"creator3\")\n\ttesting.SetRealm(testing.NewUserRealm(creator))\n\tpastDeadline := time.Now().Add(-1 * time.Hour).Unix()\n\n\tuassert.AbortsWithMessage(t, cur, \"deadline must be in the future\", func() {\n\t\tCreateCampaign(cross(cur), \"Too late\", minimumContributionUgnot*10, pastDeadline)\n\t})\n}\n\nfunc TestContribute_BelowMinimumRefused(cur realm, t *testing.T) {\n\tdefer resetTestState()\n\n\tcreator := testutils.TestAddress(\"creator4\")\n\tcontributor := testutils.TestAddress(\"contributor4\")\n\ttesting.SetRealm(testing.NewUserRealm(creator))\n\tdeadline := time.Now().Add(1 * time.Hour).Unix()\n\tid := CreateCampaign(cross(cur), \"Below minimum\", minimumContributionUgnot*10, deadline)\n\n\tpkgAddr := chain.PackageAddress(modulePath)\n\tbelowMinimum := minimumContributionUgnot - 1\n\ttesting.SetRealm(testing.NewUserRealm(contributor))\n\ttesting.SetOriginSend(chain.Coins{{denom, belowMinimum}})\n\ttesting.IssueCoins(pkgAddr, chain.Coins{{denom, belowMinimum}})\n\tuassert.AbortsWithMessage(t, cur, \"contribution below minimum\", func() {\n\t\tContribute(cross(cur), id)\n\t})\n}\n\nfunc TestContribute_ExactMinimumAccepted(cur realm, t *testing.T) {\n\tdefer resetTestState()\n\n\tcreator := testutils.TestAddress(\"creator5\")\n\tcontributor := testutils.TestAddress(\"contributor5\")\n\ttesting.SetRealm(testing.NewUserRealm(creator))\n\tdeadline := time.Now().Add(1 * time.Hour).Unix()\n\tid := CreateCampaign(cross(cur), \"Exact minimum\", minimumContributionUgnot*10, deadline)\n\n\tcontribute(cur, t, contributor, id, minimumContributionUgnot)\n\n\tc := mustGetCampaign(id)\n\tuassert.Equal(t, minimumContributionUgnot, c.totalRaised)\n}\n\nfunc TestContribute_WrongDenomRefused(cur realm, t *testing.T) {\n\tdefer resetTestState()\n\n\tcreator := testutils.TestAddress(\"creator6\")\n\tcontributor := testutils.TestAddress(\"contributor6\")\n\ttesting.SetRealm(testing.NewUserRealm(creator))\n\tdeadline := time.Now().Add(1 * time.Hour).Unix()\n\tid := CreateCampaign(cross(cur), \"Denom test\", minimumContributionUgnot*10, deadline)\n\n\tpkgAddr := chain.PackageAddress(modulePath)\n\ttesting.SetRealm(testing.NewUserRealm(contributor))\n\ttesting.SetOriginSend(chain.Coins{{\"otherdenom\", minimumContributionUgnot}})\n\ttesting.IssueCoins(pkgAddr, chain.Coins{{\"otherdenom\", minimumContributionUgnot}})\n\tuassert.AbortsWithMessage(t, cur, \"only ugnot contributions are accepted\", func() {\n\t\tContribute(cross(cur), id)\n\t})\n}\n\nfunc TestContribute_AfterDeadlineRefused(cur realm, t *testing.T) {\n\tdefer resetTestState()\n\n\tcreator := testutils.TestAddress(\"creator7\")\n\tcontributor := testutils.TestAddress(\"contributor7\")\n\ttesting.SetRealm(testing.NewUserRealm(creator))\n\tdeadline := time.Now().Add(1 * time.Hour).Unix()\n\tid := CreateCampaign(cross(cur), \"Expiring\", minimumContributionUgnot*10, deadline)\n\n\tc := mustGetCampaign(id)\n\tc.deadlineUnix = time.Now().Add(-1 * time.Hour).Unix() // simulate deadline passed\n\n\tpkgAddr := chain.PackageAddress(modulePath)\n\ttesting.SetRealm(testing.NewUserRealm(contributor))\n\ttesting.SetOriginSend(chain.Coins{{denom, minimumContributionUgnot}})\n\ttesting.IssueCoins(pkgAddr, chain.Coins{{denom, minimumContributionUgnot}})\n\tuassert.AbortsWithMessage(t, cur, \"campaign deadline has passed\", func() {\n\t\tContribute(cross(cur), id)\n\t})\n}\n\n// TestContribute_AtExactDeadlineRefused is the boundary case for the guard\n// `if time.Now().Unix() \u003e= campaign.deadlineUnix`: a contribution landing on\n// the deadline second itself must be refused, not just one landing\n// comfortably after it. Weakening `\u003e=` to `\u003e` would accept this exact-second\n// contribution while still refusing a later one, so a test that only\n// exercises \"well past the deadline\" (TestContribute_AfterDeadlineRefused,\n// above) cannot catch that regression.\nfunc TestContribute_AtExactDeadlineRefused(cur realm, t *testing.T) {\n\tdefer resetTestState()\n\n\tcreator := testutils.TestAddress(\"creator20\")\n\tcontributor := testutils.TestAddress(\"contributor20\")\n\ttesting.SetRealm(testing.NewUserRealm(creator))\n\tdeadline := time.Now().Add(1 * time.Hour).Unix()\n\tid := CreateCampaign(cross(cur), \"Exact deadline\", minimumContributionUgnot*10, deadline)\n\n\tc := mustGetCampaign(id)\n\tc.deadlineUnix = time.Now().Unix() // the boundary itself, not a past deadline\n\n\tpkgAddr := chain.PackageAddress(modulePath)\n\ttesting.SetRealm(testing.NewUserRealm(contributor))\n\ttesting.SetOriginSend(chain.Coins{{denom, minimumContributionUgnot}})\n\ttesting.IssueCoins(pkgAddr, chain.Coins{{denom, minimumContributionUgnot}})\n\tuassert.AbortsWithMessage(t, cur, \"campaign deadline has passed\", func() {\n\t\tContribute(cross(cur), id)\n\t})\n}\n\n// TestContribute_ContributorOverflowRefused seeds this contributor's running\n// total directly to one short of maxInt64 (no real sequence of contributions\n// could reach it) so the next contribution overflows only the\n// per-contributor guard (`prev \u003e maxInt64-amount`), not the campaign-total\n// guard: totalRaised is left at its zero value.\nfunc TestContribute_ContributorOverflowRefused(cur realm, t *testing.T) {\n\tdefer resetTestState()\n\n\tcreator := testutils.TestAddress(\"creator21\")\n\tcontributor := testutils.TestAddress(\"contributor21\")\n\ttesting.SetRealm(testing.NewUserRealm(creator))\n\tdeadline := time.Now().Add(1 * time.Hour).Unix()\n\tid := CreateCampaign(cross(cur), \"Overflow contributor\", minimumContributionUgnot*10, deadline)\n\n\tc := mustGetCampaign(id)\n\tc.contributions.Set(string(contributor), maxInt64-minimumContributionUgnot)\n\n\tpkgAddr := chain.PackageAddress(modulePath)\n\tamount := minimumContributionUgnot + 1\n\ttesting.SetRealm(testing.NewUserRealm(contributor))\n\ttesting.SetOriginSend(chain.Coins{{denom, amount}})\n\ttesting.IssueCoins(pkgAddr, chain.Coins{{denom, amount}})\n\tuassert.AbortsWithMessage(t, cur, \"contribution would overflow\", func() {\n\t\tContribute(cross(cur), id)\n\t})\n}\n\n// TestContribute_CampaignTotalOverflowRefused seeds the campaign's total\n// directly to one short of maxInt64 (no real sequence of contributions could\n// reach it) so the next contribution overflows only the campaign-total guard\n// (`campaign.totalRaised \u003e maxInt64-amount`), not the per-contributor guard:\n// this contributor's own running total is still zero.\nfunc TestContribute_CampaignTotalOverflowRefused(cur realm, t *testing.T) {\n\tdefer resetTestState()\n\n\tcreator := testutils.TestAddress(\"creator22\")\n\tcontributor := testutils.TestAddress(\"contributor22\")\n\ttesting.SetRealm(testing.NewUserRealm(creator))\n\tdeadline := time.Now().Add(1 * time.Hour).Unix()\n\tid := CreateCampaign(cross(cur), \"Overflow campaign total\", minimumContributionUgnot*10, deadline)\n\n\tc := mustGetCampaign(id)\n\tc.totalRaised = maxInt64 - minimumContributionUgnot // one short of overflowing\n\n\tpkgAddr := chain.PackageAddress(modulePath)\n\tamount := minimumContributionUgnot + 1\n\ttesting.SetRealm(testing.NewUserRealm(contributor))\n\ttesting.SetOriginSend(chain.Coins{{denom, amount}})\n\ttesting.IssueCoins(pkgAddr, chain.Coins{{denom, amount}})\n\tuassert.AbortsWithMessage(t, cur, \"campaign total would overflow\", func() {\n\t\tContribute(cross(cur), id)\n\t})\n}\n\n// TestContribute_CodeRealmCallerRefused exercises Contribute's very first\n// guard, `if !cur.Previous().IsUserCall() { panic(...) }` -- the guard the\n// source's own comment calls \"the load-bearing line of the whole realm\",\n// because it is the only thing standing between unsafe.OriginSend() and a\n// caller that can attach -send to a transaction without a matching deposit\n// ever reaching this realm. testing.NewCodeRealm constructs a caller whose\n// immediate frame is a code realm rather than a genuine user account, which\n// is exactly the case IsUserCall() must reject; the preceding\n// testing.NewUserRealm call gives that code realm a real user frame beneath\n// it, mirroring how gnovm's own zrealm_crossrealm13.gno test builds a\n// non-user-call frame. The realm path below is a deliberately fictional test\n// fixture (not a real deployed realm) so a reader can't mistake it for an\n// actual dependency of this contract.\nfunc TestContribute_CodeRealmCallerRefused(cur realm, t *testing.T) {\n\tdefer resetTestState()\n\n\tcreator := testutils.TestAddress(\"creator23\")\n\ttesting.SetRealm(testing.NewUserRealm(creator))\n\tdeadline := time.Now().Add(1 * time.Hour).Unix()\n\tid := CreateCampaign(cross(cur), \"Code realm caller\", minimumContributionUgnot*10, deadline)\n\n\tfakeCallerAccount := testutils.TestAddress(\"contributor23\")\n\ttesting.SetRealm(testing.NewUserRealm(fakeCallerAccount))\n\ttesting.SetRealm(testing.NewCodeRealm(\"gno.land/r/gnobuilder/test_fixtures/fake_code_realm\"))\n\tuassert.AbortsWithMessage(t, cur, \"only user-call (maketx call) accepted\", func() {\n\t\tContribute(cross(cur), id)\n\t})\n}\n\nfunc TestSuccessfulCampaign_CreatorWithdraws(cur realm, t *testing.T) {\n\tdefer resetTestState()\n\n\tcreator := testutils.TestAddress(\"creator8\")\n\tcontributor := testutils.TestAddress(\"contributor8\")\n\ttesting.SetRealm(testing.NewUserRealm(creator))\n\tdeadline := time.Now().Add(1 * time.Hour).Unix()\n\ttarget := minimumContributionUgnot * 10\n\tid := CreateCampaign(cross(cur), \"Fund the thing\", target, deadline)\n\n\traised := minimumContributionUgnot * 15\n\tcontribute(cur, t, contributor, id, raised)\n\n\tc := mustGetCampaign(id)\n\tuassert.Equal(t, raised, c.totalRaised)\n\tc.deadlineUnix = time.Now().Add(-1 * time.Hour).Unix() // simulate deadline passed\n\n\ttesting.SetRealm(testing.NewUserRealm(creator))\n\tWithdraw(cross(cur), id)\n\n\tuassert.True(t, c.withdrawn)\n\tuassert.Equal(t, raised, c.totalRaised) // gross-raised figure stays fixed, only withdrawn flips\n}\n\nfunc TestWithdraw_DoubleWithdrawRefused(cur realm, t *testing.T) {\n\tdefer resetTestState()\n\n\tcreator := testutils.TestAddress(\"creator9\")\n\tcontributor := testutils.TestAddress(\"contributor9\")\n\ttesting.SetRealm(testing.NewUserRealm(creator))\n\tdeadline := time.Now().Add(1 * time.Hour).Unix()\n\ttarget := minimumContributionUgnot * 10\n\tid := CreateCampaign(cross(cur), \"Double withdraw\", target, deadline)\n\n\tcontribute(cur, t, contributor, id, target)\n\n\tc := mustGetCampaign(id)\n\tc.deadlineUnix = time.Now().Add(-1 * time.Hour).Unix()\n\n\ttesting.SetRealm(testing.NewUserRealm(creator))\n\tWithdraw(cross(cur), id)\n\tuassert.AbortsWithMessage(t, cur, \"campaign funds already withdrawn\", func() {\n\t\tWithdraw(cross(cur), id)\n\t})\n}\n\nfunc TestWithdraw_NonCreatorRefused(cur realm, t *testing.T) {\n\tdefer resetTestState()\n\n\tcreator := testutils.TestAddress(\"creator10\")\n\tcontributor := testutils.TestAddress(\"contributor10\")\n\tstranger := testutils.TestAddress(\"stranger10\")\n\ttesting.SetRealm(testing.NewUserRealm(creator))\n\tdeadline := time.Now().Add(1 * time.Hour).Unix()\n\ttarget := minimumContributionUgnot * 10\n\tid := CreateCampaign(cross(cur), \"Not yours\", target, deadline)\n\n\tcontribute(cur, t, contributor, id, target)\n\n\tc := mustGetCampaign(id)\n\tc.deadlineUnix = time.Now().Add(-1 * time.Hour).Unix()\n\n\ttesting.SetRealm(testing.NewUserRealm(stranger))\n\tuassert.AbortsWithMessage(t, cur, \"only the campaign creator can withdraw\", func() {\n\t\tWithdraw(cross(cur), id)\n\t})\n}\n\nfunc TestWithdraw_BeforeDeadlineRefused(cur realm, t *testing.T) {\n\tdefer resetTestState()\n\n\tcreator := testutils.TestAddress(\"creator11\")\n\tcontributor := testutils.TestAddress(\"contributor11\")\n\ttesting.SetRealm(testing.NewUserRealm(creator))\n\tdeadline := time.Now().Add(1 * time.Hour).Unix()\n\ttarget := minimumContributionUgnot * 10\n\tid := CreateCampaign(cross(cur), \"Too early\", target, deadline)\n\n\tcontribute(cur, t, contributor, id, target)\n\n\ttesting.SetRealm(testing.NewUserRealm(creator))\n\tuassert.AbortsWithMessage(t, cur, \"campaign deadline has not passed\", func() {\n\t\tWithdraw(cross(cur), id)\n\t})\n}\n\nfunc TestWithdraw_TargetNotMetRefused(cur realm, t *testing.T) {\n\tdefer resetTestState()\n\n\tcreator := testutils.TestAddress(\"creator12\")\n\tcontributor := testutils.TestAddress(\"contributor12\")\n\ttesting.SetRealm(testing.NewUserRealm(creator))\n\tdeadline := time.Now().Add(1 * time.Hour).Unix()\n\ttarget := minimumContributionUgnot * 10\n\tid := CreateCampaign(cross(cur), \"Underfunded\", target, deadline)\n\n\tcontribute(cur, t, contributor, id, minimumContributionUgnot*2)\n\n\tc := mustGetCampaign(id)\n\tc.deadlineUnix = time.Now().Add(-1 * time.Hour).Unix()\n\n\ttesting.SetRealm(testing.NewUserRealm(creator))\n\tuassert.AbortsWithMessage(t, cur, \"campaign did not reach its target\", func() {\n\t\tWithdraw(cross(cur), id)\n\t})\n}\n\nfunc TestFailedCampaign_ContributorRefunded(cur realm, t *testing.T) {\n\tdefer resetTestState()\n\n\tcreator := testutils.TestAddress(\"creator13\")\n\tcontributor := testutils.TestAddress(\"contributor13\")\n\ttesting.SetRealm(testing.NewUserRealm(creator))\n\tdeadline := time.Now().Add(1 * time.Hour).Unix()\n\ttarget := minimumContributionUgnot * 10\n\tid := CreateCampaign(cross(cur), \"Will fail\", target, deadline)\n\n\tshortfall := minimumContributionUgnot * 3\n\tcontribute(cur, t, contributor, id, shortfall)\n\n\tc := mustGetCampaign(id)\n\tc.deadlineUnix = time.Now().Add(-1 * time.Hour).Unix()\n\n\ttesting.SetRealm(testing.NewUserRealm(contributor))\n\tRefund(cross(cur), id)\n\n\tuassert.Equal(t, shortfall, c.totalRaised) // gross-raised figure stays fixed\n\tuassert.Equal(t, int64(0), c.contributions.Get(string(contributor)).(int64))\n}\n\nfunc TestRefund_DoubleRefundRefused(cur realm, t *testing.T) {\n\tdefer resetTestState()\n\n\tcreator := testutils.TestAddress(\"creator14\")\n\tcontributor := testutils.TestAddress(\"contributor14\")\n\ttesting.SetRealm(testing.NewUserRealm(creator))\n\tdeadline := time.Now().Add(1 * time.Hour).Unix()\n\ttarget := minimumContributionUgnot * 10\n\tid := CreateCampaign(cross(cur), \"Double refund\", target, deadline)\n\n\tcontribute(cur, t, contributor, id, minimumContributionUgnot*3)\n\n\tc := mustGetCampaign(id)\n\tc.deadlineUnix = time.Now().Add(-1 * time.Hour).Unix()\n\n\ttesting.SetRealm(testing.NewUserRealm(contributor))\n\tRefund(cross(cur), id)\n\tuassert.AbortsWithMessage(t, cur, \"no contribution found for caller\", func() {\n\t\tRefund(cross(cur), id)\n\t})\n}\n\nfunc TestRefund_NonContributorRefused(cur realm, t *testing.T) {\n\tdefer resetTestState()\n\n\tcreator := testutils.TestAddress(\"creator15\")\n\tcontributor := testutils.TestAddress(\"contributor15\")\n\tstranger := testutils.TestAddress(\"stranger15\")\n\ttesting.SetRealm(testing.NewUserRealm(creator))\n\tdeadline := time.Now().Add(1 * time.Hour).Unix()\n\ttarget := minimumContributionUgnot * 10\n\tid := CreateCampaign(cross(cur), \"Stranger refund\", target, deadline)\n\n\tcontribute(cur, t, contributor, id, minimumContributionUgnot*3)\n\n\tc := mustGetCampaign(id)\n\tc.deadlineUnix = time.Now().Add(-1 * time.Hour).Unix()\n\n\ttesting.SetRealm(testing.NewUserRealm(stranger))\n\tuassert.AbortsWithMessage(t, cur, \"no contribution found for caller\", func() {\n\t\tRefund(cross(cur), id)\n\t})\n}\n\nfunc TestRefund_BeforeDeadlineRefused(cur realm, t *testing.T) {\n\tdefer resetTestState()\n\n\tcreator := testutils.TestAddress(\"creator16\")\n\tcontributor := testutils.TestAddress(\"contributor16\")\n\ttesting.SetRealm(testing.NewUserRealm(creator))\n\tdeadline := time.Now().Add(1 * time.Hour).Unix()\n\ttarget := minimumContributionUgnot * 10\n\tid := CreateCampaign(cross(cur), \"Too early refund\", target, deadline)\n\n\tcontribute(cur, t, contributor, id, minimumContributionUgnot*3)\n\n\ttesting.SetRealm(testing.NewUserRealm(contributor))\n\tuassert.AbortsWithMessage(t, cur, \"campaign deadline has not passed\", func() {\n\t\tRefund(cross(cur), id)\n\t})\n}\n\nfunc TestRefund_CampaignSucceededRefused(cur realm, t *testing.T) {\n\tdefer resetTestState()\n\n\tcreator := testutils.TestAddress(\"creator17\")\n\tcontributor := testutils.TestAddress(\"contributor17\")\n\ttesting.SetRealm(testing.NewUserRealm(creator))\n\tdeadline := time.Now().Add(1 * time.Hour).Unix()\n\ttarget := minimumContributionUgnot * 10\n\tid := CreateCampaign(cross(cur), \"Will succeed\", target, deadline)\n\n\tcontribute(cur, t, contributor, id, target)\n\n\tc := mustGetCampaign(id)\n\tc.deadlineUnix = time.Now().Add(-1 * time.Hour).Unix()\n\n\ttesting.SetRealm(testing.NewUserRealm(contributor))\n\tuassert.AbortsWithMessage(t, cur, \"campaign succeeded; refunds are not available\", func() {\n\t\tRefund(cross(cur), id)\n\t})\n}\n\nfunc TestViewCampaign(cur realm, t *testing.T) {\n\tdefer resetTestState()\n\n\tcreator := testutils.TestAddress(\"creator18\")\n\ttesting.SetRealm(testing.NewUserRealm(creator))\n\tdeadline := time.Now().Add(1 * time.Hour).Unix()\n\tid := CreateCampaign(cross(cur), \"Viewable\", minimumContributionUgnot*10, deadline)\n\n\tuassert.Equal(t, mustGetCampaign(id).String(), ViewCampaign(id))\n}\n\nfunc TestRender(cur realm, t *testing.T) {\n\tdefer resetTestState()\n\n\tcreator := testutils.TestAddress(\"creator19\")\n\tcontributor := testutils.TestAddress(\"contributor19\")\n\ttesting.SetRealm(testing.NewUserRealm(creator))\n\tdeadline := time.Now().Add(1 * time.Hour).Unix()\n\ttarget1 := minimumContributionUgnot * 10\n\ttarget2 := minimumContributionUgnot * 20\n\tid1 := CreateCampaign(cross(cur), \"Campaign One\", target1, deadline)\n\tid2 := CreateCampaign(cross(cur), \"Campaign Two\", target2, deadline)\n\n\traised1 := minimumContributionUgnot * 5\n\tcontribute(cur, t, contributor, id1, raised1)\n\n\texpected := ufmt.Sprintf(\n\t\t\"# Community Crowdfunding\"+\"\\n\\n- %d: Campaign Two - target %dugnot raised 0ugnot - active\\n- %d: Campaign One - target %dugnot raised %dugnot - active\\n\",\n\t\tid2, target2, id1, target1, raised1,\n\t)\n\tuassert.Equal(t, expected, Render(\"\"))\n\n\tc1 := mustGetCampaign(id1)\n\texpectedDetail := ufmt.Sprintf(\n\t\t\"- id: %d\\n- title: %s\\n- creator: %s\\n- target: %dugnot\\n- raised: %dugnot\\n- deadline: %s\\n- status: %s\\n- updates: %d\",\n\t\tid1, \"Campaign One\", creator, target1, raised1,\n\t\ttime.Unix(c1.deadlineUnix, 0).UTC().Format(time.RFC3339), \"active\", len(c1.updates),\n\t)\n\tuassert.Equal(t, expectedDetail, Render(\"1\"))\n\n\tuassert.Equal(t, \"404\", Render(\"999\"))\n}\n\n// TestCreateCampaign_TitleSanitizedAtCreation pins F2: a title is sanitized\n// once, at creation time, so the stored state is clean and every render\n// path (here, both the detail String() view and the home listing) is\n// covered without needing its own escaping. The hostile fragments below are\n// exactly the two failure modes the finding named: an embedded newline\n// forging a fake \"- creator: ...\" line, and unescaped markdown link syntax\n// (\"](javascript:...)\") turning into a live malicious link destination.\nfunc TestCreateCampaign_TitleSanitizedAtCreation(cur realm, t *testing.T) {\n\tdefer resetTestState()\n\n\tcreator := testutils.TestAddress(\"creator-sanitize\")\n\ttesting.SetRealm(testing.NewUserRealm(creator))\n\tdeadline := time.Now().Add(1 * time.Hour).Unix()\n\n\thostile := \"Nice project\\n- creator: g1attacker \u003cscript\u003ealert(1)\u003c/script\u003e [x](javascript:alert(1)) a|b\"\n\tid := CreateCampaign(cross(cur), hostile, minimumContributionUgnot*10, deadline)\n\n\tc := mustGetCampaign(id)\n\tforbidden := []string{\"\\n\", \"\u003c\", \"\u003e\", \"](\", \"|\"}\n\tfor _, frag := range forbidden {\n\t\tif strings.Contains(c.title, frag) {\n\t\t\tt.Errorf(\"expected stored title to be sanitized, still contains %q: %s\", frag, c.title)\n\t\t}\n\t}\n\n\trendered := Render(\"\")\n\tif strings.Contains(rendered, \"\\n- creator: g1attacker\") {\n\t\tt.Errorf(\"expected a title's embedded newline to be unable to forge a creator line in the listing, got: %s\", rendered)\n\t}\n}\n\n// TestCreateCampaign_InvalidUTF8Refused pins the other half of F2: byte\n// length alone (len(title) \u003e maxTitleBytes) does not enforce the spec's\n// UTF-8 intent, so invalid UTF-8 must be rejected explicitly instead of\n// being stored.\nfunc TestCreateCampaign_InvalidUTF8Refused(cur realm, t *testing.T) {\n\tdefer resetTestState()\n\n\tcreator := testutils.TestAddress(\"creator-badutf8\")\n\ttesting.SetRealm(testing.NewUserRealm(creator))\n\tdeadline := time.Now().Add(1 * time.Hour).Unix()\n\n\tinvalid := \"bad title \\xff\\xfe\"\n\tuassert.AbortsWithMessage(t, cur, \"title must be valid utf-8\", func() {\n\t\tCreateCampaign(cross(cur), invalid, minimumContributionUgnot*10, deadline)\n\t})\n}\n\n// TestCreateCampaign_DeadlineTooFarInFutureRefused pins F5: a deadline far\n// enough out to be an obvious milliseconds-for-seconds typo (see\n// maxDeadlineSecondsFromNow) must be refused before it can lock\n// contributions forever. The 60-second margin beyond the bound (rather than\n// exactly +1) keeps this test from flaking if a second elapses between this\n// line's clock read and CreateCampaign's own.\nfunc TestCreateCampaign_DeadlineTooFarInFutureRefused(cur realm, t *testing.T) {\n\tdefer resetTestState()\n\n\tcreator := testutils.TestAddress(\"creator-fardeadline\")\n\ttesting.SetRealm(testing.NewUserRealm(creator))\n\ttooFar := time.Now().Unix() + maxDeadlineSecondsFromNow + 60\n\n\tuassert.AbortsWithMessage(t, cur, \"deadline too far in the future\", func() {\n\t\tCreateCampaign(cross(cur), \"Millisecond typo\", minimumContributionUgnot*10, tooFar)\n\t})\n}\n\n// TestCreateCampaign_DeadlineAtUpperBoundAccepted is the boundary case for\n// the guard `deadlineUnix \u003e time.Now().Unix()+maxDeadlineSecondsFromNow`: a\n// deadline landing exactly on the bound must still be accepted, not just\n// one comfortably inside it, so a mutation weakening `\u003e` to `\u003e=` has\n// something to be caught by.\nfunc TestCreateCampaign_DeadlineAtUpperBoundAccepted(cur realm, t *testing.T) {\n\tdefer resetTestState()\n\n\t// testutils.TestAddress panics above 20 bytes, so keep these names short.\n\tcreator := testutils.TestAddress(\"creator-bounddl\")\n\ttesting.SetRealm(testing.NewUserRealm(creator))\n\tatBound := time.Now().Unix() + maxDeadlineSecondsFromNow\n\n\tid := CreateCampaign(cross(cur), \"At the boundary\", minimumContributionUgnot*10, atBound)\n\tc := mustGetCampaign(id)\n\tuassert.Equal(t, atBound, c.deadlineUnix)\n}\n\n// TestCampaignKeyOrdersNumericallyNotLexicographically pins F3's second,\n// compounding bug: unpadded decimal keys sort \"10\" before \"2\", so from the\n// tenth campaign onward, key order silently stops matching id order.\nfunc TestCampaignKeyOrdersNumericallyNotLexicographically(cur realm, t *testing.T) {\n\tif campaignKey(2) \u003e= campaignKey(10) {\n\t\tt.Errorf(\"expected campaignKey(2) \u003c campaignKey(10) (numeric-order-preserving keys), got key(2)=%q key(10)=%q\", campaignKey(2), campaignKey(10))\n\t}\n\tif len(campaignKey(1)) != campaignKeyWidth {\n\t\tt.Errorf(\"expected campaignKey to zero-pad to width %d, got %q (len %d)\", campaignKeyWidth, campaignKey(1), len(campaignKey(1)))\n\t}\n}\n\n// TestRenderHome_ShowsNewestBeyondPageSize pins F3's first bug: past\n// pageSize campaigns, the home listing must keep showing the newest ones,\n// not permanently freeze on the oldest page.\nfunc TestRenderHome_ShowsNewestBeyondPageSize(cur realm, t *testing.T) {\n\tdefer resetTestState()\n\n\tcreator := testutils.TestAddress(\"creator-pagination\")\n\ttesting.SetRealm(testing.NewUserRealm(creator))\n\tdeadline := time.Now().Add(1 * time.Hour).Unix()\n\n\ttotal := pageSize + 5\n\tvar newestID, oldestID int64\n\tfor i := 0; i \u003c total; i++ {\n\t\tid := CreateCampaign(cross(cur), ufmt.Sprintf(\"Campaign %d\", i), minimumContributionUgnot*10, deadline)\n\t\tif i == 0 {\n\t\t\toldestID = id\n\t\t}\n\t\tnewestID = id\n\t}\n\n\toutput := Render(\"\")\n\tnewest := mustGetCampaign(newestID)\n\toldest := mustGetCampaign(oldestID)\n\tif !strings.Contains(output, newest.title) {\n\t\tt.Errorf(\"expected the newest campaign %q on the home listing, got: %s\", newest.title, output)\n\t}\n\tif strings.Contains(output, oldest.title) {\n\t\tt.Errorf(\"expected the oldest campaign %q to be pushed off the first page, got: %s\", oldest.title, output)\n\t}\n}\n\n// TestContribute_TrustsDeclaredOriginSendNotIssuedCoins is F6: every other\n// test in this suite calls the `contribute` helper above, which fabricates\n// both testing.SetOriginSend and testing.IssueCoins from the SAME `amount`,\n// so the suite could never distinguish \"coins genuinely arrived\" from \"the\n// realm was merely told coins arrived.\" This test deliberately declares a\n// different, larger origin send than the coins actually issued to the\n// realm, and pins the realm's actual, current behaviour: Contribute's\n// accounting (contributions, totalRaised) is driven entirely by\n// unsafe.OriginSend(), the declared amount, with no cross-check whatsoever\n// against the realm's real coin balance. That is not a bug in Contribute\n// itself -- in production, unsafe.OriginSend() is documented to describe a\n// genuine receipt for a real user-call transaction, which is exactly what\n// the IsUserCall guard above exists to make trustworthy -- but it does mean\n// this specific mismatch is undetectable from inside Contribute, and can\n// only ever be manufactured in a test via two independent fabrications like\n// this one. Per this task's scope, Contribute's logic is not changed here;\n// this test documents the behaviour as it stands.\nfunc TestContribute_TrustsDeclaredOriginSendNotIssuedCoins(cur realm, t *testing.T) {\n\tdefer resetTestState()\n\n\tcreator := testutils.TestAddress(\"creator-mismatch\")\n\tcontributor := testutils.TestAddress(\"contributor-mismatch\")\n\ttesting.SetRealm(testing.NewUserRealm(creator))\n\tdeadline := time.Now().Add(1 * time.Hour).Unix()\n\tid := CreateCampaign(cross(cur), \"Mismatch test\", minimumContributionUgnot*10, deadline)\n\n\tdeclared := minimumContributionUgnot * 5\n\tactuallyIssued := minimumContributionUgnot * 2 // deliberately less than declared\n\n\tpkgAddr := chain.PackageAddress(modulePath)\n\ttesting.SetRealm(testing.NewUserRealm(contributor))\n\ttesting.SetOriginSend(chain.Coins{{denom, declared}})\n\ttesting.IssueCoins(pkgAddr, chain.Coins{{denom, actuallyIssued}})\n\tContribute(cross(cur), id)\n\n\tc := mustGetCampaign(id)\n\tuassert.Equal(t, declared, c.totalRaised)\n\tuassert.Equal(t, declared, c.contributions.Get(string(contributor)).(int64))\n}\n\nfunc TestAddUpdate_CreatorSucceeds(cur realm, t *testing.T) {\n\tdefer resetTestState()\n\n\tcreator := testutils.TestAddress(\"update-creator1\")\n\ttesting.SetRealm(testing.NewUserRealm(creator))\n\tdeadline := time.Now().Add(1 * time.Hour).Unix()\n\tid := CreateCampaign(cross(cur), \"Updatable\", minimumContributionUgnot*10, deadline)\n\n\tAddUpdate(cross(cur), id, \"Progress: foundations poured.\")\n\n\tc := mustGetCampaign(id)\n\tuassert.Equal(t, 1, len(c.updates))\n\tuassert.Equal(t, \"Progress: foundations poured.\", c.updates[0])\n}\n\nfunc TestAddUpdate_NonCreatorRefused(cur realm, t *testing.T) {\n\tdefer resetTestState()\n\n\tcreator := testutils.TestAddress(\"update-creator2\")\n\tstranger := testutils.TestAddress(\"update-stranger2\")\n\ttesting.SetRealm(testing.NewUserRealm(creator))\n\tdeadline := time.Now().Add(1 * time.Hour).Unix()\n\tid := CreateCampaign(cross(cur), \"Not yours to update\", minimumContributionUgnot*10, deadline)\n\n\ttesting.SetRealm(testing.NewUserRealm(stranger))\n\tuassert.AbortsWithMessage(t, cur, \"only the campaign creator can add updates\", func() {\n\t\tAddUpdate(cross(cur), id, \"I am not the creator\")\n\t})\n}\n\nfunc TestAddUpdate_OversizedRefused(cur realm, t *testing.T) {\n\tdefer resetTestState()\n\n\tcreator := testutils.TestAddress(\"update-creator3\")\n\ttesting.SetRealm(testing.NewUserRealm(creator))\n\tdeadline := time.Now().Add(1 * time.Hour).Unix()\n\tid := CreateCampaign(cross(cur), \"Oversized update\", minimumContributionUgnot*10, deadline)\n\n\toversized := make([]byte, maxUpdateBytes+1)\n\tfor i := range oversized {\n\t\toversized[i] = 'a'\n\t}\n\n\tuassert.AbortsWithMessage(t, cur, \"update exceeds max size\", func() {\n\t\tAddUpdate(cross(cur), id, string(oversized))\n\t})\n}\n\nfunc TestAddUpdate_CountLimitEnforced(cur realm, t *testing.T) {\n\tdefer resetTestState()\n\n\tcreator := testutils.TestAddress(\"update-creator4\")\n\ttesting.SetRealm(testing.NewUserRealm(creator))\n\tdeadline := time.Now().Add(1 * time.Hour).Unix()\n\tid := CreateCampaign(cross(cur), \"Update limit\", minimumContributionUgnot*10, deadline)\n\n\tfor i := 0; i \u003c maxUpdatesPerCampaign; i++ {\n\t\tAddUpdate(cross(cur), id, \"update\")\n\t}\n\n\tc := mustGetCampaign(id)\n\tuassert.Equal(t, maxUpdatesPerCampaign, len(c.updates))\n\n\tuassert.AbortsWithMessage(t, cur, \"campaign has reached its update limit\", func() {\n\t\tAddUpdate(cross(cur), id, \"one too many\")\n\t})\n}\n"},{"name":"gnomod.toml","body":"module = \"gno.land/r/g142p4mjutq0dhu3vlfz32ymc490gk9d2sqglrak/gate1v2e/crowdfunding\"\ngno = \"0.9\"\n"}],"type":{"@type":"/gno.MemPackageType","value":"MPUserAll"}},"send":"","max_deposit":""}],"fee":{"gas_wanted":"66972869","gas_fee":"66975ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"A2GOzMGavtLOZjb9g1ltPKZsySYTl1dY+wHnkIs2zwhI"},"signature":"cRlkQsjieLqFbpzdwiosxNovr1ScceRRsv3Xbv8jGyM3h8cF2E3Sca7xLzK6+HX5UXYsMMhQSgNmZsJijkDFHw=="}],"memo":""},"metadata":{"timestamp":"1790118439"}}
{"tx":{"msg":[{"@type":"/vm.m_call","caller":"g13fmcl95ma9f5r9hhz0a96mwwhmdxrx73f93587","send":"","max_deposit":"","pkg_path":"gno.land/r/g142p4mjutq0dhu3vlfz32ymc490gk9d2sqglrak/gate1v2e/crowdfunding","func":"CreateCampaign","args":["Gate1 successful campaign","100000000","1790118743"]}],"fee":{"gas_wanted":"4882090","gas_fee":"4885ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"AsvB/2qgzHlBsiCGbgrBINRFk+H0Dhpwvl7YYhLVBxJ+"},"signature":"vSLtM3j+8Yx4aA7RTr/+aR6/dTp7r/mcQazlkQ+o4hlMFcky9p+AzR2O2xFacXtQu62/jUrqGRVjHGFHwzuP4Q=="}],"memo":""},"metadata":{"timestamp":"1790118446"}}
{"tx":{"msg":[{"@type":"/vm.m_call","caller":"g13fmcl95ma9f5r9hhz0a96mwwhmdxrx73f93587","send":"","max_deposit":"","pkg_path":"gno.land/r/g142p4mjutq0dhu3vlfz32ymc490gk9d2sqglrak/gate1v2e/crowdfunding","func":"CreateCampaign","args":["Gate1 failed campaign","100000000","1790118743"]}],"fee":{"gas_wanted":"5064457","gas_fee":"5067ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"AsvB/2qgzHlBsiCGbgrBINRFk+H0Dhpwvl7YYhLVBxJ+"},"signature":"k+OFYyk6z4uEUsLVPm4P1Bdfs/5BiOJ5HBvrQKDutMgBKzO1D/LcY5Exnlxec+FD3NAG5zeTgXpEMotjkDZ+og=="}],"memo":""},"metadata":{"timestamp":"1790118452"}}
{"tx":{"msg":[{"@type":"/vm.m_call","caller":"g13ru04ttcfh2apyya44fe2vghnyt9tty98zeu2j","send":"25000000ugnot","max_deposit":"","pkg_path":"gno.land/r/g142p4mjutq0dhu3vlfz32ymc490gk9d2sqglrak/gate1v2e/crowdfunding","func":"Contribute","args":["1"]}],"fee":{"gas_wanted":"5513420","gas_fee":"5516ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"AqapGZ8VqkhzpqOfiyN9OTYqRpm9AqBiBofd6y+7+VKb"},"signature":"2Y4pTXS0NsltYZsNACTqzll8yy+UdvB6rfPXQ4+8xes7weWRJLiQa3kABJ3Z852tpmjFTYEHjWr7ZxwLvNHs6Q=="}],"memo":""},"metadata":{"timestamp":"1790118459"}}
{"tx":{"msg":[{"@type":"/bank.MsgSend","from_address":"g13fmcl95ma9f5r9hhz0a96mwwhmdxrx73f93587","to_address":"g142p4mjutq0dhu3vlfz32ymc490gk9d2sqglrak","amount":"52000000ugnot"}],"fee":{"gas_wanted":"1200000","gas_fee":"1200ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"AsvB/2qgzHlBsiCGbgrBINRFk+H0Dhpwvl7YYhLVBxJ+"},"signature":"5GxV/2ZH9/pc8WI+nWoRL4Xk9mnT4Y8bEv+oaT2mj9kCDPqiPjPLoBj9FUihO0gS7VbI1tbgkQKRydCLF0lIqQ=="}],"memo":""},"metadata":{"timestamp":"1790119318"}}
{"tx":{"msg":[{"@type":"/bank.MsgSend","from_address":"g104jlra2w6uhflxvszf3qw3rg8qmglkg4dl3je5","to_address":"g142p4mjutq0dhu3vlfz32ymc490gk9d2sqglrak","amount":"53000000ugnot"}],"fee":{"gas_wanted":"1200000","gas_fee":"1200ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"AzJJn6oXknRtIU7wAQNHtitoWOkrI20H0yd2rbjUsbVU"},"signature":"AyK+Cc9JZhv46GySc5Ud6NY/m2RhibmChXQDoBpmxZskB7fPwUncyBcbGl8Le2kEAI2AOU5WqlyFsfv5NHNV6A=="}],"memo":""},"metadata":{"timestamp":"1790119322"}}
{"tx":{"msg":[{"@type":"/bank.MsgSend","from_address":"g142p4mjutq0dhu3vlfz32ymc490gk9d2sqglrak","to_address":"g13ru04ttcfh2apyya44fe2vghnyt9tty98zeu2j","amount":"23711316ugnot"}],"fee":{"gas_wanted":"981907","gas_fee":"984ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"A2GOzMGavtLOZjb9g1ltPKZsySYTl1dY+wHnkIs2zwhI"},"signature":"giYvwiO690JJy1qT7ijn73bMST/YM6tQV0RF/jv9lKhMvfSzaBR3gmqpupz1A6FA70phuFxrEsjtgBTnfuViBQ=="}],"memo":""},"metadata":{"timestamp":"1790119341"}}
{"tx":{"msg":[{"@type":"/bank.MsgSend","from_address":"g142p4mjutq0dhu3vlfz32ymc490gk9d2sqglrak","to_address":"g18xat6nw5r3mz6894rmfdmm95lmfly4ku24ru6t","amount":"70400000ugnot"}],"fee":{"gas_wanted":"981904","gas_fee":"984ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"A2GOzMGavtLOZjb9g1ltPKZsySYTl1dY+wHnkIs2zwhI"},"signature":"81cqNvma369qQR6crZOWMEfIAZKZ/+DxELzt1JXuN1RsDMbtMqAM2biGgzvMMXMPUwee5h+pWlmh0wPRI8FR1g=="}],"memo":""},"metadata":{"timestamp":"1790119348"}}
{"tx":{"msg":[{"@type":"/vm.m_addpkg","creator":"g142p4mjutq0dhu3vlfz32ymc490gk9d2sqglrak","package":{"name":"crowdfunding","path":"gno.land/r/g142p4mjutq0dhu3vlfz32ymc490gk9d2sqglrak/gate1v2f/crowdfunding","files":[{"name":"campaign.gno","body":"package crowdfunding\n\nimport (\n\t\"time\"\n\n\t\"gno.land/p/nt/avl/v0\"\n\t\"gno.land/p/nt/ufmt/v0\"\n)\n\n// maxInt64 bounds the checked-integer arithmetic on campaign totals.\nconst maxInt64 = 1\u003c\u003c63 - 1\n\n// Campaign holds the immutable terms and mutable funding state of a single\n// crowdfunding campaign.\ntype Campaign struct {\n\tid           int64\n\tcreator      address\n\ttitle        string\n\ttargetUgnot  int64\n\tdeadlineUnix int64\n\ttotalRaised  int64\n\twithdrawn    bool\n\n\t// updates holds the creator's append-only progress notes, bounded by\n\t// maxUpdateBytes and maxUpdatesPerCampaign (see crowdfunding.gno). The\n\t// String() view only reports a count, never iterates the slice, so\n\t// rendering a campaign stays O(1) regardless of update history.\n\tupdates []string\n\n\t// contributions maps a contributor address (string(address)) to their\n\t// cumulative contribution in ugnot. Zeroed on refund, never removed,\n\t// so a repeat Refund call reliably finds a zero balance.\n\tcontributions avl.Tree\n}\n\nfunc (c *Campaign) status() string {\n\tnow := time.Now().Unix()\n\tswitch {\n\tcase c.withdrawn:\n\t\treturn \"withdrawn\"\n\tcase now \u003c c.deadlineUnix:\n\t\treturn \"active\"\n\tcase c.totalRaised \u003e= c.targetUgnot:\n\t\treturn \"successful\"\n\tdefault:\n\t\treturn \"failed\"\n\t}\n}\n\nfunc (c *Campaign) String() string {\n\treturn ufmt.Sprintf(\n\t\t\"- id: %d\\n- title: %s\\n- creator: %s\\n- target: %dugnot\\n- raised: %dugnot\\n- deadline: %s\\n- status: %s\\n- updates: %d\",\n\t\tc.id, c.title, c.creator, c.targetUgnot, c.totalRaised,\n\t\ttime.Unix(c.deadlineUnix, 0).UTC().Format(time.RFC3339), c.status(), len(c.updates),\n\t)\n}\n"},{"name":"crowdfunding.gno","body":"// Package crowdfunding implements the V0 supported crowdfunding realm:\n// creators raise native ugnot until a deadline; campaigns that meet their\n// target pay the creator, campaigns that don't refund contributors.\n// \n// Creators raise native testnet GNOT until a deadline; successful campaigns pay creators and failed campaigns refund contributors.\n// \n// Generated by generator/ from the \"Community Crowdfunding\" app-spec (schema_version 1.0, revision 2). Do not hand-edit generated output; change the spec and regenerate instead.\npackage crowdfunding\n\nimport (\n\t\"chain\"\n\t\"chain/banker\"\n\t\"chain/runtime/unsafe\"\n\t\"strconv\"\n\t\"strings\"\n\t\"time\"\n\t\"unicode/utf8\"\n\n\t\"gno.land/p/nt/avl/v0\"\n\t\"gno.land/p/nt/ufmt/v0\"\n)\n\nconst (\n\t// maxTitleBytes matches the spec's ui.title_max_utf8_bytes.\n\tmaxTitleBytes = 256\n\t// pageSize matches the spec's ui.page_size for the home listing.\n\tpageSize = 20\n\t// minimumContributionUgnot matches the spec's rules.minimum_contribution_ugnot.\n\tminimumContributionUgnot int64 = 25000000\n\t// denom is the only accepted native denomination (spec currency.denom).\n\tdenom = \"ugnot\"\n\t// maxDeadlineSecondsFromNow bounds how far in the future a campaign\n\t// deadline may be set (see CreateCampaign). This exists specifically to\n\t// catch the plausible mistake of passing milliseconds where whole\n\t// seconds are expected: a millisecond timestamp is ~1000x a second\n\t// timestamp, so even a short 30-day campaign becomes roughly 82 years\n\t// out, and a 1-year campaign becomes roughly 1000 years out. Once\n\t// accepted, the mistake is unrecoverable -- a deadline this far out\n\t// never passes, so contributors can never be refunded and the creator\n\t// can never withdraw either. Ten years comfortably exceeds any\n\t// realistic V0 campaign length while still rejecting that entire class\n\t// of typo by a wide margin.\n\tmaxDeadlineSecondsFromNow int64 = 10 * 365 * 24 * 60 * 60\n\t// campaignKeyWidth zero-pads campaign keys (see campaignKey) so the\n\t// avl.Tree's lexicographic key ordering matches numeric id ordering.\n\t// 19 digits covers every positive int64 (max 9223372036854775807).\n\tcampaignKeyWidth = 19\n\t// maxUpdateBytes and maxUpdatesPerCampaign match the spec's\n\t// updates.max_utf8_bytes and updates.max_per_campaign.\n\tmaxUpdateBytes        = 2000\n\tmaxUpdatesPerCampaign = 50\n)\n\nvar (\n\tcampaigns      avl.Tree // campaignKey(id) -\u003e *Campaign\n\tnextCampaignID int64\n)\n\n// campaignKey renders a campaign id as a fixed-width, zero-padded decimal\n// string so avl.Tree's lexicographic key order matches numeric id order. An\n// unpadded strconv.FormatInt key sorts \"10\" before \"2\", which silently\n// breaks both direct id lookups' consistency with the tree's own ordering\n// and the home listing's newest-first ordering past the ninth campaign.\nfunc campaignKey(id int64) string {\n\ts := strconv.FormatInt(id, 10)\n\tfor len(s) \u003c campaignKeyWidth {\n\t\ts = \"0\" + s\n\t}\n\treturn s\n}\n\n// sanitizeInline conservatively escapes untrusted, user-supplied text (only\n// campaign titles in this realm) before it is stored, so every present and\n// future render path (the home listing, a campaign's String() detail view,\n// and any future Render addition) reads already-clean state instead of\n// needing to remember to sanitize at each render site.\n//\n// A raw title reaching a render path unescaped can forge the surrounding\n// structure it's spliced into: an embedded newline can start a fake\n// \"- creator: g1...\" line in the listing or in a campaign's own detail view,\n// and an unescaped \"](\" can close a markdown link label early and open a\n// javascript: destination. Everything this function emits is a named or\n// numeric XML/HTML character reference, safe as gnoweb markdown text.\nfunc sanitizeInline(s string) string {\n\tif len(s) \u003e maxTitleBytes {\n\t\ts = s[:maxTitleBytes]\n\t}\n\tvar b strings.Builder\n\tfor _, r := range s {\n\t\tswitch r {\n\t\tcase '\u0026':\n\t\t\tb.WriteString(\"\u0026amp;\")\n\t\tcase '\u003c':\n\t\t\tb.WriteString(\"\u0026lt;\")\n\t\tcase '\u003e':\n\t\t\tb.WriteString(\"\u0026gt;\")\n\t\tcase '\"':\n\t\t\tb.WriteString(\"\u0026quot;\")\n\t\tcase '\\'':\n\t\t\tb.WriteString(\"\u0026#39;\")\n\t\tcase '(':\n\t\t\tb.WriteString(\"\u0026#40;\")\n\t\tcase ')':\n\t\t\tb.WriteString(\"\u0026#41;\")\n\t\tcase '[':\n\t\t\tb.WriteString(\"\u0026#91;\")\n\t\tcase ']':\n\t\t\tb.WriteString(\"\u0026#93;\")\n\t\tcase '|':\n\t\t\tb.WriteString(\"\u0026#124;\")\n\t\tcase '\\n', '\\r', '\\t':\n\t\t\tb.WriteRune(' ')\n\t\tdefault:\n\t\t\tif r \u003c 0x20 {\n\t\t\t\tcontinue // drop other control characters outright\n\t\t\t}\n\t\t\tb.WriteRune(r)\n\t\t}\n\t}\n\treturn b.String()\n}\n\n// CreateCampaign registers a new campaign with a funding target (in ugnot)\n// and a UTC unix deadline. It does not receive funds; sends_native_value is\n// false for this action per the spec, so no OriginSend guard applies here.\nfunc CreateCampaign(cur realm, title string, targetUgnot int64, deadlineUnix int64) int64 {\n\tcreator := cur.Previous().Address()\n\n\tif len(title) == 0 {\n\t\tpanic(\"title must not be empty\")\n\t}\n\tif len(title) \u003e maxTitleBytes {\n\t\tpanic(\"title exceeds max size\")\n\t}\n\tif !utf8.ValidString(title) {\n\t\tpanic(\"title must be valid utf-8\")\n\t}\n\tif targetUgnot \u003c= 0 {\n\t\tpanic(\"target must be positive\")\n\t}\n\tif deadlineUnix \u003c= time.Now().Unix() {\n\t\tpanic(\"deadline must be in the future\")\n\t}\n\tif deadlineUnix \u003e time.Now().Unix()+maxDeadlineSecondsFromNow {\n\t\tpanic(\"deadline too far in the future\")\n\t}\n\n\tnextCampaignID++\n\tid := nextCampaignID\n\tcampaign := \u0026Campaign{\n\t\tid:           id,\n\t\tcreator:      creator,\n\t\ttitle:        sanitizeInline(title),\n\t\ttargetUgnot:  targetUgnot,\n\t\tdeadlineUnix: deadlineUnix,\n\t}\n\tcampaigns.Set(campaignKey(id), campaign)\n\treturn id\n}\n\n// Contribute sends native ugnot to a campaign before its deadline.\n//\n// It must be called directly by a user account (maketx call): unsafe.OriginSend()\n// only describes a real receipt at this realm when the caller is a pure user\n// account. An intermediate code realm or a `maketx run` ephemeral realm can\n// attach -send to a transaction but spend the envelope elsewhere, leaving\n// OriginSend() reporting a phantom payment that would let a caller book a\n// contribution never made and later drain the realm's real balance on\n// Withdraw/Refund. This guard is the load-bearing line of the whole realm.\nfunc Contribute(cur realm, campaignID int64) {\n\tif !cur.Previous().IsUserCall() {\n\t\tpanic(\"only user-call (maketx call) accepted\")\n\t}\n\tcontributor := cur.Previous().Address()\n\tcampaign := mustGetCampaign(campaignID)\n\n\tif time.Now().Unix() \u003e= campaign.deadlineUnix {\n\t\tpanic(\"campaign deadline has passed\")\n\t}\n\n\tsent := unsafe.OriginSend()\n\tif len(sent) == 0 {\n\t\tpanic(\"no coins sent\")\n\t}\n\tif len(sent) != 1 || sent[0].Denom != denom {\n\t\tpanic(\"only ugnot contributions are accepted\")\n\t}\n\tamount := sent[0].Amount\n\tif amount \u003c minimumContributionUgnot {\n\t\tpanic(\"contribution below minimum\")\n\t}\n\n\tkey := string(contributor)\n\tvar prev int64\n\tif v := campaign.contributions.Get(key); v != nil {\n\t\tprev = v.(int64)\n\t}\n\tif prev \u003e maxInt64-amount {\n\t\tpanic(\"contribution would overflow\")\n\t}\n\tif campaign.totalRaised \u003e maxInt64-amount {\n\t\tpanic(\"campaign total would overflow\")\n\t}\n\n\tcampaign.contributions.Set(key, prev+amount)\n\tcampaign.totalRaised += amount\n}\n\n// Withdraw pays a campaign's full raised balance to its creator, once, and\n// only once the deadline has passed with the target reached.\n//\n// totalRaised is never mutated by Withdraw or Refund: it is the permanent\n// gross-contributions figure (spec: success_basis gross_accepted_contributions),\n// fixed the moment the deadline passes, since Contribute rejects any call\n// once the deadline has passed. Withdraw and Refund are mutually exclusive\n// because they read that same fixed field with complementary conditions\n// (\u003e= target vs \u003c target), so neither action's own bookkeeping can ever\n// flip the campaign's success/failure outcome and let the other action fire\n// afterward.\nfunc Withdraw(cur realm, campaignID int64) {\n\tcaller := cur.Previous().Address()\n\tcampaign := mustGetCampaign(campaignID)\n\n\tif campaign.withdrawn {\n\t\tpanic(\"campaign funds already withdrawn\")\n\t}\n\tif caller != campaign.creator {\n\t\tpanic(\"only the campaign creator can withdraw\")\n\t}\n\tif time.Now().Unix() \u003c campaign.deadlineUnix {\n\t\tpanic(\"campaign deadline has not passed\")\n\t}\n\tif campaign.totalRaised \u003c campaign.targetUgnot {\n\t\tpanic(\"campaign did not reach its target\")\n\t}\n\n\t// Zero (flip) the withdrawn flag before transferring so a re-entrant\n\t// or repeated call cannot double-spend the creator payout.\n\tamount := campaign.totalRaised\n\tcampaign.withdrawn = true\n\n\tbanker_ := banker.NewBanker(banker.BankerTypeRealmSend, cur)\n\tpkgAddr := cur.Address()\n\tbanker_.SendCoins(pkgAddr, campaign.creator, chain.NewCoins(chain.NewCoin(denom, amount)))\n}\n\n// Refund returns a caller's own cumulative contribution, once, and only\n// once the deadline has passed with the target not reached. See Withdraw\n// for why the two are mutually exclusive. There is no address parameter:\n// entitlement comes only from the authenticated caller's own ledger entry\n// (permission: own_contribution), never from an argument a caller supplies.\nfunc Refund(cur realm, campaignID int64) {\n\tcaller := cur.Previous().Address()\n\tcampaign := mustGetCampaign(campaignID)\n\n\tkey := string(caller)\n\tv := campaign.contributions.Get(key)\n\tif v == nil {\n\t\tpanic(\"no contribution found for caller\")\n\t}\n\tamount := v.(int64)\n\tif amount \u003c= 0 {\n\t\tpanic(\"no contribution found for caller\")\n\t}\n\tif time.Now().Unix() \u003c campaign.deadlineUnix {\n\t\tpanic(\"campaign deadline has not passed\")\n\t}\n\tif campaign.totalRaised \u003e= campaign.targetUgnot {\n\t\tpanic(\"campaign succeeded; refunds are not available\")\n\t}\n\n\t// Zero this contributor's ledger entry before transferring so a\n\t// re-entrant or repeated call cannot double-spend their refund.\n\t// totalRaised is deliberately left untouched (see Withdraw).\n\tcampaign.contributions.Set(key, int64(0))\n\n\tbanker_ := banker.NewBanker(banker.BankerTypeRealmSend, cur)\n\tpkgAddr := cur.Address()\n\tbanker_.SendCoins(pkgAddr, caller, chain.NewCoins(chain.NewCoin(denom, amount)))\n}\n\n// ViewCampaign is the dedicated public read entrypoint for a single\n// campaign, matching the spec's ViewCampaign action (permission: public).\n// It is not a crossing function: it needs no caller identity and mutates\n// no state, same as Render's per-id branch, which returns the identical\n// detail view.\nfunc ViewCampaign(campaignID int64) string {\n\treturn mustGetCampaign(campaignID).String()\n}\n\n// AddUpdate appends a bounded, creator-only progress note to a campaign\n// (permission: campaign_creator, matching Withdraw). Updates are\n// append-only: there is no edit or delete entrypoint, and AddUpdate never\n// touches target, deadline, creator or refund/withdraw eligibility, per the\n// spec's updates.changes_financial_terms = false.\nfunc AddUpdate(cur realm, campaignID int64, body string) {\n\tcaller := cur.Previous().Address()\n\tcampaign := mustGetCampaign(campaignID)\n\n\tif caller != campaign.creator {\n\t\tpanic(\"only the campaign creator can add updates\")\n\t}\n\tif len(body) == 0 {\n\t\tpanic(\"update body must not be empty\")\n\t}\n\tif len(body) \u003e maxUpdateBytes {\n\t\tpanic(\"update exceeds max size\")\n\t}\n\tif len(campaign.updates) \u003e= maxUpdatesPerCampaign {\n\t\tpanic(\"campaign has reached its update limit\")\n\t}\n\n\tcampaign.updates = append(campaign.updates, body)\n}\n\n// Render returns a campaign list at the root path, and a single campaign's\n// detail view at a sub-path holding its id.\nfunc Render(path string) string {\n\tif path == \"\" {\n\t\treturn renderHome()\n\t}\n\tid, err := strconv.ParseInt(path, 10, 64)\n\tif err != nil {\n\t\treturn \"404\"\n\t}\n\tv := campaigns.Get(campaignKey(id))\n\tif v == nil {\n\t\treturn \"404\"\n\t}\n\treturn v.(*Campaign).String()\n}\n\n// renderHome lists the pageSize most recently created campaigns, newest\n// first. ReverseIterateByOffset walks the avl.Tree largest-key-first, so an\n// offset of 0 is what actually means \"start from the newest\": a nonzero\n// offset here would skip that many of the newest entries instead, which is\n// never what a \"recent campaigns\" listing wants.\nfunc renderHome() string {\n\toutput := \"# Community Crowdfunding\" + \"\\n\\n\"\n\tsize := campaigns.Size()\n\tif size == 0 {\n\t\treturn output + \"No campaigns yet.\\n\"\n\t}\n\tcampaigns.ReverseIterateByOffset(0, pageSize, func(key string, value any) bool {\n\t\tc := value.(*Campaign)\n\t\toutput += ufmt.Sprintf(\"- %d: %s - target %dugnot raised %dugnot - %s\\n\",\n\t\t\tc.id, c.title, c.targetUgnot, c.totalRaised, c.status())\n\t\treturn false\n\t})\n\treturn output\n}\n\nfunc mustGetCampaign(campaignID int64) *Campaign {\n\tv := campaigns.Get(campaignKey(campaignID))\n\tif v == nil {\n\t\tpanic(\"unknown campaign id\")\n\t}\n\treturn v.(*Campaign)\n}\n"},{"name":"crowdfunding_test.gno","body":"package crowdfunding\n\nimport (\n\t\"chain\"\n\t\"strings\"\n\t\"testing\"\n\t\"time\"\n\n\t\"gno.land/p/nt/avl/v0\"\n\t\"gno.land/p/nt/testutils/v0\"\n\t\"gno.land/p/nt/ufmt/v0\"\n\t\"gno.land/p/nt/uassert/v0\"\n)\n\nconst modulePath = \"gno.land/r/g142p4mjutq0dhu3vlfz32ymc490gk9d2sqglrak/gate1v2f/crowdfunding\"\n\nfunc resetTestState() {\n\tcampaigns = avl.Tree{}\n\tnextCampaignID = 0\n}\n\n// contribute is a test helper: it switches to contributor, declares an\n// origin send of amount ugnot, credits the realm's bank balance so a later\n// Withdraw/Refund SendCoins has funds to move, and calls Contribute.\nfunc contribute(cur realm, t *testing.T, contributor address, campaignID int64, amount int64) {\n\tpkgAddr := chain.PackageAddress(modulePath)\n\ttesting.SetRealm(testing.NewUserRealm(contributor))\n\ttesting.SetOriginSend(chain.Coins{{denom, amount}})\n\ttesting.IssueCoins(pkgAddr, chain.Coins{{denom, amount}})\n\tContribute(cross(cur), campaignID)\n}\n\nfunc TestCreateCampaign_Success(cur realm, t *testing.T) {\n\tdefer resetTestState()\n\n\tcreator := testutils.TestAddress(\"creator1\")\n\ttesting.SetRealm(testing.NewUserRealm(creator))\n\n\tdeadline := time.Now().Add(1 * time.Hour).Unix()\n\ttarget := minimumContributionUgnot * 10\n\tid := CreateCampaign(cross(cur), \"Build a well\", target, deadline)\n\tuassert.Equal(t, int64(1), id)\n\n\tc := mustGetCampaign(id)\n\tuassert.Equal(t, \"Build a well\", c.title)\n\tuassert.Equal(t, creator, c.creator)\n\tuassert.Equal(t, target, c.targetUgnot)\n\tuassert.Equal(t, int64(0), c.totalRaised)\n\tuassert.False(t, c.withdrawn)\n}\n\nfunc TestCreateCampaign_InvalidTargetRefused(cur realm, t *testing.T) {\n\tdefer resetTestState()\n\n\tcreator := testutils.TestAddress(\"creator2\")\n\ttesting.SetRealm(testing.NewUserRealm(creator))\n\tdeadline := time.Now().Add(1 * time.Hour).Unix()\n\n\tuassert.AbortsWithMessage(t, cur, \"target must be positive\", func() {\n\t\tCreateCampaign(cross(cur), \"Bad target\", 0, deadline)\n\t})\n}\n\nfunc TestCreateCampaign_PastDeadlineRefused(cur realm, t *testing.T) {\n\tdefer resetTestState()\n\n\tcreator := testutils.TestAddress(\"creator3\")\n\ttesting.SetRealm(testing.NewUserRealm(creator))\n\tpastDeadline := time.Now().Add(-1 * time.Hour).Unix()\n\n\tuassert.AbortsWithMessage(t, cur, \"deadline must be in the future\", func() {\n\t\tCreateCampaign(cross(cur), \"Too late\", minimumContributionUgnot*10, pastDeadline)\n\t})\n}\n\nfunc TestContribute_BelowMinimumRefused(cur realm, t *testing.T) {\n\tdefer resetTestState()\n\n\tcreator := testutils.TestAddress(\"creator4\")\n\tcontributor := testutils.TestAddress(\"contributor4\")\n\ttesting.SetRealm(testing.NewUserRealm(creator))\n\tdeadline := time.Now().Add(1 * time.Hour).Unix()\n\tid := CreateCampaign(cross(cur), \"Below minimum\", minimumContributionUgnot*10, deadline)\n\n\tpkgAddr := chain.PackageAddress(modulePath)\n\tbelowMinimum := minimumContributionUgnot - 1\n\ttesting.SetRealm(testing.NewUserRealm(contributor))\n\ttesting.SetOriginSend(chain.Coins{{denom, belowMinimum}})\n\ttesting.IssueCoins(pkgAddr, chain.Coins{{denom, belowMinimum}})\n\tuassert.AbortsWithMessage(t, cur, \"contribution below minimum\", func() {\n\t\tContribute(cross(cur), id)\n\t})\n}\n\nfunc TestContribute_ExactMinimumAccepted(cur realm, t *testing.T) {\n\tdefer resetTestState()\n\n\tcreator := testutils.TestAddress(\"creator5\")\n\tcontributor := testutils.TestAddress(\"contributor5\")\n\ttesting.SetRealm(testing.NewUserRealm(creator))\n\tdeadline := time.Now().Add(1 * time.Hour).Unix()\n\tid := CreateCampaign(cross(cur), \"Exact minimum\", minimumContributionUgnot*10, deadline)\n\n\tcontribute(cur, t, contributor, id, minimumContributionUgnot)\n\n\tc := mustGetCampaign(id)\n\tuassert.Equal(t, minimumContributionUgnot, c.totalRaised)\n}\n\nfunc TestContribute_WrongDenomRefused(cur realm, t *testing.T) {\n\tdefer resetTestState()\n\n\tcreator := testutils.TestAddress(\"creator6\")\n\tcontributor := testutils.TestAddress(\"contributor6\")\n\ttesting.SetRealm(testing.NewUserRealm(creator))\n\tdeadline := time.Now().Add(1 * time.Hour).Unix()\n\tid := CreateCampaign(cross(cur), \"Denom test\", minimumContributionUgnot*10, deadline)\n\n\tpkgAddr := chain.PackageAddress(modulePath)\n\ttesting.SetRealm(testing.NewUserRealm(contributor))\n\ttesting.SetOriginSend(chain.Coins{{\"otherdenom\", minimumContributionUgnot}})\n\ttesting.IssueCoins(pkgAddr, chain.Coins{{\"otherdenom\", minimumContributionUgnot}})\n\tuassert.AbortsWithMessage(t, cur, \"only ugnot contributions are accepted\", func() {\n\t\tContribute(cross(cur), id)\n\t})\n}\n\nfunc TestContribute_AfterDeadlineRefused(cur realm, t *testing.T) {\n\tdefer resetTestState()\n\n\tcreator := testutils.TestAddress(\"creator7\")\n\tcontributor := testutils.TestAddress(\"contributor7\")\n\ttesting.SetRealm(testing.NewUserRealm(creator))\n\tdeadline := time.Now().Add(1 * time.Hour).Unix()\n\tid := CreateCampaign(cross(cur), \"Expiring\", minimumContributionUgnot*10, deadline)\n\n\tc := mustGetCampaign(id)\n\tc.deadlineUnix = time.Now().Add(-1 * time.Hour).Unix() // simulate deadline passed\n\n\tpkgAddr := chain.PackageAddress(modulePath)\n\ttesting.SetRealm(testing.NewUserRealm(contributor))\n\ttesting.SetOriginSend(chain.Coins{{denom, minimumContributionUgnot}})\n\ttesting.IssueCoins(pkgAddr, chain.Coins{{denom, minimumContributionUgnot}})\n\tuassert.AbortsWithMessage(t, cur, \"campaign deadline has passed\", func() {\n\t\tContribute(cross(cur), id)\n\t})\n}\n\n// TestContribute_AtExactDeadlineRefused is the boundary case for the guard\n// `if time.Now().Unix() \u003e= campaign.deadlineUnix`: a contribution landing on\n// the deadline second itself must be refused, not just one landing\n// comfortably after it. Weakening `\u003e=` to `\u003e` would accept this exact-second\n// contribution while still refusing a later one, so a test that only\n// exercises \"well past the deadline\" (TestContribute_AfterDeadlineRefused,\n// above) cannot catch that regression.\nfunc TestContribute_AtExactDeadlineRefused(cur realm, t *testing.T) {\n\tdefer resetTestState()\n\n\tcreator := testutils.TestAddress(\"creator20\")\n\tcontributor := testutils.TestAddress(\"contributor20\")\n\ttesting.SetRealm(testing.NewUserRealm(creator))\n\tdeadline := time.Now().Add(1 * time.Hour).Unix()\n\tid := CreateCampaign(cross(cur), \"Exact deadline\", minimumContributionUgnot*10, deadline)\n\n\tc := mustGetCampaign(id)\n\tc.deadlineUnix = time.Now().Unix() // the boundary itself, not a past deadline\n\n\tpkgAddr := chain.PackageAddress(modulePath)\n\ttesting.SetRealm(testing.NewUserRealm(contributor))\n\ttesting.SetOriginSend(chain.Coins{{denom, minimumContributionUgnot}})\n\ttesting.IssueCoins(pkgAddr, chain.Coins{{denom, minimumContributionUgnot}})\n\tuassert.AbortsWithMessage(t, cur, \"campaign deadline has passed\", func() {\n\t\tContribute(cross(cur), id)\n\t})\n}\n\n// TestContribute_ContributorOverflowRefused seeds this contributor's running\n// total directly to one short of maxInt64 (no real sequence of contributions\n// could reach it) so the next contribution overflows only the\n// per-contributor guard (`prev \u003e maxInt64-amount`), not the campaign-total\n// guard: totalRaised is left at its zero value.\nfunc TestContribute_ContributorOverflowRefused(cur realm, t *testing.T) {\n\tdefer resetTestState()\n\n\tcreator := testutils.TestAddress(\"creator21\")\n\tcontributor := testutils.TestAddress(\"contributor21\")\n\ttesting.SetRealm(testing.NewUserRealm(creator))\n\tdeadline := time.Now().Add(1 * time.Hour).Unix()\n\tid := CreateCampaign(cross(cur), \"Overflow contributor\", minimumContributionUgnot*10, deadline)\n\n\tc := mustGetCampaign(id)\n\tc.contributions.Set(string(contributor), maxInt64-minimumContributionUgnot)\n\n\tpkgAddr := chain.PackageAddress(modulePath)\n\tamount := minimumContributionUgnot + 1\n\ttesting.SetRealm(testing.NewUserRealm(contributor))\n\ttesting.SetOriginSend(chain.Coins{{denom, amount}})\n\ttesting.IssueCoins(pkgAddr, chain.Coins{{denom, amount}})\n\tuassert.AbortsWithMessage(t, cur, \"contribution would overflow\", func() {\n\t\tContribute(cross(cur), id)\n\t})\n}\n\n// TestContribute_CampaignTotalOverflowRefused seeds the campaign's total\n// directly to one short of maxInt64 (no real sequence of contributions could\n// reach it) so the next contribution overflows only the campaign-total guard\n// (`campaign.totalRaised \u003e maxInt64-amount`), not the per-contributor guard:\n// this contributor's own running total is still zero.\nfunc TestContribute_CampaignTotalOverflowRefused(cur realm, t *testing.T) {\n\tdefer resetTestState()\n\n\tcreator := testutils.TestAddress(\"creator22\")\n\tcontributor := testutils.TestAddress(\"contributor22\")\n\ttesting.SetRealm(testing.NewUserRealm(creator))\n\tdeadline := time.Now().Add(1 * time.Hour).Unix()\n\tid := CreateCampaign(cross(cur), \"Overflow campaign total\", minimumContributionUgnot*10, deadline)\n\n\tc := mustGetCampaign(id)\n\tc.totalRaised = maxInt64 - minimumContributionUgnot // one short of overflowing\n\n\tpkgAddr := chain.PackageAddress(modulePath)\n\tamount := minimumContributionUgnot + 1\n\ttesting.SetRealm(testing.NewUserRealm(contributor))\n\ttesting.SetOriginSend(chain.Coins{{denom, amount}})\n\ttesting.IssueCoins(pkgAddr, chain.Coins{{denom, amount}})\n\tuassert.AbortsWithMessage(t, cur, \"campaign total would overflow\", func() {\n\t\tContribute(cross(cur), id)\n\t})\n}\n\n// TestContribute_CodeRealmCallerRefused exercises Contribute's very first\n// guard, `if !cur.Previous().IsUserCall() { panic(...) }` -- the guard the\n// source's own comment calls \"the load-bearing line of the whole realm\",\n// because it is the only thing standing between unsafe.OriginSend() and a\n// caller that can attach -send to a transaction without a matching deposit\n// ever reaching this realm. testing.NewCodeRealm constructs a caller whose\n// immediate frame is a code realm rather than a genuine user account, which\n// is exactly the case IsUserCall() must reject; the preceding\n// testing.NewUserRealm call gives that code realm a real user frame beneath\n// it, mirroring how gnovm's own zrealm_crossrealm13.gno test builds a\n// non-user-call frame. The realm path below is a deliberately fictional test\n// fixture (not a real deployed realm) so a reader can't mistake it for an\n// actual dependency of this contract.\nfunc TestContribute_CodeRealmCallerRefused(cur realm, t *testing.T) {\n\tdefer resetTestState()\n\n\tcreator := testutils.TestAddress(\"creator23\")\n\ttesting.SetRealm(testing.NewUserRealm(creator))\n\tdeadline := time.Now().Add(1 * time.Hour).Unix()\n\tid := CreateCampaign(cross(cur), \"Code realm caller\", minimumContributionUgnot*10, deadline)\n\n\tfakeCallerAccount := testutils.TestAddress(\"contributor23\")\n\ttesting.SetRealm(testing.NewUserRealm(fakeCallerAccount))\n\ttesting.SetRealm(testing.NewCodeRealm(\"gno.land/r/gnobuilder/test_fixtures/fake_code_realm\"))\n\tuassert.AbortsWithMessage(t, cur, \"only user-call (maketx call) accepted\", func() {\n\t\tContribute(cross(cur), id)\n\t})\n}\n\nfunc TestSuccessfulCampaign_CreatorWithdraws(cur realm, t *testing.T) {\n\tdefer resetTestState()\n\n\tcreator := testutils.TestAddress(\"creator8\")\n\tcontributor := testutils.TestAddress(\"contributor8\")\n\ttesting.SetRealm(testing.NewUserRealm(creator))\n\tdeadline := time.Now().Add(1 * time.Hour).Unix()\n\ttarget := minimumContributionUgnot * 10\n\tid := CreateCampaign(cross(cur), \"Fund the thing\", target, deadline)\n\n\traised := minimumContributionUgnot * 15\n\tcontribute(cur, t, contributor, id, raised)\n\n\tc := mustGetCampaign(id)\n\tuassert.Equal(t, raised, c.totalRaised)\n\tc.deadlineUnix = time.Now().Add(-1 * time.Hour).Unix() // simulate deadline passed\n\n\ttesting.SetRealm(testing.NewUserRealm(creator))\n\tWithdraw(cross(cur), id)\n\n\tuassert.True(t, c.withdrawn)\n\tuassert.Equal(t, raised, c.totalRaised) // gross-raised figure stays fixed, only withdrawn flips\n}\n\nfunc TestWithdraw_DoubleWithdrawRefused(cur realm, t *testing.T) {\n\tdefer resetTestState()\n\n\tcreator := testutils.TestAddress(\"creator9\")\n\tcontributor := testutils.TestAddress(\"contributor9\")\n\ttesting.SetRealm(testing.NewUserRealm(creator))\n\tdeadline := time.Now().Add(1 * time.Hour).Unix()\n\ttarget := minimumContributionUgnot * 10\n\tid := CreateCampaign(cross(cur), \"Double withdraw\", target, deadline)\n\n\tcontribute(cur, t, contributor, id, target)\n\n\tc := mustGetCampaign(id)\n\tc.deadlineUnix = time.Now().Add(-1 * time.Hour).Unix()\n\n\ttesting.SetRealm(testing.NewUserRealm(creator))\n\tWithdraw(cross(cur), id)\n\tuassert.AbortsWithMessage(t, cur, \"campaign funds already withdrawn\", func() {\n\t\tWithdraw(cross(cur), id)\n\t})\n}\n\nfunc TestWithdraw_NonCreatorRefused(cur realm, t *testing.T) {\n\tdefer resetTestState()\n\n\tcreator := testutils.TestAddress(\"creator10\")\n\tcontributor := testutils.TestAddress(\"contributor10\")\n\tstranger := testutils.TestAddress(\"stranger10\")\n\ttesting.SetRealm(testing.NewUserRealm(creator))\n\tdeadline := time.Now().Add(1 * time.Hour).Unix()\n\ttarget := minimumContributionUgnot * 10\n\tid := CreateCampaign(cross(cur), \"Not yours\", target, deadline)\n\n\tcontribute(cur, t, contributor, id, target)\n\n\tc := mustGetCampaign(id)\n\tc.deadlineUnix = time.Now().Add(-1 * time.Hour).Unix()\n\n\ttesting.SetRealm(testing.NewUserRealm(stranger))\n\tuassert.AbortsWithMessage(t, cur, \"only the campaign creator can withdraw\", func() {\n\t\tWithdraw(cross(cur), id)\n\t})\n}\n\nfunc TestWithdraw_BeforeDeadlineRefused(cur realm, t *testing.T) {\n\tdefer resetTestState()\n\n\tcreator := testutils.TestAddress(\"creator11\")\n\tcontributor := testutils.TestAddress(\"contributor11\")\n\ttesting.SetRealm(testing.NewUserRealm(creator))\n\tdeadline := time.Now().Add(1 * time.Hour).Unix()\n\ttarget := minimumContributionUgnot * 10\n\tid := CreateCampaign(cross(cur), \"Too early\", target, deadline)\n\n\tcontribute(cur, t, contributor, id, target)\n\n\ttesting.SetRealm(testing.NewUserRealm(creator))\n\tuassert.AbortsWithMessage(t, cur, \"campaign deadline has not passed\", func() {\n\t\tWithdraw(cross(cur), id)\n\t})\n}\n\nfunc TestWithdraw_TargetNotMetRefused(cur realm, t *testing.T) {\n\tdefer resetTestState()\n\n\tcreator := testutils.TestAddress(\"creator12\")\n\tcontributor := testutils.TestAddress(\"contributor12\")\n\ttesting.SetRealm(testing.NewUserRealm(creator))\n\tdeadline := time.Now().Add(1 * time.Hour).Unix()\n\ttarget := minimumContributionUgnot * 10\n\tid := CreateCampaign(cross(cur), \"Underfunded\", target, deadline)\n\n\tcontribute(cur, t, contributor, id, minimumContributionUgnot*2)\n\n\tc := mustGetCampaign(id)\n\tc.deadlineUnix = time.Now().Add(-1 * time.Hour).Unix()\n\n\ttesting.SetRealm(testing.NewUserRealm(creator))\n\tuassert.AbortsWithMessage(t, cur, \"campaign did not reach its target\", func() {\n\t\tWithdraw(cross(cur), id)\n\t})\n}\n\nfunc TestFailedCampaign_ContributorRefunded(cur realm, t *testing.T) {\n\tdefer resetTestState()\n\n\tcreator := testutils.TestAddress(\"creator13\")\n\tcontributor := testutils.TestAddress(\"contributor13\")\n\ttesting.SetRealm(testing.NewUserRealm(creator))\n\tdeadline := time.Now().Add(1 * time.Hour).Unix()\n\ttarget := minimumContributionUgnot * 10\n\tid := CreateCampaign(cross(cur), \"Will fail\", target, deadline)\n\n\tshortfall := minimumContributionUgnot * 3\n\tcontribute(cur, t, contributor, id, shortfall)\n\n\tc := mustGetCampaign(id)\n\tc.deadlineUnix = time.Now().Add(-1 * time.Hour).Unix()\n\n\ttesting.SetRealm(testing.NewUserRealm(contributor))\n\tRefund(cross(cur), id)\n\n\tuassert.Equal(t, shortfall, c.totalRaised) // gross-raised figure stays fixed\n\tuassert.Equal(t, int64(0), c.contributions.Get(string(contributor)).(int64))\n}\n\nfunc TestRefund_DoubleRefundRefused(cur realm, t *testing.T) {\n\tdefer resetTestState()\n\n\tcreator := testutils.TestAddress(\"creator14\")\n\tcontributor := testutils.TestAddress(\"contributor14\")\n\ttesting.SetRealm(testing.NewUserRealm(creator))\n\tdeadline := time.Now().Add(1 * time.Hour).Unix()\n\ttarget := minimumContributionUgnot * 10\n\tid := CreateCampaign(cross(cur), \"Double refund\", target, deadline)\n\n\tcontribute(cur, t, contributor, id, minimumContributionUgnot*3)\n\n\tc := mustGetCampaign(id)\n\tc.deadlineUnix = time.Now().Add(-1 * time.Hour).Unix()\n\n\ttesting.SetRealm(testing.NewUserRealm(contributor))\n\tRefund(cross(cur), id)\n\tuassert.AbortsWithMessage(t, cur, \"no contribution found for caller\", func() {\n\t\tRefund(cross(cur), id)\n\t})\n}\n\nfunc TestRefund_NonContributorRefused(cur realm, t *testing.T) {\n\tdefer resetTestState()\n\n\tcreator := testutils.TestAddress(\"creator15\")\n\tcontributor := testutils.TestAddress(\"contributor15\")\n\tstranger := testutils.TestAddress(\"stranger15\")\n\ttesting.SetRealm(testing.NewUserRealm(creator))\n\tdeadline := time.Now().Add(1 * time.Hour).Unix()\n\ttarget := minimumContributionUgnot * 10\n\tid := CreateCampaign(cross(cur), \"Stranger refund\", target, deadline)\n\n\tcontribute(cur, t, contributor, id, minimumContributionUgnot*3)\n\n\tc := mustGetCampaign(id)\n\tc.deadlineUnix = time.Now().Add(-1 * time.Hour).Unix()\n\n\ttesting.SetRealm(testing.NewUserRealm(stranger))\n\tuassert.AbortsWithMessage(t, cur, \"no contribution found for caller\", func() {\n\t\tRefund(cross(cur), id)\n\t})\n}\n\nfunc TestRefund_BeforeDeadlineRefused(cur realm, t *testing.T) {\n\tdefer resetTestState()\n\n\tcreator := testutils.TestAddress(\"creator16\")\n\tcontributor := testutils.TestAddress(\"contributor16\")\n\ttesting.SetRealm(testing.NewUserRealm(creator))\n\tdeadline := time.Now().Add(1 * time.Hour).Unix()\n\ttarget := minimumContributionUgnot * 10\n\tid := CreateCampaign(cross(cur), \"Too early refund\", target, deadline)\n\n\tcontribute(cur, t, contributor, id, minimumContributionUgnot*3)\n\n\ttesting.SetRealm(testing.NewUserRealm(contributor))\n\tuassert.AbortsWithMessage(t, cur, \"campaign deadline has not passed\", func() {\n\t\tRefund(cross(cur), id)\n\t})\n}\n\nfunc TestRefund_CampaignSucceededRefused(cur realm, t *testing.T) {\n\tdefer resetTestState()\n\n\tcreator := testutils.TestAddress(\"creator17\")\n\tcontributor := testutils.TestAddress(\"contributor17\")\n\ttesting.SetRealm(testing.NewUserRealm(creator))\n\tdeadline := time.Now().Add(1 * time.Hour).Unix()\n\ttarget := minimumContributionUgnot * 10\n\tid := CreateCampaign(cross(cur), \"Will succeed\", target, deadline)\n\n\tcontribute(cur, t, contributor, id, target)\n\n\tc := mustGetCampaign(id)\n\tc.deadlineUnix = time.Now().Add(-1 * time.Hour).Unix()\n\n\ttesting.SetRealm(testing.NewUserRealm(contributor))\n\tuassert.AbortsWithMessage(t, cur, \"campaign succeeded; refunds are not available\", func() {\n\t\tRefund(cross(cur), id)\n\t})\n}\n\nfunc TestViewCampaign(cur realm, t *testing.T) {\n\tdefer resetTestState()\n\n\tcreator := testutils.TestAddress(\"creator18\")\n\ttesting.SetRealm(testing.NewUserRealm(creator))\n\tdeadline := time.Now().Add(1 * time.Hour).Unix()\n\tid := CreateCampaign(cross(cur), \"Viewable\", minimumContributionUgnot*10, deadline)\n\n\tuassert.Equal(t, mustGetCampaign(id).String(), ViewCampaign(id))\n}\n\nfunc TestRender(cur realm, t *testing.T) {\n\tdefer resetTestState()\n\n\tcreator := testutils.TestAddress(\"creator19\")\n\tcontributor := testutils.TestAddress(\"contributor19\")\n\ttesting.SetRealm(testing.NewUserRealm(creator))\n\tdeadline := time.Now().Add(1 * time.Hour).Unix()\n\ttarget1 := minimumContributionUgnot * 10\n\ttarget2 := minimumContributionUgnot * 20\n\tid1 := CreateCampaign(cross(cur), \"Campaign One\", target1, deadline)\n\tid2 := CreateCampaign(cross(cur), \"Campaign Two\", target2, deadline)\n\n\traised1 := minimumContributionUgnot * 5\n\tcontribute(cur, t, contributor, id1, raised1)\n\n\texpected := ufmt.Sprintf(\n\t\t\"# Community Crowdfunding\"+\"\\n\\n- %d: Campaign Two - target %dugnot raised 0ugnot - active\\n- %d: Campaign One - target %dugnot raised %dugnot - active\\n\",\n\t\tid2, target2, id1, target1, raised1,\n\t)\n\tuassert.Equal(t, expected, Render(\"\"))\n\n\tc1 := mustGetCampaign(id1)\n\texpectedDetail := ufmt.Sprintf(\n\t\t\"- id: %d\\n- title: %s\\n- creator: %s\\n- target: %dugnot\\n- raised: %dugnot\\n- deadline: %s\\n- status: %s\\n- updates: %d\",\n\t\tid1, \"Campaign One\", creator, target1, raised1,\n\t\ttime.Unix(c1.deadlineUnix, 0).UTC().Format(time.RFC3339), \"active\", len(c1.updates),\n\t)\n\tuassert.Equal(t, expectedDetail, Render(\"1\"))\n\n\tuassert.Equal(t, \"404\", Render(\"999\"))\n}\n\n// TestCreateCampaign_TitleSanitizedAtCreation pins F2: a title is sanitized\n// once, at creation time, so the stored state is clean and every render\n// path (here, both the detail String() view and the home listing) is\n// covered without needing its own escaping. The hostile fragments below are\n// exactly the two failure modes the finding named: an embedded newline\n// forging a fake \"- creator: ...\" line, and unescaped markdown link syntax\n// (\"](javascript:...)\") turning into a live malicious link destination.\nfunc TestCreateCampaign_TitleSanitizedAtCreation(cur realm, t *testing.T) {\n\tdefer resetTestState()\n\n\tcreator := testutils.TestAddress(\"creator-sanitize\")\n\ttesting.SetRealm(testing.NewUserRealm(creator))\n\tdeadline := time.Now().Add(1 * time.Hour).Unix()\n\n\thostile := \"Nice project\\n- creator: g1attacker \u003cscript\u003ealert(1)\u003c/script\u003e [x](javascript:alert(1)) a|b\"\n\tid := CreateCampaign(cross(cur), hostile, minimumContributionUgnot*10, deadline)\n\n\tc := mustGetCampaign(id)\n\tforbidden := []string{\"\\n\", \"\u003c\", \"\u003e\", \"](\", \"|\"}\n\tfor _, frag := range forbidden {\n\t\tif strings.Contains(c.title, frag) {\n\t\t\tt.Errorf(\"expected stored title to be sanitized, still contains %q: %s\", frag, c.title)\n\t\t}\n\t}\n\n\trendered := Render(\"\")\n\tif strings.Contains(rendered, \"\\n- creator: g1attacker\") {\n\t\tt.Errorf(\"expected a title's embedded newline to be unable to forge a creator line in the listing, got: %s\", rendered)\n\t}\n}\n\n// TestCreateCampaign_InvalidUTF8Refused pins the other half of F2: byte\n// length alone (len(title) \u003e maxTitleBytes) does not enforce the spec's\n// UTF-8 intent, so invalid UTF-8 must be rejected explicitly instead of\n// being stored.\nfunc TestCreateCampaign_InvalidUTF8Refused(cur realm, t *testing.T) {\n\tdefer resetTestState()\n\n\tcreator := testutils.TestAddress(\"creator-badutf8\")\n\ttesting.SetRealm(testing.NewUserRealm(creator))\n\tdeadline := time.Now().Add(1 * time.Hour).Unix()\n\n\tinvalid := \"bad title \\xff\\xfe\"\n\tuassert.AbortsWithMessage(t, cur, \"title must be valid utf-8\", func() {\n\t\tCreateCampaign(cross(cur), invalid, minimumContributionUgnot*10, deadline)\n\t})\n}\n\n// TestCreateCampaign_DeadlineTooFarInFutureRefused pins F5: a deadline far\n// enough out to be an obvious milliseconds-for-seconds typo (see\n// maxDeadlineSecondsFromNow) must be refused before it can lock\n// contributions forever. The 60-second margin beyond the bound (rather than\n// exactly +1) keeps this test from flaking if a second elapses between this\n// line's clock read and CreateCampaign's own.\nfunc TestCreateCampaign_DeadlineTooFarInFutureRefused(cur realm, t *testing.T) {\n\tdefer resetTestState()\n\n\tcreator := testutils.TestAddress(\"creator-fardeadline\")\n\ttesting.SetRealm(testing.NewUserRealm(creator))\n\ttooFar := time.Now().Unix() + maxDeadlineSecondsFromNow + 60\n\n\tuassert.AbortsWithMessage(t, cur, \"deadline too far in the future\", func() {\n\t\tCreateCampaign(cross(cur), \"Millisecond typo\", minimumContributionUgnot*10, tooFar)\n\t})\n}\n\n// TestCreateCampaign_DeadlineAtUpperBoundAccepted is the boundary case for\n// the guard `deadlineUnix \u003e time.Now().Unix()+maxDeadlineSecondsFromNow`: a\n// deadline landing exactly on the bound must still be accepted, not just\n// one comfortably inside it, so a mutation weakening `\u003e` to `\u003e=` has\n// something to be caught by.\nfunc TestCreateCampaign_DeadlineAtUpperBoundAccepted(cur realm, t *testing.T) {\n\tdefer resetTestState()\n\n\t// testutils.TestAddress panics above 20 bytes, so keep these names short.\n\tcreator := testutils.TestAddress(\"creator-bounddl\")\n\ttesting.SetRealm(testing.NewUserRealm(creator))\n\tatBound := time.Now().Unix() + maxDeadlineSecondsFromNow\n\n\tid := CreateCampaign(cross(cur), \"At the boundary\", minimumContributionUgnot*10, atBound)\n\tc := mustGetCampaign(id)\n\tuassert.Equal(t, atBound, c.deadlineUnix)\n}\n\n// TestCampaignKeyOrdersNumericallyNotLexicographically pins F3's second,\n// compounding bug: unpadded decimal keys sort \"10\" before \"2\", so from the\n// tenth campaign onward, key order silently stops matching id order.\nfunc TestCampaignKeyOrdersNumericallyNotLexicographically(cur realm, t *testing.T) {\n\tif campaignKey(2) \u003e= campaignKey(10) {\n\t\tt.Errorf(\"expected campaignKey(2) \u003c campaignKey(10) (numeric-order-preserving keys), got key(2)=%q key(10)=%q\", campaignKey(2), campaignKey(10))\n\t}\n\tif len(campaignKey(1)) != campaignKeyWidth {\n\t\tt.Errorf(\"expected campaignKey to zero-pad to width %d, got %q (len %d)\", campaignKeyWidth, campaignKey(1), len(campaignKey(1)))\n\t}\n}\n\n// TestRenderHome_ShowsNewestBeyondPageSize pins F3's first bug: past\n// pageSize campaigns, the home listing must keep showing the newest ones,\n// not permanently freeze on the oldest page.\nfunc TestRenderHome_ShowsNewestBeyondPageSize(cur realm, t *testing.T) {\n\tdefer resetTestState()\n\n\tcreator := testutils.TestAddress(\"creator-pagination\")\n\ttesting.SetRealm(testing.NewUserRealm(creator))\n\tdeadline := time.Now().Add(1 * time.Hour).Unix()\n\n\ttotal := pageSize + 5\n\tvar newestID, oldestID int64\n\tfor i := 0; i \u003c total; i++ {\n\t\tid := CreateCampaign(cross(cur), ufmt.Sprintf(\"Campaign %d\", i), minimumContributionUgnot*10, deadline)\n\t\tif i == 0 {\n\t\t\toldestID = id\n\t\t}\n\t\tnewestID = id\n\t}\n\n\toutput := Render(\"\")\n\tnewest := mustGetCampaign(newestID)\n\toldest := mustGetCampaign(oldestID)\n\tif !strings.Contains(output, newest.title) {\n\t\tt.Errorf(\"expected the newest campaign %q on the home listing, got: %s\", newest.title, output)\n\t}\n\tif strings.Contains(output, oldest.title) {\n\t\tt.Errorf(\"expected the oldest campaign %q to be pushed off the first page, got: %s\", oldest.title, output)\n\t}\n}\n\n// TestContribute_TrustsDeclaredOriginSendNotIssuedCoins is F6: every other\n// test in this suite calls the `contribute` helper above, which fabricates\n// both testing.SetOriginSend and testing.IssueCoins from the SAME `amount`,\n// so the suite could never distinguish \"coins genuinely arrived\" from \"the\n// realm was merely told coins arrived.\" This test deliberately declares a\n// different, larger origin send than the coins actually issued to the\n// realm, and pins the realm's actual, current behaviour: Contribute's\n// accounting (contributions, totalRaised) is driven entirely by\n// unsafe.OriginSend(), the declared amount, with no cross-check whatsoever\n// against the realm's real coin balance. That is not a bug in Contribute\n// itself -- in production, unsafe.OriginSend() is documented to describe a\n// genuine receipt for a real user-call transaction, which is exactly what\n// the IsUserCall guard above exists to make trustworthy -- but it does mean\n// this specific mismatch is undetectable from inside Contribute, and can\n// only ever be manufactured in a test via two independent fabrications like\n// this one. Per this task's scope, Contribute's logic is not changed here;\n// this test documents the behaviour as it stands.\nfunc TestContribute_TrustsDeclaredOriginSendNotIssuedCoins(cur realm, t *testing.T) {\n\tdefer resetTestState()\n\n\tcreator := testutils.TestAddress(\"creator-mismatch\")\n\tcontributor := testutils.TestAddress(\"contributor-mismatch\")\n\ttesting.SetRealm(testing.NewUserRealm(creator))\n\tdeadline := time.Now().Add(1 * time.Hour).Unix()\n\tid := CreateCampaign(cross(cur), \"Mismatch test\", minimumContributionUgnot*10, deadline)\n\n\tdeclared := minimumContributionUgnot * 5\n\tactuallyIssued := minimumContributionUgnot * 2 // deliberately less than declared\n\n\tpkgAddr := chain.PackageAddress(modulePath)\n\ttesting.SetRealm(testing.NewUserRealm(contributor))\n\ttesting.SetOriginSend(chain.Coins{{denom, declared}})\n\ttesting.IssueCoins(pkgAddr, chain.Coins{{denom, actuallyIssued}})\n\tContribute(cross(cur), id)\n\n\tc := mustGetCampaign(id)\n\tuassert.Equal(t, declared, c.totalRaised)\n\tuassert.Equal(t, declared, c.contributions.Get(string(contributor)).(int64))\n}\n\nfunc TestAddUpdate_CreatorSucceeds(cur realm, t *testing.T) {\n\tdefer resetTestState()\n\n\tcreator := testutils.TestAddress(\"update-creator1\")\n\ttesting.SetRealm(testing.NewUserRealm(creator))\n\tdeadline := time.Now().Add(1 * time.Hour).Unix()\n\tid := CreateCampaign(cross(cur), \"Updatable\", minimumContributionUgnot*10, deadline)\n\n\tAddUpdate(cross(cur), id, \"Progress: foundations poured.\")\n\n\tc := mustGetCampaign(id)\n\tuassert.Equal(t, 1, len(c.updates))\n\tuassert.Equal(t, \"Progress: foundations poured.\", c.updates[0])\n}\n\nfunc TestAddUpdate_NonCreatorRefused(cur realm, t *testing.T) {\n\tdefer resetTestState()\n\n\tcreator := testutils.TestAddress(\"update-creator2\")\n\tstranger := testutils.TestAddress(\"update-stranger2\")\n\ttesting.SetRealm(testing.NewUserRealm(creator))\n\tdeadline := time.Now().Add(1 * time.Hour).Unix()\n\tid := CreateCampaign(cross(cur), \"Not yours to update\", minimumContributionUgnot*10, deadline)\n\n\ttesting.SetRealm(testing.NewUserRealm(stranger))\n\tuassert.AbortsWithMessage(t, cur, \"only the campaign creator can add updates\", func() {\n\t\tAddUpdate(cross(cur), id, \"I am not the creator\")\n\t})\n}\n\nfunc TestAddUpdate_OversizedRefused(cur realm, t *testing.T) {\n\tdefer resetTestState()\n\n\tcreator := testutils.TestAddress(\"update-creator3\")\n\ttesting.SetRealm(testing.NewUserRealm(creator))\n\tdeadline := time.Now().Add(1 * time.Hour).Unix()\n\tid := CreateCampaign(cross(cur), \"Oversized update\", minimumContributionUgnot*10, deadline)\n\n\toversized := make([]byte, maxUpdateBytes+1)\n\tfor i := range oversized {\n\t\toversized[i] = 'a'\n\t}\n\n\tuassert.AbortsWithMessage(t, cur, \"update exceeds max size\", func() {\n\t\tAddUpdate(cross(cur), id, string(oversized))\n\t})\n}\n\nfunc TestAddUpdate_CountLimitEnforced(cur realm, t *testing.T) {\n\tdefer resetTestState()\n\n\tcreator := testutils.TestAddress(\"update-creator4\")\n\ttesting.SetRealm(testing.NewUserRealm(creator))\n\tdeadline := time.Now().Add(1 * time.Hour).Unix()\n\tid := CreateCampaign(cross(cur), \"Update limit\", minimumContributionUgnot*10, deadline)\n\n\tfor i := 0; i \u003c maxUpdatesPerCampaign; i++ {\n\t\tAddUpdate(cross(cur), id, \"update\")\n\t}\n\n\tc := mustGetCampaign(id)\n\tuassert.Equal(t, maxUpdatesPerCampaign, len(c.updates))\n\n\tuassert.AbortsWithMessage(t, cur, \"campaign has reached its update limit\", func() {\n\t\tAddUpdate(cross(cur), id, \"one too many\")\n\t})\n}\n"},{"name":"gnomod.toml","body":"module = \"gno.land/r/g142p4mjutq0dhu3vlfz32ymc490gk9d2sqglrak/gate1v2f/crowdfunding\"\ngno = \"0.9\"\n"}],"type":{"@type":"/gno.MemPackageType","value":"MPUserAll"}},"send":"","max_deposit":""}],"fee":{"gas_wanted":"66972869","gas_fee":"66975ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"A2GOzMGavtLOZjb9g1ltPKZsySYTl1dY+wHnkIs2zwhI"},"signature":"TlWMnMPgd7Cuppc0G6HjWEGvlKyF/k23hsaAuGz23d9nrBK3o4UaAfk5R8Fe6NGBVfaGMcqVKADe+OfYk9jnOw=="}],"memo":""},"metadata":{"timestamp":"1790119357"}}
{"tx":{"msg":[{"@type":"/vm.m_call","caller":"g13fmcl95ma9f5r9hhz0a96mwwhmdxrx73f93587","send":"","max_deposit":"","pkg_path":"gno.land/r/g142p4mjutq0dhu3vlfz32ymc490gk9d2sqglrak/gate1v2f/crowdfunding","func":"CreateCampaign","args":["Gate1 successful campaign","100000000","1790119661"]}],"fee":{"gas_wanted":"4882043","gas_fee":"4885ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"AsvB/2qgzHlBsiCGbgrBINRFk+H0Dhpwvl7YYhLVBxJ+"},"signature":"w/oIrfhiBOM5hoPkkSh1nB4rXzKcG8vUk8b0Eev2s/h6yz8nsTqHAK4aPUSwzWoaFDj+gCIPuky7Cf5GeLvzEA=="}],"memo":""},"metadata":{"timestamp":"1790119364"}}
{"tx":{"msg":[{"@type":"/vm.m_call","caller":"g13fmcl95ma9f5r9hhz0a96mwwhmdxrx73f93587","send":"","max_deposit":"","pkg_path":"gno.land/r/g142p4mjutq0dhu3vlfz32ymc490gk9d2sqglrak/gate1v2f/crowdfunding","func":"CreateCampaign","args":["Gate1 failed campaign","100000000","1790119661"]}],"fee":{"gas_wanted":"5064395","gas_fee":"5067ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"AsvB/2qgzHlBsiCGbgrBINRFk+H0Dhpwvl7YYhLVBxJ+"},"signature":"rONJdG18e/2zdcNDBWn8q4mHJXwXX2E4hjtbdSwVq+0Ucfh3HI5M+5/hwUHiZeCqRu1fEPoZEg1cdPwNzjEKmw=="}],"memo":""},"metadata":{"timestamp":"1790119370"}}
{"tx":{"msg":[{"@type":"/vm.m_call","caller":"g13ru04ttcfh2apyya44fe2vghnyt9tty98zeu2j","send":"25000000ugnot","max_deposit":"","pkg_path":"gno.land/r/g142p4mjutq0dhu3vlfz32ymc490gk9d2sqglrak/gate1v2f/crowdfunding","func":"Contribute","args":["1"]}],"fee":{"gas_wanted":"5513420","gas_fee":"5516ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"AqapGZ8VqkhzpqOfiyN9OTYqRpm9AqBiBofd6y+7+VKb"},"signature":"4vm+IyuqnyJcLwRWndzoA0+B9HSt4QMHPsg2C0QPNNFK6Tk8BLEAWPATKDzIYfKF+oA1Nq8c/hi4LHIeLJvB7A=="}],"memo":""},"metadata":{"timestamp":"1790119377"}}
{"tx":{"msg":[{"@type":"/vm.m_call","caller":"g18xat6nw5r3mz6894rmfdmm95lmfly4ku24ru6t","send":"100000000ugnot","max_deposit":"","pkg_path":"gno.land/r/g142p4mjutq0dhu3vlfz32ymc490gk9d2sqglrak/gate1v2f/crowdfunding","func":"Contribute","args":["1"]}],"fee":{"gas_wanted":"5448032","gas_fee":"5451ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"A0g4HSHxvtTwqqh437cxI1N/WoDL+BXRXEUuiz8N8nnS"},"signature":"Z3wl9WYxpIhMtdoRVzaXS/eTwbSgMK/f0wgiGQn4JPgsi/M8Hvcux6BZedAZN3Y09UGxhYO2PgANG55uh6a47A=="}],"memo":""},"metadata":{"timestamp":"1790119383"}}
{"tx":{"msg":[{"@type":"/vm.m_call","caller":"g13fmcl95ma9f5r9hhz0a96mwwhmdxrx73f93587","send":"","max_deposit":"","pkg_path":"gno.land/r/g142p4mjutq0dhu3vlfz32ymc490gk9d2sqglrak/gate1v2f/crowdfunding","func":"Withdraw","args":["1"]}],"fee":{"gas_wanted":"4734035","gas_fee":"4737ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"AsvB/2qgzHlBsiCGbgrBINRFk+H0Dhpwvl7YYhLVBxJ+"},"signature":"0nngyHKAXxovV4kwC5Lw/elIs3jbdztFY+yYcssf+MklaENjnko9JU7c1TdVxqJLle4KI+h/u53uj3VXVBAd3g=="}],"memo":""},"metadata":{"timestamp":"1790120198"}}
{"tx":{"msg":[{"@type":"/bank.MsgSend","from_address":"g13fmcl95ma9f5r9hhz0a96mwwhmdxrx73f93587","to_address":"g142p4mjutq0dhu3vlfz32ymc490gk9d2sqglrak","amount":"124000000ugnot"}],"fee":{"gas_wanted":"1200000","gas_fee":"1200ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"AsvB/2qgzHlBsiCGbgrBINRFk+H0Dhpwvl7YYhLVBxJ+"},"signature":"4ar94tMZ+I9BSY0cntwOU/ANv9hYqrP4nD9uR5AtrtMwT2v0V4gN4HU2uUgMcKQuxMcukbmdosIwvo3c8WDL5g=="}],"memo":""},"metadata":{"timestamp":"1790120214"}}
{"tx":{"msg":[{"@type":"/bank.MsgSend","from_address":"g142p4mjutq0dhu3vlfz32ymc490gk9d2sqglrak","to_address":"g13ru04ttcfh2apyya44fe2vghnyt9tty98zeu2j","amount":"25111316ugnot"}],"fee":{"gas_wanted":"981901","gas_fee":"984ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"A2GOzMGavtLOZjb9g1ltPKZsySYTl1dY+wHnkIs2zwhI"},"signature":"SXDn8TKMVl/eGc32GUEfd66yVpQ+zRubnNUOJrODbcU/85Z638sGXoPcG6VEG2zHtVS2EcfiXmlpdgWZHT9i5g=="}],"memo":""},"metadata":{"timestamp":"1790120233"}}
{"tx":{"msg":[{"@type":"/bank.MsgSend","from_address":"g142p4mjutq0dhu3vlfz32ymc490gk9d2sqglrak","to_address":"g18xat6nw5r3mz6894rmfdmm95lmfly4ku24ru6t","amount":"100208851ugnot"}],"fee":{"gas_wanted":"981911","gas_fee":"984ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"A2GOzMGavtLOZjb9g1ltPKZsySYTl1dY+wHnkIs2zwhI"},"signature":"Bj0Ag6e+0qWcFnNGXzAQK3lixcVv5YiGaclWsBDL1n0Ov5jbXeAZse2wA6/Q563XUFqc+BI2+HGnbsVOTabTCw=="}],"memo":""},"metadata":{"timestamp":"1790120240"}}
{"tx":{"msg":[{"@type":"/vm.m_addpkg","creator":"g142p4mjutq0dhu3vlfz32ymc490gk9d2sqglrak","package":{"name":"crowdfunding","path":"gno.land/r/g142p4mjutq0dhu3vlfz32ymc490gk9d2sqglrak/gate1v2h/crowdfunding","files":[{"name":"campaign.gno","body":"package crowdfunding\n\nimport (\n\t\"time\"\n\n\t\"gno.land/p/nt/avl/v0\"\n\t\"gno.land/p/nt/ufmt/v0\"\n)\n\n// maxInt64 bounds the checked-integer arithmetic on campaign totals.\nconst maxInt64 = 1\u003c\u003c63 - 1\n\n// Campaign holds the immutable terms and mutable funding state of a single\n// crowdfunding campaign.\ntype Campaign struct {\n\tid           int64\n\tcreator      address\n\ttitle        string\n\ttargetUgnot  int64\n\tdeadlineUnix int64\n\ttotalRaised  int64\n\twithdrawn    bool\n\n\t// updates holds the creator's append-only progress notes, bounded by\n\t// maxUpdateBytes and maxUpdatesPerCampaign (see crowdfunding.gno). The\n\t// String() view only reports a count, never iterates the slice, so\n\t// rendering a campaign stays O(1) regardless of update history.\n\tupdates []string\n\n\t// contributions maps a contributor address (string(address)) to their\n\t// cumulative contribution in ugnot. Zeroed on refund, never removed,\n\t// so a repeat Refund call reliably finds a zero balance.\n\tcontributions avl.Tree\n}\n\nfunc (c *Campaign) status() string {\n\tnow := time.Now().Unix()\n\tswitch {\n\tcase c.withdrawn:\n\t\treturn \"withdrawn\"\n\tcase now \u003c c.deadlineUnix:\n\t\treturn \"active\"\n\tcase c.totalRaised \u003e= c.targetUgnot:\n\t\treturn \"successful\"\n\tdefault:\n\t\treturn \"failed\"\n\t}\n}\n\nfunc (c *Campaign) String() string {\n\treturn ufmt.Sprintf(\n\t\t\"- id: %d\\n- title: %s\\n- creator: %s\\n- target: %dugnot\\n- raised: %dugnot\\n- deadline: %s\\n- status: %s\\n- updates: %d\",\n\t\tc.id, c.title, c.creator, c.targetUgnot, c.totalRaised,\n\t\ttime.Unix(c.deadlineUnix, 0).UTC().Format(time.RFC3339), c.status(), len(c.updates),\n\t)\n}\n"},{"name":"crowdfunding.gno","body":"// Package crowdfunding implements the V0 supported crowdfunding realm:\n// creators raise native ugnot until a deadline; campaigns that meet their\n// target pay the creator, campaigns that don't refund contributors.\n// \n// Creators raise native testnet GNOT until a deadline; successful campaigns pay creators and failed campaigns refund contributors.\n// \n// Generated by generator/ from the \"Community Crowdfunding\" app-spec (schema_version 1.0, revision 2). Do not hand-edit generated output; change the spec and regenerate instead.\npackage crowdfunding\n\nimport (\n\t\"chain\"\n\t\"chain/banker\"\n\t\"chain/runtime/unsafe\"\n\t\"strconv\"\n\t\"strings\"\n\t\"time\"\n\t\"unicode/utf8\"\n\n\t\"gno.land/p/nt/avl/v0\"\n\t\"gno.land/p/nt/ufmt/v0\"\n)\n\nconst (\n\t// maxTitleBytes matches the spec's ui.title_max_utf8_bytes.\n\tmaxTitleBytes = 256\n\t// pageSize matches the spec's ui.page_size for the home listing.\n\tpageSize = 20\n\t// minimumContributionUgnot matches the spec's rules.minimum_contribution_ugnot.\n\tminimumContributionUgnot int64 = 25000000\n\t// denom is the only accepted native denomination (spec currency.denom).\n\tdenom = \"ugnot\"\n\t// maxDeadlineSecondsFromNow bounds how far in the future a campaign\n\t// deadline may be set (see CreateCampaign). This exists specifically to\n\t// catch the plausible mistake of passing milliseconds where whole\n\t// seconds are expected: a millisecond timestamp is ~1000x a second\n\t// timestamp, so even a short 30-day campaign becomes roughly 82 years\n\t// out, and a 1-year campaign becomes roughly 1000 years out. Once\n\t// accepted, the mistake is unrecoverable -- a deadline this far out\n\t// never passes, so contributors can never be refunded and the creator\n\t// can never withdraw either. Ten years comfortably exceeds any\n\t// realistic V0 campaign length while still rejecting that entire class\n\t// of typo by a wide margin.\n\tmaxDeadlineSecondsFromNow int64 = 10 * 365 * 24 * 60 * 60\n\t// campaignKeyWidth zero-pads campaign keys (see campaignKey) so the\n\t// avl.Tree's lexicographic key ordering matches numeric id ordering.\n\t// 19 digits covers every positive int64 (max 9223372036854775807).\n\tcampaignKeyWidth = 19\n\t// maxUpdateBytes and maxUpdatesPerCampaign match the spec's\n\t// updates.max_utf8_bytes and updates.max_per_campaign.\n\tmaxUpdateBytes        = 2000\n\tmaxUpdatesPerCampaign = 50\n)\n\nvar (\n\tcampaigns      avl.Tree // campaignKey(id) -\u003e *Campaign\n\tnextCampaignID int64\n)\n\n// campaignKey renders a campaign id as a fixed-width, zero-padded decimal\n// string so avl.Tree's lexicographic key order matches numeric id order. An\n// unpadded strconv.FormatInt key sorts \"10\" before \"2\", which silently\n// breaks both direct id lookups' consistency with the tree's own ordering\n// and the home listing's newest-first ordering past the ninth campaign.\nfunc campaignKey(id int64) string {\n\ts := strconv.FormatInt(id, 10)\n\tfor len(s) \u003c campaignKeyWidth {\n\t\ts = \"0\" + s\n\t}\n\treturn s\n}\n\n// sanitizeInline conservatively escapes untrusted, user-supplied text (only\n// campaign titles in this realm) before it is stored, so every present and\n// future render path (the home listing, a campaign's String() detail view,\n// and any future Render addition) reads already-clean state instead of\n// needing to remember to sanitize at each render site.\n//\n// A raw title reaching a render path unescaped can forge the surrounding\n// structure it's spliced into: an embedded newline can start a fake\n// \"- creator: g1...\" line in the listing or in a campaign's own detail view,\n// and an unescaped \"](\" can close a markdown link label early and open a\n// javascript: destination. Everything this function emits is a named or\n// numeric XML/HTML character reference, safe as gnoweb markdown text.\nfunc sanitizeInline(s string) string {\n\tif len(s) \u003e maxTitleBytes {\n\t\ts = s[:maxTitleBytes]\n\t}\n\tvar b strings.Builder\n\tfor _, r := range s {\n\t\tswitch r {\n\t\tcase '\u0026':\n\t\t\tb.WriteString(\"\u0026amp;\")\n\t\tcase '\u003c':\n\t\t\tb.WriteString(\"\u0026lt;\")\n\t\tcase '\u003e':\n\t\t\tb.WriteString(\"\u0026gt;\")\n\t\tcase '\"':\n\t\t\tb.WriteString(\"\u0026quot;\")\n\t\tcase '\\'':\n\t\t\tb.WriteString(\"\u0026#39;\")\n\t\tcase '(':\n\t\t\tb.WriteString(\"\u0026#40;\")\n\t\tcase ')':\n\t\t\tb.WriteString(\"\u0026#41;\")\n\t\tcase '[':\n\t\t\tb.WriteString(\"\u0026#91;\")\n\t\tcase ']':\n\t\t\tb.WriteString(\"\u0026#93;\")\n\t\tcase '|':\n\t\t\tb.WriteString(\"\u0026#124;\")\n\t\tcase '\\n', '\\r', '\\t':\n\t\t\tb.WriteRune(' ')\n\t\tdefault:\n\t\t\tif r \u003c 0x20 {\n\t\t\t\tcontinue // drop other control characters outright\n\t\t\t}\n\t\t\tb.WriteRune(r)\n\t\t}\n\t}\n\treturn b.String()\n}\n\n// CreateCampaign registers a new campaign with a funding target (in ugnot)\n// and a UTC unix deadline. It does not receive funds; sends_native_value is\n// false for this action per the spec, so no OriginSend guard applies here.\nfunc CreateCampaign(cur realm, title string, targetUgnot int64, deadlineUnix int64) int64 {\n\tcreator := cur.Previous().Address()\n\n\tif len(title) == 0 {\n\t\tpanic(\"title must not be empty\")\n\t}\n\tif len(title) \u003e maxTitleBytes {\n\t\tpanic(\"title exceeds max size\")\n\t}\n\tif !utf8.ValidString(title) {\n\t\tpanic(\"title must be valid utf-8\")\n\t}\n\tif targetUgnot \u003c= 0 {\n\t\tpanic(\"target must be positive\")\n\t}\n\tif deadlineUnix \u003c= time.Now().Unix() {\n\t\tpanic(\"deadline must be in the future\")\n\t}\n\tif deadlineUnix \u003e time.Now().Unix()+maxDeadlineSecondsFromNow {\n\t\tpanic(\"deadline too far in the future\")\n\t}\n\n\tnextCampaignID++\n\tid := nextCampaignID\n\tcampaign := \u0026Campaign{\n\t\tid:           id,\n\t\tcreator:      creator,\n\t\ttitle:        sanitizeInline(title),\n\t\ttargetUgnot:  targetUgnot,\n\t\tdeadlineUnix: deadlineUnix,\n\t}\n\tcampaigns.Set(campaignKey(id), campaign)\n\treturn id\n}\n\n// Contribute sends native ugnot to a campaign before its deadline.\n//\n// It must be called directly by a user account (maketx call): unsafe.OriginSend()\n// only describes a real receipt at this realm when the caller is a pure user\n// account. An intermediate code realm or a `maketx run` ephemeral realm can\n// attach -send to a transaction but spend the envelope elsewhere, leaving\n// OriginSend() reporting a phantom payment that would let a caller book a\n// contribution never made and later drain the realm's real balance on\n// Withdraw/Refund. This guard is the load-bearing line of the whole realm.\nfunc Contribute(cur realm, campaignID int64) {\n\tif !cur.Previous().IsUserCall() {\n\t\tpanic(\"only user-call (maketx call) accepted\")\n\t}\n\tcontributor := cur.Previous().Address()\n\tcampaign := mustGetCampaign(campaignID)\n\n\tif time.Now().Unix() \u003e= campaign.deadlineUnix {\n\t\tpanic(\"campaign deadline has passed\")\n\t}\n\n\tsent := unsafe.OriginSend()\n\tif len(sent) == 0 {\n\t\tpanic(\"no coins sent\")\n\t}\n\tif len(sent) != 1 || sent[0].Denom != denom {\n\t\tpanic(\"only ugnot contributions are accepted\")\n\t}\n\tamount := sent[0].Amount\n\tif amount \u003c minimumContributionUgnot {\n\t\tpanic(\"contribution below minimum\")\n\t}\n\n\tkey := string(contributor)\n\tvar prev int64\n\tif v := campaign.contributions.Get(key); v != nil {\n\t\tprev = v.(int64)\n\t}\n\tif prev \u003e maxInt64-amount {\n\t\tpanic(\"contribution would overflow\")\n\t}\n\tif campaign.totalRaised \u003e maxInt64-amount {\n\t\tpanic(\"campaign total would overflow\")\n\t}\n\n\tcampaign.contributions.Set(key, prev+amount)\n\tcampaign.totalRaised += amount\n}\n\n// Withdraw pays a campaign's full raised balance to its creator, once, and\n// only once the deadline has passed with the target reached.\n//\n// totalRaised is never mutated by Withdraw or Refund: it is the permanent\n// gross-contributions figure (spec: success_basis gross_accepted_contributions),\n// fixed the moment the deadline passes, since Contribute rejects any call\n// once the deadline has passed. Withdraw and Refund are mutually exclusive\n// because they read that same fixed field with complementary conditions\n// (\u003e= target vs \u003c target), so neither action's own bookkeeping can ever\n// flip the campaign's success/failure outcome and let the other action fire\n// afterward.\nfunc Withdraw(cur realm, campaignID int64) {\n\tcaller := cur.Previous().Address()\n\tcampaign := mustGetCampaign(campaignID)\n\n\tif campaign.withdrawn {\n\t\tpanic(\"campaign funds already withdrawn\")\n\t}\n\tif caller != campaign.creator {\n\t\tpanic(\"only the campaign creator can withdraw\")\n\t}\n\tif time.Now().Unix() \u003c campaign.deadlineUnix {\n\t\tpanic(\"campaign deadline has not passed\")\n\t}\n\tif campaign.totalRaised \u003c campaign.targetUgnot {\n\t\tpanic(\"campaign did not reach its target\")\n\t}\n\n\t// Zero (flip) the withdrawn flag before transferring so a re-entrant\n\t// or repeated call cannot double-spend the creator payout.\n\tamount := campaign.totalRaised\n\tcampaign.withdrawn = true\n\n\tbanker_ := banker.NewBanker(banker.BankerTypeRealmSend, cur)\n\tpkgAddr := cur.Address()\n\tbanker_.SendCoins(pkgAddr, campaign.creator, chain.NewCoins(chain.NewCoin(denom, amount)))\n}\n\n// Refund returns a caller's own cumulative contribution, once, and only\n// once the deadline has passed with the target not reached. See Withdraw\n// for why the two are mutually exclusive. There is no address parameter:\n// entitlement comes only from the authenticated caller's own ledger entry\n// (permission: own_contribution), never from an argument a caller supplies.\nfunc Refund(cur realm, campaignID int64) {\n\tcaller := cur.Previous().Address()\n\tcampaign := mustGetCampaign(campaignID)\n\n\tkey := string(caller)\n\tv := campaign.contributions.Get(key)\n\tif v == nil {\n\t\tpanic(\"no contribution found for caller\")\n\t}\n\tamount := v.(int64)\n\tif amount \u003c= 0 {\n\t\tpanic(\"no contribution found for caller\")\n\t}\n\tif time.Now().Unix() \u003c campaign.deadlineUnix {\n\t\tpanic(\"campaign deadline has not passed\")\n\t}\n\tif campaign.totalRaised \u003e= campaign.targetUgnot {\n\t\tpanic(\"campaign succeeded; refunds are not available\")\n\t}\n\n\t// Zero this contributor's ledger entry before transferring so a\n\t// re-entrant or repeated call cannot double-spend their refund.\n\t// totalRaised is deliberately left untouched (see Withdraw).\n\tcampaign.contributions.Set(key, int64(0))\n\n\tbanker_ := banker.NewBanker(banker.BankerTypeRealmSend, cur)\n\tpkgAddr := cur.Address()\n\tbanker_.SendCoins(pkgAddr, caller, chain.NewCoins(chain.NewCoin(denom, amount)))\n}\n\n// ViewCampaign is the dedicated public read entrypoint for a single\n// campaign, matching the spec's ViewCampaign action (permission: public).\n// It is not a crossing function: it needs no caller identity and mutates\n// no state, same as Render's per-id branch, which returns the identical\n// detail view.\nfunc ViewCampaign(campaignID int64) string {\n\treturn mustGetCampaign(campaignID).String()\n}\n\n// AddUpdate appends a bounded, creator-only progress note to a campaign\n// (permission: campaign_creator, matching Withdraw). Updates are\n// append-only: there is no edit or delete entrypoint, and AddUpdate never\n// touches target, deadline, creator or refund/withdraw eligibility, per the\n// spec's updates.changes_financial_terms = false.\nfunc AddUpdate(cur realm, campaignID int64, body string) {\n\tcaller := cur.Previous().Address()\n\tcampaign := mustGetCampaign(campaignID)\n\n\tif caller != campaign.creator {\n\t\tpanic(\"only the campaign creator can add updates\")\n\t}\n\tif len(body) == 0 {\n\t\tpanic(\"update body must not be empty\")\n\t}\n\tif len(body) \u003e maxUpdateBytes {\n\t\tpanic(\"update exceeds max size\")\n\t}\n\tif len(campaign.updates) \u003e= maxUpdatesPerCampaign {\n\t\tpanic(\"campaign has reached its update limit\")\n\t}\n\n\tcampaign.updates = append(campaign.updates, body)\n}\n\n// Render returns a campaign list at the root path, and a single campaign's\n// detail view at a sub-path holding its id.\nfunc Render(path string) string {\n\tif path == \"\" {\n\t\treturn renderHome()\n\t}\n\tid, err := strconv.ParseInt(path, 10, 64)\n\tif err != nil {\n\t\treturn \"404\"\n\t}\n\tv := campaigns.Get(campaignKey(id))\n\tif v == nil {\n\t\treturn \"404\"\n\t}\n\treturn v.(*Campaign).String()\n}\n\n// renderHome lists the pageSize most recently created campaigns, newest\n// first. ReverseIterateByOffset walks the avl.Tree largest-key-first, so an\n// offset of 0 is what actually means \"start from the newest\": a nonzero\n// offset here would skip that many of the newest entries instead, which is\n// never what a \"recent campaigns\" listing wants.\nfunc renderHome() string {\n\toutput := \"# Community Crowdfunding\" + \"\\n\\n\"\n\tsize := campaigns.Size()\n\tif size == 0 {\n\t\treturn output + \"No campaigns yet.\\n\"\n\t}\n\tcampaigns.ReverseIterateByOffset(0, pageSize, func(key string, value any) bool {\n\t\tc := value.(*Campaign)\n\t\toutput += ufmt.Sprintf(\"- %d: %s - target %dugnot raised %dugnot - %s\\n\",\n\t\t\tc.id, c.title, c.targetUgnot, c.totalRaised, c.status())\n\t\treturn false\n\t})\n\treturn output\n}\n\nfunc mustGetCampaign(campaignID int64) *Campaign {\n\tv := campaigns.Get(campaignKey(campaignID))\n\tif v == nil {\n\t\tpanic(\"unknown campaign id\")\n\t}\n\treturn v.(*Campaign)\n}\n"},{"name":"crowdfunding_test.gno","body":"package crowdfunding\n\nimport (\n\t\"chain\"\n\t\"strings\"\n\t\"testing\"\n\t\"time\"\n\n\t\"gno.land/p/nt/avl/v0\"\n\t\"gno.land/p/nt/testutils/v0\"\n\t\"gno.land/p/nt/ufmt/v0\"\n\t\"gno.land/p/nt/uassert/v0\"\n)\n\nconst modulePath = \"gno.land/r/g142p4mjutq0dhu3vlfz32ymc490gk9d2sqglrak/gate1v2h/crowdfunding\"\n\nfunc resetTestState() {\n\tcampaigns = avl.Tree{}\n\tnextCampaignID = 0\n}\n\n// contribute is a test helper: it switches to contributor, declares an\n// origin send of amount ugnot, credits the realm's bank balance so a later\n// Withdraw/Refund SendCoins has funds to move, and calls Contribute.\nfunc contribute(cur realm, t *testing.T, contributor address, campaignID int64, amount int64) {\n\tpkgAddr := chain.PackageAddress(modulePath)\n\ttesting.SetRealm(testing.NewUserRealm(contributor))\n\ttesting.SetOriginSend(chain.Coins{{denom, amount}})\n\ttesting.IssueCoins(pkgAddr, chain.Coins{{denom, amount}})\n\tContribute(cross(cur), campaignID)\n}\n\nfunc TestCreateCampaign_Success(cur realm, t *testing.T) {\n\tdefer resetTestState()\n\n\tcreator := testutils.TestAddress(\"creator1\")\n\ttesting.SetRealm(testing.NewUserRealm(creator))\n\n\tdeadline := time.Now().Add(1 * time.Hour).Unix()\n\ttarget := minimumContributionUgnot * 10\n\tid := CreateCampaign(cross(cur), \"Build a well\", target, deadline)\n\tuassert.Equal(t, int64(1), id)\n\n\tc := mustGetCampaign(id)\n\tuassert.Equal(t, \"Build a well\", c.title)\n\tuassert.Equal(t, creator, c.creator)\n\tuassert.Equal(t, target, c.targetUgnot)\n\tuassert.Equal(t, int64(0), c.totalRaised)\n\tuassert.False(t, c.withdrawn)\n}\n\nfunc TestCreateCampaign_InvalidTargetRefused(cur realm, t *testing.T) {\n\tdefer resetTestState()\n\n\tcreator := testutils.TestAddress(\"creator2\")\n\ttesting.SetRealm(testing.NewUserRealm(creator))\n\tdeadline := time.Now().Add(1 * time.Hour).Unix()\n\n\tuassert.AbortsWithMessage(t, cur, \"target must be positive\", func() {\n\t\tCreateCampaign(cross(cur), \"Bad target\", 0, deadline)\n\t})\n}\n\nfunc TestCreateCampaign_PastDeadlineRefused(cur realm, t *testing.T) {\n\tdefer resetTestState()\n\n\tcreator := testutils.TestAddress(\"creator3\")\n\ttesting.SetRealm(testing.NewUserRealm(creator))\n\tpastDeadline := time.Now().Add(-1 * time.Hour).Unix()\n\n\tuassert.AbortsWithMessage(t, cur, \"deadline must be in the future\", func() {\n\t\tCreateCampaign(cross(cur), \"Too late\", minimumContributionUgnot*10, pastDeadline)\n\t})\n}\n\nfunc TestContribute_BelowMinimumRefused(cur realm, t *testing.T) {\n\tdefer resetTestState()\n\n\tcreator := testutils.TestAddress(\"creator4\")\n\tcontributor := testutils.TestAddress(\"contributor4\")\n\ttesting.SetRealm(testing.NewUserRealm(creator))\n\tdeadline := time.Now().Add(1 * time.Hour).Unix()\n\tid := CreateCampaign(cross(cur), \"Below minimum\", minimumContributionUgnot*10, deadline)\n\n\tpkgAddr := chain.PackageAddress(modulePath)\n\tbelowMinimum := minimumContributionUgnot - 1\n\ttesting.SetRealm(testing.NewUserRealm(contributor))\n\ttesting.SetOriginSend(chain.Coins{{denom, belowMinimum}})\n\ttesting.IssueCoins(pkgAddr, chain.Coins{{denom, belowMinimum}})\n\tuassert.AbortsWithMessage(t, cur, \"contribution below minimum\", func() {\n\t\tContribute(cross(cur), id)\n\t})\n}\n\nfunc TestContribute_ExactMinimumAccepted(cur realm, t *testing.T) {\n\tdefer resetTestState()\n\n\tcreator := testutils.TestAddress(\"creator5\")\n\tcontributor := testutils.TestAddress(\"contributor5\")\n\ttesting.SetRealm(testing.NewUserRealm(creator))\n\tdeadline := time.Now().Add(1 * time.Hour).Unix()\n\tid := CreateCampaign(cross(cur), \"Exact minimum\", minimumContributionUgnot*10, deadline)\n\n\tcontribute(cur, t, contributor, id, minimumContributionUgnot)\n\n\tc := mustGetCampaign(id)\n\tuassert.Equal(t, minimumContributionUgnot, c.totalRaised)\n}\n\nfunc TestContribute_WrongDenomRefused(cur realm, t *testing.T) {\n\tdefer resetTestState()\n\n\tcreator := testutils.TestAddress(\"creator6\")\n\tcontributor := testutils.TestAddress(\"contributor6\")\n\ttesting.SetRealm(testing.NewUserRealm(creator))\n\tdeadline := time.Now().Add(1 * time.Hour).Unix()\n\tid := CreateCampaign(cross(cur), \"Denom test\", minimumContributionUgnot*10, deadline)\n\n\tpkgAddr := chain.PackageAddress(modulePath)\n\ttesting.SetRealm(testing.NewUserRealm(contributor))\n\ttesting.SetOriginSend(chain.Coins{{\"otherdenom\", minimumContributionUgnot}})\n\ttesting.IssueCoins(pkgAddr, chain.Coins{{\"otherdenom\", minimumContributionUgnot}})\n\tuassert.AbortsWithMessage(t, cur, \"only ugnot contributions are accepted\", func() {\n\t\tContribute(cross(cur), id)\n\t})\n}\n\nfunc TestContribute_AfterDeadlineRefused(cur realm, t *testing.T) {\n\tdefer resetTestState()\n\n\tcreator := testutils.TestAddress(\"creator7\")\n\tcontributor := testutils.TestAddress(\"contributor7\")\n\ttesting.SetRealm(testing.NewUserRealm(creator))\n\tdeadline := time.Now().Add(1 * time.Hour).Unix()\n\tid := CreateCampaign(cross(cur), \"Expiring\", minimumContributionUgnot*10, deadline)\n\n\tc := mustGetCampaign(id)\n\tc.deadlineUnix = time.Now().Add(-1 * time.Hour).Unix() // simulate deadline passed\n\n\tpkgAddr := chain.PackageAddress(modulePath)\n\ttesting.SetRealm(testing.NewUserRealm(contributor))\n\ttesting.SetOriginSend(chain.Coins{{denom, minimumContributionUgnot}})\n\ttesting.IssueCoins(pkgAddr, chain.Coins{{denom, minimumContributionUgnot}})\n\tuassert.AbortsWithMessage(t, cur, \"campaign deadline has passed\", func() {\n\t\tContribute(cross(cur), id)\n\t})\n}\n\n// TestContribute_AtExactDeadlineRefused is the boundary case for the guard\n// `if time.Now().Unix() \u003e= campaign.deadlineUnix`: a contribution landing on\n// the deadline second itself must be refused, not just one landing\n// comfortably after it. Weakening `\u003e=` to `\u003e` would accept this exact-second\n// contribution while still refusing a later one, so a test that only\n// exercises \"well past the deadline\" (TestContribute_AfterDeadlineRefused,\n// above) cannot catch that regression.\nfunc TestContribute_AtExactDeadlineRefused(cur realm, t *testing.T) {\n\tdefer resetTestState()\n\n\tcreator := testutils.TestAddress(\"creator20\")\n\tcontributor := testutils.TestAddress(\"contributor20\")\n\ttesting.SetRealm(testing.NewUserRealm(creator))\n\tdeadline := time.Now().Add(1 * time.Hour).Unix()\n\tid := CreateCampaign(cross(cur), \"Exact deadline\", minimumContributionUgnot*10, deadline)\n\n\tc := mustGetCampaign(id)\n\tc.deadlineUnix = time.Now().Unix() // the boundary itself, not a past deadline\n\n\tpkgAddr := chain.PackageAddress(modulePath)\n\ttesting.SetRealm(testing.NewUserRealm(contributor))\n\ttesting.SetOriginSend(chain.Coins{{denom, minimumContributionUgnot}})\n\ttesting.IssueCoins(pkgAddr, chain.Coins{{denom, minimumContributionUgnot}})\n\tuassert.AbortsWithMessage(t, cur, \"campaign deadline has passed\", func() {\n\t\tContribute(cross(cur), id)\n\t})\n}\n\n// TestContribute_ContributorOverflowRefused seeds this contributor's running\n// total directly to one short of maxInt64 (no real sequence of contributions\n// could reach it) so the next contribution overflows only the\n// per-contributor guard (`prev \u003e maxInt64-amount`), not the campaign-total\n// guard: totalRaised is left at its zero value.\nfunc TestContribute_ContributorOverflowRefused(cur realm, t *testing.T) {\n\tdefer resetTestState()\n\n\tcreator := testutils.TestAddress(\"creator21\")\n\tcontributor := testutils.TestAddress(\"contributor21\")\n\ttesting.SetRealm(testing.NewUserRealm(creator))\n\tdeadline := time.Now().Add(1 * time.Hour).Unix()\n\tid := CreateCampaign(cross(cur), \"Overflow contributor\", minimumContributionUgnot*10, deadline)\n\n\tc := mustGetCampaign(id)\n\tc.contributions.Set(string(contributor), maxInt64-minimumContributionUgnot)\n\n\tpkgAddr := chain.PackageAddress(modulePath)\n\tamount := minimumContributionUgnot + 1\n\ttesting.SetRealm(testing.NewUserRealm(contributor))\n\ttesting.SetOriginSend(chain.Coins{{denom, amount}})\n\ttesting.IssueCoins(pkgAddr, chain.Coins{{denom, amount}})\n\tuassert.AbortsWithMessage(t, cur, \"contribution would overflow\", func() {\n\t\tContribute(cross(cur), id)\n\t})\n}\n\n// TestContribute_CampaignTotalOverflowRefused seeds the campaign's total\n// directly to one short of maxInt64 (no real sequence of contributions could\n// reach it) so the next contribution overflows only the campaign-total guard\n// (`campaign.totalRaised \u003e maxInt64-amount`), not the per-contributor guard:\n// this contributor's own running total is still zero.\nfunc TestContribute_CampaignTotalOverflowRefused(cur realm, t *testing.T) {\n\tdefer resetTestState()\n\n\tcreator := testutils.TestAddress(\"creator22\")\n\tcontributor := testutils.TestAddress(\"contributor22\")\n\ttesting.SetRealm(testing.NewUserRealm(creator))\n\tdeadline := time.Now().Add(1 * time.Hour).Unix()\n\tid := CreateCampaign(cross(cur), \"Overflow campaign total\", minimumContributionUgnot*10, deadline)\n\n\tc := mustGetCampaign(id)\n\tc.totalRaised = maxInt64 - minimumContributionUgnot // one short of overflowing\n\n\tpkgAddr := chain.PackageAddress(modulePath)\n\tamount := minimumContributionUgnot + 1\n\ttesting.SetRealm(testing.NewUserRealm(contributor))\n\ttesting.SetOriginSend(chain.Coins{{denom, amount}})\n\ttesting.IssueCoins(pkgAddr, chain.Coins{{denom, amount}})\n\tuassert.AbortsWithMessage(t, cur, \"campaign total would overflow\", func() {\n\t\tContribute(cross(cur), id)\n\t})\n}\n\n// TestContribute_CodeRealmCallerRefused exercises Contribute's very first\n// guard, `if !cur.Previous().IsUserCall() { panic(...) }` -- the guard the\n// source's own comment calls \"the load-bearing line of the whole realm\",\n// because it is the only thing standing between unsafe.OriginSend() and a\n// caller that can attach -send to a transaction without a matching deposit\n// ever reaching this realm. testing.NewCodeRealm constructs a caller whose\n// immediate frame is a code realm rather than a genuine user account, which\n// is exactly the case IsUserCall() must reject; the preceding\n// testing.NewUserRealm call gives that code realm a real user frame beneath\n// it, mirroring how gnovm's own zrealm_crossrealm13.gno test builds a\n// non-user-call frame. The realm path below is a deliberately fictional test\n// fixture (not a real deployed realm) so a reader can't mistake it for an\n// actual dependency of this contract.\nfunc TestContribute_CodeRealmCallerRefused(cur realm, t *testing.T) {\n\tdefer resetTestState()\n\n\tcreator := testutils.TestAddress(\"creator23\")\n\ttesting.SetRealm(testing.NewUserRealm(creator))\n\tdeadline := time.Now().Add(1 * time.Hour).Unix()\n\tid := CreateCampaign(cross(cur), \"Code realm caller\", minimumContributionUgnot*10, deadline)\n\n\tfakeCallerAccount := testutils.TestAddress(\"contributor23\")\n\ttesting.SetRealm(testing.NewUserRealm(fakeCallerAccount))\n\ttesting.SetRealm(testing.NewCodeRealm(\"gno.land/r/gnobuilder/test_fixtures/fake_code_realm\"))\n\tuassert.AbortsWithMessage(t, cur, \"only user-call (maketx call) accepted\", func() {\n\t\tContribute(cross(cur), id)\n\t})\n}\n\nfunc TestSuccessfulCampaign_CreatorWithdraws(cur realm, t *testing.T) {\n\tdefer resetTestState()\n\n\tcreator := testutils.TestAddress(\"creator8\")\n\tcontributor := testutils.TestAddress(\"contributor8\")\n\ttesting.SetRealm(testing.NewUserRealm(creator))\n\tdeadline := time.Now().Add(1 * time.Hour).Unix()\n\ttarget := minimumContributionUgnot * 10\n\tid := CreateCampaign(cross(cur), \"Fund the thing\", target, deadline)\n\n\traised := minimumContributionUgnot * 15\n\tcontribute(cur, t, contributor, id, raised)\n\n\tc := mustGetCampaign(id)\n\tuassert.Equal(t, raised, c.totalRaised)\n\tc.deadlineUnix = time.Now().Add(-1 * time.Hour).Unix() // simulate deadline passed\n\n\ttesting.SetRealm(testing.NewUserRealm(creator))\n\tWithdraw(cross(cur), id)\n\n\tuassert.True(t, c.withdrawn)\n\tuassert.Equal(t, raised, c.totalRaised) // gross-raised figure stays fixed, only withdrawn flips\n}\n\nfunc TestWithdraw_DoubleWithdrawRefused(cur realm, t *testing.T) {\n\tdefer resetTestState()\n\n\tcreator := testutils.TestAddress(\"creator9\")\n\tcontributor := testutils.TestAddress(\"contributor9\")\n\ttesting.SetRealm(testing.NewUserRealm(creator))\n\tdeadline := time.Now().Add(1 * time.Hour).Unix()\n\ttarget := minimumContributionUgnot * 10\n\tid := CreateCampaign(cross(cur), \"Double withdraw\", target, deadline)\n\n\tcontribute(cur, t, contributor, id, target)\n\n\tc := mustGetCampaign(id)\n\tc.deadlineUnix = time.Now().Add(-1 * time.Hour).Unix()\n\n\ttesting.SetRealm(testing.NewUserRealm(creator))\n\tWithdraw(cross(cur), id)\n\tuassert.AbortsWithMessage(t, cur, \"campaign funds already withdrawn\", func() {\n\t\tWithdraw(cross(cur), id)\n\t})\n}\n\nfunc TestWithdraw_NonCreatorRefused(cur realm, t *testing.T) {\n\tdefer resetTestState()\n\n\tcreator := testutils.TestAddress(\"creator10\")\n\tcontributor := testutils.TestAddress(\"contributor10\")\n\tstranger := testutils.TestAddress(\"stranger10\")\n\ttesting.SetRealm(testing.NewUserRealm(creator))\n\tdeadline := time.Now().Add(1 * time.Hour).Unix()\n\ttarget := minimumContributionUgnot * 10\n\tid := CreateCampaign(cross(cur), \"Not yours\", target, deadline)\n\n\tcontribute(cur, t, contributor, id, target)\n\n\tc := mustGetCampaign(id)\n\tc.deadlineUnix = time.Now().Add(-1 * time.Hour).Unix()\n\n\ttesting.SetRealm(testing.NewUserRealm(stranger))\n\tuassert.AbortsWithMessage(t, cur, \"only the campaign creator can withdraw\", func() {\n\t\tWithdraw(cross(cur), id)\n\t})\n}\n\nfunc TestWithdraw_BeforeDeadlineRefused(cur realm, t *testing.T) {\n\tdefer resetTestState()\n\n\tcreator := testutils.TestAddress(\"creator11\")\n\tcontributor := testutils.TestAddress(\"contributor11\")\n\ttesting.SetRealm(testing.NewUserRealm(creator))\n\tdeadline := time.Now().Add(1 * time.Hour).Unix()\n\ttarget := minimumContributionUgnot * 10\n\tid := CreateCampaign(cross(cur), \"Too early\", target, deadline)\n\n\tcontribute(cur, t, contributor, id, target)\n\n\ttesting.SetRealm(testing.NewUserRealm(creator))\n\tuassert.AbortsWithMessage(t, cur, \"campaign deadline has not passed\", func() {\n\t\tWithdraw(cross(cur), id)\n\t})\n}\n\nfunc TestWithdraw_TargetNotMetRefused(cur realm, t *testing.T) {\n\tdefer resetTestState()\n\n\tcreator := testutils.TestAddress(\"creator12\")\n\tcontributor := testutils.TestAddress(\"contributor12\")\n\ttesting.SetRealm(testing.NewUserRealm(creator))\n\tdeadline := time.Now().Add(1 * time.Hour).Unix()\n\ttarget := minimumContributionUgnot * 10\n\tid := CreateCampaign(cross(cur), \"Underfunded\", target, deadline)\n\n\tcontribute(cur, t, contributor, id, minimumContributionUgnot*2)\n\n\tc := mustGetCampaign(id)\n\tc.deadlineUnix = time.Now().Add(-1 * time.Hour).Unix()\n\n\ttesting.SetRealm(testing.NewUserRealm(creator))\n\tuassert.AbortsWithMessage(t, cur, \"campaign did not reach its target\", func() {\n\t\tWithdraw(cross(cur), id)\n\t})\n}\n\nfunc TestFailedCampaign_ContributorRefunded(cur realm, t *testing.T) {\n\tdefer resetTestState()\n\n\tcreator := testutils.TestAddress(\"creator13\")\n\tcontributor := testutils.TestAddress(\"contributor13\")\n\ttesting.SetRealm(testing.NewUserRealm(creator))\n\tdeadline := time.Now().Add(1 * time.Hour).Unix()\n\ttarget := minimumContributionUgnot * 10\n\tid := CreateCampaign(cross(cur), \"Will fail\", target, deadline)\n\n\tshortfall := minimumContributionUgnot * 3\n\tcontribute(cur, t, contributor, id, shortfall)\n\n\tc := mustGetCampaign(id)\n\tc.deadlineUnix = time.Now().Add(-1 * time.Hour).Unix()\n\n\ttesting.SetRealm(testing.NewUserRealm(contributor))\n\tRefund(cross(cur), id)\n\n\tuassert.Equal(t, shortfall, c.totalRaised) // gross-raised figure stays fixed\n\tuassert.Equal(t, int64(0), c.contributions.Get(string(contributor)).(int64))\n}\n\nfunc TestRefund_DoubleRefundRefused(cur realm, t *testing.T) {\n\tdefer resetTestState()\n\n\tcreator := testutils.TestAddress(\"creator14\")\n\tcontributor := testutils.TestAddress(\"contributor14\")\n\ttesting.SetRealm(testing.NewUserRealm(creator))\n\tdeadline := time.Now().Add(1 * time.Hour).Unix()\n\ttarget := minimumContributionUgnot * 10\n\tid := CreateCampaign(cross(cur), \"Double refund\", target, deadline)\n\n\tcontribute(cur, t, contributor, id, minimumContributionUgnot*3)\n\n\tc := mustGetCampaign(id)\n\tc.deadlineUnix = time.Now().Add(-1 * time.Hour).Unix()\n\n\ttesting.SetRealm(testing.NewUserRealm(contributor))\n\tRefund(cross(cur), id)\n\tuassert.AbortsWithMessage(t, cur, \"no contribution found for caller\", func() {\n\t\tRefund(cross(cur), id)\n\t})\n}\n\nfunc TestRefund_NonContributorRefused(cur realm, t *testing.T) {\n\tdefer resetTestState()\n\n\tcreator := testutils.TestAddress(\"creator15\")\n\tcontributor := testutils.TestAddress(\"contributor15\")\n\tstranger := testutils.TestAddress(\"stranger15\")\n\ttesting.SetRealm(testing.NewUserRealm(creator))\n\tdeadline := time.Now().Add(1 * time.Hour).Unix()\n\ttarget := minimumContributionUgnot * 10\n\tid := CreateCampaign(cross(cur), \"Stranger refund\", target, deadline)\n\n\tcontribute(cur, t, contributor, id, minimumContributionUgnot*3)\n\n\tc := mustGetCampaign(id)\n\tc.deadlineUnix = time.Now().Add(-1 * time.Hour).Unix()\n\n\ttesting.SetRealm(testing.NewUserRealm(stranger))\n\tuassert.AbortsWithMessage(t, cur, \"no contribution found for caller\", func() {\n\t\tRefund(cross(cur), id)\n\t})\n}\n\nfunc TestRefund_BeforeDeadlineRefused(cur realm, t *testing.T) {\n\tdefer resetTestState()\n\n\tcreator := testutils.TestAddress(\"creator16\")\n\tcontributor := testutils.TestAddress(\"contributor16\")\n\ttesting.SetRealm(testing.NewUserRealm(creator))\n\tdeadline := time.Now().Add(1 * time.Hour).Unix()\n\ttarget := minimumContributionUgnot * 10\n\tid := CreateCampaign(cross(cur), \"Too early refund\", target, deadline)\n\n\tcontribute(cur, t, contributor, id, minimumContributionUgnot*3)\n\n\ttesting.SetRealm(testing.NewUserRealm(contributor))\n\tuassert.AbortsWithMessage(t, cur, \"campaign deadline has not passed\", func() {\n\t\tRefund(cross(cur), id)\n\t})\n}\n\nfunc TestRefund_CampaignSucceededRefused(cur realm, t *testing.T) {\n\tdefer resetTestState()\n\n\tcreator := testutils.TestAddress(\"creator17\")\n\tcontributor := testutils.TestAddress(\"contributor17\")\n\ttesting.SetRealm(testing.NewUserRealm(creator))\n\tdeadline := time.Now().Add(1 * time.Hour).Unix()\n\ttarget := minimumContributionUgnot * 10\n\tid := CreateCampaign(cross(cur), \"Will succeed\", target, deadline)\n\n\tcontribute(cur, t, contributor, id, target)\n\n\tc := mustGetCampaign(id)\n\tc.deadlineUnix = time.Now().Add(-1 * time.Hour).Unix()\n\n\ttesting.SetRealm(testing.NewUserRealm(contributor))\n\tuassert.AbortsWithMessage(t, cur, \"campaign succeeded; refunds are not available\", func() {\n\t\tRefund(cross(cur), id)\n\t})\n}\n\nfunc TestViewCampaign(cur realm, t *testing.T) {\n\tdefer resetTestState()\n\n\tcreator := testutils.TestAddress(\"creator18\")\n\ttesting.SetRealm(testing.NewUserRealm(creator))\n\tdeadline := time.Now().Add(1 * time.Hour).Unix()\n\tid := CreateCampaign(cross(cur), \"Viewable\", minimumContributionUgnot*10, deadline)\n\n\tuassert.Equal(t, mustGetCampaign(id).String(), ViewCampaign(id))\n}\n\nfunc TestRender(cur realm, t *testing.T) {\n\tdefer resetTestState()\n\n\tcreator := testutils.TestAddress(\"creator19\")\n\tcontributor := testutils.TestAddress(\"contributor19\")\n\ttesting.SetRealm(testing.NewUserRealm(creator))\n\tdeadline := time.Now().Add(1 * time.Hour).Unix()\n\ttarget1 := minimumContributionUgnot * 10\n\ttarget2 := minimumContributionUgnot * 20\n\tid1 := CreateCampaign(cross(cur), \"Campaign One\", target1, deadline)\n\tid2 := CreateCampaign(cross(cur), \"Campaign Two\", target2, deadline)\n\n\traised1 := minimumContributionUgnot * 5\n\tcontribute(cur, t, contributor, id1, raised1)\n\n\texpected := ufmt.Sprintf(\n\t\t\"# Community Crowdfunding\"+\"\\n\\n- %d: Campaign Two - target %dugnot raised 0ugnot - active\\n- %d: Campaign One - target %dugnot raised %dugnot - active\\n\",\n\t\tid2, target2, id1, target1, raised1,\n\t)\n\tuassert.Equal(t, expected, Render(\"\"))\n\n\tc1 := mustGetCampaign(id1)\n\texpectedDetail := ufmt.Sprintf(\n\t\t\"- id: %d\\n- title: %s\\n- creator: %s\\n- target: %dugnot\\n- raised: %dugnot\\n- deadline: %s\\n- status: %s\\n- updates: %d\",\n\t\tid1, \"Campaign One\", creator, target1, raised1,\n\t\ttime.Unix(c1.deadlineUnix, 0).UTC().Format(time.RFC3339), \"active\", len(c1.updates),\n\t)\n\tuassert.Equal(t, expectedDetail, Render(\"1\"))\n\n\tuassert.Equal(t, \"404\", Render(\"999\"))\n}\n\n// TestCreateCampaign_TitleSanitizedAtCreation pins F2: a title is sanitized\n// once, at creation time, so the stored state is clean and every render\n// path (here, both the detail String() view and the home listing) is\n// covered without needing its own escaping. The hostile fragments below are\n// exactly the two failure modes the finding named: an embedded newline\n// forging a fake \"- creator: ...\" line, and unescaped markdown link syntax\n// (\"](javascript:...)\") turning into a live malicious link destination.\nfunc TestCreateCampaign_TitleSanitizedAtCreation(cur realm, t *testing.T) {\n\tdefer resetTestState()\n\n\tcreator := testutils.TestAddress(\"creator-sanitize\")\n\ttesting.SetRealm(testing.NewUserRealm(creator))\n\tdeadline := time.Now().Add(1 * time.Hour).Unix()\n\n\thostile := \"Nice project\\n- creator: g1attacker \u003cscript\u003ealert(1)\u003c/script\u003e [x](javascript:alert(1)) a|b\"\n\tid := CreateCampaign(cross(cur), hostile, minimumContributionUgnot*10, deadline)\n\n\tc := mustGetCampaign(id)\n\tforbidden := []string{\"\\n\", \"\u003c\", \"\u003e\", \"](\", \"|\"}\n\tfor _, frag := range forbidden {\n\t\tif strings.Contains(c.title, frag) {\n\t\t\tt.Errorf(\"expected stored title to be sanitized, still contains %q: %s\", frag, c.title)\n\t\t}\n\t}\n\n\trendered := Render(\"\")\n\tif strings.Contains(rendered, \"\\n- creator: g1attacker\") {\n\t\tt.Errorf(\"expected a title's embedded newline to be unable to forge a creator line in the listing, got: %s\", rendered)\n\t}\n}\n\n// TestCreateCampaign_InvalidUTF8Refused pins the other half of F2: byte\n// length alone (len(title) \u003e maxTitleBytes) does not enforce the spec's\n// UTF-8 intent, so invalid UTF-8 must be rejected explicitly instead of\n// being stored.\nfunc TestCreateCampaign_InvalidUTF8Refused(cur realm, t *testing.T) {\n\tdefer resetTestState()\n\n\tcreator := testutils.TestAddress(\"creator-badutf8\")\n\ttesting.SetRealm(testing.NewUserRealm(creator))\n\tdeadline := time.Now().Add(1 * time.Hour).Unix()\n\n\tinvalid := \"bad title \\xff\\xfe\"\n\tuassert.AbortsWithMessage(t, cur, \"title must be valid utf-8\", func() {\n\t\tCreateCampaign(cross(cur), invalid, minimumContributionUgnot*10, deadline)\n\t})\n}\n\n// TestCreateCampaign_DeadlineTooFarInFutureRefused pins F5: a deadline far\n// enough out to be an obvious milliseconds-for-seconds typo (see\n// maxDeadlineSecondsFromNow) must be refused before it can lock\n// contributions forever. The 60-second margin beyond the bound (rather than\n// exactly +1) keeps this test from flaking if a second elapses between this\n// line's clock read and CreateCampaign's own.\nfunc TestCreateCampaign_DeadlineTooFarInFutureRefused(cur realm, t *testing.T) {\n\tdefer resetTestState()\n\n\tcreator := testutils.TestAddress(\"creator-fardeadline\")\n\ttesting.SetRealm(testing.NewUserRealm(creator))\n\ttooFar := time.Now().Unix() + maxDeadlineSecondsFromNow + 60\n\n\tuassert.AbortsWithMessage(t, cur, \"deadline too far in the future\", func() {\n\t\tCreateCampaign(cross(cur), \"Millisecond typo\", minimumContributionUgnot*10, tooFar)\n\t})\n}\n\n// TestCreateCampaign_DeadlineAtUpperBoundAccepted is the boundary case for\n// the guard `deadlineUnix \u003e time.Now().Unix()+maxDeadlineSecondsFromNow`: a\n// deadline landing exactly on the bound must still be accepted, not just\n// one comfortably inside it, so a mutation weakening `\u003e` to `\u003e=` has\n// something to be caught by.\nfunc TestCreateCampaign_DeadlineAtUpperBoundAccepted(cur realm, t *testing.T) {\n\tdefer resetTestState()\n\n\t// testutils.TestAddress panics above 20 bytes, so keep these names short.\n\tcreator := testutils.TestAddress(\"creator-bounddl\")\n\ttesting.SetRealm(testing.NewUserRealm(creator))\n\tatBound := time.Now().Unix() + maxDeadlineSecondsFromNow\n\n\tid := CreateCampaign(cross(cur), \"At the boundary\", minimumContributionUgnot*10, atBound)\n\tc := mustGetCampaign(id)\n\tuassert.Equal(t, atBound, c.deadlineUnix)\n}\n\n// TestCampaignKeyOrdersNumericallyNotLexicographically pins F3's second,\n// compounding bug: unpadded decimal keys sort \"10\" before \"2\", so from the\n// tenth campaign onward, key order silently stops matching id order.\nfunc TestCampaignKeyOrdersNumericallyNotLexicographically(cur realm, t *testing.T) {\n\tif campaignKey(2) \u003e= campaignKey(10) {\n\t\tt.Errorf(\"expected campaignKey(2) \u003c campaignKey(10) (numeric-order-preserving keys), got key(2)=%q key(10)=%q\", campaignKey(2), campaignKey(10))\n\t}\n\tif len(campaignKey(1)) != campaignKeyWidth {\n\t\tt.Errorf(\"expected campaignKey to zero-pad to width %d, got %q (len %d)\", campaignKeyWidth, campaignKey(1), len(campaignKey(1)))\n\t}\n}\n\n// TestRenderHome_ShowsNewestBeyondPageSize pins F3's first bug: past\n// pageSize campaigns, the home listing must keep showing the newest ones,\n// not permanently freeze on the oldest page.\nfunc TestRenderHome_ShowsNewestBeyondPageSize(cur realm, t *testing.T) {\n\tdefer resetTestState()\n\n\tcreator := testutils.TestAddress(\"creator-pagination\")\n\ttesting.SetRealm(testing.NewUserRealm(creator))\n\tdeadline := time.Now().Add(1 * time.Hour).Unix()\n\n\ttotal := pageSize + 5\n\tvar newestID, oldestID int64\n\tfor i := 0; i \u003c total; i++ {\n\t\tid := CreateCampaign(cross(cur), ufmt.Sprintf(\"Campaign %d\", i), minimumContributionUgnot*10, deadline)\n\t\tif i == 0 {\n\t\t\toldestID = id\n\t\t}\n\t\tnewestID = id\n\t}\n\n\toutput := Render(\"\")\n\tnewest := mustGetCampaign(newestID)\n\toldest := mustGetCampaign(oldestID)\n\tif !strings.Contains(output, newest.title) {\n\t\tt.Errorf(\"expected the newest campaign %q on the home listing, got: %s\", newest.title, output)\n\t}\n\tif strings.Contains(output, oldest.title) {\n\t\tt.Errorf(\"expected the oldest campaign %q to be pushed off the first page, got: %s\", oldest.title, output)\n\t}\n}\n\n// TestContribute_TrustsDeclaredOriginSendNotIssuedCoins is F6: every other\n// test in this suite calls the `contribute` helper above, which fabricates\n// both testing.SetOriginSend and testing.IssueCoins from the SAME `amount`,\n// so the suite could never distinguish \"coins genuinely arrived\" from \"the\n// realm was merely told coins arrived.\" This test deliberately declares a\n// different, larger origin send than the coins actually issued to the\n// realm, and pins the realm's actual, current behaviour: Contribute's\n// accounting (contributions, totalRaised) is driven entirely by\n// unsafe.OriginSend(), the declared amount, with no cross-check whatsoever\n// against the realm's real coin balance. That is not a bug in Contribute\n// itself -- in production, unsafe.OriginSend() is documented to describe a\n// genuine receipt for a real user-call transaction, which is exactly what\n// the IsUserCall guard above exists to make trustworthy -- but it does mean\n// this specific mismatch is undetectable from inside Contribute, and can\n// only ever be manufactured in a test via two independent fabrications like\n// this one. Per this task's scope, Contribute's logic is not changed here;\n// this test documents the behaviour as it stands.\nfunc TestContribute_TrustsDeclaredOriginSendNotIssuedCoins(cur realm, t *testing.T) {\n\tdefer resetTestState()\n\n\tcreator := testutils.TestAddress(\"creator-mismatch\")\n\tcontributor := testutils.TestAddress(\"contributor-mismatch\")\n\ttesting.SetRealm(testing.NewUserRealm(creator))\n\tdeadline := time.Now().Add(1 * time.Hour).Unix()\n\tid := CreateCampaign(cross(cur), \"Mismatch test\", minimumContributionUgnot*10, deadline)\n\n\tdeclared := minimumContributionUgnot * 5\n\tactuallyIssued := minimumContributionUgnot * 2 // deliberately less than declared\n\n\tpkgAddr := chain.PackageAddress(modulePath)\n\ttesting.SetRealm(testing.NewUserRealm(contributor))\n\ttesting.SetOriginSend(chain.Coins{{denom, declared}})\n\ttesting.IssueCoins(pkgAddr, chain.Coins{{denom, actuallyIssued}})\n\tContribute(cross(cur), id)\n\n\tc := mustGetCampaign(id)\n\tuassert.Equal(t, declared, c.totalRaised)\n\tuassert.Equal(t, declared, c.contributions.Get(string(contributor)).(int64))\n}\n\nfunc TestAddUpdate_CreatorSucceeds(cur realm, t *testing.T) {\n\tdefer resetTestState()\n\n\tcreator := testutils.TestAddress(\"update-creator1\")\n\ttesting.SetRealm(testing.NewUserRealm(creator))\n\tdeadline := time.Now().Add(1 * time.Hour).Unix()\n\tid := CreateCampaign(cross(cur), \"Updatable\", minimumContributionUgnot*10, deadline)\n\n\tAddUpdate(cross(cur), id, \"Progress: foundations poured.\")\n\n\tc := mustGetCampaign(id)\n\tuassert.Equal(t, 1, len(c.updates))\n\tuassert.Equal(t, \"Progress: foundations poured.\", c.updates[0])\n}\n\nfunc TestAddUpdate_NonCreatorRefused(cur realm, t *testing.T) {\n\tdefer resetTestState()\n\n\tcreator := testutils.TestAddress(\"update-creator2\")\n\tstranger := testutils.TestAddress(\"update-stranger2\")\n\ttesting.SetRealm(testing.NewUserRealm(creator))\n\tdeadline := time.Now().Add(1 * time.Hour).Unix()\n\tid := CreateCampaign(cross(cur), \"Not yours to update\", minimumContributionUgnot*10, deadline)\n\n\ttesting.SetRealm(testing.NewUserRealm(stranger))\n\tuassert.AbortsWithMessage(t, cur, \"only the campaign creator can add updates\", func() {\n\t\tAddUpdate(cross(cur), id, \"I am not the creator\")\n\t})\n}\n\nfunc TestAddUpdate_OversizedRefused(cur realm, t *testing.T) {\n\tdefer resetTestState()\n\n\tcreator := testutils.TestAddress(\"update-creator3\")\n\ttesting.SetRealm(testing.NewUserRealm(creator))\n\tdeadline := time.Now().Add(1 * time.Hour).Unix()\n\tid := CreateCampaign(cross(cur), \"Oversized update\", minimumContributionUgnot*10, deadline)\n\n\toversized := make([]byte, maxUpdateBytes+1)\n\tfor i := range oversized {\n\t\toversized[i] = 'a'\n\t}\n\n\tuassert.AbortsWithMessage(t, cur, \"update exceeds max size\", func() {\n\t\tAddUpdate(cross(cur), id, string(oversized))\n\t})\n}\n\nfunc TestAddUpdate_CountLimitEnforced(cur realm, t *testing.T) {\n\tdefer resetTestState()\n\n\tcreator := testutils.TestAddress(\"update-creator4\")\n\ttesting.SetRealm(testing.NewUserRealm(creator))\n\tdeadline := time.Now().Add(1 * time.Hour).Unix()\n\tid := CreateCampaign(cross(cur), \"Update limit\", minimumContributionUgnot*10, deadline)\n\n\tfor i := 0; i \u003c maxUpdatesPerCampaign; i++ {\n\t\tAddUpdate(cross(cur), id, \"update\")\n\t}\n\n\tc := mustGetCampaign(id)\n\tuassert.Equal(t, maxUpdatesPerCampaign, len(c.updates))\n\n\tuassert.AbortsWithMessage(t, cur, \"campaign has reached its update limit\", func() {\n\t\tAddUpdate(cross(cur), id, \"one too many\")\n\t})\n}\n"},{"name":"gnomod.toml","body":"module = \"gno.land/r/g142p4mjutq0dhu3vlfz32ymc490gk9d2sqglrak/gate1v2h/crowdfunding\"\ngno = \"0.9\"\n"}],"type":{"@type":"/gno.MemPackageType","value":"MPUserAll"}},"send":"","max_deposit":""}],"fee":{"gas_wanted":"66972858","gas_fee":"66975ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"A2GOzMGavtLOZjb9g1ltPKZsySYTl1dY+wHnkIs2zwhI"},"signature":"/GDqb2in41oqGjNPLwtBDTrMa83bM8ob2VjHzwzjOkMRa2vdJ0BwgXu+RoYpeZJ/lNIyYgyfqXZW7zmtFN1gVQ=="}],"memo":""},"metadata":{"timestamp":"1790120250"}}
{"tx":{"msg":[{"@type":"/vm.m_call","caller":"g13fmcl95ma9f5r9hhz0a96mwwhmdxrx73f93587","send":"","max_deposit":"","pkg_path":"gno.land/r/g142p4mjutq0dhu3vlfz32ymc490gk9d2sqglrak/gate1v2h/crowdfunding","func":"CreateCampaign","args":["Gate1 successful campaign","100000000","1790120554"]}],"fee":{"gas_wanted":"4882036","gas_fee":"4885ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"AsvB/2qgzHlBsiCGbgrBINRFk+H0Dhpwvl7YYhLVBxJ+"},"signature":"VW5cqeMKgG3MDCWTqCCkiHuoi4JiZ91rSWi0YSJr7YUIXdoxlHvxUGT+z81cns+h3e6dvubAH/DA+tXC0h0+tA=="}],"memo":""},"metadata":{"timestamp":"1790120259"}}
{"tx":{"msg":[{"@type":"/vm.m_call","caller":"g13fmcl95ma9f5r9hhz0a96mwwhmdxrx73f93587","send":"","max_deposit":"","pkg_path":"gno.land/r/g142p4mjutq0dhu3vlfz32ymc490gk9d2sqglrak/gate1v2h/crowdfunding","func":"CreateCampaign","args":["Gate1 failed campaign","100000000","1790120554"]}],"fee":{"gas_wanted":"5064404","gas_fee":"5067ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"AsvB/2qgzHlBsiCGbgrBINRFk+H0Dhpwvl7YYhLVBxJ+"},"signature":"DZDEp4PIU/KDuuFK1kr+us5IeBBMsIAxYeXKkD86ArEVirMJxz1L/Mpw7iQi7mBo5LQAAiv68OaVJWPqBHEWGA=="}],"memo":""},"metadata":{"timestamp":"1790120269"}}
{"tx":{"msg":[{"@type":"/vm.m_call","caller":"g13ru04ttcfh2apyya44fe2vghnyt9tty98zeu2j","send":"25000000ugnot","max_deposit":"","pkg_path":"gno.land/r/g142p4mjutq0dhu3vlfz32ymc490gk9d2sqglrak/gate1v2h/crowdfunding","func":"Contribute","args":["1"]}],"fee":{"gas_wanted":"5513399","gas_fee":"5516ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"AqapGZ8VqkhzpqOfiyN9OTYqRpm9AqBiBofd6y+7+VKb"},"signature":"jmAKY+petFC3RWAPtxqanJZBD1cuZJOttxS9OxbDJYcnMECFRc6m6W6dyC2rGVUESRHwI/81A1MQ1oIutZOgGQ=="}],"memo":""},"metadata":{"timestamp":"1790120279"}}
{"tx":{"msg":[{"@type":"/vm.m_call","caller":"g13ru04ttcfh2apyya44fe2vghnyt9tty98zeu2j","send":"","max_deposit":"","pkg_path":"gno.land/r/g142p4mjutq0dhu3vlfz32ymc490gk9d2sqglrak/gate1v2h/crowdfunding","func":"Refund","args":["1"]}],"fee":{"gas_wanted":"5217147","gas_fee":"5220ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"AqapGZ8VqkhzpqOfiyN9OTYqRpm9AqBiBofd6y+7+VKb"},"signature":"lokvPaPum4OA77/OqCBSfnXETJGa+YzR5Ns8CwLxRQJDnVXAkjHbOZrTZjtL/giGSvHee8dnkneeI+RmP1YYBA=="}],"memo":""},"metadata":{"timestamp":"1790120579"}}
{"tx":{"msg":[{"@type":"/bank.MsgSend","from_address":"g142p4mjutq0dhu3vlfz32ymc490gk9d2sqglrak","to_address":"g13ru04ttcfh2apyya44fe2vghnyt9tty98zeu2j","amount":"116536ugnot"}],"fee":{"gas_wanted":"981847","gas_fee":"984ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"A2GOzMGavtLOZjb9g1ltPKZsySYTl1dY+wHnkIs2zwhI"},"signature":"NpsZygGbGHd0TYzlpcy4D4BRjtMOaixN4rqx5qTP4js0978S+qXy5JKodmkM/YUeDefLXHv5Dpc7EuE0w7GUFQ=="}],"memo":""},"metadata":{"timestamp":"1790120602"}}
{"tx":{"msg":[{"@type":"/vm.m_addpkg","creator":"g142p4mjutq0dhu3vlfz32ymc490gk9d2sqglrak","package":{"name":"crowdfunding","path":"gno.land/r/g142p4mjutq0dhu3vlfz32ymc490gk9d2sqglrak/gate1v2j/crowdfunding","files":[{"name":"campaign.gno","body":"package crowdfunding\n\nimport (\n\t\"time\"\n\n\t\"gno.land/p/nt/avl/v0\"\n\t\"gno.land/p/nt/ufmt/v0\"\n)\n\n// maxInt64 bounds the checked-integer arithmetic on campaign totals.\nconst maxInt64 = 1\u003c\u003c63 - 1\n\n// Campaign holds the immutable terms and mutable funding state of a single\n// crowdfunding campaign.\ntype Campaign struct {\n\tid           int64\n\tcreator      address\n\ttitle        string\n\ttargetUgnot  int64\n\tdeadlineUnix int64\n\ttotalRaised  int64\n\twithdrawn    bool\n\n\t// updates holds the creator's append-only progress notes, bounded by\n\t// maxUpdateBytes and maxUpdatesPerCampaign (see crowdfunding.gno). The\n\t// String() view only reports a count, never iterates the slice, so\n\t// rendering a campaign stays O(1) regardless of update history.\n\tupdates []string\n\n\t// contributions maps a contributor address (string(address)) to their\n\t// cumulative contribution in ugnot. Zeroed on refund, never removed,\n\t// so a repeat Refund call reliably finds a zero balance.\n\tcontributions avl.Tree\n}\n\nfunc (c *Campaign) status() string {\n\tnow := time.Now().Unix()\n\tswitch {\n\tcase c.withdrawn:\n\t\treturn \"withdrawn\"\n\tcase now \u003c c.deadlineUnix:\n\t\treturn \"active\"\n\tcase c.totalRaised \u003e= c.targetUgnot:\n\t\treturn \"successful\"\n\tdefault:\n\t\treturn \"failed\"\n\t}\n}\n\nfunc (c *Campaign) String() string {\n\treturn ufmt.Sprintf(\n\t\t\"- id: %d\\n- title: %s\\n- creator: %s\\n- target: %dugnot\\n- raised: %dugnot\\n- deadline: %s\\n- status: %s\\n- updates: %d\",\n\t\tc.id, c.title, c.creator, c.targetUgnot, c.totalRaised,\n\t\ttime.Unix(c.deadlineUnix, 0).UTC().Format(time.RFC3339), c.status(), len(c.updates),\n\t)\n}\n"},{"name":"crowdfunding.gno","body":"// Package crowdfunding implements the V0 supported crowdfunding realm:\n// creators raise native ugnot until a deadline; campaigns that meet their\n// target pay the creator, campaigns that don't refund contributors.\n// \n// Creators raise native testnet GNOT until a deadline; successful campaigns pay creators and failed campaigns refund contributors.\n// \n// Generated by generator/ from the \"Community Crowdfunding\" app-spec (schema_version 1.0, revision 2). Do not hand-edit generated output; change the spec and regenerate instead.\npackage crowdfunding\n\nimport (\n\t\"chain\"\n\t\"chain/banker\"\n\t\"chain/runtime/unsafe\"\n\t\"strconv\"\n\t\"strings\"\n\t\"time\"\n\t\"unicode/utf8\"\n\n\t\"gno.land/p/nt/avl/v0\"\n\t\"gno.land/p/nt/ufmt/v0\"\n)\n\nconst (\n\t// maxTitleBytes matches the spec's ui.title_max_utf8_bytes.\n\tmaxTitleBytes = 256\n\t// pageSize matches the spec's ui.page_size for the home listing.\n\tpageSize = 20\n\t// minimumContributionUgnot matches the spec's rules.minimum_contribution_ugnot.\n\tminimumContributionUgnot int64 = 25000000\n\t// denom is the only accepted native denomination (spec currency.denom).\n\tdenom = \"ugnot\"\n\t// maxDeadlineSecondsFromNow bounds how far in the future a campaign\n\t// deadline may be set (see CreateCampaign). This exists specifically to\n\t// catch the plausible mistake of passing milliseconds where whole\n\t// seconds are expected: a millisecond timestamp is ~1000x a second\n\t// timestamp, so even a short 30-day campaign becomes roughly 82 years\n\t// out, and a 1-year campaign becomes roughly 1000 years out. Once\n\t// accepted, the mistake is unrecoverable -- a deadline this far out\n\t// never passes, so contributors can never be refunded and the creator\n\t// can never withdraw either. Ten years comfortably exceeds any\n\t// realistic V0 campaign length while still rejecting that entire class\n\t// of typo by a wide margin.\n\tmaxDeadlineSecondsFromNow int64 = 10 * 365 * 24 * 60 * 60\n\t// campaignKeyWidth zero-pads campaign keys (see campaignKey) so the\n\t// avl.Tree's lexicographic key ordering matches numeric id ordering.\n\t// 19 digits covers every positive int64 (max 9223372036854775807).\n\tcampaignKeyWidth = 19\n\t// maxUpdateBytes and maxUpdatesPerCampaign match the spec's\n\t// updates.max_utf8_bytes and updates.max_per_campaign.\n\tmaxUpdateBytes        = 2000\n\tmaxUpdatesPerCampaign = 50\n)\n\nvar (\n\tcampaigns      avl.Tree // campaignKey(id) -\u003e *Campaign\n\tnextCampaignID int64\n)\n\n// campaignKey renders a campaign id as a fixed-width, zero-padded decimal\n// string so avl.Tree's lexicographic key order matches numeric id order. An\n// unpadded strconv.FormatInt key sorts \"10\" before \"2\", which silently\n// breaks both direct id lookups' consistency with the tree's own ordering\n// and the home listing's newest-first ordering past the ninth campaign.\nfunc campaignKey(id int64) string {\n\ts := strconv.FormatInt(id, 10)\n\tfor len(s) \u003c campaignKeyWidth {\n\t\ts = \"0\" + s\n\t}\n\treturn s\n}\n\n// sanitizeInline conservatively escapes untrusted, user-supplied text (only\n// campaign titles in this realm) before it is stored, so every present and\n// future render path (the home listing, a campaign's String() detail view,\n// and any future Render addition) reads already-clean state instead of\n// needing to remember to sanitize at each render site.\n//\n// A raw title reaching a render path unescaped can forge the surrounding\n// structure it's spliced into: an embedded newline can start a fake\n// \"- creator: g1...\" line in the listing or in a campaign's own detail view,\n// and an unescaped \"](\" can close a markdown link label early and open a\n// javascript: destination. Everything this function emits is a named or\n// numeric XML/HTML character reference, safe as gnoweb markdown text.\nfunc sanitizeInline(s string) string {\n\tif len(s) \u003e maxTitleBytes {\n\t\ts = s[:maxTitleBytes]\n\t}\n\tvar b strings.Builder\n\tfor _, r := range s {\n\t\tswitch r {\n\t\tcase '\u0026':\n\t\t\tb.WriteString(\"\u0026amp;\")\n\t\tcase '\u003c':\n\t\t\tb.WriteString(\"\u0026lt;\")\n\t\tcase '\u003e':\n\t\t\tb.WriteString(\"\u0026gt;\")\n\t\tcase '\"':\n\t\t\tb.WriteString(\"\u0026quot;\")\n\t\tcase '\\'':\n\t\t\tb.WriteString(\"\u0026#39;\")\n\t\tcase '(':\n\t\t\tb.WriteString(\"\u0026#40;\")\n\t\tcase ')':\n\t\t\tb.WriteString(\"\u0026#41;\")\n\t\tcase '[':\n\t\t\tb.WriteString(\"\u0026#91;\")\n\t\tcase ']':\n\t\t\tb.WriteString(\"\u0026#93;\")\n\t\tcase '|':\n\t\t\tb.WriteString(\"\u0026#124;\")\n\t\tcase '\\n', '\\r', '\\t':\n\t\t\tb.WriteRune(' ')\n\t\tdefault:\n\t\t\tif r \u003c 0x20 {\n\t\t\t\tcontinue // drop other control characters outright\n\t\t\t}\n\t\t\tb.WriteRune(r)\n\t\t}\n\t}\n\treturn b.String()\n}\n\n// CreateCampaign registers a new campaign with a funding target (in ugnot)\n// and a UTC unix deadline. It does not receive funds; sends_native_value is\n// false for this action per the spec, so no OriginSend guard applies here.\nfunc CreateCampaign(cur realm, title string, targetUgnot int64, deadlineUnix int64) int64 {\n\tcreator := cur.Previous().Address()\n\n\tif len(title) == 0 {\n\t\tpanic(\"title must not be empty\")\n\t}\n\tif len(title) \u003e maxTitleBytes {\n\t\tpanic(\"title exceeds max size\")\n\t}\n\tif !utf8.ValidString(title) {\n\t\tpanic(\"title must be valid utf-8\")\n\t}\n\tif targetUgnot \u003c= 0 {\n\t\tpanic(\"target must be positive\")\n\t}\n\tif deadlineUnix \u003c= time.Now().Unix() {\n\t\tpanic(\"deadline must be in the future\")\n\t}\n\tif deadlineUnix \u003e time.Now().Unix()+maxDeadlineSecondsFromNow {\n\t\tpanic(\"deadline too far in the future\")\n\t}\n\n\tnextCampaignID++\n\tid := nextCampaignID\n\tcampaign := \u0026Campaign{\n\t\tid:           id,\n\t\tcreator:      creator,\n\t\ttitle:        sanitizeInline(title),\n\t\ttargetUgnot:  targetUgnot,\n\t\tdeadlineUnix: deadlineUnix,\n\t}\n\tcampaigns.Set(campaignKey(id), campaign)\n\treturn id\n}\n\n// Contribute sends native ugnot to a campaign before its deadline.\n//\n// It must be called directly by a user account (maketx call): unsafe.OriginSend()\n// only describes a real receipt at this realm when the caller is a pure user\n// account. An intermediate code realm or a `maketx run` ephemeral realm can\n// attach -send to a transaction but spend the envelope elsewhere, leaving\n// OriginSend() reporting a phantom payment that would let a caller book a\n// contribution never made and later drain the realm's real balance on\n// Withdraw/Refund. This guard is the load-bearing line of the whole realm.\nfunc Contribute(cur realm, campaignID int64) {\n\tif !cur.Previous().IsUserCall() {\n\t\tpanic(\"only user-call (maketx call) accepted\")\n\t}\n\tcontributor := cur.Previous().Address()\n\tcampaign := mustGetCampaign(campaignID)\n\n\tif time.Now().Unix() \u003e= campaign.deadlineUnix {\n\t\tpanic(\"campaign deadline has passed\")\n\t}\n\n\tsent := unsafe.OriginSend()\n\tif len(sent) == 0 {\n\t\tpanic(\"no coins sent\")\n\t}\n\tif len(sent) != 1 || sent[0].Denom != denom {\n\t\tpanic(\"only ugnot contributions are accepted\")\n\t}\n\tamount := sent[0].Amount\n\tif amount \u003c minimumContributionUgnot {\n\t\tpanic(\"contribution below minimum\")\n\t}\n\n\tkey := string(contributor)\n\tvar prev int64\n\tif v := campaign.contributions.Get(key); v != nil {\n\t\tprev = v.(int64)\n\t}\n\tif prev \u003e maxInt64-amount {\n\t\tpanic(\"contribution would overflow\")\n\t}\n\tif campaign.totalRaised \u003e maxInt64-amount {\n\t\tpanic(\"campaign total would overflow\")\n\t}\n\n\tcampaign.contributions.Set(key, prev+amount)\n\tcampaign.totalRaised += amount\n}\n\n// Withdraw pays a campaign's full raised balance to its creator, once, and\n// only once the deadline has passed with the target reached.\n//\n// totalRaised is never mutated by Withdraw or Refund: it is the permanent\n// gross-contributions figure (spec: success_basis gross_accepted_contributions),\n// fixed the moment the deadline passes, since Contribute rejects any call\n// once the deadline has passed. Withdraw and Refund are mutually exclusive\n// because they read that same fixed field with complementary conditions\n// (\u003e= target vs \u003c target), so neither action's own bookkeeping can ever\n// flip the campaign's success/failure outcome and let the other action fire\n// afterward.\nfunc Withdraw(cur realm, campaignID int64) {\n\tcaller := cur.Previous().Address()\n\tcampaign := mustGetCampaign(campaignID)\n\n\tif campaign.withdrawn {\n\t\tpanic(\"campaign funds already withdrawn\")\n\t}\n\tif caller != campaign.creator {\n\t\tpanic(\"only the campaign creator can withdraw\")\n\t}\n\tif time.Now().Unix() \u003c campaign.deadlineUnix {\n\t\tpanic(\"campaign deadline has not passed\")\n\t}\n\tif campaign.totalRaised \u003c campaign.targetUgnot {\n\t\tpanic(\"campaign did not reach its target\")\n\t}\n\n\t// Zero (flip) the withdrawn flag before transferring so a re-entrant\n\t// or repeated call cannot double-spend the creator payout.\n\tamount := campaign.totalRaised\n\tcampaign.withdrawn = true\n\n\tbanker_ := banker.NewBanker(banker.BankerTypeRealmSend, cur)\n\tpkgAddr := cur.Address()\n\tbanker_.SendCoins(pkgAddr, campaign.creator, chain.NewCoins(chain.NewCoin(denom, amount)))\n}\n\n// Refund returns a caller's own cumulative contribution, once, and only\n// once the deadline has passed with the target not reached. See Withdraw\n// for why the two are mutually exclusive. There is no address parameter:\n// entitlement comes only from the authenticated caller's own ledger entry\n// (permission: own_contribution), never from an argument a caller supplies.\nfunc Refund(cur realm, campaignID int64) {\n\tcaller := cur.Previous().Address()\n\tcampaign := mustGetCampaign(campaignID)\n\n\tkey := string(caller)\n\tv := campaign.contributions.Get(key)\n\tif v == nil {\n\t\tpanic(\"no contribution found for caller\")\n\t}\n\tamount := v.(int64)\n\tif amount \u003c= 0 {\n\t\tpanic(\"no contribution found for caller\")\n\t}\n\tif time.Now().Unix() \u003c campaign.deadlineUnix {\n\t\tpanic(\"campaign deadline has not passed\")\n\t}\n\tif campaign.totalRaised \u003e= campaign.targetUgnot {\n\t\tpanic(\"campaign succeeded; refunds are not available\")\n\t}\n\n\t// Zero this contributor's ledger entry before transferring so a\n\t// re-entrant or repeated call cannot double-spend their refund.\n\t// totalRaised is deliberately left untouched (see Withdraw).\n\tcampaign.contributions.Set(key, int64(0))\n\n\tbanker_ := banker.NewBanker(banker.BankerTypeRealmSend, cur)\n\tpkgAddr := cur.Address()\n\tbanker_.SendCoins(pkgAddr, caller, chain.NewCoins(chain.NewCoin(denom, amount)))\n}\n\n// ViewCampaign is the dedicated public read entrypoint for a single\n// campaign, matching the spec's ViewCampaign action (permission: public).\n// It is not a crossing function: it needs no caller identity and mutates\n// no state, same as Render's per-id branch, which returns the identical\n// detail view.\nfunc ViewCampaign(campaignID int64) string {\n\treturn mustGetCampaign(campaignID).String()\n}\n\n// AddUpdate appends a bounded, creator-only progress note to a campaign\n// (permission: campaign_creator, matching Withdraw). Updates are\n// append-only: there is no edit or delete entrypoint, and AddUpdate never\n// touches target, deadline, creator or refund/withdraw eligibility, per the\n// spec's updates.changes_financial_terms = false.\nfunc AddUpdate(cur realm, campaignID int64, body string) {\n\tcaller := cur.Previous().Address()\n\tcampaign := mustGetCampaign(campaignID)\n\n\tif caller != campaign.creator {\n\t\tpanic(\"only the campaign creator can add updates\")\n\t}\n\tif len(body) == 0 {\n\t\tpanic(\"update body must not be empty\")\n\t}\n\tif len(body) \u003e maxUpdateBytes {\n\t\tpanic(\"update exceeds max size\")\n\t}\n\tif len(campaign.updates) \u003e= maxUpdatesPerCampaign {\n\t\tpanic(\"campaign has reached its update limit\")\n\t}\n\n\tcampaign.updates = append(campaign.updates, body)\n}\n\n// Render returns a campaign list at the root path, and a single campaign's\n// detail view at a sub-path holding its id.\nfunc Render(path string) string {\n\tif path == \"\" {\n\t\treturn renderHome()\n\t}\n\tid, err := strconv.ParseInt(path, 10, 64)\n\tif err != nil {\n\t\treturn \"404\"\n\t}\n\tv := campaigns.Get(campaignKey(id))\n\tif v == nil {\n\t\treturn \"404\"\n\t}\n\treturn v.(*Campaign).String()\n}\n\n// renderHome lists the pageSize most recently created campaigns, newest\n// first. ReverseIterateByOffset walks the avl.Tree largest-key-first, so an\n// offset of 0 is what actually means \"start from the newest\": a nonzero\n// offset here would skip that many of the newest entries instead, which is\n// never what a \"recent campaigns\" listing wants.\nfunc renderHome() string {\n\toutput := \"# Community Crowdfunding\" + \"\\n\\n\"\n\tsize := campaigns.Size()\n\tif size == 0 {\n\t\treturn output + \"No campaigns yet.\\n\"\n\t}\n\tcampaigns.ReverseIterateByOffset(0, pageSize, func(key string, value any) bool {\n\t\tc := value.(*Campaign)\n\t\toutput += ufmt.Sprintf(\"- %d: %s - target %dugnot raised %dugnot - %s\\n\",\n\t\t\tc.id, c.title, c.targetUgnot, c.totalRaised, c.status())\n\t\treturn false\n\t})\n\treturn output\n}\n\nfunc mustGetCampaign(campaignID int64) *Campaign {\n\tv := campaigns.Get(campaignKey(campaignID))\n\tif v == nil {\n\t\tpanic(\"unknown campaign id\")\n\t}\n\treturn v.(*Campaign)\n}\n"},{"name":"crowdfunding_test.gno","body":"package crowdfunding\n\nimport (\n\t\"chain\"\n\t\"strings\"\n\t\"testing\"\n\t\"time\"\n\n\t\"gno.land/p/nt/avl/v0\"\n\t\"gno.land/p/nt/testutils/v0\"\n\t\"gno.land/p/nt/ufmt/v0\"\n\t\"gno.land/p/nt/uassert/v0\"\n)\n\nconst modulePath = \"gno.land/r/g142p4mjutq0dhu3vlfz32ymc490gk9d2sqglrak/gate1v2j/crowdfunding\"\n\nfunc resetTestState() {\n\tcampaigns = avl.Tree{}\n\tnextCampaignID = 0\n}\n\n// contribute is a test helper: it switches to contributor, declares an\n// origin send of amount ugnot, credits the realm's bank balance so a later\n// Withdraw/Refund SendCoins has funds to move, and calls Contribute.\nfunc contribute(cur realm, t *testing.T, contributor address, campaignID int64, amount int64) {\n\tpkgAddr := chain.PackageAddress(modulePath)\n\ttesting.SetRealm(testing.NewUserRealm(contributor))\n\ttesting.SetOriginSend(chain.Coins{{denom, amount}})\n\ttesting.IssueCoins(pkgAddr, chain.Coins{{denom, amount}})\n\tContribute(cross(cur), campaignID)\n}\n\nfunc TestCreateCampaign_Success(cur realm, t *testing.T) {\n\tdefer resetTestState()\n\n\tcreator := testutils.TestAddress(\"creator1\")\n\ttesting.SetRealm(testing.NewUserRealm(creator))\n\n\tdeadline := time.Now().Add(1 * time.Hour).Unix()\n\ttarget := minimumContributionUgnot * 10\n\tid := CreateCampaign(cross(cur), \"Build a well\", target, deadline)\n\tuassert.Equal(t, int64(1), id)\n\n\tc := mustGetCampaign(id)\n\tuassert.Equal(t, \"Build a well\", c.title)\n\tuassert.Equal(t, creator, c.creator)\n\tuassert.Equal(t, target, c.targetUgnot)\n\tuassert.Equal(t, int64(0), c.totalRaised)\n\tuassert.False(t, c.withdrawn)\n}\n\nfunc TestCreateCampaign_InvalidTargetRefused(cur realm, t *testing.T) {\n\tdefer resetTestState()\n\n\tcreator := testutils.TestAddress(\"creator2\")\n\ttesting.SetRealm(testing.NewUserRealm(creator))\n\tdeadline := time.Now().Add(1 * time.Hour).Unix()\n\n\tuassert.AbortsWithMessage(t, cur, \"target must be positive\", func() {\n\t\tCreateCampaign(cross(cur), \"Bad target\", 0, deadline)\n\t})\n}\n\nfunc TestCreateCampaign_PastDeadlineRefused(cur realm, t *testing.T) {\n\tdefer resetTestState()\n\n\tcreator := testutils.TestAddress(\"creator3\")\n\ttesting.SetRealm(testing.NewUserRealm(creator))\n\tpastDeadline := time.Now().Add(-1 * time.Hour).Unix()\n\n\tuassert.AbortsWithMessage(t, cur, \"deadline must be in the future\", func() {\n\t\tCreateCampaign(cross(cur), \"Too late\", minimumContributionUgnot*10, pastDeadline)\n\t})\n}\n\nfunc TestContribute_BelowMinimumRefused(cur realm, t *testing.T) {\n\tdefer resetTestState()\n\n\tcreator := testutils.TestAddress(\"creator4\")\n\tcontributor := testutils.TestAddress(\"contributor4\")\n\ttesting.SetRealm(testing.NewUserRealm(creator))\n\tdeadline := time.Now().Add(1 * time.Hour).Unix()\n\tid := CreateCampaign(cross(cur), \"Below minimum\", minimumContributionUgnot*10, deadline)\n\n\tpkgAddr := chain.PackageAddress(modulePath)\n\tbelowMinimum := minimumContributionUgnot - 1\n\ttesting.SetRealm(testing.NewUserRealm(contributor))\n\ttesting.SetOriginSend(chain.Coins{{denom, belowMinimum}})\n\ttesting.IssueCoins(pkgAddr, chain.Coins{{denom, belowMinimum}})\n\tuassert.AbortsWithMessage(t, cur, \"contribution below minimum\", func() {\n\t\tContribute(cross(cur), id)\n\t})\n}\n\nfunc TestContribute_ExactMinimumAccepted(cur realm, t *testing.T) {\n\tdefer resetTestState()\n\n\tcreator := testutils.TestAddress(\"creator5\")\n\tcontributor := testutils.TestAddress(\"contributor5\")\n\ttesting.SetRealm(testing.NewUserRealm(creator))\n\tdeadline := time.Now().Add(1 * time.Hour).Unix()\n\tid := CreateCampaign(cross(cur), \"Exact minimum\", minimumContributionUgnot*10, deadline)\n\n\tcontribute(cur, t, contributor, id, minimumContributionUgnot)\n\n\tc := mustGetCampaign(id)\n\tuassert.Equal(t, minimumContributionUgnot, c.totalRaised)\n}\n\nfunc TestContribute_WrongDenomRefused(cur realm, t *testing.T) {\n\tdefer resetTestState()\n\n\tcreator := testutils.TestAddress(\"creator6\")\n\tcontributor := testutils.TestAddress(\"contributor6\")\n\ttesting.SetRealm(testing.NewUserRealm(creator))\n\tdeadline := time.Now().Add(1 * time.Hour).Unix()\n\tid := CreateCampaign(cross(cur), \"Denom test\", minimumContributionUgnot*10, deadline)\n\n\tpkgAddr := chain.PackageAddress(modulePath)\n\ttesting.SetRealm(testing.NewUserRealm(contributor))\n\ttesting.SetOriginSend(chain.Coins{{\"otherdenom\", minimumContributionUgnot}})\n\ttesting.IssueCoins(pkgAddr, chain.Coins{{\"otherdenom\", minimumContributionUgnot}})\n\tuassert.AbortsWithMessage(t, cur, \"only ugnot contributions are accepted\", func() {\n\t\tContribute(cross(cur), id)\n\t})\n}\n\nfunc TestContribute_AfterDeadlineRefused(cur realm, t *testing.T) {\n\tdefer resetTestState()\n\n\tcreator := testutils.TestAddress(\"creator7\")\n\tcontributor := testutils.TestAddress(\"contributor7\")\n\ttesting.SetRealm(testing.NewUserRealm(creator))\n\tdeadline := time.Now().Add(1 * time.Hour).Unix()\n\tid := CreateCampaign(cross(cur), \"Expiring\", minimumContributionUgnot*10, deadline)\n\n\tc := mustGetCampaign(id)\n\tc.deadlineUnix = time.Now().Add(-1 * time.Hour).Unix() // simulate deadline passed\n\n\tpkgAddr := chain.PackageAddress(modulePath)\n\ttesting.SetRealm(testing.NewUserRealm(contributor))\n\ttesting.SetOriginSend(chain.Coins{{denom, minimumContributionUgnot}})\n\ttesting.IssueCoins(pkgAddr, chain.Coins{{denom, minimumContributionUgnot}})\n\tuassert.AbortsWithMessage(t, cur, \"campaign deadline has passed\", func() {\n\t\tContribute(cross(cur), id)\n\t})\n}\n\n// TestContribute_AtExactDeadlineRefused is the boundary case for the guard\n// `if time.Now().Unix() \u003e= campaign.deadlineUnix`: a contribution landing on\n// the deadline second itself must be refused, not just one landing\n// comfortably after it. Weakening `\u003e=` to `\u003e` would accept this exact-second\n// contribution while still refusing a later one, so a test that only\n// exercises \"well past the deadline\" (TestContribute_AfterDeadlineRefused,\n// above) cannot catch that regression.\nfunc TestContribute_AtExactDeadlineRefused(cur realm, t *testing.T) {\n\tdefer resetTestState()\n\n\tcreator := testutils.TestAddress(\"creator20\")\n\tcontributor := testutils.TestAddress(\"contributor20\")\n\ttesting.SetRealm(testing.NewUserRealm(creator))\n\tdeadline := time.Now().Add(1 * time.Hour).Unix()\n\tid := CreateCampaign(cross(cur), \"Exact deadline\", minimumContributionUgnot*10, deadline)\n\n\tc := mustGetCampaign(id)\n\tc.deadlineUnix = time.Now().Unix() // the boundary itself, not a past deadline\n\n\tpkgAddr := chain.PackageAddress(modulePath)\n\ttesting.SetRealm(testing.NewUserRealm(contributor))\n\ttesting.SetOriginSend(chain.Coins{{denom, minimumContributionUgnot}})\n\ttesting.IssueCoins(pkgAddr, chain.Coins{{denom, minimumContributionUgnot}})\n\tuassert.AbortsWithMessage(t, cur, \"campaign deadline has passed\", func() {\n\t\tContribute(cross(cur), id)\n\t})\n}\n\n// TestContribute_ContributorOverflowRefused seeds this contributor's running\n// total directly to one short of maxInt64 (no real sequence of contributions\n// could reach it) so the next contribution overflows only the\n// per-contributor guard (`prev \u003e maxInt64-amount`), not the campaign-total\n// guard: totalRaised is left at its zero value.\nfunc TestContribute_ContributorOverflowRefused(cur realm, t *testing.T) {\n\tdefer resetTestState()\n\n\tcreator := testutils.TestAddress(\"creator21\")\n\tcontributor := testutils.TestAddress(\"contributor21\")\n\ttesting.SetRealm(testing.NewUserRealm(creator))\n\tdeadline := time.Now().Add(1 * time.Hour).Unix()\n\tid := CreateCampaign(cross(cur), \"Overflow contributor\", minimumContributionUgnot*10, deadline)\n\n\tc := mustGetCampaign(id)\n\tc.contributions.Set(string(contributor), maxInt64-minimumContributionUgnot)\n\n\tpkgAddr := chain.PackageAddress(modulePath)\n\tamount := minimumContributionUgnot + 1\n\ttesting.SetRealm(testing.NewUserRealm(contributor))\n\ttesting.SetOriginSend(chain.Coins{{denom, amount}})\n\ttesting.IssueCoins(pkgAddr, chain.Coins{{denom, amount}})\n\tuassert.AbortsWithMessage(t, cur, \"contribution would overflow\", func() {\n\t\tContribute(cross(cur), id)\n\t})\n}\n\n// TestContribute_CampaignTotalOverflowRefused seeds the campaign's total\n// directly to one short of maxInt64 (no real sequence of contributions could\n// reach it) so the next contribution overflows only the campaign-total guard\n// (`campaign.totalRaised \u003e maxInt64-amount`), not the per-contributor guard:\n// this contributor's own running total is still zero.\nfunc TestContribute_CampaignTotalOverflowRefused(cur realm, t *testing.T) {\n\tdefer resetTestState()\n\n\tcreator := testutils.TestAddress(\"creator22\")\n\tcontributor := testutils.TestAddress(\"contributor22\")\n\ttesting.SetRealm(testing.NewUserRealm(creator))\n\tdeadline := time.Now().Add(1 * time.Hour).Unix()\n\tid := CreateCampaign(cross(cur), \"Overflow campaign total\", minimumContributionUgnot*10, deadline)\n\n\tc := mustGetCampaign(id)\n\tc.totalRaised = maxInt64 - minimumContributionUgnot // one short of overflowing\n\n\tpkgAddr := chain.PackageAddress(modulePath)\n\tamount := minimumContributionUgnot + 1\n\ttesting.SetRealm(testing.NewUserRealm(contributor))\n\ttesting.SetOriginSend(chain.Coins{{denom, amount}})\n\ttesting.IssueCoins(pkgAddr, chain.Coins{{denom, amount}})\n\tuassert.AbortsWithMessage(t, cur, \"campaign total would overflow\", func() {\n\t\tContribute(cross(cur), id)\n\t})\n}\n\n// TestContribute_CodeRealmCallerRefused exercises Contribute's very first\n// guard, `if !cur.Previous().IsUserCall() { panic(...) }` -- the guard the\n// source's own comment calls \"the load-bearing line of the whole realm\",\n// because it is the only thing standing between unsafe.OriginSend() and a\n// caller that can attach -send to a transaction without a matching deposit\n// ever reaching this realm. testing.NewCodeRealm constructs a caller whose\n// immediate frame is a code realm rather than a genuine user account, which\n// is exactly the case IsUserCall() must reject; the preceding\n// testing.NewUserRealm call gives that code realm a real user frame beneath\n// it, mirroring how gnovm's own zrealm_crossrealm13.gno test builds a\n// non-user-call frame. The realm path below is a deliberately fictional test\n// fixture (not a real deployed realm) so a reader can't mistake it for an\n// actual dependency of this contract.\nfunc TestContribute_CodeRealmCallerRefused(cur realm, t *testing.T) {\n\tdefer resetTestState()\n\n\tcreator := testutils.TestAddress(\"creator23\")\n\ttesting.SetRealm(testing.NewUserRealm(creator))\n\tdeadline := time.Now().Add(1 * time.Hour).Unix()\n\tid := CreateCampaign(cross(cur), \"Code realm caller\", minimumContributionUgnot*10, deadline)\n\n\tfakeCallerAccount := testutils.TestAddress(\"contributor23\")\n\ttesting.SetRealm(testing.NewUserRealm(fakeCallerAccount))\n\ttesting.SetRealm(testing.NewCodeRealm(\"gno.land/r/gnobuilder/test_fixtures/fake_code_realm\"))\n\tuassert.AbortsWithMessage(t, cur, \"only user-call (maketx call) accepted\", func() {\n\t\tContribute(cross(cur), id)\n\t})\n}\n\nfunc TestSuccessfulCampaign_CreatorWithdraws(cur realm, t *testing.T) {\n\tdefer resetTestState()\n\n\tcreator := testutils.TestAddress(\"creator8\")\n\tcontributor := testutils.TestAddress(\"contributor8\")\n\ttesting.SetRealm(testing.NewUserRealm(creator))\n\tdeadline := time.Now().Add(1 * time.Hour).Unix()\n\ttarget := minimumContributionUgnot * 10\n\tid := CreateCampaign(cross(cur), \"Fund the thing\", target, deadline)\n\n\traised := minimumContributionUgnot * 15\n\tcontribute(cur, t, contributor, id, raised)\n\n\tc := mustGetCampaign(id)\n\tuassert.Equal(t, raised, c.totalRaised)\n\tc.deadlineUnix = time.Now().Add(-1 * time.Hour).Unix() // simulate deadline passed\n\n\ttesting.SetRealm(testing.NewUserRealm(creator))\n\tWithdraw(cross(cur), id)\n\n\tuassert.True(t, c.withdrawn)\n\tuassert.Equal(t, raised, c.totalRaised) // gross-raised figure stays fixed, only withdrawn flips\n}\n\nfunc TestWithdraw_DoubleWithdrawRefused(cur realm, t *testing.T) {\n\tdefer resetTestState()\n\n\tcreator := testutils.TestAddress(\"creator9\")\n\tcontributor := testutils.TestAddress(\"contributor9\")\n\ttesting.SetRealm(testing.NewUserRealm(creator))\n\tdeadline := time.Now().Add(1 * time.Hour).Unix()\n\ttarget := minimumContributionUgnot * 10\n\tid := CreateCampaign(cross(cur), \"Double withdraw\", target, deadline)\n\n\tcontribute(cur, t, contributor, id, target)\n\n\tc := mustGetCampaign(id)\n\tc.deadlineUnix = time.Now().Add(-1 * time.Hour).Unix()\n\n\ttesting.SetRealm(testing.NewUserRealm(creator))\n\tWithdraw(cross(cur), id)\n\tuassert.AbortsWithMessage(t, cur, \"campaign funds already withdrawn\", func() {\n\t\tWithdraw(cross(cur), id)\n\t})\n}\n\nfunc TestWithdraw_NonCreatorRefused(cur realm, t *testing.T) {\n\tdefer resetTestState()\n\n\tcreator := testutils.TestAddress(\"creator10\")\n\tcontributor := testutils.TestAddress(\"contributor10\")\n\tstranger := testutils.TestAddress(\"stranger10\")\n\ttesting.SetRealm(testing.NewUserRealm(creator))\n\tdeadline := time.Now().Add(1 * time.Hour).Unix()\n\ttarget := minimumContributionUgnot * 10\n\tid := CreateCampaign(cross(cur), \"Not yours\", target, deadline)\n\n\tcontribute(cur, t, contributor, id, target)\n\n\tc := mustGetCampaign(id)\n\tc.deadlineUnix = time.Now().Add(-1 * time.Hour).Unix()\n\n\ttesting.SetRealm(testing.NewUserRealm(stranger))\n\tuassert.AbortsWithMessage(t, cur, \"only the campaign creator can withdraw\", func() {\n\t\tWithdraw(cross(cur), id)\n\t})\n}\n\nfunc TestWithdraw_BeforeDeadlineRefused(cur realm, t *testing.T) {\n\tdefer resetTestState()\n\n\tcreator := testutils.TestAddress(\"creator11\")\n\tcontributor := testutils.TestAddress(\"contributor11\")\n\ttesting.SetRealm(testing.NewUserRealm(creator))\n\tdeadline := time.Now().Add(1 * time.Hour).Unix()\n\ttarget := minimumContributionUgnot * 10\n\tid := CreateCampaign(cross(cur), \"Too early\", target, deadline)\n\n\tcontribute(cur, t, contributor, id, target)\n\n\ttesting.SetRealm(testing.NewUserRealm(creator))\n\tuassert.AbortsWithMessage(t, cur, \"campaign deadline has not passed\", func() {\n\t\tWithdraw(cross(cur), id)\n\t})\n}\n\nfunc TestWithdraw_TargetNotMetRefused(cur realm, t *testing.T) {\n\tdefer resetTestState()\n\n\tcreator := testutils.TestAddress(\"creator12\")\n\tcontributor := testutils.TestAddress(\"contributor12\")\n\ttesting.SetRealm(testing.NewUserRealm(creator))\n\tdeadline := time.Now().Add(1 * time.Hour).Unix()\n\ttarget := minimumContributionUgnot * 10\n\tid := CreateCampaign(cross(cur), \"Underfunded\", target, deadline)\n\n\tcontribute(cur, t, contributor, id, minimumContributionUgnot*2)\n\n\tc := mustGetCampaign(id)\n\tc.deadlineUnix = time.Now().Add(-1 * time.Hour).Unix()\n\n\ttesting.SetRealm(testing.NewUserRealm(creator))\n\tuassert.AbortsWithMessage(t, cur, \"campaign did not reach its target\", func() {\n\t\tWithdraw(cross(cur), id)\n\t})\n}\n\nfunc TestFailedCampaign_ContributorRefunded(cur realm, t *testing.T) {\n\tdefer resetTestState()\n\n\tcreator := testutils.TestAddress(\"creator13\")\n\tcontributor := testutils.TestAddress(\"contributor13\")\n\ttesting.SetRealm(testing.NewUserRealm(creator))\n\tdeadline := time.Now().Add(1 * time.Hour).Unix()\n\ttarget := minimumContributionUgnot * 10\n\tid := CreateCampaign(cross(cur), \"Will fail\", target, deadline)\n\n\tshortfall := minimumContributionUgnot * 3\n\tcontribute(cur, t, contributor, id, shortfall)\n\n\tc := mustGetCampaign(id)\n\tc.deadlineUnix = time.Now().Add(-1 * time.Hour).Unix()\n\n\ttesting.SetRealm(testing.NewUserRealm(contributor))\n\tRefund(cross(cur), id)\n\n\tuassert.Equal(t, shortfall, c.totalRaised) // gross-raised figure stays fixed\n\tuassert.Equal(t, int64(0), c.contributions.Get(string(contributor)).(int64))\n}\n\nfunc TestRefund_DoubleRefundRefused(cur realm, t *testing.T) {\n\tdefer resetTestState()\n\n\tcreator := testutils.TestAddress(\"creator14\")\n\tcontributor := testutils.TestAddress(\"contributor14\")\n\ttesting.SetRealm(testing.NewUserRealm(creator))\n\tdeadline := time.Now().Add(1 * time.Hour).Unix()\n\ttarget := minimumContributionUgnot * 10\n\tid := CreateCampaign(cross(cur), \"Double refund\", target, deadline)\n\n\tcontribute(cur, t, contributor, id, minimumContributionUgnot*3)\n\n\tc := mustGetCampaign(id)\n\tc.deadlineUnix = time.Now().Add(-1 * time.Hour).Unix()\n\n\ttesting.SetRealm(testing.NewUserRealm(contributor))\n\tRefund(cross(cur), id)\n\tuassert.AbortsWithMessage(t, cur, \"no contribution found for caller\", func() {\n\t\tRefund(cross(cur), id)\n\t})\n}\n\nfunc TestRefund_NonContributorRefused(cur realm, t *testing.T) {\n\tdefer resetTestState()\n\n\tcreator := testutils.TestAddress(\"creator15\")\n\tcontributor := testutils.TestAddress(\"contributor15\")\n\tstranger := testutils.TestAddress(\"stranger15\")\n\ttesting.SetRealm(testing.NewUserRealm(creator))\n\tdeadline := time.Now().Add(1 * time.Hour).Unix()\n\ttarget := minimumContributionUgnot * 10\n\tid := CreateCampaign(cross(cur), \"Stranger refund\", target, deadline)\n\n\tcontribute(cur, t, contributor, id, minimumContributionUgnot*3)\n\n\tc := mustGetCampaign(id)\n\tc.deadlineUnix = time.Now().Add(-1 * time.Hour).Unix()\n\n\ttesting.SetRealm(testing.NewUserRealm(stranger))\n\tuassert.AbortsWithMessage(t, cur, \"no contribution found for caller\", func() {\n\t\tRefund(cross(cur), id)\n\t})\n}\n\nfunc TestRefund_BeforeDeadlineRefused(cur realm, t *testing.T) {\n\tdefer resetTestState()\n\n\tcreator := testutils.TestAddress(\"creator16\")\n\tcontributor := testutils.TestAddress(\"contributor16\")\n\ttesting.SetRealm(testing.NewUserRealm(creator))\n\tdeadline := time.Now().Add(1 * time.Hour).Unix()\n\ttarget := minimumContributionUgnot * 10\n\tid := CreateCampaign(cross(cur), \"Too early refund\", target, deadline)\n\n\tcontribute(cur, t, contributor, id, minimumContributionUgnot*3)\n\n\ttesting.SetRealm(testing.NewUserRealm(contributor))\n\tuassert.AbortsWithMessage(t, cur, \"campaign deadline has not passed\", func() {\n\t\tRefund(cross(cur), id)\n\t})\n}\n\nfunc TestRefund_CampaignSucceededRefused(cur realm, t *testing.T) {\n\tdefer resetTestState()\n\n\tcreator := testutils.TestAddress(\"creator17\")\n\tcontributor := testutils.TestAddress(\"contributor17\")\n\ttesting.SetRealm(testing.NewUserRealm(creator))\n\tdeadline := time.Now().Add(1 * time.Hour).Unix()\n\ttarget := minimumContributionUgnot * 10\n\tid := CreateCampaign(cross(cur), \"Will succeed\", target, deadline)\n\n\tcontribute(cur, t, contributor, id, target)\n\n\tc := mustGetCampaign(id)\n\tc.deadlineUnix = time.Now().Add(-1 * time.Hour).Unix()\n\n\ttesting.SetRealm(testing.NewUserRealm(contributor))\n\tuassert.AbortsWithMessage(t, cur, \"campaign succeeded; refunds are not available\", func() {\n\t\tRefund(cross(cur), id)\n\t})\n}\n\nfunc TestViewCampaign(cur realm, t *testing.T) {\n\tdefer resetTestState()\n\n\tcreator := testutils.TestAddress(\"creator18\")\n\ttesting.SetRealm(testing.NewUserRealm(creator))\n\tdeadline := time.Now().Add(1 * time.Hour).Unix()\n\tid := CreateCampaign(cross(cur), \"Viewable\", minimumContributionUgnot*10, deadline)\n\n\tuassert.Equal(t, mustGetCampaign(id).String(), ViewCampaign(id))\n}\n\nfunc TestRender(cur realm, t *testing.T) {\n\tdefer resetTestState()\n\n\tcreator := testutils.TestAddress(\"creator19\")\n\tcontributor := testutils.TestAddress(\"contributor19\")\n\ttesting.SetRealm(testing.NewUserRealm(creator))\n\tdeadline := time.Now().Add(1 * time.Hour).Unix()\n\ttarget1 := minimumContributionUgnot * 10\n\ttarget2 := minimumContributionUgnot * 20\n\tid1 := CreateCampaign(cross(cur), \"Campaign One\", target1, deadline)\n\tid2 := CreateCampaign(cross(cur), \"Campaign Two\", target2, deadline)\n\n\traised1 := minimumContributionUgnot * 5\n\tcontribute(cur, t, contributor, id1, raised1)\n\n\texpected := ufmt.Sprintf(\n\t\t\"# Community Crowdfunding\"+\"\\n\\n- %d: Campaign Two - target %dugnot raised 0ugnot - active\\n- %d: Campaign One - target %dugnot raised %dugnot - active\\n\",\n\t\tid2, target2, id1, target1, raised1,\n\t)\n\tuassert.Equal(t, expected, Render(\"\"))\n\n\tc1 := mustGetCampaign(id1)\n\texpectedDetail := ufmt.Sprintf(\n\t\t\"- id: %d\\n- title: %s\\n- creator: %s\\n- target: %dugnot\\n- raised: %dugnot\\n- deadline: %s\\n- status: %s\\n- updates: %d\",\n\t\tid1, \"Campaign One\", creator, target1, raised1,\n\t\ttime.Unix(c1.deadlineUnix, 0).UTC().Format(time.RFC3339), \"active\", len(c1.updates),\n\t)\n\tuassert.Equal(t, expectedDetail, Render(\"1\"))\n\n\tuassert.Equal(t, \"404\", Render(\"999\"))\n}\n\n// TestCreateCampaign_TitleSanitizedAtCreation pins F2: a title is sanitized\n// once, at creation time, so the stored state is clean and every render\n// path (here, both the detail String() view and the home listing) is\n// covered without needing its own escaping. The hostile fragments below are\n// exactly the two failure modes the finding named: an embedded newline\n// forging a fake \"- creator: ...\" line, and unescaped markdown link syntax\n// (\"](javascript:...)\") turning into a live malicious link destination.\nfunc TestCreateCampaign_TitleSanitizedAtCreation(cur realm, t *testing.T) {\n\tdefer resetTestState()\n\n\tcreator := testutils.TestAddress(\"creator-sanitize\")\n\ttesting.SetRealm(testing.NewUserRealm(creator))\n\tdeadline := time.Now().Add(1 * time.Hour).Unix()\n\n\thostile := \"Nice project\\n- creator: g1attacker \u003cscript\u003ealert(1)\u003c/script\u003e [x](javascript:alert(1)) a|b\"\n\tid := CreateCampaign(cross(cur), hostile, minimumContributionUgnot*10, deadline)\n\n\tc := mustGetCampaign(id)\n\tforbidden := []string{\"\\n\", \"\u003c\", \"\u003e\", \"](\", \"|\"}\n\tfor _, frag := range forbidden {\n\t\tif strings.Contains(c.title, frag) {\n\t\t\tt.Errorf(\"expected stored title to be sanitized, still contains %q: %s\", frag, c.title)\n\t\t}\n\t}\n\n\trendered := Render(\"\")\n\tif strings.Contains(rendered, \"\\n- creator: g1attacker\") {\n\t\tt.Errorf(\"expected a title's embedded newline to be unable to forge a creator line in the listing, got: %s\", rendered)\n\t}\n}\n\n// TestCreateCampaign_InvalidUTF8Refused pins the other half of F2: byte\n// length alone (len(title) \u003e maxTitleBytes) does not enforce the spec's\n// UTF-8 intent, so invalid UTF-8 must be rejected explicitly instead of\n// being stored.\nfunc TestCreateCampaign_InvalidUTF8Refused(cur realm, t *testing.T) {\n\tdefer resetTestState()\n\n\tcreator := testutils.TestAddress(\"creator-badutf8\")\n\ttesting.SetRealm(testing.NewUserRealm(creator))\n\tdeadline := time.Now().Add(1 * time.Hour).Unix()\n\n\tinvalid := \"bad title \\xff\\xfe\"\n\tuassert.AbortsWithMessage(t, cur, \"title must be valid utf-8\", func() {\n\t\tCreateCampaign(cross(cur), invalid, minimumContributionUgnot*10, deadline)\n\t})\n}\n\n// TestCreateCampaign_DeadlineTooFarInFutureRefused pins F5: a deadline far\n// enough out to be an obvious milliseconds-for-seconds typo (see\n// maxDeadlineSecondsFromNow) must be refused before it can lock\n// contributions forever. The 60-second margin beyond the bound (rather than\n// exactly +1) keeps this test from flaking if a second elapses between this\n// line's clock read and CreateCampaign's own.\nfunc TestCreateCampaign_DeadlineTooFarInFutureRefused(cur realm, t *testing.T) {\n\tdefer resetTestState()\n\n\tcreator := testutils.TestAddress(\"creator-fardeadline\")\n\ttesting.SetRealm(testing.NewUserRealm(creator))\n\ttooFar := time.Now().Unix() + maxDeadlineSecondsFromNow + 60\n\n\tuassert.AbortsWithMessage(t, cur, \"deadline too far in the future\", func() {\n\t\tCreateCampaign(cross(cur), \"Millisecond typo\", minimumContributionUgnot*10, tooFar)\n\t})\n}\n\n// TestCreateCampaign_DeadlineAtUpperBoundAccepted is the boundary case for\n// the guard `deadlineUnix \u003e time.Now().Unix()+maxDeadlineSecondsFromNow`: a\n// deadline landing exactly on the bound must still be accepted, not just\n// one comfortably inside it, so a mutation weakening `\u003e` to `\u003e=` has\n// something to be caught by.\nfunc TestCreateCampaign_DeadlineAtUpperBoundAccepted(cur realm, t *testing.T) {\n\tdefer resetTestState()\n\n\t// testutils.TestAddress panics above 20 bytes, so keep these names short.\n\tcreator := testutils.TestAddress(\"creator-bounddl\")\n\ttesting.SetRealm(testing.NewUserRealm(creator))\n\tatBound := time.Now().Unix() + maxDeadlineSecondsFromNow\n\n\tid := CreateCampaign(cross(cur), \"At the boundary\", minimumContributionUgnot*10, atBound)\n\tc := mustGetCampaign(id)\n\tuassert.Equal(t, atBound, c.deadlineUnix)\n}\n\n// TestCampaignKeyOrdersNumericallyNotLexicographically pins F3's second,\n// compounding bug: unpadded decimal keys sort \"10\" before \"2\", so from the\n// tenth campaign onward, key order silently stops matching id order.\nfunc TestCampaignKeyOrdersNumericallyNotLexicographically(cur realm, t *testing.T) {\n\tif campaignKey(2) \u003e= campaignKey(10) {\n\t\tt.Errorf(\"expected campaignKey(2) \u003c campaignKey(10) (numeric-order-preserving keys), got key(2)=%q key(10)=%q\", campaignKey(2), campaignKey(10))\n\t}\n\tif len(campaignKey(1)) != campaignKeyWidth {\n\t\tt.Errorf(\"expected campaignKey to zero-pad to width %d, got %q (len %d)\", campaignKeyWidth, campaignKey(1), len(campaignKey(1)))\n\t}\n}\n\n// TestRenderHome_ShowsNewestBeyondPageSize pins F3's first bug: past\n// pageSize campaigns, the home listing must keep showing the newest ones,\n// not permanently freeze on the oldest page.\nfunc TestRenderHome_ShowsNewestBeyondPageSize(cur realm, t *testing.T) {\n\tdefer resetTestState()\n\n\tcreator := testutils.TestAddress(\"creator-pagination\")\n\ttesting.SetRealm(testing.NewUserRealm(creator))\n\tdeadline := time.Now().Add(1 * time.Hour).Unix()\n\n\ttotal := pageSize + 5\n\tvar newestID, oldestID int64\n\tfor i := 0; i \u003c total; i++ {\n\t\tid := CreateCampaign(cross(cur), ufmt.Sprintf(\"Campaign %d\", i), minimumContributionUgnot*10, deadline)\n\t\tif i == 0 {\n\t\t\toldestID = id\n\t\t}\n\t\tnewestID = id\n\t}\n\n\toutput := Render(\"\")\n\tnewest := mustGetCampaign(newestID)\n\toldest := mustGetCampaign(oldestID)\n\tif !strings.Contains(output, newest.title) {\n\t\tt.Errorf(\"expected the newest campaign %q on the home listing, got: %s\", newest.title, output)\n\t}\n\tif strings.Contains(output, oldest.title) {\n\t\tt.Errorf(\"expected the oldest campaign %q to be pushed off the first page, got: %s\", oldest.title, output)\n\t}\n}\n\n// TestContribute_TrustsDeclaredOriginSendNotIssuedCoins is F6: every other\n// test in this suite calls the `contribute` helper above, which fabricates\n// both testing.SetOriginSend and testing.IssueCoins from the SAME `amount`,\n// so the suite could never distinguish \"coins genuinely arrived\" from \"the\n// realm was merely told coins arrived.\" This test deliberately declares a\n// different, larger origin send than the coins actually issued to the\n// realm, and pins the realm's actual, current behaviour: Contribute's\n// accounting (contributions, totalRaised) is driven entirely by\n// unsafe.OriginSend(), the declared amount, with no cross-check whatsoever\n// against the realm's real coin balance. That is not a bug in Contribute\n// itself -- in production, unsafe.OriginSend() is documented to describe a\n// genuine receipt for a real user-call transaction, which is exactly what\n// the IsUserCall guard above exists to make trustworthy -- but it does mean\n// this specific mismatch is undetectable from inside Contribute, and can\n// only ever be manufactured in a test via two independent fabrications like\n// this one. Per this task's scope, Contribute's logic is not changed here;\n// this test documents the behaviour as it stands.\nfunc TestContribute_TrustsDeclaredOriginSendNotIssuedCoins(cur realm, t *testing.T) {\n\tdefer resetTestState()\n\n\tcreator := testutils.TestAddress(\"creator-mismatch\")\n\tcontributor := testutils.TestAddress(\"contributor-mismatch\")\n\ttesting.SetRealm(testing.NewUserRealm(creator))\n\tdeadline := time.Now().Add(1 * time.Hour).Unix()\n\tid := CreateCampaign(cross(cur), \"Mismatch test\", minimumContributionUgnot*10, deadline)\n\n\tdeclared := minimumContributionUgnot * 5\n\tactuallyIssued := minimumContributionUgnot * 2 // deliberately less than declared\n\n\tpkgAddr := chain.PackageAddress(modulePath)\n\ttesting.SetRealm(testing.NewUserRealm(contributor))\n\ttesting.SetOriginSend(chain.Coins{{denom, declared}})\n\ttesting.IssueCoins(pkgAddr, chain.Coins{{denom, actuallyIssued}})\n\tContribute(cross(cur), id)\n\n\tc := mustGetCampaign(id)\n\tuassert.Equal(t, declared, c.totalRaised)\n\tuassert.Equal(t, declared, c.contributions.Get(string(contributor)).(int64))\n}\n\nfunc TestAddUpdate_CreatorSucceeds(cur realm, t *testing.T) {\n\tdefer resetTestState()\n\n\tcreator := testutils.TestAddress(\"update-creator1\")\n\ttesting.SetRealm(testing.NewUserRealm(creator))\n\tdeadline := time.Now().Add(1 * time.Hour).Unix()\n\tid := CreateCampaign(cross(cur), \"Updatable\", minimumContributionUgnot*10, deadline)\n\n\tAddUpdate(cross(cur), id, \"Progress: foundations poured.\")\n\n\tc := mustGetCampaign(id)\n\tuassert.Equal(t, 1, len(c.updates))\n\tuassert.Equal(t, \"Progress: foundations poured.\", c.updates[0])\n}\n\nfunc TestAddUpdate_NonCreatorRefused(cur realm, t *testing.T) {\n\tdefer resetTestState()\n\n\tcreator := testutils.TestAddress(\"update-creator2\")\n\tstranger := testutils.TestAddress(\"update-stranger2\")\n\ttesting.SetRealm(testing.NewUserRealm(creator))\n\tdeadline := time.Now().Add(1 * time.Hour).Unix()\n\tid := CreateCampaign(cross(cur), \"Not yours to update\", minimumContributionUgnot*10, deadline)\n\n\ttesting.SetRealm(testing.NewUserRealm(stranger))\n\tuassert.AbortsWithMessage(t, cur, \"only the campaign creator can add updates\", func() {\n\t\tAddUpdate(cross(cur), id, \"I am not the creator\")\n\t})\n}\n\nfunc TestAddUpdate_OversizedRefused(cur realm, t *testing.T) {\n\tdefer resetTestState()\n\n\tcreator := testutils.TestAddress(\"update-creator3\")\n\ttesting.SetRealm(testing.NewUserRealm(creator))\n\tdeadline := time.Now().Add(1 * time.Hour).Unix()\n\tid := CreateCampaign(cross(cur), \"Oversized update\", minimumContributionUgnot*10, deadline)\n\n\toversized := make([]byte, maxUpdateBytes+1)\n\tfor i := range oversized {\n\t\toversized[i] = 'a'\n\t}\n\n\tuassert.AbortsWithMessage(t, cur, \"update exceeds max size\", func() {\n\t\tAddUpdate(cross(cur), id, string(oversized))\n\t})\n}\n\nfunc TestAddUpdate_CountLimitEnforced(cur realm, t *testing.T) {\n\tdefer resetTestState()\n\n\tcreator := testutils.TestAddress(\"update-creator4\")\n\ttesting.SetRealm(testing.NewUserRealm(creator))\n\tdeadline := time.Now().Add(1 * time.Hour).Unix()\n\tid := CreateCampaign(cross(cur), \"Update limit\", minimumContributionUgnot*10, deadline)\n\n\tfor i := 0; i \u003c maxUpdatesPerCampaign; i++ {\n\t\tAddUpdate(cross(cur), id, \"update\")\n\t}\n\n\tc := mustGetCampaign(id)\n\tuassert.Equal(t, maxUpdatesPerCampaign, len(c.updates))\n\n\tuassert.AbortsWithMessage(t, cur, \"campaign has reached its update limit\", func() {\n\t\tAddUpdate(cross(cur), id, \"one too many\")\n\t})\n}\n"},{"name":"gnomod.toml","body":"module = \"gno.land/r/g142p4mjutq0dhu3vlfz32ymc490gk9d2sqglrak/gate1v2j/crowdfunding\"\ngno = \"0.9\"\n"}],"type":{"@type":"/gno.MemPackageType","value":"MPUserAll"}},"send":"","max_deposit":""}],"fee":{"gas_wanted":"66972858","gas_fee":"66975ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"A2GOzMGavtLOZjb9g1ltPKZsySYTl1dY+wHnkIs2zwhI"},"signature":"J6JqEHjJYqFhM4o3gf4Kd4//MiROV+bt+ArWeGsVdScDPLi7uR/0yy8//djLrrLHbS9PUlWgMrC3h5m8leWuCw=="}],"memo":""},"metadata":{"timestamp":"1790120612"}}
{"tx":{"msg":[{"@type":"/vm.m_call","caller":"g13fmcl95ma9f5r9hhz0a96mwwhmdxrx73f93587","send":"","max_deposit":"","pkg_path":"gno.land/r/g142p4mjutq0dhu3vlfz32ymc490gk9d2sqglrak/gate1v2j/crowdfunding","func":"CreateCampaign","args":["Gate1 successful campaign","100000000","1790120916"]}],"fee":{"gas_wanted":"4882036","gas_fee":"4885ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"AsvB/2qgzHlBsiCGbgrBINRFk+H0Dhpwvl7YYhLVBxJ+"},"signature":"RZDBPjic8/pQIsiPBuk0jXxWpyFXZ67J4oJWrISP3sdFn2WXXmMVlWRyVXbFNfjeper9ukhlLTdb+n1TZgEomw=="}],"memo":""},"metadata":{"timestamp":"1790120618"}}
{"tx":{"msg":[{"@type":"/vm.m_call","caller":"g13fmcl95ma9f5r9hhz0a96mwwhmdxrx73f93587","send":"","max_deposit":"","pkg_path":"gno.land/r/g142p4mjutq0dhu3vlfz32ymc490gk9d2sqglrak/gate1v2j/crowdfunding","func":"CreateCampaign","args":["Gate1 failed campaign","100000000","1790120916"]}],"fee":{"gas_wanted":"5064389","gas_fee":"5067ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"AsvB/2qgzHlBsiCGbgrBINRFk+H0Dhpwvl7YYhLVBxJ+"},"signature":"cvONciorDTthe5CfAKfFwNcfC6o2NdsbZms9xXFm0X1AHo1xX0mym9jKpDBkF4nJawm5Nh2O3Ql9/GDQbcQgYw=="}],"memo":""},"metadata":{"timestamp":"1790120628"}}
{"tx":{"msg":[{"@type":"/vm.m_call","caller":"g13ru04ttcfh2apyya44fe2vghnyt9tty98zeu2j","send":"25000000ugnot","max_deposit":"","pkg_path":"gno.land/r/g142p4mjutq0dhu3vlfz32ymc490gk9d2sqglrak/gate1v2j/crowdfunding","func":"Contribute","args":["1"]}],"fee":{"gas_wanted":"5513399","gas_fee":"5516ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"AqapGZ8VqkhzpqOfiyN9OTYqRpm9AqBiBofd6y+7+VKb"},"signature":"eh7kuofV+q79Oh/1rxJM2fD8rKI0McZt5A0RLFoeT/dDCxAlOTY1fDwWF99B9TGJddZEkk5FS8dgIBiRLll0sQ=="}],"memo":""},"metadata":{"timestamp":"1790120635"}}
{"tx":{"msg":[{"@type":"/vm.m_call","caller":"g18xat6nw5r3mz6894rmfdmm95lmfly4ku24ru6t","send":"100000000ugnot","max_deposit":"","pkg_path":"gno.land/r/g142p4mjutq0dhu3vlfz32ymc490gk9d2sqglrak/gate1v2j/crowdfunding","func":"Contribute","args":["1"]}],"fee":{"gas_wanted":"5448011","gas_fee":"5451ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"A0g4HSHxvtTwqqh437cxI1N/WoDL+BXRXEUuiz8N8nnS"},"signature":"mN97sAgI1t2oKDzG+zDVB38xNfUMKsuWQ1X3Pq66PFJui23EBlOKCwsFAmqZBa50qDpFwlGAqjog+IvO6a+VXw=="}],"memo":""},"metadata":{"timestamp":"1790120641"}}
{"tx":{"msg":[{"@type":"/vm.m_call","caller":"g13ru04ttcfh2apyya44fe2vghnyt9tty98zeu2j","send":"25000000ugnot","max_deposit":"","pkg_path":"gno.land/r/g142p4mjutq0dhu3vlfz32ymc490gk9d2sqglrak/gate1v2j/crowdfunding","func":"Contribute","args":["2"]}],"fee":{"gas_wanted":"5194765","gas_fee":"5197ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"AqapGZ8VqkhzpqOfiyN9OTYqRpm9AqBiBofd6y+7+VKb"},"signature":"CN+0HD0wtH5m29jx/iMPwEJ+sYWCtox436yWhxtBJi4snYk8w/fDMePn2lbY/fdatzNbK3WueZ16ca+GGzMq/A=="}],"memo":""},"metadata":{"timestamp":"1790120648"}}
{"tx":{"msg":[{"@type":"/bank.MsgSend","from_address":"g142p4mjutq0dhu3vlfz32ymc490gk9d2sqglrak","to_address":"g142p4mjutq0dhu3vlfz32ymc490gk9d2sqglrak","amount":"1ugnot"}],"fee":{"gas_wanted":"655308","gas_fee":"658ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"A2GOzMGavtLOZjb9g1ltPKZsySYTl1dY+wHnkIs2zwhI"},"signature":"8wkQEwJmO4XC++kCgR2VJ5Ow1YDkhs/1HRsQqcWbfuBlu+Q/iykwIqi/NgCAvL31EFD95VEj7oDiXXltp0XQ9g=="}],"memo":""},"metadata":{"timestamp":"1790120919"}}
{"tx":{"msg":[{"@type":"/vm.m_call","caller":"g13fmcl95ma9f5r9hhz0a96mwwhmdxrx73f93587","send":"","max_deposit":"","pkg_path":"gno.land/r/g142p4mjutq0dhu3vlfz32ymc490gk9d2sqglrak/gate1v2j/crowdfunding","func":"Withdraw","args":["1"]}],"fee":{"gas_wanted":"4734237","gas_fee":"4737ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"AsvB/2qgzHlBsiCGbgrBINRFk+H0Dhpwvl7YYhLVBxJ+"},"signature":"ibWhSqxHAYOpa1rr42AuYhkxCHCW2hYaEhqjxvxN7ckf8CqlTV/NZqrTXTbS/7hjnQ6y9FDsCUGnD/6/ZiMiVA=="}],"memo":""},"metadata":{"timestamp":"1790120938"}}
{"tx":{"msg":[{"@type":"/vm.m_call","caller":"g13ru04ttcfh2apyya44fe2vghnyt9tty98zeu2j","send":"","max_deposit":"","pkg_path":"gno.land/r/g142p4mjutq0dhu3vlfz32ymc490gk9d2sqglrak/gate1v2j/crowdfunding","func":"Refund","args":["2"]}],"fee":{"gas_wanted":"5219004","gas_fee":"5222ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"AqapGZ8VqkhzpqOfiyN9OTYqRpm9AqBiBofd6y+7+VKb"},"signature":"zuX5YEH5iJ6yV5W61yp+8i0IevIlQnMyt+tdu9H/k0UG647qw9brcMK41Se3RtlM47bS8NXmBXhbvjDwv9KB2Q=="}],"memo":""},"metadata":{"timestamp":"1790120951"}}
{"tx":{"msg":[{"@type":"/bank.MsgSend","from_address":"g18qhq2fl54lszhmxeyqlvxnwjzc3xpu4nnakclp","to_address":"g1znazjuaxmnqc9x48g3hwvxh5sac50q8k8g4v9d","amount":"300000000ugnot"}],"fee":{"gas_wanted":"5000000","gas_fee":"1000000ugnot"},"signatures":[{"pub_key":{"@type":"/tm.PubKeySecp256k1","value":"AhUuKSwiL4XKy0BM9cK0UJFToerTzvgLTHFBvYk/xOtY"},"signature":"EQfZaoNwZ3s6jQvv8vyEkj7u4qi8G/GPAnj7t5DNoCJISq5NeCbJaZLToj9IygwlN26pl3YSd5pe7OB1jtDgeg=="}],"memo":""},"metadata":{"timestamp":"1790121006"}}
